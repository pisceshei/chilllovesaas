<h1 id="ptitle">{{ product.title }}</h1><span id="pprice">{{ product.price | money }}</span><span id="pstatus">{{ product.status }}</span>{% render 'badge' %}
{% schema %}{ "name": "Main product" }{% endschema %}
