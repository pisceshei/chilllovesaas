<span class="boxed-in">{{ block.settings.label }}</span>
{% schema %}{ "name": "Boxed", "tag": "section", "class": "boxed", "settings": [ { "id": "label", "type": "text", "default": "x" } ] }{% endschema %}
