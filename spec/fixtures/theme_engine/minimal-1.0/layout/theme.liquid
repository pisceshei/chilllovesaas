<!doctype html><html><head><title>{{ page_title }}</title>{{ content_for_header }}<meta name="brand-color" content="{{ settings.brand_color }}"></head><body data-shop="{{ shop.name }}">{% sections 'test-group' %}{% sections 'header-group' %}
{{ content_for_layout }}
{% sections 'footer-group' %}</body></html>
