<!doctype html><html><head><title>{{ page_title }}</title><link rel="canonical" href="{{ canonical_url }}">{{ content_for_header }}<meta name="brand-color" content="{{ settings.brand_color }}"></head><body data-shop="{{ shop.name }}">{%- liquid
  sections 'test-group'
-%}{% sections 'header-group' %}
{{ content_for_layout }}
{% sections 'footer-group' %}</body></html>
