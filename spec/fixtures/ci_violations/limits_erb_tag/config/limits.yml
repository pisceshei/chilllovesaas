# 故意違反：**非輸出型**的 ERB 控制流標籤（與 limits_erb 的輸出型是不同寫法）。
#
# 🔴 兩個 ERB fixture 缺一不可。補這一個之前，突變測試證實：
#    把閘門的正則從「任何開標籤」收窄成只認 limits_erb 那一個字面值，
#    回歸測試**仍然全綠**——閘門實際上已經放行了其他所有 ERB 寫法。
#
# 🔴 本註釋刻意**不寫出開標籤的字面**（2026-08-15 依 PR #40 的 Codex review 修）。
#    初版在這裡寫了正則字面，結果與 limits_erb 修掉的是**同一個 bug**：
#    把下面真正的違規行刪掉、只留註釋，checker 仍然打紅（實測 exit 1）
#    ⇒ 這份 fixture 分不出「擋控制流標籤」與「擋註釋裡的字串」。
#    **同一個自我觸發的坑，在同一個 PR 裡踩了兩次。**
# 期望：scripts/check-limits-keys.rb 打紅（ERB fail-closed）。
foo:
  bar: baz
  <% if false %>
  extra: 1
  <% end %>
