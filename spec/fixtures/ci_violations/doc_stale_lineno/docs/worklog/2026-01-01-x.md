# 故意違反：行號指到檔案結尾之外（該檔只有 3 行，引用寫 99）。
# 守的是 R3。行號是最容易腐爛的一種引用——實質結論可能仍為真，
# 但指到的位置已經不存在，照著去看的人會找不到東西。
#
# 本檔的說明刻意不含任何 R2 錨定詞。第一版踩過：說明裡寫了「否則會觸發錨定」
# 那句話本身含有錨定詞，於是這份 fixture 豁免了自己、exit 0。
詳見 `config/short.yml:99` 的那一段。
