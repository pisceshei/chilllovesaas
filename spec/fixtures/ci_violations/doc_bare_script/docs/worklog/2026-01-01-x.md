# 故意違反：**裸檔名**（不帶 scripts/ 前綴）指向不存在的腳本。
# 🔴 這一份守的是 R1 的 BARE_SCRIPT 分支——PR #39 第 3 輪那條 🔴 就是這個形態，
#    原文寫「`check-limits-keys.rb`、`check-exec-bits.sh` 都有回歸測試」，
#    四個全是裸檔名，只認帶前綴的話整類掃不到。
同批的 `check-nonexistent.sh` 也有回歸測試。
