# 83 — Admin 1:1 稽核第三輪（49 → 53 → 本檔）

> 🔴 **編號說明**：本檔原以 71 號產出於 2026-08-13 的雲端 session，但同期本機另有
> `docs/specs/71-admin-parity-sweep.md`（parity 總登記簿 R0–R14）也用了 71——**撞號**。
> 2026-08-14 合併時改編為 **83**（parity 側引用量大、不動；本檔改號）。
> 稽核序列因此是 **49 → 53 → 83**。

> **這是 `docs/specs/49`（第一輪，205 缺口）與 `docs/specs/53`（第二輪，162 缺口）之後的第三次逐按鈕機械比對。**
> 稽核日：2026-08-13（雲端 session）。對象：`docs/design/chilllove-admin-v2.html`（**11,714 行**；53 號成文時 7,299 行——之後 59/60/61/63/66/67/70 各輪落地，成長 +4,415 行，53 的行號全數失效，本檔證據行號一律為現值）。
> 方法：與 49/53 相同（grep / python re 逐項核對＋`scripts/lint-prototype.py` 全綠基線 ERROR 0 / WARN 13），**本輪未做 Chromium 實跑**（雲端沙箱無既配 playwright-python；行為級判定沿用 53 的實跑結論，僅在靜態證據足以翻案時更新）。
> 分工：六路並行稽核（訂單／產品／設定／顧客-折扣-市場-分析-內容／元件級計數／外殼橫切），本檔為六路結果的合訂本。
> 新基準：本輪首次以 **59/60（商品頁實站拆解）、67（多語言）、70（商品 CSV）** 為尺——53 成文時這些文件尚不存在。
>
> 狀態定義沿用 53：**已修復／部分修復／仍缺／新增缺口**。證據 `A:行號`（admin）、`P:`（platform）、`S:`（storefront）；查無一律寫「grep 無命中」。

---

## 0. 總表

| 塊 | 53 號判定遺留 | 本輪已修復 | 本輪仍缺 | 本輪新發現 |
|---|---|---|---|---|
| 訂單（§2） | 9 仍缺＋P0-01/17 | **2**（FO 分卡＝P0-01、草稿空車＝P0-17） | 7 | **20**（含 1 條規則回歸） |
| 產品（§3） | 3 仍缺 | **1**（庫存四欄） | 1＋1 部分 | 列表批次鏈死、CSV modal 三缺、59/60 缺口若干 |
| 設定（§4） | 1-E 10＋1-F 12＋P0 ×4 | **1-E 6、1-F 4、P0-07、R-05 全清、N-05、X-07 清** | 1-E 4、1-F 8、P0-10/11/21 | **9**（T-01～T-09，含 1 條 P0 自相矛盾＋1 條合規） |
| 顧客-市場-內容（§5） | 1-D 5＋1-G 11＋P0-02/19 | **P0-02、P0-19** | 1-D 4、1-G 10 | **1 個真 bug（segCount）**＋折扣組合缺＋捨入 DOCS 超前等 |
| 元件級（§6） | 32 元件門檻 0 過＋R×7＋N×11＋P0×6 | **R-01/02/03/05/07、N-01..05/07/11、P0-14/15/16/18** | R-04/06、N-06/08/09/10、P0-20③、F-06(S) | 死 CSS 全量 35 條、aria 三屬性掛點已備 |
| 外殼（§7） | 1-A 1＋404×2＋listbar | 0 | 全部 | palette 假資料＋DOCS 超前 5 鍵＋雙實作等 |

**53 → 本輪淨變化的關鍵事實**：53 的 10 條「未解決 P0」中 **7 條已在 53 之後的輪次解決**（P0-01/02/07/14/15/16/18/19 — 其中 07 為稅務三檔、09 結帳規則引擎亦已落地見 §4 F2），剩餘未動的是 P0-10（聯動）、P0-11（通知分組）、P0-20③（頁尾儲存鈕）、P0-21（配額分母）。**本輪最嚴重的三條新發現**：訂單退款上限被寫回「硬擋＋DB 鎖」（22 號紅字明令不得回退的形態）、分群 `segCount()` 假解析器（會顯示錯誤數字）、請款模式 UI 只渲染 3/4 顆（22 §9.1 紅字防回退標記的正反面）。

---

## 1. 53 號遺留項總結帳

### 1.1 已在 53 之後輪次修復（本輪驗證確認，不需再動）

| 項 | 證據 |
|---|---|
| P0-01 訂單依 fulfillment order 分卡 | `foSeed/foGet/foSync/foCard/foActionBar` A:2513–2778；7 status × 8 requestStatus × 12 supportedActions；cancel 產生替代單、split 產生剩餘單 |
| P0-02 市場父子繼承 | `mktDerive()` A:7224（conditions 推導＋specificity）、`mktRes/mktResAdd` A:7241/7248、三態 UI A:7257–7288、市場階層卡 A:7551 |
| P0-07 稅務三檔 | `TAX_TIERS` A:8238、`taxRegionCard` A:8286 radiogroup、`taxManualEditor` A:8273 僅 manual 檔出現 |
| P0-09 結帳規則引擎 | `CK_RULES` A:9578、`ckrPage` A:9593、`ckrEditPage` A:9658（條件→動作＋擋單計數＋validation function 出處） |
| P0-14 focus trap／還原／inert（A/P） | `withFocusTrap()` A:3653、`ftSync()` A:3632、`ftRestoreFocus()` A:3644；6 個浮層全掛；P 同構 P:1646–1663。**S 仍缺** |
| P0-15 sr-only（A/P） | `.vh` A:255／P:202；`announce()`×19、`aria-live`×5 |
| P0-16 拖曳鍵盤 | `.grip` role=button＋tabindex＋`dragKey` A:6921–7044、`aria-grabbed`×7、`#dragHelp` |
| P0-17 草稿空車付款區 | `draftTaxState()` 四態＋「未計算」A:4972–4990、`draftPaySync()` A:4998–5012 |
| P0-18 URL 路由 | hash 路由 A:2238–2360、pushState×4／replaceState×4；註釋明寫「實作換 cursor」A:2258 |
| P0-19 財務 2FA 閘門 | `financePage()` 未過閘早退、含金額字串不進 DOM A:7645–7699 |
| R-01/N-01 billingPage 覆蓋事故 | 改名 `setBillingPage` A:8362；lint `r_dup_functions` 固化 |
| R-02/N-02 --focus-ring 雙宣告 | A:245 單一宣告，`#4b92e5` 生效 |
| R-03/N-03 savebar 焦點環被壓 | A:996 以 (0,3,0) 重宣告＋原因註釋 |
| R-05/N-04 16 條假 chevron | 全清（`r_fake_affordance` lint 0 命中）；permCats 改唯讀目錄、apps 管道刪 chevron |
| N-05 events 分頁 | `evPage` A:8373 真 CRUD＋12 事件白名單 |
| R-07 openDiscModal 重複 | 僅 1 處宣告 A:7958 |
| N-07 同名函式 lint | `r_dup_functions` 已入 lint |
| N-11 48 號契約三處失真 | 48:291／452／2110／2285 全同步，舊值降為原文註釋 |
| X-07 設定區 DOCS 孤兒 | 199 鍵 0 孤兒（service_tier、地址蒐集已有對應 UI） |
| 53 §1-F F2 結帳規則／F3 地址蒐集／F4 稅務 tier | 見上；F3＝`openAddrFields` A:9776 逐國三態 |
| 53 §1-E E2 skip link／E6 events／E8 customdata／E9 apps | A:1702；A:8392；A:8355；A:8106 |

### 1.2 仍缺（53 判定維持，本輪未修）

訂單側 7 條（18 類篩選、更多動作搜尋、側欄兩卡、檢視地圖、重量拆解、時間軸分組——追蹤連結見 §2 N-13 已升級為死寫）；產品列表操作欄；RTE 下拉形態；1-E 4 條（設定搜尋框、安全性子頁、帳號導向、通知分組=P0-11）；1-F 8 條（購物車上限 modal、海關批次、額外 3 checkbox、配額分母=P0-21、出貨要求通知、運送 accordion、爬蟲存取權、取貨點設定頁）；1-D 4 條（成長橫幅、市場樹導航、圖表檢視、launches tab）；1-G 10 條（listbar ⋯、主題雙預覽、版本展開、AI 生成框、URL 匯入、報告 13 類、雙層 banner、404×2、探索建構器）；P0-10 聯動、P0-20③ 頁尾儲存鈕；R-04、R-06；N-06/08/09/10；F-06(S)；表 4 兩列（per=8、報告 11 類）。

**裁定重分類（不再計為 UI 缺口）**：1-E E3 角色子頁（已裁定：唯讀顆粒權限目錄，角色 CRUD 屬 M5，A:8016–8020）、E5 銷售管道分頁（已裁定：管道設定屬該管道自己的 app，A:8104–8107）——兩條移入反向缺口表備查。

---

## 2. 訂單模組

### 2.1 53 遺留項現狀

| 項 | 53 | 現狀 | 證據 |
|---|---|---|---|
| 篩選 18 類分類選單 | 仍缺 | 仍缺（listbar 僅 1 個 input＋4 檢視；「已封存」分支恆空） | A:1804–1807、A:2452、A:2468 |
| FO 分卡 | 仍缺 | **已修復**（見 §1.1） | A:2513–2778 |
| 追蹤連結兩段式 | 仍缺 | **仍缺且退化為死寫**：`foFulfillOK` 寫 `fo.tracking`（A:2697），`foCard` 全文不讀——出貨後畫面看不到追蹤 | 寫 A:2697、讀 0 處 |
| 更多動作可搜尋 | 仍缺 | 仍缺（固定 6 列） | A:9265–9272 |
| 側欄其他詳細資訊／轉換詳細資訊 | 仍缺 | 仍缺 | grep 無命中 |
| 檢視地圖 | 仍缺 | 仍缺 | A:2836 |
| 運送列重量拆解 | 仍缺 | 仍缺 | A:2808 |
| 時間軸日分組＋actor＋展開 | 仍缺 | 仍缺（`tl-day` 硬字串「最近」） | A:2825、A:2852–2859 |

### 2.2 新發現（O-01～O-20；原六路報告編號 N-01～N-20，為避免與 53 §9 撞號改前綴 O）

| # | 項 | 類型 | 證據 | 修法 |
|---|---|---|---|---|
| **O-01** | 🔴 **Refund 上限寫成「硬擋＋DB 層鎖」**——22 §1b 紅字明令不得改回的形態（會擋掉「先發店內額度再原路超額退款」的合法情境）。UI help 與 DOCS 雙錯 | 規則回歸 | A:9291（UI help）、A:3478（DOCS） | `orderRefund()`：軟上限＝netPayment，超額→caution banner＋`orders.over_refund` 權限 chip＋二次確認；DOCS 同步改寫並加防回退註釋（出處 46c:223/241） |
| **O-02** | Refund 數量 0 品項不退 guard 缺（Σqty=0 且金額 0 仍可送出） | 缺 guard | A:9285–9293 | 主鈕 `disabled aria-disabled`（照 `ckDirty()` 先例） |
| **O-03** | Refund 禮品卡優先扣抵＋多付款方式分配缺 | 完全缺 | grep 無命中 | 退款去向分配表：先填滿禮品卡列（唯讀），餘額落其他方式 |
| **O-04** | Refund 不可撤銷無二次確認 | 缺 guard | A:9293 | `confirmDanger('退款一經發起無法撤銷…退款後不能再建退貨')` |
| **O-05** | restock 勾選寫死「回補到主倉」，不依 FO 地點、不依 track 開關 | 假控件 | A:9290 | 讀該行 FO `assignedLocation`；`ptrack=false` 不顯示 |
| **O-06** | Edit 訂單整條缺（九條 guard／CalculatedOrder／session TTL 全無；DOCS 只列 4 條 guard 且漏「已出貨品項的品項層折扣仍可管理」反直覺規則） | 佔位＋DOCS 落後 | A:2795 toast、A:3479 | 先補 DOCS 九條聯集；guard 命中時編輯鈕 disabled＋tooltip |
| **O-07** | Cancel 原因 enum 5 值（22 要 6 值，缺員工錯誤） | 值域錯 | A:9274 | 補第 6 值，改 `[code,label]` 二元組 |
| **O-08** | Cancel 五條 guard 全無（已取消單仍可再取消） | 缺 guard | A:9271、A:2796 | `orderMore()` 依 `o.cancelled`／pending／live FO 判 disabled＋理由 |
| **O-09** | Cancel 缺退款方式三選一／staffNote ≤255／非同步進度 | 完全缺 | A:9273–9280 | modal 補 SEL＋TXT(max:255)＋job 進度提示 |
| **O-10** | Return 建立流程整條缺（DOCS 寫滿、UI 全空；`AWAITING_RETURN_ITEMS` hold reason 已存在可接） | DOCS 超前 | DOCS A:10128–10137；UI grep 無命中 | 新增 `orderReturn()`：CLOSED FO line item→原因→restocking fee/退運費（RRULE 建議值可覆寫）→Exchange→ON_HOLD FO |
| **O-11** | Fulfill 卡無部分出貨數量欄；MARK_AS_DELIVERED／取消出貨不在動作表；取貨點 FO 仍顯示收件地址 | 部分缺 | A:2692–2696、A:2545–2550、A:2836 | 數量 input＋deliveryMethod 含取貨時渲染門市卡 |
| **O-12** | DOCS 孤兒 `fulfill` 鍵（描述已刪除的整單 split button） | 死 DOCS | A:3481；`data-doc="fulfill"` 0 命中 | 刪或改為 fo-actions alias |
| **O-13** | `fo.tracking` 死寫（同 2.1 追蹤列） | 死碼 | 寫 A:2697、讀 0 | `foCard` 品項列前插兩段式追蹤列 |
| **O-14** | 訂單列表是全站唯一不進 URL 的清單（`URLQ.lk` 無 orders；`renderOrders`/`setView` 不呼叫 urlSync）——P0-18 的修復漏掉旗艦頁 | 一致性 | A:2462、A:2459、A:4102 | 最小版特判；完整版遷移 listPage 引擎 |
| **O-15** | 列表 5 處 toast 存根＋假分頁 | 佔位 | A:1792、1808、1813–1815、1824 | 隨 O-14 完整版一併消 |
| **O-16** | 表格 10 欄 vs 44 §2.1 實測 13 欄（缺標記／出貨期限／配送狀態／配送方式） | 完全缺 | A:1818–1821 | 隨 O-14 完整版 |
| **O-17** | 棄單挽回信預設延遲寫成 1 小時（22 §1d 裁定預設 10 小時）；缺對象二選一 | 值域錯 | A:5198 | 改預設值＋對象 radiogroup（引 `limits.abandoned_checkout.*`） |
| **O-18** | 正式訂單無 Capture／Collect payment（pending 卡只有一顆 toast 鈕） | 完全缺 | A:2785 | pending 分支補三途（可複用 `draftCollect()` A:5155） |
| **O-19** | 「匯出交易歷史」獨立按鈕 0 命中 | 完全缺 | grep 無命中 | orderMore() 加一項 |
| **O-20** | 付款卡／顧客卡／備註卡／標籤卡／五個 FO modal 缺 data-doc | 缺註釋 | A:2806–2811、2830–2847、2684–2778 | 各補 DOCS 鍵 |

---

## 3. 產品模組

### 3.1 53 遺留＋59/60 新尺結論

| 項 | 現狀 | 證據 |
|---|---|---|
| 列表操作欄（53 C-1） | 仍缺（thead 7 欄，且缺類別／管道） | A:1857、A:2947–2948 |
| RTE 工具列（53 C-2） | 部分修復：13 鍵齊但全是單鍵 toast，無 `⌄` 下拉面板（59 §3 要求段落／顏色／對齊為下拉） | A:5525 |
| 庫存四欄＋調整記錄（53 C-3） | **已修復**（含恆等式與「待入庫不在此表」文案、isNew 分支） | A:5433、5438、5379–5388 |
| ⭐ 60 §1「無變體商品不存在」 | **已正確落地**：有變體時三卡整組消失（A:5552）、三卡全檔只寫一次、`PV_IMPLICIT` 隱含變體（A:5275）、`pvRefresh()` 形態翻面守衛（A:5765–5771） | — |
| 59 A-1 pill 收合／A-2 利潤／A-4 包材／A-8 SEO 價格列／A-10 RTE 鍵數／A-11 模板眼睛 | 全落地 | A:3837–3850、5347–5358、5371–5377、5567–5572、5612–5615 |
| 建立/詳情同元件（59 §7） | 落地：單一 `productPage()`＋isNew 四差異 | A:5258、5488–5497 |
| 變體詳情頁（60） | 落地：側欄切換＋搜尋＋aria-current＋三卡共用 | A:5853–5895 |

### 3.2 新發現

| # | 項 | 證據 | 修法 |
|---|---|---|---|
| **PR-01** | 🔴 **列表批次選取整鏈是死的**：thead 全選框無 onclick、列身 checkbox 無 onchange、無 bulkbar；連帶匯出「已選取的商品」成空指向 | A:1857、A:2944、A:2966 | 掛 handler＋bulkbar（批次改狀態/標籤/系列/封存/刪除）＋「選取此頁全部」＋補類別/管道/操作三欄 |
| **PR-02** | 70 §F.2 兩個覆寫旗標只有一個（缺「覆寫既有翻譯」且不得連動）；§G 預覽三類破壞性只有 1 個數字；無 Variant ID 改選項值警告缺 | A:2981、A:2991–2997 | 加第二旗標（引 `i18n.import.overwrite_existing_default`）＋三類分計＋>20% 二次確認 |
| **PR-03** | 70 §E.2.4 匯出 modal 缺語言勾選器四行為＋寬表/長表選擇器 | A:2959–2972；grep 無命中 | 單一控制項＋勾第 4 預告、第 5 擋下；長表斷言不含 Cost per item |
| **PR-04** | 類別欄位是純 input，缺 taxonomy 樹狀 picker（258 選項） | A:5545–5551 | 樹狀 picker（59 A-6/U-3） |
| **PR-05** | 中繼欄位卡缺「檢視全部／新增定義／app 中繼欄位列」；銷售卡缺空態；Duplicate 無勾選範圍對話框；handle 改名缺「建立轉址」勾選 | A:5561–5563、5601–5606、5688、5577–5581 | `openDefModal()` A:3066 可複用；空態分支；範圍對話框；勾選框 |
| **PR-06** | 全店智慧系列 ≤5,000 未列（其他 4 個 limits 鍵已列）；禮品卡停用 confirmDanger 文案未講「永久不可復原」 | A:6081–6084、A:6408 | 補 note＋文案 |
| **PR-07** | 庫存頁匯出/匯入僅 toast（「僅 On hand 可寫＋防過期校驗欄」只在 toast 字串裡） | A:6222–6223 | 隨 CSV modal 輪補 |

**不得動**（已裁定/明示）：系列 per-source `selection` 可互轉（13 §F4 刻意背離 22）；變體矩陣/購買選項/危險區（59 §5-B、60 §9 明令不得以「Shopify 沒有」為由刪改）。產品區 46 個 data-doc 全有條目、死碼 0、DOCS 超前 0——**全站註釋紀律最好的區**。

---

## 4. 設定區

### 4.1 53 遺留項現狀（摘要；已修復項見 §1.1）

仍缺：E1 設定搜尋框（`.set-search` 仍 div）、E4 安全性子頁、E10 帳號導向、F1 購物車上限 modal、F5 海關批次入口、F6 額外 3 checkbox（僅價格含稅 1 顆）、F9 出貨要求通知、F10 運送 accordion＋預計配送日期、F11 爬蟲存取權（注意 A:8913 是 geo 豁免，非此機制）、F12 取貨點設定頁（運送頁其他配送方式只有 2 列）。
市場運送 2 列升為已修復（`MKT_SHIP` A:8146、`mktShipOpen/mktShipPage` A:8157–8181）。R-05 假 chevron 全清（殘餘 1 條形態問題：品牌色列 A:8085–8086 無控件未標 `is-ro`）。

### 4.2 P0 遺留三條（本輪重點）

| # | 現狀 | 證據 |
|---|---|---|
| P0-10 | 三態齊備、**聯動仍 0**：`acctReq` 只開合說明 banner，無 JS 鎖 `ckContact`。X-06（DOCS 寫成已實作）未解 | A:3295/3298、A:8027、DOCS A:11163/11376/11441 |
| P0-11 | 仍 15 條平鋪；**14 條可關**只鎖訂單確認——與 22 §8「只有 4 分組可關、其餘強制寄灰化+tooltip」**方向相反**（合規約束被降格） | A:8336–8341 |
| P0-21 | 只有分子；同檔語言頁有正確作法可抄（`N / M`＋超額 disabled） | A:8328 vs A:8517/8539/8599 |

### 4.3 新發現（T-01～T-09）

| # | 項 | 證據 | 級別 |
|---|---|---|---|
| **T-01** | 🔴 **請款「四模式」標題下只渲染 3 顆 radio**——缺 Plus 專屬「每次履行時自動請款」。22 §9.1 與 DOCS A:9228 都有紅字防回退標記，UI 卻正好是被防的三模式 | A:8136–8137 | **P0** |
| **T-02** | 授權效期表（Plus 30/10/7 天依卡組）無 UI，只有一句「授權期 7 天」 | A:8137 vs DOCS A:9228 | P1 |
| **T-03** | 通知「只有 4 分組可關」規則不存在且 DOCS 也沒寫全（合規） | A:8336–8341、A:9235 | **P0** |
| **T-04** | 「跨方案運費相加＋合併鍵＝費率名稱」UI 無表達；DOCS 明寫「改名不是純視覺操作」而費率名稱正是可編輯 cellin | A:8173/8181/8213 vs A:11219 | P1 |
| **T-05** | 市場繼承三類只到兩類（缺「市場本地」獨立分組） | A:7255–7261、7567–7580 | P2 |
| **T-06** | `.card-stack` 純死 CSS 三輪未動 | A:1270–1274 | P2 |
| **T-07** | `data-doc="setnav"` 無條目落 fallback；`.set-search` 無 data-doc | A:1922/1924、A:3557 | P2 |
| **T-08** | 政策頁六條「編輯」仍 toast | A:9354 | P2 |
| **T-09** | `setPage` 被重新賦值包裝（同 R-01 反模式族；行為目前正確） | A:10096–10099 | P2 流程 |

---

## 5. 顧客／折扣／成長／市場／財務／分析／內容

### 5.1 53 遺留：P0-02、P0-19 已修復（§1.1）；1-D 剩 4、1-G 剩 10（清單見 §1.2）

### 5.2 新發現

| # | 項 | 證據 | 級別 |
|---|---|---|---|
| **C-01** | 🔴 **`segCount()` 假解析器**：只認 3 個 regex，其餘一律回全部顧客數——會顯示錯誤數字（不是佔位是錯誤）；成員預覽表同錯。`segIns()` 不產生 MATCHES 骨架 | A:6553–6561、A:6592 | **P0（錯誤）** |
| **C-02** | 折扣組合「每單 ≤5 product+order 碼＋1 shipping」完全缺；2,000 萬／25 個配額僅 note；≤100 適用清單 picker 不擋 | A:6774–6779、A:6708、A:6735 | P1 |
| **C-03** | 市場幣別捨入：DOCS 兩處寫了（A:3514、A:9170），`MKT_OPTS` 無此維度——DOCS 超前。「一國僅屬一市場」僅文字級 | A:7269、A:7562、A:7203 | P1 |
| **C-04** | Dashboard 自訂完全缺；`page-analytics` 整段靜態 markup；分析頁日期切換僅 toast | A:1871–1900、A:1880 | P1 |
| **C-05** | 顧客合併只有 toast（排除清單／email 衝突規則無 UI） | A:6499、A:6431 | P1 |
| **C-06** | 報告 11 類 vs 13 類＋單選 select vs 多選 popover（三輪未動，掛兩張表） | A:7722、A:7729 | P1 |
| **C-07** | 主題庫 20 上限文字級；預覽分享連結 2 天/30 天只在 toast；選單連結欄是裸 input（help 說有 picker） | A:7822、A:7828–7838、A:7060 | P2 |
| **C-08** | 財務缺稅務卡（僅一顆連往設定的 link） | A:7688 | P2 |
| **C-09** | 67 §E.2-1 堆疊式/分頁式雙輸入模式缺（誤譯視覺防線） | grep 無命中 | P1 |
| **C-10** | DOCS 超前 2 鍵：`ck-acct-credit`／`ck-announce`（元件應在 S，DOCS 只寫在 A） | A:11093/11096 | P2 |

**亮點（已落地確認）**：Live View 完整（雙色點＋3D/2D/Streamer）；報表 1,000 列上限＋tfoot 總計；67 號多語言 admin 大面積落地（market_locales 白名單聯集＋唯讀繼承區、`urlPrefixOf`/`hreflangCodesOf` 分立、翻譯後台資源樹、非來源語言唯讀欄位、MT provider 可插拔）；檔案庫刪除警告帶引用數；元物件存取範圍四開關。

---

## 6. 元件級機械計數（第三輪）

### 6.1 全域計數（53 → 現值）

| 事實 | A | P | S |
|---|---|---|---|
| `:active` 規則 | 1 → **1** | 0 | 0 |
| `aria-busy` | 0 → **0** | 1 | 1 |
| `aria-invalid` | 0 → **0** | 0 | 0 |
| `inert` | 0 → **22** ✅ | 0 → **14** ✅ | 0 → **0** 🔴 |
| `aria-disabled` | 41 → 64 | 8 → 12 | 18 → 19 |
| `aria-sort` | 0 → **0**（A:4114 掛點已備） | 0 | 0 |
| `role=switch`/`aria-checked` | 15/15 ✅ 配平 | 4/5 | 3/9 |
| `sr-only`/`.vh` | 定義＋2 用 ✅ | 定義＋1 用 ✅ | 6 用 |
| `aria-grabbed` | 0 → **7** ✅ | 0 | 0 |
| 硬編 z-index | 0 | 0 | 9（區域堆疊 1/2） |
| `em` 斷點 | 17/0 | 4/0 | 7/0 |

**表 2 門檻結論**：32 元件仍無一跨過九態門檻，但卡點已收斂為 **aria-busy＋aria-invalid＋aria-sort＋:active 四項**（掛點全部已存在，合計 ≈16 行）。

### 6.2 遺留與新發現

| 項 | 現狀 | 證據 |
|---|---|---|
| R-04 searchbox 隱藏破口 | 仍缺（A:1394 無 settings 排除） | A:1394、A:1621–1625 |
| R-06/N-06 `.b-caution,.b-caution` | 仍缺 | A:399 |
| N-08 `chip('智慧型','ai')` 畫 ●（47 §D 要 ✦） | 仍缺 | A:3008–3009 |
| N-09 `.savebar{transition:none}` | 仍缺（判為 P2 設計取捨，登記不動） | A:1389–1390 |
| N-10 `b2bSeed()` demo-only | 仍缺 | A:10230/10253 |
| P0-20③ 頁尾儲存鈕 | 仍缺（選單編輯器 A:6913 仍雙模式並存——三輪未動的最後 P0 子項） | grep `savefoot` 無命中 |
| F-06 `.btn` font-weight | A/P 已修（`--fw-control`=500）；**S:204 仍硬寫 600** | A:362/124、P:270/97、S:204 |
| 死 CSS（動態前綴排除後） | **A 12／P 9／S 14 條**。含 `.card-stack`（A:1270/P:263 三輪 0 用）與 53 §F-01 宣稱落地的 `.cl-hairline`/`.cl-hairline-t`（三份全 0 用——F-01 的「5 個 cl-* 原語」實為 3 個） | A: 231/232/395/964/1104/1175/1191/1193/1270/1469/1675/1676；P: 173/174/194/263/269/308/396/447/456；S: 98/99/115/211/234/236/237/238/256/321/420/466/620/805 |
| per=8 vs 實測 50 | 仍缺（`const per=c.per||8` A:4161；另 per:4 A:6623、per:10 A:7732） | 權威值 44:124、59:183 |
| lint 缺口 | 建議補 `r_dup_selector`（R-06 兩輪存活）＋`r_focus_trap_coverage`（S 現況＝有 aria-modal 無 inert，53 X-02 形態）＋aria-sort/aria-invalid 掛點規則 | scripts/lint-prototype.py |

---

## 7. 外殼與橫切機制

### 7.1 53 遺留：Pulse `?ql=` 深連仍缺（但目標端 `reportQL` A:7764–7769 本輪已就位）；頂層/區段 404 仍缺（無效路由被 `urlRestore()` 靜默吞掉 A:2316–2326；`h.miss()` 誠實降級但非 404 版型 A:2333）；listbar 第二個 `⋯` 仍缺。

### 7.2 22 §0 逐列對照

| 列 | 現狀 | 關鍵缺口 |
|---|---|---|
| 全域搜尋 CTRL K | 殼層完整（trap/鍵盤/G 系列 A:3416–3453）；**結果是 12 個寫死 div、不讀 ORDERS/PRODUCTS/CUSTOMERS；純子字串大小寫敏感** | 結構化語法 0；**DOCS 超前**（A:3467 寫「支援 title:/tag:/AND/OR」） |
| saved views | 引擎有 chip/另存/URL；**欄位設定是殼**（勾選靜態、套用 toast、不寫 view state） | 改名/複製/刪除/釘選 4 動作缺；預設檢視唯讀無表達 |
| 篩選器 | 條件間 AND 成立（A:4145）；**同條件多值 OR 不成立**（單選 select、`s.f[fk]` 純量） | **DOCS 超前**（A:3473/A:9005 寫「空格=AND、逗號=OR」）；訂單主列表 0 個 filter |
| 匯出對話框 | 僅商品是真對話框（**三選一**缺「本頁」）；其餘 7 處 toast；50 筆門檻只在批次 toast（A:4209）與對話框的 MB 分檔規則**是兩條不同規則，需裁定** | A:2959–2972 |
| 匯入 CSV | **八列中最紮實**：空值 vs 缺欄語義、清空計數、翻譯 CSV 三種不變更表達＋20% 二次確認 | 「欄名區分大小寫」0 命中；逐行錯誤報告無視圖 |
| 批次編輯器 | **不存在**（兩入口 toast；`.cellin` 是即時單格輸入，語義相反） | 獨立工項（80–150 行），另立條目 |
| 時間軸留言 | compose 真插入；**☺/@ 兩鈕無 onclick（死控件）**；顧客時間軸另一份實作、發佈只 toast（**53 N-01 同型分家**） | @提及/#引用/5 分鐘窗全缺；**DOCS 超前**（A:3482/A:9110） |
| 權限可見性 | **零示範**（sidebar 無條件渲染；無切換身分示範） | 22 §0 核心判準驗證不到 |

### 7.3 首頁：任務 chip 數字寫死（違反自己 DOCS A:3470「對帳原則」）；設定指南缺「下一個任務」副行＋關閉 ✕＋收合；通知中心點擊只 toast。外殼 chrome §9 基本完成。
**DOCS 超前 5 鍵**：search／filterbar＋list-filters／columns／timeline＋cd-timeline／setup。
**a11y**：palette `role="option"` 無外層 listbox／`aria-activedescendant`。

---

## 8. 修復佇列

### 8.1 本輪修復（batch，со worklog 對應）

| batch | 內容 | 依據 |
|---|---|---|
| **A 快贏** | R-06 重複選擇器；R-04 searchbox 排除；per=8→50；S font-weight；N-08 ai pip→✦；aria-sort/busy/invalid 三屬性 | §6 |
| **B 訂單** | O-01 退款軟上限回正＋O-02/04 guard＋O-07/08/09 取消六值五 guard＋O-13 追蹤渲染＋時間軸分組 actor＋O-17 棄單 10 小時＋O-12 DOCS 孤兒 | §2 |
| **C 設定** | T-01 第四請款模式＋T-02 效期表＋T-03/P0-11 通知 12 分組合規閘門＋P0-10 聯動＋P0-21 配額分母 | §4 |
| **D 顧客市場外殼** | C-01 segCount 真解析器＋C-03 捨入維度＋Pulse 深連＋時間軸死鈕 | §5/§7 |
| **E lint＋死碼** | r_dup_selector＋r_focus_trap_coverage＋死 CSS 35 條清理（`.card-stack` 裁定：刪） | §6 |

### 8.2 後續輪佇列（依價值/工作量排序，各項附實作註釋見各節修法欄）

1. **O-10 Return 建立流程**（~90 行）——唯一「DOCS 寫滿、UI 全空」整塊；不做則 O-08「進行中退貨不可取消」永遠判不了
2. **PR-01 商品列表批次鏈＋三欄**（~85 行）——解匯出空指向
3. **O-14 完整版：訂單列表遷 listPage 引擎**（~70 行）——一次解 18 類篩選骨架/13 欄/URL/存根×5
4. **PR-02/03 CSV 匯出入 modal 補齊**（~90 行）——70 號三缺
5. **404 兩種版型**（~60 行）——一次關 4 條舊帳
6. **palette 結構化語法解析器＋資源驅動**（~70 行）——消最大 DOCS 超前
7. **C-06 報告 13 類＋多選**（~60 行）；**C-02 折扣組合上限表達**（~70 行）；**F12 取貨點設定頁**（~60 行）
8. **saved views CRUD＋多值 OR**（~55 行）；**C-04 Dashboard 自訂**（~180 行）；**批次編輯器**（獨立工項）；**RTE 三下拉**（~40 行）；**P0-20③ 頁尾儲存鈕**（~25 行）；**C-05 顧客合併 UI**；**權限可見性示範**；**O-06 Edit 訂單**（大工項，M4 射程）
9. 內容層 DOCS 超前殘餘：X-06 三處（隨 P0-10 修）、T-04（隨取貨點輪）、C-10 兩鍵（隨 S 檔輪）

### 8.3 S 檔（storefront）待辦登記
S 仍缺：focus trap/inert（lint 新規則會抓）、font-weight 600、9 條區域 z-index、死 CSS 14 條、53 §1-H 帳號藥丸/公告列、§1-I 三條。另立輪次處理。

---

*本檔由六路並行稽核合訂（2026-08-13，雲端 session）。下輪重跑時：行號以當時現值為準；方法沿 49/53；本檔 §8.2 佇列做完一項劃一項。*
