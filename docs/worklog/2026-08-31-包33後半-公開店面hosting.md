# 2026-08-31 包 33 後半：公開店面 hosting＋帶前綴路由＋頁級快取＋cart vehicle 收口

## 已完成的工作 (Done)

- **公開店面路由**（六步方案步 2；67 §F.1(b)(c) 路由紀律）：租戶 host 限定的 storefront
  區塊（robots／theme-assets／root／catch-all，只收 GET；平台 host 照舊 /admin）。
  根與無前綴路徑 ⇒ **302** 補預設前綴（保留路徑與 query；B12）；第一段長得像前綴
  （`Markets::UrlPrefix::SEGMENT` 同一來源）但查無 ⇒ **404** 不重導（前綴≡身分）。
- **`Storefront::PagesController`／`AssetsController`／`BaseController`**：匿名面
  （不繼承 admin 鏈——無 staff session／Pundit／allow_browser）；已發布主題渲染走
  `ThemeEngine::PageRenderer`（asset_base="/theme-assets"、cart_json 恆 nil——
  個人化不進頁快取，14 §F1-4）；B13＝robots 全站 Disallow＋X-Robots-Tag noindex
  （步 4 一起摘）。刪 `public/robots.txt`（靜態檔會搶答動態路由）。
- **頁級快取 `Storefront::PageCache`**（63 §D.3）：key＝[shop, theme.updated_at,
  locale, market(+stamp), currency, catalog_version, page_kind, resource_stamp,
  path, 白名單 params]；**key-based expiry 唯一**（無 delete）；只快取 200 整頁；
  volatile 頁 TTL 壓 60s（63 §D.5 兩級 staleness——drop 讀 inventory_quantity ⇒
  registers[:render_flags] ⇒ Result#volatile? ⇒ TTL，三節鏈各有格）。
- **A1 收口**：CartController 改繼承 `Storefront::BaseController`——staff 閘摘除、
  匿名買家可用（host 解析恆由 TenantResolver）；`_cl_buyer` host-only 語義自此真實；
  平台 host（無租戶）404。staff 預覽在同一租戶 host、同端點自然共用。
- **A2**：`shops.cart_item_limit`（default 50）＋`cart_item_limit_enabled`（default true）
  兩欄（migration 20260831170000）；CartWriter add 閘總件數，422 繁中文案。
- **A3**：`Storefront::CartPurgeJob`（limits `cart.purge_days`=90；分批 delete、行走
  FK cascade）＋ recurring 每日 5am。
- **限流**：Rack::Attack `storefront-cart/ip`（60/min）＋`storefront-page/ip`（300/min），
  值＝limits `storefront.rate_limits`（§25，ours 提案）；S9 canary 釘住判別式
  （中介層順序已驗：TenantResolver(5) → Rack::Attack(25)，env 鍵可見）。
- **host→shop 解析接上 `domains` 表**（步 2 工作卡明列；84 §4 值域的行為半邊）：
  TenantResolver 自訂網域分支改 domains 優先（active 列才可路由）、`shops.custom_domain`
  降為 M0 相容兜底；HostAuthorization lambda 同表覆核放行；**redirect 型 ⇒ 301 到
  primary domain 保留路徑與 query**（本尊逐字「Directs users to the primary domain」）、
  **alias 型原樣服務**（「doesn't redirect or update the address bar」）。D1／D2 格。
- **開發中抓到並同輪封閉的兩個真缺陷**：
  1. 🔴 **GREATEST 遇 NULL 回 NULL**：rollup 欄未寫入前是 NULL ⇒ resource_stamp 恆 0
     ⇒ 改價改名都換不了 key＝永遠舊頁（S6 紅）。修＝逐欄 COALESCE 以 updated_at 兜底。
  2. 🔴 **主題根內路徑逃逸**：`/theme-assets/../config/settings_data.json` 在主題根內，
     FileSource 只擋根外 ⇒ 商家設定與模板原始碼可被公開讀走（S7 紅）。
     修＝兩個 asset 端點（公開＋staff 預覽）同輪加 `..`／`\` 拒絕（同 producer 封閉）。

## 修改的檔案與核心邏輯 (Changes)

- `app/controllers/storefront/{base,pages,assets}_controller.rb`（新）；`cart_controller.rb`
  （re-parent＋摘 authorize）；`admin/storefront_preview_controller.rb`（逃逸閘）
- `app/services/storefront/page_cache.rb`（新）；`cart_writer.rb`（enforce_item_limit!）
- `app/jobs/storefront/cart_purge_job.rb`（新）；`config/recurring.yml`
- `app/liquid/theme_engine/{runtime,page_renderer,drops}.rb`（asset_base 參數／
  render_flags／Result#volatile?）
- `config/routes.rb`（storefront 區塊）；`config/initializers/rack_attack.rb`；
  `config/limits.yml`（§25 storefront.rate_limits）；`db/migrate/20260831170000`；
  `public/robots.txt`（刪）；fixture `assets/site.css`（新）
- specs：storefront_pages（S1–S9）／storefront_cart_public（R8–R10）／page_cache
  （C1–C5）／cart_purge_job／volatile_flag ×2

**突變驗證（20.2⑤／20.3）**：
| # | 突變 | 預期紅 | 結果 |
|---|---|---|---|
| MUT-A | resource_stamp 不 COALESCE（GREATEST 裸用） | S6 | 開發中實紅（紅→修→綠）✅ |
| MUT-B | asset 端點無逃逸閘 | S7 | 開發中實紅（紅→修→綠）✅ |
| MUT-C | 根重導改 301 | S1/S2 | 2 failures ✅ |
| MUT-D | 未知前綴改補預設前綴重導 | S4 | 1 failure ✅ |
| MUT-E | BaseController 摘 require_shop! | R9 | 1 failure ✅ |
| MUT-F | drops 摘 volatile register | volatile_flag | 1 failure ✅ |
| MUT-G | resolver 摘 domains 查詢 | D1/D2 | 2 failures ✅ |

20.2 適用類型：④（狀態空間：前綴形×命中×開放×發布 逐格；平台/租戶 host 兩軸）；
⑤（S9 canary 釘 fail-open；六突變）；②（preview asset 同型缺陷同輪同步）。

## 尚未完成或需注意的風險 (Pending / TODO)

- **section 級 Russian-doll 快取**（63 §D.3 第三列）未做——v1 頁級先行；Ella 讀庫存 ⇒
  商品/集合頁整頁降 60s TTL（63 §D.5 已登記的代價，做 section 級後縮小半徑）。
- **每頁 ≤15 SQL 的 system test**（14 §F1-5）與 **theme publish 預熱**（F1 坑 3）未做。
- **密碼保護頁**（14 §F6）：`shop.password_enabled` 欄未建，整條未做（登記）。
- **未知前綴 404＝head 空 body**，不是主題 404 頁（無 market 脈絡可渲染；本尊形態未取證）。
- **快取 key 以 `theme.updated_at` 兼任 template.updated_at**：前提＝DB template 覆寫
  寫入時 touch theme。🔴 主題編輯器包落地 template 寫入面時**必須**接 touch，否則編輯
  不換 key（此刻無寫入者，前提空真）。
- **cart change/update 加量不過 item_limit 閘**（上限文案掛「加入購物車」——44:378；
  本尊 change 行為未取證，登記）。
- `?sections=`／`?section_id=` fragment 請求不進頁快取（每次直渲染）；cart bundled
  sections 帶 cart_json（個人化）本來就不可快取——一致。
- 主題資產無指紋 ⇒ max-age=300 折衷；指紋化管線與 CDN（14 §F1-6）後續。
- 限流兩組數值＝ours 提案（本尊閾值不可觀測），壓測後只改 limits。
