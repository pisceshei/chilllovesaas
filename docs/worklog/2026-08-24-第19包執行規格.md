# worklog：第 19 包執行規格（事件與 outbox 轉發）

## 已完成的工作 (Done)

- 依第 18 包先例（執行規格先行、獨立 PR）寫出 `docs/plans/2026-08-24-第19包執行規格.md`，
  把總交接 §4 的全部擋路石逐一收掉：
  - **三道形狀裁定**（§4.3）：`dead` 進 STATUSES；`event_deliveries`／`processed_events`
    本包不做並把門檻寫死進 63 §L-4；`dedupe_key` 用「合併型有值、豁免與商品事件 NULL」
    ＋MySQL 唯一索引允許多列 NULL 的行為（官方 8.4 Reference「UNIQUE Index」節逐字 "This constraint does not apply to NULL values"；取證 2026-08-24，<https://dev.mysql.com/doc/refman/8.4/en/create-index.html>；並由規格 §6-2 的 spec 釘住），讓豁免筆天生不碰撞。
  - **四件基建裁定**（§4.6–4.7）：租戶模式 A 案（without_tenant 取件、逐店 with_tenant）
    ＋殘留 spec；測試骨架＝service 可直呼（`Events::Relay.drain!`）＋test.rb 明文
    `:test` adapter；觀測降級成結構化 log 並明文登記；歷史 pending 洪水＝
    「零消費者 ⇒ 照常 drain 即安全標 published」，上線前量測記 worklog。
  - **limits 先行**（§4.1）：四鍵有規格值可抄；`outbox_lock_timeout_s: 300` 規格沒給值
    ⇒ 列裁定候選 A1，不靜默發明。
  - **射程收斂**（§5）：七個真實消費者／對外 webhook／IndexNow 真 ping／兩張投遞表
    全部明文不做；DoD＝rspec＋bt3 三件 runner 觀測（§1、§7）。
- 隨規格 PR 完成總交接 §4.1 點名的文檔更正：18 §F1-1 的 `events_outbox` 字樣（1 處）
  與 63 §C 全章（5 處）改為實物表名 `event_outbox`，各附更正註；
  63 §L-4 補「第一個多消費者包先建 `event_deliveries`」門檻句。

## 修改的檔案與核心邏輯 (Changes)

| 檔 | 變更 |
|---|---|
| `docs/plans/2026-08-24-第19包執行規格.md` | 新建（9 節＋假設清單 A1–A5） |
| `docs/specs/18-spec-messaging-events-webhooks.md` | §F1-1 表名字樣更正＋更正註 |
| `docs/specs/63-product-data-flow.md` | §C 5 處表名字樣更正＋更正註＋§L-4 門檻句 |
| `docs/worklog/2026-08-24-第19包執行規格.md` | 本檔 |
| `docs/handoff/2026-08-24-第19包執行規格.md` | 四段交接 |

零代碼變更——本 PR 是純文檔（規格＋更正）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 實作 PR 的五條假設（A1 lock timeout 300s／A2 dead 同受 purge／A3 topic＝
  `inventory.adjusted`／A4 租戶 A 案／A5 歷史 pending 照常 drain）已列 PR 描述，
  使用者不推翻即生效——實作前最後看一眼 PR 有無回覆。
- `docs/specs/36-platform-ops-modules.md`（2 處）與 `39-platform-engineering-modules.md`
  （4 處）也有 `events_outbox` 字樣——**平台域規格、未被總交接點名，本輪刻意不動**
  （只修點名處）；接手平台域的包時順同型更正。
- 觀測（告警＋metric）明文降級成結構化 log——是登記過的缺口，不是做完了。
- 規格 §4.2 要求 migration 在有資料的本機庫實跑每個分支——實作 PR 的 DoD，勿略。
