# PR #64 第十輪 Claude 驗收修復

## 已完成的工作 (Done)

- 鐵律 15.2 在 push 前攔截 Claude comment `5361847317` 與 wrapper `5361850971`；判詞綁
  exact head `fd90a125d2b0f3f6ddde5f99ada8f913f49f6d18`，新增阻擋項為 🟡2、範圍外為
  ⚪3，沒有把 review job success 冒充零意見。
- 修法前沿 `git log -p` 核對第五／第六輪 20.3 表的成文 commit，並以完整 SHA 實跑：
  `7c10b6a86155cf4fe43965fb574b6473e1939d78..1041c03e8d485916fb15107158a0eb870be79efa`
  為四檔，`1041c03e8d485916fb15107158a0eb870be79efa..db555efbea08fb8528b8e8d66f50780806866cf4`
  為五檔；兩個同範圍加 `.github/workflows` pathspec 都無輸出。驗收方對類型名與可移動
  `HEAD` 的事實陳述成立，修法採歷史層相鄰更正，不覆寫原表。
- 第五／第六輪 worklog 分別追加 dated HTML correction：④改回鐵律 20.2 的「狀態空間漏分與
  競態窗口」，⑥改回「workflow 的本機假綠與字面塊事故」；原 4／5 檔快照各自綁完整
  immutable end commit，workflow 不適用由空 pathspec 與非空 canary 共同證明。
- ⚪1 新登 validator 未承重的輸入缺失／工具失敗分支；⚪2 經 GitHub CLI 官方 pinned commit
  `fadd4efb7daddd8afd8a5517a0cb5f5f39af6ada` 獨立重取後只登記 A9 引文丟情態／上下文，沒有
  擴改 external-facts；⚪3 與既有 P-8 pointer／跨行守衛條目同根，僅補本輪來源，不重複立項。
- targeted 複驗確認兩個 historical workflow pathspec 都為空、移除 pathspec 的 `diff-tree -r`
  分別列出 4／5 筆；兩個第十輪 correction 內部不含雙連字號。PR64 現行 validator 的正常集合
  為 11 份，duplicate、drop、missing-section、off-date、delete、rename 六條 mutation 全部命中
  各自拒絕原因。
- GitHub `/markdown` API 實際渲染：第五／第六輪及本檔各為 table 2／tr 13／th 5／td 29，
  91 為 table 3／tr 25／th 11／td 72；raw fence 都是 0，兩個 correction 的 `5361847317`
  均未洩漏到 HTML。
- 最後回寫前的第一輪 production-equivalent runner 已從 setup 起完整輸出
  `GATES_ALL_GREEN=29`：Rails 284 examples／0 failures、frontend 5 tests／0 failures、RuboCop
  162 檔零違規、Brakeman 0 errors／0 warnings、兩種 audit 零漏洞、prototype ERROR 0；runner
  內的 doc-claims 掃描 297 檔且 warning 0。此 bullet 是其後的 repo edit，不能拿該輪冒充最終
  閘門。另以 base `72cccfd…` 的 pre-commit doc-claims 對 R4／R5 回報 0 份／0 行，故未拿它
  充當新 worklog 證據，commit 後仍須 scoped 重跑。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 第五輪已要求 immutable diff，第六輪又要求 preserved base；本輪 `5361847317` 🟡2 仍抓到兩份 20.3 表用可移動 `HEAD` 保存歷史輸出，🟡1 另揭露適用類型被自造名稱替換 |
| 既有防線失效原因 | 只檢查表格七列外觀與當時輸出，沒有把第一欄逐字對回 20.2 類型全集，也沒有把歷史命令的兩端都要求為 immutable ref |
| 被漏掉的固定步驟 | 20.3 稽核須同時做「類型名稱集合核對」與「每個歷史輸出 ref 可重跑性核對」，不可以列數正確代替類型正確，也不可以當時 HEAD 正確代替歷史可重跑 |
| 可重跑反向複驗 | `rg` 取第五／第六輪原表與相鄰更正，正確類型須逐字命中 CLAUDE.md 20.2④／⑥；兩個完整 SHA 範圍分別列出 4／5 檔，workflow pathspec 為空且移除 pathspec 非空 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 證據／當前 head／驗收攝取不完整 | 四種 GitHub 來源全量重拉，Claude 判詞與 wrapper 依 ID／時間去重 | `5361847317` 為內容判詞，`5361850971` 只作 wrapper；Codex current 新意見仍為 `3825320726` |
| ② 生產者／消費者／終態／歷史未同步 | 兩份歷史表相鄰更正、91 §3／A.1 與本 worklog 同步 | 原表保留；A.1 新增本檔；⚪3 併既有條目 |
| ③ 易腐計數、行號、全稱句與完成性聲明 | 歷史檔數改綁完整起訖 SHA，workflow 空集合另帶非空 canary | immutable diff 分別輸出 4／5 檔；兩個 workflow pathspec 無輸出 |
| ④ 狀態空間漏分與競態窗口 | 第五／第六輪 correction 明列正確類型與當輪狀態格 | 不再以「零值／空值」替換 20.2④；缺證、skip、人工、失敗／逾時均 fail-closed |
| ⑤ fail-open、happy-path-only 與自我證明閘門 | 外部證據以 pinned 官方原文獨立重取；內部計數有空 pathspec／非空 canary | 官方短引文含 `requires that`；移除 workflow pathspec 後命令必須輸出歷史檔案 |
| ⑥ workflow 的本機假綠與字面塊事故 | 本輪不改 workflow；以完整 SHA pathspec 證明不適用，不以本機 YAML 綠外推 GitHub | 兩個歷史範圍的 `.github/workflows` diff 都無輸出；canary 各非空 |
| ⑦ Markdown 與 Windows 工具鏈假結果 | correction 用 HTML comment；表格字面直線與 GitHub render 另驗 | 最後 repo edit 後待實跑 Markdown API 與完整 Windows runner |

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-21-PR64第五輪雙驗收修復.md`：相鄰更正 20.2④／⑥類型與 4 檔
  immutable snapshot。
- `docs/worklog/2026-08-21-PR64第六輪雙驗收修復.md`：相鄰更正 20.2④／⑥類型與 5 檔
  immutable snapshot。
- `docs/specs/91-pit-register.md`：登記 ⚪1／⚪2、去重補 ⚪3 來源，A.1 補本 worklog。
- `docs/worklog/2026-08-21-PR64第十輪Claude驗收修復.md`：保存 exact-head 判詞、沿革、
  20.4／20.3 與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本段實測回寫是最後 repo edit；須從 gate 01 重跑完整 Windows production-equivalent 29 項，
   commit 後再跑 scoped doc-claims warning 0，才可按鐵律 15 重建清單與重拉留言。
2. 新 head 推送後 CI、Claude、Codex 都必須重新取得；`fd90a125…` 的結果不得外推。
3. ⚪1／⚪2／⚪3 只登記未擴修；後續 validator／evidence-format 包若處理，須重新確認射程與授權。
