# handoff 工作單位節奏與本地保存更正

## 已完成的工作 (Done)

- 依使用者 2026-08-20 裁定，恢復「按照以前的形式」做 handoff：每個可獨立驗收的工作單位、
  一輪驗收修復、正式阻塞／回滾或收工交接點，集中寫一份四段式交接；命令、查詢、讀檔、測試、
  commit、push 與等待不是各自獨立的 handoff 單位。
- 以 Git 沿革獨立查證修法：`git show d9e6a458:CLAUDE.md` 的舊制度要求每個可獨立驗收的「部分」
  提交 worklog，並在一輪工作結束前寫一份 handoff；`2ed2403d06eb50bba0f82e74fcacf44643a81bd8`
  才把它擴張成每個具名步驟與 handoff-only commit。此次以 D36 明文取代 D35 的錯誤擴張，但保留
  D35 供追溯，不改寫歷史。
- 明定新 handoff 僅保存在倉庫外本地交接目錄，不 commit、不 push、不出現在 PR；既有
  `docs/handoff/` 全部保留為歷史唯讀。tracked worklog 仍按可獨立驗收單位提交並由
  `docs/specs/91-pit-register.md` 附錄 A.1 索引。
- 完整閘門以 `config/ci.rb` 為清單，將 `bin/setup` 依其源碼展開為四支後逐支執行，最終輸出
  `GATES_TOTAL=29`、`GATES_FAILED=0`；Rails `284 examples, 0 failures`、Vitest `5 passed`、
  doc-claims 警告 0，workflow 33 個 run 區塊與 11 條 regression 均通過。首輪 Windows
  `ruby bin/ci` 的無副檔名 `bin/*` 失敗、pnpm 版本切換失敗，以及第一次 workflow 檢查選到
  WSL 而看不到 Windows Temp 的結果均已撤回；以明確 pnpm／Python 執行檔和 Git Bash PATH
  從第一支重跑後全綠，沒有修改 checker 或產品碼掩蓋環境問題。

## 修改的檔案與核心邏輯 (Changes)

- `CLAUDE.md`、`AGENTS.md`：以工作單位節奏、本地四段式交接、禁止小步驟拆分與禁止遠端 handoff
  取代逐步 tracked handoff；同步保留 worklog、證據與驗收紀律。
- `HANDOFF.md`、`docs/DECISIONS.md`：把倉庫外本地 handoff 納入接手順序，將既有
  `docs/handoff/` 降為歷史證據，新增 D36 並標記 D35 已被取代。
- `docs/dev/m0-review-convergence.md`、兩份階段方案與 `docs/specs/71-admin-parity-sweep.md`：同步
  active producer／consumer 敘述，避免後續執行者又按每個小步驟建立遠端 handoff。
- `docs/specs/91-pit-register.md`：附錄 A 只要求新增 tracked worklog 同 commit 補列；本地 handoff
  不入索引，A.2 既有 handoff 清單不刪不改。本 worklog 同步列入 A.1。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本輪修改 `CLAUDE.md` 與 `AGENTS.md` 本文，屬鐵律 18.3 人工合併範圍；commit 後
  doc-claims、push 後 exact-head CI／Codex／Claude 驗收仍須全數取得證據，雙零後也只能通知
  使用者人工合併，Codex 不得代行。
- 本輪只更正未來 handoff 節奏與保存位置；既有 tracked handoff 是歷史紀錄，未獲另外授權前
  不得刪除或批量改寫。
