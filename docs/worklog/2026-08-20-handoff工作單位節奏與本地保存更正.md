# handoff 工作單位節奏與本地保存更正

## 已完成的工作 (Done)

- 依使用者 2026-08-20 裁定，恢復「按照以前的形式」做 handoff：每個可獨立驗收的工作單位、
  一輪驗收修復、正式阻塞／回滾或收工交接點，集中寫一份四段式交接；命令、查詢、讀檔、測試、
  commit、push 與等待不是各自獨立的 handoff 單位。
- 以 Git 沿革獨立查證修法：`git show d9e6a458:CLAUDE.md` 的舊制度要求每個可獨立驗收的「部分」
  提交 worklog，並在一輪工作結束前寫一份 handoff；`2ed2403d06eb50bba0f82e74fcacf44643a81bd8`
  才把它擴張成每個具名步驟與 handoff-only commit。此次以 D36 明文取代 D35 的錯誤擴張，但保留
  D35 供追溯，不改寫歷史。
<!-- 🔴 2026-08-20 更正（Codex inline 3822393091）：上句的 `2ed2403d06eb50bba0f82e74fcacf44643a81bd8`
是 PR #61 於 2026-08-20 的 pre-squash commit，不得假設它在只含 main 歷史的新 clone 可達。
可由 PR commits API 重取：
`gh api repos/pisceshei/chilllovesaas/pulls/61/commits --paginate --jq '.[] | select(.sha=="2ed2403d06eb50bba0f82e74fcacf44643a81bd8") | {sha,html_url,commit:{message:.commit.message,author_date:.commit.author.date}}'`；
2026-08-20 實跑回傳該 SHA、commit message `Docs: require handoff for every step` 與
<https://github.com/pisceshei/chilllovesaas/commit/2ed2403d06eb50bba0f82e74fcacf44643a81bd8>。 -->
- 明定新 handoff 僅保存在倉庫外本地交接目錄，不 commit、不 push、不出現在 PR；既有
  `docs/handoff/` 全部保留為歷史唯讀。tracked worklog 仍按可獨立驗收單位提交並由
  `docs/specs/91-pit-register.md` 附錄 A.1 索引。
<!-- 🔴 2026-08-20 更正（Codex inline 3821620375）：上句「全部保留」是未附全集查法的全稱句；
本 PR 在 ref 38a9126b130ec4e65c8c1ef2312276fa7a2853a3 未修改既有 docs/handoff，複驗
`git diff --name-status 1800b20aa006ee67f6f8c88cd24e50322db99a4c...38a9126b130ec4e65c8c1ef2312276fa7a2853a3 -- docs/handoff`
輸出為空。 -->
- 完整閘門以 `config/ci.rb` 為清單，將 `bin/setup` 依其源碼展開為四支後逐支執行，最終輸出
  `GATES_TOTAL=29`、`GATES_FAILED=0`；Rails `284 examples, 0 failures`、Vitest `5 passed`、
  doc-claims 警告 0，workflow 33 個 run 區塊與 11 條 regression 均通過。首輪 Windows
  `ruby bin/ci` 的無副檔名 `bin/*` 失敗、pnpm 版本切換失敗，以及第一次 workflow 檢查選到
  WSL 而看不到 Windows Temp 的結果均已撤回；以明確 pnpm／Python 執行檔和 Git Bash PATH
  從第一支重跑後全綠，沒有修改 checker 或產品碼掩蓋環境問題。
<!-- 🔴 2026-08-20 更正（Codex inline 3821620367）：上句數字只代表 2026-08-20 在 ref
38a9126b130ec4e65c8c1ef2312276fa7a2853a3 的本機重跑快照，不是後續 HEAD 的永久常數；相同 ref
的 GitHub CI 證據為 run 32369600470，可用
`gh run view 32369600470 --repo pisceshei/chilllovesaas --json headSha,conclusion,jobs` 重取。 -->

### 2026-08-20 驗收補記（鐵律 20.3）

- **適用類型**：20.2.1（exact-head 驗收攝取）、20.2.2（D36 規則生產者與活性消費者同步）、
  20.2.3（易腐數字／全稱句）、20.2.4（舊制／新制及本地／遠端分支）、20.2.7（AGENTS 表格
  Markdown）。20.2.5／20.2.6 不適用：本 PR 沒有修改 checker、`scripts/` 或 workflow；驗收找到的
  workflow／script 延後消費者已登記 `docs/specs/91-pit-register.md` §3，依 18.3 分包。
- **固定處理**：以 `git log -p` 查出 D35 引入與 D36 取代沿革；用識別字掃規則本文、裁定、
  convergence、兩份方案與 parity SOP；必要數字綁日期／ref／重取命令；分開寫首次交付、驗收
  修復、合法 tracked 新 head、無合法產物的人工停點；以 GitHub Markdown API 渲染 AGENTS 表格。
- **反向複驗輸出**：在本修復工作樹執行
  `git grep -n -E '沒有產品碼也要交接 commit|分支、三件套|每個具名步驟及會改變流程分支|每個計畫／任務具名步驟' -- AGENTS.md CLAUDE.md HANDOFF.md docs/DECISIONS.md docs/dev/m0-review-convergence.md docs/plans/2026-08-18-總方案.md docs/plans/2026-08-20-階段一執行方案.md docs/specs/71-admin-parity-sweep.md`
  輸出為空；`git diff --name-status 1800b20aa006ee67f6f8c88cd24e50322db99a4c...38a9126b130ec4e65c8c1ef2312276fa7a2853a3 -- docs/handoff`
  亦為空。GitHub `/markdown` 渲染後，含「歷史層」的目標列為一列、該列四個 `td`，且含
  「整體唯讀」文字；API 重取以 `gh api markdown --method POST -F text=@AGENTS.md -f mode=gfm -f context=pisceshei/chilllovesaas`
  為入口。exact-head 遠端攝取則取得 Codex review `4982782311`、inline `3821620367`／
  `3821620375`／`3821620385`／`3821620393`，以及 Claude issue comment `5356127623`；未把
  initial-head 意見或 success job 外推成修復後新 head 已通過。

## 修改的檔案與核心邏輯 (Changes)

- `CLAUDE.md`、`AGENTS.md`：以工作單位節奏、本地四段式交接、禁止小步驟拆分與禁止遠端 handoff
  取代逐步 tracked handoff；同步保留 worklog、證據與驗收紀律。
- `HANDOFF.md`、`docs/DECISIONS.md`：把倉庫外本地 handoff 納入接手順序，將既有
  `docs/handoff/` 降為歷史證據，新增 D36 並標記 D35 已被取代。
- `docs/dev/m0-review-convergence.md`、兩份階段方案與 `docs/specs/71-admin-parity-sweep.md`：同步
  active producer／consumer 敘述，避免後續執行者又按每個小步驟建立遠端 handoff。
- `docs/specs/91-pit-register.md`：附錄 A 只要求新增 tracked worklog 同 commit 補列；本地 handoff
  不入索引，A.2 既有 handoff 清單不刪不改。本 worklog 同步列入 A.1。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本輪修改 `CLAUDE.md` 與 `AGENTS.md` 本文，屬鐵律 18.3 人工合併範圍；commit 後
  doc-claims、push 後 exact-head CI／Codex／Claude 驗收仍須全數取得證據，雙零後也只能通知
  使用者人工合併，Codex 不得代行。
- 本輪只更正未來 handoff 節奏與保存位置；既有 tracked handoff 是歷史紀錄，未獲另外授權前
  不得刪除或批量改寫。
