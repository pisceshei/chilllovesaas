# PR #58 第九次新 head 驗收修復

> 對應驗收：Claude R37 issue comment `5344159684`（審 `7aadf4ae`）｜本檔與 handoff 同 commit

## 已完成的工作 (Done)

- 完整拉取 R37 判詞，確認唯一阻擋項是 `docs/dev/external-facts.md` 的兩段 blockquote 之間缺空行；R37 其餘四條 Codex 修復、終態表、附錄集合與 INTERVAL 契約均判定成立。
- 以 `git log -p -L 154,165:docs/dev/external-facts.md --` 追沿革：缺口由 `3356525` 引入，`7aadf4ae` 換掉其銜接句但保留相同段落邊界，故根因是 Markdown 結構而非文字內容。
- 依鐵律 16 重查 CommonMark 官方規格。官方逐字："The Laziness clause allows us to omit the `>` before paragraph continuation text:"；來源 <https://spec.commonmark.org/current/>，取證日期 2026-08-19。
- 對未修片段呼叫 GitHub `POST /markdown`（GFM mode），實際只產生一個 `<blockquote><p>`，merge queue 引文、我方銜接句與 latest-SHA 引文全被包進同一段；補空行後實測為兩個 `<blockquote>`、中間一個獨立 `<p>`。修法沒有改任何引文或宣稱。
- R37 的四條 ⚪ 逐條核對後寫入 `docs/specs/91-pit-register.md` §3.4：⚪1 被 `2811225:HANDOFF.md:126` 與 `22d5448^:HANDOFF.md:126` 反證，既有「漂移錯錨」定性不改；⚪2–⚪4 摘寫附錄全稱句、主 worklog 歷史殘影與 MAX_POLLS 超大整數假逾時，只登記、不修範圍外實物。

## 修改的檔案與核心邏輯 (Changes)

- `docs/dev/external-facts.md`：在 merge queue 引文來源與我方銜接句之間增加唯一一個空行，切斷 lazy continuation。
- `docs/specs/91-pit-register.md`：落籍 R37 ⚪×4，並把本輪 worklog／handoff 納入附錄 A。
- `docs/worklog/2026-08-18-P0-方案落庫與鐵律16-18.md`：同步 Changes 表與第九次新 head 查證、修法及驗證紀錄。
- 本 worklog 與 `docs/handoff/2026-08-19-PR58-第九次新head驗收修復.md`：形成本輪獨立三件套記錄。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本 commit 推送後，Claude 與 Codex 都必須對新的精確 head 重驗；`7aadf4ae` 的 Claude R37 與 Codex 零問題結果不能沿用。
- R37 ⚪1 已證偽、不得在 P-1 採用；⚪2–⚪4 本輪依法只落籍，歷史層更正留給 P-1 收割，MAX_POLLS 上界留給 #60 rebase 時與五參數 deadline 一起處理。
- #60 仍須等 #58 進 main 後 rebase；合併時要同時保留 MAX_POLLS 上界驗證與 #58 的 INTERVAL 900–1500 秒契約。
