# 鐵律 21：閘門 Shell 路徑復驗

## 已完成的工作 (Done)

- 在 MySQL 回覆 `mysqld is alive` 後執行 Windows 等價 production 閘門；第一輪如實得到非零，
  沒有沿用先前 head 的綠燈，也沒有把部分通過宣稱為全綠。
- 第一輪已確認 Rails `284 examples, 0 failures`、frontend `5 passed`、Ruby style、安全稽核、
  typecheck／build 與大部分 invariants 通過；失敗集中在 Bash 消費者。
- 查明 PowerShell 的裸 `bash` 解析到 WSL `/bin/bash`，它把 Windows checkout／暫存檔路徑當成
  Linux 路徑，因此 `scripts/check-exec-bits.sh` 回 127，workflow syntax 對 Windows temp path
  報不存在。修正執行法為在同一 PowerShell process 把 `C:\Program Files\Git\bin` 放到 PATH
  最前，讓 checker 內部的 `bash -n` 也解析到 Git for Windows Bash；不修改倉庫 checker。
- smoke check 實跑顯示 `(Get-Command bash).Source` 為 `C:\Program Files\Git\bin\bash.exe`、工作目錄
  為 `/c/Users/pisce/Downloads/shopifysystem/chilllovesaas`，且目標 shell 檔存在，退出碼 0；其後
  從四條 setup 子命令開始完整重跑，得到 `PRODUCTION_GATE_COUNT=29`、
  `PRODUCTION_GATE_FAILED=0`。

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-鐵律21閘門Shell路徑復驗.md` 與同名 handoff：保存失敗退出碼、
  根因、被推翻假設與 fail-closed 重跑入口。
- `docs/specs/91-pit-register.md`：把本次環境坑及其三件套加入附錄 A；沒有把它擴修成未獲授權的
  `scripts/`／`bin/` 變更。

## 尚未完成或需注意的風險 (Pending / TODO)

- 第一輪 26/4 只保留為失敗快照；第二輪已按既有 Windows 口徑把四條 setup 子命令分別計數並
  從頭實跑。結果為 29/0，Rails `284 examples, 0 failures`、frontend `5 passed`、prototype
  `ERROR 0 / WARN 136`、doc-claims Warnings 0、workflow 33 個 run 區塊與 11 條回歸均通過。
- 本次結果文字回寫後仍須重跑 doc-claims；commit 後再跑 doc-claims，不能直接沿用回寫前輸出。
- Node 實跑版本為 v24.19.0，而專案 engine 要求 `>=22.12 <23`；pnpm 發出 warning，但本輪前端
  測試、typecheck、build 都通過。這是環境差異，不能外推為支援 Node 24。
