# PR #64 第六輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收基準固定為 exact head `1041c03e8d485916fb15107158a0eb870be79efa`。鐵律 15.2 三端點
  全量重取後，Claude comment `5360596028` 有兩條 🟡；Codex review `4986421292` 的 inline
  為 `3824556128`、`3824556135`、`3824556137`。CI quality／test 同 head 綠，Claude job
  實際產生判詞，沒有 validation skip。
- P-8 終態集合改用 squash 後由 main 保存的 base `0fbe520502588b34f9b9cad6ae9b3a282d4db643`，
  並就近定義兩側過濾規則、回指現行 fenced 複驗入口；不再把 PR 內 commit 當合併後前置。
- 第五輪 worklog 依歷史層規則保留原文、緊鄰加 dated correction；現行命令移出表格，避開
  內嵌反引號切斷 code span，守衛同時涵蓋「首輪」與「第 N 輪」。
- A9 收窄為官方手冊與 pinned 原始碼實際支持的單一 `$endCursor` 邊界；多個、不完整或交錯
  `pageInfo` 的選擇／停止規則明標未取得，不把驗收方 mock 結果升格成公開契約。
- 91 的 HTML list breakage 來源改指 §3.3 第 8 輪條目，並明確區分 §2 G-02 的 fence gate 缺口。
- 2026-08-21 pre-commit 實跑：worktree 等價集合兩側各取同一組 PR #64 驗收 worklog；插入
  `PR64首輪驗收修復` mutation 時 exit 1 且輸出 `manual round list returned`。GitHub
  `/markdown` 回應中兩張表的 `<tr>`／`<th>`／`<td>` 結構成立，現行 fenced block 是單一
  `<pre>`，同時含 preserved base 與違規守衛；本機每張表各列的未跳脫直線數一致。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | head `7c10b6a` 的 `5360279873`／`3824363605` 已點名手抄集合失真；head `1041c03` 的 `5360596028`／`3824556128` 又證明替代查法有過濾、渲染及 squash 可達性缺口 |
| 既有防線失效原因 | 只在當前 worktree 跑集合相等，沒有從合併後可保存物件、Markdown 實際載體與違規 mutation 三面驗證「可重跑」宣稱 |
| 被漏掉的固定步驟 | 鐵律 19.2 的可重跑輸出、20.2.5 的違規斷言與 20.2.7 的 Markdown 渲染複驗沒有共同套到同一條現行命令 |
| 可重跑反向複驗 | 以 `docs/worklog/2026-08-21-PR64第五輪雙驗收修復.md`「現行可重跑反向複驗」fenced block 為現行入口；正常樹集合相等，插入「PR64首輪驗收修復」mutation 必須非零 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取 | issue comments、review body、paginated inline 全量重取並綁 exact head | 上列 Claude／Codex 五條均綁 `1041c03…`，Claude 沒有 skip |
| ② 生產者與消費者同步 | 反查 P-8 終態、第五輪命令、A9、91 §3／A.1 與 PR 描述 | 本輪只更新被五條意見直接影響的消費者；新增本 worklog 同步 A.1 |
| ③ 易腐數字／完成聲明 | 基準只用 preserved full SHA；外部 mixed-token 邊界標未取得 | base 可由 main 保存；不發布 mock 特定結果或「首組完整」全稱 |
| ④ 零值／空值 | 集合任一來源缺失須 abort；外部不完整 token 不推定 | 正常、缺 A.1 區段與集合不等三路由現行命令區分 |
| ⑤ fail-open／happy path | 守衛同時涵蓋「首輪／第 N 輪」，並對首輪手抄做 mutation | 正常樹 exit 0；插入「PR64首輪驗收修復」後 exit 非零 |
| ⑥ 超大值／資源上限 | 不適用：本輪不改輪詢、數值解析或資源上限 | `git diff --name-only 1041c03..HEAD` 僅限本 worklog及四個被點名文件 |
| ⑦ Markdown／歷史層 | 舊敘事原文不改；更正用 dated comment；現行命令置於 fenced block | fence 配平、表格未跳脫直線數、doc-claims 與 GitHub Markdown render 均須通過 |

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-P8證據來源與合併後文件債收斂.md`：終態層改用 preserved base、
  精確集合定義與現行複驗入口。
- `docs/worklog/2026-08-21-PR64第五輪雙驗收修復.md`：保留歷史原文，追加穩定基準、fenced
  命令與「首輪」違規斷言更正。
- `docs/dev/external-facts.md`：移除 A9「首組完整」過強契約，標明不完整交錯邊界未取得。
- `docs/specs/91-pit-register.md`：把 list breakage 精確回指 §3.3，區分 G-02，並同步 A.1。
- `docs/worklog/2026-08-21-PR64第六輪雙驗收修復.md`：保存本輪五條輸入、20.4、20.3、
  修改射程與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 最後 repo edit 後須從 setup 起跑完整 Windows production-equivalent 29 閘門；commit 後再跑
   doc-claims、現行命令、鐵律 15 與三端點增量。
2. push 會產生新 head，舊 head 的 CI／Claude／Codex 全部失效；#64 僅在 exact-head 四條件與
   evaluator `1111` 同時成立時可代行 squash merge。
