# 2026-09-02 Ella 修復 PR-13：Liquid 全域覆蓋批（pages/images/vendors/filters 等）

## 已完成的工作 (Done)

- 6 軸診斷艦隊（Ella 522 檔全量掃描軸）剩餘覆蓋缺口批次收口，官方語義
  逐項取證（shopify.dev，2026-09-02）：
- **pages 全域**（官方 "All of the pages on a store."，by-handle）：新
  `PagesDrop`（仿 BlogsDrop）→ 修復 header/手機工具列 wishlist 連結
  （Ella toolbar-mobile:187 等 9 用/3 檔恆走 fallback 的缺口）。
- **images 全域**（官方 "All of the images that have been uploaded to a
  store."，by-filename）：新 `ImagesDrop` → StoredFile → FileImageDrop
  → 修復品牌/vendor 圖卡（20 用/6 檔全走無圖分支的缺口）。
- **shop.vendors／shop.types**（官方 array of string）：distinct pluck＋
  去空＋不分大小寫排序（原恆 []——品牌 A-Z 頁與 mega menu 空白）。
- **collection.filters ⇒ []**：與 SearchDrop 一致的顯式空集合——Ella
  facets.liquid:90 `nil != empty` 為真會渲染空容器；facets 整包仍走
  91 §3.61 排程，本條只封空容器誤入。
- **powered_by_link**：官方 HTML 形（target/rel/文案結構）對位、🔴 品牌
  與連結換我方（鐵律 9）＝ours。
- **page_image**（官方＝product/collection/article 用資源 featured image，
  其餘退 social sharing image）：PageRenderer 從 assigns 導出；social
  sharing image 無資料面 ⇒ nil（主題 `!= blank` 閘走無圖分支）。
- **unit_price_with_measurement**（官方輸出形 `$50.00/kg`、ref>1 ⇒
  `/100ml`）：吃 integer cents（鐵律 3）＋measurement Hash/Drop 寬容。
- 測試 LG1–LG6；突變 3/3 殺（拔 pages/images 全域、拔 collection.filters、
  拔 ref>1 分支）。

## 修改的檔案與核心邏輯 (Changes)

- `app/liquid/theme_engine/drops.rb`：PagesDrop／ImagesDrop 新增；
  ShopDrop#vendors/#types 實作；CollectionDrop#filters=[]。
- `app/liquid/theme_engine/runtime.rb`：global_assigns 加 pages/images/
  powered_by_link。
- `app/liquid/theme_engine/page_renderer.rb`：page_image_for 導出＋assign。
- `app/liquid/theme_engine/filters.rb`：unit_price_with_measurement 實作
  ＋dig_measurement 輔助。
- 新 spec `spec/liquid/liquid_globals_spec.rb`。

## 尚未完成或需注意的風險 (Pending / TODO)

- pages 全域未做官方「paginate 50/頁」面（Ella 只用 by-handle 形，迭代形
  未消費）；ImagesDrop 未做迭代（官方同為 by-filename 主形）。
- page_image 的 social sharing image 後備＝無資料面（V）；article
  featured image 未實作（ArticleDrop 無 image 欄）——三型中只有
  product/collection 真出圖。
- 影片/3D 媒體鏈（video_tag 四件套）＝資料模型雙層缺口，另 PR；
  facets 整包＝91 §3.61 照舊。
- 艦隊 low 級剩餘（form.errors／order／gift_card／加速結帳族）維持
  顯式 stub，各自綁功能線。
