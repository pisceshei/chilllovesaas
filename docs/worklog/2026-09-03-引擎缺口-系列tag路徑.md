# 2026-09-03 引擎缺口 PR-9：系列 tag 路徑 `/collections/{handle}/{tag1+tag2}`＋系列語境商品 URL

分支 `engine/collection-tags-9`（自 PR-8 分支長出，PR-8 合併後 rebase 到 main）。配對 handoff：
`docs/handoff/2026-09-03-引擎缺口-系列tag路徑.md`。收口 PR-6 登記的「link_to_*tag 連結可生成、點進去 404」與
PR-6 `within` 產生的 `/collections/{handle}/products/{p}` 未路由。

## 已完成的工作 (Done)
- 證據：help.shopify.com url-redirect 逐字 "URLs that use collection tag filtering (such as
  yourstore.com/collections/collection-name/tag-name). Even if no products exist with that tag, the URL path is still
  considered valid"；make-collections-findable 逐字 "display only the products that match all of the tags that you
  enter"（多 tag AND）；真店 hoko.vip（2026-09-03）`/collections/all/red`／`/collections/frontpage/red`（無此 tag）皆 200，
  `<title>` 分別「产品」／「首頁」。
- `PageRenderer#resolve`：`/collections/{handle}/{tags}` 分支（`+` 分隔、CGI.unescape）→ `CollectionDrop(current_tags:)`
  ＋ assign `current_tags`；request_path 含 tag 段（PR-6 的 tag_base_path 以此切基底）；虛擬 all／vendors／types 同收。
  `/collections/{handle}/products/{p}` 走商品頁。
- `CollectionDrop`：URL tag handle 以 handleize 相等對系列內實際 tag（`resolved_tags`）；`products`／facets／
  `filtered_relation` 帶 tag（當前檢視），`unfiltered_relation`（all_tags／all_products_count）不帶。
  `CollectionProductsDrop.base_relation(tags:)` 逐 tag `JSON_SEARCH … IS NOT NULL`（AND，與 BlogArticlesDrop 同形）。
- `PageTitles`：`/collections/all/{tag}` 標題 zh＝「产品」（hoko 逐字，與 `/collections/all` 的「商品」不同字）、en＝Products。
- spec `spec/requests/storefront_collection_tags_spec.rb` CT1–CT7；fixture `main-collection.liquid` 加 `ctags`／`calltags`。
- 突變輪：M1 路由不收 tag 段 ⇒ CT1–CT5＋CT7 紅；M2 拔 tag 過濾 ⇒ CT1／CT2／CT3／CT5 紅；M3 只套第一個 tag（AND→單）
  ⇒ CT2 紅；M4 pool 不做 handleize（`index_by { |tag| tag }`）⇒ CT3 紅（先前拔「第二次 fallback 查找」那格**沒紅**——
  pool 鍵本來就是 handle，該 fallback 只服務非 handle 形輸入，屬冗餘防線，照記）；M5 tagged-all 標題退 Products ⇒ CT5 紅；
  M6 拔系列語境商品路由 ⇒ CT6 紅。
- 回歸：`bundle exec rspec spec/requests/storefront_collection_tags_spec.rb spec/requests/storefront_collections_spec.rb
  spec/requests/storefront_drops_gap_spec.rb spec/requests/storefront_page_title_spec.rb
  spec/requests/storefront_section_wrapper_spec.rb spec/liquid/theme_conformance_spec.rb spec/liquid/filters_gap_spec.rb` 綠。

## 修改的檔案與核心邏輯 (Changes)
- `app/liquid/theme_engine/page_renderer.rb`：collections 分支 regex 收第二段 tag；商品分支 regex 收 `/collections/x/` 前綴。
- `app/liquid/theme_engine/drops.rb`：`CollectionDrop` 的 `current_tags:`／`resolved_tags`／`tag_handle`、
  `filtered_relation` 帶 tag；`CollectionProductsDrop` 的 `tags:`／`base_relation(tags:)`／`relation`。
- `app/liquid/theme_engine/page_titles.rb`：`products_tagged` 字串＋ collection 分支。
- `spec/requests/storefront_collection_tags_spec.rb`：新檔；`spec/fixtures/theme_engine/minimal-1.0/sections/main-collection.liquid`：加兩個 span。

## 尚未完成或需注意的風險 (Pending / TODO)
- `all_products_count` 在 tag 頁取全集（不含 tag）——官方 "includes products that have been filtered out" 的「filter」
  是否含 tag 路徑＝未逐字；真店兩例零商品無法分辨。
- 系列語境商品頁（`/collections/x/products/y`）v1 不把系列放進 assigns（官方 `collection` 物件在該頁可用＝未取證）。
- tagged-all 英文標題（kylie 對 tag 路徑 302 回 /collections/all）＝未取得，沿用 Products。
- 真店三產品仍無 tag，本包的真店逐字只覆蓋「無此 tag 仍 200」與標題形；tag 命中形靠本地 spec。
