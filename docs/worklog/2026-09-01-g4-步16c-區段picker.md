# 2026-09-01 G4 步 16c：add-section picker

## 已完成的工作 (Done)

- `Theme.sectionCatalog`（GraphQL JSON）：掃 sections/*.liquid schema，🔴 **只列帶
  presets 的區段**（24 §1.4：本尊「加入區段」清單＝presets 驅動；main-*/hero 這類
  骨架區段無 preset 不可手加，E4/M1 紅證）。回 `{type,name,preset:{settings,blocks}}`。
- 編輯器「新增區段」picker：目錄清單→點選＝addSection op（走 16b applyOp 快照棧，
  可 undo）：preset settings/blocks 深拷貝（M3 紅證）、id 撞名 `-N` 尾碼、
  插 order 尾端（M2 紅證）、自動選中新區段開設定面板。
- fixture：minimal-1.0 新增 `sections/promo.liquid`（帶 preset；hero 維持無 preset
  ＝picker 過濾的反向格）。
- 突變 3/3 殺（M1 目錄過濾／M2 order 插入／M3 preset 拷貝）。

## 修改的檔案與核心邏輯 (Changes)

- 改：`app/graphql/types/theme_type.rb`（sectionCatalog field＋resolver）、
  `ThemeEditorPage.tsx`（picker UI＋addSection op）、editor 測試 6→7 例（ED7）、
  `theme_editor_bootstrap_spec.rb` 3→4 例（E4）、i18n 3 鍵 ×5、admin.css。
- 新：`spec/fixtures/theme_engine/minimal-1.0/sections/promo.liquid`。

## 尚未完成或需注意的風險 (Pending / TODO)

- dnd 拖曳排序不做（鐵律 1 未討論依賴；move 上下鍵已交付排序功能）——維持 ⚪。
- 16d＝30 控件對映表＋佈景設定寫回（themeSettingsUpsert＋touch theme）；
  16e+＝code editor／頁面模板管理。
- ⚠ 本輪事故（未出倉自捕）：突變還原誤用 `git checkout --` 打掉**未提交**的 16c
  patch（theme_type/ThemeEditorPage 兩檔重寫一次）——突變一律 patch 正反向還原，
  不得對帶未提交工作的檔案跑 checkout。
- 生產煙測：部署後 picker 親走一輪（新增 promo→save→前台出現）。
