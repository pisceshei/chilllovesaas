# 2026-09-01 G4 步 16a：主題編輯器 shell（三面板＋postMessage 橋）

## 已完成的工作 (Done)

- 取證＝在庫雙錨：24 §1（編輯器 shell/樹/設定面板/切換器實測形）＋14 §F3
  （架構與坑：origin 白名單不用 *／橋雙向）。
- 全螢幕 shell（AdminShell 外路由 /admin/themes/:id/editor——24 §1.1 本尊無側欄
  形）：左樹（order 序＋disabled 眼睛態＋block 子層唯讀）／中 iframe
  （?editor=1 ⇒ design_mode）／右設定唯讀檢視；模板切換器（templates/*.json）；
  StorePage 加 Customize 動作（41 §634 首位動作）。
- postMessage 橋雙向（同源嚴格比對兩端）：樹點選 ⇒ cl:highlight 滾動外框；
  頁內點 section ⇒ cl:select 反選左樹。design_mode 才注入橋 JS＋
  data-shopify-editor-section（公開店面不帶——ME3 紅證）。
- ThemeType.templateJson（🔴 DB Template row 覆寫優先＝Runtime 同讀序；key
  白名單防逃逸）＋ThemeFileType.body（UTF-8 可解者；15b 登記的讀取面前拉）。
- 突變 5/5 殺（後端 ME1-3／前端 ME4-5）；rspec 1887＋vitest 330 全綠。

## 修改的檔案與核心邏輯 (Changes)

- 新：ThemeEditorPage.tsx＋test（3 例）、theme_editor_bootstrap_spec.rb（3 例）。
- 改：theme_type.rb（templateJson/body）、runtime.rb（EDITOR_BRIDGE_JS）、
  page_renderer.rb（design_mode 注入）、storefront_preview_controller（?editor=1）、
  App.tsx（shell 外路由）、StorePage（Customize）、admin.css（cl-editor 佈局）、
  i18n 12 鍵 ×5。

## 尚未完成或需注意的風險 (Pending / TODO)

- 16b＝op-stack 編輯管線（六原子 op／undo-redo／autosave／lock_version 衝突／
  🔴 儲存必 touch theme——步 2 紅字）；16c+=picker/30 控件/佈景設定/code editor。
- 樹與設定面板本包**刻意唯讀**；Save 鈕 disabled 帶 tooltip。
- 生產煙測隨 16b（唯讀 shell 部署後可看但無寫入面）。
