# Parity R2：首頁指標系統（4 槽×16 池＋口徑目錄）——「數據分析功能」落地

> 對應規格：docs/specs/71 §E R2、§F（R2-MISS1/DOC1/V1/V2）；docs/research/72（新建，口徑目錄）｜
> 雙源：實測 test（指標挑選器 16 項全清單、ⓘ tooltip 公式原文、拆解表實值、雙期 legend、
> `S-SHOPIFYQL-METRIC-CARD`/`S-SHOPIFYQL-DATE-CONTROLS` 元件架構）＋help zh-TW（agent：16 指標公式、
> session 30 分鐘/UTC 午夜規則、轉換漏斗三階段 2024-10 口徑、reversals 2026-05-01 換軌、4 槽 per-user）｜
> 使用者指令（2026-08-13）：每個 tag 有下拉、每個數據表有計算說明，逐一點擊體驗後做出數據分析功能。

## 已完成的工作 (Done)

- **docs/research/72 口徑目錄**：16 指標逐條「選單名/圖型/公式/出處」＋交互規則 10 條＋unknowns 4 條。
  重點口徑：總銷售額＝毛銷售額−折扣−銷售撤銷款項＋稅額＋關稅＋運費＋手續費；AOV＝(銷售總額−折扣)÷訂單數；
  轉換率漏斗三階段（到達結帳＝checkout 開始載入即計，排除 draft/exchange/POS）；session＝30 分鐘或 UTC 午夜切，
  未同意 cookie 不計入；撤銷記處理日；拆解表逐列公式。
- **原型實作（「數據分析功能」）**：
  - `HM_METRICS` 16 指標註冊表（id/圖型 icon/名稱/口徑 calc/格式）＋確定性 demo 資料（`HM_SERIES`）。
  - 4 槽 pill 列：點槽展開/收合大圖卡；選中槽出鉛筆 ✎ → **選取指標挑選器**（搜尋列即時過濾＋16 列
    ＋圖型 icon＋ⓘ hover 口徑 tooltip＋選中列高亮）→ 選定即生效（無儲存鈕，per-user 語義入 DOCS）。
  - **大圖卡**：標題虛線底線＝口徑 tooltip；當期值；`line` 型＝雙期折線（當期實線＋滾動同長前期虛線＋
    legend 兩段日期）；`breakdown` 型＝拆解表（小計→關稅逐列，**稅額列標「HK 無銷售稅」恆 0——鐵律 11
    jurisdiction pack 語義**）；表型＝POS 地點/員工/管道/商品列表；「檢視報表」（同源，鐵律 7）。
  - 即時訪客獨立控件保留（口徑=5 分鐘窗，入 DOCS hm-live）。
- **DOCS**：pulse 條目改寫＋新增 hm-picker/hm-chart/hm-live 三條（含 [api:TBD-homeMetricsUpdate]、
  [tbl:staff_home_metrics] 落表指向）。
- **驗證**：lint ERROR 0/WARN 13（=基線）；localhost 煙霧測試 13/13（渲染/展開/挑選器 16 列/公式 tooltip/
  拆解表/收合）；console 零錯誤。

## 修改的檔案與核心邏輯 (Changes)

- `docs/design/chilllove-admin-v2.html`（pulse 區塊容器化＋約 +120 行 JS 引擎＋DOCS 4 條）、
  `docs/research/72-admin-home-metrics.md`（新建）、`docs/specs/71`（§E R2 拆 R2/R2b、§F 五條）。
- **為什麼 demo 資料用確定性生成**：Math.random 會讓每次開頁數字不同——與「同源同口徑」的驗收敘事打架；
  種子式生成讓截圖可重現。
- **為什麼拆解表稅額寫死 0 並標註**：基準法域 HK 無銷售稅（鐵律 11）——這是 pack 行為不是缺資料；
  防止日後有人「補上稅額 demo 數字」破壞法域語義。
- **舊 drawSparks 成為 no-op**（.spk 元素已不存在）——刻意保留函式不刪（其他頁面若引用不炸），R14 收斂輪清理。

## 尚未完成或需注意的風險 (Pending / TODO)

- **R2b 殘項**（71 §E 已開）：搜尋 CtrlK/快訊動態/Sidekick 形態/API 搜尋入口/期間控制正式版
  （S-SHOPIFYQL-DATE-CONTROLS 選項集已實測到手）/管道×期間全域下拉。
- 71-R2-V1：ⓘ tooltip 以 title 屬性近似（本尊為樣式化浮層、指標詞帶底色）——R11 做正式 tooltip 元件時替換。
- 71-R2-V2：出廠預設第 3 槽（help=總訂單數 vs 實測店=拆解）已註記，低優先。
- 指標挑選器的鍵盤導航（↑↓ 選擇、Enter 確定）未實作——本尊行為未實測，先不臆造，R2b 補測後定。
