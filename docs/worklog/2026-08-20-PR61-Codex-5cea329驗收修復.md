# PR #61 Codex current-head 驗收修復（`5cea329`）

## 已完成的工作 (Done)

- 以 GitHub CLI 核對 PR #61 當前 head 為 `5cea3293f05ddb5a7e2dc4dbe4decb1c9c1d69b5`；
  current-head Codex review `4978950448` 於 2026-08-20 提出 inline `3818520933`、
  `3818520936`、`3818520940`。CI `quality`／`test` 均通過；Claude Review run
  `32330060810` 的 log 明載 validation skip，故只記「Claude 證據未取得」，不換算成通過。
- 先用 `git log -p` 與全樹識別字搜尋核對內部沿革：GraphQL `reviewThreads` 已在鐵律 20 與
  `docs/dev/m0-review-convergence.md` 成為固定處理；commit 後 doc-claims 已在 `AGENTS.md` 明定，
  缺口是執行方案 consumer 沒有同步，不是規則不存在。
- 查取 SHOPLINE 英文官方 Help Center：逐項取得三種 coupon claim 入口、會員中心 UI 分類、
  定向發送最短排程與失敗報表下載期限的英文原文及 URL，取證日期 2026-08-20。原方案自行串成的
  單一生命週期沒有來源支持，已降為「未取得、禁止作規格輸入」；並明載 SHOPLINE 只作輔助參考，
  Shopify 才是交互邏輯基準。
- 依鐵律 20.4 把本輪復發證據回寫證據帳：外部證據回掃、驗收資料攝取與 producer／consumer
  失同步，分別綁定三則 inline id；修法只覆蓋被點名的執行方案與終態記錄。
- 2026-08-20 pre-commit 驗證快照：依 `config/ci.rb` 當前 production 清單逐支執行，實跑彙總
  `GATE_TOTAL=17`、`GATE_FAILED=0`；doc-claims 警告 0，prototype lint 為
  `ERROR 0 / WARN 136`。Windows 預設 Bash 指到未掛載 C: 的 WSL，初跑屬工具未執行；切換至本機
  Git for Windows Bash 並把它的 `usr/bin` 置於 PATH 後，exec-bit 與 workflow syntax 的
  production／regression 閘門均退出 0。

## 修改的檔案與核心邏輯 (Changes)

- `docs/plans/2026-08-20-階段一執行方案.md`：每次 commit 後、push 前補跑 doc-claims；後續修復輪
  與雙零判定加入 paginated GraphQL `reviewThreads` fail-closed 對帳；優惠券種子補英文證據、
  收窄候選映射並撤回無證據的完整生命週期。
- `docs/dev/m0-review-convergence.md`：把本輪三則 current-head inline 加入既有 A／B／C 根因的
  復發證據，記錄「規範已立但 consumer／既有種子未回掃」的防線失效點。
- `docs/worklog/2026-08-20-階段一開場包.md`、`docs/handoff/2026-08-20-階段一開場包.md`：回寫
  PR #61 終態入口；`docs/specs/91-pit-register.md` 同步本工作記錄與配對 handoff。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本單位發布後仍須取得新 head 的 Codex 審核；舊 review `4978950448` 只能證明本輪輸入，不能
  證明修復後為零意見。
- PR #61 修改 workflow 與鐵律本文，命中鐵律 18.3；Claude 對本 PR 的 current-head run 已
  validation skip，故本 PR 不可能用綠色 skip 冒充雙清，最後仍由使用者獨立人工審核與合併。
- SHOPLINE 來源只支撐所引英文句；S-14 仍須獨立核對 Shopify 與我方需求，不能把候選映射或
  `2`／`7` 外推為所有優惠券通道的既定規格。
