# 2026-09-01 G3 步 15a：主題儲存目錄制＋zip 匯入管線

## 已完成的工作 (Done)

- 三源開場：官方研究（agent——OnlineStoreTheme 契約/ThemeRole 七值/limits 表
  50MB·250MB·100k 檔/唯一硬要求 layout/theme.liquid/themeCreate 非同步）＋
  admin 親點＝41 §634 既有動作選單全集沿用（本輪 /themes 頁工具面全滅——
  script injection 恆逾時，3 重載未解，V 項誠實登記）⇒
  `docs/research/99-theme-storage-import-teardown.md`。
- themes.content_checksum＋theme_import_reports 表＋limits theme_import 區塊
  （官方值逐字註）。
- Themes::ImportZip 八步管線：INVALID_ZIP（官方碼＋訊息形）／50MB／逃逸／
  symlink／壓縮比炸彈（ours 閾值）／單根剝除（ours——官方未取得）／頂層白名單
  略過＋警告／layout 硬要求／SHA-256 內容定址落 storage/themes/{checksum}
  （不可變、併發先到者贏）／相容掃描報告（Liquid parse 錯誤集）＋授權聲明 gate
  （鐵律 9）。
- 🔴 AST cache 跨租戶汙染根治：Sources.key_for 對匯入主題回 sha256- 內容鍵
  （同鍵恆同內容）；resolve 讀 storage 目錄。
- 突變 7/7 殺（MT5 兩輪——**檔名集也不同的對照組殺不掉「只摘要檔名」**＝
  equality-trap 新例，改同名不同內容格）；全套 1880 例綠。

## 修改的檔案與核心邏輯 (Changes)

- 新：migration、app/services/themes/import_zip.rb、app/models/theme_import_report.rb、
  spec（7 例）、99 號 teardown。
- 改：sources.rb（內容定址優先）、limits.yml（theme_import 九鍵）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 15b（theme CRUD API：themeCreate 走 stagedUploads＋Rename/Duplicate/Delete＋
  theme.files）＋15c（OS 頁動作 UI）接續。
- 91 §3.66：theme_files 表延後（目錄制＝共用地基；表隨步 16 寫入層）／symlink
  防線無正向測試（rubyzip 造 symlink 條目繁瑣——防線在、測試 V）／單根剝除
  ours／theme_import_reports 無清理 job。
- 生產煙測隨 15b API 面一起（管線本身無 HTTP 面）。

## 追補（同日：生產 boot LoadError 事故＋修復）

- #250 合併部署 ⇒ puma boot 炸 `cannot load such file -- zip`（rubyzip 只在
  dev/test 傳遞鏈；production bundle 不含）——服務中斷約 3 分鐘，
  deploy.sh cc0d0f8 回滾即復。事故與固定處理＝91 §3.67。
- 修：Gemfile 預設群組加 rubyzip ~> 3.4；以 BUNDLE_WITHOUT="development:test"
  親驗 require 成功＋spec 7 例綠。
