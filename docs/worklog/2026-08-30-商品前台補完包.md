# 2026-08-30 — 商品前台補完包（缺口分析切分 1＋A2 選中佈線）

## 已完成的工作 (Done)

- 依 `docs/plans/2026-08-30-商品模塊-Liquid對接缺口分析.md`（#199）落地：
  - **A1** `VariantDrop.available` 真庫存判準（untracked ⇒ true；tracked ⇒
    合計可售量>0 ∨ policy=continue）；`ProductDrop.available`＝任一變體可購。
  - **A2（選中半邊）** `?variant=` 佈線：controller query params → renderer →
    `ProductDrop.selected_variant_id` → `selected_variant`／
    `selected_or_first_available_variant`（選中→首可購→首位）。壞值忽略（ours，
    本尊行為未取證已登記）。Section Rendering 端點面仍歸包 33。
  - **A3** `options_with_values` 升級真 drop：新 `ProductOptionDrop`
    （name/position/values/selected_value）＋ `OptionValueDrop`
    （id/name/to_s/available/selected/variant/product_url/swatch=nil＋
    `==` 值語義覆寫——Ella `color == value` 跨陣列比較的相容前提）。
  - **A′** `inventory_quantity`（跨地點合計）／`inventory_management`
    （tracked ⇒ "shopify"，主題 JS 硬編碼相容字串）／`inventory_policy`／
    `barcode`／`weight`（公克）／`variant.url`（?variant=）／變體專圖
    （media.product_variant_id 優先）／`compare_at_price_varies` 計算。
  - **E10 抓到的額外真缺口**：`ProductDrop.variants` 原為 association 預設
    id 序 ⇒ 改 position 排序（Liquid 契約）。
  - renderer preload 擴充（庫存鏈＋選項座標＋變體專圖＋商品媒體
    ——商品媒體原本未 preload，`images` 恆空）。
- 測試：`spec/liquid/drops_spec.rb` 17 格（V1–V6/P1–P4/O1–O5/SP1–SP2，
  假綠殺手矩陣見檔頭）＋ `page_renderer_spec` E10 整頁佈線格。
  突變 M1–M6 實跑全部轉紅（available 硬編碼回歸／單地點合計／恆 first／
  拿掉 == 覆寫／value.available 恆 true／renderer 不佈線）。
  全套 rspec **1340 examples, 0 failures**；rubocop 改動檔零違規。

## 修改的檔案與核心邏輯 (Changes)

- `app/liquid/theme_engine/drops.rb`——VariantDrop 庫存/專圖/url/selected；
  ProductDrop 選中佈線/available/varies/position 排序；新 ProductOptionDrop
  ＋OptionValueDrop。
- `app/liquid/theme_engine/page_renderer.rb`——`render(path, params:)`、
  商品 preload 擴充、`selected_variant_id` 解析。
- `app/controllers/admin/storefront_preview_controller.rb`——query params 佈線。
- `spec/liquid/drops_spec.rb`（新）＋ `spec/liquid/page_renderer_spec.rb`
  （E10）＋ `spec/fixtures/theme_engine/minimal-1.0/sections/main-product.liquid`
  （pselected span）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 未取證格（實作皆取安全側並註記）：壞 `?variant=` 值的本尊行為；全售罄時
  selected_or_first… 的本尊 fallback；compare_at nil 混值的 varies 語義。
  三格待 Shopify CLI 探針（授權流程進行中）對表。
- 缺口分析 A′5–7（collections/metafields/category 出口）與 §C 切分 2 其餘
  （Section Rendering API 端點）按原切分歸後續包。
- swatch／selling plans／unit price 維持 nil stub（SP 格釘死）。
