# 合併輪：R11／R12／R13 依序併入 main

> 對應規格：docs/specs/71 §E R11-R13 皆 ✅｜commit：42f26d7（R11）／5e70500（R12）／1bdd73c（R13）｜
> 推送：`af2192c..1bdd73c main -> main`

## 已完成的工作 (Done)

- **依 R13 handoff 的建議順序合併**：R11 → R12 → R13，每次合完跑 lint ＋ 煙霧再提交。
  三個分支各自從 main 開、互不含對方改動，同時動了 `chilllove-admin-v2.html`、`config/limits.yml`、
  `docs/specs/71`，因此後兩次都有衝突。
- **R11 合併**：無衝突（main 之後沒有其他變更）。lint 0／YAML OK。
- **R12 合併**：2 處衝突，皆為「雙方各自在檔尾追加」型 ⇒ 保留雙方。
  - `config/limits.yml`：R11 `analytics` 節 ＋ R12 七節並存
  - `71 §E`：R11 與 R12 兩列**雙方都寫過**（各自把自己那輪標 ✅），逐列取非佔位版
  - `71 §F`：R11 28 條 ＋ R12 17 條並存
  - 煙霧 12/12（R11 六項＋R12 五項＋跨輪 DOCS）
- **R13 合併**：3 檔 5 處衝突，其中**一處是真語義衝突**：
  - 🔴 `chilllove-admin-v2.html` 第一處：**R11 刪掉了 `reportQL()`**（R11-BUG1：本尊沒有
    「ShopifyQL 進階模式」這個模式，QL 編輯器常駐報告主欄），而 **R13 從舊 main 開分支、
    還帶著這個函式**。若照「保留雙方」自動處理，會把 R11 修掉的 bug 又帶回來。
    解法＝保留 R11 的刪除說明 ＋ 保留 R13 的管道程式碼，**丟掉 R13 帶回的舊 `reportQL()`**（514 chars）。
  - 其餘四處（HTML 的 DOCS 區、limits、71 §E、71 §F）為純追加，保留雙方。
  - 煙霧 15/15。
- **合併驗證另外抓到一個殘留並修正**：`DOCS['set-perm-cats']` 仍寫「列出 13 類顆粒權限」，
  但 R12 已把 `permCats` 改成實測 **17 群組／115 權限**——數字已對齊，並補上「依賴是圖不是樹
  （授予往上補齊祖先、撤銷往下連動移除）＋庫存/目錄→產品·檢視不可取消」的說明。
- **合併後總狀態**：71 §E 的 R11/R12/R13 三列皆 ✅；R0-STUB1 標為 R13 結案；
  §F 共 55 條新登記（28＋17＋10）；`limits.yml` 十一節（analytics／settings／users_and_roles／
  notifications／brand_assets／store_activity_log／languages／policies／sales_channels／agentic／pos／apps）。

## 修改的檔案與核心邏輯 (Changes)

- 三個 merge commit（`--no-ff`，保留各輪的 PR 痕跡）：42f26d7 / 5e70500 / 1bdd73c。
- 衝突解析工具：`scratchpad/resolve-append.py`——處理「雙方各自在檔尾追加」型衝突，
  🔴 **內建安全閥**：偵測到兩側有同一鍵（表格列編號）時不自動合併，改逐列取非佔位版並列印重疊清單；
  真正的語義衝突（如 `reportQL()`）**不交給腳本，人工處理**。
- `docs/design/chilllove-admin-v2.html`：`DOCS['set-perm-cats']` 由「13 類」改為「17 群組／115 權限」
  ＋補依賴圖說明。

## 尚未完成或需注意的風險 (Pending / TODO)

- **三個 feature 分支尚未刪除**（`m0/parity-r11-analytics`／`-r12-settings`／`-r13-channels`）。
  遠端也還在。要不要清理由使用者決定——留著可回溯，刪掉較乾淨。
- 🔴 **PR #12（`m0/rails-foundation`）沒有動**：它是 M0 Rails 骨架（155 檔案／10,640 行），
  現在 **22 commits behind main**（合併三輪後又多 3）。它與 parity 輪是不同性質的合併，
  且需要先把 main 併進去解衝突再跑實測（rspec／Vitest／tsc／rubocop／brakeman）。**未處理**。
- **未跑後端測試**：本次合併只動 docs 與原型 HTML／YAML，沒有碰 Rails 程式碼，
  因此只跑了 lint ＋ 原型煙霧。若之後合 PR #12，必須跑全套測試。
- **同型衝突會再發生**：R14 若也從 main 開分支、又動同樣三個檔案，
  一樣會撞 §E／§F／limits 檔尾。建議 R14 **直接從最新 main 開**（現在已含三輪）。
