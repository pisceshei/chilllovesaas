# 2026-09-04 E15 部署後複驗（bt3 mirror 店五語言五市場＋八頁三寬 computed 對表）

配對 handoff：`docs/handoff/2026-09-04-鏡像店五語言五市場E15.md`（同一工作包，§①③ 終態同步更新）。規範：`docs/dev/e12-computed-parity.md` §2b。

## 已完成的工作 (Done)

- PR #324 squash 合併 → main `a879b13a`；bt3 `scripts/deploy.sh origin/main` → head `a879b13`；無 migration；`render_parity:mirror[mirror]`
  冪等對齊（bt3 shop #2：五語言 zh-Hans 來源、五市場各一 presence／白名單 5、`available_countries`＝31）；`Rails.cache.clear`。
- 公開 `https://mirror.chilling.com.hk/zh-hans-tw/`：200、`Shopify.locale = "zh-CN"`、頁首語言鈕 1＋section-fetcher（`CountryLocalizationList`）、hreflang 156。
- computed 對表 8 頁 × 3 寬（hoko live vs bt3 public mirror，`--open-details 1 --wait 3000`）：逐頁逐寬＝E12 基線（表在 e12 §2b）；
  本機出現的 promotion_popup 時序項在 bt3 全部消失。`/collections/frontpage @1280` 首抓 10/15 為捲動觸發動畫中間態，同寬重抓 14/15。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `docs/dev/e12-computed-parity.md` | §2b 加「E15 部署後（bt3）」表與 mirror 店實測 |
| `docs/handoff/2026-09-04-鏡像店五語言五市場E15.md` | §① 部署與對表終態、§③ 移除「bt3 未部署」 |

docs-only；本機跑 doc-claims 兩支（check／rules），完整閘門由 PR 的 CI `quality`＋`test` 執行。

## 尚未完成或需注意的風險 (Pending / TODO)

- D80 未裁定（hreflang 156 條 vs 本尊 6、切換器 `root_url` 帶地區）；頁首區段 fetch 內容（31 國／5 語言清單）尚未做 SRA HTML 段 diff；91 §3.83 的 V 照舊。
- 擷取瞬態（捲動動畫中間態、popup 計時器）會偶發把單頁單寬拉成非全同：判讀時先同寬重抓一次再登記。
