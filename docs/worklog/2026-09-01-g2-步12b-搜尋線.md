# 2026-09-01 G2 步 12b：搜尋線（/search＋suggest 雙形＋recommendations 雙形）

## 已完成的工作 (Done)

- 三源取證沿用 96 號（§3–§5 本包實作面）：官方參數表逐字＋chill.deals 親點
  （17 結果頁形／sort 恰 3 值 DOM 抓取／Ella header 打 section 形實錄）＋
  live payload（suggest 16 鍵 decimal 字串／recommendations 空形 intent 回聲）。
- /search 頁（search 物件九屬性＋混型結果真分頁＋object_type＋price 排序非商品
  推尾）；不進頁快取（q 無界）。
- `GET /search/suggest.json`（官方參數全驗 422 fail-closed；只回請求型鍵；
  `_pos/_psq/_psid/_ss=e` 歸因）＋`GET /search/suggest`（section 檔名直渲染＋
  predictive_search 物件——Ella 硬依賴形）。
- `GET /recommendations/products.json`＋section 形（官方三錯誤逐字；related v1
  ＝共同系列成員；complementary＝空＝未配置真實形）。
- section_data_for 第三層 fallback：section_id＝檔名（Dawn/Ella 實際請求形）。
- 突變輪 12/12 殺＋canary；全套 1847 例 0 failures。

## 修改的檔案與核心邏輯 (Changes)

- 新：`app/services/storefront/search_query.rb`（LIKE 詞×欄位矩陣；變體欄位走
  EXISTS 帶 shop_id——鐵律 2③）、`app/controllers/storefront/search_controller.rb`、
  `app/controllers/storefront/recommendations_controller.rb`、
  `spec/requests/storefront_search_spec.rb`（12 例）＋fixture 四檔。
- 改：drops.rb（SearchDrop/SearchResultsDrop/PredictiveSearchDrop＋ProductDrop/
  PageDrop object_type）、page_renderer.rb（/search resolve＋render assigns: ＋
  檔名 fallback）、routes.rb（四端點 ×裸/帶前綴）、pages_controller（/search
  繞頁快取＋CACHE_PARAMS+type）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.61 六 ⚪（欄位集 ours／prefix·unavailable 不改匹配／query 建議空／
  relevance ours／422 body 細形／section 形 locale 預設字典）。
- articles 型恆空（步 14 blog 線接上後 SearchQuery 補 articles 分支）。
- 生產煙測待部署（demo /search 從 404 轉 200＋Ella header 預測下拉出真結果
  是本包可見交付）。
