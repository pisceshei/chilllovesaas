# 2026-09-02 Ella 修復 PR-11：編輯器改即見全面化（draft_page 全頁草稿渲染）

## 已完成的工作 (Done)

- chill.deals 對表艦隊 editor-live 軸①②③④收口（本尊行為實測 2026-09-01）：
- **①佈景主題設定改即見**：本尊改佈景設定全頁即時重渲染，我方原本零反應。
  `Runtime` 增 `draft_settings:`——三層設定（schema defaults → 檔案 current →
  DB 覆寫）之後再疊未儲存草稿；SettingsDrop／color_scheme 全鏈自動吃到。
- **②結構操作改即見**：加／刪／移／隱藏 section 原本 stale 到 Save。
  `Runtime` 增 `draft_sections:`——整頁渲染（`render_template_sections`）與
  群組帶（`render_section_group`）兩處都以 draft 覆蓋最優先（與片段路徑
  `section_data_for` 同序）。🔴 初版只接了片段路徑，DP1 紅過一輪才補整頁
  與群組兩處——draft 覆蓋是三消費點，不是一個。
- **③undo/redo 驅動預覽**：前端 `draftsVersion` 計數器＝唯一觸發源；
  applyOp／setThemeSetting／undo／redo 全部 bump。
- **④資源語境**：新 GraphQL 欄 `previewPaths`（模板型 → 該店首個資源樣本
  路徑；無資源不出鍵）；iframe src 與 draft 渲染路徑都按當前模板走樣本頁；
  片段 fast-path 的「v1 鎖首頁」登記限制一併收口。
- **新端點 `draft_page`**（POST /admin/store/preview/:theme_id/draft_page）：
  收 path＋sections（全帶合併）＋settings，design_mode 渲整頁 HTML ⇒ 前端
  debounce 600ms 後 srcdoc 換入（srcdoc 繼承父 origin，postMessage 橋不斷）、
  保捲動位置。單 section 設定變更仍有 400ms 片段 fast-path 先行。
- 測試：DP1（draft settings＋sections 整頁生效、不落 DB、design_mode 橋）、
  DP2（不帶 settings 走檔案 current；previewPaths 形狀）、ED14（改佈景設定
  → draft_page POST 帶 settings → srcdoc 換入）、ED15（undo 驅動刷新）。
- 突變 3/3 殺：M11-1 拔整頁 draft 覆蓋（DP1 紅）、M11-2 拔 settings merge
  （DP1 紅）、M11-3 拔 setThemeSetting bump（ED14 紅）。

## 修改的檔案與核心邏輯 (Changes)

- `app/liquid/theme_engine/runtime.rb`：`draft_settings:`／`draft_sections:`
  kwargs＋群組帶覆蓋。
- `app/liquid/theme_engine/page_renderer.rb`：render 透傳兩個 draft 參數；
  `render_template_sections` 疊 draft。
- `app/controllers/admin/storefront_preview_controller.rb`：`draft_page` action。
- `config/routes.rb`：draft_page 路由。
- `app/graphql/types/theme_type.rb`：`previewPaths` 欄。
- `app/frontend/admin/pages/ThemeEditorPage.tsx`：draftsVersion 計數器＋
  全頁刷新 effect＋previewPath 語境＋fast-path 路徑改 previewPath。
- `spec/fixtures/theme_engine/minimal-1.0/layout/theme.liquid`：加
  `{{ settings.brand_color }}` meta 錨（DP1 斷言面）。
- specs：theme_editor_bootstrap_spec（DP1/DP2）、ThemeEditorPage.test.tsx
  （ED14/ED15）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本尊逐 setting 的 partial morph（改一個字只 patch 受影響 DOM）＝未對位；
  我方是整頁 srcdoc 換入＋保捲動，互動狀態（開著的 drawer）會重置。
- previewPaths 取「首個資源」；本尊可在編輯器內切換預覽資源（下拉選商品）
  ＝未做，登記後續。
- block 錨點（fleet 軸⑤ data-block-id 高亮）仍未做，登記後續。
- srcdoc 換入後 `<base>` 缺席時相對 URL 以父頁為基＝已由主題內連結全走
  絕對路徑（url_prefix）緩解；未見破圖，未逐資產窮舉（V）。
