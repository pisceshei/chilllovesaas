# 2026-09-03 店級貨幣格式 D81（`shop.money_format`／`money_with_currency_format`）

分支 `money/d81-money-format`（自 main `3ba1083f`）。規範 `docs/dev/d81-shop-money-format.md`；證據 `docs/dev/external-facts.md` §G15；
未取得 `docs/specs/91-pit-register.md` §3.77；裁定 `docs/DECISIONS.md` D81（附落地註）。

## 已完成的工作 (Done)

- 證據：官方 help「Currency formatting」八佔位符與例值、filters/money 族四頁、objects/shop 三屬性逐字；真店 pnrjnw-sy admin
  Settings › General › Change currency formatting 四欄實讀（HTML with `HK${{amount}} HKD`／without `${{amount}}`／Email 同值）；
  前台印證（商品卡 `$19.99`、購物車抽屜 `HK$0.00 HKD`、`window.money_format`、`og:price:amount` `188.00`）。
- schema：`shops.money_format`／`money_with_currency_format`（migration 20260903140000，逐欄 `column_exists?` 守衛、回填
  `safety_assured`）；HKD 既有店回填真店四值，其餘依 `Shop::MoneyFormatDefaults` 通用形。
- 引擎：新 `ThemeEngine::MoneyFormat`（八佔位符、`{{ amount }}` 空白容忍、未知佔位符原樣、整數算術不經 float、`amount_only`、
  `strip_trailing_zeros`、`coerce`）；`money`／`money_with_currency`／`money_without_currency`／`money_without_trailing_zeros` 全改讀
  registers 兩鍵；`unit_price_with_measurement` 走 `money`；三處 `{ "HKD" => "HK$" }` 表移除（Runtime／ShopDrop／Notifications）。
- `ShopDrop#money_format`／`#money_with_currency_format` 直出兩欄；`#permanent_domain`／`#domain` 改 `{subdomain}.{base_host}`
  （硬編 `chilllove.example` 收口）。
- 鏡像店：`hoko.json` 帶兩鍵＋證據註；`Mirror#ensure_shop` 對齊兩欄（缺鍵 ⇒ 幣別種子）。
- Shop model：`before_validation :seed_money_formats, on: :create`（明給者不覆寫）＋ presence／length 驗證。
- 對表（本機 dev server，mirror.lvh.me vs live hoko.vip 首頁，2026-09-03）：21 段 **18 段位元組相同**（E8 收尾為 17 段；
  商品卡段 `section_7hRDd8` 收斂），餘 3 段＝`__head__` 0.926（平台注入＋dev 主機）、`header_default` 0.968、`cart_drawer` 0.988
  ——三段的差異片段全部是本尊新版顧客帳戶 URL 與 JSON-LD 的主機／前綴（D80 範圍），無金額差異。
- 閘門與突變：見本檔「閘門」段（凍結 tree 後填）。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260903140000_add_money_formats_to_shops.rb`、`db/schema.rb`：兩欄＋回填 SQL（幣別集合與 Ruby 表同源，MF8 雙向核對）。
- `app/models/shop/money_format_defaults.rb`（新）、`app/models/shop.rb`：種子表與建店 callback。
- `app/liquid/theme_engine/money_format.rb`（新）：格式引擎。
- `app/liquid/theme_engine/filters.rb`：money 族改讀 registers；`format_money` 移除；檔頭註釋更新。
- `app/liquid/theme_engine/runtime.rb`：`base_registers` 注入 `money_format`／`money_with_currency_format`；`money_symbol` 移除。
- `app/liquid/theme_engine/drops.rb`：ShopDrop 兩欄直出、permanent_domain／domain 走 base_host。
- `app/services/notifications/renderer.rb`：registers 同款；`money_symbol` 移除。
- `app/services/render_parity/mirror.rb`、`spec/fixtures/render_parity/hoko.json`：鏡像店對齊兩欄。
- 規格：新 `spec/liquid/money_format_spec.rb`（MF1–MF10）；改值 `page_renderer_spec`／`storefront_seo_spec`／`theme_js_runtime_spec`／
  `shop_fonts_gap_spec`／`notifications/renderer_spec`／`delivery_chain_spec`／`liquid_globals_spec`（註釋）。
- 文檔：`docs/dev/d81-shop-money-format.md`（新）、`docs/dev/external-facts.md` §G15、`docs/specs/91-pit-register.md` §3.77、
  `docs/dev/e8-render-parity.md` §3／§4、`docs/DECISIONS.md` D81 落地註。

## 尚未完成或需注意的風險 (Pending / TODO)

- **未取得（91 §3.77）**：各幣別預設符號表；`amount_no_decimals` 恰 .50 捨入；負值形；非整數／非數字輸入；
  `money_without_trailing_zeros` 對非全零小數——擬以副本主題 Custom Liquid 探針補測，本輪因本尊編輯器分頁在背景
  （`visibilityState=hidden`）側欄不載入而未執行。
- **未做**：設定 UI（Settings › General 對話框）、Admin GraphQL `shop.currencyFormats`、Email 專用兩欄、Markets 多幣別格式。
- **行為改變**：既有 HKD 店（demo／mirror）前台自本包起顯示 `$1,480.00`（原 `HK$1,480.00`）；admin 後台顯示不變（鐵律 10）。
- **部署**：bt3 `scripts/deploy.sh` 不跑 migration ⇒ 部署後手動 `RAILS_ENV=production bin/rails db:migrate` 再重啟 puma，
  之後重跑 `render_parity:mirror[mirror]` 讓鏡像店兩欄對齊快照（回填已給 HKD 同值，mirror 任務只是確認）。
- **下一包**：Announcement bar 起逐控件與本尊編輯器對照（需該分頁在前景）；D80 路由前綴（會收掉本輪剩下的 JSON-LD 前綴差異）。

## 閘門（凍結 tree 後全跑，2026-09-03 本機，dev server 已停）

| 閘門 | 結果 |
|---|---|
| `bin/rubocop` | 979 files inspected, no offenses detected（exit 0） |
| `bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error` | exit 0 |
| doc-claims／doc-claims-rules／tenant-isolation／tokens-sync／baseline-raise／lint-prototype／lint-rules／exec-bits／exec-bits-rules | 全部 exit 0（worklog／handoff 寫入後再補跑 doc-claims 兩支，見 PR body） |
| `pnpm typecheck` | exit 0 |
| `pnpm test` | Test Files 37 passed / Tests 407 passed |
| `bundle exec rspec` | 2102 examples, 0 failures |
| 突變 M95–M101（scratchpad `mutate_d81.py`，commit 後跑） | 輸出貼於 PR body |
