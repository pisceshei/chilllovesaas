# PR #65 第三輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收基準固定為 head `1ce7e59e4461c1ca076a9c0656f178720cd2b8fc`。鐵律 15.2 三端點
  全量重取後，Claude issue comment `5359967807` 有兩條 🔴、三條 🟡；Codex review
  `4985880560` 的 current-head inline 為 `3824100538`／`3824100544`。Claude Review run
  `32400324063` completed success，沒有 validation skip；CI quality／test 也對該 head success。
- `git log -p` 證明 51 號表 6 與文件本身都建立於
  `e50b9120cc9b2514fde4995a5ff4f6ff15332bff`。因此沒有把歷史 82／4／78 改成 HEAD 現值；
  修法是把一覽列、整張表 6、§6.2、§6.4 落地率與表末統計都明確綁同一快照，並把過寬的
  「目前樹不得沿用」收斂成「發布目前樹數字前必須重算」。CLAIM-005 重跑輸出仍為
  `declarations=82 / overlap=4 / new_tokens=78`，只證明該歷史快照。
- 84／92 把未結案口徑拆清楚：狀態格含 `⬜` 為 77，當中 76 列以 `⬜` 起頭，混合狀態只有
  `71-R3-DOC1`。第一版 recheck 因 Ruby `split` 丟掉結尾空欄而實際得到 0／0，已被實跑抓住；
  改用 `split("|", -1)` 後六條 CLAIM recheck 全部通過。71 本體不在累積 diff，只登 91 §3。
- R6 縮排 metadata 先新增負向／正向 fixture：舊 checker 對缺 recheck 的縮排 count 實得
  exit 0；規則改為接受 0–3 個空格後，負向 exit 1、合法成對案例 exit 0。官方 CommonMark
  0.31.2 §5.2 逐字為 "preceding each line of Ls by up to three spaces of indentation"，來源
  <https://spec.commonmark.org/0.31.2/#list-items>（取證 2026-08-21），並已集中到 external-facts B9。
- 未關閉圍欄 fixture 也先重現舊邏輯 exit 0，再讓 R6 在起始行 fail-closed；修後整份回歸為
  22 個 fixture case、3 個 git 情境與 1 個供給情境全綠；複驗入口：
  `ruby scripts/test-doc-claims-rules.rb`。CommonMark 本身允許圍欄延伸到 EOF；
  本專案禁止它是為防結構化索引被靜默截斷的更嚴格政策，不冒充外部規格。
- `docs/worklog/2026-08-21-PR65第二輪雙驗收修復.md` 保留原始 Markdown API 敘事，緊鄰加
  日期 HTML 更正：exact response artifact／body 未保存，所以渲染結果與衍生計數均為「未取得」。
- Claude 本輪三條 ⚪ 與同根未點名的未關閉 HTML comment 只登記到 91 §3；沒有擴修 71、83
  舊命令／欄位標題或 checker 的 comment EOF 分支。PR 描述的 head／實跑／清況屬 GitHub
  外部狀態，待 push 後依新 exact head 重建。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 首輪 m0-review-convergence 同檔消費者、第二輪 83 §0 總表、本輪 51 同檔快照消費者；來源依序為 Claude comments `5359074865`、`5359428067`、`5359967807` |
| 既有防線失效原因 | 第二輪反向複驗只鎖 P0-14／P0-18，沒有把「本輪改動語義的識別字做同檔全掃」升成通用步驟，因此 51 的 82／78 其他消費位置未進閉合面 |
| 可重跑反向複驗 | `git grep -n -E "82 token|82 個 token|/ 82|82 宣告|78 新增|82／4／78" -- docs/specs/51-token-conformance.md` 的所有命中都位於帶 `e50b9120` 邊界的一覽列或表 6；CLAIM-005 重跑通過 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取 | current head 三端點全部 `--paginate`，逐則讀 review body 與 inline | 上列 issue comment／review／inline／run ID 均對應 `1ce7e59e…`；Claude run completed success |
| ② 生產者與消費者同步 | 以 82／78、CLAIM、R6、未結案標記反查來源、契約註釋、docs/dev、fixture、84／92、91 與 worklog | 51 同檔命中已綁快照；R6 三處契約描述同步 ID 唯一／零標頭／圍欄收尾；76／77 兩個消費位置使用同一 recheck |
| ③ 易腐數字／完成聲明 | 歷史數字綁 commit；HEAD 口徑由可失敗命令同時斷言分量與例外 ID | 六條 CLAIM recheck 全過；PR 描述只在 push 後綁新 head，不外推本輪輸入 head |
| ⑤ fail-open／happy path | 每個新邊界先以負向 fixture 重現舊漏檢，再保留合法反向案例 | metadata 負向舊值 exit 0、修後 exit 1；合法縮排 exit 0；未關閉圍欄舊值 exit 0、修後 exit 1；整份 suite 全綠 |
| ⑦ 外部 Markdown／Windows | 外部語義採官方逐字；沒有 response artifact 就寫未取得 | CommonMark URL／逐字／日期已入 external-facts B9；第二輪 worklog 的 GitHub Markdown 結果已更正為未取得 |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：縮排 count／recheck 使用 0–3 空格契約；未關閉圍欄 fail-closed；
  R6 契約註釋同步零標頭、唯一 ID 與圍欄收尾。
- 契約文字反查：`git grep -n -E "至少有一個活性|ID 唯一|圍欄必須收尾" -- scripts/check-doc-claims.rb docs/dev/m0-review-convergence.md`。
- `scripts/test-doc-claims-rules.rb`、`spec/fixtures/ci_violations/doc_claim_*`：新增縮排 metadata
  正反案例與未關閉圍欄負向案例，並提高 suite 下限。
- `docs/dev/m0-review-convergence.md`、`docs/dev/external-facts.md`：同步 R6 終態契約、fixture／突變
  射程，以及 CommonMark 官方證據與本專案更嚴格政策的邊界。
- `docs/specs/51-token-conformance.md`、`docs/specs/84-m1-gate-triage.md`、
  `docs/specs/92-claim-index.md`：表 6 快照閉合、76／77 口徑與可失敗 recheck。
- `docs/worklog/2026-08-21-PR65第二輪雙驗收修復.md`：保留歷史原文並標記外部 response 未取得。
- `docs/specs/91-pit-register.md`：只登記本輪範圍外與未點名同根問題，並同步 A.1。
- `docs/worklog/2026-08-21-PR65第三輪雙驗收修復.md`：保存取證、失敗先行、20.4／20.3 與待辦。
- PR 描述（GitHub 外部狀態）：push 後更新 exact head、實跑證據與第三輪清況。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本段定稿後須從 setup 跑完整 Windows production 等價閘門；任一非零即停止。其後才可
   commit、跑 commit 後 doc-claims、執行鐵律 15、push 與更新 PR 描述。
2. push 後須強制移動 `pr65-last-push` 到新 head，重新取得 CI、Codex、Claude、threads、
   mergeability 與 evaluator；舊 head 的任何結果不得外推。
3. #65 修改 `scripts/`，命中鐵律 18.3；即使雙清與四條件齊，也只能通知使用者人工合併，
   互動式 Codex 不得代行。
