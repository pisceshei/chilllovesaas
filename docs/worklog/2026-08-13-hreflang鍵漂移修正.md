# 71-R0-V1 結案：limits.yml hreflang 鍵組落齊 2026-08-13 恆帶地區裁定

> 對應規格：62 §I.2／§I.2-1／§L 鍵變更表（1193-1205）、71 §F V1 ｜ commit：見本檔所在 commit

## 已完成的工作 (Done)

- 查證坐實 guard 工作流的漂移警告：`seo.hreflang.region_qualified_when_single_country_market: true`
  連鍵名帶註釋（「單國市場 ⇒ 語言-國家；多國市場 ⇒ 語言碼」）都是**裁定前語義**，與 62 §I.2 現行
  「恆帶地區、多國市場逐國展開、裸碼 CI 紅燈」直接矛盾——62 §L 早開好處方但從未落地。
- 照處方落齊 **8 鍵**：改名 `always_region_qualified: true`（D-2 形態，不留別名、原位留追溯）＋新增
  `multi_country_market_expands_per_country`／`bare_language_code_forbidden`／`multi_presence_resolution:
  nearest_own_then_ancestor`／`dedupe_must_be_page_independent`／`over_limit_action: emit_partial_and_lint`／
  `validator_accepts_bare_code_for_audit_only`／`x_default_target: default_market_locale_prefixed_url`。
- YAML 解析驗證通過（hreflang 區塊 18 鍵）。舊鍵名全庫 grep 僅剩追溯註釋與 62/67 處方行（正常）。

## 修改的檔案與核心邏輯 (Changes)

- `config/limits.yml`（seo.hreflang 區塊）、`docs/specs/71-admin-parity-sweep.md`（V1 標結案）。
- **為什麼一次落 8 鍵不只改 1 鍵**：67:1645 明言處方是「六鍵等」整組——只改名不補配套鍵（裸碼禁令/展開/
  超限動作）會做出「規格說恆帶地區、鍵組卻表達不了執法點」的半套狀態，正是 62/67「不得只改一邊」教訓。
- **為什麼 locale_prefix/market_locales/naming_contract 三個新區塊不在本輪**：它們是 62 §L 同表的
  1203-1205 行，但屬 i18n／命名契約家族、規模大（67 §J 也宣稱過未落）——已在既有待辦（market_id 輪
  worklog Pending 段）登記為獨立任務，混進來會讓 V1 這個「一次修復」單位失去可驗收邊界。

## 尚未完成或需注意的風險 (Pending / TODO)

- `i18n.locale_prefix.*`／`i18n.market_locales.*`／`naming_contract.*` 三區塊仍未落鍵（獨立待辦，出處
  62 §L:1203-1205＋67 §J）。
- 62 §O REG-4 的 CI 掃描（裸碼紅燈）目前只是規格文字——落地時點＝M2 SEO 基線；本輪只把鍵備好。
