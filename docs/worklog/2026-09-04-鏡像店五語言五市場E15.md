# 2026-09-04 E15：鏡像店同步真店五語言五市場＋`localization.available_countries`

配對 handoff：`docs/handoff/2026-09-04-鏡像店五語言五市場E15.md`。規範落點：`docs/dev/e8-render-parity.md` §1／§2c／§3、
`docs/dev/e12-computed-parity.md` §2、`docs/dev/external-facts.md` §G22、`docs/specs/91-pit-register.md` §3.83。鐵律 22（D82／D83）。

## 已完成的工作 (Done)

- 快照 `spec/fixtures/render_parity/hoko.json`：加 `shop.languages`（zh-Hans／zh-Hant／en／fr／ja，序＝本尊首頁 hreflang 序）、
  `market_name` 改「台灣」、`markets[]`（美國 US／香港 HK／日本 JP／欧盟 27 國，各附本尊市場 id 與證據註；欧盟逐國未讀 ⇒ europa.eu 清單代位 V）。
- `RenderParity::Mirror`：`align_locales` 改為語言集對齊（建缺列→切來源→逐列 published／enabled／position→集合外撤發布、不刪列）；
  `align_market` 改 `align_markets`（主市場名稱／handle 跟隨；非主市場 find-or-create、regions 加缺刪多、恰一個 subfolder presence
  （suffix＝handle）、白名單同語言集）；抽 `align_presence_locales`（先撤舊預設旗再改 presence 預設，集合外白名單列刪除）。
- 引擎缺口 #66（e8 §2c）：真店五語言五市場後本尊頁首初始 HTML 走 Ella「地區＋語言」分支（按鈕＋section-fetcher），我方同步語言後仍只有
  1 國 ⇒ 走「只有語言」分支（內嵌整個表單）。修法＝`Storefront::LocalizationContext#available_countries`（與當前 presence 同 effective
  domain 的 active region 市場 regions 聯集；在地名／順序讀 `ThemeEngine::CountryOptionTags` 字典；`currency`＝店幣別；`popular?` 恆 false）、
  `localization.country` 同形；`ThemeEngine::LocalizationDrop` 收 `available_countries:`；`CountryOptionTags` 加 `entry_for`／`label_for`／`sort_key`。
- 規格：`mirror_spec` MR1（擴：五語言序、主市場名、四個非主市場／regions／presence／白名單、前綴解析）、MR2（計數加市場／region／
  presence／白名單／語言列）、MR3（新：對齊收斂——ko 撤發布、CA 刪、預設語言復原、關閉列重開）、MR4（新：資料面接渲染面——
  available_languages 五項＋首頁 `dropdown-localization__button`＋`CountryLocalizationList`）；`localization_context_spec` LC1–LC4（新）。
- 本機 dev shop 5（mirror.lvh.me）已對齊：五語言（zh-Hans 來源）、五市場、25 白名單列；探針 `tmp/probe_mirror_home.rb`：
  `Shopify.locale = "zh-CN"`、語言鈕 1＋section-fetcher 1（與 hoko 初始 HTML 同形；修前 section-fetcher 0）、hreflang 161（本尊 6，D80）。
- 取證入 external-facts §G22：官方 `localization`／`country` 逐字；hoko header 區段（SRA）31 國清單形；首頁初始 HTML 形；Ella 分支行號；EU-27。
- 對表（本機 dev vs hoko live，8 頁 × 3 寬，`--open-details 1 --wait 3000`）：見下「對表」節。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `spec/fixtures/render_parity/hoko.json` | `shop.languages`／`market_name`／`markets[]`＋證據註 |
| `app/services/render_parity/mirror.rb` | `languages`、`align_locales`（語言集）、`align_markets`（主＋非主）、`align_presence_locales`；檔頭②③ |
| `app/services/storefront/localization_context.rb` | `languages_for`、`available_countries`、`country_hash`；`drop` 帶 `available_countries:` |
| `app/liquid/theme_engine/drops.rb` | `LocalizationDrop` 新 kwarg `available_countries:`（缺省退「只有當前國」） |
| `app/liquid/theme_engine/country_option_tags.rb` | `entry_for`／`label_for`／`sort_key` |
| `spec/services/render_parity/mirror_spec.rb` | MR1–MR4 |
| `spec/services/storefront/localization_context_spec.rb` | 新：LC1–LC4 |
| `docs/dev/e8-render-parity.md` | §1 鏡像店列、§2c #66、§3 hreflang 展開列 |
| `docs/dev/e12-computed-parity.md` | §2 E15 對表列 |
| `docs/dev/external-facts.md` | §G22 |
| `docs/specs/91-pit-register.md` | §3.83 |

## 對表（本機 dev server `mirror.lvh.me:3000` vs hoko.vip live；`scripts/computed-parity.mjs` `--open-details 1 --wait 3000`；2026-09-04 09:2x–09:42 UTC）

| 頁 | 1280 | 768 | 390 | 非全同段（全部三寬合併） |
|---|---|---|---|---|
| / | 20/21 | 20/21 | 19/21 | `__root__`（平台 iframe／shop-cart-sync）；390 另 promotion_popup（時序，見下） |
| /collections/all | 12/15 | 14/15 | 14/15 | `__root__`；1280 另 promotion_popup＋multitasking_bar（時序） |
| /collections/frontpage | 14/15 | 14/15 | 14/15 | `__root__` |
| /products/acme-tee | 11/15 | 13/15 | 12/15 | `__root__`、`template--T__main`（countdown 秒數＋本尊 payment button，91 §3.79）；1280／390 另 promotion_popup（＋1280 multitasking_bar）（時序） |
| /pages/contact | 15/16 | 15/16 | 15/16 | `__root__` |
| /search | 11/12 | 11/12 | 11/12 | `__root__` |
| /cart | 14/15 | 13/15 | 14/15 | `__root__`；768 另 promotion_popup（時序） |
| /nope（404） | 11/12 | 11/12 | 11/12 | `__root__` |

- 頁首 `header_default` 段三寬全同（含新出現的語言鈕＋section-fetcher）；E12 基線（e12 §2）逐頁逐寬相同，唯一新增的非全同段＝promotion_popup。
- **promotion_popup／multitasking_bar 差＝擷取時序，非引擎差**：diff 逐行是 `promotion-popup` `visibility hidden→visible`／`opacity 0→1`、
  `body overflow-y visible→clip`（Ella `base.css` L148 `body.overflow-hidden`）、multitasking bar x 位移 1px——即我方頁面在擷取時 popup 已開。
  兩邊 `<promotion-popup … data-expire="1" data-delay="10000">` 同值（`promotion-popup.js` L6／L56：connectedCallback 起算 10 秒 `setOpenPopup`）；
  本機 dev server 靜態資產由 puma 逐檔慢送，`loadEventFired` 晚於 10 秒 ⇒ 工具的「load＋fonts＋3 秒」擷取點落在 popup 開啟之後；
  同頁在另一寬度（資產已暖）即全同。bt3（nginx 送資產）在 E12 同頁無此差；部署後以 bt3 重跑為準（handoff §④）。
- 兩邊 collections/all 都有 Ella `details-disclosure.js` 的 `DropdownDetails.attributeChangedCallback … reading 'button'` 例外（hoko 同款）——主題自身，非引擎。
- 報告與擷取 JSON：scratchpad `computed/report-local-<page>-<w>.md`、`hoko-*.json`／`local-*.json`（重跑：`parity_e15.sh local`）。

## 閘門（凍結 tree 後全跑，本機 2026-09-04 17:45–18:0x 本機時間；`tmp/gates_e15.py` 以 Git Bash 非 login shell＋`PYTHONIOENCODING=utf-8` 逐支執行 config/ci.rb 步驟；dev server 已停；log `tmp/gates_e15.log`）

| 閘門 | 命令 | 退出碼 | 摘要 |
|---|---|---|---|
| Setup | `bin/setup --skip-server` | exit=1 | Windows：`bin/setup` 的 `system("bin/rails db:prepare")` ENOEXEC（shebang 腳本不可直接 exec）；bootstrap 步驟非判準，CI 在 Linux 跑（同 E14b 不列） |
| Style: Ruby | `bin/rubocop` | exit=0 | 987 files inspected, no offenses detected |
| Security: Gem audit | `bin/bundler-audit` | exit=0 | No vulnerabilities found |
| Security: Frontend audit | `pnpm audit --audit-level high` | exit=1 → 重跑 exit=0 | 首次 npm registry `advisories/bulk` error 23 逾時（總交接 §1 已知抖動）；同一 tree 獨立重跑（`tmp/gates_e15_audit_retry.log`）No known vulnerabilities found |
| Security: Brakeman code analysis | `bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error` | exit=0 | Security Warnings: 0 |
| Test: Rails | `bundle exec rspec` | exit=0 | tally 未解析（runner 只留輸出尾 4000 字，尾端為 rspec-rails `have_http_status` 棄用警告的呼叫鏈）；targeted：MR1–MR4＋LC1–LC4 8 例 0 失敗、相關家族 94 例 0 失敗 |
| Test: Frontend | `pnpm test` | exit=0 | Test Files 37 passed (37)；Tests 412 passed (412) |
| Type check: Frontend | `pnpm typecheck` | exit=0 | |
| Build: Frontend | `pnpm build` | exit=0 | |
| Invariants: Exec bits | `bash scripts/check-exec-bits.sh` | exit=0 | OK |
| Invariants: Exec bit rules regression | `bash scripts/test-exec-bits-rules.sh` | exit=0 | OK |
| Invariants: Prototype lint | `python scripts/lint-prototype.py` | exit=0 | ERROR 0 / WARN 136 |
| Invariants: Lint rules regression | `python scripts/test-lint-rules.py` | exit=0 | OK |
| Invariants: Dead-control baseline | `python scripts/check-baseline-raise.py` | exit=0 | OK |
| Invariants: Doc claims | `ruby scripts/check-doc-claims.rb` | exit=0 | OK |
| Invariants: Doc claim rules regression | `ruby scripts/test-doc-claims-rules.rb` | exit=0 | OK |
| Invariants: Tenant isolation | `ruby scripts/check-tenant-isolation.rb` | exit=0 | OK |
| Invariants: Design token single source | `ruby scripts/check-tokens-sync.rb` | exit=0 | |
| Invariants: Reversal naming | `ruby scripts/check-reversal-naming.rb` | exit=0 | OK |
| Invariants: Money unit boundary | `ruby scripts/check-money-boundary.rb` | exit=0 | OK |
| Invariants: Money rules regression | `ruby scripts/test-money-rules.rb` | exit=0 | OK |
| Invariants: Limits key types | `ruby scripts/check-limits-keys.rb` | exit=0 | OK |
| Invariants: Limits key rules regression | `ruby scripts/test-limits-key-rules.rb` | exit=0 | OK |
| Invariants: Rem token integrity | `ruby scripts/check-rem-tokens.rb` | exit=0 | OK |
| Invariants: Rem token rules regression | `ruby scripts/test-rem-token-rules.rb` | exit=0 | OK |
| Invariants: CI parity (ci.yml ⊆ config/ci.rb) | `ruby scripts/check-ci-parity.rb` | exit=0 | OK |
| Invariants: CI parity rules regression | `ruby scripts/test-ci-parity-rules.rb` | exit=0 | OK |
| Invariants: Workflow syntax | `ruby scripts/check-workflow-syntax.rb` | exit=0 | OK |
| Invariants: Workflow syntax rules regression | `ruby scripts/test-workflow-syntax-rules.rb` | exit=0 | OK |
| doc-claims 兩支（本閘門表寫入後補跑） | `ruby scripts/check-doc-claims.rb`／`ruby scripts/test-doc-claims-rules.rb` | 見 PR body | |

突變輪（commit 後，scratchpad `mutate_e15.py`）：M114–M121，結果見 PR body。

## 尚未完成或需注意的風險 (Pending / TODO)

- bt3 尚未部署與對齊（合併後：`scripts/deploy.sh origin/main` → `render_parity:mirror[mirror]` → cache clear → 公開 mirror 複驗 → 8 頁三寬對表）。
- D80 未裁定：五市場後我方 hreflang 逐國展開 161 條、語言切換器 `root_url` 帶地區——`__head__`／切換器連結形差屬登記差異（e8 §3），裁定後回收。
- 91 §3.83 的 V：欧盟逐國清單、`popular?` 判準、`continent`／`unit_system` 對映、有自己網域的市場是否列入 `available_countries`、市場獨立幣別。
- 頁首互動內容（區段 fetch 的 31 國清單／語言清單）只由 LC1／LC2 以資料面對齊，HTML 段 diff 待下一包用 SRA 端點跑。
- CI `pnpm audit` 撞 npm registry 逾時時對同一 head `gh run rerun --failed`（總交接 §1）。
