# 2026-09-01 G4 步 16b：op-stack 編輯管線

## 已完成的工作 (Done)

- themeTemplateUpsert（整份 JSON 寫回＝14 §F3 儲存語義）：🔴 寫入必 touch theme
  （步 2 紅字——頁快取鍵旋轉，U3/MB2 紅證）＋樂觀鎖（templates.lock_version
  新欄；🔴 **直接賦值 lock_version 不武裝 AR 樂觀鎖**——WHERE 用載入值，顯式
  比對 client 底版＋AR 原生鎖雙層，U2 紅證）＋content 形閘＋key 逃逸拒收。
- 編輯器五原子 op（24 §3 對照表）：set-setting（string/number/boolean 控件；
  複合值唯讀待 16d）／toggle-disabled（眼睛＝隱藏不是刪除）／move 上下／
  remove／duplicate（-copy 尾碼）。add-section=16c。
- Undo/Redo＝JSON 快照棧（50 深；Save 後清空＝24 §3 語義）；dirty 指示；
  STALE_OBJECT ⇒ 衝突 toast 不清 dirty；成功 ⇒ iframe 重載。
- 突變 5/5 殺（MB5＝儲存漏未動 section 的部分覆蓋殺手格）；
  rspec 1891＋vitest 333 全綠。

## 修改的檔案與核心邏輯 (Changes)

- 新：migration（templates.lock_version）、theme_template_upsert.rb、
  theme_template_upsert_spec.rb（4 例）。
- 改：theme_type（templateLockVersion）、mutation_type、ThemeEditorPage
  （op 層＋快照棧＋save）、editor 測試 3→6 例、i18n 11 鍵 ×5、admin.css。

## 尚未完成或需注意的風險 (Pending / TODO)

- 16c＝add-section picker（sections registry＋presets）＋dnd 拖曳；16d＝30 控件
  對映表＋佈景設定（settings_data 寫回）；16e+=code editor／頁面模板管理。
- ⚠ 兩個 fixture 教訓：①內容相同的 update 是 no-op 不進位 lock（U2 首輪）；
  ②typed error code 必須在 enum 值域（STALE→STALE_OBJECT pool 碼）。
- 生產煙測：部署後編輯器親走一輪（select→hide→save→前台變）。
