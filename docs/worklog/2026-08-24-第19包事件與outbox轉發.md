# worklog：第 19 包實作（事件與 outbox 轉發）

## 已完成的工作 (Done)

- **limits 先行**：`config/limits.yml` 新增 `events:` 五鍵（poll 5s／batch 100／dead 8／
  retention 30d 皆抄 specs/18 F1；lock_timeout 300s＝假設 A1）；poll interval 鍵旁註明
  與 `config/recurring.yml` 的雙寫點。
- **migration**：`event_outbox` 加 `dedupe_key`（VARCHAR(191) NULL）＋`coalesced_count`
  （INT NOT NULL DEFAULT 1）＋`(shop_id, dedupe_key)` 唯一索引（查法
  `grep -n uq_event_outbox_dedupe_key db/schema.rb`）。
  🔴 已在**有資料的本機 dev 庫**實跑（#113 教訓；本 migration 無資料狀態分支，
  strong_migrations 未要求 safety_assured）。
- **形狀裁定落地**：`EventOutbox::STATUSES` 加 `dead`＋`TERMINAL_STATUSES`；
  `Events::Topics` 常數表（對外 7＋內部 5；einvoice 留註不留值）。
- **產生端兩處**：`Catalog::SaveProduct` payload 升級 63 §C.2 形（resource_version＝
  lock_version、changed_fields 濾 NOISE_FIELDS、只有欄位名沒有值）；status 變更集中走
  新建的 `Catalog::StatusTransition`（13 §F1.2(e) 一處實作——事件面）補
  `product.publication.changed`。`Inventory::Adjust` 在既有 transaction 內掛
  `inventory.adjusted`（availability_flipped 在算 leaf 新值同段算；合併窗
  upsert＋豁免筆 dedupe_key=NULL；ledger 不動）。
- **`Events::Relay`**：SKIP LOCKED 取件（短 transaction，投遞在鎖外）、locked_at 逾時
  回收、指數退避、attempts≥8→dead、終態清 dedupe_key（release-on-terminal）、
  A 案租戶模式（without_tenant 取件、逐店 with_tenant）、結構化 log。
  `Events::RelayJob`（every 5 seconds）＋`Events::OutboxPurgeJob`（每日 4am，
  刪保留期外 published/dead）掛進 `config/recurring.yml`。
- **測試骨架**：`config/environments/test.rb` 明文 `queue_adapter = :test`。
- **文檔回寫**：28 §15 補內部 topic 清單（§4.4 要求）；p19 規格兩條實作期更正註
  （dedupe_key 加時間桶尾碼、adjustment payload 以 quantity_name 取代
  from_state/to_state/sub_type——Move 未做，無轉移對）。
- **測試**：新增 25 例（relay 8＋併發 1＋model 4＋產生端 11＋purge 1）。
  全套：rspec **482/482**、vitest 60/60、typecheck 綠、rubocop 0、brakeman 0、
  tokens-sync 綠、lint-prototype ERROR 0。
- **守衛注入回歸**（總交接 §6）三件，全部紅→還原→綠：
  ①拿掉 publish 清 dedupe_key ⇒ 2 紅（release-on-terminal 兩條 spec）
  ②拿掉 claim 的 locked_at 標記 ⇒ 1 紅（併發雙 worker 重複取件）
  ③availability_flipped 恆 false ⇒ 4 紅（豁免與三態 spec）。
- **bt3 歷史 pending 量測**（§4.6；rails runner 實測 2026-08-24）：
  `{["pending","products/create"]=>3, ["pending","products/update"]=>5}`＝共 **8 筆**，
  照假設 A5 由 relay 安全 drain 成 published（零消費者＝無副作用）。

## 修改的檔案與核心邏輯 (Changes)

| 檔 | 變更 |
|---|---|
| `config/limits.yml` | `events:` 五鍵 |
| `db/migrate/20260824140000_add_coalesce_columns_to_event_outbox.rb` | 新建 |
| `app/models/event_outbox.rb` | STATUSES＋dead、TERMINAL_STATUSES |
| `app/services/events/topics.rb` | 新建（常數表） |
| `app/services/events/relay.rb` | 新建（核心） |
| `app/jobs/events/relay_job.rb`／`outbox_purge_job.rb` | 新建（薄包裝） |
| `config/recurring.yml` | events_relay（5s）＋events_outbox_purge（每日） |
| `config/environments/test.rb` | queue_adapter :test |
| `app/services/catalog/status_transition.rb` | 新建（一處實作，事件面） |
| `app/services/catalog/save_product.rb` | payload 63 §C.2 形＋topic 常數＋StatusTransition |
| `app/services/inventory/adjust.rb` | enqueue_adjust_event!（合併窗＋豁免＋同 transaction） |
| `spec/services/events/`（3 檔）＋`spec/models/event_outbox_spec.rb` | 新建 25 例 |
| `spec/services/inventory/adjust_concurrency_spec.rb` | purge 補 EventOutbox（新 FK） |
| `docs/research/28-api-contract.md` | §15 內部 topic 回寫 |
| `docs/plans/2026-08-24-第19包執行規格.md` | 兩條實作期更正註 |

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **觀測明文降級**：specs/18 F1-5 的「告警」與 63 §C.3 的 metric 不在本包，
  現值＝結構化 log（`event=outbox_published|outbox_retry|outbox_dead|outbox_reclaimed`）。
  接消費者或觀測基建的包補。
- 🔴 **內部 `product.updated`／`product.variant.updated`／`inventory.level.changed`
  只有常數留位、本包不發**（63 §C.1 的完整事件族在接消費者的包啟用；
  前置＝`event_deliveries`，63 §L-4 門檻）。
- `dead` 列目前只能靠 DB 改回 pending 重推（specs/18 F1-5 的「後台可重推」UI 不在本包）。
- **兩個工程坑本輪實踩，登記**：①`update_all` 繞過 dirty-tracking——舊物件再 assign 回
  載入值會被 AR 判無變更而漏出 UPDATE（解法＝claim 後重讀乾淨物件）；
  ②**`git checkout <file>` 對 untracked 新檔無效、對 tracked 檔會把整輪未 commit 的
  補丁一起洗掉**——注入回歸的還原一律用反向字串替換，不用 checkout。
- 線上三觀測（§1 可見成果）在部署 bt3 後執行，結果貼 PR；歷史 8 筆 pending 的
  drain 結果一併記錄。
