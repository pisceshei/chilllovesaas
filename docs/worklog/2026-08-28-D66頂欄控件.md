# 2026-08-28 — D66 頂欄內部控件逐項對齊本尊

`docs/specs/91` §3.39 W-2 登記的「已量未實作」那一項。🟢 純 CSS，未動 markup，不觸鐵律 18.3。

## 已完成的工作 (Done)

### ① 取證（本機 Chrome，viewport 2560，暗色域內，擴充功能污染已停用）

逐控件量到本尊的值，並確認**顏色全部對得上暗色域 token**
（#282828＝surface-2、#2f2f2f＝surface-3、#dcdcdc＝icon、#aaa＝text-2）
⇒ 我方一律寫語意名，不寫色值。完整表在 `docs/DECISIONS.md` D66。

### ② 兩處更正

- 🔴 `91` §3.39 W-2 記的「圖示鈕 18×18」是**量錯節點**——那是圖示不是鈕，鈕是 **36×36**。
- 🔴 搜尋鈕的 **545 不是控件上限**，是與「View as」鈕分掉中央 640 槽的結果 ⇒ 取槽寬 640。

### ③ 改了四個控件（原型與 `admin.css` 同步）

搜尋觸發鈕、快捷鍵標籤、圖示鈕、商店 chip（＋ chip 內頭像）。

刪掉的硬編值：`#c9cace`（hover 邊色，直接硬編色）、`34px` 高、`9px` 圓角、`11px`、
`600` 字重、`24px` 頭像、`7px` 頭像圓角、`600px` 上限。

🔴 **`10px` 保留為字面值**：本尊的 kbd 是 10px，而**兩邊的字級階最小都是 11px**
（本尊 `--p-font-size-275` ＝ .6875rem ＝ 11px）。這是本尊的階外值，
照實記錄、不硬湊到 11；不建 token（單一用途，建了是死 token）。

🔴 **頭像 28×28 只加在 chip 內**：`.avatar` 是通用類（原型 markup 內 9 個用點），
直接改會把列表、顧客卡一起放大。

🔴 **不照抄本尊搜尋標籤的 `font-weight: 400`**——那是 UA button 預設漏出（該 span 沒設 weight，
同鈕 computed font 讀出 `13.3333px/normal/400/normal`）。400 不在我方值域 ⇒ 用 `--fw-regular`（450）。

### ④ 驗證

**原型與 `admin.css` 各自量一次**（不是量一邊推論另一邊），1280 下五個控件九個軸逐項相同，
且與本尊相符：搜尋鈕 640×36 / r12 / bd 0 / rgb(40,40,40) / rgb(220,220,220) / 13/20；
kbd rgb(47,47,47) / rgb(170,170,170) / 10/16/550 / r4 / 2px 4px / bd 0；
圖示鈕 36×36 / r12 / 透明 / rgb(220,220,220)；chip 高 36 / r12 / bd 0 / 透明 / 12/16/550；
頭像 28×28 / r8。

768 與 390：兩份皆**溢出 0**，頂欄高 56、底 rgb(10,10,10) 不變。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `docs/design/chilllove-admin-v2.html` | `.searchbox`／`.searchbox:hover`／`.kbd`／`.icon-btn`／`.store-chip` 五條規則改寫（含取證註釋）；新增 `.store-chip .avatar` 一條 |
| `app/assets/stylesheets/admin.css` | `.cl-search-trigger`／`:hover`／`kbd`／`.cl-icon-button`／`.cl-store-chip`／`.cl-store-chip__avatar` 同一組 |
| `docs/DECISIONS.md` | D66 |
| `docs/specs/91-pit-register.md` | §3.40（W-1…W-7） |

`app/assets/tokens.css` **未變動**（本包沒有新增或修改 token，`check-tokens-sync.rb` 仍逐位元組相同）。

## 20.2 適用類型／固定處理／反向複驗輸出

| 20.2 類型 | 適用 | 固定處理已跑 | 反向複驗輸出 |
|---|---|---|---|
| ① 證據不完整 | 是 | 值一律取自本尊 computed；未量到的（hover、搜尋鈕內部節奏）明寫「未取得」，不推測 | `91` §3.40 W-3／W-7 |
| ② 生產者已改、消費者未同步 | 是 | 原型與 `admin.css` **同一提交**改同一組控件，且**各自量測驗收** | 兩份輸出九個軸逐項相同 |
| ③ 易腐數字／全稱句 | 是 | 「刪掉的硬編值」用列舉不用計數；本尊值附取證條件（視口、暗色域、污染已停用） | `check-doc-claims` exit 0 |
| ④ 狀態空間漏分 | 是 | `.avatar` 是通用類 ⇒ 檢查全檔用點（9 個）後才決定只在 chip 內覆寫；行動覆寫區塊（≤760）另外驗 390 | 390 溢出 0 |
| ⑤ fail-open | 是 | 不只驗「改了」，驗**兩份檔各自的 computed 與本尊逐軸相同**；並在 768／390 驗溢出 | 上表 |
| ⑥ workflow 假綠 | 否 | 未動 `.github/` | — |
| ⑦ Markdown／Windows | 是 | 改檔腳本寫成檔案再跑；先 `encode()` 再 `open(...,"wb")` | 無截斷事故 |

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **本尊頂欄是三欄 grid（`1fr 640px 1fr`），我方仍是 flex**（`91` §3.40 W-1）。
  ⇒ 我方搜尋框的中線是「剩餘空間的中央」，不是視口中央；左右內容不等寬時會偏。
  改它要動 markup（包三個 zone div），且**本尊窄寬度的分欄行為未取得**。
- 🔴 **hover 態全部未取得**（W-7）——三個控件的 hover 底色都是沿用既有值，不是對齊結果。
- **搜尋鈕內部圖示／間距節奏未取得**（W-3），右內距本尊 10、我方 12。
- 🔴 **頭像底色兩份檔不一致**（W-4）：`admin.css` 實色、原型漸層。本尊是每店生成色，
  那是一個功能不是一個值。
- **行動覆寫是 px 斷點 `max-width: 760px`**（W-5），違反 `48`「三條硬規則」的 em 要求，
  且與 767.9975 差 8px。改它要重驗窄版形態，而本尊窄版未取得。
- **左區資訊架構不同**（W-6）：本尊左區是平台 logo、店名在右 chip；我方左區是品牌名 ＋ 版本 pill。
  鐵律 9 禁用本尊 logo ⇒ 不能照抄，要先裁定我方的頂欄身分擺法。
