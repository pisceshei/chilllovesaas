# PR #65 第十三輪 Claude 驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `cf9fa514244d54b8c012fb4b1d93bc471c937522`：Claude 判詞
  `5364105225`、wrapper `5364106908`，結論為 🟡1／⚪1；quality／test 雙綠不能代替當時尚未回覆
  current head 的 Codex，也沒有把前一 head 的結果外推。送驗前 Iron 15 後來攝取到 Codex
  review `4988929408` 與 inline `3826579181`／`3826579184`／`3826579188`，也都綁同一 head。
- 依鐵律 16 以 `git log -p` 追 `active_markdown_lines` 的 block／inline 分流沿革；再以 CommonMark
  0.31.2 §4.6 <https://spec.commonmark.org/0.31.2/#html-blocks>（取證 2026-08-21）核對逐字
  "The block begins with a line that meets a start condition (after up to three optional spaces of
  indentation)." 與 "Start condition: line begins with the string `<!--`."。判準是 raw line，不是
  `visible.empty?` 或遮罩後餘段。
- failure-first 複驗證實：`- type: count <!-- a`／`--> qualitative` 在舊 head exit 0；
  closing-line reopen case 雖 exit 1，診斷值卻是空字串而不是仍可見的 `qualitative`。GitHub 官方
  Render a Markdown document endpoint
  <https://docs.github.com/en/rest/markdown/markdown#render-a-markdown-document>（取證
  2026-08-21）對同一跨行輸入的 raw response 含 `<li>type: count  qualitative</li>`，證明換行
  不能把 suffix 從同一可見 metadata 值中丟掉。
- 修法只改被點名的 parser 射程：每個 raw line 先算一次 block start，只有第一次掃描的 opener
  可啟動 block；inline comment state 保存 prefix 與原始行號，跨 raw line 收尾後把 suffix 接回
  同一邏輯活性行。四支跨行／reopen 正反 fixture 與直接抽取生產 helper 的五條 parser probe
  已實跑；runner 實跑輸出快照為 46 個 fixture 全合預期、5 條 probe 全合預期、3 個 git 情境與 2 個供給
  情境全綠。
- Claude ⚪「`recheck*` 診斷射程與 `type*` 不對稱」只登記到 91 §3，不改 checker。內部全掃另
  發現本 worklog 前一輪 20.3 表頭少「輸出」二字；`git log --follow -p` 證明由 `cf9fa51`
  首次寫入，依 17.2 也只登記、不回寫歷史表格。
- 晚到 Codex 意見逐一處置：W11 Changes 以相鄰更正列出 fixture 名稱；91 的尾端空白 pit 依
  current matcher、`.strip` 與 `git log -p` 改標已撤回；CLAIM-003 recheck 除了重算 71，現在也
  解析並比對 84 開頭發布的不同口徑、混合狀態數與 ID。把 84 的發布值在記憶體改成錯值時，
  新比較會以 `published split drift` 非零退出。
- 本輪最後一版 repo edit 後完整 Windows production-equivalent runner 實跑輸出（取證
  2026-08-21）為 `GATES_ALL_GREEN=29`：Rails 284 examples／0 failures、前端 5 tests／0
  failures、RuboCop 162 files／0 offenses、Brakeman 0 errors／0 warnings、doc-claims warning 0；
  Node v24.19.0 高於專案宣告的 Node 22 範圍，只記環境邊界，不外推為受支援版本。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 第十一輪修 block closing suffix、第十二輪修單行 inline suffix；Claude `5364105225` 證明同一 parser 的跨 raw-line state 與 raw-line start condition 仍未納入；Codex `3826579188` 證明 CLAIM-003 仍只驗 producer、不驗 consumer |
| 既有防線失效原因 | fixture 只枚舉 opener／closer 在同一行；`visible.empty?` 與 masked 餘段被誤當 raw-line position，跨行 inline state 只保存 block flag／開啟行號；CLAIM-003 只硬斷言來源衍生值，沒有解析 84 的發布值 |
| 被漏掉的固定步驟 | 逐行 parser 的 start condition 必須在未遮罩 raw line 上判定且每行只判一次；inline terminator 需測同列、跨列、closing-line reopen 與 raw-code-span prefix；宣稱命令須同時核對 producer 與列入 sources 的 consumer |
| 可重跑反向複驗 | `ruby scripts/test-doc-claims-rules.rb`；跨行畸形 1、跨行合法 0、reopen 畸形 1 且值為 `qualitative`、reopen 合法 0；production helper probes 輸出固定；CLAIM-003 對 84 錯值 mutation 以 `published split drift` 非零退出 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取 | REST 全端點、review body、paginated inline 與 GraphQL threads 綁 exact head | Claude comments、Codex review `4988929408` 與 inline `3826579181`／`3826579184`／`3826579188` 均綁 `cf9fa51`；晚到結果先處置再推 |
| ② producer／consumer／終態 | checker、runner、fixture、external-facts、m0、W11／W12 相鄰更正、91／92／84／A.1 同步 | `rg` 全掃 block start、inline multiline、comment ID、新增 fixtures 與 CLAIM-003 sources；被點名的契約與終態 inventory 均有現行更正 |
| ③ 完成性聲明 | 只發布已實跑的 failure-first、GitHub raw response與 runner 輸出 | 舊碼跨行畸形 0；修後 runner 46／46、parser probes 5／5，最後 repo edit 後 `GATES_ALL_GREEN=29` |
| ④ 狀態空間 | 同列 inline、跨列 inline、closing-line reopen、raw code-span prefix、縮排 block start、raw block closing suffix分格 | 新四 fixture 各守拒絕／放行方向；五條 probe 直接執行抽出的生產 helper，不複製 parser |
| ⑤ fail-open | metadata 的 prefix／suffix 必須重組；代理行首不得把可見 suffix 丟掉；consumer 漂移不得被 producer 硬編值遮住 | 拒絕 needles 均命中且合法 cases exit 0；84 發布值 mutation 令 CLAIM-003 非零 |
| ⑥ workflow 本機假綠 | 本輪不改 workflow；script 包仍須 exact-head CI 與兩方獨立驗收 | #65 命中 18.3，即使雙零也只通知使用者人工合併 |
| ⑦ Markdown／Windows | exact request 用雙反引號包內含 code span 的 JSON；表格裸直線、GitHub render、UTF-8 與完整 Windows runner 分別複驗 | 完整 Windows runner `GATES_ALL_GREEN=29`；Markdown 結構另於送驗前以 GitHub API 複驗 |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：以 raw-line start condition 取代 visible／masked 代理；inline comment
  保存跨行 prefix／原始行號並於收尾重組 suffix，block closing suffix 仍維持不可見。
- `scripts/test-doc-claims-rules.rb` 與四組新 fixture：加入跨行 inline、closing-line reopen 的正反格；
  直接抽取生產 helper 跑四條 parser probe，fixture 下限只往上調。
- `docs/dev/external-facts.md`、`docs/dev/m0-review-convergence.md`：終態契約收窄到 raw-line 判準，
  保存跨行 GitHub raw response、fixture 表與 mutation／probe 射程。
- `docs/worklog/2026-08-21-PR65第十二輪Codex驗收修復.md`：保留原歷史 bullet，追加有日期與
  comment ID 的相鄰更正。
- `docs/worklog/2026-08-21-PR65第十一輪雙驗收修復.md`、`docs/specs/91-pit-register.md`：相鄰
  更正手抄 fixture inventory；撤回已由 `.strip` 消除的假 pit，未點名項仍只登記。
- `docs/specs/92-claim-index.md`：CLAIM-003 同時重算 71 producer 與解析 84 consumer 發布值；
  A.1 納入本 worklog。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本輪最後 repo edit 後已取得 `GATES_ALL_GREEN=29`；若 commit 前再改任何 repo 檔，必須從
   第一道重跑，不得把本次結果外推到新內容。
2. commit 後須重跑 doc-claims warning 0、六條 CLAIM 原文 recheck、鐵律 15.1／15.2 全端點；
   push 後只接受 exact new head 的 Claude／Codex 結果。
3. #65 修改 `scripts/`，命中鐵律 18.3；即使 exact-head 雙零、approved threads 全收口與四條件
   齊全，也只能通知使用者人工合併，互動式 Codex 不得代行。
