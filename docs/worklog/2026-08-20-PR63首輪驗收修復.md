# 2026-08-20 — PR #63 首輪 Codex 驗收修復

## 已完成的工作 (Done)

對 current-head `ec4eb1d7f4c91cb2ca091bc3062340fe15028cf7` 的 Codex review
`4984000690`，已按鐵律 15.2 全量拉取 issue comments、review body、paginated inline 與
GraphQL threads；`hasNextPage=false`，共有兩條未解 inline：

1. `3822600825` 要求提交 `docs/handoff/`。事實層面正確描述了 `main` 現行 AGENTS.md，但修法
   與使用者 2026-08-20 最新明示裁定衝突。PR #62 current-head `5209087` 的
   `docs/DECISIONS.md` D36 已逐字落籍「handoff 只存 Git 倉庫外本地工作區，不 commit／push」；
   同 head 的 AGENTS.md／CLAUDE.md 已把逐步入庫制改回工作單位節奏。#63 是為了解除 #62 的
   Fable 5 quota 阻塞而先行的 workflow-only PR，若反向提交 handoff，會直接違反本輪使用者
   指令並讓 #62 再刪一次。故本條裁定不修，落點為 PR #63 描述與 inline 回覆；本輪四段 handoff
   已依 D36 保存在倉庫外本地，不加入 Git index。
2. `3822600830` 指出主 worklog 的「其餘 workflow 行為全部不動」是無邊界全稱句。查證成立。
   原句依歷史層規則保留，已在原處追加日期更正，將可證範圍限為 `1800b20..ec4eb1d` 的
   workflow 文字 diff，並附可重跑命令，明示不外推 action 執行期語義。

修復後的第一輪 production 等價 runner 如實得到 `GATES_TOTAL=29`、`GATES_PASSED=22`、
`GATES_FAILED=7`：命令包裝層吃掉 Windows 路徑的反斜線，使裸 `bash` 落到 WSL，且 bundled
Python 絕對路徑失效；失敗集中在兩項 shell、三項 Python 與兩項依賴 bash 的 workflow gate，
不是受驗文件的內容失敗。該輪未被宣稱為通過，也沒有只補跑七項。改用已先以 `Test-Path` 與
`Get-Command` 證明存在的 Git for Windows Bash／bundled Python 絕對路徑後，從四項 setup
重新完整執行，實得 `GATES_TOTAL=29`、`GATES_PASSED=29`、`GATES_FAILED=0`：Rails
`284 examples, 0 failures`、Vitest `5 passed`、RuboCop 162 檔／0 offenses、Brakeman 0 error／
0 warning、prototype 合計 `ERROR 0 / WARN 136`、doc-claims warning 0、workflow 2 份 YAML／
33 個 `run:` 區塊與 11 條回歸均通過。Node 實跑 v24.19.0 超出專案 `>=22.12 <23`，這批綠燈
只證明本次環境結果，不外推 Node 24 支援。

提交 `1152ee6` 後實跑 doc-claims：exit 0，但本檔原表格摘要在 line 35 產生 R5 warning 1。
摘要已改為直接指向上方的 immutable diff 與過濾命令，不在表格另寫超出該命令證據範圍的結論；
本次 warning 修正必須另 commit，並重新取得 commit 後 warning 0 的輸出才可 push。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `docs/worklog/2026-08-20-Claude-Fable5額度回退Opus5.md` | 保留原句並追加日期更正；可證範圍、immutable base/head diff 與過濾命令見本檔 Done §2 |
| `docs/worklog/2026-08-20-PR63首輪驗收修復.md` | 記錄兩條 current-head input、逐條查證、D36 過渡期裁定不修與實際修復 |
| `docs/specs/91-pit-register.md` | 附錄 A.1 同 commit 補列本驗收修復 worklog；本輪沒有新增坑項 |

## 尚未完成或需注意的風險 (Pending / TODO)

1. 上述結果回填後必須再從 setup 起重跑最終完整 29 項、commit、commit 後 doc-claims，再 push
   一次新 HEAD；此前 `ec4eb1d` 的 CI／Codex 結果不得冒充新 HEAD 結果。
2. `3822600825` 是使用者裁定與尚未合併的倉庫修法之間的過渡衝突，不是機械修復。#63 命中
   鐵律 18.3，current-head CI、Codex 與 validation-skip 證據完成後仍須由使用者獨立審查並人工合併。
3. #63 合併後，#62 要一次性更新到新 `main` 並與它自己的兩條 Codex inline 批次修復，產生
   一個新 HEAD 後重跑 CI、Codex 與 Opus Claude canary；不得沿用 Fable 失敗輪或舊 review。
