# worklog：第 22 包（productSet options 樹＋兩階段 diff＋initialQuantities）

## 已完成的工作 (Done)

- **`Catalog::VariantSync`**（63 §B.4/§B.5 引擎；在呼叫端 transaction 內執行）：
  選項樹宣告式同步（name 對應、值「刪＋加」語義、position 重排）→
  **階段 A 投影**（既有變體補新選項第一值；隱含變體升級＝保留原 id）→
  **階段 B 比對**（id 優先＝`variant_identity_id_wins`；無 id 以**投影後** digest；
  未列出＝DeleteVariant——63 §B.4 硬規則 1）→ title 生成（B3：值依選項 position
  join " / "，改值同 transaction 重算）→ initialQuantities（create-only、
  走 `Inventory::Adjust` 衍生鍵——D43 的唯一入口紀律，複驗 `grep -rn EventOutbox.create app/services/inventory`＝零直寫）。
- **SaveProduct 接線**：解除單筆限制（有 options 走新路、無 options 原判準不動）；
  normalize 多變體列（金額錯誤帶正確列 index）；`VariantRejected`／
  `InitialQuantityRejected` 例外鏈轉 userErrors。
- **GraphQL 面**：ProductSetOptionInput／VariantOptionValueInput／
  InitialQuantityInput＋variants[].id；三策略 enum 曝露（B5——schema 契約先定，
  productOption* mutation 後補）；error enum 補 LAST_VARIANT_REQUIRED／
  OPTIONS_OVER_LIMIT（VARIANT_LIMIT_EXCEEDED 已在共用池）。
- 五語 locale ×9 鍵；spec 新增 5 例（含 63:1141 靈魂測試：加選項後原 variant.id
  與 inventory_item.id 完全相同、ledger 連續）。
- **注入回歸**：模擬「跳過階段 A 直接比 digest」（63:311-316 唯一事故形態）⇒
  63:1141 測試紅（id 被換掉如規格預言）→ 還原綠。
- 全套：rspec **499/499**、vitest 60/60、typecheck、rubocop 0、brakeman 0。

## 修改的檔案與核心邏輯 (Changes)

| 檔 | 變更 |
|---|---|
| `app/services/catalog/variant_sync.rb` | 新建（引擎本體） |
| `app/services/catalog/save_product.rb` | 分流接線＋多列 normalize＋例外鏈 |
| `app/graphql/types/inputs/`（3 新檔＋2 擴充） | options 樹／座標／initialQuantities |
| `app/graphql/types/product_option_*_strategy_enum.rb`（3 檔） | 三策略 enum 曝露 |
| `app/graphql/types/errors/product_set_user_error_code.rb` | ＋2 碼 |
| `config/locales/*.yml`（五語） | ＋9 鍵 |
| `spec/requests/product_set_variants_spec.rb` | 新建 5 例 |
| `spec/requests/{product_set,variant_read_surface}_spec.rb` | 常數改名（見下） |

## 尚未完成或需注意的風險 (Pending / TODO)

- **四個實作中自抓的坑，登記**：
  ①`ActiveRecord::Rollback` 在巢狀服務裡會被外層 transaction **靜默吞掉**
  （回成功、資料已滾）——一律用帶 userErrors 的自訂例外上拋；
  ②position 重排逐列 update 會撞 uq(shop,product,position) 唯一索引（整合規格 §1.4 同型；複驗 `grep -n uq_product_variants_product_id_position db/schema.rb`）——兩階段落位已套用
  在 matched 變體，🔴 **sync_options!/sync_values! 的 position update 尚未套**
  （選項重排走到才會撞，第 23 包拖曳前必補——登記為 23 的前置檢查項）；
  ③`OptionValuesDigest` 收 `[[option_id, value_id]]` 對陣列不是 hash（rspec 抓）；
  ④🔴 **spec 頂層常數跨檔污染**：兩檔都定義 `MUTATION` ⇒ 後載蓋先載、單獨跑綠
  全套跑紅（症狀＝nil.sole）——spec 頂層常數一律帶檔案前綴。
- 值改名（Red→Crimson）在宣告式下＝刪＋加（無法與真刪區分）——引用被刪值的變體
  被視為未列出而刪除。第 23 包 UI 若要支援「改名保變體」需 optionValuesToUpdate
  型 API（productOption* 後補射程）。
- productOption* 四支獨立 mutation（B2 後補）；三策略 enum 已曝露未掛 args。
