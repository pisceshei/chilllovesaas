# PR #64 第九輪 Codex 驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `fd90a125d2b0f3f6ddde5f99ada8f913f49f6d18`：Codex review
  `4987319876` 的唯一新 inline 為 `3825320726`。同 head quality／test completed success；Claude
  Review run `32416842072` 仍在進行，因此本 worklog 不把 Claude 狀態寫成已取得。
- 修法前沿 `git log -p` 核對第五輪至第八輪 validator 演進；failure-first 以第八輪 Codex worklog
  為 target，模擬刪除該檔並刪 A.1 列，舊 expected／listed 仍相等，實跑輸出：

~~~text
DELETE_TARGET=docs/worklog/2026-08-21-PR64第八輪Codex驗收修復.md
OLD_DELETE_SURVIVED=true
OLD_RENAME_HAS_GUARD=false
~~~

- 第五輪 worklog 保留第八輪 block 作歷史層，新增第九輪現行入口。現行 validator 先讀
  `git diff --name-status --find-renames`；任何 D／R 狀態碰到 PR64 驗收 worklog 即 fail-closed，
  再執行既有 AM 集合與 A.1 multiplicity／集合檢查。
- delete 與 rename 分別有獨立 mutation；兩者必須命中 `historical PR64 worklog deleted or renamed`。
  P-8 終態入口同步到第九輪 heading，沒有手抄輪次集合。
- 本輪 Codex 沒有新範圍外觀察；舊 unresolved threads 依 createdAt／ID 可辨為歷史輸入，不誤算成
  current review 新意見，也不在 exact-head 雙零前提前 resolve。
- targeted validator 的正常集合為 10 份；duplicate、drop、missing-section、off-date、delete、
  rename 六條 mutation 全數命中各自拒絕原因。GitHub `/markdown` API 實際渲染四份變更：
  第五輪 worklog 為 table 2／tr 13／th 5／td 29／pre 4，本檔為 2／12／5／26／1，91 為
  3／25／11／72／2，三者 raw fence 都是 0；P-8 沒有表格或圍欄，符合正文結構。
- 最後回寫前的第一輪 production-equivalent runner 已從 setup 起完整輸出
  `GATES_ALL_GREEN=29`：Rails 284 examples／0 failures、frontend 5 tests／0 failures、RuboCop
  162 檔零違規、Brakeman 0 errors／0 warnings、兩種 audit 零漏洞、prototype ERROR 0，
  doc-claims 掃描 296 檔且 warning 0。此 bullet 是其後的 repo edit，不能拿該輪冒充最終閘門。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 第五輪曾漏 multiplicity、第七輪漏 HEAD 邊界、第八輪漏跨日期 producer；本輪 `3825320726` 又指出 `--diff-filter=AM` 漏 D／R，仍是 producer/consumer 同時消失的對稱假綠 |
| 既有防線失效原因 | 狀態空間只列「檔名是否符合 pattern」與 A/M，沒有把 Git change status 的 A／M／D／R 分開；集合相等無法辨識兩側同時刪除 |
| 被漏掉的固定步驟 | 歷史資產 validator 除集合與 multiplicity 外，必須另列 destructive status；D／R 不進正常集合比較，而是先行 fail-closed |
| 可重跑反向複驗 | 第五輪 worklog 的第九輪現行 block；delete 與 rename mutation 分別注入 `D`／`R100` name-status 記錄，任一存活即整支非零 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① exact-head 驗收攝取 | review body、paginated inline、threads 依 ID／createdAt 去重 | current 新意見只取 `3825320726`；Claude 明標仍在進行 |
| ② producer／consumer／終態 | 第五輪 validator、P-8 pointer、91 A.1 與本 worklog 同步 | pointer 精確指第九輪 heading；A.1 補本檔；歷史 block 不覆寫 |
| ③ 狀態空間／multiplicity | A／M 正常集合、D／R destructive guard、A.1 重複／缺項各自分支 | duplicate、drop、missing-section、off-date、delete、rename 各有獨立 mutation |
| ④ failure-first／happy path | 舊洞先證明存活；修後 normal 集合仍須通過 | `OLD_DELETE_SURVIVED=true`；第九輪 block 最後列出 normal 集合 |
| ⑤ 完成性聲明 | 不宣稱 Claude 已完成；不把歷史 unresolved thread 當新意見 | PR 描述須等新 head 後按實際狀態重建 |
| ⑥ Markdown／Windows | 中文路徑固定 `core.quotepath=false`；name-status 以 tab 欄位解析 | GitHub render 與完整 Windows runner 待最後 repo edit 後執行 |

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-21-PR64第五輪雙驗收修復.md`：新增第九輪現行 validator、D／R guard
  與 delete／rename mutations。
- `docs/worklog/2026-08-20-P8證據來源與合併後文件債收斂.md`：終態 pointer 改指第九輪
  heading，明列 destructive status mutations。
- `docs/specs/91-pit-register.md`：A.1 補本 worklog，未新增範圍外坑項。
- `docs/worklog/2026-08-21-PR64第九輪Codex驗收修復.md`：保存 exact-head 輸入、failure-first、
  20.4／20.3 與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. Claude Review run `32416842072` 尚未完成；push 前鐵律 15.2 必須再次全量重拉，若有晚到判詞
   先合併處置，不把本 worklog 當「本輪全部輸入已收齊」。
2. 本段實測回寫是最後 repo edit；依鐵律 15.4 必須從 gate 01 重跑完整 29 項，commit 後再原封
   不動執行第九輪 HEAD validator 與 doc-claims warning 0，才可依鐵律 15 決定是否 push。
3. 新 head 推送後須重新取得 CI、Claude、Codex；舊 head `fd90a125…` 的結果不得外推。
4. 本機 Node v24.19.0 超出 `package.json` 宣告的 `>=22.12 <23`；本輪前端實跑成功不構成
   專案已支援 Node 24 的發布宣稱，GitHub CI 仍以倉庫指定 Node 22 為準。
