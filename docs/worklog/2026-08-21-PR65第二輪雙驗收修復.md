# 2026-08-21 — PR #65 第二輪 Codex／Claude 驗收修復

## 已完成的工作 (Done)

- 驗收基準固定為 head `31223621cb9c6b9cc12f1ffaf421317c33d2ffbc`，並全量讀取三端點：
  Codex review `4985525215` 的 inline `3823799558`／`3823799562`／`3823799568`／
  `3823799573`／`3823799574`／`3823799581`，以及 Claude issue comment `5359428067`。
- R6 先以倉庫外最小 fixture 重現縮排標頭、重複 ID、fenced code／HTML comment 三類錯誤
  exit 0，再改為只解析活性 Markdown、接受 CommonMark 的 0–3 個前導空格並阻擋重複 ID。
  回歸入口為 `ruby scripts/test-doc-claims-rules.rb`；2026-08-21 定向實跑快照為
  19 個 fixture case、3 個 git 情境與 1 個供給情境全綠。
- `docs/worklog/2026-08-21-P2宣稱索引與R6.md` 的可見 blockquote 更正改為規定 HTML comment；
  GitHub Markdown 衍生計數因 exact response artifact 未保存而標「未取得」，閘門數字標成
  dated terminal snapshot 並附導出入口，均不外推 current HEAD。
- 本 worklog 以 GitHub `POST /markdown` 的 `@file` 輸入實際渲染；2026-08-21 終端快照為
  `table=2 / tr=10 / th=5 / td=21`，每列未跳脫直線數也符合各表欄數。API response body 未另存，
  所以此處只記可重跑快照、不外推未來 HEAD；複驗入口：
  `$h=gh api -X POST markdown -F 'text=@docs/worklog/2026-08-21-PR65第二輪雙驗收修復.md' -f mode=gfm; 'tr={0} th={1} td={2}' -f ([regex]::Matches($h,'<tr\b').Count),([regex]::Matches($h,'<th\b').Count),([regex]::Matches($h,'<td\b').Count)`。
- `docs/specs/83-admin-1to1-audit-round3.md` §0 總表把 P0-14／P0-18 移出已修復欄、明列部分；
  CLAIM-002 的命令同步斷言總表與 §1.1 狀態一致。CLAIM-001 同時斷言計算值與 50 號實際發布的
  表 3／合計儲存格；兩條命令已實跑輸出 `summary_synced: true`、`table_3: 25`、`total: 313`。
- current-head Claude 的範圍外觀察與同型反查只登記到 `docs/specs/91-pit-register.md` §3：
  71 號舊狀態副本、m0-review-convergence 表格續行、首輪 worklog 證據快照，以及 R6 尚未點名的
  多 `type:`／production ROOT 供給洞；沒有擴改那些位置。
- 2026-08-21 commit 前終端快照：在 Git Bash、Ruby 3.4.10、本機 MySQL 與 UTF-8 Python
  可見的本機外層，從 setup 起連續兩輪得到 `GATES_TOTAL=29 / GATES_PASSED=29 /
  GATES_FAILED=0`；Rails 為 `284 examples, 0 failures`、前端為 `5 passed`、R6 回歸為
  整份 suite 全綠。閘門總數的可重跑導出仍是
  `Select-String '^\s*step ' config/ci.rb` 現值扣除 setup 一項，再加 Windows 拆出的四個 setup
  動作；Node v24.19.0 超出專案宣告的 Node 22 範圍，不由本次實跑外推為支援。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 前輪 Claude comment `5359074865` 🔴-1 點名 m0-review-convergence 同檔消費者漏同步；本輪 comment `5359428067` 🔴-1 再點名 83 §0 總表漏同步 |
| 既有防線失效原因 | 前輪 20.3 的 ② 列只反查 checker／docs-dev，CLAIM-002 命令也只切 §1.1；兩者都沒把被改的 83 §0 總表納入閉合面 |
| 可重跑反向複驗 | `ruby -EUTF-8:UTF-8 -e 'text=File.read("docs/specs/92-claim-index.md"); cmds=text.scan(/^- recheck: `(.*)`$/).flatten; cmds.first(2).each{\|cmd\| abort unless system(cmd)}'` 已輸出 `summary_synced: true`；日後總表再漂移會由 CLAIM-002 非零退出 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取 | current head 三端點全部 `--paginate`，逐則讀 review body 與 inline | 上列 review／inline／comment ID 均對應 `31223621cb…`；Claude run `32396377572` completed success |
| ② 生產者與消費者同步 | 以 CLAIM／P0-14／P0-18 反查 checker、fixture、docs/dev、83、92、91 與 worklog | CLAIM-001／002 命令實跑通過；83 §0 與 §1.1 同時受斷言，範圍外 71 只登記 §3 |
| ③ 易腐計數 | 發布數字進可失敗斷言；歷史快照明示邊界 | CLAIM-001 斷言表 3／合計發布值；P2 worklog 的舊 gate／Markdown 數字不再外推 HEAD |
| ⑤ fail-open／happy path | 正常、違規、縮排、重複、非活性區域都有 fixture | `ruby scripts/test-doc-claims-rules.rb` 定向快照全綠；新增 fixture 各自釘住期望輸出 |
| ⑦ Markdown／Windows 假結果 | HTML correction 保持歷史層；外部回應未留 artifact 就寫未取得 | 本 worklog 的 GitHub `POST /markdown` 已以可重跑 `@file` 入口取證；完整閘門仍列 Pending |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：R6 只讀活性 Markdown，支援合法縮排，忽略圍欄／註解並阻擋重複 ID。
- `scripts/test-doc-claims-rules.rb`、`spec/fixtures/ci_violations/doc_claim_*`：新增縮排標頭、重複 ID、
  非活性標頭的正反 case，並提高 suite 下限。
- `docs/dev/m0-review-convergence.md`：同步新增 fixture 與突變射程的終態說明。
- `docs/specs/83-admin-1to1-audit-round3.md`、`docs/specs/92-claim-index.md`：總表狀態閉合；
  CLAIM-001／002 把發布值與所有消費位置納入 abort。
- `docs/worklog/2026-08-21-P2宣稱索引與R6.md`：按點名修正歷史標記、外部證據與閘門快照邊界。
- `docs/specs/91-pit-register.md`：未點名同型問題只進 §3，並同步附錄 A.1。
- `docs/worklog/2026-08-21-PR65第二輪雙驗收修復.md`：本輪取證、鐵律 20.4／20.3 與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 上述終端結果回填本身改變了受驗文檔；本段定稿後須在不再修改倉庫檔案的前提下，從 setup
   最終重跑完整閘門，任一非零即停止。其後的 commit、commit 後 doc-claims、鐵律 15 差異核對
   與 push 仍待完成。
2. push 後的新 head 必須重新取得 CI、Codex、Claude 與 evaluator 結果；本輪列出的舊 head
   ID 只證明修復輸入，不代表新 head 已驗收。
3. 本 PR 修改 `scripts/`，命中鐵律 18.3；即使雙清與四條件齊，也只能通知使用者人工合併，
   互動式 Codex 不得代行。
