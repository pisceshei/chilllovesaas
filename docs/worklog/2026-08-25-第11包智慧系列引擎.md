# 2026-08-25 第 11 包：智慧系列求值引擎與最小地基（D50 案 A）

對應 dev doc：`docs/dev/m2-smart-collections.md`｜裁定：`docs/DECISIONS.md` D50
研究輸入：第 11 包三維度研究合成（本地正典盤點／Shopify 官方逐字／實作安全；
要點已入 D50、dev doc 與本檔——原始合成在 session scratchpad，未入庫）

---

## 已完成的工作 (Done)

1. **新表與 collections 新欄**（清單導出＝`git show 20260826058000 --stat` 或讀該
   migration 的 create_table 列；冪等 if_not_exists——
   MySQL DDL 非交易的第 3 包教訓）：`collection_sources`（**兩型**來源；include/exclude
   是區塊不是極性——95 §1.1 的 2026-08-24 修正）、`collection_source_rules`（typed value，
   金額唯 `value_cents`——鐵律 3；`block` 欄＋寫入層 per-block 白名單）、
   `collection_memberships`（物化；`variant_key` 產生欄擋 MySQL 唯一索引把 NULL 視為相異的陷阱——13 §F4.1 陷阱註）、
   `product_tags`（`tag_key` collation 明文 utf8mb4_bin）＋回填（正式環境 3 筆帶標籤商品）。
2. **`Tags::Normalize`**：六步實作（消費者導出＝`git grep -n "Tags::Normalize" app/ db/`
   ——寫入端／查詢端／回填共用這一支）。官方等價形態 red_new/red+new/red&new/red-new
   同鍵；NFKC／壓縮／casefold 三步標 ours V-136）。寫入端（SaveProduct 同 tx diff 同步）
   與查詢端（RuleCompiler）共用。
3. **`Collections::RuleCompiler`**：條件→SQL。注入安全兩軸（值全綁定；識別字只出自
   frozen 常數表，查不到＝Unsupported fail-closed）。逐型形態見 dev doc §1 值域表，
   關鍵語義：tag＝等值 EXISTS 禁 LIKE 禁 IN 合併；compare_at is_set＝**ALL variants**
   （官方逐字）；其餘變體條件＝any-variant（V-58 結案語義）。
4. **`Collections::Rebuild`**：世代戳＋id 批＋逐批短 txn（`Collection.lock`）＋
   `INSERT…SELECT…ON DUPLICATE` upsert＋世代掃尾。編不了＝整系列 ERROR 零寫入。
5. **`Collections::ResyncProduct`＋`ResyncConsumer`**：同一段 WHERE 的單商品判定；
   outbox 消費者掛 PRODUCTS_CREATE／PRODUCTS_UPDATE／INVENTORY_ADJUSTED（P11-U17
   的 ours 裁定）。ARCHIVED/刪除移出、UNLISTED 不移出、ERROR 系列跳過。
6. **rules 契約**：`CollectionSetInput.sources`＋`CollectionSourceInput`／
   `CollectionRuleInput`（金額走 `valueMoney` 十進位字串、服務端 `parse_money_for`
   折 cents——與變體價格同一個 Money::Decimal 邊界）；寫入層驗 per-block 型別白名單、
   relation 白名單（與編譯器同一份 RELATIONS）、contains ≥3、60 條 per-collection。
   commit 後 `RebuildJob.perform_later`（只帶 id）。
7. **`collectionRuleConditions` 執行期 query**（對齊本尊 CollectionRuleConditions 形狀）
   ——前端不得硬編對照表（`condition_relations_source: runtime_query`）。
8. **三處成員數收斂**：`MEMBER_COUNT_SELECT` CASE by type／GraphQL `products_count`
   （smart 未成功 rebuild 回 null）／前端 `—` 分支語義更新。
9. **rake `catalog:rebuild:collections`**（補上 `projection_rebuild_tasks` 清單的名額）。
10. **canon 回寫**（D50 配套）：spec 13 §F4.1 過時 schema 塊改為落地形＋V-58 結案
    （含 compare_at ALL-variants 例外）；blueprint 01-products 的 V-57 舊句加更正註；
    `collection_rule.rb`／`collection_product.rb` 的「不物化」誤導註釋改寫（工作卡點名）；
    legacy `collection_rules` 標記停止寫入。
11. **limits 新鍵**：contains 最小長度／rule_limit_scope／over_limit_grace／
    inclusion 19 型清單／rebuild_batch_size／compare_at_is_set_variant_scope／
    variant_match_scope（各帶官方出處或 ours 標記）。

### 開發中抓到並修掉的（自查，非審查輪）

- 🔴 **零變更 rebuild 白打快取**：初版用 `affected_rows` 判「有沒有變」——
  `ON DUPLICATE KEY UPDATE rebuilt_at` 對既有列**每輪**都改世代戳（MySQL 記 2）⇒
  每次 rebuild 都 bump stamp＋發 collections/update。煙測實跑抓到；改為
  `created_at >= generation`（只有真 INSERT 會設）＋swept>0。spec 釘住
  （零變更重跑 ⇒ stamp 不動、outbox 不增）。
- outbox 事件缺 `available_at`（NOT NULL）；`errors.product.price_invalid` 鍵不存在
  （locale 完整性 spec 抓到，改用既有 `amount_invalid`）。

## 修改的檔案與核心邏輯 (Changes)

新增：`db/migrate/20260826058000_*.rb`、`app/services/tags/normalize.rb`、
`app/services/collections/{rule_compiler,rebuild,resync_product,resync_consumer}.rb`、
`app/jobs/collections/rebuild_job.rb`、`lib/tasks/catalog.rake`、
`app/models/{product_tag,collection_source,collection_source_rule,collection_membership}.rb`、
`app/graphql/types/inputs/collection_source_input.rb`、
`app/graphql/types/collection_rule_condition_type.rb`、
spec：`spec/services/collections/`（4 檔）＋`spec/services/tags/normalize_spec.rb`＋
`spec/requests/smart_collections_spec.rb`、本檔＋dev doc＋handoff

修改：`app/services/catalog/save_product.rb`（`sync_product_tags!` 同 tx；
`parse_money_for` 公開邊界）、`app/services/catalog/save_collection.rb`
（normalize_sources／replace_sources!／enqueue_rebuild）、`app/services/events/consumers.rb`
（三 topic 掛 ResyncConsumer）、`app/models/collection.rb`（COUNT CASE）、
`app/graphql/types/collection_type.rb`（products_count／rebuild_status）、
`app/graphql/types/query_type.rb`（collectionRuleConditions）、
`app/graphql/types/inputs/collection_set_input.rb`（sources）、`config/limits.yml`、
五語 locale 檔、canon 三檔（spec 13／blueprint／兩個 model 註釋）、CollectionsPage 註釋

核心不變量：①求值只有 SQL 一套（rebuild=INSERT…SELECT、resync=同 WHERE+id）
②per-source 相減（A 排除 X＋B 包含 X ⇒ X 仍在——spec 釘住）③智慧成員只進 memberships
④序列化點＝collection 列鎖（規則編輯/rebuild/resync 同一把；併發 spec 以人工閘重現）
⑤金額規則值只有 cents。

## 突變驗證（逐項列於下方，不寫總數——理由見本節末的更正註）

C1 tag→LIKE／C2 tag 不過 Normalize／C3 contains 不跳脫／C4b exclusion 整塊拿掉／
C5 is_set 改 any-variant／C6 exclusion 白名單拿掉／C7 變更判定恆真／C8 掃尾拿掉／
C9b Unsupported 吞掉／C10 ARCHIVED 不移出／C11 ERROR 照算／C12 拿掉列鎖（併發 spec 紅）／
C13 60 上限／C14 per-block 白名單／C15 contains 下限／C16 金額存字串／C17 UNLISTED 也移出／
C18 tags 同步拿掉。全部紅（腳本在 scratchpad，不入庫）。

🔴 **2026-08-26 更正（收斂輪 H10）**：本節原文寫「17 格全紅／**17/17** 紅」，
但上面逐項列出的標籤比它多——句子被它自己的列舉推翻。腳本沒入庫、
當時的輸出也沒留存 ⇒ 現在無法重新導出確切數字，因此改為以**列舉**為準、不寫死總數
（鐵律 19.5：保留原文、追加有日期的更正；20.2 第 3 類：易腐計數必附導出法）。
🔴 二次更正（同日）：本節標題第一次修時改成了「標籤共 N 個」那種**寫死的總數**，
而它同樣沒有複驗方式——`check-doc-claims` 當場擋下。這正是該檢查註釋記載的形態
（「兩次『修掉過期數字』的修正本身都寫進了新的錯數字」）⇒ 改為完全不寫總數。

## 2026-08-26 整合修復（對抗審查輪：提出 5／確認 3／否決 2）

處置清單（20.3 三欄：處置／20.2 適用類型／反向複驗）：

| # | 嚴重度 | finding | 處置 | 20.2 類型 | 反向複驗 |
| --- | --- | --- | --- | --- | --- |
| F1 | 🟡 | `not_eq`／`not_contains` 對可空欄（product_type／vendor）三值邏輯靜默剔除 NULL 商品；同功能 tag 否定（NOT EXISTS）卻納入——兩個 is-not 空值語義打架 | 修復：兩個否定 relation 帶 `OR IS NULL`（`rule_compiler.rb` compile_string）；語義基準＝「未設定＝不是那個值」（與 tag 否定、本尊空字串儲存一致） | 4（狀態空間漏分：NULL 格） | MF1 紅＋engine_spec NULL 商品行為測試 |
| F2 | 🔴 | 兩場同系列 rebuild 交錯：逐批列鎖不序列化整場，晚開場小世代以無條件 `rebuilt_at=世代` **降**現任列的戳，另一方掃尾（`< 世代`）把整組成員刪光（審查 gated-threads 重現 BASELINE=[1282]→FINAL=[]，兩場皆回報 OK） | 修復：整場 advisory `GET_LOCK('chilllove:rebuild:<shop>:<collection>')`（sources 讀取＋編譯也在鎖內；等待 `rebuild_lock_wait_seconds`，等不到＝讓位＋RebuildJob 延後 `rebuild_lock_requeue_delay_seconds` 重排，不靜默丟）＋ upsert 改 `GREATEST(rebuilt_at, ?)` 單調帶 | 4（競態窗口） | MF2a／MF2b／MF2c 三格紅＋併發 spec 兩測（鎖占＝讓位零寫入＋重排；舊世代降不了戳） |
| F3 | 🔴 | 更新缺席 collectionType／sortOrder 被 normalize 的 `\|\| "manual"` 預設覆蓋後寫回——部分更新把智慧系列**靜默改手動**、排序重置；與 CollectionSetInput 宣告的「缺席＝保持現值」及 SaveProduct 同族語義相反 | 修復：normalize 缺席＝nil（驗證只對有帶執行）；預設只在 create 補；update 的 sortOrder 缺席＝保持現值；**collectionType 建立後不可變**（顯式不同值＝INVALID `type_immutable`，本尊官方逐字取證 2026-08-26："You can't change an automatic collection to a manual collection."——2026 新模型頁沉默，從嚴照舊）；sources 的 smart-only 閘移到 create／update（normalize 看不見更新目標現行型別） | 2（生產者契約與消費者不同步） | MF3a／MF3b／MF3c 三格紅＋request spec 三測 |
| R1 | ⚪ | exclusion 引用系列讀物化 memberships（手動系列讀不到） | 否決＝V-140 已裁定、已測、已註記的刻意形態 | — | — |
| R2 | ⚪ | type-flip 後 sources 殘留、ResyncProduct 照算 | 否決＝F3 下游；隨 F3 的型別不可變硬拒一併封閉（無 flip 路徑＝無孤兒面） | — | — |

Changes（本輪）：`rule_compiler.rb`（NULL-guard）、`rebuild.rb`（with_rebuild_lock／
rebuild! 拆分＋GREATEST）、`rebuild_job.rb`（逾時重排）、`save_collection.rb`
（宣告式 normalize／型別不可變／sources_smart_only_gate!）、`collection_set_input.rb`
（契約描述）、`config/limits.yml`（rebuild_lock_wait_seconds／
rebuild_lock_requeue_delay_seconds）、五語 locale（type_immutable）、
dev doc §1／§3、四個 spec 檔（新增 7 測——導出：
`git diff ea4467f fa6a4fc -- spec app/frontend | grep -cE "^\+ *it[ (\"]"`；
🔴 2026-08-26 收斂輪 H10 更正：原文寫「8 測」）。

### 追加突變（2026-08-26；累計 17＋7）

MF1 NULL-guard 拿掉／MF2a 鎖拿掉／MF2b GREATEST 退回覆蓋／MF2c 逾時重排拿掉／
MF3a 預設回填／MF3b 不可變守衛拿掉／MF3c sortOrder 重置——**7/7 紅**，
錨點逐一 grep 復原確認（第 7 包固定處理③）。

## 2026-08-26 第二輪整合修復（fix commit 的 delta 對抗審查：提出 7／確認 3／否決 4）

🔴 **本輪最重要的一件事**：delta 審查對象是**上一輪的修復本身**，而它確認的三條
全部是「修法只封了被點名的那幾格、同一元件裡的其餘格與下游消費者沒跟上」——
與第 7 包 CsvImport「抄了一半」同形態。固定處理＝**修完一個根因，先列出同一
producer／consumer／元件的完整狀態矩陣，逐格確認，再送驗**（鐵律 17.2 的射程定義）。

| # | 嚴重度 | finding | 處置 | 20.2 類型 | 反向複驗 |
| --- | --- | --- | --- | --- | --- |
| F4 | 🔴 | 型別做成不可變，但**唯一的執行消費者**（admin 編輯頁）沒同步：下拉仍可翻、存檔無條件送 `collectionType`；被拒的值留在表單狀態 ⇒ 該頁**之後每一次存檔**（改標題／SEO／譯文）都被同一個 INVALID 擋下，商家看到「改標題卻說型別不可變」且存不進去 | 修復：既有系列的下拉 `disabled={!isNew}`＋hint 改不可變說明；存檔 payload **只在建立時**帶 `collectionType`（宣告式「缺席＝保持現值」）；原本翻下拉的前端測試移到建立頁 | 2（生產者改、消費者未同步） | MG4／MG5 紅＋前端兩測（既有：停用、payload 無該鍵；建立：可用、payload 有該鍵） |
| F5 | 🔴 | F3 的根因是「缺席欄位補預設後寫回」，但同一個 `normalize` 裡**同根因的 `description_html` 沒動**：`input[:description_html].to_s` ⇒ 任何不帶 descriptionHtml 的部分更新把說明清空、userErrors 為空。商品側早有同款事故註釋（V29-D1）＝參照物是對的、只抄了一半 | 修復：`.nil?` 判準（空字串仍清除）、create 補 `\|\| ""`、update `\|\| collection.description_html` | 2 | MG1／MG2／MG3 紅＋request spec（缺席保持、顯式空字串清除） |
| F6 | 🟡 | F2 讓 `:skipped` 多出「鎖等逾時」語義，`RebuildJob` 同步了但 rake 兜底沒有：只數 `:error` ⇒ 鎖爭用時印 `rebuild OK` 並以 0 退出，而這支正是 dev doc §4／P11-B9 明文倚賴的兜底 | 修復：`contended` 單獨計數、訊息分列、非零結束；新增 `spec/lib/tasks/catalog_rebuild_spec.rb`（四格：全成功／鎖逾時／ERROR／正常跳過） | 2＋5（fail-open） | MG6 紅＋rake spec 四格 |

否決 4 條（各附反駁證據）：rake 逾時計數的**併發維度版本**（反駁：持鎖者只可能是另一場
同系列 rebuild，那一場會完成工作 ⇒ 危害是重現腳本造出來的；但**語義維度版本**的
「兜底回報綠色卻沒做事」成立，已修）／worker slot 被鎖等占住（反駁：HEAD^ 的
`Collection.lock` 本來就阻塞，`innodb_lock_wait_timeout` 預設 50 秒，方向與 finding 相反）／
sources 錯誤不再與其他 normalize 錯誤同批回報（反駁：parent 對同一輸入只回 sources 一則、
把 per-rule 錯誤全吞掉，「全部收集」從未成立）／型別下拉的**語義維度版本**（反駁：
修復前翻型別不是可用流程，是靜默丟成員——但**契約維度版本**點出的表單狀態毒化成立，已修）。

Changes（本輪）：`save_collection.rb`（description 宣告式三處）、`catalog.rake`
（contended 計數）、`CollectionDetailPage.tsx`（停用下拉／payload 條件化）、
五語前端 i18n（`collections.type.immutableHint`）、`docs/dev/m1-collections.md`
（型別在編輯態的更正＋V 項）、`docs/dev/m2-smart-collections.md`（兩種 `:skipped`）、
新增 `spec/lib/tasks/catalog_rebuild_spec.rb`、`smart_collections_spec.rb`＋
`CollectionDetailPage.test.tsx`（共 +8 測——導出：
`git diff fa6a4fc ca3233e -- spec app/frontend | grep -cE "^\+ *it[ (\"]"`；
🔴 2026-08-26 收斂輪 H10 更正：原文寫「+6 測」）。

### 追加突變（2026-08-26 第二輪）

MG1 說明回到 `.to_s`／MG2 update 直接指派／MG3 判準改 `.blank?`／MG4 前端照送型別／
MG5 下拉不停用／MG6 rake 不數鎖爭用——**6/6 紅**，錨點逐一 grep 復原確認。

## 2026-08-26 第三輪整合修復（收斂輪：提出 7／確認 7／否決 0；去重後 4 條＋自查 1 條）

🔴 **本包三輪的統計事實**：共確認 10 條，其中 **8 條是同一形態**——修法只封了被點名的
那幾格，同一方法裡的鄰居欄位或同一契約的下游消費者沒跟上。第三輪的收斂輪**零否決**，
說明前兩輪的修法射程一直畫得太小。⇒ 本輪除修完之外，另外交付
**dev doc §7「寫入契約狀態矩陣＋消費者影響圖」**（鐵律 17.4 要求的收斂交付物），
讓下一個改 `SaveCollection`／`Rebuild` 的人先看見整張表再動手。

| # | 嚴重度 | finding | 處置 | 20.2 類型 | 反向複驗 |
| --- | --- | --- | --- | --- | --- |
| G1 | 🔴 | `sources: []`（契約明文、本包 request spec 自己在測的合法輸入）清掉來源後，`rebuild!` 在 `sources.empty?` **早退於世代掃尾之前** ⇒ 物化成員**永久殘留**、`rebuild_status` 永停 PENDING，且三條清理路徑同時失能（早退不掃尾／resync 與 rake 的工作清單都從 `collection_sources` 導出，該系列已不在清單）。殘留列不是死資料：`compile_collection_exclusion` 讀 memberships ⇒ **別的系列**把幽靈成員減掉，成員判定當場出錯（審查實跑：Y 應含紅玫瑰卻回空） | 修復三處：①早退判準改「非智慧系列才跳過」，零 source 的智慧系列照樣走掃尾 ②`ResyncProduct#smart_collection_ids` 改「全部智慧系列」⇒ 逐商品自癒 ③rake 工作清單同改 | 2＋5 | MH1／MH7／MH8 紅＋engine spec 三格（清空後掃尾／跨系列污染／manual 照舊跳過）＋resync 自癒格＋rake 造訪格 |
| G2 | 🔴 | F1 的 NULL-guard 只封了 `compile_string`，同編譯器裡**唯一另一個「可空欄 × 允許 not_eq」**的格沒跟上：`v.compare_at_price_cents` 可空，`numeric` 產出裸 `<> ?` ⇒「比價不等於 X」把沒設過比價的商品靜默剔除。同一個 condition_type 內部自相矛盾（`is_not_set` 把 NULL 當未設定納入、`not_eq` 排除同一批） | 修復：`numeric` 對 `not_eq` 帶 `OR IS NULL`；其餘走該方法的欄位皆 NOT NULL 或已 COALESCE ⇒ 多出的 guard 恆假無害。F1 家族至此封閉 | 4（狀態空間漏分：NULL 格） | MH4 紅＋compiler 兩格（not_eq 帶 guard／gt 不帶）＋engine 行為格 |
| G3 | 🟡 | 系列說明**完全沒有上限**：F5 抄了 SaveProduct 的 nil-guard，緊接的 TOO_BIG 檢查沒抄 ⇒ 同一份內容在 productSet 回 TOO_BIG、在 collectionSet 靜默存進去；而譯文硬上限明文「對齊 `product.description_max_bytes`」⇒ 超限系列說明的 body_html 譯文永遠寫不進去 | 修復：沿用 `product.description_max_bytes`（與同方法 title 用 `product.title_max_chars` 同慣例）＋五語 `errors.collection.description_too_big` | 2 | MH5 紅＋request spec TOO_BIG 格 |
| G4 | 🟡 | `ResyncProduct` 逐系列迭代無依賴排序，而 exclusion 的 `collection` 型讓 A 的成員依賴 B 的物化列 ⇒ A.id < B.id 時（先建 A、後建 B 再加排除）第一趟算 A 時商品還沒進 B，A 誤留該商品，且錯誤狀態持續到該商品下次有事件 | 修復：第二趟重算「帶 `collection` 型 exclusion」的系列。互相引用需不動點迭代，兩趟不保證收斂 ⇒ 登記 **P11-B10**，rake 兜底 | 4 | MH6 紅＋resync spec（A.id < B.id 單次事件收斂） |
| F7 | 🔴 | **自查（非審查方提出）**：`rebuilt_at` 可空，而 MySQL `GREATEST(NULL, x)` **回 NULL** ⇒ F2 的單調帶讓 NULL 戳的列變成「再也蓋不上戳、也掃不掉」的不死列；掃尾的 `< 世代` 在三值邏輯下同樣漏掉 NULL | 修復：`GREATEST(COALESCE(rebuilt_at, 世代), 世代)`＋掃尾顯式收 `IS NULL` | 4 | MH2／MH3 紅＋engine 兩格（NULL 戳能蓋上／不再命中的 NULL 戳被掃掉） |

⚪ 範圍外觀察（僅登記，不擋）：`title` 在兩支宣告式 upsert 裡「缺席＝BLANK 錯誤」而非
保持現值——`SaveProduct` 與 `SaveCollection` 逐字同款＝家族一致的既有行為，
單方面只改系列側會讓兩支語義分家 ⇒ 登記 `docs/specs/91-pit-register.md` §3.16。

Changes（本輪）：`rebuild.rb`（早退判準／COALESCE／掃尾收 NULL）、`resync_product.rb`
（工作清單／第二趟）、`rule_compiler.rb`（`numeric` 的 not_eq guard）、
`save_collection.rb`（說明上限）、`catalog.rake`（工作清單）、五語 locale、
dev doc（§5 補 P11-B10、新增 §7 狀態矩陣與消費者影響圖、原 §7 順延為 §8）、
`91` §3.16、五個 spec 檔（+12 測——導出：
`git diff ca3233e 394db58 -- spec app/frontend` 的 `--name-only` 與
`grep -cE "^\+ *it[ (\"]"`；🔴 2026-08-26 收斂輪 H10 更正：原文寫「四個」）。

### 追加突變（2026-08-26 第三輪）

MH1〜MH8 **8/8 紅**。🔴 值得記下來的是**第一次跑只有 6/8 紅**：MH7／MH8（兩個兜底的
工作清單）綠——修法改了、但沒有任何測試看著它。補上 resync 自癒格與 rake 造訪格後
才轉紅。**「改了生產碼卻沒加對應的測試」在突變表上長得跟「守衛無效」一模一樣**，
這正是突變輪要跑的理由。

## 2026-08-26 第四輪整合修復（最終收斂輪：提出 12／確認 11／否決 1；去重後 9 條）

🔴 **本輪最重要的發現：兩條 🔴 從初始候選 `ea4467f` 就在，前三輪、947 個 spec、
32 格突變、四次 CI 全綠都沒抓到**——因為它們的觸發條件是**規則值裡的一個字元**
（`?` 或連續空白），而所有 fixture 的值都很「乾淨」。這比「修法射程太小」更值得記：
**測試資料的形狀本身就是一種盲區**。

| # | 嚴重度 | finding | 處置 | 20.2 類型 | 反向複驗 |
| --- | --- | --- | --- | --- | --- |
| H1 | 🔴 | `RuleCompiler.where_sql` 產出的是**已 quote 完的片段**，兩個消費者卻把它內插進**還要再走一次 `sanitize_sql_array` 位置參數**的字串。Rails 的 `replace_bind_variables` 用 `statement.count("?")` 對位、**不分引號內外** ⇒ 商家值裡的一個 `?`（`"Why not?"`、tag `sale?-item`⋯⋯寫入層不濾）就讓 arity 不符、`PreparedStatementInvalid`。爆炸半徑遠大於該系列：`ResyncProduct` 是逐一掃全店智慧系列的迴圈，例外穿出整個 `call` ⇒ **該店所有商品／庫存事件**的 resync 消費者永久失敗、排在後面的無辜系列再也拿不到增量更新；rebuild 端拋的不是 `Unsupported` ⇒ 走不到 `mark_error!`，`rebuild_status` 永停 PENDING | 修復：`where_sql` **在 `sanitize_sql_array` 之後才代入**——模板留一個註解形式的槽 `WHERE_SLOT`，用 `sub` 的 **block 形式**換入。三件事同時解決：①sanitize 只看得到模板自己的 `?` ②`squish` 只作用在代入之前的模板上 ③block 形式不解讀反向參照（mysql2 為跳脫而產生的兩個字元 `\\` 不會被折回一個——字串形式會，而 MySQL 讀 `\1` 就是 `1`，於是比對到完全不同的字串）。🔴 **第一版改成逐值 `connection.quote` 全手拼，Brakeman 立刻報 2 個 SQL Injection（Medium）**——工具無法證明手拼安全，這個抱怨是對的；改成本形態後 Brakeman 回零且三個性質都保住。紀律寫在 `RuleCompiler#bind` 上方（產出者處，下一個消費者先看到） | 5（fail-open）＋2 | MI1／MI2b／MI3 紅＋engine spec 四格（`?` 值能重建／tag 含 `?`／一個問號系列不得讓整店停擺／反斜線值原樣比對） |
| H2 | 🔴 | 同根因的第二面：`<<~SQL.squish` 在**字串內插之後**才跑 ⇒ 壓縮的不只是 SQL 排版，還有商家值字面量**內部**的空白（`'紅玫瑰  禮盒'` → `'紅玫瑰 禮盒'`）。resync 端沒有 squish ⇒ **兩支引擎對同一條規則給出不同答案**，且是靜默的：成員在每次事件與每次 rebuild 之間來回翻轉，13:716 的「連跑兩次列數不變」驗收錨也不成立 | 隨 H1 一併消除——`squish` 只作用在**代入之前**的模板（收斂輪後改為先 sanitize 再代入） | 4 | MI2 紅＋engine spec 一格（雙空白值：rebuild 與 resync 同答案＋連跑兩次 0/0） |
| H3 | 🔴 | **INVENTORY_ADJUSTED 整條觸發鏈是死的**：生產端 `Inventory::Adjust#enqueue_adjust_event!` 的 payload 只有 `{adjustment, location_id, inventory_item_id, availability_flipped}`，**從來沒有** `product_id`／`product_variant_id`，而消費者只讀後兩者 ⇒ 永遠早退，Relay 記成 done、事件標 published，**無錯誤、無重試**。`variant_inventory` 條件的智慧系列永遠不因庫存變動更新。舊 spec 自己捏了帶 `product_variant_id` 的 payload 所以綠——**fixture 與真實生產者不符＝假綠** | 修復：消費者自己反查 `inventory_item_id` → `product_variant_id` → `product_id`（三個鍵都支援）。**不改生產端**——那是庫存包的契約且 D43 規定庫存寫入只走 `Inventory::Adjust`（20.5：不跨元件擴修） | 2 | MI6 紅＋consumer spec 用**真實 payload 形狀**（只有 inventory_item_id） |
| H4 | 🔴 | 全量 rebuild 的 `INSERT…SELECT` **沒有 ARCHIVED 守衛**，而 resync 在 Ruby 端有 ⇒ 兩支引擎對封存商品給出相反答案。商家封存商品（resync 移出）後，只要再存一次條件或跑 rake 兜底，封存品就回到物化表，且掃尾刪不掉（同一輪剛蓋上新世代戳）。成員集合變成「取決於最後跑的是哪一支引擎」，直接違反 13 §F4.6-5 與本包自己宣稱的「兩支必須永遠是同一段」 | 修復：資格謂詞抽成 `RuleCompiler::PRODUCT_ELIGIBLE_SQL` 單一字面，兩支引擎都拼它；resync 的 Ruby 守衛下沉到 SQL ⇒ 構造上不可能再分岔 | 2 | MI4／MI5 紅＋engine spec 三格（rebuild 不塞回／resync 移出後 rake 不加回／UNLISTED 照舊是成員） |
| H5 | 🟡 | G4 的第二趟是**追加**不是**改序**：A 仍在第一趟先算一次、**提交**錯誤成員列、bump 快取、發 `collections/update`，第二趟才刪掉再發一則 ⇒ ①前台在該窗口讀得到依規則絕不該在 A 裡的商品 ②每個被排除的商品每次事件固定產生兩倍的快取失效與 outbox 事件 | 修復：`(ids - referencing) + referencing`（改序，無中間寫入） | 4 | MI7 紅＋consumer spec（joined 只算一次、outbox 只有 B 一則） |
| H6 | 🟡 | F7 的修法只進了程式碼與 spec，**沒進「下一個人先讀的那兩份」**：dev doc §3 仍逐字寫 `GREATEST(rebuilt_at, 世代)`（F7 判定會產生不死列的那個形態），handoff §④ 更教人「至少複用 GET_LOCK ＋ GREATEST 單調帶」——照字面實作等於把 F7 重新做壞一次 | 修復：兩處都改成 `GREATEST(COALESCE(...))` 並註明 COALESCE 不可省的理由 | 2＋19.5 | 文字複驗（`grep -n "GREATEST" docs/dev/m2-smart-collections.md docs/handoff/2026-08-25-第11包智慧系列引擎.md`） |
| H7 | 🟡 | 本包動了 `m1-collections.md` 但**只同步型別下拉那一句**，同檔另外四句仍與本包程式碼相反（`collection_products` 的「不物化」理由、`collection_rules` 未標 Legacy、商品數 `—` 的判準、§6「求值引擎未做」） | 修復：四句逐一改寫並標日期更正 | 2 | 文字複驗（四處各有 2026-08-26 更正註） |
| H8 | 🟡 | 「13 §F4.2 的三條必測**全**在 engine_spec」是缺證的完成性聲明：實際只有 2 個 `it`，第三條（手動加入＋明確排除）在 v1 **結構上測不了**（`collection_source_members` 未建） | 修復：改成「釘住其中兩條」＋登記 **P11-B11** | 3（全稱句） | 數 describe 內的 `it` |
| H9 | 🟡 | `limits.yml` 註釋寫「metafield **六型**」，被它自己下方的 value 列舉推翻（列了 7 個）；算術 10＋6＋2＝18 ≠ 宣稱的 19。正典 95 §1.2 逐字「19 種扣掉 **7** 個 Metafield\* 與 Unknown」 | 修復：改七型並補算術 | 3（自指宣稱自我推翻） | `95` §1.2 對照 |
| H10 | 🟡 | 四個易腐計數與實際 diff 不符（第一輪突變「17 格」但列了 18 個標籤；三輪的新增測數／spec 檔數各差一） | 修復：全部改成**附導出指令**的數字；突變那格因腳本未入庫、輸出未留存 ⇒ 改以列舉為準、不寫死總數，並保留原文追加更正（19.5） | 3 | 各數字旁的 `git diff … \| grep -c` |

否決 1 條：handoff §③ 的 P11-U 清單處置宣稱（反駁：識別字盤點口徑與該句射程不符）。

Changes（本輪）：`rule_compiler.rb`（`PRODUCT_ELIGIBLE_SQL`＋`bind` 上方的消費紀律）、
`rebuild.rb`（upsert 改逐值 quote／資格謂詞／檔頭②）、`resync_product.rb`（同形改寫／
資格謂詞下沉／第二趟改序）、`resync_consumer.rb`（inventory_item 反查）、
`config/limits.yml`（metafield 七型）、dev doc m2（§3 upsert 形態／§3 必測聲明／
§5 補 P11-B11）、dev doc m1（四句更正）、handoff（§④ GREATEST 全形）、
本檔（H10 四處計數）、兩個 spec 檔（+10 測——導出：`git diff 996b90b 14954b3 -- spec app/frontend | grep -cE "^\+ *it[ (\"]"`；🔴 2026-08-26 收斂輪 J8 更正：原文寫「+9 測」，而本輪才剛把前三輪的同型錯誤全部改成附導出指令——同一個根因在同一份檔案裡復發，見鐵律 20.4）。

### 追加突變（2026-08-26 第四輪）

MI1〜MI7＋MI2b（`sub` 字串形式）＋MI4b（resync 的資格謂詞）＝**9/9 紅**。
🔴 MI2b 第一次跑是**綠**的：block 形式是刻意選的，卻沒有任何測試看著它——
補上「條件值含反斜線」那一格才轉紅。與第三輪 MH7／MH8 同一課：
**刻意的實作選擇如果沒有對應測試，它就只是碰巧還沒被改壞。**

## 2026-08-26 第五輪整合修復（髒值輪：提出 13／確認 12／否決 1；去重後 8 條）

本輪的維度是**髒值掃蕩**——直接針對第四輪學到的那一課（fixture 的值太乾淨）設計：
把每個吃自由文字的欄位用 `?`／`%`／`_`／引號／反斜線／連續空白／Tab／全形空白／
零寬字元／emoji／4-byte CJK／貼近上限的極長值逐一實跑。它一次找出**兩條 🔴**，
而且**兩條都是同一類**：DB 欄寬沒有鏡射到寫入層 ⇒ `ValueTooLong` 漏成 500。

| # | 嚴重度 | finding | 處置 | 20.2 類型 | 反向複驗 |
| --- | --- | --- | --- | --- | --- |
| J1 | 🔴 | 規則值 `value_text` 在寫入層**完全沒有上限**，而欄位是 varchar(255)。>255 字 ⇒ `create!` 拋 `ValueTooLong`（`StatementInvalid` 子類、不在 SaveCollection 的 rescue 清單）⇒ 漏成 top-level INTERNAL、userErrors 為空（違反鐵律 4①）。同一個 normalize 的 title／description／seo／handle **全都有**上限，只有規則值沒有 | 修復：`condition_value_max_chars` ＋ `value_within_limit?` 共用守衛，五個字串型與 exclusion 同型條件全過 | 2 | MJ1 紅＋request spec TOO_LONG 格 |
| J2 | 🔴 | `Tags::Normalize.key` **會讓字串變長**（NFKC 展開 ㍿→株式会社、Ⅷ→viii；casefold ß→ss、™→tm），而寫入層驗的是**原字串**、落庫的是 key，`tag_key` 也是 varchar(255)、上限也是 255 ⇒ 任何膨脹必然溢位。商品側因此**整筆商品**回 INTERNAL（`sync_product_tags!` 在 SaveProduct 交易內）；migration 回填遇到這種既有標籤會讓 `db:migrate` 中斷。🔴 **本包引入的回歸**——之前 `product_tags` 不存在，同一輸入存得進去 | 修復三處：①SaveProduct 同時驗原字串與 key ②`ProductTag` model 補 `tag_key` 長度驗證（第二道，migration 也經過它）③回填迴圈對超長 key 跳過並記 log，不中斷 migrate。系列側的 tag 條件同樣兩者都驗 | 2＋4 | MJ2a／MJ2b／MJ2c 紅＋engine spec 兩格＋normalize spec 膨脹表＋新增 `spec/models/product_tag_spec.rb` |
| J3 | 🔴 | H5 的「改序」修掉了翻轉，卻把**第二次求值機會**一起拿掉 ⇒ **鏈式**單向引用 A→B→C 時 A 與 B 同落尾段、順序由 pluck 決定，A 讀到本次事件完全沒被重算過的 B ⇒ 錯且不自癒。P11-B10 只登記**互相引用**不收斂，並逐字宣稱「單向引用因此在一次事件內收斂」——該宣稱因本回歸而失真。🔴 **本包第四輪引入的回歸** | 修復：改成**依引用關係的拓樸序**（被引用者先算，DFS＋visiting 標記斷環）。環（A⇄B）沒有拓樸序 ⇒ 以穩定順序打破，仍登記 P11-B10 由 rake 兜底 | 4 | MJ3 紅＋resync spec 鏈式格（🔴 見下方「測試拓樸」註） |
| J4 | 🟡 | `product_status` 的值**沒有值域白名單**：同 normalize 的 collection_type／sort_order、同方法的 block／condition_type／relation 全部白名單，唯獨狀態值直通 ⇒ 打錯字存檔成功、系列恆空、零回饋，且與**合法值** `archived`（因 `PRODUCT_ELIGIBLE_SQL` 恆 0 成員）在畫面上無法區分 | 修復：對 `Limits.enum(:product, :status_values)` 白名單，並正規化成小寫 | 2 | MJ4 紅＋request spec 兩格（打錯字 INVALID／大寫合法值通過並存成小寫） |
| J5 | 🟡 | exclusion 的 `collection` 型**沒擋自我引用**。`compile_collection_exclusion` 讀物化成員 ⇒「排除自己」＝「凡已在本系列的商品就排除」，**不存在不動點**：每次 rebuild／resync 都翻面，每翻一次 bump 一次 cache stamp、發一筆 `collections/update`（EXTERNAL topic）。與 P11-B10 不同——自引在 rake 兜底下是每跑一次翻一次，兜底本身就是震盪源 | 修復：`current_id` 串到 `normalize_rule`，自引回 INVALID | 4 | MJ5 紅＋request spec 自引格 |
| J6 | 🟡 | `collectionRuleConditions` 只走 `INCLUSION_TYPES`，而 `collection` 是 **exclusion 專用**型 ⇒ 這支 query 永遠不回傳它，前端拿不到它的 relations。但寫入層接受它、編譯器也編得出來 ⇒ 規則編輯器只剩「違反『不得硬編』明文」或「做不出 collection 排除」兩條路 | 修復：改成 `INCLUSION_TYPES \| EXCLUSION_TYPES` 聯集 | 2 | MJ6 紅＋request spec（🔴 原斷言把 bug 寫成期望值，一併更正） |
| J7 | 🟡 | G1 之後工作清單改「全部智慧系列」（該改法本身對），但每則事件都對全店每一個智慧系列開短交易＋列鎖＋求值，即使該系列一條 source 都沒有。H3 把庫存事件接活後這條放大鏈才真正通電（庫存事件的合併鍵不含商品 ⇒ V 個變體 × L 個地點各一則，每則再乘系列數上限） | 修復：工作清單取**聯集**——有 conditions source 的（可能加入）∪ 本商品已有物化列的（可能移出，即 G1 的自癒面，走主鍵索引）。兩者都不在 ⇒ 構造上不可能有變化，跳過是正確而非抄捷徑 | 4 | MJ7a／MJ7b 紅＋resync spec（無關系列不被鎖／殘留列系列仍被造訪） |
| J8 | 🟡 | 第四輪的兩個計數又錯（「+9 測」實際 10；handoff 的「7 格突變」與它自己指向的 worklog 第四節不一致；兩處「32 格突變」含第一輪那個**依 H10 自己的更正就不可導出**的數）| 修復：全部改成附導出指令或不寫死 | 3（且是 20.4 的「同一根因復發」） | 各數字旁的 `git diff … \| grep -c` |

否決 1 條：四張新表沒有外鍵（反駁：與倉庫既有慣例一致，非本包引入）。

### 🔴 測試拓樸也會騙人（本輪第二課）

MJ3 第一次跑是**綠**的。原因不是守衛無效，而是**我的測試拓樸選錯了**：
A→B→C 鏈裡若 C 含該商品，B 就是空的，於是 A 無論什麼順序都「碰巧」算對。
把 C 的規則改成商品**沒有**的標籤（C 空 ⇒ B 含商品 ⇒ A 應為空）之後才轉紅。
⇒ 教訓：**測依賴順序的測試，必須讓「被依賴方非空」**，否則測的是空集合的交換律。
同輪 MJ2c 也綠過一次（model 驗證被服務層守衛遮住），補 model 層 spec 才轉紅。

Changes（本輪）：`save_collection.rb`（值長度守衛／status 白名單／自我引用／current_id 串接）、
`save_product.rb`（tag 驗 key 長度）、`product_tag.rb`（model 長度驗證）、
`20260826058000` 回填（超長 key 跳過）、`resync_product.rb`（拓樸序／工作清單聯集）、
`query_type.rb`（resolver 聯集）、`config/limits.yml`（`condition_value_max_chars`）、
五語 locale（三個新鍵）、新增 `spec/models/product_tag_spec.rb`、四個既有 spec 檔。

### 追加突變（2026-08-26 第五輪）

MJ1／MJ2a-c／MJ3／MJ4／MJ5／MJ6／MJ7a-b **全紅**（逐項見上表末欄）。

## 2026-08-26 第六輪整合修復（提出 10／確認 9／否決 1；去重後 7 條）

🔴 **本輪把一個重複六輪的根因做成結構**。前五輪每次改「求值語義」都只改**一條路徑**，
另外兩條就分岔——G4／H5／J3 三次都只改 `ResyncProduct`；H4 是 Rebuild 沒跟上
resync 的 ARCHIVED 守衛；K2 是兜底 rake 沒跟上 resync 的拓樸序；K8 是規則編輯路徑
**完全沒有**反向傳播。求值路徑其實有**三條**（增量／單系列全量／兜底全量），
外加一個「B 變了誰要跟著重算」的維度。⇒ 收斂成 `Collections::ReferenceGraph`
（`topological` ＋ `referrers`），三條路徑都只准用它，並在 dev doc §7 影響圖新增一列
明文要求「新增任何求值路徑都必須接上這兩個」。

| # | 嚴重度 | finding | 處置 | 20.2 類型 | 反向複驗 |
| --- | --- | --- | --- | --- | --- |
| K1 | 🔴 | J3 的拓樸排序用**無深度上限的 lambda 遞迴**，DFS 深度＝引用鏈長度，而 `max_smart_collections_per_shop` 允許 5000 ⇒ 鏈長約 1100 就 `SystemStackError`。它**不是 `StandardError`** ⇒ `Events::Relay` 的兩處 rescue 都接不到：該事件 `attempts` 永不遞增、永不進 dead-letter，`locked_at` 逾時被回收後再炸（**永久毒丸**），且同一批 `claim_batch` 內**其他商店**的事件一起不投遞。🔴 本包第五輪引入 | 修復：改**顯式堆疊的迭代 DFS**（`ReferenceGraph.topological`），深度只受記憶體限制 | 5（fail-open）＋4 | MK1／MK1b 紅＋`reference_graph_spec` 1500 層深鏈格 |
| K2 | 🔴 | J3 只把**增量**引擎改成拓樸序，兜底 `catalog:rebuild:collections` 仍照 id 序 ⇒ A→B→C 鏈上 A 讀到還沒重建的 B，**同一份規則 resync 給 A=[]、rake 給 A=[商品]**，任務仍印 `rebuild OK` 以 0 結束。這是 H4「成員集合取決於最後跑的是哪一支引擎」被重新打開；第五輪改寫的 P11-B10 逐字宣稱「單向引用（含鏈）在一次事件內收斂」對兜底路徑為假（鐵律 19.2）。🔴 本包第五輪引入 | 修復：rake 改用 `ReferenceGraph.topological`，與 resync 同一份 | 2 | MK2 紅＋engine spec 跨引擎一致格 |
| K8 | 🔴 | **反向傳播完全缺席**：`compile_collection_exclusion` 讓 A 的成員依賴 B 的物化列，但 `enqueue_rebuild` 只排自己那一個系列的 RebuildJob，`collections/update` 事件在註冊表**沒有任何消費者**，兜底 rake 未排進 `recurring.yml` ⇒ B 的規則改了、B 重建了，引用 B 的 A **永久錯誤且無自癒路徑**，而 memberships 是前台唯一查詢對象 | 修復：`notify_members_changed!` 增加第三個對外面——`ReferenceGraph.referrers` 逐一排 `RebuildJob`。只在真的變了時傳播、只傳一層（被排的 A 若也變了會再往上傳，天然逐層收斂） | 2＋4 | MK8／MK8b 紅＋engine spec 兩格（有引用者⇒排／無引用者⇒不排） |
| K3 | 🔴 | `valueMoney` 的**上界**沒鏡射到寫入層：`parse_money_for` 只擋格式與負數，×100 後可超出 `value_cents` 的 BIGINT ⇒ `create!` 拋 `ActiveModel::RangeError`——它**既不是** `StatementInvalid` **也不是** `ActiveRecordError`，連 GraphQL controller 的兜底 rescue 都接不到 ⇒ 直接 500、userErrors 為空。這是 J1／J2 修法的**殘留面**：varchar 兩格補了，同一個 `normalize_rule` 裡的 BIGINT 這格沒補 | 修復：`condition_value_max_cents` ＋寫入層檢查 | 2 | MK3 紅＋request spec TOO_LONG 格 |
| K5 | 🟡 | J2 的 migration 跳過分支讓 `products.tags` 與 `product_tags` 分岔，但**分岔沒登記**；更嚴重的是 `sync_product_tags!` 每次更新都從 `product.tags` 重算並補建 ⇒ 撞上 J2 新增的 model 驗證 ⇒ `RecordInvalid` ⇒ **商品變成唯讀**（即使完全沒帶 tags）。另：第五輪在 model 與 migration 各硬編一個 255（鐵律 6），且 skip 分支只守 key 沒守 raw（`tag_display` 同為 varchar(255)，正規化可**縮短**） | 修復：`sync_product_tags!` 同樣跳過並記 log；三處判準統一引 `Limits`；skip 同時守 raw 與 key；分岔登記 **P11-B12**（含「不修的理由」與「集合只會縮小」的推論） | 2＋6 | MK5 紅＋engine spec 唯讀格 |
| K6 | 🟡 | `sources` **陣列本身沒有上限**：60 條上限只算條件總數，空來源（合法輸入）貢獻 0 條 ⇒ 一次請求可無限量建 source 列，且寫入在 `Collection.lock` 之內 ⇒ 把 rebuild／resync 共用的序列化點壓住整個寫入時長 | 修復：`max_sources_per_collection` ＋寫入層檢查 | 4 | MK6 紅＋request spec 格 |
| K7 | 🟡 | dev doc §7「消費者影響圖」第 5 列在 HEAD 已過期（第五輪 J7 把工作清單改成聯集，該列仍寫「兩者都必須是全部智慧系列、**不得**從 `collection_sources` 導出」）——**照它動手的人會把 J7 改回去**。而 §7 自陳的存在理由正是「讓下一個人先看見整張表再動手」⇒ 收斂交付物本身復發了它要防的形態 | 修復：該列改寫成現行語義並說明兩半各自的理由；另新增「系列之間的引用」一列 | 2 | 文字複驗 |

否決 1 條：`collectionRuleConditions` 無法區分 exclusion 專用型（反駁：limits 的
`condition_relations_source` 禁令射程只到 relation，不涵蓋「哪個區塊有哪些型別」）。

Changes（本輪）：**新增 `app/services/collections/reference_graph.rb`**（三條路徑共用）、
`resync_product.rb`（改用共用圖、刪自己那份）、`catalog.rake`（拓樸序）、
`rebuild.rb`（`enqueue_referrers!` 反向傳播）、`save_collection.rb`（金額上界／來源數上限）、
`save_product.rb`（超長既有標籤跳過）、`product_tag.rb`（判準引 limits）、
`20260826058000`（skip 守 raw＋引 limits）、`config/limits.yml`（兩個新鍵）、
五語 locale（兩個新鍵）、dev doc（§5 補 P11-B12、§7 影響圖兩列）、
**新增 `spec/services/collections/reference_graph_spec.rb`**、兩個既有 spec 檔。

### 追加突變（2026-08-26 第六輪）

MK1／MK1b／MK2／MK3／MK5／MK6／MK8／MK8b／MKR **全紅**。

## 2026-08-26 第七輪整合修復（提出 16／確認 10／否決 6；去重後 6 條）

| # | 嚴重度 | finding | 處置 | 20.2 類型 | 反向複驗 |
| --- | --- | --- | --- | --- | --- |
| L1 | 🔴 | 第六輪的反向傳播（K8）在**奇數長度的引用環**上**永不終止**。根因是個乾淨的數學性質：「A 排除 B」讀 B 的**物化成員** ⇒ 反單調函數 a := ¬b。偶數環是偶數次反單調的合成＝**單調**，有不動點（實測 n=2／n=4 各 1／2 個 job 就停）；奇數環仍是反單調 ⇒ **沒有不動點**、成員週期震盪（n=3 軌道週期 6），而傳播條件是「本輪有沒有變」，震盪的每一步都「有變」⇒ 無界 RebuildJob 鏈＋無界 `collections/update` outbox（EXTERNAL topic，會外發 webhook），且兜底 rake 自己跑一輪就是新的震盪源。寫入層當時只擋自引（J5） | 修復兩道：①**任何長度的環在寫入層一律拒**（`ReferenceGraph.reaches?` 迭代可達性；自引只是 n=1 格）——判準不是「偶數放行奇數拒」，偶數環雖會停但答案取決於起始狀態，環在這個語義下沒有一個「對」的答案 ②既有環（守衛上線前建立的）走 `rebuild_propagation_max_depth` 有界傳播，到上限記警告 | 4（狀態空間漏分）＋5 | ML1a／ML1b／ML1c 紅＋`reference_graph_spec` 可達性三格＋request spec 二元環拒收格＋engine spec **種在震盪軌道上**的既有環終止格 |
| L2 | 🔴 | 三值邏輯的**第三次**現形。F1（字串否定 relation）與 G2（數值否定 relation）兩次都只封了「被點名的那個 relation」，**沒封真正產生否定的地方**——`where_sql` 的**區塊層 `NOT`**。於是**正向**謂詞落進 exclusion 區塊時照樣中招：`NOT (p.product_type = 'X')` 在該欄為 NULL 時回 NULL ⇒ WHERE 丟掉整列 ⇒ 沒設類型的商品被一個根本不該命中它的排除條件剔除。受影響＝可空欄 × 非 NULL-safe relation（product_type／product_vendor × eq／starts_with／ends_with／contains） | 修復：`AND NOT COALESCE(expr, FALSE)`。🔴 **這一層封住之後，任何新增的 exclusion 型別與 relation 都自動 NULL-safe**——F1／G2／L2 三次的共同根因就在這一行，逐格補是補不完的 | 4 | ML2 紅＋engine spec 兩格（eq／contains 的 NULL 欄商品必須留下）＋compiler SQL 形態格 |
| L3 | 🔴 | `RuleCompiler#bind` 上方的「消費紀律」寫的是 H1 **第一版被否決**的修法，三個子句對 HEAD **全部為假**（`connection.quote` 在該目錄一次都沒出現；兩個消費者都用位置參數；`rebuild.rb` 明明有 `squish`）。這是最危險的一種文檔錯誤——它是**寫給下一個消費者看的規範**，照它實作就會撞回 Brakeman 擋下的那一版 | 修復：改寫成 HEAD 實況（sanitize 之後才代入／`WHERE_SLOT`／`sub` block 形式），並保留「第一版被 Brakeman 擋下」的沿革 | 2＋19.5 | 文字複驗（`rg -n "connection.quote" app/services/collections/` 應為 0 命中） |
| L4 | 🟡 | 第六輪 K2 在兜底 rake 上的接線**承重卻無 spec 覆蓋**：既有五個 example 全部 stub 掉 `Rebuild.call`，排序在該檔構造上不可觀測；engine spec 那一格則是**在測試裡重寫一遍 rake 的內容**，從不 `Rake::Task#invoke` ⇒ 把拓樸序那一行刪掉整套仍綠。K2 被抓到的形態在測試面原樣重演 | 修復：新增**真的 invoke 任務、不 stub 引擎**的 rake 格，斷言三個系列的最終成員 | 5（自我證明閘門） | ML4 紅 |
| L5〜L9 | 🟡 | 五處文檔與 HEAD 不符：dev doc §3 的 `:skipped` 語義停在 G1 之前（「沒有 conditions source」，而判準早已改成「非智慧系列」）；handoff §② 仍寫「三條必測全釘」——**正是 H8 確認為缺證並由 P11-B11 明文禁止的那句**；`collection_type.rb` 的 `products_count` 註釋停在「規則引擎未落地」（該引擎正是本包）；handoff §① 的輪次紀錄停在第四輪（漏了第五／六輪、一個新服務檔與一個新 spec 檔）；dev doc §7 表首寫「三輪 10 條」 | 逐處改寫，並在易腐處改為「請數 worklog」而非寫死 | 2＋3 | 文字複驗 |

否決 6 條（含：反向傳播的冗餘扇出、未追蹤品項的庫存合計、B10 編號、worklog 頂層
Changes 段、§8 驗證指令、model 檔頭的 exclusion 型別數）。

Changes（本輪）：`rule_compiler.rb`（區塊層 `NOT COALESCE`＋`bind` 紀律改寫）、
`reference_graph.rb`（`reaches?`＋檔頭④改寫）、`save_collection.rb`（環拒收）、
`rebuild.rb`（傳播深度上限）、`rebuild_job.rb`（depth 參數，預設 0 ⇒ 舊 payload 相容）、
`config/limits.yml`（`rebuild_propagation_max_depth`）、五語 locale（`reference_cycle`）、
`collection_type.rb` 註釋、dev doc（§3／§5 B10／§7 表首）、handoff（§①補兩輪、§②更正）、
三個 spec 檔＋`spec/lib/tasks/catalog_rebuild_spec.rb`（真 invoke 格）。

### 追加突變（2026-08-26 第七輪）

ML2／ML1a／ML1b／ML1c／ML4 **全紅**。🔴 ML1c 前兩次都是綠的，兩次原因不同：
第一次沒有測試看著深度上限；補了測試之後**仍然綠**，因為我的起始狀態不在震盪軌道上
（(0,0,0) 起跑第一步就收斂）。把成員種在軌道上（a=1,b=c=0，從 B 起跑）才轉紅。
⇒ 與第五輪 MJ3 同一課的加強版：**測震盪／順序這類動態性質，起始狀態必須落在
會展現該性質的那條軌道上**，否則測到的是一個碰巧收斂的特例。

## 2026-08-26 第八輪整合修復（提出 9／確認 8／否決 1；去重後 6 條）

🔴 **嚴重度分布首次明顯下降**：本輪只有**一條 🔴**（前幾輪各 2〜4 條），
且它不是新引入的功能缺陷，而是「對外面的原子性」這種收尾層級的問題。

| # | 嚴重度 | finding | 處置 | 20.2 類型 | 反向複驗 |
| --- | --- | --- | --- | --- | --- |
| M5 | 🔴 | `notify_members_changed!`（cache stamp／outbox／反向傳播三件事）在寫成員的 transaction **之外**呼叫。變更判定是「本輪 `created_at >= 世代` 或 swept>0」，所以只要 notify 失敗一次（`bump_collection_members!` 的 `update_all` 撞上別人的 `Collection.lock` 而 LockWaitTimeout；或 worker 在 commit 與 notify 之間被 deploy 殺掉），成員已落庫、status 已 OK，**重跑一律 inserted=0／swept=0** ⇒ 三個對外面永久遺失＝K8 修掉的「引用者永久錯誤且無自癒」原樣復發。倉庫慣例相反（`SaveProduct#commit` 的 `enqueue_event!` 在 txn 內） | 修復**三層**：①notify 移進掃尾 txn（三者都是同一個 DB 的寫入，不是鐵律 5 禁止的 txn 內外部 IO）②🔴 **實跑才發現移進去還不夠**——批次 upsert 是逐批各自的 txn，成員列在失敗後仍留著，重跑的 inserted／swept 仍是 0 ⇒ 變更判定加一項「上一輪沒走到 OK（status ≠ OK）就強制 notify」③🔴 **再一層**：`resumed` 必須讀**鎖定讀回傳的那一列**——`update_columns` 在回滾後仍留著記憶體裡的新值，用傳進來的物件會宣稱自己是 OK | 5（fail-open）＋4 | MM5a／MM5b／MM5c 三格紅＋engine spec（notify 失敗 ⇒ status 不停在 OK；解除故障後重跑**必須補上** outbox） |
| M1 | 🟡 | 第七輪的傳播深度上限擋的是**路徑長度**而不是環：一條合法**無環**的引用鏈只要超過上限，尾端以後的系列永遠收不到重建通知、成員停在錯值（K8 的症狀在長鏈上重開；實測 24 層鏈的第 21〜23 層停在錯值）。而 dev doc／limits／程式碼註釋三處都把它框成「既有環的安全帶、正常資料不會有環」——無環資料照樣觸發那行警告，運維據此找環會找不到 | 修復：改用 **visited 集合**（只跳過這條傳播鏈上已算過的系列）⇒ 環走一圈就止步、無環長鏈照走完，鏈長天然有界於「這家店的智慧系列數」。刪掉 `rebuild_propagation_max_depth` 鍵與三處誤導敘述 | 4＋2 | MM1a／MM1b 紅＋engine spec 24 層鏈格（**種下「每層都會變」的起始狀態**，否則第一層就收斂） |
| M2 | 🟡 | 環守衛是純讀的 TOCTOU：`reaches?` 在 `normalize` 內跑，位於 transaction 與 `Collection.lock` **之外**，而那個列鎖只鎖被編輯的**那一列**——被引用方那一列從頭到尾沒鎖 ⇒ 兩個並行請求各加環的一端時構造上不互斥，兩邊都通過、環落庫。倉庫對「跨兩列的不變量」已有現成做法（`HandleChange.serialize!` 的店級序列化） | 修復：`replace_sources!`（在 txn 內）先 `serialize!(shop)` 再用 `ancestors` 複查一次 | 4 | MM2 紅（測試把 **pre-flight 打瞎**才測得到第二道——序列化測試裡兩個請求本來就不交錯） |
| M3 | 🟡 | `reaches?` 每訪問一個節點打一次 DB，且 `normalize_rule` 對**每一條** collection 型規則各叫一次、跨規則零記憶化 ⇒ 上限相乘可在同步請求路徑打出數十萬次查詢 | 修復：新增 `ReferenceGraph.ancestors`（逐層批次查，一次算完 target 的全部祖先），寫入層每請求算一次、所有規則共用 | 4 | MM3 紅＋graph spec（查詢次數與**層數**成正比而非節點數） |
| M4 | 🟡 | 三條求值路徑只有兩條實作「遇 unknown ⇒ 整系列 ERROR、零寫入」：`ResyncProduct` 沒有 rescue ⇒ `Unsupported` 穿出逐系列迴圈，**拓樸序中排在它後面、與壞系列無關的系列整批漏算**，且每次重試都在同一點再炸 ⇒ 那些系列永久停在錯值 | 修復：per-collection rescue，標 ERROR（共用 `Rebuild.mark_error_for`）後**跳過這一個**，其他照算 | 2 | MM4 紅＋engine spec（壞系列 ERROR、無關系列照算） |
| M7 | 🟡 | handoff §③ 三個子句在 HEAD 全為假（「B1〜B10」實為 B1〜B12；「互相引用需要不動點迭代、由 rake 兜底」已被 L1 的寫入層拒收取代；「resync 的兩趟」早已是單趟拓樸序）。§③ 正是鐵律 21.2 要求「不得留空、要能直接接手」的那一段 | 修復：改寫成 HEAD 實況並標更正註 | 2 | 文字複驗 |

否決 1 條：環上的既有系列無法用現有 sources 存檔（反駁：宣告式契約下該情境屬既有環的登記面）。

Changes（本輪）：`rebuild.rb`（notify 進 txn／resumed 判定／鎖定讀／visited）、
`resync_product.rb`（Unsupported 守衛／notify 進 txn）、`reference_graph.rb`（`ancestors`）、
`save_collection.rb`（記憶化＋序列化複查）、`rebuild_job.rb`（visited 參數）、
`config/limits.yml`（刪深度上限鍵）、dev doc §5、handoff §③、四個 spec 檔。

### 追加突變（2026-08-26 第八輪）

MM1a／MM1b／MM5a／MM5b／MM5c／MM4／MM2／MM3 **全紅**。
🔴 MM2 第一次是綠的：序列化測試裡兩個請求本來就不會交錯，pre-flight 先擋掉了
⇒ 必須把 **pre-flight 打瞎**才測得到序列化點內的第二道。與 MJ3／ML1c 同一家族：
**測「第二道防線」時，必須先讓第一道失效**，否則測到的是第一道。

### 🔴 本輪踩到的流程坑（登記；候選 91 §3）

**timeout 砍掉的突變 runner 會把突變留在樹上**：外層 `timeout` 的 SIGTERM 讓 Python 的
`finally`（還原原檔）不執行，三次逾時各留下一個活突變（C13 的 `if false &&`、
C14 的白名單放寬、C18 的 create 路徑同步移除），其中 C14/C15 進一步讓後續 runner 報
「錨點找不到」而**跳過驗證**——看起來像跑過，實際上該格從未紅過。
固定處理：①每格獨立進程＋比 timeout 短的預算 ②runner 掛 SIGTERM handler 轉
SystemExit（讓 finally 跑）③突變輪結束後**逐一 grep 每個被突變的錨**確認還原
（抽查一兩個＝本輪誤判「樹完整」的原因）④跑一次完整相關 spec 當終驗。

## 尚未完成或需注意的風險 (Pending / TODO)

見 dev doc §5 的 P11-B1〜B10（2026-08-26 收斂輪新增 B10＝互相引用系列的不動點；
sub_collections／variants 目標／手動系列的 exclusion
引用邊界／unknown 白名單暫閉／排序三份打架屬包 8／per-source 60 上限未實測／
shareable 語義／命中數預覽屬 UI 包／變體子頁觸發面未驗證）。
另：本包 UI 未動——商家目前只能經 GraphQL 契約建智慧系列；規則編輯器＝包 13。
