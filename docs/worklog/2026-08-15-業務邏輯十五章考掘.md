# 工作記錄：Shopify 業務邏輯十五章考掘（2026-08-15）

> 交付：`docs/research/90-blueprint/01-15`（15 檔，6320 行）｜考掘日 2026-08-14｜來源 487 條唯一 URL

## 已完成的工作 (Done)

使用者要求「深度分析和研究所有官方文檔，寫一份可落實在系統開發上的業務邏輯」。
倉庫已有 `00-10` 模組研究、`21/22/44/59/60/7x` admin 實測、`docs/specs/11-19` 我方規格
——**缺的不是資料，是一份「規則正典」**：狀態機的值域全集、公式的捨入規則、
上限值的出處、以及每一條「本尊怎麼做 vs 我方裁定怎麼做」。本輪補這一層。

15 個領域各一章，每章固定七節（A 物件模型／B 狀態機／C 規則與不變量／D 流程／
E 跨模組耦合／F 落地對應／G 來源），硬性要求：值域窮舉不得寫「等等」、
狀態機不得有孤兒態、計算類給公式含 rounding、每條斷言附 URL＋取證日期。

### 三輪：考掘 → 批判 → 補證

批判階段三個視角（完整性／可實作性／與鐵律一致性）挑出 **51 條缺口**，
12 章回官方文檔補證修訂。幾個有代表性的：

| 章 | 原稿的問題 | 補證後 |
|---|---|---|
| 10 稅務 | 捨入粒度列為 openQuestion | 官方分步敘述定案＝**單價×率→banker's→×量**，且**排除**「行小計×率」（同一數例兩法差 8 cents） |
| 04 訂單 | 風險 `recommendation` 的聚合寫成事實 | 覆核官方頁確認**完全未載** ⇒ 改為明文登記我方裁定（最壞者勝）＋待實測 |
| 05 支付 | 交易型別用萬用字元壓縮 | 逐值全錄 **91 個生態值**（壓縮寫法連計數都錯：LENDING_* 實為 12 不是 10） |
| 02 庫存 | `restockType` 沿用 46a 的 3 值 | 實抓確認是 **4 值**且**沒有叫 `RESTOCK` 的值**——抄成 3 值會讓未履行退款的行留在訂單上可再履行 |
| 09 履約 | 官方運費合併例抄漏一個 profile | 補全後「各組取最便宜者相加」的規則才自洽；FO topic 由 13 支點算為 **20 支** |
| 01 商品 | 「四態兩兩可達」 | 降級為「無孤兒狀態」——ARCHIVED 的出邊只在動作面證實，ARCHIVED→UNLISTED 官方兩面都沒提 |

### 🔴 法律紅線自檢抓到 9 處

掃描全部 15 章的引號內英文長度，抓到 9 處 16–20 字的官方原句直引，
全部改寫為中文轉述並保留來源 URL（鐵律 9）。
最長那條在 03 章（20 字，cart 行拆分條件）。剩餘最長 12 字，附出處，屬合理引用。

## 修改的檔案與核心邏輯 (Changes)

- `docs/research/90-blueprint/01-products.md` … `15-platform-core.md`（新增 15 檔）
  ——每章七節結構已機器驗證齊備（A–G 無缺節）。
- `docs/research/90-blueprint/03-cart-checkout.md`／`06`／`07`／`10`：引句改寫（9 處）。
- 章與章的交叉引用一律用 `§{章號} {節}` 格式，章號即檔名前綴 01–15。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **本輪全部是文檔考掘，零實測**。標 ⚠️ 的條目一律是「shopify.dev／help 查不到」，
  **不是「大概是這樣」**。這些必須依鐵律 12 回測試店 `chill-love-u5q5mnzq` 親自點擊確認。
  清單已收斂進總綱 §7（112 條，其中 Tier A 阻塞 M0/M1 的有 31 條）。
- 🔴 **來源會改版**。全部斷言取證日期＝2026-08-14，跨季度 API 版本需重新取證。
  尤其 01 章記錄的 collection `sources` 新模型是 2026-07 版才轉的，`ruleSet` 已 deprecated。
- ⚠️ **各章的 F 節「本尊 vs 我方裁定」是各章自行判定的**，跨章重複與衝突在總綱 §6 才收斂
  （98 條去重後）。單看某一章的 F 節可能與另一章對同一條裁定的措辭不同。
- ⚠️ **13 章表頭記 `fulfillment_orders` 21 支、09 章兩次點算為 20 支**，尚未回頭統一
  （已登記 §7 Q-68，最終數以 introspection 實測為準）。
- ⚠️ **46a 的 `restockType` 3 值尚未回寫**——本輪只在 90-blueprint 內收斂，
  既有 `docs/research/46a` 仍是舊值，下一輪要回頭改，否則兩份文檔各說各話。
