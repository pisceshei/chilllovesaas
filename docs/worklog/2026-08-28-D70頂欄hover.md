# 2026-08-28 — D70 頂欄 hover 態對齊（視覺對齊弧收尾）

使用者採納建議：hover 收尾 → 回 S6c；其餘兩項擱置並記錄。🟢 純 CSS，不觸 18.3。

## 已完成的工作 (Done)

- **取證**：本尊 hover 機制＝外層 `_BorderGradient` 的 `_Hover` 常駐能力類 ＋ `:hover` 選擇器；
  規則在 `render-common-*.css`（不在 main-*.css——CSS modules 分塊，40 張表逐一 fetch 定位）。
  真觸發實測：三控件 hover 全部收斂 **#222＝暗域 surface-hover**、圖示色恆 #dcdcdc。
  ⚠️ 搜尋鈕 hover（#222）比 rest（#282828）**更暗**，照實照抄。
- **實作**：`.cl-scope-dark` 下覆寫 icon-button／store-chip 的 hover → `var(--surface-hover)`；
  **不改基礎規則**——亮域 7 個用點的 `--surface-2`（#f7f7f7）恰等於本尊亮域 hover 值
  （`91` §3.39 W-3 名稱倒置），改了反而更遠。原型同步。
- **驗證**：兩份檔**各自**真觸發實測（`computer.hover` ＋ `matches(':hover')` 斷言 ＋ 讀 computed）：
  暗域三控件全 #222；亮域 icon-button hover 仍 #f7f7f7（未被波及）。
- **兩項擱置裁定落地**（D70 ①②）：四族 rem 擱置（重啟條件＝實際回饋或下次 token 大改制）；
  `rwd-check.mjs` 擱置到下一個大 UI 輪開頭（作為首包，18.3）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `app/assets/stylesheets/admin.css` | `.cl-scope-dark .cl-icon-button:hover`／`.cl-scope-dark .cl-store-chip:hover` 兩條（含取證註釋） |
| `docs/design/chilllove-admin-v2.html` | `.cl-scope-dark .icon-btn:hover`／`.cl-scope-dark .store-chip:hover` 同構兩條 |
| `docs/DECISIONS.md` | D70（hover ＋ 兩項擱置） |
| `docs/specs/91-pit-register.md` | §3.43（W-1 active 態未實作／W-2 座標框架坑／兩條處置補記） |

`tokens.css` 未變動（無 token 增改，sync 394 行相同）。

## 20.2 適用類型／固定處理／反向複驗輸出

| 類型 | 適用 | 固定處理已跑 | 反向複驗輸出 |
|---|---|---|---|
| ① 證據不完整 | 是 | hover 值不從記憶推：真觸發實測 ＋ 原文交叉（兩者同指 #222） | D70 表 |
| ② 生產者／消費者 | 是 | 兩份檔同一提交、**各自**真觸發量測 | 兩份三控件全 #222 |
| ④ 狀態空間 | 是 | 亮域用點先列舉（7 個）再決定 scoped 覆寫；亮域 hover 驗證未被波及 | 亮域 #f7f7f7 |
| ⑤ fail-open | 是 | 🔴 hover 落空不報錯（座標框架坑）⇒ 每次 hover 後斷言 `matches(':hover')` 才讀值 | 第一輪四格全 `isHovered:false` 形態被識破後重測 |
| ⑦ 工具鏈 | 是 | 註釋掃描 `\*/` 緊接非空白＝0 新增（D68 事故的固定檢查） | 0 |

## 尚未完成或需注意的風險 (Pending / TODO)

- **`:active` 按壓態未實作**（`91` §3.43 W-1）——含本尊 `[data-admin-next]` 閘門下的
  scale 按壓與 rest 70% 機制（該閘門在測試店頁面未啟用）。
- **名稱對映（§3.39 W-3）仍未解**：本輪靠「值恰好相等」繞過，根治要做對映表。
- 視覺對齊弧到此收束；**下一步＝S6c 系列 popover**（`Popover` 原語已在 main）。
