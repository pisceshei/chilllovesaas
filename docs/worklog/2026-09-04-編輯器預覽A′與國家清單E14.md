# 2026-09-04 E14 本尊編輯器預覽擷取（A′）與 `all_country_option_tags` 更正（鐵律 22.1「兩者皆是」收口）

分支 `parity/e14-editor-a-prime`（自 main `bd0ffd4e`，E13 #320 合併部署後）。規範 `docs/dev/e13-theme-editor-preview-parity.md`（§1 #4、§2、§4 改為已取得）；
官方逐字與第一方觀察 `docs/dev/external-facts.md` §G20；未取得／範圍外／更正 `docs/specs/91-pit-register.md` §3.81。`scripts/` 屬 18.3 ⇒ 依 D83 由工作階段代合。

## 已完成的工作 (Done)

- **A′ 取得法**（使用者把 Chrome 帶到前景＋DevTools 貼出文件請求）：本尊編輯器預覽不是 hoko.vip，而是 `https://pnrjnw-sy.myshopify.com/?oseid=<編輯器工作階段 id>`；
  `&source=visualPreviewInitialLoad` 那份是零 section 的殼（編輯器再串流填入），不帶 source 的那份是完整設計模式渲染（`html.shopify-design-mode`、
  20 段、`data-shopify-editor-*`、`Shopify.designMode = true`）。在非嵌入情境載入時，Shopify 的 `storefront-modules` 執行期（`OnlineStoreEditorData`／
  `oseid` LocationBehavior）會把導航改寫到一般店面（live 主題、無設計模式），故工具加 `--block "pat1,pat2"`（CDP `Network.setBlockedURLs`）擋掉
  `storefront-modules` 與 `visual-preview-modules` 才量得到；live 店面本來就不載這兩支，擋掉只移除編輯器管線。
- **A′ vs 我方編輯器預覽（首頁 1280／768／390）**：修法前 19/21，修 `all_country_option_tags` 後 **20/21**——只剩 `__root__`（我方橋 13 個覆疊元素
  ／本尊兩個 JS 建立的 video popup 抽屜／`#Drawer-Search` 內容的 auto margin 報告值 920 vs 0 而 `offsetLeft` 同為 920）。
  A′ vs hoko live＝五個 popup-group 段＋`__root__` 差——與 E13 §3 的主題自定義設計模式差異判斷一致（本尊自己也如此）。
- **由 A′ 抓到並修的缺口 #4**：`all_country_option_tags` 先前做成「market ∩ 有費率 zone」的國碼 option（那是 `country_option_tags` 的語義且格式錯）；
  本尊設計模式渲染 Ella cart-shipping-calculator 出 238 個 option（首項 `---`、英文 value、店面語言在地名、`data-provinces` 的 [[英文, 在地名]] JSON、
  依在地名碼位排序、value 保留 `'`、JSON 走 `&quot;`／`&#39;`）。新 `ThemeEngine::CountryOptionTags`（字典 `config/country_option_tags.json`：237 國、
  1128 個子區域、zh-CN 在地名；由本尊 zh-CN 渲染輸出整理——ISO 3166 對不上本尊集合：少 21 碼、多 9 項、23 個命名差、12 國省份數不同）；
  `country_option_tags` 改為運送區域內國家（沿用 `RateResolver.sellable_countries`）同一形；`CartController#estimate_rates` 收國名或國碼。
- 商品頁 A′：`product_recommendations`／`section_product_tabs`／footer 的差全是被擋執行期的副作用（其 XHR 攔截器負責補 `oseid`，擋掉後
  recommendations／recently-viewed 的 fetch 被 abort ⇒ 骨架未換）——登記量測限制，不是引擎缺口（E12 店面對表已覆蓋該段）。

## 修改的檔案與核心邏輯 (Changes)

- `scripts/computed-parity.mjs`：`--block`（`applyBlocks`；capture JSON `blocked` 欄）。
- `config/country_option_tags.json`（新，平台字典表）；`app/liquid/theme_engine/country_option_tags.rb`（新：`all`／`only`／`code_for`；形與跳脫規則）。
- `app/liquid/theme_engine/runtime.rb`：全域 `all_country_option_tags`＝`CountryOptionTags.all(locale:)`、新增 `country_option_tags`＝`only(sellable, locale:)`；
  舊 `country_option_tags(shop)` 改名 `sellable_country_codes`。
- `app/controllers/storefront/cart_controller.rb`：`estimate_rates` 以 `CountryOptionTags.code_for` 收國名／國碼。
- 規格：`spec/liquid/storefront_display_wiring_spec.rb` W4 重寫＋W4b（在地名／排序／退英文）、`spec/requests/storefront_cart_shipping_rates_spec.rb` R5。
- 文檔：e13 doc §1 #4／§2／§3／§4、e12 doc §0、external-facts §G20、91 §3.81（含 §3.80 更正）、本 worklog／handoff。

## 尚未完成或需注意的風險 (Pending / TODO)

- 字典的在地名只有 zh-CN（本尊 zh-CN 店面觀察）；其他語言退英文名——需要各語言的本尊渲染或裁定資料來源（91 §3.81 V）。
- 🔴 假設待使用者確認：以本尊渲染輸出整理的國家／子區域清單作平台字典（地名事實資料，非文案）；若不接受，退回 ISO 3166（`countries` gem，MIT）
  並登記與本尊的集合／命名差。
- `__root__` 兩項未取得：本尊 DOM 內 `PopupModal-product-tabs-video`／`…__popup_link_desc_video` 抽屜的來源（伺服器 HTML 與 section fetch 回應皆無）；
  `#Drawer-Search` 內容 auto margin 的報告值差（幾何相同）。
- oseid 是短效工作階段票證（本輪同一值用了約一小時仍有效）；重跑 A′ 要重新從編輯器 DevTools 取。憑證類字串不入倉、不進 PR。
- 部署：無 migration；bt3 deploy → restart；驗證＝使用者 Chrome 開 demo 店編輯器，在預覽 iframe 數 `#ShippingCalculatorCountry- option` 應為 238。

## 閘門（凍結 tree 6c4c9195 後全跑，本機，dev server 已停；scratchpad `gates_e8b.py` 以 `PYTHONIOENCODING=utf-8` 逐支以 bash.exe 執行 config/ci.rb 步驟）

| 閘門 | 命令 | 退出碼 | 摘要 |
|---|---|---|---|
| Style: Ruby | `bin/rubocop` | exit=0 | 986 files inspected, no offenses detected |
| Security: Gem audit | `bin/bundler-audit` | exit=0 | No vulnerabilities found |
| Security: Frontend audit | `pnpm audit --audit-level high` | exit=0（閘門跑批與第一次重跑皆 npm registry `advisories/bulk` 逾時 exit=1；第三次重跑 exit=0） | No known vulnerabilities found |
| Security: Brakeman code analysis | `bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error` | exit=0 | No warnings found |
| Test: Rails | `bundle exec rspec` | exit=0 | （未解析） |
| Test: Frontend | `pnpm test` | exit=0 |  |
| Type check: Frontend | `pnpm typecheck` | exit=0 |  |
| Build: Frontend | `pnpm build` | exit=0 |  |
| Invariants: Exec bits | `bash scripts/check-exec-bits.sh` | exit=0 |  |
| Invariants: Exec bit rules regression | `bash scripts/test-exec-bits-rules.sh` | exit=0 |  |
| Invariants: Prototype lint | `python scripts/lint-prototype.py` | exit=0 | ERROR 0 / WARN 136 |
| Invariants: Lint rules regression | `python scripts/test-lint-rules.py` | exit=0 |  |
| Invariants: Dead-control baseline | `python scripts/check-baseline-raise.py` | exit=0 |  |
| Invariants: Doc claims | `ruby scripts/check-doc-claims.rb` | exit=0 |  |
| Invariants: Doc claim rules regression | `ruby scripts/test-doc-claims-rules.rb` | exit=0 |  |
| Invariants: Tenant isolation | `ruby scripts/check-tenant-isolation.rb` | exit=0 |  |
| Invariants: Design token single source | `ruby scripts/check-tokens-sync.rb` | exit=0 |  |
| Invariants: Reversal naming | `ruby scripts/check-reversal-naming.rb` | exit=0 |  |
| Invariants: Money unit boundary | `ruby scripts/check-money-boundary.rb` | exit=0 |  |
| Invariants: Money rules regression | `ruby scripts/test-money-rules.rb` | exit=0 |  |
| Invariants: Limits key types | `ruby scripts/check-limits-keys.rb` | exit=0 |  |
| Invariants: Limits key rules regression | `ruby scripts/test-limits-key-rules.rb` | exit=0 |  |
| Invariants: Rem token integrity | `ruby scripts/check-rem-tokens.rb` | exit=0 |  |
| Invariants: Rem token rules regression | `ruby scripts/test-rem-token-rules.rb` | exit=0 |  |
| Invariants: CI parity (ci.yml ⊆ config/ci.rb) | `ruby scripts/check-ci-parity.rb` | exit=0 |  |
| Invariants: CI parity rules regression | `ruby scripts/test-ci-parity-rules.rb` | exit=0 |  |
| Invariants: Workflow syntax | `ruby scripts/check-workflow-syntax.rb` | exit=0 |  |
| Invariants: Workflow syntax rules regression | `ruby scripts/test-workflow-syntax-rules.rb` | exit=0 |  |
| doc-claims 兩支（worklog 閘門表寫入後補跑） | `ruby scripts/check-doc-claims.rb`／`ruby scripts/test-doc-claims-rules.rb` | 見 PR body | |
