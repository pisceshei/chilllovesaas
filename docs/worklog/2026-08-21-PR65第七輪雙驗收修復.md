# PR #65 第七輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `2d70b480a459e6386b22fac6b0217318028a7043`：Claude comment
  `5361587574`、wrapper `5361590096`；Codex review `4987154051` 的 current-head inline 為
  `3825176230`、`3825176236`、`3825176240`。quality／test 在該 head completed success，
  但兩方都有阻斷意見，因此沒有把綠 CI 誤報成雙清。
- 修法前用 `git log -p` 追到 checker、runner、CLAIM-005 與第六輪 worklog 沿革；failure-first
  dated output snapshot 如下。四種 metadata／recheck 輸入都在舊 checker exit 0；把 snapshot
  token 換成不存在的同 cardinality 名稱後，舊 CLAIM-005 自查仍通過但與 baseline source 不等：

~~~text
Type-case: exit=0 survived=true
type-space: exit=0 survived=true
types-key: exit=0 survived=true
prose-recheck: exit=0 survived=true
BASELINE_OBJECT=available
OLD_SELF_CHECK_PASS=true
MUTATED_MATCHES_SOURCE=false
ACTUAL_COUNTS=[82, 4, 78]
~~~

- checker 檔頭的 R6 摘要、用法與退出碼表已同步 current 行為：明列 fixture flag、誤用 exit 2、
  production R6 零供給 exit 3、type key/value 契約及 recheck 必須以支援工具開頭。
- type parser 現以語義處理 key：`Type:`／`type :` 配合小寫 `count` 放行；`types:` 等
  type-like 畸形鍵與非小寫 count 值在原行 fail-closed。recheck 另用 R6 專屬 anchored regex，
  code span 只在散文中提到 `ruby` 不再算命令。
- CLAIM-005 先 `git cat-file -e` 驗 baseline object，取不到即非零；取得後直接 `git show` 兩份
  source 重算，再與樹內 snapshot 交叉核對。實跑輸出為
  `{declarations: 82, overlap: 4, new_tokens: 78}`；快照不再能單獨證明自己。
- 第六輪 worklog 的 GitHub Markdown API 數字因 exact response／request artifact 未留存，已在
  原處追加「未取得」更正；CLAIM-005 的自證修法及漏盤腳本檔頭也保留原文、相鄰更正。
- Claude 四條範圍外觀察只登記到 91 §3：runner 摘要漂移、W2 未釘 `config/ci.rb`、長 recheck
  的 Markdown／PowerShell 可攜性、m0 changelog 缺 PR #65；沒有順手修改未點名實物。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 第六輪已新增 type 值與 recheck cardinality，本輪 `5361587574`／`3825176236`／`3825176240` 又指出 key 狀態與命令起始狀態未列；同 comment 的 🔴1 是同一 producer/consumer 漏同步 |
| 既有防線失效原因 | 第六輪 20.3 只列 checker 等檔名，沒有把腳本檔頭／用法／退出碼表列為獨立契約 consumer；metadata 狀態只列 value，recheck 只列「含工具名」 |
| 被漏掉的固定步驟 | 對結構化欄位分開列 key 正常／key 變體／key 畸形、value、cardinality；對命令分開列欄位存在／工具出現／工具起頭；改腳本時把檔頭三區列為必查 consumer |
| 可重跑反向複驗 | `ruby scripts/test-doc-claims-rules.rb`；bad type key、合法 key variants、prose recheck 與既有 type value／cardinality fixture 各走獨立分支 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取／exact head | issue comments、review body、paginated inline、GraphQL threads 分源重取 | 上列 ID 全綁 `2d70b480…`；push 前仍須依 cutoff 重拉 |
| ② 生產者／消費者／契約層 | checker 實作、檔頭三區、runner、fixtures、m0、92、主 worklog 同步 | `rg` 對 type／recheck／fixture-mode／退出碼逐處核對；範圍外 changelog 只登記 |
| ③ 易腐數字／自我證明 | baseline 計數讀 source object；樹內 snapshot 只作雙向交叉核對 | mutation 保持 82／4／78 仍被 source mismatch 擋；baseline 不可達時 `git cat-file` 非零 |
| ④ 狀態空間／multiplicity | key、value、count、recheck cardinality、命令起始分開 | 三個新 fixture 與既有 CASE 各有獨立期望；合法 key variants 是反向案例 |
| ⑤ 外部回應／歷史層 | 沒有 retained artifact 就標未取得；歷史原文不覆寫 | 第六輪 Markdown API 句旁追加來源 ID 與未取得邊界 |
| ⑥ workflow／Windows 供給 | 本輪不改 workflow／`config/ci.rb`；發現未點名 wiring 缺口只登記 | `git diff --name-only 2d70b480..HEAD -- .github/workflows config/ci.rb` 提交後須為空 |
| ⑦ Markdown | 新表格每列欄數固定；code span 不嵌套反引號 | 本檔與 m0 以 GitHub `/markdown` API 只驗 current response，不外推歷史回應 |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：同步檔頭契約，新增 type-like key parser 與 anchored
  `CLAIM_RECHECK_CMD`，不改 R1–R5 的 `RECHECK_CMD` 射程。
- `scripts/test-doc-claims-rules.rb` 與新 fixture：加入 malformed key、合法 key variants、
  prose mentions tool 三條承重案例，並同步終態 fixture 表／突變清單。
- `docs/specs/92-claim-index.md`：CLAIM-005 恢復直接讀 baseline source 的 fail-closed recheck，
  snapshot 改為交叉核對，命令內不再嵌套 Markdown 反引號。
- `docs/dev/m0-review-convergence.md`、`docs/worklog/2026-08-21-P2宣稱索引與R6.md`：同步 R6
  終態契約、fixture 消費者與 CLAIM-005 source 邊界。
- `docs/worklog/2026-08-21-PR65第六輪雙驗收修復.md`：原處更正自證修法、漏盤契約層與未留存
  Markdown API 回應。
- `docs/specs/91-pit-register.md`：只登記四條範圍外觀察，並補本 worklog 的 A.1 關係。
- `docs/worklog/2026-08-21-PR65第七輪雙驗收修復.md`：保存本輪輸入、failure-first、20.4、20.3
  與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 最後 repo edit 後須跑定向 fixture、六條 CLAIM recheck、Markdown 結構、完整 Windows
   production-equivalent runner；提交後再跑 doc-claims 並要求 warning 0。
2. commit 後須依鐵律 15 對 `pr65-last-push` 逐項核對，重拉四種 GitHub 資料，確認無晚到意見
   才可 push、更新 tag 與 exact-head PR 描述；舊 head 結果不得外推。
3. #65 修改 `scripts/`，命中鐵律 18.3；即使雙清、threads resolved、mergeable 與 evaluator
   `1111` 全成立，也只能通知使用者人工合併，不得代行。
