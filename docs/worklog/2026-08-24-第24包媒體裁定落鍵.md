# 2026-08-24 第 24 包：媒體裁定 B6–B9 落 limits 鍵＋兩處更正註

對應規格：整合執行規格 §4-24／§5（B6–B10）；排程塊 E 第 24 包（無可見成果）。

## 已完成的工作 (Done)

- **limits.yml `media` 節落鍵**：B6 `storage_backend: self_hosted_presigned_post`
  （不啟用 Active Storage——ours 裁定）；B7 `size_limit_source_keys`（正典＝
  `content.files_*` 五鍵，不重複數字只登記來源）；B8
  `duplicate_resolution_modes: [append_uuid, raise_error, replace]`（官方三值照搬，
  shopify.dev enum `FileCreateInputDuplicateResolutionMode`，取證 2026-08-24）＋
  `duplicate_resolution_mode_default: append_uuid`（🔴 官方未載明預設＝ours：
  永不靜默覆蓋、永不因撞名失敗）；B9 `upload_media_types_enabled: [image]`；
  `statuses: [uploaded, processing, ready, failed]`（MediaStatus 四態正典）。
  B10（處理走 outbox）無 limits 鍵——落點＝第 25 包 event_deliveries。
- **CI enum 對照**：`spec/config/m0_configuration_spec.rb` 新增媒體例（四鍵釘值＋
  `size_limit_source_keys` 逐鍵解參照斷言 Numeric——引用斷鏈在 CI 紅，不是
  消費端上線後 KeyError）。注入回歸：改掉 statuses 一值 ⇒ 紅 ⇒ 還原綠。
- **兩處更正註**（B7；官方 help 逐字＝圖 20MB/20MP/100:1–1:100、影片 1GB/10min、
  泛型 20MB，出處 file-uploads 頁，取證 2026-08-24）：
  ①`docs/specs/13-spec-products-inventory-media.md` §F3——AS 敘述（B6 推翻）與
  「影片 200MB／像素如 50MP」（非官方值）加更正註、原文依分層保留；
  ②排程第 24 列的「官方 25MP」誤記與決問 20 就地更正（官方＝20MP，limits 現值正確）。

## 修改的檔案與核心邏輯 (Changes)

- `config/limits.yml`：media 節 +6 鍵（見上）。
- `spec/config/m0_configuration_spec.rb`：+1 例（raw YAML 形態——本檔是無 Rails
  快測，`Limits` 常數不可用，坑：首版用 `Limits.enum` 直接 NameError）。
- `docs/specs/13-spec-products-inventory-media.md`／
  `docs/plans/2026-08-24-三方向執行順序.md`：更正註。

## 尚未完成或需注意的風險 (Pending / TODO)

- `media.status` schema default `"ready"` 與四態起點 `uploaded` 的矛盾＝**第 26 包
  migration 修**（整合規格 §4-26 明列；本包只立正典鍵）。
- 26 包做 Media model 時 inclusion 驗證取 `Limits.enum(:media, :statuses)`
  （同 `InventoryAdjustmentGroup::REASONS` 形態），不得另抄一份。
