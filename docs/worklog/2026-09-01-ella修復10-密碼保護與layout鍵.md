# 2026-09-01 Ella 修復 PR-10：storefront 密碼保護＋layout 鍵＋current_page

## 已完成的工作 (Done)

- chill.deals /password 黃金標本對表軸（wf_4fdcbcc5-7ba）三件收口：
- **storefront 密碼保護整條**（此前功能整體不存在——標本軸 critical ①）：
  shops.storefront_password_digest（BCrypt、NULL=off——demo 店保持開放）＋
  GET/POST /password＋全 storefront 閘（PW2/M10-1 紅證）；🔴 通行 cookie
  綁密碼雜湊 ⇒ 改密碼即失效全部舊 cookie（PW2/M10-2 紅證）。
- **平台級密碼頁**（標本實錘：真店 /password＝零 script/零主題資產/單一
  style 塊的平台頁，非 Ella 渲染）：Rails ERB 直渲染不進 Liquid 引擎；
  結構/類名/文案逐字對表（"This store is password protected…"、
  autocomplete="nope"、error-container、referrer=never、noindex）；
  🔴 CSS 原文/ShopifySans/Shopify 商標＝鐵律 9 紅線 ⇒ 量測值重寫＋系統
  字型棧＋我方店名（PW1 逐項釘）。
- **template JSON layout 鍵**（官方三值逐字取證 2026-09-01）：字串→
  layout/{name}.liquid、false→無 layout、缺鍵→theme.liquid；指名缺檔回落
  theme（PW4/M10-3 紅證——Ella password.json 的 "layout":"password" 從此
  正確包殼）；{% layout %} tag 與 wrapper 屬性未接（登記 V）。
- **current_page 全域**（官方：非分頁資源恆 1）：nil!=1 令每頁 title 長
  「– Page 」尾巴的全頁性 bug 根治（PW5/M10-4 紅證）。
- 突變 4/4 殺。

## 修改的檔案與核心邏輯 (Changes)

- 新：migration（shops.storefront_password_digest）、password_controller.rb、
  views/storefront/password/show.html.erb、storefront_password_spec.rb（4 例）。
- 改：routes（GET/POST /password）、storefront/base_controller（閘）、
  page_renderer（render_layout 三值）、runtime（current_page）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 標本開放問題（誠實登記）：真店為何回**平台頁**而非主題 password.liquid
  （官方文檔言明密碼頁可主題化）＝判準未取得——兩形我方現皆有
  （平台 ERB＋layout 鍵已通），切換判準待付費密碼保護店取證。
- 密碼設定的 admin UI 未接（settings 線後補）；wrapper 屬性／{% layout %}
  tag＝V。
- checkout 是否豁免密碼閘＝未取證（現行全閘）。
