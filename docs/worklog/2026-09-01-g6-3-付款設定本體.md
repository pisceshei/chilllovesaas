# 2026-09-01 G6-3（步 2）付款設定本體

## 已完成的工作 (Done)

- 20 步方案的步 2：Settings → Payments 的三塊缺口補齊（86 §1–§3 對位）：
  1. **Payment capture method**：`shops.payment_capture_method` 欄（default
     `automatic_at_checkout`）＋query/mutation＋86 §2 三 radio modal；
     `automatic_per_fulfillment`（Plus 專屬）誠實拒絕（`FEATURE_NOT_ENABLED`）。
  2. **Manual payment methods**：子頁＋⊕ 四值選單（已存在內建消失）＋兩欄 setup 表單
     ＋activate/deactivate（設定保留）；GraphQL 全套 4 mutations＋query。
  3. **Provider activation 狀態機**：`shopPaymentProviderActivate/Deactivate`；
     activate 前置＝憑證指紋；checkout `configured_provider` 改讀 `status=="active"`
     （唯一啟用真相；三個消費點＝付款段選項／pay／pay_status，同一函式——見
     `docs/dev/g6-payment-settings.md` §3②）。
- 突變輪 7/7 殺＋canary（同 harness 未突變 exit 0）：
  M1 值域檢查拿掉→S1 紅；M2 Plus 值全收→S2 紅；M3 activate 不驗憑證→S3 紅；
  M4 checkout 退回指紋門→M4 格紅；M5 deactivate 改刪列→S4 紅；
  M6 create 吞 model 錯誤→S5 紅；M7 保留名驗證拔掉→S6 紅。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260901030000_add_payment_capture_method_to_shops.rb`＋`db/schema.rb`
- GraphQL 新檔：`app/graphql/types/shop_payment_method_type.rb`、
  `app/graphql/types/errors/{shop_payment_method,payment_capture_method}_user_error_{code,type}.rb`、
  `app/graphql/mutations/payment_capture_method_update.rb`、
  `app/graphql/mutations/shop_payment_method_{create,update,activate,deactivate}.rb`、
  `app/graphql/mutations/shop_payment_provider_{activate,deactivate}.rb`
- 註冊：`app/graphql/types/query_type.rb`（`shopPaymentMethods`／`paymentCaptureMethod`）、
  `app/graphql/types/mutation_type.rb`（本包新增註冊，數量可複驗：
  `git diff main -- app/graphql/types/mutation_type.rb | grep -c "^+    field"`）
- 🔴 `app/controllers/storefront/checkouts_controller.rb`：`configured_provider` 改
  `status == "active"`；`psp_payment_options` 的過期註釋（「status 欄不參與」）同批更正
- 前端：`SettingsManualPaymentMethodsPage.tsx`（新）＋`SettingsPaymentsPage.tsx`
  （capture modal／provider Activate/Deactivate 鈕／manual methods 真連結）＋
  `App.tsx` 路由＋`app/assets/stylesheets/admin.css`（cl-choice／cl-menu-list／
  cl-config-list__button）＋i18n 五語系各 +33 鍵
- 測試：`spec/graphql/payment_settings_spec.rb`（新，16 例）；
  `spec/requests/storefront_psp_payment_spec.rb`（setup 顯式 `status: "active"`＋
  M4 守衛格）；`SettingsPaymentsPage.test.tsx` 同步＋2 新例；
  `SettingsManualPaymentMethodsPage.test.tsx`（新，4 例）
- 文檔：`docs/dev/g6-payment-settings.md`（新）

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **部署後一次性遷移**：既有已設憑證的店 status 仍 inactive ⇒ 部署本包後結帳頁
  PSP 選項消失，需在生產跑一次「有指紋的列翻 active」（本輪部署段執行並記輸出）。
- capture 模式**設定面完整、行為面待 authorization 流**（G6-1 Airwallex 現為即時
  capture）：`automatic_after_fulfilled`／`manual` 的兩段式請款行為隨 orderCapture
  完整版落地；已在 `docs/specs/91-pit-register.md` §3 登記。
- 86 §2 的 V 項照舊（官方 help 四值 vs modal 三值疑合併形）——limits 四值不動。
- 前端未做拖曳排序（`position` 欄已留；本尊 manual methods 無排序 UI，不補）。
