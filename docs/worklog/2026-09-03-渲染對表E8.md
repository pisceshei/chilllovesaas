# 2026-09-03 渲染 1:1 對表（E8）：對表工具＋鏡像店＋首頁首批引擎形差

> 使用者裁定（2026-09-03）：「你做的都必須要和右邊的 live preview 和買家前台要和 shopify 本尊完全一樣。包括他的 css 和尺寸以及全部的參數」。
> 規範落點：`docs/dev/e8-render-parity.md`；官方逐字：`docs/dev/external-facts.md` §G；範圍外／V：`docs/specs/91-pit-register.md` §3.75。

## 已完成的工作 (Done)

- **對表工具**：`RenderParity::Normalizer`（只抹平台／租戶身分形：主機含埠、CDN 主題資產與 `?v=`、字型雜湊、`sections--{數字}`／`template--{數字}`、block 實例前綴、CSRF／reqid、placeholder 插圖本體、`Shopify.shop`／theme id／cdnHost；可抹我方路由前綴；切段補 `__head__`，終點含 `<!-- END sections:`）、`RenderParity::Report`（token 多重集合 Jaccard＋首差片段＋尾差片段＋head 資產集合差）、rake `render_parity:diff`／`render_parity:mirror`。
- **鏡像店**：`RenderParity::Mirror`＋`spec/fixtures/render_parity/hoko.json`（hoko.vip 快照：店名「我的商店 3」、HKD、zh-Hans⇔zh-CN、市場 TW、登入連結／含稅旗標、3 商品、集合 frontpage、聯絡頁、主選單首頁·目錄·聯絡我們；主題 fixture／`THEME_CHECKSUM`）。本機 `mirror` 店建成、對表全程在本機 rails server（3000）跑。
- **首頁對表結果（本機 mirror vs hoko.vip 2026-09-03 快照，正規化後）**：21 段（含 `__head__`）**17 段位元組相同**；其餘 4 段差異全部落在已登記項——`section_7hRDd8`（0.960）只剩貨幣格式 `$19.99` vs `HK$19.99`；`header_default`（0.968）／`cart_drawer`（0.988）只剩本尊新版顧客帳戶登入 URL 與 JSON-LD 主機；`__head__`（0.925）只剩平台注入腳本、dev 主機、字型檔路徑。孤立 `\r` 數 14,794 vs 本尊 14,762。
- **引擎形差 27 條**（逐條證據見 dev doc §2）：bug-compatible whitespace trimming；`<style data-shopify>`；`section.index`／`index0`／`location`（SRA／編輯器 nil）；缺群組零輸出＋群組 BEGIN／END 換行；`inline_asset_content` 照檔輸出；`shop.customer_accounts_enabled`（新欄）；語言碼 zh-Hans⇔zh-CN（`LocaleTags`，含表單反查）；`{% schema %}` tag 留位；form 隱藏欄位無分隔；block 實例 id（`BlockIds`＋重複渲染尾綴 `-N`＋每 block 尾接 LF）；`ColorDrop` 與字串相等；placeholder 逐名外框、無 class 參數即無 class；partial 錯誤名 `snippets/x`；`window.Shopify` 頭段形；字串取屬性回空字串；`nil == empty`；資源型 setting（未選＝空字串、查無＝nil、動態純量＝blank、drop `to_s`＝handle、缺鍵仍走資源規則）；靜態 block 先合併 `closest` 再求值動態來源；`{% render <block>, k: v %}` 參數；json `\/`；`stylesheet_tag` 不輸出 `preload` 屬性；`font_face` woff 備援行；`color_modify` alpha 整數形；`link.handle`／`current`／`child_current`（Unicode handleize）；`linklists[缺]` 回空物件；`cart.taxes_included`（新欄）；`money` 空值為空。
- **規格**：RP1–RP6（工具）、RF1–RF21（形差，含更正的 PS1／F1／F3／F8／SG1／B2／B3／W1–W4）；`spec/liquid`＋`spec/requests`＋`spec/services`＋`spec/graphql`＋`spec/models` 1984 例，僅 7 條併發規格在同機負載下假紅、單獨重跑全綠（91 §3.75 登記）。
- 三次自我更正（同日、已在註釋與規格留痕）：`inline_asset_content` 曾誤判為修尾；整數取屬性曾誤納入空字串規則；F8「隱藏欄位各一行」是 DevTools 排版誤讀。

## 修改的檔案與核心邏輯 (Changes)

- 對表：`app/services/render_parity/{normalizer,report,mirror}.rb`、`lib/tasks/render_parity.rake`、`spec/fixtures/render_parity/hoko.json`、`spec/services/render_parity/render_parity_spec.rb`。
- 引擎：`app/liquid/theme_engine/runtime.rb`（PARSE_OPTIONS、SCHEMA_RE 三組、PartialNamer、theme_info、section index／location、群組輸出、block 實例 id／尾綴／LF、closest 先於動態來源、NumericLookup／NilEmpty 掛載）、`drops.rb`（SectionDrop index／location、BlockDrop key／instance_id／path、ColorDrop ==、LinkDrop handle／current、LinkListsDrop 空物件、CartDrop taxes_included、SettingsDrop 資源型與缺鍵、資源 drop to_s、ShopDrop customer_accounts_enabled、RequestDrop iso_code）、`tags.rb`（StyleTag、FormTag、RenderTag 參數形）、`filters.rb`（inline_asset_content、stylesheet_tag preload、font_face、handleize、css_alpha、money 空值）、`placeholder_svg.rb`（FRAMES）、`shopify_global.rb`、`page_renderer.rb`（template 迴圈 index、bootstrap 參數）、`json_serializer.rb`（script_safe）、新檔 `block_ids.rb`／`locale_tags.rb`／`numeric_lookup.rb`／`nil_empty.rb`。
- 其他：`app/services/storefront/localization_context.rb`、`app/controllers/storefront/localization_controller.rb`（本尊碼反查）、`app/graphql/types/theme_type.rb`（SCHEMA_RE 第 2 組）、migrations `20260903120000`（customer_accounts_enabled）／`20260903121000`（taxes_included）、`db/schema.rb`。
- 文檔：`docs/dev/e8-render-parity.md`（新）、`docs/dev/external-facts.md` §G1–G12、`docs/specs/91-pit-register.md` §3.75、本 worklog、`docs/handoff/2026-09-03-渲染對表E8.md`。

## 尚未完成或需注意的風險 (Pending / TODO)

- **待使用者裁定**（dev doc §4）：①路由前綴恆帶地區（67 §F.1(b)）vs 本尊主市場預設語言無前綴；②店級貨幣格式（本尊 `shop.money_format`）vs 鐵律 10 的 `HK$` 範例。未裁定前這兩處永遠是差異。
- 其餘頁面（products／collections／cart／search／404／pages／blogs）快照已在 scratchpad，尚未逐頁跑；computed CSS 逐元素比對未做（需本機 Chrome 同寬並排）。
- bt3 線上鏡像店未建（需 `THEME_CHECKSUM`＝demo 主題匯入 checksum，storage 目錄可共用）；部署含兩支 migration。
- 未實作：stylesheet `preload` 的 Link header；woff 字型檔（只輸出 URL 形）；本尊新版顧客帳戶 URL（平台功能）。
- V 項（91 §3.75）：停用 section 是否佔 index、群組缺 type、`linklists[缺]` 物件形、section／layout 錯誤路徑名、zh-Hant⇒zh-TW、money 空值、link.active 判準。
- 併發規格在同機負載下假紅（單獨重跑全綠）——CI 環境無此干擾，但本機全套跑時先停 dev 伺服器。
- 突變輪 M55–M83（`mutate_e8.py`）於 commit 後跑，結果補記於 PR 描述。
