# 工作記錄：主題資產 URL 本尊形 T12（`asset_url` 家族＋字型＋Files 頁檔）——2026-09-05

> 路線圖 T12；規格章節＝`docs/dev/t12-theme-asset-urls.md`；取證＝`docs/dev/external-facts.md` §G28；未取得＝`docs/specs/91-pit-register.md` §3.89；
> handoff＝`docs/handoff/2026-09-05-主題資產URL本尊形T12.md`。真店變更清單：無（本包只讀 hoko.vip 公開頁與官方文檔）。

## 已完成的工作 (Done)

- 七個資產 URL 濾鏡改出本尊形（主機含埠＋`/cdn/shop/t/{id}/assets/{file}?v={摘要}{版本秒}`、`_{size}` 尺寸形、`/cdn/shop/files/{name}?v=`、
  `themes_support/{stem}-{8hex}{ext}`、`/cdn/s/global/{file}`、`/cdn/fonts/{family}/{handle}.{sha1}.woff2`）；`font_face` 兩行 src 走 `font_url`。
- `compiled_assets` 的 `?v=` 由 10 位時間戳改成本尊 29 位形（摘要＋版本秒），主機改含埠。
- 供給端：`/cdn/shop/t/:theme_id/assets/*`（租戶內任一主題 id、尺寸形回原檔、逃逸 404、標頭照 hoko）、`/cdn/fonts/*`（雜湊比對、immutable＋CORS）、
  `/cdn/shop/files/*`（filename → StoredFile，尺寸形 → 官方尺寸表 width）、themes_support／global（404，V）。舊 `/theme-assets/*` 留作相容。
- 規格 TA1–TA10（新檔）＋F1／F3／C7 校準；fixture `t12-asset-probe` section＋`product.t12.json`。
- 本機 Ella mirror 對照：89 個 asset URL（hoko 同頁 89）、`?v=` 28／29／30 位、`/theme-assets/` 0 處、資產／字型／compiled 抽查 200（dev doc §5）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `app/liquid/theme_engine/asset_urls.rb` | 新：七種形、`version`（Rails.cache 鍵 `[theme.id, updated_at, file]`）、`digest64`、尺寸表、`split_size`／`sized_name` |
| `app/liquid/theme_engine/font_files.rb` | 新：`public/fonts/*/*.woff2` 啟動時讀成常量（SHA-1＋本體） |
| `app/liquid/theme_engine/filters.rb` | `asset_url`／`asset_img_url`／`file_url`／`file_img_url`／`shopify_asset_url`／`global_asset_url`／`font_url`／`font_face`；private `asset_host`／`stored_file_named` |
| `app/liquid/theme_engine/runtime.rb`、`page_renderer.rb` | `asset_host:` kwarg；registers `asset_host`／`theme`／`source`；ContentForHeader 收 `asset_host` |
| `app/services/storefront/content_for_header.rb` | `compiled_version(name)`（快取）；compiled src 主機＝asset_host |
| `app/controllers/storefront/{pages,recommendations,search,cart}_controller.rb`、`app/controllers/admin/storefront_preview_controller.rb` | `asset_host: request.host_with_port` |
| `app/controllers/storefront/assets_controller.rb` | `cdn`／`font`／`themes_support`／`global_asset`／`not_found_asset`；`THEME_ASSET_MAX_AGE = 31_557_600.seconds` |
| `app/controllers/storefront/media_controller.rb` | `by_filename`；`serve` 抽出 |
| `config/routes.rb` | 五條 cdn 路由（compiled 路由之後） |
| `spec/requests/storefront_theme_asset_urls_spec.rb` | 新 TA1–TA10 |
| `spec/liquid/font_pipeline_spec.rb`、`spec/requests/storefront_content_for_header_spec.rb` | F1／F3 字型形、C7 compiled `?v=` 形 |
| `spec/fixtures/theme_engine/minimal-1.0/sections/t12-asset-probe.liquid`、`templates/product.t12.json` | 整頁探針 |
| `docs/dev/t12-theme-asset-urls.md`、`docs/dev/external-facts.md` §G28、`docs/specs/91-pit-register.md` §3.89＋§3.88 ⚪ 終態、路線圖 T12、`docs/dev/e8-render-parity.md`／`e12-computed-parity.md`／`e19-content-for-header.md` 終態行 | 文檔 |

## 閘門

（凍結 tree 後填。）

## 尚未完成或需注意的風險 (Pending / TODO)

- bt3 部署後複驗（mirror 商品頁 asset URL 形、抽查 200、`__head__` 對表仍全同）——收尾 PR。
- 91 §3.89 V：`?v=` 摘要演算法、`asset_img_url` 縮放、缺檔形、themes_support／global 本體（`currencies.js`／`option_selection.js`／`qrcode.js` 自寫＝後續包）、
  Cache-Control 指令順序、平台 host 預覽、字型 URL 數差（T5）。
- 🔴 `Rails.cache` 為 null store 的環境（test）每次重算摘要；production 為 Solid Cache——主題檔一改必 `theme.touch`，否則 `?v=` 不旋轉、瀏覽器抓舊檔一年。
- 🔴 舊 `/theme-assets/*` 與 `/admin/store/preview/{id}/assets/*` 路由留著只為相容；新碼不得再引用 `asset_base` register 產 URL。
