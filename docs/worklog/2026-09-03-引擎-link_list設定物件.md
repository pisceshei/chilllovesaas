# 2026-09-03 引擎 E3c：`link_list` 型 setting 回 linklist 物件

## 已完成的工作 (Done)

- E3b 部署後在 demo 店複驗：main-menu 已 seed（Home／Catalog／Contact）、header 設定 `menu: "main-menu"`，但 header
  的 `nav.header__inline-menu` 仍 0 個連結。追到 `ThemeEngine::SettingsDrop#coerce` 沒有 `link_list` 分支 ⇒ setting 值是
  handle 字串，Ella 的 `{% for link in header_settings.menu.links %}` 取 `.links` 為 nil ⇒ 整條主選單空白。
- `coerce` 加 `when "link_list"`：走 `linklists` 全域 drop（同一 shop 語境與 url 前綴）查 handle ⇒ `LinkListDrop`；
  空值／查無／無 context ⇒ nil（for 迴圈為空、不炸）。
- 規格 `spec/liquid/link_list_setting_spec.rb` LL1–LL3（物件形、空值與查無、無 linklists 語境）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `app/liquid/theme_engine/drops.rb` | `SettingsDrop#coerce` 新增 `link_list` 分支 |
| `spec/liquid/link_list_setting_spec.rb` | 新增 LL1–LL3 |

閘門（本機）：`bundle exec rspec spec/liquid/link_list_setting_spec.rb spec/liquid/page_renderer_spec.rb`、
`bundle exec rubocop app/liquid/theme_engine/drops.rb spec/liquid/link_list_setting_spec.rb`、
`bin/brakeman --no-pager --exit-on-warn --exit-on-error` 退出 0；doc-claims 於提交後跑（見 handoff）。

突變：M23 拿掉 `link_list` 分支（回到 `else val`）⇒ LL1／LL3 轉紅（結果見 handoff §①）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 規格以 `Liquid::Context` 直接餵 `linklists` 全域；PageRenderer 的整頁路徑另有 E17 類規格覆蓋 layout，但「header 透過
  section setting 取選單」的整頁規格未加——demo 店部署後以實機複驗（handoff §①）。
- 官方「`link_list` setting 回 linklist 物件」的逐字未取得；證據＝Ella `header-menu.liquid` 的 `.links` 用法與本尊
  同一 JSON 在真店渲染出選單（2026-09-03 截圖）。
