# 2026-08-31 — G6-0(a)：訂單成立（15-F5 manual 形）

## 已完成的工作 (Done)

- **`Orders::CreateFromCheckout`**（整條結帳線最關鍵的一個 transaction）：
  冪等兩層（Idempotency::Guard claim/replay＋orders.checkout_id 唯一索引 DB 兜底）；
  單一交易、鎖序全專案固定（鎖 checkout FOR UPDATE→open→completed 條件轉移→
  **shop counter +1 讀回〔鎖序首位〕**→逐行條件式扣庫存 available−/committed+
  〔deny=`available>=qty` 進 WHERE、continue=允許負值〕→order＋line_items 快照＋
  manual transaction 列〔kind=sale/status=pending/gateway=method_type/金額＝應收〕→
  timeline event→outbox orders/create→**cart 同交易刪除**）；庫存不足＝整單
  raise→rollback；🔴 manual 不發 orders/paid（付清走 markAsPaid 補發）。
- 四個 AR model 首建：Order（三軸獨立狀態機）／LineItem（快照語義）／
  OrderTransaction（kind 儲存子集＋金額恆正）／Event（timeline）。
- `shops.order_counter`（default 1000 ⇒ 首單 #1001；每店連號防平台總量洩漏）。
- Topics 增 ORDERS_CREATE／ORDERS_PAID（時點分離註記）。
- 前台接線：結帳頁「完成訂單」點亮（前置未齊＝disabled＋原因；齊備＝真 form）；
  `POST /checkouts/:token/complete` 建單→303；`GET .../complete`＝thank-you
  （單號＋摘要＋**payment_instructions**——86 §3 helper② 的落點）；open checkout
  GET complete 導回結帳頁（不洩單）。
- 突變輪 MUT-1～7 全紅（拔 deny 條件／counter 定值／拔 outbox／拔 cart 刪除／
  continue 誤套 deny／manual 誤發 paid／拔付款前置），全復原後 10/10＋全量
  1574/1574 綠。

## 修改的檔案與核心邏輯 (Changes)

- `app/services/orders/create_from_checkout.rb`（核心；MUTATION_NAME 綁 Guard）
- `app/models/{order,line_item,order_transaction,event}.rb`（首建）
- `db/migrate/20260831250000_add_order_counter_to_shops.rb`
- `app/services/events/topics.rb`（ORDERS_CREATE/PAID）
- `app/controllers/storefront/checkouts_controller.rb`（complete/thank_you 動作＋
  complete_button_html）；`config/routes.rb` 兩條 complete 路由
- `spec/requests/storefront_order_creation_spec.rb`（O1–O10）

## 尚未完成或需注意的風險 (Pending / TODO)

- PSP capture/authorize 形 transaction（G6-1/G6-2 接上——金額必經 to_storage 入向
  轉換，15-F5 步 2 🔴）；orders/paid 發送點（markAsPaid／capture 路徑）。
- 確認信 job（transaction 外掛載點已註記；「不對外發信」約束下暫不接）；
  棄單挽回 job 取消（F7 未建，同步登記）。
- admin 訂單列表/詳情（G6-6 API 對位包）；order status 頁簽名 token（F6 後半）。
- buyer_jurisdiction 對映表（v1＝國碼小寫 fallback，ours 登記；法域 pack 擴充接手）。
- 併發 50 執行緒同 checkout 壓測（F5 驗收句）＝隨 G6-6 驗收包補（Guard＋DB 兜底
  雙防線已各有殺手格）。
- 全量套件曾見 staff_authentication 順序性 flake 一次（隔離綠、重跑綠、與本包
  無檔案交集）——CI 若復現另開排查，不隨本包。
