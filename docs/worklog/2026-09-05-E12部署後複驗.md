# 2026-09-05 E12 部署後複驗：公開站商品頁與七頁 768／390 的 computed 結果回寫

分支 `docs/e12-followup`（自 main `83e85398`，E12 #318 合併部署後）。純文檔：`docs/dev/e12-computed-parity.md` §2 追加列。

## 已完成的工作 (Done)

- bt3 部署 83e8539 後以 `scripts/computed-parity.mjs` 公開重量（ref＝hoko.vip live，cand＝mirror.chilling.com.hk，open-details，本機 Chrome 152 headless）：
  商品頁 1280＝13/15（餘 `main`＝countdown 時間性文字＋本尊 `shopify-accelerated-checkout`；`__root__`＝平台注入），與合併前本機數字一致；
  七頁 768／390 各寬：collections/all 14/15、collections/frontpage 14/15、pages/contact 15/16、search 11/12、cart 14/15、404 11/12（餘皆 `__root__`）、
  商品頁 13/15（同 1280 的兩段）。
- 結果寫進 e12 doc §2（列由 scratchpad `e12_followup_rows.py` 從報告檔生成，報告可重跑）。

## 修改的檔案與核心邏輯 (Changes)

- `docs/dev/e12-computed-parity.md` §2：追加公開商品頁 1280 與七頁 768／390 十五列。
- 本 worklog／handoff。

## 尚未完成或需注意的風險 (Pending / TODO)

- 主題編輯器 live preview 的 computed 對表（鐵律 22.1「兩者皆是」）尚未跑；工具可直接量編輯器預覽 iframe 的 URL。
- 91 §3.79 的 V（recommendations 規則、details 關閉態、CDN CSS 管線、Vimeo 縮圖、文章圖片模型）照舊。

## 閘門

（凍結 tree 後填。）
