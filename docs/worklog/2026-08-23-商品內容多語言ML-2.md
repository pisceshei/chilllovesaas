# worklog 2026-08-23 — 商品內容多語言 ML-2（標題／說明／SEO 三組，五語）

## 已完成的工作 (Done)

- GraphQL 契約：`ProductSetInput.translations`（`TranslationInput{locale,field,value}`）、`Product.translations(locales:)`、`Product.translationStatus`、`Query.shopLocales`（＋`ShopLocale`／`PlatformLocale` type）。
- 服務層 `Translations::Upsert`：驗證（語言啟用／非來源語言／欄位射程／長度依來源欄位上限）→ upsert/delete → digest 重算 → 過期標記（minor/major）→ 逐語言進度物化；`Locales::Registry` 作為語言集合唯一查詢入口。
- `Catalog::SaveProduct` 接線：建立與更新兩分支都在**商品的 transaction 內**寫譯文；`TranslationRejected` ⇒ 整棵樹回滾。
- 🔴 修正一個真缺口：譯文缺席時原本整段跳過，導致「改英文標題後其他語言不標過期」——改成過期偵測無條件執行（spec 抓到）。
- 前端 `LocalizedField`（stacked／tabbed 兩佈局、一個資料契約）；商品頁標題改堆疊式、說明與 SEO 兩欄改分頁式；載入既有譯文與 outdated 集合；儲存逐欄位一列送出且略過來源語言。
- 五語 bundle 新增 i18n 欄位相關 key；ML-2 CSS（tab 列、狀態點、原文摺疊）。
- 測試：後端 `spec/requests/product_translations_spec.rb`（複驗：`bundle exec rspec spec/requests/product_translations_spec.rb`）；前端新增五例（複驗：`npx vitest run app/frontend/admin/pages/ProductDetailPage.test.tsx`）。全套 rspec 快照 `367 examples, 0 failures`；vitest 快照 `Tests 31 passed`；rubocop／tsc 無 offense。

## 修改的檔案與核心邏輯 (Changes)

- 新增：`app/services/translations/upsert.rb`、`app/services/locales/registry.rb`、`app/graphql/types/inputs/translation_input.rb`、`app/graphql/types/translation_type.rb`、`app/graphql/types/translation_status_type.rb`、`app/graphql/types/shop_locale_type.rb`、`app/graphql/types/platform_locale_type.rb`、`app/frontend/admin/components/LocalizedField.tsx`、`spec/requests/product_translations_spec.rb`、`docs/dev/m1-product-translations.md`
- 修改：`app/services/catalog/save_product.rb`、`app/graphql/types/inputs/product_set_input.rb`、`app/graphql/types/product_type.rb`、`app/graphql/types/query_type.rb`、`app/graphql/types/errors/product_set_user_error_code.rb`、`app/frontend/admin/pages/ProductDetailPage.tsx`、`app/frontend/admin/pages/ProductDetailPage.test.tsx`、`app/frontend/admin/i18n/messages/*.json`（五語）、`config/locales/*.yml`（五語，新增 `errors.translation.*`）、`app/assets/stylesheets/admin.css`

## 尚未完成或需注意的風險 (Pending / TODO)

- v1 射程只含四個欄位；選項名／值、變體 title、媒體 alt 屬後續包（67 §B.2 有清單）。
- 說明仍是 textarea（RTE 未落地）；`renderTabbed` 已預留給 RTE 實例，切 tab 換值的形態不會變。
- 商品列表「翻譯」欄與設定頁語言總覽在 ML-4；本包只有編輯頁 tab 上的狀態點。
- 過期集合只在載入時取得；同頁改來源文字不會即時標記（需重載）——刻意不在前端猜 digest。
- Collection 翻譯在 ML-3（同元件、同服務，只差 resource_type）。
