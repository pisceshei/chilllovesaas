# worklog 2026-08-24 — 庫存剩餘裁定（排程第 15 包）

## 已完成的工作 (Done)

- **D41**：冪等 failed 語義——11 §2.1(b)（同 key 重試）勝出，
  `IDEMPOTENCY_PREVIOUS_ATTEMPT_FAILED` 我方不發、不進庫存錯誤 enum。
  證據鏈：11:81 逐字「failed＝視為未執行，允許以同一把 key 重試」；本尊 adjust/set/move 各 mutation 的
  enum 描述逐字「Retry with a **new** idempotency key」（95 §4，三支同文）；
  91 §3.7 明文「庫存線落地前必須先解此矛盾」；productSet 已依 11 落地＝既成事實。
- **D42**：庫存權限鍵＝獨立的 `inventory.view`／`inventory.edit`（12 §2 清單已補），
  本尊佐證＝scope 層即分 read_inventory/write_inventory。`totalInventory` 沿用 products.view
  （本尊同樣以 read_products 讀該欄）。
- **D43**：批量編輯器與庫存 CSV **明文延後**；第 17 包禁直寫 cop **不留豁免口**；
  「批量也寫 ledger」屬對本尊的行為超集，**落地前需使用者批准入 71 §A**（本條不預支）。
- 第 15 包原第 2 條（reason 7 vs 17）已由 R95 §3 解掉（兩個投影都要），不再立 D。

## 修改的檔案與核心邏輯 (Changes)

- `docs/DECISIONS.md`：D41–D43。
- `docs/specs/12-spec-tenancy-auth-permissions.md`：§2 key 清單補兩鍵＋D42 引註。

## 尚未完成或需注意的風險 (Pending / TODO)

- D41 的差異是機器可見的（error enum 少一個值）——歸入「enum 刻意重造」的既有範圍，
  未另立 §A 條目；若日後裁定 enum 也要逐值對齊，本條要重開。
- D43 的「批量也寫 ledger」超集**尚未獲 §A 批准**——bulk editor 落地輪要先呈使用者。
