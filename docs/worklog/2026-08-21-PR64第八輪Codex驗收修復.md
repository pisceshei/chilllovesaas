# PR #64 第八輪 Codex 驗收修復

## 已完成的工作 (Done)

- 驗收輸入固定為 exact head `8902f8a0c0e45329915ac279eaf2a981555a56a2`。CI quality／test
  completed success；Codex review `4987003396` 綁同一 commit，兩則 current-head inline 為
  `3825052909`、`3825052915`。Claude run `32413004039` 仍停在「Claude 驗收」step，沒有判詞，
  因此本輪沒有把 pending 或缺席結果當作通過／不通過。
- 修法前用 `git log -p -- docs/dev/external-facts.md` 追到 A9／A10 自第二輪建立、第三與第六輪
  收窄的沿革。failure-first 檢視舊 head 證明 A9 只有 `pageInfo` 逐字，完整取頁與
  `$endCursor` 要求是轉述；A10 只有 250 上限逐字，超限 fallback 也是轉述。
- A9 已補 GitHub CLI 官方手冊的三段最小逐字，分別覆蓋完整取頁、原查詢接受
  `$endCursor: String` 與 `pageInfo` 欄位；URL 與取證日期保留在相鄰位置。
- A10 已改引 GitHub REST 官方同段的完整 fallback 原文，逐字包含「more than 250 commits」
  與「use the List commits endpoint」，不再由另一句上限文字外推處置方向。
- 外部證據於 2026-08-21 重新取自官方頁：<https://cli.github.com/manual/gh_api>、
  <https://docs.github.com/en/rest/pulls/pulls#list-commits-on-a-pull-request>。本輪只修兩條被點名的
  缺句，不改 A9 mixed-token 邊界或 A10 squash 後倉庫快照。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | PR #64 前輪已多次補外部證據，但 exact head `8902f8a…` 的 Codex `3825052909`／`3825052915` 仍指出同一段落以逐字支持一部分、以轉述發布另一部分 |
| 既有防線失效原因 | 送驗前只確認每節有 URL、日期與至少一段引文，沒有把節內每一個可操作外部結論逐句映射到原文；「同段另要求」掩蓋了未引用的承重句 |
| 被漏掉的固定步驟 | 鐵律 16 與 19 要求發布前逐主張查證；固定步驟應是把每個承重外部動詞列出，再逐項標成直接逐字、明示推論或未取得，而非以整節有引文代替 |
| 可重跑反向複驗 | 直接核對 A9 引文同時含 `sequentially be requested`、`accepts an $endCursor: String variable`、`pageInfo`；A10 引文同時含 `more than 250 commits` 與 `use the List commits endpoint`，任一缺少即不得發布該句 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 證據／當前 head／驗收攝取 | 三端點全量拉取並綁 exact review commit；Claude pending 單獨標示 | review `4987003396` 與兩則 inline 均綁 `8902f8a…`；沒有捏造 Claude 判詞 |
| ② 生產者／消費者／終態／歷史同步 | 只同步 external-facts 終態、本 worklog、91 A.1 與 PR 描述 | A9／A10 原有邊界不改；新 worklog 與 A.1 同 commit |
| ③ 易腐數字／完成性聲明 | review IDs 與 head 作 immutable snapshot；不發布「全節已完整」 | 只宣稱兩條被點名的承重句已有逐字，不外推其他外部契約 |
| ④ 狀態空間／競態窗口 | 分開 CI success、Codex 有意見、Claude pending 與新 head 待驗 | 舊 Claude job 不被當作新 head 結果；push 後三方全部重新判定 |
| ⑤ fail-open／happy path／自我證明 | 正向檢查五個承重短語存在；反向標準是任一短語缺少即不得發布 | 修前 A9 缺兩個短語、A10 缺 fallback 短語；修後定向檢查須全命中 |
| ⑥ workflow 本機假綠／字面塊 | 不適用：本輪不修改 workflow；仍須檢查新 Claude run 是否 validation skip | `git diff --name-only 8902f8a..HEAD -- .github/workflows` 提交後須為空 |
| ⑦ Markdown／Windows 工具鏈 | blockquote 分段不放表格裸直線；GitHub `/markdown` API 驗 external-facts 片段 | 須得到對應 blockquote HTML，且 `git diff --check` 通過 |

## 修改的檔案與核心邏輯 (Changes)

- `docs/dev/external-facts.md`：A9 補完整取頁、GraphQL 游標變數與 `pageInfo` 三個逐字錨；A10
  把僅有上限的引文換成官方完整 fallback 句。
- `docs/worklog/2026-08-21-PR64第八輪Codex驗收修復.md`：保存 exact-head 輸入、查證、20.4
  復發原因、20.3 射程及待驗條件。
- `docs/specs/91-pit-register.md`：只同步附錄 A.1 的新 tracked worklog，沒有順手修改未被點名項。

## 尚未完成或需注意的風險 (Pending / TODO)

1. repo 最後一個 edit 後仍須跑定向短語檢查、GitHub Markdown render、doc-claims 與完整 Windows
   production-equivalent 29 閘門；任何一項紅燈都不得 commit。
2. commit 後仍須重跑 doc-claims、鐵律 15、push 並重取 CI、Codex、Claude 與 threads；
   `8902f8a…` 的 CI 或 review 結果不能外推到新 head。
3. #64 不命中 18.3；只有新 exact head 的 CI、Codex、Claude 雙零、threads resolved、mergeable
   與 evaluator `1111` 同時成立，才可依 D31／D32 代行 squash merge。
