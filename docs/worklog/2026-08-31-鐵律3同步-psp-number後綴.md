# 2026-08-31 — 鐵律 3 摘要句同步＋C2 後綴白名單增 `_psp_number`（18.3 人工合併包）

## 已完成的工作 (Done)

- **CLAUDE.md 鐵律 3** 同步 #217 已合併的 65 號現值：`amount_format` 三值（＋PayPal＝
  decimal_string／Airwallex＝decimal_number 更正，2026-08-31 有日期更正註巢於原
  V-188 註之後）；型別清單增 `Money::PspNumber`、後綴增 `*_psp_number`；fixture pack
  「兩種」→「三種」；per-currency 位數逐家宣告句（HUF 兩家皆覆蓋 ISO）。
- **scripts/check-money-boundary.rb C2**：後綴白名單增 `_psp_number`（kwarg 名＋值側
  regex＋訊息＋summary）；summary 的「兩種 amount_format」硬編數字改讀 limits 清單長度。
- **反向釘死（20.2⑤）**：c2_bad_kwarg fixture 增一行合法 `amount_psp_number:` 呼叫；
  test-money-rules 增第四欄 forbidden 斷言（輸出點名合法後綴＝紅）——實測：白名單
  縮回兩值 ⇒ 「合法寫法被誤擋」轉紅；復原 ⇒ 11 項全過。

## 修改的檔案與核心邏輯 (Changes)

- `CLAUDE.md`（鐵律 3 的四個 bullet）
- `scripts/check-money-boundary.rb`（C2 三值＋summary）
- `scripts/test-money-rules.rb`（CASES 第四欄 forbidden＋迴圈判斷）
- `spec/fixtures/ci_violations/c2_bad_kwarg/app/services/caller.rb`（合法對照行）

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 本 PR 改規範本文＋scripts/ ⇒ **18.3 人工合併**（D46 判例）——CI 綠後**等使用者
  合併，不自合**。G6-1 的 Airwallex adapter（`_psp_number` kwarg）依賴本 PR 先進 main。
- C2 既有誠實聲明不變：動態 send／`**payload` 看不到（checker 檔頭第 3 點）。
