# PR #65 第四輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收基準固定為 head `98189f70d5b36ff1f00d9b08ec867c6a5b8695ea`。鐵律 15.2 三端點
  全量重取後，Claude issue comment `5360428264` 點名未關閉 HTML comment 的 fail-open；Codex
  review `4986322814` 的 inline 為 `3824470982`／`3824470986`／`3824470988`／`3824470994`。
  Claude Review run `32405113633` completed success，CI quality／test 同 head completed success。
- HTML comment 修復先新增負向 fixture；舊 checker 的整份 suite 精確失敗 `1/23`，唯一失敗為
  `doc_claim_unclosed_comment`「期望 exit 1、實得 0」。helper 改為保存 comment 起始行並把未收尾
  狀態回傳呼叫端後，`ruby scripts/test-doc-claims-rules.rb` 實跑為 23 組 fixture、3 組 git 情境、
  1 組生產供給情境全綠。
- CommonMark 0.31.2 §4.5 官方逐字為 "Unclosed code blocks are closed by the end of the document"，
  URL <https://spec.commonmark.org/0.31.2/#fenced-code-blocks>（取證 2026-08-21）；B9 已與 §5.2
  清單縮排證據分列，R6 的拒絕仍明示為本專案更嚴格政策。
- CLAIM-006 failure-first 在記憶體把來源發布儲存格改為 `999`，舊命令仍輸出 derived 180 並
  通過；新命令讀取發布值，正常 HEAD 輸出 row_occurrences 180，該突變則以
  `EXPECTED_REJECTION [162, 7, 11, 999]` 非零拒絕。
- P-2 主 worklog 的 Changes 終態表補齊縮排、活性 Markdown、重複 ID、圍欄／comment 收尾、
  現行 fixture 與後續 worklog；worklog 集合不逐輪手抄，改以 `origin/main...HEAD` 重取。
- 第三輪 worklog 的 77／76／混合 ID 按歷史層保留原文，緊鄰補上綁 head `98189f7` 的 dated
  output snapshot 與 CLAIM-003 單一命令入口。Claude／Codex 的範圍外觀察按根因登 91 §3，
  已經存在的 71 圖例條目不重複建立。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 第三輪以 `doc_claim_unclosed_fence` 修圍欄 EOF fail-open；第四輪 Claude comment `5360428264` 又在同一 helper 點名 HTML comment EOF 靜默截斷 |
| 既有防線失效原因 | 修法只把 `fence` 狀態與起始行回傳，沒有枚舉 helper 內所有跨行遮罩狀態；`comment` 仍停在函式內部布林值，呼叫端看不見 EOF 未收尾 |
| 被漏掉的固定步驟 | 鐵律 20.2⑤要求每個新邊界同時做 fail-open 與 happy path；當時只對圍欄做 EOF 負向 fixture，沒有對同函式的 HTML comment 做對稱 EOF 突變 |
| 可重跑反向複驗 | `ruby scripts/test-doc-claims-rules.rb` 必須包含 `doc_claim_unclosed_fence` 與 `doc_claim_unclosed_comment` 各自 exit 1，且 `doc_claim_inactive_headers` 仍 exit 0 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取 | current head 的 issue comments、review body、inline 全部 `--paginate` 重取 | 上列 Claude／Codex ID 均綁 `98189f7…`；Claude run completed success |
| ② 生產者與消費者同步 | 反查 checker 註釋、R6 規則表、fixture 表、突變清單、主 worklog Changes、91 A.1 與 external-facts | HTML comment 契約與 fixture 四端同步；主 Changes 改成現行行為與動態 worklog 集合 |
| ③ 易腐數字／完成聲明 | 歷史值綁 exact head snapshot；HEAD 宣稱同時斷言來源分量與發布儲存格 | CLAIM-003 snapshot 綁 `98189f7`；CLAIM-006 對 999 突變非零 |
| ④ 零值／空值 | comment 起始行必保存；未收尾狀態不能以空 active 行冒充無違規 | 新 fixture 在第一個合法 CLAIM 後開 comment，證明 headers 非空時仍會被 EOF violation 擋住 |
| ⑤ fail-open／happy path | 未收尾 fence／comment 各有負向；合法 inactive fence／comment 共用反向案例 | suite 實跑兩個未收尾 case exit 1、`doc_claim_inactive_headers` exit 0 |
| ⑥ 超大值／資源上限 | 不適用：本輪沒有數值解析上限或輪詢資源變更 | `git diff --name-only 98189f7..HEAD` 只允許被點名文件、R6 checker／fixture 與 91／worklog |
| ⑦ Markdown／歷史層 | 外部語義用官方逐字；歷史 Done 原文只加相鄰更正；表格直線結構機械複驗 | CommonMark §4.5 URL／逐字／日期齊；`git diff --check`、doc-claims warning 0 與表格每列等量直線須全過 |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：未收尾 HTML comment 與 Markdown fence 對稱 fail-closed，錯誤定位
  到各自起始行；R1–R5 與 R6 其餘射程不變。
- `scripts/test-doc-claims-rules.rb`、`spec/fixtures/ci_violations/doc_claim_unclosed_comment/`：新增
  failure-first 負向案例並提高 suite 下限。
- `docs/dev/m0-review-convergence.md`、`docs/dev/external-facts.md`：同步 R6 終態契約、fixture／突變
  射程與 CommonMark §4.5 官方逐字證據。
- `docs/specs/92-claim-index.md`：CLAIM-006 同時斷言 162／7／11 分量、發布列次值與加總相等。
- `docs/worklog/2026-08-21-P2宣稱索引與R6.md`：Changes 終態表更新為 current HEAD 行為，驗收
  worklog 以 PR 三點 diff 動態重取。
- `docs/worklog/2026-08-21-PR65第三輪雙驗收修復.md`：保留歷史原文並追加 exact-head dated
  output snapshot。
- `docs/specs/91-pit-register.md`：原 HTML comment 候選改為結案候選；登記未點名同型問題並
  同步 A.1。
- `docs/worklog/2026-08-21-PR65第四輪雙驗收修復.md`：保存本輪輸入、官方取證、failure-first、
  20.4／20.3、修改射程與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本段定稿後須先重跑六條 CLAIM 命令與 23 組 fixture suite，再從 setup 跑完整 Windows
   production-equivalent 閘門；任一非零即停止。
2. #64 與本分支都修改 91；若 #64 先合併，須先把最新 main 整合進本分支、保留兩批來源邊界，
   對整合後任何新 head 重跑全部閘門與雙驗收，禁止沿用合併前結果。
3. 本 PR 修改 `scripts/`，命中鐵律 18.3；即使新 exact-head 雙零與評估器成立，也只能通知
   使用者人工合併，不得代行。
