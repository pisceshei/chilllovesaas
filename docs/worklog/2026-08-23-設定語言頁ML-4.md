# worklog 2026-08-23 — 設定 › 語言 ML-4（新增／停用／發布，不建表）

## 已完成的工作 (Done)

- 平台語言字典擴充：`PlatformLocale::CATALOG_SEED`（含 `ar`／`he` 兩個 RTL）＋ migration `20260823110000`（冪等，走 `PlatformLocale.seed!` 同一正典）；`LAUNCH_SEED` 改為由字典前五筆派生。
- API：`shopLocaleEnable`／`shopLocaleUpdate`／`shopLocaleDisable` 三支 mutation＋`availableLocales` query＋`shopLocales(includeDisabled:)` 參數＋`ShopLocale.enabled` 讀取面欄位；錯誤碼 `NOT_FOUND`／`ALREADY_EXISTS`／`INVALID`／`LOCALE_LIMIT_EXCEEDED`／`SOURCE_LOCALE_IMMUTABLE`（五語訊息）。
- UI：`/admin/settings/languages`（已啟用／新增／已停用三區塊）＋`/admin/settings` 設定索引（取代 placeholder）；28 個 i18n key × 五語。
- 測試：`spec/requests/shop_locale_settings_spec.rb`（複驗：`bundle exec rspec spec/requests/shop_locale_settings_spec.rb`）與 `app/frontend/admin/pages/SettingsLanguagesPage.test.tsx`（複驗：`npx vitest run app/frontend/admin/pages/SettingsLanguagesPage.test.tsx`）。全套 rspec 快照 `378 examples, 0 failures`；vitest 快照 `Tests 36 passed`；rubocop／tsc 無 offense。
- `docs/dev/m1-locale-settings.md`：§1 正面回答「為什麼不需要每語言一張表」、§5 說明這個模型如何支撐翻譯 CSV 匯入匯出。

## 修改的檔案與核心邏輯 (Changes)

- 新增：`db/migrate/20260823110000_expand_platform_locale_catalog.rb`、`app/graphql/mutations/shop_locale_enable.rb`／`shop_locale_update.rb`／`shop_locale_disable.rb`、`app/graphql/types/errors/shop_locale_user_error_code.rb`／`shop_locale_user_error_type.rb`、`app/frontend/admin/pages/SettingsLanguagesPage.tsx`／`SettingsPage.tsx`／`SettingsLanguagesPage.test.tsx`、`spec/requests/shop_locale_settings_spec.rb`、`docs/dev/m1-locale-settings.md`
- 修改：`app/models/platform_locale.rb`（CATALOG_SEED／seed!）、`app/graphql/types/query_type.rb`、`app/graphql/types/mutation_type.rb`、`app/graphql/types/shop_locale_type.rb`、`app/frontend/admin/App.tsx`、`app/frontend/admin/i18n/messages/*.json`（五語）、`config/locales/*.yml`（五語，新增 `errors.locale.*`）、`app/assets/stylesheets/admin.css`、`spec/models/shop_locale_spec.rb`（字典總數斷言改為「首發五語在字典中」——總數會隨候選語言增加而漂）

## 尚未完成或需注意的風險 (Pending / TODO)

- 排序（`position`）只有 API，UI 拖曳未做。
- 翻譯 CSV 匯入匯出（ML-5b）＝使用者點名的下一個目標；契約已寫在 dev doc §5，實作時照 67 §E.6（空白＝不動作、清空走 `__CLEAR__`、覆寫走顯式旗標）。
- 來源語言變更精靈未開放；per-market 語言白名單屬 M2（與本頁是兩個層級）。
- 字典 collation 值只寫 MySQL 8.4 實際存在的名稱；日後加語言時要照查，不得臆造。
