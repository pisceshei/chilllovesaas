# 工作記錄：業務邏輯總綱合成（跨模組正典，2026-08-15）

> 交付：`docs/research/90-shopify-business-logic-blueprint.md`（2292 行，§0–§11）
> <!-- 🔴 2026-08-17 更正（PR #52 第 4 輪）：行數為考掘當日快照；驗收輪更正註記已使 HEAD 增行，
>      現值以 `wc -l docs/research/90-shopify-business-logic-blueprint.md` 為準。 -->
> 前置：`docs/worklog/2026-08-15-業務邏輯十五章考掘.md`

## 已完成的工作 (Done)

15 章各自完整，但**跨章的東西沒有家**：訂單成立那一刻到底有幾件事要在同一個
transaction 裡發生？哪些狀態機互相聯動？98 條裁定裡哪些是全域生效的？
先做哪一章？本檔就是這一層。

### §2 端到端主幹（本文件最重要的一張表）

從加入購物車（S0）到分析歸檔（S17）逐階段一列，欄位＝階段／操作者／系統動作／
**狀態變化**／**庫存量化態變化**／**金流變化**／**發出的事件**／**失敗分支**。
再加三個原子點的契約（T1 訂單成立／T2 履行／T3 退款），各自逐條列出
「同一 transaction 內必須一起發生」與「必須排到 transaction 外」。

### 🔴 §2.4 跨章矛盾登記——14 條，這是合成階段最大的產出

分章各自寫得都對，合起來才看得出矛盾。最要命的一條：

**M1 庫存 commit 的觸發時點**。03 章寫「付款成功才 commit」，02 章寫「訂單成立即 commit」。
採 02 章，理由是 **COD／bank deposit／B2B payment terms／`orderCreate` 的 PENDING 單
全都是「訂單已成立、付款未成功」**——照 03 章實作，這四類訂單完全不占庫存。
而 checkout 路徑下兩者同刻，所以**用 checkout 測永遠測不出來**。
連帶 M2 補一條：程式碼裡 commit 的呼叫點必須掛在「訂單成立」事件上，
不得掛在「付款 SUCCESS」回呼上。

另外幾條：M7 退款上限是**軟**上限（over-refund 是官方合法流程，做成 DB CHECK 會擋掉它）；
M8 `refunds/create` 在**金流確認前**就發（綁在 PSP 成功上，退款失敗的訂單會完全沒有 outbox 紀錄）；
M12 `CANCELED`（單 L，Return）與 `CANCELLED`（雙 L，FulfillmentOrder）**兩種拼字都照抄不統一**，
且要加 CI 拼字快照測試防有人「順手改成一致」。

### §3–§9 其餘六節

- **§3** 103 個狀態機總表＋正交軸清單（訂單四軸、FO 三軸）＋跨實體聯動。
- **§4** 171 條不變量＋**40 條併發要害**（每條給要害情境／保護機制／必備併發測試）＋冪等鍵覆蓋表。
  <!-- 🔴 2026-08-17 更正（PR #52 首輪）：「171 條不變量」計法有誤——§4.1 實數 131 列，
       171＝131＋40（把併發要害計入）。原文不動，正確拆分見 handoff §①。 -->
- **§5** 111 條事件總表＋mermaid 耦合方向圖＋「必須同交易 vs 必須非同步」的邊分類＋環路打破方式。
- **§6** 98 條裁定去重登記，前面歸納出**五類系統性差異**（金額單位／多租戶／錯誤與冪等加嚴／法域 pack 化／生態系不做）。
- **§7** 112 條未決問題，依「擋住什麼」分三個 Tier，點名**開工前必須先裁定的 9 條**。
- **§8** 52 個領域的 `config/limits.yml` 增補（已用 Ruby YAML 驗證可解析）＋衝突值仲裁摘要。
- **§9** 依賴拓撲＋M0–M6 里程碑對應＋逐里程碑併發／金額測試門檻＋**前 8 個「做錯要重寫」的決策點**。

### 🔴 §9.1 的意外發現：原始硬依賴圖是單一強連通分量

把 15 章宣告的 `hard` 依賴當有向邊做 SCC 分解，**15 章全落在同一個 SCC**——
沒有任何一章是拓撲起點。這不是資料錯，是電商領域本來就互相咬合。
解法是先宣告斷環規則：把硬依賴分成 **S 型（schema，對方的表或欄位必須先存在）**
與 **F 型（flow，流程觸發但表可獨立）**，只用 S 型分層。套用後 S 型子圖才是 DAG。

⇒ 地基三單元：**§15 平台核心**（13 章依賴它，全是 migration 級）、
**§13 事件**（與 15 互鎖必須同批）、**§01 商品＋§02 庫存**（唯一真正的 schema 級互鎖對，
`InventoryItem!` non-null 1:1，任何「先做商品之後補庫存」的排法都會產生一次無法追認的資料回填）。

## 修改的檔案與核心邏輯 (Changes)

- `docs/research/90-shopify-business-logic-blueprint.md`（新增；行數不寫死，`wc -l` 為準——驗收輪多次帶更正註記增行）：由七個片段組裝
  （前言與系統模型自寫；主幹時序／狀態機與不變量／事件耦合／裁定與未決／實作排序四節由合成代理產出；分章索引與邊界聲明自寫）。
- `CLAUDE.md` 文件地圖：新增 `90` 與 `90-blueprint` 條目，並寫明**與 `22`／`7x` 的職責分界**
  （UI 以 22／7x 為準、業務規則以 90 為準、我方裁定以 specs 與鐵律為準）。
- `HANDOFF.md` 檔案地圖：同步新增兩行。
- 🔴 2026-08-17 補記（PR #52 第 11 輪，Codex c63；第 13 輪改可計算式；第 18 輪補合併後查法）：
  驗收後修正各輪的異動檔案集合**以本 PR diff 名單為準**（複驗：合併前
  `git fetch origin main && git diff origin/main...HEAD --name-only`；**合併後**＝該次
  squash/merge commit 的 `git show --name-only`，或 PR #52 的 Files changed 存檔頁——
  main 前進到含本 PR 後，`main...HEAD` 三點式退回 merge-base、回傳空集，不可再用），
  逐輪明細見下方各「驗收後修正」節；本表初稿只列合成當刻的三檔。第 11 輪的枚舉式補記
  在第 12 輪把 specs/16/18/86 帶進 diff 時即過期——枚舉會腐化的又一實證，故改查法。

### 三處在組裝時修掉的實質缺陷

1. **§9 的 65 條未決問題引用全部對不上 §7**（§9 用 `Q-{章}-{序}`、§7 用扁平 `Q-NN`）。
   逐條語義比對後對映 56 處；2 處 §7 根本沒有 ⇒ 一條補成 **Q-112**（`ON_SALE` ＋ SCHEDULED FO
   的 commit 時點）、一條確認已被 §10 章號索引解決。
2. **§8 的 YAML 解析失敗**（3 行用分號串 inline mapping）。宣稱「可直接貼進 `config/limits.yml`」
   而實際貼不進去，比沒有這節更糟。改成 flow mapping 後 Ruby `YAML.safe_load` 通過，52 個頂層領域。
3. **27 處 `§65`／`§76`／`§77`／`§56` 是倉庫文檔編號，卻長得像章號**（本文件的 `§NN` 慣例指 01–15 章）。
   全部改寫為 `docs/specs/65`／`docs/research/76` 這種明確路徑。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **§7 的 112 條未決問題一條都還沒解**。其中「開工前必須先裁定的 9 條」需要**使用者裁定**
  （分析日界時區、webhook 訂閱 owner、訂閱 schema 是否 M0 預留、cart 過期天數、
  Live View 金額公式、方案分層是否複刻、前台 cart 是否驗庫存）或**實測本尊**
  （庫存 transfer 出帳時點、退款稅額分攤規則）。**未裁定不得動工**是本文件明寫的紀律。
- 🔴 **發現一個既有缺陷未修**：`config/limits.yml` 有兩個相似頂層鍵 `notification:`（45 範本／12 分組）
  與 `notifications:`（47 範本／11 分組），記同一件事、值不同、**都自稱實測**。
  程式碼引用到哪個鍵就拿到哪個數字，沒有任何測試會紅——鐵律 6「一律引用 limits.yml」擋不住這形態。
  已登記 §7 Q-111 與 §8.1，**本輪不自行仲裁**（要回測試店重數），已開背景任務。
- ⚠️ **§5.2 的 Tier 與 §9.1.3 的 L 層是兩套分層**（前者含軟依賴看耦合密度、後者只看 schema 硬依賴看 migration 順序），
  對 §11 markets 與 §12 online-store 的落點不同。已在 §5.2 加對照說明並指明**排序以 §9.1.3 為準**，
  但兩套分層並存本身仍是讀者的絆腳石，下輪可考慮合併。
- ⚠️ **§9.1 的 S/F 分型是本輪對 15 章 `dependencies` 宣告的推導結果**，不是官方陳述。
  若某章的 `hard` 標記本身標錯，分層會跟著錯。每條 S 型判定已註明依據的原文語句可逐條複核。
- ⚠️ **§6 的 98 條裁定沒有一條經過使用者確認**。它們是「各章 F 節既有裁定的彙總去重」，
  其中標「待裁定」的必須進 §7 流程，**不得因為寫進總表就當成已定案**。
- **本輪沒有動任何代碼、沒有跑 rspec**（純文檔）。`scripts/lint-prototype.py` 不適用（未改原型）。

## 驗收後修正（PR #52 第 1 輪，2026-08-17）

### Done

- 🔴-1 總綱 §6.1 D-03「HTTP 恆 200」＝鐵律 4 已廢措辭 ⇒ 我方裁定欄改為**三層**
  （①業務 200 userErrors ②限流 200 ③認證/租戶停用/payload 非 200），帶更正註記。
- 🔴-2 選修法①：`docs/specs/15` F5 兩處「付款成功才扣」措辭**直接回寫**為「訂單成立」
  （標題＋cart 坑清單，各帶更正註記；03 章併發測試行同源措辭一併修）；
  另在 CLAUDE.md 文件地圖裁決句補「90 §2.4 已登記矛盾以 90 為準，直到規格回寫」
  ——判詞指出沒有這句時，該裁決句會把 15-F5 裁回錯的一側。
- 🔴-3 終態層計數回寫：HANDOFF.md 110→112；handoff §①「171 條不變量」改
  「131 條不變量＋40 條併發要害」（附複驗指令）；本檔上方原句加歷史層更正註記。
- 🟡-1 清法①：兩處 D- 錯引修正（D-31→D-21；D-7→§3 D7），§0 補**命名空間規則**
  （三套形近編號的跨節引用必須帶節號）。
- ⚪ 四條處置：HANDOFF.md 487 條 URL 行改快照標記＋:126 過時「尚未有代碼」行更新
  （順手清）；103 個狀態機／111 條事件計法未固定——登記於 PR 描述，待坑登記簿
  （PR-E1）建立後轉入。

### Pending

- 103／111 兩個快照數字的固定計法未補（⚪ 級，不擋）；規格側 15-F5 依 D-32 回寫後，
  90 §2.4 對應矛盾條目的狀態更新屬總綱下輪維護，本輪不動。

## 驗收後修正（PR #52 第 2 輪＝Codex 首輪，2026-08-17）

### Done（Codex 五條：一條已由第 1 輪修掉，四條本輪收）

- 🔴 **T3 退款條件式 UPDATE 堵死合法超退**：檢查式補 `+ approved_over_refund_cents`
  授權分支（預設 0；`allowOverRefunding`＋權限＋二次確認才寫入）——原式與自家
  §4 R-2／§2.4 M7 矛盾。
- 🔴 **S3 付款先於持久化**：改為 PSP 呼叫前先落 checkout 級 payment attempt＋outbox
  intent，OrderTransaction 於 T1 建立回連——原寫法在 S4 rollback 時留下無紀錄的
  已授權款項，且與 T1 自述矛盾。
- 🔴 **M-11 稅基漏折扣攤分**：`unit_price_cents` → `taxable_unit_cents`（M-9 折扣後
  稅基的單件基數＋不整除時最大餘數法）——原式對折扣品多收稅。
- 「487 條唯一 URL」計法錯誤：考掘篇歷史層加更正註記（487＝出現次數、去重約 459，
  附複驗指令）。

### Pending

- 無新增。上輪 Pending 不變。

## 驗收後修正（PR #52 第 3 輪＝Codex 第 2 輪，2026-08-17）

### Done（Codex 五條全收）

- 🔴 A-3 AOV 反向斷言收窄：fixture 必須含 post-order adjustments（無調整日兩式數值
  相等，無條件不等式會把合法 rollup 打紅）。
- 🔴 §9 M0「前置未決」剔除 Q-93/Q-94（§7.3 已裁 Tier C 不阻擋——原列法讓排程器
  無法判定 M0 可否動工），改列「延後項」並註明各自的驗證時點。
- 🔴 S5 結清跨付清門檻：同 transaction 重算 `displayFinancialStatus → PAID`
  ＋發 `orders/paid`/`orders/updated`（原寫「Order 不變」會讓履約/通知/分析
  永遠不知道訂單已付清）。
- 🔴 02 章 transfer reject 必須沖回 destination `incoming −q`（S10 字面「不改任何
  地點數量」僅對 on_hand/available 成立；照字面實作與自家 F.3-1 例證表矛盾，
  拒收量會永久滯留在途）——轉移表／例證註／誠實聲明三處對齊。
- 🔴 02 章 B.1「官方 8 個互斥狀態」語義錯誤：on_hand 是聚合投影（六 bucket 之和）、
  互斥只適用六個組成 bucket；照原句建模會做成單一 enum 或重複計數。

### Pending

- 無新增。

## 驗收後修正（PR #52 第 4 輪＝bot 第 3 輪＋Codex 第 3 輪，2026-08-17）

### Done（bot 🔴1＋🟡4、Codex 新十條，計 15 項全收）

- 🔴 **10 章稅式同步 M-11**（同語義另一副本漏改——正是首輪 🔴-2 同型）；
- 副本同步四件：S5 事件欄補 `orders/paid`/`orders/updated`；02 章 C.9-3 與 cancel 分支
  補 incoming 沖回（accept/reject/cancel 三動作對齊例證表）；14 章 AOV 斷言同 A-3；
  §9.2 M4 門檻行補授權分支；
- 我方修正自身的缺陷三件：T3 條件式 Σ 寫法雙重扣除（改 new ≤ captured−refunded+approved）；
  「見 D 節」錯引（→B.3）；A-3 前提補強（reversal 可被加項抵銷的巧合）；
- 新內容缺陷五件：A4 草稿轉正未保留分支（條件式 available→committed）；B3 編輯加品
  跨單併發超賣（走 T1 同一條件式服務＋跨單併發測試）；restock 冪等鍵兩路
  （收貨時 disposition line／退款時 refund_line_item，防雙回補）；displayStatus 三軸
  合成（status 覆蓋陳舊事件）；15-F1 行合併鍵（(cart_id,variant_id) 唯一索引會
  靜默合併客製屬性行）；
- 終態層回寫：本檔 Changes 行數不寫死＋handoff §① 補四輪更正清單（c14）。

### Pending

- 無新增。

## 驗收後修正（PR #52 第 5 輪＝bot 第 4 輪＋Codex 第 4 輪，2026-08-17）

### Done（bot 🔴3＋🟡1、Codex 十條，計 14 項全收）

- 依據鏈回寫三件（bot 🔴，「修正所引的來源自身還是舊形」）：06 章 restock 單路鍵→兩路；
  09 章 B.4 latest-event→三軸合成；總綱 T1 表 draft 列→保留/未保留分支（M5 裁定欄
  同步加「曾保留者」限定）。
- specs/15 素描同步（bot 🟡）：cart_line_items 四承重欄，價格非「僅供顯示」。
- Codex 新內容九件：02 章 DRAFT 直轉出帳分支（reserved 下溢）；metafield 唯一索引補
  shop_id 前綴（鐵律 2）；QST 997.5 非整數→全表升 ppm；15-F5 非 PSP 變體（manual/
  PENDING 不發 orders/paid）；12 章 comment「全 5 態可達」測試不可能→4 態可達＋入向
  斷言；14 章/A-7 COGS 快照時點提前到 T1（展開時讀現值會污染歷史毛利）；08 章 B2B
  價格解析先 specificity 後 min（全域 min 繞過議價合約）；13 章 webhook 先落庫再 ack
  （事件遺失窗口）；fulfillmentCancel 原子回補 committed/on_hand（總綱＋09 章）。

### Pending

- fulfillmentCancel「貨已實體寄出但記錄取消」的對帳形態官方未明文，列 V 實測。

## 驗收後修正（PR #52 第 6 輪＝Codex 第 5 輪六條，2026-08-17）

### Done

- 稅率尺度雙軌契約：M-11／D-74 改「鍵後綴宣告制」（`*_bp` /10_000、`*_ppm` /1_000_000，
  禁止混讀）——上輪只改表不改契約，通用實作會把 GST 50000 讀成 500%。
- displayStatus 優先序改**逐狀態**：異常/終態才覆蓋事件；普通 SUCCESS 不搶先
  （fulfillment 建立即 SUCCESS，status 一律優先會讓事件分支永不可達）——總綱 S9＋
  09 章 B.4 同步。
- 14 章 C.13 規範句回寫（上輪只改流程步 :274，規範說明 :259 仍是展開時讀現值）。
- specs/15 PSP 形分 **capture／authorize-only**：manual capture 記 AUTHORIZATION/
  AUTHORIZED、不發 orders/paid——上輪的 PSP 形無條件 SALE/SUCCESS 會把未請款單標已付。
- 02 章外部 destination 分支：留空目的地不記 incoming（鏡像外部 origin）。
- S5 鎖單範圍判別：僅限「存在未決 PSP 交易」的 pending；manual 單不鎖
  orderMarkAsPaid——無判別則全部 COD/轉帳/賒帳單永久未付。

### Pending

- 無新增。

## 驗收後修正（PR #52 第 7 輪＝bot 第 5 輪＋Codex 第 6 輪，2026-08-17）

### Done（bot 🔴6＋🟡2、Codex 六條，計 16 項全收）

- 四個修正家族的全部殘留副本同步：S5 鎖單判別 ×5（總綱連動表/guard 聯集/M11＋05 章
  兩處）；稅率尺度 ×2（10 章公式＋F.2#10 表示法規則）；restock 兩路鍵 ×2（總綱 dispose
  事件行＋冪等鍵覆蓋表）；metafield shop_id ×2（15 章流程行＋驗收要點）。
- specs/15 F5 與 T1 正典兩處互斥：步 2 鏈首插取單號（鎖序「先 counter 後 inventory」，
  原鏈跨入口死鎖）；cart 刪除/棄單解除移入 T1 同 transaction（崩潰窗口＝二次下單）。
- handoff §① 改**指標句**（指向本檔各驗收後修正節）——逐輪列舉每輪都過期，治本。
- 09 章 B.5 寫入合成序（我方裁定標 ⚠️）＋ERROR 映射；優先序末段修正：label/pickup
  先於 FULFILLED 兜底（原序令 READY_FOR_PICKUP 永不可達——上輪修正的同病複發）。
- Codex 四條新讀點：authorize-only 金額＝授權額非實收（否則 capturable=0 無法請款）；
  02 章 B.3 shipment 表外部目的地分支；12 章 redirect 查詢先於 410（否則商家 301
  對 unpublish 資源永不生效）；fileCreate 外部 URL 補 SSRF 防護。

### 全倉語義 grep 證據（2026-08-17 實跑快照，殘留清零後）

- 鎖單：`grep -rn "不可 mark as paid" docs/` → 6 命中/3 檔——內容檔 5 處全帶
  「未決 PSP 交易」判別，餘 1＝本節證據行自身
- restock 鍵：`grep -rn '冪等 key＝\`refund_line_item\` 唯一鍵' docs/research/90-shopify-business-logic-blueprint.md` → 0（單路無判別形清零）
- 稅率：`grep -rn "tax_bp / 10" docs/` → 僅本節證據行自身，內容檔 0
- metafield：`grep -rn "(ownerType, namespace, key) 唯一" docs/research/90-blueprint/15-platform-core.md` → 1＝:26 本尊物件模型轉述行（已加「我方＝shop_id 前綴」註）

### Pending

- 55 §B.3（bp 表示法出處）在本 PR 範圍外，⚪ 已登記——重建 Phase R 收斂。

## 驗收後修正（PR #52 第 8 輪＝Codex 第 7 輪十條，2026-08-17）

### Done

- 金額逐分支（capture＝實收/authorize＝授權額——共用「實收」令 manual-capture 記 0 額）；
- M-11 不整除分支＝逐件基數各自捨入求和（單一基數×量對混合件次算錯）；
- 兩路 restock 鍵補**互斥機制**（disposition 單位級 guard＋退款路條件式跳過——
  僅稱互斥、兩鍵各自唯一仍可雙回補）；
- abandoned_at 保留不清（recovered 推導依賴它；改為取消挽回信 job）；
- SSRF 補連線時 IP 釘選/重驗（DNS rebinding）；
- C11 狀態**就地收斂**（子交易＝新金流動作，非狀態推進——誤記子列則原列永掛 pending）；
- 09 章 untracked/digital 行跳過 ledger（從未入帳，動帳下溢）；
- closed 判定式限定**自動封存資格**（手動 orderClose 官方無前置）；
- draft COMPLETED＝已轉單非已付款（payment terms 轉正金流非 PAID）；
- handoff §① 行數不寫死。

### Pending

- 無新增。

## 驗收後修正（PR #52 第 9 輪＝bot 第 6/7 輪＋Codex 第 8 輪，2026-08-17）
<!-- 🔴 2026-08-17 更正（PR #52 第 10 輪）：本節標題與 Done 稱「19 項全收」不實——
     bot 第 7 輪 🔴-7（untracked 六處全域註記）整項漏做，第 10 輪補上。原文保留。 -->

### Done（19 項；bot 第 6 輪 19:27 與 push 19:29 時序交錯致漏，本輪併收）

- 稅率尺度終清（總綱差異表①/規則③/D-11 改後綴宣告制；10 章不整除補逐件求和）；
- S5 第六副本（04 章 guard 措辭變體——字面 grep 漏掃實證）；closed 三處
  （P-16/04 鐵則/測試綁自動封存 job）；C11（M3 切片就地收斂＋:374 引註）；
  transfer :558 分支指引；webhook 去重四處升 (shop_id, webhook_id)；
- 新六項：單號 rollback 語義／FEFO NULL 排序／metaobject 128/256 調和／
  自動折扣全區間重疊原子驗證／segment ≤1,000 顯式例外／specs15 通則逐形＋註記包裹。

### 方法論更正（bot 第 6 輪點破）

字面 pattern 清零 grep 抓不到語義變體——此後清零驗證一律用**語義變體組**。

### Pending

- ⚪ 三組（B2B 舊式五處/specs55 tax_bp/closed 倉庫外三處）補入 PR 描述。

## 驗收後修正（PR #52 第 10 輪＝bot 第 8 輪，2026-08-17）

### Done（🔴5＋🟡2）

<!-- 🔴 2026-08-17 更正（bot 第 9 輪）：本節標「🔴5＋🟡2」全收與 HEAD 不符——
     :1573 與 10 章 :193 兩個修法明列點位未動（第 11 輪補）；「高流量三處補僅 tracked 限定」
     中跨域 T3（總綱 T1 列）行內限定亦未補、由 T2 全域註記涵蓋。上一節「19 項全收」
     假宣稱的更正註記正上方再現同型不實宣稱＝鐵律 15.3 的又一實證。原文保留。 -->

- 🔴-1 補上一輪漏做的 untracked 家族：總綱高流量三處（T2 必做表/連動表/跨域 T3）
  補「僅 tracked 行」限定＋T2 表**全域註記**（涵蓋 S7/S8/移轉/退款 restock 全部
  履約動帳列）；上一節「19 項全收」假宣稱已加歷史層更正註記。
- 🔴-2~5 四家族副本：metaobject 仲裁表/M0 欄改 128/256 檔位制；07 章 B.1 前置改
  全區間重疊＋X-6 補限定；13 章 :220 升 (shop_id, webhook_id)；A-20＋15 章通則
  補 segment 具名例外。
- 🟡：:558 摘要補 on_hand−（漏了會破恆等式）；M-11 Σ 分支加「我方裁定 ⚠️」標
  （與規則④宣稱強度對齊）。

### Pending

- ⚪ 三組（B2B 舊式五處/specs55 tax_bp/closed 倉庫外三處）——本輪已補入 PR 描述
  ⚪ 段（瀏覽器端，改後取證）。

## 驗收後修正（PR #52 第 11 輪＝bot 第 9 輪＋Codex c63–c79，2026-08-17）

### Done

- bot 🔴-1/🔴-3 殘餘點位補齊：:1573 註釋改檔位制同 :2051；A-20 與 15 章例外清單補
  variants root connection 2048＋bulkCreate 2048（G1/G7）；15 章 app 定義數兩處補
  「changelog 256＝Plus 檔位值」另一判讀並存（撤「以 changelog 為傾向」）；
  :1640「商家自建同值」誤註改檔位制（Codex c65 同族）。
- bot 🔴-2：07 章 B.1 重新啟用列前置補「全區間重疊 ≤25 原子驗證＋重啟後區間無界」。
- bot 🟡-1：10 章 Σ 句補「我方裁定 ⚠️（parity 依 F.3#2）」。
- c64 M5 稅務判準改正典式（SCALE 後綴雙軌＋不整除 Σ 分支）；c67 兩處 ≤25 標籤補「全區間重疊」。
- c68 sales_reversals 拆分量（商品段專屬；稅/運/關稅/費各入淨值分量——雙扣例已註）。
- c69 edit session 鍵、c74 cart 行鍵、c75 InventoryLevel 鍵：三鍵一律 shop_id 開頭（鐵律 2）。
  <!-- 🔴 2026-08-17 更正（bot 第 10 輪）：「三鍵一律」當刻與 HEAD 不符——cart 鍵的總綱
       兩副本（X-21、M2 判準⑤）未動，第 12 輪已補。原文保留。 -->
- c70 「一切按日聚合 shop 時區」改 sessions 切日除外（Q-01 未裁）；c72 HALF_UP 降候選
  （M-5＋11 章 fixture 斷言，Q-11 未裁；官方例不區分 half-up/banker's）。
- c71 conversion_fee_bp 本尊商業費率降 parity 參考值，鍵改 null（Q-91）。
- c73 訂單成立動帳「僅 tracked 行」補 T1 正典列＋連動表＋S-4＋02 章兩處（履約側前輪已有）。
- c76 restock 兩路 guard 由「pre-check 跳過」改**原子 claim**（TOCTOU）。
- c77 品項守恆取數排除被替代歷史段（總綱 :215／判準⑦／09 章 C.9-1 三處同步）。
- c78 redirect 觸發規則句與差異表改「資源不可渲染（404 與 unpublish 410 皆查，D.5）」
  ——分支鏈 :297 第 7 輪已修，本輪把與其互斥的規則句同步。
- c79 消費端五步排序：D-89＋13 章 D-11 兩副本改「原子落庫（含去重）→200」。
- worklog 終態 Changes 清單補記各輪另改檔（c63）；bot 併帶點名的不實節標加歷史層更正註記。

### Pending

- 無新增；B⚪（CLAUDE 鐵律 4 與 research/28 未載分頁例外，兩檔不在本 PR diff）記入
  <!-- 🔴 2026-08-17 更正（bot 第 10 輪 🟡-4）：「記入 PR 描述 ⚪ 段」當刻不實——實際
       登記位置＝08-17 05:31 的 PR 留言（⚪ 續記段）；91 號坑登記簿建立前以該留言為
       登記處。原文保留。 -->
  PR 描述 ⚪ 段，待 91 號坑登記簿建立後搬入。

## 驗收後修正（PR #52 第 12 輪＝bot 第 10 輪 🔴5＋🟡4，2026-08-17）

### Done

- 「全家族清零」宣稱被證偽的補課：cart 鍵總綱兩副本（X-21/M2⑤）補 shop_id；S-14 守恆
  補排除式（第四副本）；redirect 家族 D-85/M2 切片/V-18 三處改「資源不可渲染（404 與
  unpublish 410 皆查）」；13 章 D.3 步 2/3 合併為原子落庫（去重鍵同一寫入）。
- 三個引據規格**直接回寫**（不留登記債）：specs/16 F3.2#2 守恆改排除式；specs/18 F4#4
  消費端指南補原子落庫且正序；specs/86 §3.2 公式第一項改商品段（口徑限定引言同補）
  ——09 章 C.9-1／14 章 :275／總綱 :110 引註同步「86 管來源互斥、金額段口徑＝商品段」。
- 🟡：11 章 C.1 補 Q-11 錨（候選語氣）＋鍵名統一 markets.currency_rounding_conversion_mode；
  M-5 引節 C.5→C.1；X-8 補兩路鍵＋原子 claim 指針（06 章 D.3-4 同步 claim 語義）；
  metafield 檔位跨概念引據兩處標「沿用 metaobject 屬我方推定」（Q-93 分別落定則兩鍵分立）。
- 歷史層：上兩節不實宣稱（「三鍵一律」「記入 PR 描述」）各加更正註記；B⚪ 登記處
  更正為 05:31 PR 留言（清法乙：改措辭——body 編輯本輪工具不可達，留言為現行登記處）。

### Pending

- 無新增；grep 清零宣稱本輪不再自稱，交驗收方覆核（教訓：字面 pattern 清零 ≠ 語義家族
  清零，「(cart_id, 合併鍵)」中文欄名即漏網形）。

## 驗收後修正（PR #52 第 13 輪＝bot 第 11 輪 🔴3＋🟡1，2026-08-17）

### Done

- 三家族同檔漏網補齊：總綱 :1020 事件耦合表 redirect 備註改「資源不可渲染（404 與
  unpublish 410 皆查）」；specs/16 驗收段拆單不變量補排除式（同 F3.2#2）；specs/86
  驗算表表頭限定口徑＝商品段（各列「訂單總計」讀作商品段總計）。
- 預防性同族：12 章 :298 redirect 查詢掛點改「資源不可用（404/410）handler 前」
  （概念 sweep 中人工核出，未被點名）。
- 🟡-1：worklog c63 補記由枚舉式改可計算式（diff 名單為準）——枚舉在第 12 輪
  帶進三個 spec 時即過期。
- 方法變更（本輪起）：動到的檔案整檔通讀該概念（specs/86 全檔重讀即本輪做法）；
  概念 sweep 用寬鬆詞根（「404」「總和」全 hit 人工核）取代精確 pattern。

### Pending

- 無新增；⚪ 承前（specs/52:68 同形不在 diff；specs/19 F1 回寫義務已登記在 86 §4.1）。

## 驗收後修正（PR #52 第 14 輪＝bot 第 12 輪 🔴2，2026-08-17）

### Done

- redirect 家族收尾：12 章 :347 我方欄舊錨點「掛 404 handler 前」改「資源不可用
  （404/410）handler 前」——上輪只改同列觸發欄、我方欄漏改（同列不同欄的漏網形）；
  :298 引據的兩個出處直接回寫（specs/13 F2 改 handle 查表句、specs/14 F5 掛點句，
  範圍皆同 90-blueprint/12 C.5）。
- specs/13/14 全檔概念核：其餘 404 hit＝商品狀態碼／soft-404 不變量／preview 簽名過期，
  皆非 redirect 掛點語義，不動。

### Pending

- 無新增。

## 驗收後修正（PR #52 第 15 輪＝主動封閉掃，2026-08-17）

### Done

- 不等驗收方點名，對歷史全部修正家族做一次寬根 sweep（稅率/S5/transfer/closed/C11/
  M-11/FEFO/單號/metaobject/≤25/三鍵/webhook 去重/日界/費率/守恆/redirect/五步/
  reversals/cart 鍵/HALF_UP），逐 hit 人工核。四處真殘餘＋一處引述加註：
  ①T4（transfer 跨域行）②X-39（三邊帳要害行）補 §02 B.2 分支——同 :558 已修家族；
  ③D-13「同時 active ≤25」補全區間重疊（原文漏未來排程形）；
  ④duties_transaction_fee_bp 寫死本尊費率改 null＋參考值（同 c71 conversion_fee 原則）；
  ⑤specs/16 的 46a 逐字引述（unique index on order_id）下加我方落地鍵註（X-19）。
- 其餘家族 sweep 結果＝無殘餘（bp 命中皆折扣/carrier 語境、mark-as-paid 命中皆
  金流狀態機語境等，逐 hit 已核）。
<!-- 🔴 2026-08-17 更正（PR #52 第 18 輪，Codex）：本節「四處真殘餘＋一處引述加註」與
     「其餘家族 sweep 結果＝無殘餘」皆為手寫計數／全稱宣稱，無同行複驗指令或標明時點的
     實跑快照，不合 AGENTS.md「可由代碼算出的數字」三種合法形式。原文保留；讀者以
     上列 ①–⑤ 逐項枚舉為準（枚舉本身即該輪的全量清單），計數與「無殘餘」不作宣稱——
     該輪 sweep 無留存可重跑腳本，事後無法補出重現指令。 -->

### Pending

- 無新增。

## 驗收後修正（PR #52 第 16 輪＝bot 第 14 輪 🔴2，2026-08-17）

### Done

- :1751 late_capture_surcharge_bp: 175 由可操作費率鍵改 informational 註釋形——與
  P1-06「不實作收費」裁定及 limits.yml 既有 informational-only 鍵互斥（正是第 15 輪
  修法④ duties 的同族；§8 規則⑤「YAML 存我方值」＝我方值是不收費）。
- edit session 單開鎖統一為 MySQL 8 可建形：生成欄 open_flag（open⇒1／終態⇒NULL）＋
  UNIQUE(shop_id, order_id, open_flag)（58 §G.3 waybill 生成欄先例；partial index
  <!-- 🔴 2026-08-17 更正（bot 第 15 輪）：上行「58 §G.3」錯節——waybill 生成欄先例
       實在 58 §D.5(b)（銷號帳）；§G.3 是金額轉換節。正規文本兩處已直接改對。原文保留。 -->
  MySQL 8 不支援）——總綱 X-19/:811/:1345、specs/16 C1、:933 註五處同形。
- 第 15 輪「其餘家族 sweep 無殘餘」被 :1751 證偽的根因：該行同時含 bp＋費率字根、
  同檔同族的 :1910 有抓到、本行卻在逐 hit 判讀時被「§05 C.1 出處」表象放行——
  判讀漏不是 pattern 漏；sweep 的人工核對費率類 hit 一律回對 P1/裁定表再放行。

### Pending

- ⚪ limits.yml:765 informational 鍵以 float 主單位率 0.0175 表示、與 §8 規則③
  整數＋後綴口徑不合（informational 是否豁免無明文）——不在本 PR diff，登記待
  下次動 limits.yml 時釐清。

## 驗收後修正（PR #52 第 17 輪＝bot 第 15 輪 🔴1，2026-08-17）

- waybill 生成欄先例引節改對：58 §G.3（金額轉換節）→ **58 §D.5(b)**（銷號帳，58:848
  的 active_waybill_key）——總綱 X-19 與 specs/16 C1 直接改，worklog 第 16 輪節追加
  更正註記。根因＝寫引用時憑印象未開檔核節號；本輪起新引節一律開檔對標題再落筆。

## 驗收後修正（PR #52 第 18 輪＝Codex 佇列 flush 批，2026-08-17）

### Done

#53 合併使 base 移動，Codex 對新 merge ref flush 三個全量 review。處理法＝逐條以
引文對現行樹分類：引文已不存在者回覆標明已修輪次（不動檔）；仍存在者修**該點名處**、
取意見給的最小修法。逐條處置對映見本輪 PR 留言；異動檔案集合複驗同上方補記的
diff 名單查法。修正的主要語義群（逐條明細以 PR 留言為準）：

- 總綱狀態表：S4 庫存 commit 補 tracked=true 條件；S7 FO move 未決對補全（Q-23 未裁
  不動工）；S13 CANCEL 回補限曾 commit 的行；D6 退貨自動關閉改「全行終局 disposition」；
  F6 Market enum 改 2 值硬刪；refundCreate 投影延至 REFUND 交易 SUCCESS；V-2 purchasable
  補 UNLISTED；X-30 補店級序列化鎖；退貨子冪等補共用防重 guard 機制；A1 通知改事件
  時點快照；M2 行合併鍵 bundle parent_id 暫定入鍵（Q-44 登記）。
- 05 章：UNKNOWN 進入限非終態；R1 超時查無/無終局改轉 UNKNOWN（僅明確拒絕落 FAILURE）；
  C.13-3 退款上限改條件式計數器（M7 軟上限）。
- 各領域章：03 D.3 checkout hold 標本尊觀察；04 C.4 補 checkout-extension 訂閱 guard
  ＋2019 前標不復刻；07 store credit FEFO 補 NULL 排序；08 匿名化擴到全部持久化 PII
  副本；09 3PL callback SSRF＋ERROR→FAILURE 顯示＋carrier cache key 全請求 digest；
  10 含稅抽取式分支；12 originalSource SSRF＋zip 解壓邊界；13 webhook 投遞 SSRF；
  14 COGS 快照句對齊 T1 凍結。
- specs：15-F5 服務簽名補 idempotency_key＋參數 digest；19 Total sales 分量拆分一致化；
  86 §3.2 無 refund_line_items 退款 fallback；28 redirects 查表擴至 404/410；01 章單位
  定價顯示位數依市場 locale。
- worklog 本檔：diff 名單查法補合併後形；第 15 輪 sweep 計數宣稱加更正註記。

### Pending

- ⚪ 第 15 輪 sweep 無可重跑腳本一事已以更正註記封存，無法事後補指令——後續輪次的
  sweep 類宣稱依鐵律（fix-only）不再產生。其餘無新增；上輪 ⚪（limits.yml:765）不變。

## 驗收後修正（PR #52 第 19 輪＝bot 第 17 輪 🔴5＋Codex 16 則，2026-08-17）

### Done

第 18 輪「只動點名處」被兩驗收方點名的家族副本與兩處第 18 輪自傷，本輪全部點名處修復：

- **R1 超時語義家族四處**：總綱 S5 欄／P-8／D-47（決策改寫留沿革）／05 章 F 測試 7①，
  全數同步「查得照落、明確拒絕 FAILURE、查無/無終局轉 UNKNOWN 進 R2」。
- **退貨自動關閉家族六處**：06 章 T5（[G3] 官方轉述地位明確化——我方裁定形＋本尊行為
  標 V 待實測）／06 D 步 4／06 F 測試 1／總綱 S13 欄／:524 連動表／:932 事件表，
  全數改「全行終局 disposition；restock 僅限 RESTOCKED 行」。
- **第 18 輪自傷兩處改正**：總綱:808 防重機制由 pre-check 形改回 T3 正典原子 claim 形；
  10 章含稅基數式改正——第 18 輪誤把 C.2 抽取式結果（稅額）標成淨額基數，
  正確＝`price_inc ÷ (1 + rate)`，兩法擇一不串聯。
- 其餘點名副本：13 章 E.2 改事件時點快照（A1 同步）；12 章 C.6 補店級序列化鎖；
  03 章 completed 前置改我方無 hold 形＋B.4 標本尊觀察＋C.1 parent_id 暫定入鍵
  ＋F 測試補跨父不併；總綱 S0／specs/15 兩處合併鍵同步 parent_id；總綱:2239 M1
  可得式補 UNLISTED；總綱:2187 M5 Market 切片改二態；05 章 refundCreate 轉移列
  投影延至 SUCCESS；02 章 move 三處（B.1 列／D.1 步 2／F.3 測試）封鎖於 Q-23；
  specs/16 F4＋04 章取消流程補「僅 T1 曾 commit 的行」；09 章 cache key 改整包
  payload 序列化（不再維護欄位清單）；28 號 ThemeRole 補 DEMO（themePublish 拒絕）；
  08 章匿名化補 crypto-shredding 機制（唯一同時相容 A1 凍結與 16:19 不可變的候選）；
  limits.yml 新增 `outbound_http.*` 三鍵、三條出站契約各自引用。

### Pending

- 06 T5 我方裁定形（終局 disposition 即關）的本尊實際行為＝V 待實測（實測店造
  NOT_RESTOCKED 退貨驗證）；Q-23／Q-44 兩未決不變，相關數量帳/合併鍵標暫定。
- 其餘無新增；⚪ 清單見 PR 描述（本輪隨 bot ⚪ 段補登 specs/14:27、specs/18:11 等項）。

## 驗收後修正（PR #52 第 20 輪＝bot 第 18 輪 🔴2🟡2，2026-08-17）

### Done

- 🔴 03 章 B.2 `completed` 列二次改正（第 19 輪修一半的回歸）：去掉「付款成功」
  我方前置——completed 閘在**訂單成立（T1）原子 commit**，付款結果只決定
  `displayFinancialStatus`；進入條件欄同改（COD／bank deposit／B2B terms／admin
  PENDING 單「訂單已成立、付款未成功」，照舊文永遠停在 open）。
- 🔴 outbound_http 逾時四鍵落鍵（webhook connect 1s／response 5s＝13 C.3 官方值；
  callback／file_fetch＝我方裁定）；09:53／12:257 兩句「大小/時間上限＝bytes 鍵」
  拆開各指其鍵；limits.yml 出處註釋「12 §B files」改「12 §C.7」（引節錯由第 19 輪
  修復自身引入，與第 15 輪 waybill 同型）。
- 🟡 總綱 T3 步驟表 `displayFinancialStatus` 列補「REFUND PENDING 投影不變、
  待 SUCCESS（R-11）」限定；28:392 出處字串改「總綱 §3 F11 列＋語義詳 12 B.1」
  （F11 非 12 章節號）。

### Pending

- 無新增待辦。⚪ 側：PR 描述 ⚪ 段的補登已於 bot 快照後 4 分鐘落地（時序錯位，
  非未執行——回覆附 API 證據）；05:214-215 觸發欄與 13 D.6 沉默兩條隨 R-11／A1
  家族下次收斂處理（bot 已登 ⚪ 不擋）。

## 驗收後修正（PR #52 第 21 輪＝bot 第 19 輪 🔴4🟡3，2026-08-17）

### Done

- 🔴 R-11 SUCCESS 出口分目的地（六處同步：總綱 T3 表/耦合列/R-11 本文、05 章轉移列、
  06 章 D.4 步 2、specs/16 F5 步 3）：外部金流分支＝PSP webhook 確認；**內部目的地
  分支（禮品卡餘額回加／store credit 寫入／manual 線下）＝同一本地 transaction 內
  即建 status=SUCCESS**（manual 需人工確認走 16 §F5 COD 條件式 UPDATE 形）——
  原單一 webhook 出口會讓本 PR 自己窮舉的三類無 PSP 退款投影永卡 PAID。
- 🔴 webhook_response_bytes_max 1 MiB → **65536**（權威＝specs/18 F4-2 既定 64KB，
  39 號兩處同）；🔴 file_fetch_download_bytes_max 100 MiB → **20971520**（＝12 §C.1
  泛型/圖片門檻；原值在業務門檻前永不生效）＋逾時鍵註釋隨改。
- 🔴 specs/15 兩處 checkout 狀態名 active → **open**（F5 步 2 條件轉移＋F3 步 1 建列
  ——Checkout 恰 3 值，active 是 cart 的狀態名，照抄則 T1 的條件式 UPDATE 匹配 0 行）。
- 🔴 12 章 B.6 fileCreate 列三合一：大小門檻引節 §C.7→**§C.1**、逾時補鍵、
  大小/逾時與 §C.7 業務規則列同鍵源。
- 🟡 13 章 C.3 表兩列標「已落 outbound_http.*（第 20 輪）」防重複落鍵；13:274 SSRF
  清單補逾時鍵（三副本形態一致）；callback_response_bytes_max 補各自理由
  （被解析的 SKU map，webhook 的「不消費」理由不適用）；PR 描述 doc-claims 條數
  3→4 更新＋⚪ 登記一條（03:124 更正註記 orderCreate 舉例越界）。

### Pending

- 無新增。R-11 家族的 05:214-215 觸發欄（⚪ 沿掛）隨下次該家族收斂處理；
  03:124 註記的 orderCreate 舉例越界已 ⚪ 登記於 PR 描述（敘事層、不擋）。

## 驗收後修正（PR #52 第 22 輪＝bot 第 20 輪 🔴2🟡2，2026-08-17）

### Done

- 🔴 R-11 家族第七、八份副本同步＋測試門檻：總綱 §9.2 M4 完成判準③、§5.3 T5
  耦合列改「SUCCESS 出口分目的地」形；§9.3 M4 測試門檻③補三型出口各一條斷言
  （破壞任一型必紅）。
- 🔴 三處引節 §F5→**§F4.4 COD 對帳回寫**（16 F5 步 3、06 D.4 步 2、總綱 R-11
  本文——「撥款」全檔僅命中 F4.4；同型於 12:155／waybill 先例，第三次教訓）。
- 🟡 內部目的地拆兩型（判準＝目的地是否即為平台帳本內餘額）：**帳本內即時型**
  （禮品卡餘額、store credit——餘額即錢本身）＝同交易 SUCCESS；**線下待確認型**
  （manual 家族：bank_deposit／COD 退匯——錢在系統外流動）＝停 pending 待 F4.4
  對帳式 UPDATE——八份 R-11 副本全數帶型；原「manual 即建 SUCCESS」＝錢未匯出
  即宣稱已退，正是 R-11 自己點名的失敗模式。
- 🟡 12 §C.7 業務規則列補範圍子句（20MB 上限僅限外部 URL 抓取；staged 走 §C.1
  預檢，影片 ≤1GB 不受影響）；limits.yml 抓檔鍵註釋補「我方裁定」標記與回寫指標。

### Pending

- 無新增。⚪ 新登記兩條（specs/50:45 S-28 checkout 舊三值——PR 外；18/39 三處
  硬編 64KB 與鍵同值四份持有）入 PR 描述 ⚪ 段；其餘 ⚪ 沿掛不變。

## 驗收後修正（PR #52 第 23 輪＝bot 第 21 輪 🔴1🟡1，2026-08-17）

### Done

- 🔴 五份副本句首「transaction=pending」寫死形照 :2181 已定形收（總綱 R-11 本文／
  耦合列、05 轉移列、06 D.4 步 2、specs/16 F5 步 3）——內容清單只寫 transaction、
  =pending 移進外部金流分支；與同句「帳本內即時＝即建 SUCCESS」的互斥解除。
- 🟡 線下待確認型出口補「人工確認」一路到其餘六份（bank_deposit 無 carrier／
  無對帳檔，純 F4.4 對帳形出口不可達）；specs/16 F5 步 3 給人工確認**落地格**：
  觸發＝財務「標記已匯出」動作、條件式 UPDATE 單次生效＋idempotencyKey、
  🔴 不得走 Orders::MarkAsPaid（收款服務，方向相反）；:2221 第三型斷言措辭同步。
- ⚪ 順手（判詞明邀）：16:278「永遠先落 pending 再打金流」加（外部金流分支）限定，
  免成第七份寫死形。

### Pending

- 無新增。⚪ 登記兩條（specs/55:79 M09 單一出口舊形——PR 外；12:308 staged 預檢
  content-length-range 缺口——下次動 12 章）入 PR 描述。

## 驗收後修正（PR #52 第 24 輪＝bot 第 22 輪 🔴2🟡2，2026-08-17）

### Done

- 🔴 線下待確認型**併一路＝人工確認**（判詞修法①）：COD 對帳形出口不可達
  （F4.4 對帳檔僅收款列、比對鍵含 cod_tracking_no、方向＝物流商代收撥商家；
  58 §K13 六家全 false）——九份副本全數改「人工確認（16 §F5 步 3 落地格）」、
  F4.4 降為條件式 UPDATE 形狀先例不作出口；五份掛錯節（F4.4 名下）與四份
  COD-對帳形一次收齊。
- 🟡 90:935 refunds/create 備註由「transaction=pending 階段就發」改「Refund 物件
  建立時（本地 transaction 內）即發，不等金流確認」（帳本內即時型無 pending 階段，
  照字面掛 status 條件會漏發）。
- 🟡 16 §F5 步 3 落地格補治理三件：mutation `refundMarkAsSettled(transactionId)`
  （resourceVerb，鐵律 4）、權限 `orders.mark_refund_settled`＋二次確認（治理形同
  over_refund）、冪等鍵 `refundsettle-{transaction_id}` 單一 service、audit log 必落；
  28 號退款列與金流 mutation 清單同步登列（複驗：`grep -c "refundMarkAsSettled"
  docs/research/28-api-contract.md`）。
  <!-- 🔴 2026-08-17 更正（bot 第 23 輪）：本行原文以「前值→後值 支」括號形裸寫金流
       mutation 支數、無複驗方式，doc-claims 於 HEAD 抓 error——且該輪閘門跑在
       commit 前、diff 制掃描看不到未提交行。自此輪起 commit 後必重跑 doc-claims
       再 15.1。原句已改附複驗指令。 -->

### Pending

- 無新增。⚪ 登記四條（22:32 與原型 :3963/:10919-20 未分支 pending 形——皆 PR 外；
  specs/50:33 S-16 單一出口舊形——PR 外；16:278 枚舉待下次動 F5 補全）入 PR 描述。

## 驗收後修正（PR #52 第 25 輪＝bot 第 23 輪 🔴4🟡2，2026-08-17）

### Done

- 🔴 16 §F5 落地格謂詞補全資格條件（原僅 `WHERE status='pending'` 會翻掉 Stripe
  未確認退款列與 COD 收款列）：`WHERE id=:id AND kind='refund' AND status='pending'
  AND gateway IN ('manual','cod','bank_deposit')`（暫定值域＝15-F5 manual 形；
  正式 `(kind,gateway)` 值域表標待釘）＋非本家族/非 pending 一律 reject。
- 🔴 「58 §K13 全 false」誤讀四處全刪（90 兩處、06、16）——K13 為 5 carrier 欄、
  ⛔/—/❌ 語義各異、— ＝未查證非否定；第一理由（對帳檔僅收款列）單獨支撐併一路，
  各處留更正註記。
- 🔴 治理三件落 limits.yml 機器可讀鍵：`idempotency.required_for` 增列
  refundMarkAsSettled（:529「以下 N 支」註與 :584 出處分層、:459 法域無關註、
  28:154 同步）；`business_unique_keys.refundMarkAsSettled: [transaction_id]`；
  退款區 `mark_refund_settled_requires_permission`／`_requires_confirm` 兩鍵。
- 🔴 worklog 裸計數行改附複驗指令＋更正註記；流程修正＝**commit 後必重跑
  doc-claims 再 15.1**（diff 制掃描在 commit 前看不到新行——本輪事故根因）。
- 🟡 28:154「這 9 支」→「這 10 支」並明文 refundMarkAsSettled 屬 core_all_packs
  （COD 法域素材不改變 core 歸屬——關閉「第 10 支＝pack-scoped 例外」的誤推）。
- 🟡 0-row 錯誤契約三因分流入落地格（冪等回舊態／INVALID_STATE／
  INVALID_TRANSACTION_KIND，typed code 同 16:399 表）。

### Pending

- ⚪ 55 §A 總表 M42 列（refundMarkAsSettled）：55 不在本 PR diff 且該檔多處
  計數宣稱，收斂期不開新檔面——已登記 PR 描述，55 下次更新時補列（limits.yml
  增列處註釋已互指）。mutation 名 resource 邊界案例（名為 refund、改的列是
  OrderTransaction）同登記，改名與否待 91 號建立後裁定。
