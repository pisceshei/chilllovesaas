# worklog 2026-08-23 — 後台介面語言包 ML-1（en／zh-Hant／zh-Hans／ja／fr）

## 已完成的工作 (Done)

- 前端 i18n 機制：`app/frontend/admin/i18n/format.ts`（ICU 子集）、`I18nContext.tsx`（Provider／useT／translateWith，缺鍵回 key）、`locales.ts`（limits 鏡像）、五語 `messages/*.json`（en 正典；四語由我方翻譯，裁定 C6）。
- 全部畫面文案改 `t()`：navigation（label→labelKey）、AdminShell（含新增**介面語言切換器**）、App placeholder、ProductsPage、ProductDetailPage、IndexTable、Button；contexts 的開發者錯誤改英文。
- 注入鏈：`staff_members.locale` → `admin/spa/show.html.erb` `data-ui-locale`／`<html lang>` → entrypoint → `AdminRoutes.uiLocale`。
- 後端：`staffLocaleUpdate` mutation（＋錯誤型別）；`StaffMember.locale` 值域驗證；GraphQL controller `I18n.with_locale`；`Catalog::SaveProduct` 22 則 userErrors 訊息改 `I18n.t`；`config/locales` 五語 yml；`config.i18n.available_locales`。
- 測試：vitest 快照 `Tests 26 passed`（新增 `i18n/messages.test.ts`、`layout/AdminShell.test.tsx`）；rspec 快照 `359 examples, 0 failures`（新增 `spec/requests/staff_locale_update_spec.rb`、`spec/config/locales_spec.rb`；系統測試斷言改英文）；rubocop／tsc 無 offense。
- `docs/dev/m1-admin-i18n.md`。

## 修改的檔案與核心邏輯 (Changes)

- 新增：`app/frontend/admin/i18n/format.ts`／`I18nContext.tsx`／`locales.ts`／`messages.test.ts`／`messages/en.json`／`messages/zh-Hant.json`／`messages/zh-Hans.json`／`messages/ja.json`／`messages/fr.json`；`app/frontend/admin/layout/AdminShell.test.tsx`；`app/graphql/mutations/staff_locale_update.rb`；`app/graphql/types/errors/staff_locale_update_user_error_code.rb`／`staff_locale_update_user_error_type.rb`；`config/locales/zh-Hant.yml`／`zh-Hans.yml`／`ja.yml`／`fr.yml`；`spec/requests/staff_locale_update_spec.rb`；`spec/config/locales_spec.rb`；`docs/dev/m1-admin-i18n.md`
- 修改：`app/frontend/admin/App.tsx`、`layout/AdminShell.tsx`、`layout/navigation.ts`、`pages/ProductsPage.tsx`、`pages/ProductDetailPage.tsx`、`components/IndexTable.tsx`、`components/Button.tsx`、`lib/ToastContext.tsx`、`lib/SaveBarContext.tsx`、`app/frontend/entrypoints/admin.tsx`、`app/views/admin/spa/show.html.erb`、`app/views/layouts/admin.html.erb`、`app/graphql/types/mutation_type.rb`、`app/models/staff_member.rb`、`app/controllers/admin/api/v202608/graphql_controller.rb`、`app/services/catalog/save_product.rb`、`config/application.rb`、`config/locales/en.yml`、`app/assets/stylesheets/admin.css`、頁測試兩檔（`uiLocale="zh-Hant"`）、`spec/requests/product_set_spec.rb`、`spec/system/m0_admin_shell_spec.rb`

## 尚未完成或需注意的風險 (Pending / TODO)

- 伺服端仍硬編繁中的訊息：GraphQL controller top-level 錯誤、`Idempotency::Guard::Conflict`、`translate_record_invalid`、登入頁——ML-1b 改鍵。
- 五檔 JSON 靜態 import；bundle 變大到一定程度再 code-split。
- 內容語言（商品五語）屬 ML-2；商品頁內容語言 chip 暫為「English」靜態字。
- 本尊各語言後台的措辭未對照（鐵律 9 不抄文案）；若使用者對特定語言用詞有偏好，改 JSON 即可、零程式碼。
