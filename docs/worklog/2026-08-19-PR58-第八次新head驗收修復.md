# PR #58 第八次新 head 驗收修復

> 對應驗收：GitHub Codex review `4973362395`（審 `4335450`）｜本檔與 handoff 同 commit

## 已完成的工作 (Done)

- 依鐵律 15.2 拉取 exact-head review body 與全部 inline；review `4973362395` 精確綁定 `4335450cd92a3715dd065d936b8d6f819de32d11`，共有 inline `3813973524`、`3813973532`、`3813973539`、`3813973544` 四條。
- 以 `git log -p -S` 追查四個斷言的沿革，確認 latest SHA 與 rate-limit 兩句是先前修復輪留下的證據精度問題，`--sp-500`×6 是 2026-08-18 盤點快照，INTERVAL≥300 則來自 #59 的 API 額度下限、與其後鐵律 17.1 的 15–25 分鐘窗沒有同步。
- 重查 GitHub 官方 required-check 文檔，補入逐字 "Required checks must pass on the latest commit SHA. Checks from earlier commits don't satisfy the requirement."、URL 與取證日期 2026-08-19；同時依 test merge commit 例外刪除過寬的「一律看合併當下 head」。
- 重查 GitHub primary／secondary rate-limit 官方頁，證實官方段落只能支持 reset 時點與 secondary 有限次重試，不能證明 primary「沒有任何」總界；終態外部事實檔降回證據邊界，未決推論按規則移入 `docs/specs/91-pit-register.md` §3.4。
- 以 `rg -n --glob '*.html' -- '--sp-500' docs/design` 取得完整現值集合，刪除會漂移的手寫數量；P-4 改要求未來 `r_undefined_token` 對命令的全部命中逐一處置。
- 將 `scripts/await-verdict.sh` 的 INTERVAL 契約收緊為 900–1500 秒，且先比十進位位數再做數值比較，避免超大整數使 shell 比較 fail-open；同步 CLAUDE 機制註記與終態自動化文檔。

## 修改的檔案與核心邏輯 (Changes)

- `docs/dev/external-facts.md`：A7 補 latest SHA 官方證據與 test merge 精度；B6 撤除「primary 沒有任何門檻」全稱句。
- `docs/specs/91-pit-register.md`：登記 rate-limit 推論邊界，並把本輪 worklog／handoff 納入附錄 A。
- `docs/plans/2026-08-18-總方案.md`：兩處 `--sp-500`×6 改成由命令導出的完整集合，不另寫快照數字。
- `scripts/await-verdict.sh`、`CLAUDE.md`、`docs/dev/m0-automation-infra.md`：把 bot 等待間隔的可接受區間與鐵律 17.1 對齊為 900–1500 秒。
- `docs/worklog/2026-08-18-P0-方案落庫與鐵律16-18.md`：同步第八次新 head 輪次、檔案列與本輪查證結論。
- 本 worklog 與 `docs/handoff/2026-08-19-PR58-第八次新head驗收修復.md`：形成本輪獨立三件套記錄。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本 commit 推送後須重新取得 Claude 與 GitHub Codex 對精確新 head 的驗收；`4335450` 的四條意見已處理，但任何舊 head 的通過都不能沿用。
- PR #60 已把 poller 擴為五參數與 deadline；待 #58 合併後 rebase 時，必須把本輪 900–1500 秒 INTERVAL 契約帶入，不能保留其現行 35 天 INTERVAL 上限。
- GitHub primary rate-limit 的總重試／時間契約沒有取得官方明文；本輪只撤除過度宣稱，不把有限檢索包裝成「官方從未規定」。
