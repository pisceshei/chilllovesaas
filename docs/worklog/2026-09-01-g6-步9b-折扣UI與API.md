# 2026-09-01 G6 步 9b：折扣 API／admin／前台

## 已完成的工作 (Done)

- 實測親點（折扣空態/型別四值 modal/Amount off order 全表單/Combinations 展開
  逐字——含 shipping "(best value wins)" 官方實錘）→ dev doc §5。
- GraphQL：discounts/discount query＋官方同名 Basic 四支＋lifecycle 三支（ours
  合一）＋DiscountUserError 三件套＋DiscountBasicInput（basisPoints Integer）。
- admin：/admin/discounts 佔位轉正（列表/空態/型別 modal）＋DiscountFormPage
  （method 分段/code+隨機產碼〔排除 0O1IL 易混字元——17-F1.2〕/值/門檻 radio/
  用量勾選/Combinations 三勾帶官方 helper/日期/lifecycle 鈕）。
- 前台：摘要折扣輸入欄＋折扣列；POST /checkouts/:token/discount（壞碼統一文案＋
  不留殘）；GET /discount/:code 分享連結（cookie→建結帳兌現→清→重算）；
  refresh_discounts! 抽出（delivery 重算同一條鏈——鐵律 7 不開第二條）。
- 突變輪 6/6 殺＋canary。

## 修改的檔案與核心邏輯 (Changes)

- 新：discount GraphQL 13 檔（type/connection/edge/errors×2/input/mutations×7）、
  `DiscountsPage.tsx`＋`DiscountFormPage.tsx`（＋test）
- 改：query_type/mutation_type、`config/routes.rb`（discount 兩路由）、
  `app/controllers/storefront/checkouts_controller.rb`（apply_discount/
  discount_link/refresh_discounts!/create 兌現 cookie/摘要折扣列＋輸入欄）、
  `App.tsx`、admin.css（cl-segmented）、i18n ×5（+59 鍵）
- 測試：`discount_mutations_spec`（6 例）＋`storefront_discount_ui_spec`（4 例）＋
  FE 2 例

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.55：eligibility segment／銷售通路 access／Tags／BxGy／碼輸入限流
  （17-F4.1 的 10 次/分——Rack::Attack 隨限流包）／per-type lifecycle 官方分拆。
- 折扣詳情 performance 區（17-F4.3 applications 聚合報表）隨分析線。
- 生產煙測＝本包部署後做（建碼→前台套用→金額→清理）。
