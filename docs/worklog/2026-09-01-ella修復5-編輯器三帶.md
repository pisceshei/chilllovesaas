# 2026-09-01 Ella 修復 PR-5：編輯器三帶——section groups 進樹＋群組編輯寫回

## 已完成的工作 (Done)

- 使用者指正「編輯頁面完全無法裝修 Ella」的結構性根因之一收口：編輯器
  先前只有 template 層，Ella 的 header/footer（layout `{% sections %}` 群組）
  **不可視不可編**（六軸診斷 editor 軸 critical ①）。
- 後端 `sectionGroups` 讀面：layout 掃描（與渲染同一正則）→ 群組 JSON
  （經 source 讀＝吃 16e1 檔案覆寫層）＋overlay lockVersion。E7 紅證
  （含渲染帶群組的可視面）。
- 前端三帶樹（24 §1 本尊樹形）：頁首帶（name 含 header 的群組）→ 範本帶
  → 頁尾/其他群組帶；帶標籤 i18n ×5。
- 🔴 band 化 op 管線：undo/redo 棧改 `{band, snap}`（群組與模板**同一棧**、
  跨帶復原到正確帶——MH3 突變證錯位會被抓）；五原子 op＋setSetting 全
  band 路由；選中含帶（同 id 跨帶不撞）；預覽 cl:select 跨帶定位。
- 群組寫回：Save 對 dirty 群組逐一 themeFileUpsert
  （`sections/{name}.json`＋lockVersion——16e1 覆寫層＝後端 touch theme
  同軸，前台即時生效）；模板未動不發 templateUpsert（ED11 釘死）。
- 突變 3/3 殺（MH1 掃描退空／MH2 寫回斷線／MH3 跨帶錯位）。

## 修改的檔案與核心邏輯 (Changes)

- 改：theme_type（sectionGroups）、ThemeEditorPage（band 架構＋三帶樹＋
  群組 save）、editor 測試 9→11（ED10/11；ED1/2/8 band 化修正）、
  bootstrap spec 6→7（E7）、minimal fixture（layout {% sections %}＋
  header/footer-group.json）、i18n 3 鍵 ×5、admin.css。

## 尚未完成或需注意的風險 (Pending / TODO)

- 群組帶的 add-section picker 未接（picker 仍只加到範本帶）；block 級
  操作＝PR-6；即時預覽＝PR-7；UI 對位＝PR-8。
- 群組分帶規則＝name 含 "header" 在上（Ella 形）；aside/overlay 群組
  （popup 等）落「其他」帶以群組名顯示。
