# PR #61：Codex `44ebd39` 延遲意見修復後閘門復驗

## 已完成的工作 (Done)

- 從 setup 起執行 production-equivalent 閘門。第一輪在前 14 項全綠後，第 15 項
  `python3 scripts/lint-prototype.py` 回 9009；`Get-Command python3` 證實它只是 WindowsApps
  App Execution Alias。A/B 探針改用本機 Python 3.12.10 絕對路徑並設
  `PYTHONIOENCODING=utf-8`，prototype lint 實得三份 `ERROR 0 / WARN 136`、exit 0。
- 依鐵律 15.4 不沿用第一輪的 14 項，第二輪從 bundle check 重新開始，正式結果為
  `GATES_ALL_GREEN=29`。其中 MySQL `db:prepare` exit 0、Rails 284 examples／0 failures、前端
  5 tests／0 failures、RuboCop 162 檔零違規、兩種 audit 與 Brakeman 零漏洞／零警告。
- 自訂閘門亦全綠：prototype `ERROR 0`、doc-claims warning 0、租戶／撤銷／金額／limits／CI
  parity 均通過；workflow syntax 解析 2 份 YAML、33 個 `run:`，11 條回歸測試全綠。
- Q-1 三式分別輸出 token 35、元件列 16、連續節號 37。局部表格每原始列為 4 個未跳脫
  直線；GitHub Markdown API 的實際 HTML 為 7 個 `<tr>`、3 個 `<th>`、18 個 `<td>`。

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-PR61-Codex-44ebd39延遲意見閘門復驗.md` 與同名 handoff：保存
  Python 假直譯器、A/B 探針、完整正式輪與 Markdown 渲染證據。
- `docs/specs/91-pit-register.md` §3.5：只登記 WindowsApps `python3.exe` 存在但不可執行的
  環境坑；未擴修 `config/ci.rb` 或腳本。
- `docs/handoff/2026-08-20-階段一開場包.md`：增加本步終態入口；前一步 worklog／handoff
  同步由「待完整閘門」更新為已驗證狀態。

## 尚未完成或需注意的風險 (Pending / TODO)

- Node v24.19.0 超出 `package.json` 宣告的 `>=22.12 <23`；本輪前端實跑綠只證明這次命令
  成功，不構成專案已支持 Node 24 的發布宣稱。
- 本步文件落地後仍須再依 15.4 跑最終完整閘門、commit，並補跑 commit 後 doc-claims；任何
  error 或 warning 都會阻擋 push。
- GitHub 兩個 delayed threads 尚未回覆／resolve；新 head 的 CI、Codex 與合併守衛尚未取得。
- Claude validation-skip 仍是平台的 default-branch workflow mismatch；本步沒有 workflow 修法
  授權，不能把 shell success 當 Claude 驗收意見。
