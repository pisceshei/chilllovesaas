# 2026-08-28 — S8 上架事件層（listing events）

🟢 不觸 18.3。裁定＝D74；dev doc＝`docs/dev/m2-listing-events.md`；登記＝`91` §3.45。

## 已完成的工作 (Done)

- **研究**：官方三句逐字（ADD 有 active 限定、REMOVE/UPDATE 無）＋到點一句；
  variant listing topic 官方不存在（2026-08-28 掃描）⇒ ours 自訂。
- **交付**：Topics +6 EXTERNAL；Write 即時轉場發射（同交易）；
  `ListingEventTranslator`（同 topic 第二消費者，DB 現值判準＋dedupe 冪等）。
- **驗證**：spec 13 格全綠；五突變全轉紅；PR-C 24 格照綠。
- 🔴 開發中抓到並修掉：`joins(:variants)` 關聯名錯（實為 `product_variants`）——
  是 PR-C 的 T08b 在 relay 全鏈路下炸出來的（同 topic 掛第二消費者，任一個 raise
  整筆事件轉 pending）⇒「新增消費者會被既有消費者的 spec 連帶驗證」這個性質是真的。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `app/services/events/topics.rb` | 六個 listing topic ＋ EXTERNAL |
| `app/services/publications/write.rb` | `emit_listing_transition!`／`listing_topics_for`／`listing_addable?`／`emit_listing_event!`；`remove_publication!` 帶 `at` 並判已發布 |
| `app/services/publications/listing_event_translator.rb` | 新增 |
| `app/services/events/consumers.rb` | 同 topic 掛第二消費者 |
| `spec/services/publications/listing_events_spec.rb` | 新增 13 格 |
| `docs/dev/m2-listing-events.md`／`docs/DECISIONS.md`／`docs/specs/91` | 文檔三件 |

## 20.2 適用類型／固定處理／反向複驗輸出

| 類型 | 適用 | 固定處理 | 反向複驗 |
|---|---|---|---|
| ① 證據 | 是 | 三句官方逐字＋日期；未取得處全部 fail-closed 並登記（R6／status 翻轉／UPDATE 寬義／取消排程 REMOVE） | 91 §3.45 |
| ④ 狀態空間 | 是 | 轉場矩陣九格全列（dev doc ②），每格有事件或 fail-closed 理由 | spec 13 格 |
| ⑤ fail-open | 是 | 🔴 兩消費者順序競態被設計掉（DB 現值判準）；五突變逐一實跑 | 全轉紅 |
| ② 同步 | 是 | ADD 閘與 D53 同一集合（PURCHASABLE）；dev doc 與 91 同 commit | — |

## 尚未完成或需注意的風險 (Pending / TODO)

- **訂閱／投遞面不存在**（§3.45 W-1）——事件現在只累積在 outbox（零消費者的
  EXTERNAL topic 由 relay 標 published，行為與 products/create 等既有外部事件一致）。
- **status 翻轉的逐管道事件**（W-2）與 **R6 的 REMOVE 與否**（W-3）＝待本尊實測。
- **UPDATE 語義刻意收窄**（W-4）。
- **variant_listings/* 是 ours**（W-5），本尊 ship 正式 topic 時要對齊。
