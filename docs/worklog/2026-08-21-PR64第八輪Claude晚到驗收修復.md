# PR #64 第八輪 Claude 晚到驗收修復

## 已完成的工作 (Done)

- 鐵律 15.2 在 local commit `78260412efe56d701bbae7dcab666ce70491ebd4` 尚未 push 前抓到
  Claude comment `5361414731` 與 wrapper `5361416695`；判詞審查的 remote exact head 仍是
  `8902f8a0c0e45329915ac279eaf2a981555a56a2`。依 15.4，原 push 流程立即作廢並回到修復起點。
- 依鐵律 16 以 `git log -p` 追第五輪 validator 與 P-8 終態沿革；第六輪兩側 pattern 不同時，
  非固定日期的新檔會造成 loud mismatch，第七輪把兩側統一到固定 `2026-08-21-` 後才引入
  「兩側同時濾掉」的 silent pass。
- failure-first 在未改 validator 前，以 HEAD diff 加入一個**尚未建立的假想路徑**
  `docs/worklog/2026-08-22-PR64跨日驗收修復.md` 的內存 mutation，實跑輸出
  `OLD_OFF_DATE_SURVIVED=docs/worklog/2026-08-22-PR64跨日驗收修復.md` 與
  `FILTERED_COUNT=8`，證明舊 pattern 沒有攝取該輸入。
- 新現行 block 使用任意 ISO 日期前綴，不再把 PR64 worklog 綁死於單日；diff 與 A.1 仍共用
  同一個 pattern。新增 off-date missing A.1 mutation，先斷言合成 diff 確實改變 expected，再要求
  validator 精確命中 `set mismatch`，避免 mutation 在進 validator 前被濾掉。
- P-8 終態入口同步到第八輪 heading、任意日期 pattern 與跨日期 mutation。Claude 的 ⚪1／⚪2
  依根因合併登記到 91 §3.6；⚪3 已在既有「PR 內 commit main-only clone」條目追加本輪來源，
  沒有改動其歷史實物。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | head `db555ef…` 的 Claude `5360974435` 已要求集合與 mutation 斷根；head `8902f8a…` 的 Claude `5361414731` 證明第七輪把兩側統一後仍可讓跨日期漏列 silent pass |
| 既有防線失效原因 | 前輪只驗 duplicate／drop／missing-section 三種 A.1 mutation，沒有讓新 producer 路徑先通過 filename selector；共用 selector 被錯誤收窄時，兩側一致地看不見輸入 |
| 被漏掉的固定步驟 | 鐵律 20.2.5 要求對 producer selection 本身做違規方向反證；固定步驟是先斷言 mutation 改變 expected，再驗缺少 consumer 會 fail，而非只在已選集合內改 A.1 |
| 可重跑反向複驗 | commit 後執行第五輪 worklog 的「現行可重跑反向複驗（2026-08-21 第八輪更正）」；off-date mutation 必須先通過 `did not alter expected` canary，繼而輸出 `off-date-missing: set mismatch` |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 證據／當前 head／驗收攝取 | 15.2 四入口全量重拉，晚到判詞綁 comment／wrapper 與 remote head | `5361414731`／`5361416695` 晚於上一輪 cutoff；未把舊 pending 當通過 |
| ② 生產者／消費者／終態／歷史同步 | 同步 producer selector、A.1 consumer selector、P-8 終態、91 §3／A.1 與本 worklog | 兩側同一任意日期 pattern；舊第七輪 block 保留並相鄰標成非現行 |
| ③ 易腐數字／完成性聲明 | failure-first 數字只綁本輪 old-HEAD 實跑；終態不手抄集合總數 | `FILTERED_COUNT=8` 只作 `7826041` 前的失敗快照；新 block 以 HEAD 動態輸出 |
| ④ 狀態空間／競態窗口 | 分開 remote `8902f8a…`、未 push local commit、晚到 Claude 與下一個新 head | 晚到意見使原 15.1／15.2 作廢；修後重新跑全套而不沿用舊驗收 |
| ⑤ fail-open／happy path／自我證明 | 正常、duplicate、drop、missing-section、off-date missing A.1 都走同一 validator | off-date 合成輸入先改變 expected；若 selector 又收窄，canary 先紅，不會假通過 |
| ⑥ workflow 本機假綠／字面塊 | 不適用：本輪不改 workflow；新 head 仍須確認 Claude 非 validation skip | `git diff --name-only 7826041..HEAD -- .github/workflows` 提交後須為空 |
| ⑦ Markdown／Windows 工具鏈 | 新 block 使用 bash fence 與 UTF-8 Ruby；中文路徑的 git diff 明示 `core.quotepath=false` | Git Bash 實跑、`git diff --check`、未跳脫直線計數與 GitHub `/markdown` render 均須通過 |

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-21-PR64第五輪雙驗收修復.md`：保留第七輪歷史 block，新增任意日期
  selector、可注入 expected 的 validator 與 off-date 承重 mutation。
- `docs/worklog/2026-08-20-P8證據來源與合併後文件債收斂.md`：現行入口改指第八輪 block，終態
  pattern 去固定日期，並明列跨日期漏列 mutation。
- `docs/specs/91-pit-register.md`：登記兩條新 ⚪ 根因、為既有 ⚪3 補來源，並同步 A.1。
- `docs/worklog/2026-08-21-PR64第八輪Claude晚到驗收修復.md`：保存晚到時序、failure-first、
  20.4／20.3、射程與後續條件。

## 尚未完成或需注意的風險 (Pending / TODO)

1. repo 最後一個 edit 後仍須跑 Ruby block 語法／worktree 等價 mutation、GitHub Markdown、
   doc-claims 與完整 Windows production-equivalent 29 閘門；任一非零都不得 commit。
2. commit 後須在 clean HEAD 實跑正常與四個 mutation、post-commit doc-claims、鐵律 15，然後才可
   push branch／tag。local `7826041` 與 remote `8902f8a…` 之間的中間狀態不得發布完成性結論。
3. 新 head 仍須取得 CI、Codex、Claude 雙零後再 resolve threads；#64 不命中 18.3，四條件與
   evaluator `1111` 全齊才可代行 squash merge。
