# 2026-08-28 — S7 狀態機交互收口（批量狀態動作＋排程 banner）

🟢 不觸 18.3。實測＝`docs/research/82` §19；裁定＝D73。
🔴 **PR-C 經查已完成**（#153/#155，2026-08-27）——本輪指示的三包中它零工作量，
證據：`app/services/publications/scheduled_publication_consumer.rb`＋backfill＋spec＋
`docs/dev/m2-publication-scheduling.md` 全在，consumers.rb 註冊行在 main。

## 已完成的工作 (Done)

- **實測（82 §19）**：選取列 `☑ N selected ˅｜Bulk edit｜Set as draft｜⋯`；
  🔴 頂層鈕依選集動態；⋯ 完整值域 12 項（逐字記錄）；`Bulk edit` 是獨立編輯器頁
  （真實 URL 已取得）。全程唯讀＋勾選，量畢還原。
- **批量狀態動作**：IndexTable 加 `selectionResetSignal`；ProductsPage 批量列
  （動態頂層鈕＋溢出取消刊登／封存＋確認框），逐筆 `productSet {id, status}`
  （缺席＝保持現值——save_product.rb 既有契約；status 變更走 StatusTransition
  ⇒ 自動投 PR-C 的內部事件，兩包在此接上）。
- **排程 banner**：`SchedulePopover.notice`（role=status、控件不禁用——本尊 F1-② 形態）；
  `PublishingModal` 依 `status ∉ {ACTIVE, UNLISTED}` 傳文案（與 D53 PURCHASABLE 對齊）；
  Collection 呼叫端不傳（無 status，D53 本規則不適用）。
- **測試 4 格**全綠；i18n 十鍵 ×5 語系。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `docs/research/82-admin-channels.md` | §19（四小節） |
| `app/frontend/admin/components/IndexTable.tsx` | `selectionResetSignal` prop |
| `app/frontend/admin/components/SchedulePopover.tsx` | `notice` prop ＋ 渲染 |
| `app/frontend/admin/pages/ProductsPage.tsx` | 批量列＋`applyBulkStatus`＋確認框 |
| `app/frontend/admin/pages/ProductDetailPage.tsx` | `ChannelScheduleButton`／`PublishingModal` 傳遞 `notice` |
| `app/frontend/admin/pages/*.test.tsx` ×2 | +4 格 |
| `app/assets/stylesheets/admin.css` | `.cl-sched__notice`／`.cl-bulkbar*` |
| `app/frontend/admin/i18n/messages/*.json` ×5 | 十鍵 |
| `docs/DECISIONS.md`／`docs/specs/91-pit-register.md` | D73／§3.44 |

## 20.2 適用類型／固定處理／反向複驗輸出

| 類型 | 適用 | 固定處理 | 反向複驗 |
|---|---|---|---|
| ① 證據 | 是 | 值域逐字記錄；未取得處標 V（混合選集規則、批量確認框、payload）；PR-C 完成度用檔案存在性驗證不憑記憶 | 82 §19.3；git log 錨 |
| ② 同步 | 是 | banner 判準與 D53 PURCHASABLE 同一集合；批量走既有 productSet 契約不另立寫入路徑 | — |
| ④ 狀態空間 | 是 | 選集三態（全 A／全非 A／混合）各有規則並標 ours；banner 兩態都有測試 | 測試 4 格 |
| ⑤ fail-open | 是 | 「確認前零 mutation」斷言配「確認後恰 N 筆」正向對照；清空選取驗工具列消失 | 全綠 |
| ⑦ 工具鏈 | 是 | 🔴 本輪踩「空字串錨＋`\n`」把測試塊插進檔頭 import 後——空錨等於第一個換行。固定處理＝插入一律用**非空整行錨**或行號 splice，插完 `head` 驗檔頭 | 已復原並驗證 |

## 尚未完成或需注意的風險 (Pending / TODO)

- 混合選集的本尊規則未取得（我方 ours 規則可被實測推翻，`91` §3.44 W-3）。
- 批量動作逐筆送、無進度指示——50 筆內可接受；更大選集要批次 API（未有）。
- `Suspended`／Duplicate 繼承／溢出九項＋Bulk edit 頁＝登記（W-1/W-2/W-4）。
