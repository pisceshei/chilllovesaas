# 2026-09-01 G2 步 13b：render 變數形＋巢狀 blocks＋色階群組＋快取版本維

## 已完成的工作 (Done)

- `{% render <var> %}` 變數形（RenderTag 子類；quoted 形走原生 super）——
  Ella `_editorial_list` 的 parse fatal 收口（97 §2 實測 FATAL→OK 雙證）。
- `block.blocks` 巢狀 children（兩個構造點：render_block＋ordered_block_drops
  遞迴，官方 ≤8 層 cap）。
- 色階群組 drop 化（97 §3）：ColorSchemeGroupDrop（迭代＝official 範例形）＋
  ColorSchemeDrop（to_s=id）＋SettingsDrop coerce（group 存整 def；color_scheme
  型解引用回 scheme 物件）＋runtime schemes_drop 三處 threading。
- 🔴 頁快取 BOOT_STAMP 版本維（13a 生產實錘：部署後首頁仍吐舊 stub @font-face
  ——key 無代碼版本維、TTL 1 天內跨部署舊頁；boot 時戳每次重啟自然換 key）。
- 突變 6/6 殺（MB1/MB2/MB2b/MC1/MC2/MK1）＋canary；全套 1857 例綠。

## 修改的檔案與核心邏輯 (Changes)

- tags.rb：RenderTag（bare-var bypass＋BlockDrop 派發）＋註冊覆蓋 render。
- drops.rb：ColorSchemeDrop/ColorSchemeGroupDrop 新增；SettingsDrop schemes:
  ＋coerce 兩分支；BlockDrop children:/blocks；SectionDrop schemes:。
- runtime.rb：extract_types 保留 group 整 def；schemes_drop；三構造點 threading；
  ordered_block_drops 遞迴＋depth cap。
- page_cache.rb：BOOT_STAMP 進 key。
- fixture：_parent/_leaf blocks＋blocks-demo/blocks-iter sections＋schema/data
  色階兩 scheme；spec 5 例。

## 尚未完成或需注意的風險 (Pending / TODO)

- 突變教訓（MB2 兩輪）：`{% render block %}` 會經 render_block **重建** children
  ⇒ 純渲染路徑殺不掉 ordered_block_drops 的 children 退化——要 drop 面直讀
  （`section.blocks.first.blocks.size`）才封住（recomputed-value masking，
  equality-trap 家族新形）。
- 91 §3.63 ⚪：content_for 'blocks' 在 block 內（官方巢狀正典形）已由 frame 機制
  覆蓋但無專測；色階 role 欄（editor 預覽用）未實作；BOOT_STAMP＝重啟即清倉。
- 生產煙測待部署（Ella editorial/lookbook 段落從缺席轉渲染）。
