# 2026-09-03 主題編輯器 E5：區段／區塊 picker 依本尊重做（D79）

## 已完成的工作 (Done)

- 依 `docs/research/100-theme-editor-live-teardown.md` §4 與 §8.1 1:1 量測，把「Add section」／「Add block」改成本尊兩欄
  popover：貼左欄右緣、與該列同高（`Popover` 新 `placement: right-start`）；左欄＝搜尋框（focus 藍框）→ Sections｜Apps
  分段 → Generate（登記形、不可用）→ 該群組／模板可用的扁平清單（(n/limit) 灰化）→ 依 `category` 的分類收合區；右欄＝
  預覽（一律 "No preview available"）；Apps 分頁空態句。
- 左樹：Add section 列／右鍵 before-after 帶錨點開 picker；picker 開著時目標位置出插入線（藍 2px＋⊕，`data-insert`）；
  Add block 列改開 block picker（分類＝block schema `category`，本地 def 無分類時退回同型 theme block 的分類）。
- 後端 catalog 改列**每個 preset**（名稱＝preset name 翻譯、category 翻譯、presetIndex、blocks／block_order 原樣）；
  `block_defs_for`／`theme_block_defs` 帶 `category`。
- 新增 section 時 preset blocks 兩形實例化：array（逐項生 id `{type}_{6 碼 base62}`、巢狀遞迴、static 不進 block_order）／
  hash（原鍵為 id、無 block_order 依鍵序排除 static）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `app/frontend/admin/editor/SectionPicker.tsx` | 新增：兩欄 picker（搜尋／分段／Generate／扁平＋分類收合／預覽／Apps 空態） |
| `app/frontend/admin/components/Popover.tsx` | `placement` prop（`right-start`） |
| `app/frontend/admin/editor/SectionsTree.tsx` | `onAddSection(band, atIndex, anchor)`／`onOpenBlockPicker`／`insertAt`＋`data-insert`；移除 E3 inline add list |
| `app/frontend/admin/pages/ThemeEditorPage.tsx` | `pickerFor`／`blockPicker` 帶錨點；`pickerItems`／`blockPickerItems`；`instantiatePresetBlocks`／`newPresetBlockId`；掛兩個 `SectionPicker` |
| `app/frontend/admin/pages/ThemeEditorPage.test.tsx` | ED7／ED12／ED23／ED35 隨 picker 改；新增 ED52–ED54 |
| `app/frontend/admin/i18n/messages/{en,zh-Hant,zh-Hans,ja,fr}.json` | picker 新鍵 |
| `app/assets/stylesheets/admin.css` | `.cl-picker__*`、`.cl-tree__item[data-insert]` |
| `app/graphql/types/theme_type.rb` | `section_catalog` 每 preset 一項；`category` 翻譯（section preset／block def／theme block） |
| `spec/fixtures/theme_engine/minimal-1.0/sections/promo.liquid`、`locales/en.default.schema.json` | promo 第二個 preset（name／category／array blocks）＋翻譯鍵 |
| `spec/requests/theme_editor_bootstrap_spec.rb` | E18 |
| `docs/dev/e5-theme-editor-pickers.md` | 功能文檔 |

閘門（本機）：`pnpm -s typecheck`／`pnpm -s test`／`pnpm -s build`／`bin/rubocop`（全量）／`bin/brakeman --no-pager
--exit-on-warn --exit-on-error`／`ruby scripts/check-tokens-sync.rb`／`bundle exec rspec`（全套，fixture 有改）；
`ruby scripts/check-doc-claims.rb --base origin/main --require-base` 於文檔提交後跑（結果見 handoff §①）。

突變輪（先 commit 基線、跑完 `git diff HEAD` 為空；每格＝改一處生產碼 → 跑對應測試 → 記轉紅 → 還原）：

| 突變 | 改動 | 轉紅 |
|---|---|---|
| M36 | picker 不分類（全部扁平） | ED52 |
| M37 | picker 搜尋忽略 query | ED23 |
| M38 | preset 陣列：static 進 block_order | ED53 |
| M39 | preset block id 無 base62 尾碼 | ED53 |
| M40 | 後端只列第一個 preset | E18 |
| M41 | 後端 category 不翻譯 | E18 |
| M42 | 插入線不標 | ED54 |
| M43 | block picker 不退回 theme block category | ED54 |
| M44 | 選第二個 preset 用錯 settings（一律第一個） | ED53 |

## 尚未完成或需注意的風險 (Pending / TODO)

- 右欄預覽一律 "No preview available"（本尊多數也是；縮圖預覽需 CSS 語境，另包評估）。
- 分類排序本尊未取得（字母序、無分類殿後）；Apps 分頁內容與 "Recommended apps" 未做（無 app 層）。
- 預覽「+」插入點開 picker＝E6；資源 picker（E4b）可沿用兩欄形。
- block id 形取 25 §5 觀察形，本尊逐字未取得。
- 本機目視驗證未做（admin 登入需密碼）；合併部署後在 demo 店以截圖複驗 popover 位置與兩欄寬。
- 一次操作事故：用 `git checkout --` 還原測試檔的暫時 debug 行時，把尚未 commit 的 ED52–ED54 一起沖掉（同
  `mutation-revert-trap`），已重套腳本補回；教訓＝暫時 debug 改動要用獨立檔或先 commit。
