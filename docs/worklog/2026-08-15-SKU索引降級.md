# 工作記錄：SKU 唯一索引降級（M1 PR-1 第 1 步，2026-08-15）

> 規格出處：`docs/research/61` §1.5（help 原文）｜`docs/research/63` §B.6／§L-1
> ｜`config/limits.yml:2497-2510`（`catalog_flow` 區塊）

## 已完成的工作 (Done)

把 `product_variants.sku` 從**硬唯一**降級為**軟唯一**——這是全 PR-1 唯一一項
「規格與實作**已經**互相矛盾」的，修它不需要任何新裁定。

`limits.yml:2504` 早在建表前就宣告 `sku_unique_per_shop: false`（出處 61 §1.5：
本尊偵測到重複 SKU 顯示警告但**不阻擋**），而 M0 建表時
（`20260811000000` 第 272 行 `tenant_index :product_variants, :sku, unique: true`）
建成了唯一索引。矛盾的那一側是 DB，而 DB 是會擋住商家的那一側。

🔴 **唯一索引具體擋掉兩種官方允許的情境**：①同款不同包裝共用 SKU；
②CSV 批次匯入（本尊是警告後照樣寫入，唯一索引會讓整批在中途 `RecordNotUnique` 失敗）。
兩者都是「商家做了官方允許的事，我方回失敗」，方向與鐵律 12 相反。

### 同批修 `docs/specs/11`（63 §L-1 要求）

11 §2 第 1 點與 §3 第 3 點都把 SKU 當成「業務唯一性用唯一索引兜底」的例子。
**改了 DB 卻不改 11，倉庫裡會同時存在兩份互相矛盾的生產級規格**，
而 M0 建表時就是照那個例子建的——例子不改，下一個人會再建一次。
兩處都留了逐字引用原文的防回退批註，並補上「軟唯一不走唯一索引」的判準。
順帶把原文的錯字「討code」改為「折扣碼」。

### 🔴 負面驗證：綠不等於有效

新增的 9 條 spec 全綠之後，我把索引**還原成唯一**再跑一次——
**5/9 紅**（含併發那條）。沒有這一步，這些測試可能只是恰好通過。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260815000000_relax_product_variant_sku_index.rb`（新增）：
  `uq_product_variants_sku` → `ix_product_variants_sku`。
  🔴 **索引名一起換**（`uq_` → `ix_`）：本倉庫的前綴是語義的一部分，
  留著 `uq_` 會讓之後讀 schema.rb 的人以為它還是唯一索引——那正是本次矛盾的來源形態。
- `db/schema.rb`：version → `2026_08_15_000000`，`product_variants` 的 sku 索引改非唯一。
- `docs/specs/11-production-baseline.md`：§2 第 1 點、§3 第 3 點（各附防回退批註）。
- `spec/factories/product_variants.rb`（新增）：`shop { product.shop }` 刻意**不**用 association
  ——各自 association 會生出兩間店的變體（跨租戶髒資料），而 `acts_as_tenant` 不驗證這件事。
- `spec/models/product_variant_spec.rb`（新增，9 條）：軟唯一 ×3、租戶隔離、索引形狀 ×3、
  真併發 ×2。
- `spec/migrations/pr1_schema_alignment_spec.rb`（新增，8 條）：對**原始碼**的不變量斷言，
  與 model spec 對**執行期 DB** 的斷言互補。

### 🔴 併發測試的取捨（寫下來免得被當成偷懶）

- **SKU 重複寫入不用執行緒**：唯一索引會在第二筆 INSERT 就 raise，與併發無關。
  順序寫入寫得進去就證明索引不在了。為併發而併發會寫出一條看起來嚴謹、
  實際上不多測任何東西的測試。
- **`handle` 併發衝突非用執行緒不可**：`Product` 有 model 層的
  `validates :handle, uniqueness:`，順序寫入時 validation 先擋，**唯一索引沒機會發言**。
  只有兩條連線同時通過 validation 再同時 INSERT，才逼得出 `uq_products_handle`。
  降級 SKU 索引時最容易的誤操作就是連帶動到別的索引，而那只會在併發下顯形。
- `self.use_transactional_tests = false`：預設的 transactional fixtures 讓另一條連線
  看不到本 example 的資料 ⇒ 兩個執行緒各寫各的、兩邊都成功、測試永遠綠。那會是假併發。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **降級之後重複 SKU 的提示沒有地方回**。63 §B.6 要求走 payload 的
  `warnings{field,message,code}`（不是 `userErrors`——後者代表操作失敗，鐵律 4），
  而 28 §0.3 的 payload 只有 `{resource, userErrors}`，**`warnings` 慣例至今不存在**
  （63 §L-9 已登記）。在第一支變體 mutation 落地前，重複 SKU 是**靜默允許**的。
- 🔴 **sku 雙寫未解**：`sku` 同時在 `product_variants` 與 `inventory_items` 兩張表，
  而 63 §B.6 判定權威表是 `inventory_items`（`limits.yml:2499` `sku_owner: inventory_item`）。
  這個偏差在本次之前就存在，**刻意不順手加同步 callback**——那會把結構問題用隱形副作用蓋住。
  登記為已知偏差，免得下一輪稽核當成本輪引入。
- **`limits.yml` 的 `sku_recommended_max_chars: 16` 沒有實作驗證**——這是對的
  （官方措辭是「建議」不是上限），但 UI 提示也還沒做，登記在此。
- **V-91 未裁定**：要不要提供「SKU 必須唯一」設定項以服務有 WMS 整合的商家（比官方嚴）。
  `limits.yml:2510` 已登記需使用者裁定，本次不動。
- **`pr1_schema_alignment_spec` 的白名單**目前 3 個路徑，每個都寫了理由。
  它掃的是**字串出現**不是**索引建立**——所以任何新檔案只要提到那個索引名就會紅。
  這是刻意的（寧可誤擋也不要漏放行），但改動白名單時要逐項寫理由，
  不得寫成「測試檔一律排除」。
