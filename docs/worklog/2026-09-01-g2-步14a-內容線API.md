# 2026-09-01 G2 步 14a：內容線資料層＋Admin API

## 已完成的工作 (Done)

- 三源開場：官方研究（agent——blog 14 屬性/article 21 屬性/CommentPolicy 三值/
  MenuItemType 13 值/menuUpdate 整棵替換語義）＋admin 親點（建 S14 Probe Post／
  blog 三 radio 逐字／menus 三預設選單＋link picker 值域窮舉／Pages 位置）＋
  真店實測（複合 URL 200／comment form 契約 DOM 抓取——CAPTCHA 紅線前止步）
  ⇒ `docs/research/98-content-line-teardown.md`。
- blogs/articles/article_comments 三表＋pages.template_suffix；三 model；
  CRUD 12 mutations＋queries（keyset updated_at desc）＋GID 解析四型註冊；
  Content::SaveMenu（整棵替換＋≤3 層＋型別驗證）；MenuItem 型別擴充。
- 突變 9/9 殺（MC2 首輪偏靶——P2 缺 published→hidden 反向，補格後 MC2b 紅）；
  全套 1866 例綠。

## 修改的檔案與核心邏輯 (Changes)

- 新：migration ×2、app/models/{blog,article,article_comment}.rb、
  app/services/content/save_menu.rb、app/graphql/mutations/{base_content_mutation
  ＋12 支}、types ×13（PageType/BlogType/ArticleType/MenuType/MenuItemType/
  兩 enum/錯誤型/連接型 ×4/MenuItemInput）、spec（9 例）。
- 改：query_type（6 fields＋resolvers）、mutation_type（12 註冊）、
  chilllove_schema（RESOLVABLE_TYPES＋resolve_type 四型）、menu/menu_item model。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.64 登記（image/metafields/留言管理 API/三個 MenuItemType 值/feed）。
- 14b（admin 三頁）＋14c（前台 drops/模板/tagged/留言 POST）接續；生產部署
  隨 14c 一起煙測（純 API 層部署後無可見面）。
- ⚠ repo 契約：新 mutation 必呼叫 enforce_idempotency_contract!；抽象底座檔名
  必須 base_* 開頭（mutation_idempotency_call_spec 全域掃描——首輪紅過）。
