# PR #61：Rails 冷啟動閘門復驗

> 對應規格：`CLAUDE.md` 鐵律 15.4／17.2／19／21 ｜ commit：本提交

## 已完成的工作 (Done)

- 執行 PR #61 本輪完整 29 支 production 等價閘門；首跑 28 支通過，Rails 測試為
  `284 examples, 1 failure`，唯一失敗是 `spec/system/m0_admin_shell_spec.rb:18` 預期
  `/admin/products`、當下仍讀到 `/login`。
- 核對同次失敗截圖與 `log/test.log`：截圖已是 CHILL LOVE 商品空狀態；日誌顯示登入建立
  session、302 到 `/admin`、`GET /admin` 回 200、Vite 首次建置與 layout render 約 3.58 秒，
  GraphQL 再回 200。這組證據排除「登入失敗」的表面解讀，定位為冷啟動等待競態。
- 核對程式路徑：`app/frontend/admin/App.tsx` 由 React `Navigate` 把 `/admin` 導到
  `/admin/products`；全倉沒有 `Capybara.default_max_wait_time` 設定。依鐵律 17.2，本包不改未被
  點名的 system spec 或測試基建，只在 `docs/specs/91-pit-register.md` §3.5 登記後續獨立包。
- 首次定向復驗在受限程序中於 boot 階段出現 Zeitwerk error、`0 examples`。A/B 核對證明同一
  gem 絕對路徑在受限程序內 `File.exist?` 為 true、`Dir.glob` 卻回空；移至獲准的本機外層後，
  glob 可列出、Rails 8.1.3.1 boot、MySQL `SELECT 1` 與該 spec `1 example, 0 failures` 全通過。
  因此沒有把沙箱假紅歸因到產品碼，也沒有把 `0 examples` 當測試結果。
- 回到可正確枚舉 gems 且可連 MySQL 的本機層後，第一次完整復跑 29/29 通過：Rails
  `284 examples, 0 failures`、前端 `5 passed`、prototype `ERROR 0 / WARN 136`、doc-claims
  掃描 242 檔且警告 0；其餘安全、契約、對等性與 workflow 規則閘門均 exit 0。
- 初始 `mysqladmin ping` 未帶專案憑證而回 `Access denied for user 'ODBC'`，不能當成服務停止；
  同輪 `mysqld` 行程存在，且 Rails 以應用設定連線執行 `SELECT 1` 回 1。後續以應用實際連線作
  本包的資料庫活性證據，不用無憑證探測取代它。

## 修改的檔案與核心邏輯 (Changes)

- `docs/specs/91-pit-register.md`：保存首跑數字、截圖／日誌反證、冷建置耗時、客戶端 redirect 與
  射程邊界，避免後續把暖快取偶然通過誤報為斷根。
- 本 worklog 與 `docs/handoff/2026-08-20-PR61-Rails冷啟動閘門復驗.md`：把「問題、證據、做了
  什麼、需注意什麼」獨立落庫；這是驗收流程中的具名步驟，不併吞進較早的 review 修復記錄。
- `docs/handoff/2026-08-20-階段一開場包.md`：將本步的 worklog／handoff 加入終態入口，讓下一位
  不必從 console 殘影重建診斷。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本文件定稿時的證據快照：定向 system spec 為 `1 example, 0 failures`，第一次完整復跑為
  29/29；本次結果回填改變了受驗文檔，因此原子收尾仍須再從頭跑完整 29 閘門。依鐵律 21.3，
  該收尾結果不再反向改寫本文件；由 commit 後輸出及綁新 head 的 PR remote handoff 承接。
- 真正斷根需要另立明確射程，選擇穩健等待或測前建置後同時驗冷、暖路徑；本 PR 未獲授權修改
  `spec/system/m0_admin_shell_spec.rb`，不得順手修。
- 本機 Node v24.19.0 仍超出 `package.json` 宣告的 `>=22.12 <23`；即使前端閘門通過，也只能記為
  此環境實跑結果，不能宣稱 Node 24 已受支援。
- Rails gate 若在受限程序內跑，外部 gem 的絕對 glob 會造成 Zeitwerk `0 examples` 假紅；後續須
  先跑 glob canary，並保留「受限失敗／本機外層通過」的 A/B 證據。
