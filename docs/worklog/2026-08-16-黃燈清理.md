# Worklog — 合併批黃燈清理（2026-08-16）

PR #42／#41／#38 合併時各自判詞留下的未擋合併 🟡，湊成一個清理 PR 一次處理。
逐項出處＝各 PR 最後一輪驗收留言。

## Done

- 🔴 `check-workflow-syntax.rb` 同族崩潰第四＋第五入口（PR #42 第 6 輪 🟡，
  驗收方因沙箱未能復現、本輪**先實測復現再修**）：
  ①`defaults:` 是 Hash 但 `defaults.run:` 掛字串 ⇒ `dig("run","shell")` 對 String
  丟 TypeError——防護改成**每層都驗** `is_a?(Hash)`；
  ②step 的 `run:` 掛 sequence ⇒ `script.gsub` 對 Array NoMethodError——
  照該輪建議 `next unless script.is_a?(String)`（少檢不是崩潰）。
  fixture `wf_bad_run_defaults` 一份蓋兩形態；兩個突變（dig 退化／拿掉 String 跳過）
  各自實測**被抓**。
- `test-doc-claims-rules.rb` `MIN_CASES` 8→9（PR #42 第 6 輪 🟡：CASES 已九條而
  下限未動，canary 鬆了一格）。
- `check-doc-claims.rb` 逐檔 diff 失敗的執行期 canary（PR #42 第 6 輪 🟡）：
  name-only diff 成功、逐檔 `-U0` 才失敗 ⇒ 原本 `.to_s` 吞成空集合靜默跳過，
  現改 fail-closed exit 2；摘要行補印新增行總數（「共 N 個新增行」）。
- PR #41 worklog:228 快照註記插在 GFM 表格中間會切斷表格（PR #41 複驗輪 🟡）：
  刪註記行、照同表 docs/dev 列的既有正解把快照寫進儲存格。
- PR #41 handoff §④ 補「閘門必須是 commit 前最後一步且親眼看退出碼」教訓與
  ci.yml 衝突解法——先前只活在 commit message（PR #41 複驗輪 🟡）。
- `ci.yml` parity 註釋段一行掉到第 0 欄，補回六格縮排（PR #41 兩輪連提的格式 🟡）。

## Changes

| 檔案 | 改動 |
|---|---|
| `scripts/check-workflow-syntax.rb` | defaults.run 逐層 Hash 防護；非字串 run 跳過 |
| `spec/fixtures/ci_violations/wf_bad_run_defaults/` | **新增**（兩形態合一份） |
| `scripts/test-workflow-syntax-rules.rb` | 加 `wf_bad_run_defaults` case |
| `scripts/test-doc-claims-rules.rb` | `MIN_CASES` 8→9 |
| `scripts/check-doc-claims.rb` | 逐檔 diff 失敗 exit 2；摘要印新增行總數 |
| `docs/worklog/2026-08-15-執行位元閘門抽出與補洞.md` | 表格內快照取代切表註記 |
| `docs/handoff/2026-08-15-執行位元閘門抽出與補洞.md` | §④ 補教訓與衝突解法 |
| `.github/workflows/ci.yml` | 註釋縮排 |

## Pending / TODO

- PR #38 早輪的代碼層 🟡（`effective_option_value_pairs` 記憶化等）**刻意不在本 PR**：
  各自帶有已登記的取捨理由，屬功能線後續，不是清理項。
- 「第 N+1 輪」字面標題（PR #41 複驗輪 🟡）屬歷史層不回改；本條規範已吸收：
  往後輪次標題寫具體輪次或日期。
