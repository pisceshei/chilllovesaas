# 2026-08-21 — PR #65 首輪 Codex／Claude 驗收修復

## 已完成的工作 (Done)

- 審查基準固定為 PR #65 head `28602e50c62802b681527dbc939e221a4805b911`。鐵律 15.2 三端點
  全量重取：Codex review `4985224623` 有 inline `3823546970`／`3823546974`／
  `3823546979`／`3823546989`／`3823546997`；Claude issue comment `5359074865`；
  Claude run `32393172652` 完成且 log 沒有 validation skip。
- 依 `git log -p` 與來源列語義逐項證實：C-16 是正面對照、71-R3-DOC1 含未結案標記、
  P0-14／P0-18 仍部分修復、§6／§9 有成對重述。修法因此分別採語義分類、狀態欄標記、
  最後一欄未結案標記計數，以及「180 只代表原始列次」的窄化宣稱。
- R6 補齊假命令、零標頭、標頭前計數、位數／階層／括注畸形與生產索引零供給的 fail-closed
  分支。舊 checker 對畸形標頭與標頭前計數 fixture 均錯誤 exit 0；兩個針對性 mutation
  （只判 `recheck:` 存在、停用零標頭分支）也均錯誤 exit 0。修後
  `ruby scripts/test-doc-claims-rules.rb` 通過 16 個 fixture、3 個 git 情境與 1 個供給情境。
- `docs/dev/m0-review-convergence.md` 同步 R6 規則表、納管範圍、fixture／突變清單與已知限制；
  `docs/specs/92-claim-index.md` 的 CLAIM-003／005 改為值漂移即 `abort`。
- 本 worklog 寫入後從 setup 起跑 Windows production 等價完整閘門，首輪 29／29 全綠：
  Ruby 162 檔零違規、Brakeman 0 errors／0 warnings、Rails 284 examples／0 failures、前端
  5 tests／0 failures、原型 lint `ERROR 0 / WARN 136`、doc-claims warning 0、R6 回歸為
  16 fixture＋3 git＋1 supply 全綠，workflow syntax 解析 2 份 YAML／33 個 run 區塊。
  複驗入口：`ruby scripts/test-doc-claims-rules.rb`；完整閘門總數由
  `Select-String '^\s*step ' config/ci.rb` 的現值扣除 setup 一項、再加本機拆出的四個 setup 動作導出。

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 證據／當前 head／驗收攝取 | 釘 head 後全量拉三端點、讀 review body／inline 與 run log | 上列 review／comment／run ID 均屬 head `28602e50c6`；Claude run completed success，無 skip 字樣 |
| ② 生產者與消費者同步 | 以 R6／`docs/specs/92-*` 識別字掃 checker、測試、docs/dev、總方案與坑簿 | `docs/dev` 已同時具 R6 規則、納管、fixture、突變與窄例外；`ruby scripts/check-doc-claims.rb` 掃到 1 份索引且 warning 0 |
| ③ 易腐計數與完成聲明 | 數字改為語義分類並在同位置附可失敗斷言 | CLAIM-001＝C rows 16／conflicts 15／positive 1／T 10／table 25／total 313；CLAIM-002＝rows 10／fixed 8／partial P0-14、P0-18；CLAIM-003＝77；CLAIM-006＝row occurrences 180、distinct 不宣稱 |
| ⑤ fail-open／happy path／自證閘門 | 正常、違規、輸入缺失、零掃描與 mutation 都要能令 suite 辨識 | 舊／突變 checker 的四種洞均重現 exit 0；修後 16 fixture＋3 git＋1 supply 全綠，生產 doc-claims exit 0、warning 0 |
| ⑦ Markdown／Windows 假結果 | UTF-8 執行、只掃本輪 hunk 的表格裸直線，並走 GitHub `/markdown` 實際渲染 | 2026-08-21 快照：九個修改區塊 API 全成功；本 worklog 為 H2=3、tr=16、th=5、td=33，另八段均有預期標籤且無 API 失敗 |

## 變更的檔案與原因 (Changes)

| 檔案 | 原因 |
|---|---|
| `docs/specs/50-logic-gap-register.md`、`docs/specs/92-claim-index.md` | 排除 C-16 正面對照，恢復表 3＝25、全簿＝313，並讓 recheck 按語義分類 |
| `docs/specs/53-ui-gap-recheck.md` | 把 180 限定為未去重列次，列明六組成對重述 |
| `docs/specs/83-admin-1to1-audit-round3.md` | 十筆 P0 列加明示狀態，完成數改為 8、部分數改為 2 |
| `docs/specs/84-m1-gate-triage.md` | 最後一欄只要含未結案標記即計入，現值為 77 |
| `scripts/check-doc-claims.rb` | 畸形／游離 CLAIM 與生產索引零供給改為 fail-closed |
| `scripts/test-doc-claims-rules.rb`、`spec/fixtures/ci_violations/doc_claim_*` | 補 R6 判準兩極、局部零掃描與生產供給 canary |
| `docs/dev/m0-review-convergence.md` | 同步 R6 終態規則、消費者、測試與限制 |
| `docs/specs/91-pit-register.md` | 登記 Claude 的範圍外 AGENTS 列舉缺口，並收錄本 worklog |
| `docs/worklog/2026-08-21-P2宣稱索引與R6.md` | 保留首推歷史輸出並追加具名日期更正；Changes 表同步 HEAD |

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本 worklog 已完成 GitHub Markdown 實際渲染與首輪完整 29 閘門；因本段回填發生在首輪之後，
   仍須依鐵律 15.4 從 setup 完整重跑。其後的 commit、commit 後 doc-claims、鐵律 15 已提交
   差異核對與 GitHub 新 head 驗收也仍待完成；任一未完成都不得 push／宣稱雙清。
2. 本 PR 修改 `scripts/`，命中鐵律 18.3；即使新 head 的 CI、Codex、Claude 與四條件評估全清，
   也只能通知使用者人工合併，不得由互動式 Codex 代行。
3. Claude 的 AGENTS 嚴重度列舉觀察已依 17.2 登記到 91 §3；本輪不得順手改鐵律本文。
