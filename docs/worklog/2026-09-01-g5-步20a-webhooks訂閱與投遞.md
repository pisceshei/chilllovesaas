# 2026-09-01 G5 步 20a：對外 webhooks——訂閱 CRUD＋投遞管線

## 已完成的工作 (Done)

- 資料層（specs/18 F4 逐字表設計）：webhook_subscriptions（topic/url/secret/
  status/failure_count）＋webhook_deliveries（投遞紀錄 7 天窗；FK cascade）。
- 🔴 SSRF 紅線（18 F4「頭號安全風險」）：`Webhooks::UrlGuard`——HTTPS only＋
  **resolve 層**拒私網/loopback/link-local（含 169.254.169.254 雲 metadata）/
  IPv6 ULA/未指定/multicast；字面 IP 同判準；**建立與投遞雙時點**（W2/MW-1、
  D2/MW-2 紅證）；投遞以 vetted IP 直連（Net::HTTP#ipaddr）——關 resolve→
  connect 的 rebinding 窗；client 不跟 redirect（3xx＝失敗，D3）。
- 投遞（28 §15）：七 header（X-CL-Topic/Shop-Domain/Webhook-Id/Event-Id/
  Triggered-At/API-Version/Hmac-Sha256）＋HMAC=base64(HMAC-SHA256(raw body,
  per-subscription secret))（D1/MW-3 紅證）；timeout 5s；回應存檔截 1KB
  （讀取上限 64KB——18 F4）；投遞紀錄逐筆落 webhook_deliveries。
- outbox 驅動：`Events::Consumers.for` 掛載縫——EXTERNAL topic 一律追加
  `Webhooks::FanoutConsumer`（內部 topic 永不；D5/MW-4 紅證）；每 active 訂閱
  一顆 DeliverJob（逐訂閱重試隔離）；失敗指數退避 re-enqueue（demo 3 次＝
  limits webhook.delivery_max_attempts；規格目標 8 次/4h 註記保留）；連續失敗
  達門檻 ⇒ disabled（D4/MW-5 紅證；成功歸零）。
- GraphQL：webhookSubscriptions keyset connection＋Create（topic 白名單＝
  Topics::EXTERNAL，W1b 紅證；secret **一次性回傳**、讀面無此欄——W1 以
  __type introspection 釘死）/Update（改 URL 重驗紅線；re-enable 歸零）/Delete。
- limits `webhook` 六鍵（28 §15＋18 F4 出處逐鍵註記）。
- 突變 5/5 殺（MW-1 建立時 SSRF／MW-2 投遞時再驗／MW-3 HMAC 格式／
  MW-4 topic 過濾／MW-5 門檻停用）。

## 修改的檔案與核心邏輯 (Changes)

- 新：migration（兩表）、webhook_subscription.rb、webhook_delivery.rb、
  url_guard.rb、deliver.rb、fanout_consumer.rb、deliver_job.rb、
  webhook_user_error_{code,type}.rb、webhook_subscription_{type,edge,connection}
  _type.rb、三 mutation、webhook_subscriptions_spec.rb（5 例）、
  delivery_spec.rb（5 例）。
- 改：events/consumers.rb（for 的 EXTERNAL 掛載縫）、query_type、mutation_type、
  limits.yml。

## 尚未完成或需注意的風險 (Pending / TODO)

- ⚪ 停用時通知商家（18 F4「disabled + 通知商家」後半）；⚪ per-host circuit
  breaker（18 F4 ⚠ 重試風暴）；⚪ webhook_deliveries 7 天 purge job（現只有
  留存欄位與索引）；⚪ admin UI（設定頁 webhooks 卡）＝20b——四項登記 91。
- 🔴 payload＝outbox event.payload 原樣（producers 已是 snake_case＋
  admin_graphql_api_id 由 producer 決定）——豐富化（28 §15「資源 snake_case
  JSON」全量形）隨資源序列化統一輪。
- 生產煙測：部署後建測試訂閱（webhook.site 類端點會外呼——**demo 紅線只限
  費用與發信，外呼 HTTP 允許**）→ 觸發事件→投遞紀錄；或 runner 直跑 Deliver
  對 httpbin。
