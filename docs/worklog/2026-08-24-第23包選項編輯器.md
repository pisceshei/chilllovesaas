# 2026-08-24 第 23 包：選項編輯器＋卡底變體表

對應規格：整合執行規格（`docs/plans/2026-08-24-第20-37包整合執行規格.md`）§4-23；
63 §B.4／§B.5 的 UI 面；排程判準「填『尺寸／S,M,L』變體表即時三列；儲存後
variant.id 保持（22 的契約在 UI 面首驗）」。

## 已完成的工作 (Done)

- **後端前置**（第 22 包登記的坑）：`sync_options!`／`sync_values!` position
  兩階段落位（刪除先行＋負區間＋reload，同 `apply_matched!` 三步）——選項交換、
  值交換、中段刪除佔位三種 1062 撞法一次消掉；新增重排回歸測試（含 title 依
  新選項序重算）。注入回歸：拔掉挪負區間 ⇒ 測試紅 ⇒ 還原綠。
- **`app/frontend/admin/lib/variantMatrix.ts`**：變體列模型（顯式清單、非笛卡兒積）。
  `rebuildRows`＝與後端階段 A 同構的投影＋「新值補列」；
  🔴 三不變量入測：稀疏格不復活（63 §B.4 規則 1）、座標塌縮時帶 id 列優先存活、
  回聲欄（compare/cost/barcode/taxable）跟列走。上限鏡射
  `MAX_PRODUCT_OPTIONS=3`／`VARIANT_ROW_CAP=250`（正典＝limits.yml）。
- **選項編輯器 UI**（排程形態：popover→自訂選項→inline→chips）：
  「＋新增選項」menu-popover（尺寸/顏色/材質/款式＋自訂）→ inline 名稱欄＋
  值 chips（TagsField 泛化復用）＋移除鈕。🔴 改名不走 rebuild（coords 位置制，
  餵進 rebuild 會被當刪＋加、整維度歸位重置）；🔴 零值選項＝UI 草稿不進列模型
  （否則「先加選項後打值」的空檔會把既有列清光）。
- **卡底變體表**：座標 title 列首＋逐列價格（必填）／SKU／建立態數量欄
  （initialQuantities，套用第一個據點）。選項模式下定價卡與庫存 pillset 轉
  per-variant note（商品級價格入口消失，Shopify 同型）。
- **送出契約**：options 樹＋逐列 variants；🔴 既有列必帶 id（改名選項時後端靠
  id-match 保座標）；回聲欄照送（宣告式缺席＝清除——缺了就是儲存一次抹掉
  比較價/成本/條碼）。`variants.N.*` 伺服錯誤逐列映射＋聚焦。
- i18n 五語 ×26 鍵（含 `common.remove`）、刪 2 死鍵（`product.variants.add`／
  `.add.pending`）；vitest 84/84（+7 lib、+3 頁面）；rspec `product_set_variants`
  6/6；typecheck 綠。
- 注入回歸（UI 層）：rebuild 全走 freshRow（id 丟失）⇒ lib 4 例＋頁面 id 保持例
  紅 ⇒ 還原綠。
- **push 前對抗式審查輪**（22 agents 四維度；17 條確認、16 條本輪修畢）：
  ①🔴 >250 變體截斷儲存＝靜默整批刪（宣告式全量＋查詢只載 250）⇒ 查詢加
  `pageInfo.hasNextPage`、命中即整頁儲存封鎖＋警示（tooMany）②🔴 刪值再加回
  ＝空白 freshRow 被後端 digest-match 後宣告式抹掉回聲欄 ⇒ **列墓場**（本編輯
  階段被剔除的列以「選項 key 序＋座標」入墓，座標重現即復活原列含 id 與回聲欄）
  ③🔴 兩選項暫時同名（改名途中）在 name-keyed Map 下 last-wins 毀列 ⇒ OptionDraft
  改帶**穩定 key**（uuid；改名不變、送出剝除），rebuild 全面 key-diff
  ④COORD_SEP 是裸 NUL 位元組、git 把整檔判 binary ⇒ 改為跳脫字面（反斜線 u0000）
  ⑤儲存成功後就地認 id（mutation 回讀 variants 以座標對回）——否則本次新建列
  之後遇選項改名（後端刪＋加）digest 對不上、變體被換新 ⑥saving 期間變體卡
  fieldset 鎖定（防途中編輯讓伺服錯誤映射錯列）＋伺服錯誤聚焦改 saving 解鎖後
  effect（disabled input 收不了 focus，實測抓到）⑦overflow 靜默丟輸入 ⇒ toast
  ⑧後端 casefold 重複驗證（ai_ci 索引下 Red/RED 撞 1062）＋ `RecordNotUnique`
  安全網（accent 級 e/é 穿過 casefold ⇒ 轉 INVALID、整體回滾，實測命中）
  ⑨釘 reload 守衛（[A,B,C]→[A,C,B] 不動索引位者必落正位）⑩改名 id-match
  端到端（UI＋後端 spec）⑪`variants.N`（無尾段）錯誤映射＋測試
  ⑫中位草稿選項不變量測試。
- 審查餘項：**digest 軸無兩階段**（`apply_matched!` 逐列改座標，若 digest 有唯一
  索引則兩列互換座標會 1062）——P22 既有、非本包引入，UI 無法表達互換（API 可）；
  登記待 `docs/specs/91-pit-register.md` §3 入籍（複驗：
  `grep -n option_values_digest db/schema.rb`）。

## 修改的檔案與核心邏輯 (Changes)

- `app/services/catalog/variant_sync.rb`：兩階段落位（見上）。
- `spec/requests/product_set_variants_spec.rb`：+重排回歸例（6 例）。
- `app/frontend/admin/lib/variantMatrix.ts`（新）＋ `.test.ts`（新，7 例）。
- `app/frontend/admin/pages/ProductDetailPage.tsx`：PRODUCT_QUERY 擴
  options＋variants(first:250)；FormValues +options/variantRows；載入映射容錯
  （`options ?? []`——舊 fixture 與過渡 payload 都安全）；validate／
  applyServerErrors／save 三處選項模式分流；TagsField 泛化
  （label/placeholder/removeLabel props）；變體卡 stub → 編輯器＋表。
- `app/frontend/admin/pages/ProductDetailPage.test.tsx`：+3 例（判準全覆蓋）。
- `app/assets/stylesheets/admin.css`：選項列＋變體表區段（全 token）。
- `app/frontend/admin/i18n/messages/*.json` ×5。

## 尚未完成或需注意的風險 (Pending / TODO)

- **整合規格 §1.7 說第 23 包的選項 popover 用 Modal 原語——實作為 menu-popover**
  （actionsmenu 同型）：選項名稱選單是錨定下拉不是模態，Modal 的 scrim／焦點鎖
  形態不符。Modal 的本批消費者實為 27（刪除確認）／28（選檔 modal）。
- 值改名（Red→Crimson）在 UI＝chips 刪＋加 ⇒ 該值的列被剔除再以新值補列
  （價格重置為 seed）——與後端宣告式語義一致；「改名保列」屬 productOption*
  後補射程（B2，第 22 包已登記）。
- 選項重排（拖曳）與值重排 UI 未做（後端已支援；29 包或 B2 一併）。
- `VARIANT_ROW_CAP` 超限＝變更靜默不套用（無 toast）——組合爆炸前 UI 早已
  難用，v1 接受；29 包變體子頁再補提示。
- 建立態數量欄只套用第一個據點（多據點逐點設定屬 29 包子頁射程）。
