# PR #61 Codex `96ffc01` 驗收修復

## 已完成的工作 (Done)

- 2026-08-20 以 GitHub API 拉取 review `4978735798`，核對其 `commit_id` 為
  `96ffc0182d324758d2dbc6b4f1332d3a8ddfd136`；再以 `pulls/61/comments --paginate` 取得
  六條新 inline：`3818337781`、`3818337787`、`3818337791`、`3818337796`、
  `3818337799`、`3818337801`。Review body 只有一般說明，實質意見全在 inline。
- 內部修法先以 `git log -p -S` 追查 workflow 外部取證規則、D31／D32 合併通道、
  鐵律 15 首次交付豁免與方案單 PR 迴圈沿革；沒有採用驗收方未核對的修法原句。
- `.github/workflows/claude-review.yml` 的 prompt 已同步鐵律 19：取不到外部原文只能記
  「未取得」並停止該語義與修法方向，搜尋結果只可作導航線索，不能再發布〔推論〕。
- 階段一'執行方案已拆開首次交付與後續修復輪、要求每個新 head 明確觸發 `@codex review`、
  增加合併前 base 更新迴圈，並明載 `--match-head-commit` 只鎖 head、不鎖 base。
- `docs/dev/m0-automation-infra.md` 與 `docs/dev/m0-review-convergence.md` 已同步 D31／D32：
  `AUTO_MERGE=false` 證明 workflow 不合併，不再被外推成所有 PR 都須使用者逐次操作。
- Cyberbiz 官方頁於 2026-08-20 直接取得兩段中文原文：訂單狀態異動即時重算，以及核心設定
  D+2 生效；但官方英文原文未取得，故兩項均降為待補證並禁止作 `specs/110` 輸入。
  來源：<https://help.cyberbiz.io/ec/members/vip/>。
- Claude workflow run `32326821682` 對同一 head 於 2026-08-20 完成，判詞 issue comment
  `5350972681` 新開兩條 🟡：第四輪 worklog 原處缺更正註，以及鐵律 19／workflow prompt
  失同步。前者已在原文後追加日期更正；後者由本單位的 workflow 實質同步處置，並已抽取
  至 `91` §3.5。該 run 結論為需修改，不得作通過證據。
- 在上述八條意見都納入 staged 候選後，本地 selector 閘門逐支實跑輸出
  `TOTAL=17 FAILED=0`；`check-doc-claims.rb` 為警告 0，prototype lint 為
  `ERROR 0 / WARN 136`。另以 YAML 解析取得 review prompt：`PROMPT_BYTES=22917`、
  `PROMPT_EXPRESSIONS=0`、舊推論規則命中 0、新「未取得」詞命中 5；附錄集合比對為
  `FILES=191 LISTED=191 MISSING=0 EXTRA=0`，方案表格結構為 `TABLE_BLOCKS=7 BAD=0`。
  以上均是 2026-08-20 的本地 staged 候選快照，不是 GitHub 下一個 head 的遠端驗收證據。

## 修改的檔案與核心邏輯 (Changes)

- `.github/workflows/claude-review.yml`：驗收 prompt 從「可標推論／可發布未查證修法」改為
  fail-closed 的「未取得／停止聲明」。
- `docs/plans/2026-08-20-階段一執行方案.md`：補可達成的首次 push 流程、逐 head Codex 觸發、
  最新 main 更新迴圈與 Cyberbiz 證據邊界。
- `docs/dev/m0-automation-infra.md`、`docs/dev/m0-review-convergence.md`：補 D31／D32 現行通道。
- `docs/worklog/2026-08-20-PR61-第四輪驗收修復.md`：保留錯誤原文並在原處追加日期更正，
  列出 review id、六個 inline id 與撤回記錄位置。
- `docs/specs/91-pit-register.md`：抽取本輪三項可復用坑，並把本 worklog／handoff 以已讀已抽取
  狀態加入附錄 A；本檔與配對 handoff 構成本單位三件套。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本單位的本地 staged 候選已通過 workflow 結構與完整 selector 閘門；尚須完成最終重跑、
  commit 後 diff、鐵律 15 三端點重拉與 push。任一後續失敗即不得發布。
- Claude 對 head `96ffc01` 的判詞已取得且為需修改；兩條 🟡 雖已在本地處置，仍須由下一個
  head 的獨立驗收確認，不能自行宣稱已清。
- 本次修改 review workflow；下一個 head 若實跑命中 `validation-skip`，只能記
  「Claude 證據未取得／未達雙清」，PR #61 仍由使用者獨立人工審閱與合併。
- Cyberbiz 兩項英文原文仍未取得；S-14／`specs/110` 不得使用其事件或 D+2 延遲值。
