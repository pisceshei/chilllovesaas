# Parity R13：管道（R0-STUB1 結案＋管道即 app＋UCP 有答案）

> 對應規格：docs/specs/71 §E R13、§F（R0-STUB1 ✅ 結案、R12-V1 ✅ 結案；2 DOC＋8 V）｜
> 研究：**docs/research/82**｜help/shopify.dev：管道·POS·agentic·app 五主題（103K）｜
> commit：見本檔所在 commit

## 已完成的工作 (Done)

- **🔴 R0-STUB1 結案（掛 13 輪，最大顆佔位）**：三顆純 toast 導航項轉為真頁面——
  `m-agentic`（代理式）／`m-pos`（門市 POS）／`m-apps`（應用程式），
  以 `MODULES {full:true, custom:fn}` 形態註冊（自帶 page-head）。
- **🔴 最重架構發現：銷售管道全部是 app**。實測 href 直證——代理式＝`/apps/agentic`、
  銷售點＝`/apps/point-of-sale-channel`、Translate & Adapt＝`/apps/translate-and-adapt`；
  **只有線上商店是第一方特例走 `/themes`**。設定層雖分兩頁（`/settings/sales_channels` 3 個管道
  vs `/settings/apps` 4 個 app），但 **`⋯` 選單完全相同**【開啟應用程式／檢視詳情／解除安裝】。
  ⇒ 資料模型應是 **`App` 之下的 `Channel`**，不是兩張平行表（R13-V2）。
- **🔴 發布模型是三層 AND**：`Publishable`（商品/系列/子類選項）×`Publication`（綁管道，帶 autoPublish／
  supportsFuturePublishing）×`Catalog`（APP／COMPANY_LOCATION／MARKET）。
  help 原文：商品必須**同時**在該管道市場的目錄內**且**已發布到該管道。四個掛載點要同步（R13-V4）。
- **🔴 R12-V1 結案（實測直證）**：`/settings/gift_cards` **302 → `/settings/payments?hasMovedNavItem=true`**
  ——query 參數自己講明「導航項目已搬家」。禮品卡設定在**付款**底下。
- **代理式全頁實測**：狀態列＋主標（「**未上線，但您的商品可能仍會顯示在 AI 代理程式中**」）＋
  準備清單 2 項＋「允許 Shopify 為我管理」開關＋**AI 管道 4 個**（ChatGPT／Microsoft Copilot／
  其他管道〔tooltip：Perplexity 等〕／Shop）＋**資料來源 2 個**（Shopify Catalog／Knowledge Base）＋補充條款。
  管道詳情浮卡揭露 🔴 **「發現來源」與「結帳位置」是兩件互相獨立的事**＋四格指標。
- **🔴 B-7／UCP 待決案有答案了**：UCP＝Shopify 與 Google 共同開發的**開放標準**，規格全文在 **ucp.dev**，
  開發者入口 `shopify.dev/docs/agents`。五個 MCP 端點、能力協商用 platform profile、
  checkout 四態、**`update_cart`/`update_checkout` 是 PUT 語義**、agent 只能查自己促成的訂單、
  擴充採 reverse-domain 命名無中央審批。**技術面不再是未知，剩產品決策**（R13-V7）。
- **🔴 POS 完整材料到手**（給範圍裁定用）：Lite vs Pro **逐項對照表**——
  **Lite ≈ 收銀機**（能收錢·退款·改庫存數量）／**Pro ≈ 門市營運系統**（換貨·訂單取消·全部履行·
  庫存追蹤與調整·接收轉移·日報表·店內分析·零售角色權限·無限 POS-only 員工）；
  計價 per-location US$89/月（Plus 前 20 個免費）；**POS 權限 9 群組**且
  🔴 **走 organization role 而非 store permission、只能指派角色不能指派單一權限、粒度以「裝置所在地點」為軸**。
- **層⓪紀律再次奏效**：POS 管道頁首次載入內容區空白，**重載一次即出現**——不是「本尊沒有內容」。
- **層⓪紀律第二次**：我猜的 `/settings/point_of_sale` 得 404 ⇒ 依鐵律 12.1 **不寫「本尊沒有這頁」**，
  改登記為條件閘控未驗證（R13-V5）。
- **原型**：3 頁新函式＋`AGENT_CHANNELS` 資料＋`agentChannel()` 詳情浮卡＋`appRowMenu()` 三動作；
  NAV 三顆 toast 換成真路由；DOCS ＋9 條（鐵律 12.4 四件事）。
- **limits.yml ＋4 節 59 鍵**：`sales_channels`／`agentic`（含 `ucp` 子節）／`pos`／`apps`。
- **驗證**：lint ERROR 0／WARN 13（基準線）；煙霧 8/8；三頁實地渲染確認；原型 console 零錯誤。

## 修改的檔案與核心邏輯 (Changes)

- `docs/research/82-admin-channels.md`（新檔）：§0 架構（管道即 app／發布模型三層 AND／條件閘控）／
  §1 代理式全頁／§2 **UCP 全貌**（治理·能力協商·5 個 MCP 端點·三條實作陷阱·受限 Liquid context）／
  §3 POS（Lite/Pro 對照·計價·權限 9 群組·smart grid·現金追蹤·小費·UI extensions）／
  §4 app（4 型·2026-01-01 分界·scope 呈現·protected data 三級·theme extension）／
  §5 第一方管道清單／§6 CSS 三段式 7 條／§7 裁定面。
- `docs/design/chilllove-admin-v2.html`：
  - 新增 `AGENT_CHANNELS` 常數 ＋ `agenticPage()`／`agentChannel()`／`posPage()`／
    `appsChannelPage()`／`appRowMenu()` 五個函式
  - NAV 三顆 toast → `go('m-agentic')`／`go('m-pos')`／`go('m-apps')`
  - MODULES 註冊三個 `{full:true, custom:fn}` 模組
  - DOCS ＋9 條（ag-ready／ag-auto／ag-channels／ag-sources／pos-hero／pos-analytics／pos-reco／
    app-installed／app-channels）
- `config/limits.yml`：新增 4 節 59 鍵。
- `docs/specs/71`：§E R13 ✅；§F R0-STUB1 標結案、新增 10 條（2 DOC＋8 V）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **兩條需使用者裁定（本輪的主要待決）**：
  - **R13-V1 POS 範圍**：做不做／做到 Lite 還是 Pro／per-location 計價要不要照抄。
    本輪只做管道殼，**POS 本體（smart grid／register session／PIN／班次／換貨／收據／小費）完全沒做**。
  - **R13-V7 UCP 相容層**：技術規格已完整，要不要實作是產品決策。
- **M1/M2 前必答**：V2（App→Channel 資料模型）／V3（受限 Liquid context）／V4（發布模型四掛載點）／
  V6（POS 權限模型與 admin 不同）。
- **R13-V5**：POS 設定頁未驗證（條件閘控，需有 POS 的店）。
- 🔴 **分支順序注意**：本輪從 **main** 開分支，**不含 R11／R12 的改動**（那兩輪各自在自己的分支上，
  PR 尚未合併）。三個 PR 合併時會在 `chilllove-admin-v2.html` 與 `limits.yml` 產生衝突，
  **建議依序合併 R11 → R12 → R13**。
- **本輪未做的管道**：Shop 管道詳情頁、線上商店管道（R9 已做過但走 /themes）、
  第三方管道的安裝流程（App Store 內頁）、app 詳情頁（計費·活動與權限·隱私三區塊）。
- 既有未決：R8-V2、R9-V1、R10-V2/V3、R11 十一條（含鐵律 7 的 AOV 例外）、R12 九條（含 RBAC vs 鐵律 2）、
  B-8（RTE）、PR #12 待合併。
