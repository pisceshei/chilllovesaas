# 2026-08-31 — 結帳線第三包：manual 付款方式＋checkout 付款段＋前台顯示對接

## 已完成的工作 (Done)

- **研究輪**（使用者指示「深度研究運送/運費/付款方式的後台設定與買家前台對接」）：
  6-agent 研究艦隊（倉庫盤點×2＋官方文檔×3＋MIT/BSD 開源對照）＋我親自實測本尊
  Settings→Payments 全鏈（capture 三值 modal／manual 四值選單／兩欄表單／啟停語義）
  與前台（商品頁 BUY IT NOW unbranded 鈕、footer 付款圖示空集合、checkout Payment 段
  單/多方法形＋Complete order＋billing 全球國家清單）。正典＝`docs/research/86`。
  🔴 研究中再次實測到 medusajs 文檔頁內嵌 agent 注入（要求提交 feedback）——照鐵律
  16.3 未執行、已登記。
- **manual 付款方式資料層**：`shop_payment_methods` 表（四型 enum＋builtin_guard 虛擬欄
  ＝內建型別每店唯一〔86 §3 已啟用者從選單消失〕＋官方九保留名擋 custom）；
  `checkouts.payment_method_snapshot` json（含 payment_instructions——確認頁/F5 要用）。
  🔴 15-F4.2 定位不變：PSP 方式＝capability 查詢不落表，本表只承載 manual。
- **checkout 付款段**（86 §4 實測形）：零方法＝「此商店目前無法接受付款」；單一方法
  無 radio＋details 直出；多方法 radio 手風琴（選中者展開）；帳單地址 radio 恰兩值
  （預設 same_as_shipping）；「完成訂單」佔位 disabled（F5 接）。`POST payment` 走
  server 重驗（active∧本店——商家停用後客戶端殘留 radio 不落庫）。
- **Liquid 前台顯示對接**（86 §7 差距收口）：
  ①🔴 CartDrop `duties_included` 顯式 false——Ella tax-note 四分支用 `== false` 顯式
  比較，缺鍵時整段稅注靜默空白（含「未含稅」文案）；
  ②ShopDrop `enabled_payment_types`＝顯式空集合（86 §5 實錘：manual 不進圖示列）；
  ③`additional_checkout_buttons`/`content_for_additional_checkout_buttons` 顯式 stub；
  ④`all_country_option_tags`＝market ∩ 有費率 zone 的 option 串（與結帳頁同源——鐵律 7）。
- **cart 運費試算三支**（86 §6 官方現值）：`GET /cart/shipping_rates.json`＋prepare＋
  async（同步引擎）；官方形 `{"shipping_rates":[{"name","price","source"}]}`；
  🔴 price＝十進位主單位字串（divmod，不走 float、不吐裸 cents）；帶前綴形同收。
- 突變輪 MUT-1～7 全紅（拔顯式 duties_included／保留名驗證／內建唯一驗證〔DB guard
  第二道被 M2 的 RecordInvalid 斷言區分出來〕／拔 active 重驗／price 裸 cents／
  快照掉 instructions／拔 option_tags 佈線），全復原後 33/33 綠。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260831230000_manual_payment_methods_and_checkout_payment.rb`
- `app/models/shop_payment_method.rb`（METHOD_TYPES 四值／BUILTIN_NAMES／RESERVED_NAMES
  九名／snapshot 五鍵）；`app/models/shop.rb`（delete_all 關聯）
- `app/controllers/storefront/checkouts_controller.rb`（payment_html＋payment 動作）
- `app/controllers/storefront/cart_controller.rb`（estimate_rates＋decimal_string＋三動作）
- `app/liquid/theme_engine/drops.rb`（CartDrop duties_included×2、ShopDrop
  enabled_payment_types）；`runtime.rb`（三個全域 assign＋country_option_tags）
- `config/routes.rb`（payment 路由＋試算三支裸/帶前綴）
- specs：`shop_payment_method_spec`（M1–M5）／`storefront_checkout_payment_spec`
  （P1–P6）／`storefront_cart_shipping_rates_spec`（R1–R4）／
  `liquid/storefront_display_wiring_spec`（W1–W4）

## 尚未完成或需注意的風險 (Pending / TODO)

- **PSP 付款線未動**（刻意）：F4 Stripe/PSP pack、payment intent 欄、order_transactions
  消費者、`shops.payment_providers` 欄——隨 F4 包；enabled_payment_types 屆時由
  provider 能力導出（完整 enum 官方未載，86 §5 V）。
- **billing different 的地址表單未做**：v1 只落 mode 旗標；完整帳單地址表單（全球
  國家清單——86 §4 實測不受 market 限制）隨 F3 欄位三態包。
- **稅未接**：Calculator 支援 tax 但結帳頁不傳（tax_settings 表已有 include_in_prices
  ——法域 pack 接線另包；cart.taxes_included 真值同源）。
- 登記 V：capture modal 三值 vs 官方 help 四值（86 §2）；checkout 付款方法排序
  （實測疑＝啟用序，未構造第三方法驗證）；官方 prepare_shipping_rates 回應形未取得
  （ours＝回同結果）；試算國家 option 顯示名暫用國碼（國家名字典隨 markets 包）；
  shop.shipping_policy／policies 表未建（Ella with_policy 分支恆走 without——安全降級）。
- 「完成訂單」鈕是 disabled 佔位——F5 訂單成立包必吃 payment_method_snapshot＋
  shipping_lines＋同一 Calculator Result（manual ⇒ financial_status=PENDING，
  86 §3 官方句＋90-blueprint/05 §C.12）。
