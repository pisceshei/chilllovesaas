# 2026-08-31 — G6 打磨包：結帳線三頁視覺＋/cart 頁縫補＋設定索引樣式

## 已完成的工作 (Done)

- **結帳頁改雙欄**（≥990px：主流程＋sticky 訂單摘要；窄幅單欄摘要在前）：
  `checkout.css` 自寫（值全取我方 tokens——鐵律 8/9；結構對位 24 §5／86 §4 實測形；
  🔴 87 號 1:1 量測輪後再校準到量測值，本包不宣稱 1:1）。配送／付款卡片化、
  選項列邊框＋選中高亮、完成鈕黑底全寬、合計 tabular-nums。樣式主要吃既有
  `[data-*]` 選擇器——HTML 只換頁殼（header／layout／aside）。
- **thank-you 頁**卡片化（訂單摘要表＋付款指示卡＋繼續購物）；**QR 付款頁**
  接同一 stylesheet（拆 inline style）。三頁共用 `checkout_head`
  （viewport＋noindex＋propshaft digest link）。
- 🔴 **/cart 404 縫補**（G2 縫的可及部分）：page_renderer 補 `"/cart"`→cart 模板
  對映；pages controller 對本路徑**繞過頁快取**＋`Cache-Control: no-store`、
  以買家 cookie 真車注入 cart_json（14 §F1-4「個人化不進頁快取」＝不快取本頁，
  不是快取空車）；純瀏覽不建車列。minimal fixture 補 cart 模板；C1/C2 兩格
  （含「加車後數量更新＝殺被頁快取吃掉」）。**/collections 清單頁仍 404**
  （需 collections 全域 drop——歸 G2，登記 91 §3）。
- admin 設定索引 `.cl-settings-*` 樣式補齊（ML-4 起一直吃瀏覽器預設）。
- **本地親眼驗證**：dev server 全流程走過（結帳雙欄／窄幅／thank-you 卡）截圖確認。

## 修改的檔案與核心邏輯 (Changes)

- `app/assets/stylesheets/checkout.css`（新）＋`app/assets/stylesheets/admin.css`
  （.cl-settings-*）
- `app/controllers/storefront/checkouts_controller.rb`（三頁殼＋checkout_head＋
  delivery h2）
- `app/liquid/theme_engine/page_renderer.rb`（/cart 對映）＋
  `app/controllers/storefront/pages_controller.rb`（快取繞過＋buyer_cart_json）
- `spec/fixtures/theme_engine/minimal-1.0/`（templates/cart.json＋sections/main-cart）
  ＋`spec/requests/storefront_pages_spec.rb`（C1/C2）

## 尚未完成或需注意的風險 (Pending / TODO)

- 87 號結帳頁六層實測輪（需 chill.deals 放行）後做 1:1 量測校準；地址表單
  （姓名/街道欄位）與 Express checkout 區＝G6-4 本體。
- 單一付款方式仍需按「更新付款方式」才落快照（行為面，非視覺）——G6-3 一併理。
- /collections 清單頁 404 留 G2（collections 全域 drop）。
- checkout.css 用 `:has()` 做選中高亮（基線瀏覽器 2023+；舊瀏覽器僅失去高亮，
  功能不受影響）。
