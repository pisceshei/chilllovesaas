# PR #61：Codex `5a70431` 七則驗收修復查證

## 已完成的工作 (Done)

- 全量讀取 exact-head review `4981088935` 與 inline `3820221202`、`3820221207`、
  `3820221212`、`3820221217`、`3820221220`、`3820221227`、`3820221234`；七則都綁定
  `5a70431ef3923222475a52bdbe4b395932f24bc3`。已停止合併，沒有把 bot 建議直接當修法。
- 內部沿革以 `git log -p`／`git blame` 查證：validation-skip 分支沒有時間上限；P-5 的
  `currency_conversion_fee_percent` 自總方案起就同時被寫成「應抓」與比例例外；CD-1 只驗 full
  dump；B/C/D 的同依賴並行、兩輪零發現收斂與 TikTok 未取得總表都由本方案既有提交引入。
- Shopify 官方《Navigating the Shopify admin》（取證 2026-08-20）逐字支撐
  "You can toggle specific alerts as read or unread, or mark all alerts as read."，並明載 alerts 會送給
  "staff with relevant permissions"；同頁列出五種 alert 情境。故 inline 所稱「只有 badge 有證據」
  不成立，但方案欠缺這些逐字與日期的事實成立。URL：
  <https://help.shopify.com/en/manual/shopify-admin/shopify-admin-overview>。
- Shopify 官方《Setting up customer notifications》（取證 2026-08-20）只支撐大多數顧客通知
  不能停用，以及頁面列出的六種可停用通知；它不支撐我方已選逐範本 `can_deactivate` schema
  與 default。URL：
  <https://help.shopify.com/en/manual/fulfillment/setup/notifications/customer-notifications>。
- MySQL 8.4 官方《Point-in-Time Recovery Using Binary Log》（取證 2026-08-20）逐字說
  "The source of information for point-in-time recovery is the set of binary log files"；
  `mysqldump` 官方頁明載 `--source-data` 會把 binlog coordinates 寫進 dump；`mysqlbinlog` 官方頁
  明載 `--stop-datetime` 可作 PITR。URL：
  <https://dev.mysql.com/doc/refman/8.4/en/point-in-time-recovery-binlog.html>、
  <https://dev.mysql.com/doc/refman/8.4/en/mysqldump.html>、
  <https://dev.mysql.com/doc/refman/8.4/en/mysqlbinlog.html>。
- TikTok Shop Product API 的第一方逐字與 production endpoint 已在方案 §11.10 取得；因此
  §11.12 的缺口只能保留為 TikTok Shop feed URL 能力未取得，不能再說 Product API 未取得。

## 修改的檔案與核心邏輯 (Changes)

- 本 worklog 與配對 handoff 保存七則 exact-head 意見、內部沿革、官方逐字、URL、日期及預定
  修法；`docs/specs/91-pit-register.md` 附錄 A 與中央 handoff 同步本具名步驟。
- `docs/specs/91-pit-register.md` §3.5 只登記兩個查證時發現、但 inline 未點名的同型問題：總方案
  P-5 仍含 percent／金額矛盾，以及總方案 CD-1 收口矩陣仍只要求最小 full-dump 還原。依鐵律
  17.2，本步不順手修改總方案。
- 本步沒有修改執行方案；先完成鐵律 16／21 的查證停點。

## 尚未完成或需注意的風險 (Pending / TODO)

- 下一步只修七個被點名位置：validation-skip 有界輪詢；通知中心補證並把未選 schema 降級；
  TikTok 未取得總表同步；P-5 排除 percent；CD-1 加 PITR；收斂先驗完整覆蓋清單；B/C/D 每頁
  串行重建基線。
- 總方案兩個同型敘述只登記未修；若後續被獨立點名，須另包處置，不能把本輪執行方案修正
  冒充總方案已同步。
- 新 commit 會使 `5a70431` 的 CI、Claude validation-skip 與 Codex review 失效；實作後必須重跑
  全部閘門與最新 head GitHub 驗收。
