# 2026-08-31 包 32：Markets 資料層四表＋url_prefix／hreflang_codes 型別分離

## 已完成的工作 (Done)

- **實測取證輪**（鐵律 12；證據正典＝`docs/research/84`）：Markets 索引／市場詳情（繼承 8 維度＋
  Domain and language ⊕ 兩形）／Create market 表單（status 恰兩值、條件類型恰四值——DOM 收割）／
  Settings→Domains 列表與網域詳情（domain type 恰三值逐字）。零資料殘留（表單 Discard）。
- **資料層五表**（migration `20260831150000`）：`markets`（primary_guard 生成欄位恰一 primary）／
  `market_regions`／`domains`（host 全域唯一＋恰一 primary）／`market_web_presences`
  （domain XOR subfolder 的 DB CHECK）／`market_web_presence_locales`（67 §C.8 三欄
  is_market_default／open_to_buyers／closed_at＋default_guard＋複合 FK→shop_locales＝③⊆②執法點）。
- **`Markets::UrlPrefix`**（67 §F.1(b) 恆帶地區全函式；SF-9：簽名拒 Set ⇒ TypeError；
  多國市場 region 來源＝subfolder_suffix（V-225 暫案 C），缺 ⇒ MissingRegionSource fail-closed）。
- **`Markets::HreflangCodes`**（62 §I.2 逐字：base＝language[+script]，多國逐國展開回 Set；
  空 region raise 不退裸碼）。**與 UrlPrefix 型別分離**（String vs Set，兩驗證域不合併）。
- **`Markets::PrefixIndex`**（67 §F.1(c) 前綴≡身分：剝段查表，open ∧ published 才可路由，未命中=404 判準）。
- **`Markets::ProvisionDefaults`**（單一實作：Shop#after_create ＋回填 migration `20260831151000`
  兩個薄呼叫端）：建店即 HK primary market＋primary domain＋presence＋已發布語言白名單（B11 鏈接通）。
- **limits 落鍵**：`i18n.locale_prefix.*`（11 鍵）／`i18n.market_locales.*`（13 鍵）／
  `i18n.admin.translation_input_mode.*`（5 鍵）／頂層 `naming_contract:`（§24；67 §J 原文寫 §23 已被佔用）。
- **67 §J 不實宣稱更正**（鐵律 19.5）：「本輪已落鍵」對 2026-08-13 那組當時不實——追加有日期更正註，
  原文保留。
- Shop 模型：markets／domains 關聯（**刪除順序在 shop_locales 之前**，複合 FK restrict 所迫）＋
  `provision_default_market` callback（在 enable_launch_locales 之後）。
- Specs 29 例全綠：U1–U8／H1–H5／M1–M10／P1–P5（含 DB 層與 model 層各自的格）。
- **cache stamp 絆線收口**（cache_stamps_spec 的 known_pending 機制照設計觸發）：
  `market_settings_version` 落成 **`markets.updated_at`**（帶前綴現況寫法，S10 price_lists 同形）；
  region／presence／白名單三類寫入以 belongs_to touch 鏈 bump（mwpl→presence→market 級聯實測）；
  新增接線格（別市場寫入不動本市場 stamp 的對照半格）。消費端沿 resolved lineage 取 max 的
  義務寫進 limits 註釋（步 2 落地）。
- **7 個非交易式併發 spec 的 purge 幫手補市場鏈刪除順序**（markets／domains 先於 shop_locales，
  複合 FK 所迫）——本包 FK 落地後全套跑一次抓出，非既有缺陷。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260831150000_create_markets_foundation.rb`、`20260831151000_backfill_default_markets.rb`
- `app/models/{market,market_region,domain,market_web_presence,market_web_presence_locale}.rb`
- `app/services/markets/{url_prefix,hreflang_codes,prefix_index,provision_defaults}.rb`
- `app/models/shop.rb`（關聯＋callback）；`spec/factories/markets.rb`
- `config/limits.yml`（三個 i18n 子區塊＋naming_contract §24）
- `docs/specs/67-multilingual.md`（§J 更正註）；`docs/research/84-markets-domains-live-teardown.md`（新）
- `spec/services/markets/*_spec.rb` ×3、`spec/models/markets_data_layer_spec.rb`

**突變驗證（鐵律 20.2⑤／20.3）**：
| # | 突變 | 預期紅 | 結果 |
|---|---|---|---|
| MUT-1 | UrlPrefix.region_of 改 suffix 優先 | U3（單國市場用 market region） | 2 failures ✅ |
| MUT-2 | HreflangCodes 多國退回裸碼 | H2／H3 | 3 failures ✅ |
| MUT-3 | PrefixIndex 拿掉 published 閘 | P5 | 1 failure ✅ |
| MUT-4 | MWPL 拿掉 prefix_stays_unique | M9 | 1 failure ✅ |
| MUT-5 | MWPL 拿掉 belongs_to touch | cache_stamps 市場鏈接線格 | 1 failure ✅ |

20.2 適用類型：①（外部語義＝29／67 既有研究＋本輪 84 實測，零記憶補值）；③（全稱句
「恰兩值／恰三值／恰四值」皆附 DOM 收割或對話截圖出處）；⑤（DB 與 model 兩層各測、
四突變全紅）；⑦（lint-prototype 未涉及——本包無原型改動）。

## 尚未完成或需注意的風險 (Pending / TODO)

- **V-225 未裁定**：多國市場前綴 region 來源暫採 C（subfolder_suffix）；多國＋純網域 presence
  的組合 fail-closed raise。裁定改變時連帶 `i18n.locale_prefix.multi_country_region_source`。
- **自帶 region 的 locale tag（pt-BR）不成前綴**：67 §F.1(b) 正則結構上排除（script 段限 4 字母），
  UrlPrefix 對此 raise（U8 覆蓋）。67 §A.4 明文允許 pt-BR 作 shop locale ⇒ 兩規格間有未解縫，
  開 pt-BR 市場語言前必須先裁定（登記，不猜）。hreflang 側無此問題（base 逐字丟 locale region，H4）。
- **derived_parent_market_id 重算器未實作**：v1 單市場恆 NULL；submarket 出現前必須先落
  lineage 重算（29 §1.5(a)：同 transaction 重算受影響子樹＋nightly 對帳）。
- **市場變動未 bump `shops.catalog_version`**：欄註釋寫「寫入者隨第 32 包」，但 v1 無市場 CRUD UI、
  provision 只在建店時跑（該店尚無快取可失效）⇒ 實際接線隨步 2 快取 key 消費時一起做，避免空轉。
  62 §I.3(b) 失效管線的觸發（shop_locales／mwpl 變更）同屬步 3／4 消費端。
- **domains.host 平台子網域列是環境快照**（base_host 換環境成死列）：步 2 解析器必須保留
  shops.subdomain 兜底；redirect／alias 的行為實作也在步 2。
- **`seo.hreflang.max_expanded_countries_per_market`（40）未在 HreflangCodes 內執法**：
  超限處置（部分標註＋lint）是 62 §I.3(c) 的 SEO 消費端語義，隨步 4 落。
- market_settings／market_currency_settings／catalog 掛接（29 §1.4 其餘表）不在本包射程；
  V-02（shipping 繼承矛盾）維持未定案、兩策略開關已在 limits 既有鍵。
