# PR #61：本機 MySQL 當前查活

## 已完成的工作 (Done)

- 以 2026-08-20 15:34（Asia/Taipei）、輸入 head
  `6cdd7c9c7f7d4006b9f8e32fc73c70d81129aaea` 為本步快照，重新判別本機 MySQL 狀態。
  `Get-Service` 沒有 MySQL／MariaDB 註冊服務，但 `Get-Process` 找到 `mysqld`；`netstat -ano`
  證實 PID 7308 正監聽 `127.0.0.1:3306` 與 `0.0.0.0:33060`，所以本步沒有重複啟動 launcher。
- 以 MySQL 8.4 原生客戶端的完整長參數重驗；`mysqladmin` 輸出 `mysqld is alive`，SQL 查詢回
  MySQL Community Server `8.4.9` 與 `root@localhost`。先前 PowerShell 使用 `-h127.0.0.1`
  被錯傳成主機 `127` 的輸出已作廢，未用於可用性聲明。
- 受限執行層內的 Rails boot 再現 `91` §3.5 已登記的工作區外 gem 絕對 `Dir.glob` 假空，並使
  Solid Cache／Zeitwerk 在資料庫連線前失敗；直接 `mysql2` 亦在該層遇到 Windows SSPI
  `SEC_E_NO_CREDENTIALS`。依既有 A/B 方法改在有完整本機檔案枚舉權的執行層跑
  `RAILS_ENV=test ruby bin/rails db:prepare`，再由 Rails Active Record 查詢，exit 0 且輸出
  `{adapter: "Mysql2", database: "chilllove_test", server: "8.4.9", probe: 1}`。

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-PR61-本機MySQL當前查活.md`：保存當前 process／listener、原生客戶端
  與 Rails 應用層三層證據，並撤回兩個受限層失敗輸出的錯誤外推。
- `docs/handoff/2026-08-20-階段一-PR61-本機MySQL當前查活.md`：交接本次輸入 head、命令邊界、
  已知沙箱限制與後續查活入口。
- `docs/specs/91-pit-register.md` 附錄 A 與
  `docs/handoff/2026-08-20-階段一開場包.md`：只補本步文件入口；根因已由 `91` §3.5 的受限工具
  環境條目涵蓋，沒有另造重複坑項，也沒有修改 Rails／MySQL 設定。

## 尚未完成或需注意的風險 (Pending / TODO)

- PID 7308、監聽狀態與 Rails 查詢只證明 2026-08-20 15:34 前後的快照；後續 Rails gate 前仍須
  重新跑 `mysqladmin ping`，不能把本次 PID 或 `probe: 1` 外推成永久存活。
- 本機 MySQL 不是 Windows 已註冊服務；若之後程序停止，沿用既有入口
  `C:\Users\pisce\chilllove-mysql\start-mysql.cmd`，啟動後仍須重新查活，不能只看 launcher exit。
- 受限層內的 Zeitwerk／SSPI 失敗沒有被宣稱已修復；本步依 17.2 不修改未被點名的工具、gem、
  `config/database.yml` 或 TLS 設定。Rails gate 必須在已有完整 gem 枚舉權的本機層執行。
- 本步是環境查活文件，不取代 `6cdd7c9` 前已完成的 29 項產品閘門；新增文件提交後仍須重跑
  doc-claims、15.1／15.2，再 push PR #61 的新 head。
