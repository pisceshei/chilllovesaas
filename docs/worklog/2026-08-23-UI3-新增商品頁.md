# 2026-08-23 — UI-3：前端「新增商品」頁（isNew 建立態，接 productSet）

## 已完成的工作 (Done)

- `ProductDetailPage`（59 §7：建立與詳情**同一元件**的 isNew 分支；v1 只開建立態）：
  原型卡片樹全數落地——標題與說明／多媒體(空態)／商品類別／定價(pill 收合＋利潤
  衍生)／庫存(SKU/條碼 pill)／運送／子類／購買選項／SEO(handle)／右欄發布卡。
  建立態四差異照 DOCS newproduct：徽章固定草稿、內容語言 chip 停用、右欄無狀態卡、
  頁首只剩儲存。
- **SaveBar 狀態機**（44 §22.5）：dirty 時取代 topbar 搜尋槽位（≥48em in-flow、
  <48em 底部浮動）；快照 dirty 判定；捨棄還原；驗證失敗 toast＋cl-shake＋focus
  首壞欄位；guardNav（useBlocker：首攔 shake＋toast、4 秒內再點同意離開）。
- **接 productSet**：送完整樹（B.4 規則 1）＋顯式 DRAFT＋建立態 uuid idempotencyKey
  （進頁生成、重試沿用）；金額主單位字串→恆兩位小數（整數運算不經 float）；
  三層錯誤全接（userErrors 映射欄位／AdminGraphQLError 帶 code／非 200）。
- 基建：ToastProvider（原型 .toast 深色浮層）、SaveBarContext、AdminGraphQLError、
  `lib/money.ts`（parse/format/profitState，null≠0）、TextField ref 轉發（React 19）、
  入口改 `createBrowserRouter`（useBlocker 需 data router）。
- CSS：詳情頁家族逐類對照原型量測（page-head/od-grid 633+317/pill 28 無框透明/
  swrow/savebar 雙形態/toast/dropzone/derow），全 token。
- 測試：vitest 10 例全綠（渲染樹／驗證擋 API／完整樹契約含 key regex／
  userErrors 映射／SaveBar 取代與捨棄）。

## 修改的檔案與核心邏輯 (Changes)

- `pages/ProductDetailPage.tsx`＋`.test.tsx`：新增（主體）。
- `lib/`：`money.ts`／`ToastContext.tsx`／`SaveBarContext.tsx` 新增。
- `api/graphql.ts`：AdminGraphQLError（保留 extensions.code）。
- `layout/AdminShell.tsx`：SearchOrSaveBar 槽位切換＋shake 重播。
- `App.tsx`：Provider 包裹＋/admin/products/new 換真頁。
- `entrypoints/admin.tsx`：BrowserRouter→createBrowserRouter（單 splat 包既有樹）。
- `components/TextField.tsx`：ref 轉發。
- `admin.css`：詳情頁 CSS 家族（量測出處逐類註明）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 未接線欄位一律 **disabled**（不收集後丟棄）：數量/地點（庫存包）、重量/包材/
  原產地/HS（運送）、SEO 標題/說明（input 無 seo 欄）、發布開關、類別、中繼欄位、
  單位定價、繼續銷售。各依賴見 dev doc §4/§6。
- 說明欄 v1＝純文字→段落 HTML（前端 escape＋後端白名單雙保險）；富文本工具列
  視覺佔位、AI 生成鈕 disabled（媒體/RTE 包）。
- guardNav 的 4 秒雙擊語義只攔 router 導航；瀏覽器整頁關閉/重整未掛 beforeunload
  （原型也沒有；要加時注意與 blocker 的互動）。
- 測試消歧教訓（第三次同型）：側欄「草稿」與徽章、雙儲存鈕——**查詢一律 scope
  到語義容器**（main／savebar region），全文件查詢在 shell 存在後恆有撞名風險。
