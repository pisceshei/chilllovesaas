# PR #61：Codex `5a70431` 七則驗收修復

## 已完成的工作 (Done)

- validation-skip 分支新增有界輪詢契約：沿用 `await-verdict.sh` 的時間參數與預設，明定最大輪次／
  deadline、每輪證據、逾時四段 remote handoff、通知與非零終止；缺證據時不能合併。
- 通知中心補上 Shopify 官方 read/unread、mark-all-read、relevant permissions 與五種現行情境證據；
  同時把逐範本 `can_deactivate` default 降為未選 schema 候選，測試店證據與使用者選案前禁止入規格。
- TikTok 未取得總表已改為 Shop feed URL 能力；P-5 明確排除百分比比例 float，basis points 另需
  使用者裁定；CD-1 演練加入 dump coordinates、連續 binlog 與目標時間／位置重播。
- A–F 收斂改為先完成固定權威覆蓋清單，再用兩輪零新發現確認穩定；清單未完成時連續三輪零新
  只觸發排程升級。B/C/D 改為每頁 B→C→D 串行，每節點從已含上游修復的 main 重建基線。
- 修改與初版交接完成後，完整 29 支閘門均已實跑至退出 0：Rails `284 examples, 0 failures`、
  Vitest `5 passed`、RuboCop 162 檔零 offense、Brakeman 零 warning、doc-claims 掃描 274 份且
  警告 0。workflow syntax 首次因 Ruby 找到 WSL `/bin/bash` 而看不到 Windows Temp 路徑假紅；
  依既有 Shell 路徑交接把 Git Bash 放到 PATH 首位後，33 個 run 區塊與 11 條回歸 fixture 全綠，
  沒有修改 checker。Node v24.19.0 仍會觸發 engine warning；前端 gate 綠不代表專案支援 Node 24，
  GitHub CI 仍以 `.node-version`／workflow 指定的 22.17.1 為準。本段回寫後已再從第 1 支完整重跑，
  第二輪同樣 `GATES_ALL_GREEN=29`；Rails 284/0、Vitest 5/0、doc-claims 274 份／警告 0、附錄 A
  雙向集合 missing 0／extra 0。三組改動表格逐列未跳脫分隔符實得 5／5／5、5、4，符合各自
  四欄／四欄／三欄結構；GitHub `/markdown` API 實際回傳 table 7、tr 69、th 23、td 219，
  證明不是只靠本機文字目視判定。

## 修改的檔案與核心邏輯 (Changes)

- `docs/plans/2026-08-20-階段一執行方案.md`：只修改七則 inline 點名的判準與它們在同檔內必須
  同步的執行細節；沒有修改 workflow、scripts、config 或未點名的總方案敘述。
- 本 worklog、配對 handoff、中央開場 handoff 與 `docs/specs/91-pit-register.md` 附錄 A：保存
  問題、證據、修法、驗證入口與自身集合索引。
- 修法前查證依據為 commit `d52c76f` 的配對 worklog／handoff；七則來源是 exact-head review
  `4981088935` 與 inline `3820221202`、`3820221207`、`3820221212`、`3820221217`、
  `3820221220`、`3820221227`、`3820221234`。

## 尚未完成或需注意的風險 (Pending / TODO)

- commit 後 doc-claims、15.1／15.2 與新 head GitHub 驗收尚待即時完成；任何非零、doc-claims
  warning、未清 thread 或 exact-head 新意見都禁止合併。
- 七則 inline 尚未逐條 API 回覆與 resolve；只能在修復 commit 存在且回讀回覆 body 成功後處置。
- 總方案 P-5／CD-1 的同型敘述只依 17.2 登記於 91 §3.5，不能宣稱已同步修正。
