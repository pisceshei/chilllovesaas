# 2026-09-04 主題編輯器 E2：shell＋頂欄依本尊重做（D79）

## 已完成的工作 (Done)

- 依 `docs/research/100-theme-editor-live-teardown.md` §1／§5／§6／§7 把編輯器外殼與頂欄重做：
  三欄版面（左欄＝面板切換器所選面板；右欄只在選中 section／block 時掛載；面板切換器再點已啟用者 ⇒ 全寬預覽）；
  頂欄 Exit（有未存變更 ⇒ 「離開頁面並放棄未儲存的變更？」Stay／Leave page）、Sections／Theme settings／App embeds
  三顆 icon 鈕（tooltip＋鍵帽）、主題 chip（名稱＋作用中／草稿徽章）、市場選擇器（Store default）、模板選擇器
  （搜尋＋兩層清單＋"已指派給 N 個…"＋建立模板）、inspector 切換、手機檢視、Undo／Redo、「…」（Edit code／Preview／
  Keyboard shortcuts）、Publish（「儲存並發布 {name}？」先存再 `themePublish`）、Save（儲存中進度條）。
- 佈景設定移到左欄手風琴（展開分類寫 `category=`）；App embeds 面板（搜尋＋空態句）。
- 快捷鍵單一表 `editorShortcuts.ts`（tooltip 鍵帽／對話框／綁定共用）：Ctrl+S、Ctrl+Z、Ctrl+Y／Ctrl+Shift+Z、Ctrl+/、
  Ctrl+Alt+1/2/3、Ctrl+Alt+I、Ctrl+Shift+I、Ctrl+Shift+H、Shift+⌫、Esc；modal 開著全部不攔；輸入框內不攔編輯類。
- URL 狀態：`template`／`section`／`block`／`context=theme|apps`／`previewMode=fullscreen`／`previewPath`／`category`。
- 後端 `ThemeType` 加 `templateKeys`（來源 ∪ DB Template 列）與 `templateAssignments`（依 `template_suffix` 分組）。
- 編輯器 CSS 整塊改寫成 tokens（舊硬編碼色值／尺寸全數移除），幾何照 100 §8 換算取整。
- 五語系 i18n 鍵齊補；`messages.test` 五語包 key 集合一致。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `app/frontend/admin/pages/ThemeEditorPage.tsx` | 接 `EditorTopBar`；`panel`／`fullscreen`／`inspector` 狀態；右欄條件掛載；佈景設定手風琴；apps 面板；URL 參數；快捷鍵綁定；離開／發布／建立模板流程 |
| `app/frontend/admin/editor/EditorTopBar.tsx` | 新增：三區頂欄 |
| `app/frontend/admin/editor/EditorIconButton.tsx` | 新增：icon 鈕＋tooltip 鍵帽 |
| `app/frontend/admin/editor/TemplateSwitcher.tsx` | 新增：模板選擇器（含 `splitTemplateKey`） |
| `app/frontend/admin/editor/MarketSwitcher.tsx` | 新增：市場選擇器（Store default；清單預留） |
| `app/frontend/admin/editor/ShortcutsDialog.tsx`、`editorShortcuts.ts` | 新增：快捷鍵表與對話框 |
| `app/frontend/admin/editor/CreateTemplateDialog.tsx` | 新增：建立模板對話框 |
| `app/frontend/admin/pages/ThemeEditorPage.test.tsx` | ED1／ED3／ED9／ED14／ED16／ED18／ED19／ED22 隨 UI 改；新增 ED28–ED34（含 ED33b） |
| `app/frontend/admin/i18n/messages/{en,zh-Hant,zh-Hans,ja,fr}.json` | `editor.*` 新鍵（頂欄、對話框、模板選擇器、快捷鍵表） |
| `app/assets/stylesheets/admin.css` | `.cl-editor__*` 整塊重寫（tokens） |
| `app/graphql/types/theme_type.rb` | `template_keys`／`template_assignments`／`suffix_counts` |
| `spec/requests/theme_editor_bootstrap_spec.rb` | E9／E10 |
| `docs/dev/e2-theme-editor-shell.md` | 功能文檔 |

閘門（本機）：`pnpm -s typecheck` 退出 0；`pnpm -s test` 全綠；`bundle exec rspec spec/requests/theme_editor_bootstrap_spec.rb`
全綠；`bundle exec rubocop`／`bin/brakeman --no-pager --exit-on-warn --exit-on-error`／`ruby scripts/check-doc-claims.rb
--base origin/main --require-base` 見 handoff §①。

突變輪（鐵律 20.2⑤；每一格＝改一處生產碼 → 跑 `pnpm -s vitest run app/frontend/admin/pages/ThemeEditorPage.test.tsx`
→ 記轉紅的測試 → `git checkout --` 還原）：

| 突變 | 改動 | 轉紅 |
|---|---|---|
| M1 | 全寬切換不寫 `previewMode` | ED28 |
| M2 | Publish 不先 `save()` | ED32 |
| M3 | 移除「modal 開著不攔快捷鍵」守衛 | ED33 |
| M4 | 建立模板用空內容取代 base JSON | ED29 |
| M5（無效） | 把 `hasRightPanel` 設 true 但保留 `selected` 條件 | 無——語義未變（右欄仍需選取），屬「突變沒改到行為」；改 M5b |
| M5b | 無選取時仍掛載提示右欄（舊形態） | ED3、ED19、ED28、ED30、ED33、ED33b |
| M6 | Exit 不經確認直接離開 | ED31 |

## 尚未完成或需注意的風險 (Pending / TODO)

- 本機目視驗證未做：admin 需登入，規則禁止在表單輸入密碼；本包以 DOM 斷言（35 個 ED）替代。合併部署後請使用者在
  bt3 demo 店目視三欄／頂欄／對話框。
- 市場選擇器只有 Store default；市場清單與預覽前綴（Markets GraphQL 面＋`Markets::PrefixIndex`）另包。
- 帶 `⊞` 的本尊鍵位未取得（100 §V V3），我方用 Ctrl+Alt+N／Ctrl+Alt+I；若日後取得本尊鍵位，只改 `editorShortcuts.ts`。
- Ctrl+Shift+I 在 Chrome 與 DevTools 衝突（本尊表列同組合，實際是否被瀏覽器攔截未驗）；E6 接 inspector 時實測。
- 左樹列解剖、區段／區塊 picker 兩欄形態、右欄「…」選單（Rename／Duplicate／Hide／Edit code）在 E3／E4／E5。
- 「Checkout and customer accounts」「Create metaobject template」「View documentation」「Get support」四個入口未做（無目標頁）。
- `editor.selectHint`／`editor.saveComing` 等舊鍵仍在語言包（無消費者）；清理隨 E3。
