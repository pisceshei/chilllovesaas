# 鐵律 21：逐步交接沿革查證

## 已完成的工作 (Done)

- 完整讀取 `CLAUDE.md` 與 `AGENTS.md`，確認當前分支的規範本文已包含鐵律 19／20，而
  `origin/main` 的最大編號仍是 18；本次修改因此必須留在 PR #61，且依鐵律 18.3 由使用者
  人工審核與合併。
- 用 `git log -p -S '每次工作結束前，一律寫交接文件' -- CLAUDE.md` 追到
  `d3a78faa5fe919a03f0d71e27642205564d87568`：既有 handoff 義務只在「工作結束前」觸發。
- 用 `git log -p -S '每完成一個部分，就寫一份工作記錄' -- CLAUDE.md` 追到
  `f36b32a37e0aec646d0f95203d1949948babd2d0`：既有 worklog 義務以「可獨立驗收部分」為邊界，
  但沒有要求每個會改變下一步決策的中間步驟各自留下 handoff。
- 用 `rg` 搜尋現行消費端，識別出必須同步的入口：`CLAUDE.md`、`AGENTS.md`、
  `docs/plans/2026-08-20-階段一執行方案.md`、`docs/DECISIONS.md`、
  `docs/dev/m0-review-convergence.md`，以及 PR #61 的開場終態 worklog／handoff。
- 2026-08-20 閘門快照：完整 `bin/ci` 明確回報失敗，其中 Rails specs 的輸出是 MySQL
  `127.0.0.1:3306` 拒絕連線，故沒有把整套 CI 記為通過；改依純文檔包既定枚舉式，使用 native
  Python 3.12、Ruby 3.4.10 與 Git for Windows Bash 逐支執行 `scripts/` 下名稱符合
  `^(check|test|lint)-` 的檔案，彙總輸出為 `GATE_ENUM_COUNT=17`、`GATE_FAILED_COUNT=0`。
- 使用者指出本機已有 MySQL 環境後，環境喚起與完整復驗另立
  `docs/worklog/2026-08-20-鐵律21本機MySQL與閘門復驗.md`，避免把後續環境結論倒寫成沿革查證
  一開始就知道的事實。

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-鐵律21逐步交接沿革查證.md`：保存本步驟的問題、沿革證據、
  影響面與下一步輸入。
- `docs/handoff/2026-08-20-鐵律21逐步交接沿革查證.md`：讓下一步能直接依已查證範圍落地，
  不必重新猜測 handoff 與 worklog 的既有邊界。
- `docs/specs/91-pit-register.md`：把本步驟的配對記錄加入附錄 A，維持追蹤集合一致。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本步驟只完成內部沿革與影響面查證，尚未修改規範本文；下一步必須新增鐵律 21，並同步上述
  消費端後才算落地。
- 本步驟的初次完整 Rails 閘門未取得成功證據；其後 MySQL 喚起與 Rails 復驗結果記在配對的
  後續工作記錄。GitHub CI 仍要在推送新 head 後重新執行，不能用本地結果取代。
- 「每一個步驟」必須有可執行邊界，否則會把寫 handoff 本身遞迴視為下一個步驟。下一步要明定：
  計畫／任務中的具名步驟與改變流程決策的節點各產生一份 handoff；同一步內的命令記入該份
  handoff，不再為 handoff 的收尾動作遞迴建立文件。
- PR #61 修改規範本文且 workflow 亦有變更；即使 CI 與 Codex 通過，Claude validation skip
  仍只能記「證據未取得」，最後維持使用者人工審核與合併。
