# 2026-09-01 G3 步 15b：主題 CRUD API

## 已完成的工作 (Done)

- POST /admin/themes/import（multipart；99 §4 Import theme 對位）——🔴 走 HTTP
  不走 GraphQL themeCreate：官方收 staged URL、我方 staged uploads 只收圖片
  content types ⇒ 專用端點（UploadsController 同理；GraphQL 版隨 staged 放寬
  ——91 §3.68）。
- themeRename（50 字官方上限閘）／themeDuplicate（"Copy of" help 逐字形＋撞名
  尾碼；🔴 零複製＝共享 content_checksum＋**DB 覆寫層 templates/theme_setting
  一併拷**——漏拷＝複製後商家編輯全丟，MU2 紅證）／themeDelete（官方
  "delete an unpublished theme"——published 拒刪 MU1 紅證；storage 目錄不刪）。
- theme(id) query＋ThemeType 擴充：source/licenseAttested/files（filenames
  萬用字元 ≤50 官方形＋first ≤2500）/importReport；FileSource.list/size_of。
- 突變 5/5 殺＋canary；全套 1884 例綠。

## 修改的檔案與核心邏輯 (Changes)

- 新：themes_controller.rb（import）、theme_{rename,duplicate,delete}.rb、
  ThemeManageUserError{Type,Code}、ThemeFileType、spec（5 例）。
- 改：theme_type.rb（六欄＋files/importReport）、query_type（theme(id)）、
  mutation_type（三註冊）、file_source.rb（list/size_of）、routes。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.68：GraphQL themeCreate 隨 staged uploads content-type 放寬另補／
  Download（寄信 zip——41 §634）不實作（匯出面 ⚪）／file body 三形 union 隨
  步 16 讀取面。
- 15c（OS 頁 UI：動作選單接線）接續；生產煙測隨 15c 一起（API 面已由 15a 煙測
  覆蓋管線本體）。
- ⚠ Template.create! 必帶 template_type（本輪 spec 首紅一次——模型驗證閘）。
