# PR #62 首輪驗收修復

## 已完成的工作 (Done)

- 對 initial head `38a9126b130ec4e65c8c1ef2312276fa7a2853a3` 全量讀取 GitHub 三個
  paginated REST 集合與 GraphQL review threads；Codex review `4982782311` 的 inline
  `3821620367`、`3821620375`、`3821620385`、`3821620393` 均綁該 head。Claude Review run
  `32369622769`／job `96426911054` 實跑完成、非 validation-skip，判詞為 issue comment
  `5356127623`；沒有把 workflow success 或留言缺席換算成通過。
- 依鐵律 16／20.2.2 先查 `git log -p` 與活性消費者再選修法：base 的「交接 commit」原本符合
  D35，D36 把 handoff 移出 Git 後才產生矛盾。修法因此改由實際修法或該驗收輪的 tracked
  worklog 形成新 head；本地 handoff、handoff-only commit 與空白 commit 都不得當 C1 刷新載體。
- 補齊歷史 handoff 的合規更正路徑：既有 `docs/handoff/` 保持唯讀；錯誤由新 worklog 回指精確
  路徑與穩定內容錨，使用者裁定另進 `docs/DECISIONS.md`，未點名同型坑另進 `91` §3。同步
  `CLAUDE.md`、`AGENTS.md` 與 review convergence 的活性契約。
- 保留 initial worklog 原文，以日期更正註補上 gate 快照的日期／ref／GitHub run 重取命令及
  `docs/handoff/` name-status 複驗；另補鐵律 20.3 的適用類型、固定處理與反向複驗輸出。
- 處理 Claude 的程序與射程項：執行方案的 umbrella 前言不再引用已失去定義的「三件套」；PR
  描述新增假設清單與 `codex/` 分支偏離說明；workflow prompt、script 契約註釋及 D36 後不可執行
  的舊 handoff 待辦只登記到 `docs/specs/91-pit-register.md` §3，不跨 18.3 射程順手修改。
- 已通讀本 worklog；抽取輸入固定為 Codex review `4982782311` 的上述 inline 與 Claude comment
  `5356127623`，對應新增 `91` §3 的 D36 延後消費者及凍結 handoff 待辦條目。本檔同步列入
  附錄 A.1；輸入全集可由本節首項的三個 REST 集合與 GraphQL threads 重取。

## 修改的檔案與核心邏輯 (Changes)

- `AGENTS.md`、`CLAUDE.md`、`docs/dev/m0-review-convergence.md`：把 worklog 原處更正與凍結
  handoff 的外部回指更正分流寫清楚，解除「既要原處更正、又禁止改檔」的死結。
- `docs/plans/2026-08-20-階段一執行方案.md`：以合法 tracked 修復／worklog 取代 handoff commit，
  並移除 umbrella 前言的舊「三件套」用語；無合法 tracked 產物時明定走人工審核停點。
- `docs/worklog/2026-08-20-handoff工作單位節奏與本地保存更正.md`：保留歷史原文並追加兩則
  證據更正及鐵律 20.3 補記。
- `docs/specs/91-pit-register.md`：登記 workflow-only、script-only 後續包及唯讀 handoff 舊待辦
  的新處置邊界，並把本 worklog 列入附錄 A.1。
- GitHub PR #62 描述：補假設清單、分支命名偏離與綁 initial head 的證據入口；這是 PR metadata，
  不屬 Git diff。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本地最終完整閘門、commit 後 doc-claims、`pr62-last-push..HEAD` 差異復核及三端點／threads
  提交前攝取仍須在本檔入庫後重跑；任何失敗都不得以 initial head 的綠燈覆蓋。
- push 後必須重新取得新 head 的 CI、Codex 與 Claude 證據並逐條回覆／resolve initial-head
  inline；本 PR 修改 `CLAUDE.md`／`AGENTS.md`，即使雙清也屬鐵律 18.3，只能通知使用者人工合併。
- `docs/specs/91-pit-register.md` §3 已登記的 workflow prompt 與 script 契約註釋必須分成後續
  18.3 工作包；本 PR 不宣稱兩者已修。
