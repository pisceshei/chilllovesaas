# 2026-08-28 — S6c-2 商品列表的 Channels 欄與唯讀 popover

🟢 不觸 18.3。teardown＝`docs/research/82` §18；裁定＝D72。

## 已完成的工作 (Done)

- **實測**（全程唯讀、避開禁碰 fixture）：欄序八欄；格＝計數＋hover/focus 露 `˅`
  （整格透明覆蓋鈕、aria 兩態）；popover 唯讀列已發布管道（計數 ⇔ 列數雙列驗證：
  CHOICE 1⇔1、D53-QA 3⇔3）；列 13/20/450、h32、pad6、r8；🔴 **點列導航到管道 admin 首頁**
  （Online Store → /themes 實測）。
- **落地**：`ProductsPage` 查詢＋欄＋`ChannelsCell`＋CSS（token 化）＋i18n 三鍵 ×5；
  測試 +2 格（管道格全流程／零管道空態），10/10 綠。
- 🔴 **落地抓到真缺陷**：popover 點擊冒泡到 onRowActivate ⇒ 開個 popover 就被導去
  商品詳情。修＝格鈕與列鈕 `stopPropagation`（IndexTable select 格既有同構）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `docs/research/82-admin-channels.md` | §18（五小節） |
| `app/frontend/admin/pages/ProductsPage.tsx` | 查詢欄位、`CHANNEL_ROUTES`、`ChannelsCell`、欄定義、deps |
| `app/frontend/admin/pages/ProductsPage.test.tsx` | +2 格 |
| `app/assets/stylesheets/admin.css` | `.cl-chcell*` 一組 |
| `app/frontend/admin/i18n/messages/*.json` ×5 | `products.col.channels`／`products.channels.*` |
| `docs/DECISIONS.md` | D72 |

## 20.2 適用類型／固定處理／反向複驗輸出

| 類型 | 適用 | 固定處理 | 反向複驗 |
|---|---|---|---|
| ① 證據 | 是 | 計數⇔列數雙列驗證；點列行為實測；未取得處標 V（零管道形態、POS/Shop 導航目標、面板精確 shadow） | 82 §18.4 |
| ② 同步 | 是 | 計數判準與詳情頁同一份（handle 非 null）；i18n 五語系同批 | messages 測試綠 |
| ④ 狀態空間 | 是 | 三態：多管道（含 app 濾除）／無路由管道停用／零管道空態，各有測試或登記 | 測試 2 格 |
| ⑤ fail-open | 是 | 冒泡缺陷靠測試炸出（useBlocker invariant）而非人眼；修後同測試轉綠 | 10/10 |

## 尚未完成或需注意的風險 (Pending / TODO)

- **shop 管道無我方頁面** ⇒ 停用列；等管道頁落地時解除（82 §18.5）。
- **零管道形態是我方自定**（本尊未取得，V）。
- **Catalogs 欄**（本尊同構的鄰欄）未做——需要 catalog 功能線（S10）先行。
- 列表查詢每列多抓 publications：50 列 × ≤4 列＝可忽略；若日後列表加分頁大小，注意 N+1（後端 resolver 現況足夠）。
