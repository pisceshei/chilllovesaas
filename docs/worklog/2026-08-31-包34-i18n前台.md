# 2026-08-31 包 34：i18n 前台——三層字串＋內容翻譯＋切換器＋/localization

## 已完成的工作 (Done)

- **三層字串解析**（67 §F.3(a)；SF-5）：`Runtime#build_locale_dict`——③平台字串集
  （B14＝`config/storefront_locales/*.yml`，新 loader `Storefront::PlatformStrings`，
  截尾鏈＋en 兜底、🔴 zh 系不落裸 zh）← ②主題檔（`*.default.json` ← 截尾鏈檔 ← 精確
  locale 檔，逐檔獨立深併——F.3(b)3 兩套語言清單不對稱）。①商家覆寫層未接（見 Pending）。
- **JSONC 容錯補 BOM**（SF-6；F.3(b)1 Ella 實證形態）：`tolerant_json` 加 `﻿` 剝除
  （原本 BOM 開頭的 locale 檔會靜默解析失敗、整頁退回英文——fixture 以真 BOM 檔釘住）。
- **內容翻譯走 drops**（67 §F.3(c)；CT1）：PageRenderer 以 `Translations::Resolve.batch`
  **一次批載**（63 §D.1 N+1 防線）產 overlay，ProductDrop／CollectionDrop 收
  `translations:`（title／body_html；handle 不可翻不在值域）。
- **localization 真值**（67 §F.2；SW1）：`Storefront::LocalizationContext`——切換器
  available_languages **只列當前 presence 開放∧已發布**、position 序、endonym
  （platform_locales）、root_url＝帶前綴（定義上不可能產生 404 連結）；未傳 presence
  （預覽面）維持包 30 合成單語。
- **/localization 端點**（F.2 兩欄位）：country→market（active region）→presence→語言
  （開放∧已發布否則落 presence 預設）；return_to 剝舊前綴＋open redirect 防線；
  裸與帶前綴兩形路由。**帶前綴 cart 路由**同輪收（RoutesDrop 吐 `{prefix}/cart/add`，
  POST 不經 GET catch-all——L4）；cart bundled sections 的 sections_url 剝前綴。
- Rack::Attack cart throttle 擴帶前綴形＋/localization。
- **SF 驗收落格**：SF-1/2/3/5/6/7/10/11②③⑤＋CT1/SW1/L1–L4（13 例）；SF-4＝包 33 S4、
  SF-9③④＝包 32 H5/U7 既有格；SF-8 歸結帳線（步 6）。

## 修改的檔案與核心邏輯 (Changes)

- `config/storefront_locales/{en,zh-Hant}.yml`（新；平台字典，文案自寫——鐵律 9/10）
- `app/services/storefront/{platform_strings,localization_context}.rb`（新）
- `app/controllers/storefront/localization_controller.rb`（新）；`cart_controller.rb`
  （sections_url 剝前綴）；`config/routes.rb`（/localization＋帶前綴 scope）
- `app/liquid/theme_engine/runtime.rb`（build_locale_dict／BOM／web_presence／localization
  真值分流）；`page_renderer.rb`（locale overlay＋web_presence 佈線）；`drops.rb`
  （Product/CollectionDrop translations overlay）
- `config/initializers/rack_attack.rb`；fixture：`locales/en.default.json`＋
  `locales/zh-Hant.json`（真 BOM＋註解＋尾逗號）＋`hero.liquid`（t ×2＋切換器 nav）
- `spec/requests/storefront_i18n_spec.rb`（13 例）

**突變驗證（20.2⑤／20.3）**：
| # | 突變 | 預期紅 | 結果 |
|---|---|---|---|
| MUT-i1 | 字典拿掉平台層 | SF-5 | 1 failure ✅ |
| MUT-i2 | tolerant_json 拿掉 BOM 剝除 | SF-5/SF-6 | 2 failures ✅ |
| MUT-i3 | 切換器拿掉 published 閘 | SW1 | 1 failure ✅ |
| MUT-i4 | return_to 不剝舊前綴 | L1/L2 | 2＋1 failures ✅ |
| MUT-i5 | ProductDrop title 無視 overlay | CT1 | 1 failure ✅ |

## 尚未完成或需注意的風險 (Pending / TODO)

- **①商家覆寫層（THEME_LOCALE_CONTENT）未接**：`Translations::Resolve::RESOURCE_TYPE_BY_CLASS`
  只有 PRODUCT/COLLECTION——擴型別是 ML 線的 schema 決策（Translation::RESOURCE_TYPES
  封閉兩值、批載每型一條 SQL 的推導都要重審），不在本包射程（登記）。
- **`t` 未命中終端仍是 humanized key 尾段**＋`count_miss` 遙測：三層都缺時的輸出，
  嚴格說仍是 key 衍生文字（F.3(a)「絕不輸出 key 名」的邊界情形）——平台層兜底鍵集
  越全越少發生；F.3(a) 的 dev「translation missing:」形態未做。
- **Accept-Language 建議橫幅**（F.2：只影響橫幅與高亮）未做——locale-invariant 片段，
  隨前台 UX 包。**G.2 維度降維**（touched_dimensions 決定 key 維度）未做——v1 key
  恆含 locale/market，正確但快取分片偏多（優化項）。
- 變體選項值／metafield／頁面／主題 section 內容的翻譯 overlay 未做（Fields 值域
  現＝title/body_html/meta_*）；meta_* overlay 由 SEO 步 4 消費時接。
- as_storefront_json（.js 端點）**不吃 overlay**（維持 #204 真店對表的 base 行為；
  本尊 .js 在語言前綴下回不回譯文＝未取證，登記待量測）。
- SF-8（結帳鎖語言＋locale_snapshot 消費）＝步 6 結帳線。
