# Parity R3：設定五頁（應用程式／銷售管道／網域／顧客隱私／custom_data）＋RTE 考證

> 對應規格：docs/specs/71 §E R3、§F（71-R3-STRUCT1/2・MISS1–4・DOC1/2 ✅；V1–V4 遞延）｜
> 實測：測試店五頁全拆（Chrome 中斷後重連續拆）＋隱私三子頁＋custom_data 建立頁＋metaobject 建立頁｜
> help 雙源：3 agent（apps/channels ✅、domains ✅、privacy/custom_data ✅；前一輪工作流 2 agent 結果因
> session 重啟丟失，已重派補齊）｜commit：見本檔所在 commit

## 已完成的工作 (Done)

- **實測補完**：①隱私頁全 IA（隱私權設定三列各帶「自動化」badge；政策＝modal、Cookie 橫幅＝整頁
  （雙 tab＋顏色三檔＋位置五選＝置中/底部全幅/底部置中/左下角/右下角＋結帳頁開關附轉換率警告）、
  退出頁面＝`/settings/privacy/dns` 子頁（導覽選單掛載 Footer menu 建議））；②行銷設定兩列驗明＝
  **跨頁錨點深連結**（結帳 `#marketingconsentoptions`／通知 `customer#optin`，頂欄出現返回 chip）；
  ③custom_data owner 全 15 類（DOM 連結掃描補到螢幕捲動漏掉的「地點／轉移」）；④型別選單 9 組 26 型
  全量；⑤metaobject 建立頁選項五開關（含 help 未載的「顧客帳號 API 存取權」）。
- **🔴 RTE 考證（使用者指令）**：本尊富文本編輯器＝**TinyMCE 6.8.3**（DOM 實證：`window.tinymce`
  6.8.3、editor id `rte-uplift-PRIVACY_POLICY-*`、iframe `tox-edit-area__iframe`、plugins
  `autoresize,lists,table`、原生 toolbar 停用＝工具列自繪）。**6.8.3＝最後 MIT 版**（7.x 起 GPL2+/商業）。
  引擎選型登記開放決策 **B-8**（建議 TipTap 2，理由見 open-decisions），不裁定不動工。
- **原型修復（6 組）**：①設定導覽拆「應用程式／銷售管道」兩頁＋位置對齊＋「中繼欄位與 metaobject」
  改標籤；②應用程式頁（兩鈕＋已安裝 tab＋列 ⋯ 六動作＋詳情六區段 modal＋解除安裝對話框含原因下拉
  與資料保留條款）；③銷售管道頁（新增鈕＋⋯ 解除安裝＝必勾風險確認框才啟用紅鈕）；④網域頁（樹狀
  三列＋連結現有網域分裂鈕＋詳情 modal：DNS checklist／全球六地區點陣＋上次檢查／TLS／指向與類型＋
  變更鈕／網域設定▾）；⑤隱私頁 IA 重建（三列子編輯器＋跨店資料智慧卡＋行銷兩深連結＋託管地點卡；
  GPC 與退出連結文字遷入退出子頁、生成器補 HK PDPO 選項）；⑥custom_data 三層結構（owner 入口→
  per-owner 列表（搜尋＋釘選切換）→編輯器升級（15 owner／單一/清單前綴／類別指派／mixed_reference））
  ＋metaobjDefNew（選項五開關）。
- **limits.yml 兩節**：`custom_data.*`（250／50／20／15）＋`domains.*`（20／1000／60／30／15／48）——
  全部標 help 出處。
- **DOCS**：5 主條目改寫＋14 新條目（set-domain-row／set-cd-owner／sub-cd-owner／set-metaobj-add／
  set-pv-policy／set-pv-intel／set-pv-marketing／set-pv-mkt-checkout／set-pv-mkt-optin／set-pv-hosting／
  set-pv-hosting-loc／sub-pv-banner／sub-pv-optout 等）＋3 舊條目追溯（GPC 遷移／metaobject 用語）。
- **驗證**：lint ERROR 0／WARN 13（＝基線；途中抓到自己一處 1px 硬編當場改 var(--hairline)）；
  localhost 煙霧測試 **18/18**（拆頁導覽／app 詳情與解除安裝／管道風險閘門「未勾禁用→勾後啟用」／
  網域樹與詳情／隱私 IA＋三子頁／owner 15 類／列表搜尋過濾／編輯器新欄位／metaobject 五開關）；
  console 零錯誤。

## 修改的檔案與核心邏輯 (Changes)

- `docs/design/chilllove-admin-v2.html`：設定導覽（拆頁＋改序）；`APPS_INSTALLED`/`DOMAINS_DATA` 常數
  （SET_PAGES 之前，載入期展開）；SET_PAGES.apps/channels/domains/privacy 四頁重寫；`customdataPage()`
  改 owner 入口＋新函式塊（cdOwnerOpen/cdOwnerPage/cdOwnerRowsHtml/cdOwnerRefresh/cdPinToggle/
  metafieldDefFor/metaobjDefNew/appDetail/appUninstall/chUninstall/domainDetail/pvPolicyOpen/
  pvBannerOpen+pvBannerPage/pvOptoutOpen+pvOptoutPage）；`metafieldDef(i,presetOwn)` 簽名擴充＋
  `metafieldSave` 讀 `_ml` 清單旗標＋SETSUB 判斷返回點（不用會殘留的 CD_OWNER）；METAFIELD_DEFS
  owner 正規化（商品→產品）；privacyGenPage 法規基準補 hk。
- `config/limits.yml`：新增 13b 節 `custom_data:`＋`domains:`。
- `docs/specs/71-admin-parity-sweep.md`：§E R3 ✅；§F 追加 71-R3 十二條。
- `docs/handoff/2026-08-12-open-decisions.md`：新增 **B-8 富文本編輯器引擎**（含 TinyMCE 授權事實
  與建議）；頭部索引同步。
- **為什麼詳情用 modal 不用子頁**：本尊 app 詳情是獨立頁、網域詳情是獨立頁——但原型的設定層只有
  「主頁／setSubOpen 次級視圖／openGen modal」三種容器；隱私三件套照本尊形態分配（政策=modal、
  橫幅與退出=子頁），app/網域詳情用 modal 是容器預算內的等價表達，DOCS 已註明本尊為獨立頁。

## 尚未完成或需注意的風險 (Pending / TODO)

- 71-R3-V1：Cookie 橫幅「Cookie 偏好設定」tab 未逐控件展開；GPC 在本尊 2026 主頁未見——GPC 最終
  落點（偏好 tab？併入自動化？）複驗後才能定案。
- 71-R3-V2：metaobject「顧客帳號 API 存取權」help 全站未載——API 契約化前需 dev 文件補證。
- 71-R3-V3：網域模型分歧（本尊 per-domain 三型 vs 我方 62 §J 全域導向開關）——R10 市場輪連
  「各市場網址結構」一起裁定，本輪不動 62 §J。
- 71-R3-V4：全站「商品／產品」用語混用——R12 做對照表一次收，本輪只改了 custom_data owner。
- B-8（RTE 引擎）待使用者裁定；裁定前原型維持 textarea＋工具列殼。
- `custom_data.max_definitions=250` 的口徑（全店 vs 每資源）help 未明說——M1 落表前要複驗，
  limits.yml 已註記。
- per-owner 列表的拖曳排序（本尊有，釘選順序＝顯示順序）原型以釘選鈕近似，排序 demo 未做——
  已在 sub-cd-owner DOCS 註明。
