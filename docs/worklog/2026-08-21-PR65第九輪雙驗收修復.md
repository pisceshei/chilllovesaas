# PR #65 第九輪雙驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `d24239e1c2206bb0e00ce04f8c9716be85eb35b3`：Claude comment
  `5362761839`、wrapper `5362764882`；Codex review `4987956545`／inline `3825788982`。
  quality／test／Claude Review 都 completed success，但兩方均有未清意見，故沒有把 workflow 綠
  誤報成雙清。
- 鐵律 15.2 全量重拉：issue comments 27、reviews 10、paginated inline 32、GraphQL threads 31；
  review body 與 inline 都已攝取。Claude 的兩條 ⚪ 依 17.2 只登記 91 §3。
- 依鐵律 16 以 `git log -p -S CLAIM_COUNT_TYPE` 追 `28602e5`、`3122362`、`b2df242`、
  `eb282ab`：`CLAIM_COUNT_TYPE` 已於第七輪併入現行 `CLAIM_TYPE`，91 新增條目卻未同步名稱。
- failure-first 新增雙區塊 fixture：CLAIM-001 合法，CLAIM-002 只有 `typ: count`；修前 checker
  印 `OK` 且 `FAILURE_FIRST_EXIT=0`，證明檔案級 count canary 不能保證每個活性 CLAIM 都納管。
- 內部契約由 `docs/specs/92-claim-index.md` 與 `docs/dev/m0-review-convergence.md` 互證：本索引只收
  可重算計數，現行唯一 type 是 `count`。因此修法不是猜測 typo 相似度，而是每個合法活性
  `CLAIM-NNN` 都必須至少有一筆可辨識的 type metadata；完全沒有匹配時在標頭行 fail-closed。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 第七輪已處置 `types:` 等 type-like 畸形鍵；本輪 Codex `3825788982` 證明刪成 `typ:` 時仍可在 exact matcher 前消失 |
| 既有防線失效原因 | `count_claim_blocks` 只做檔案級非零 canary；區塊內 `type_entries.empty?` 直接 `next`，另一個合法區塊可遮住缺件 |
| 被漏掉的固定步驟 | 20.2② 只核對 regex 能抓到的 type-like 鍵，沒有逐一要求每個活性 CLAIM 的必要 metadata 完整 |
| 可重跑反向複驗 | `doc_claim_missing_type_metadata` 在修前 exit 0；修後必須 exit 1 且含 `R6 CLAIM-002 缺 type: count metadata`，既有合法／畸形鍵 fixtures 仍各守原方向 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗 |
|---|---|---|
| ① 驗收攝取 | 三個 REST 端點全量分頁＋review body＋GraphQL threads | 四個來源計數與 current IDs 如上，沒有只讀 inline |
| ② producer／consumer／終態 | checker、runner、fixture、92、m0、91 §3／A.1 同步 | `rg CLAIM_COUNT_TYPE` 在修後不得再命中生產識別字敘述 |
| ③ 易腐數字／完成聲明 | 計數只作本輪輸入 snapshot；fixture 下限與 CASES 同 commit 更新 | runner 實際輸出為準，不從舊輪數字外推 |
| ④ 狀態空間／競態 | 合法 type、type-like 畸形鍵、完全未匹配的缺字鍵、合法鍵變體分開 | `types:` 與 `typ:` 各有不同 fixture；合法 `Type:`／`type :` 仍綠 |
| ⑤ fail-open／happy path | failure-first 固定「另一合法區塊遮住缺件」的 production 判定路徑 | 新 fixture 修前 0、修後 1；乾淨 fixture 仍 0 |
| ⑥ workflow 本機假綠 | 本輪不改 workflow；仍跑 parity／workflow syntax 與 GitHub exact-head CI | push 前完整 production-equivalent runner 任何非零即停止 |
| ⑦ Markdown／Windows | 中文路徑、表格直線、UTF-8 與明確 Ruby／Python／Git Bash 入口分開處理 | `git diff --check`、表格結構、doc-claims warning 0 與完整 Windows runner |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：對每個合法活性 CLAIM 增加 type metadata 必填 guard；保留
  `CLAIM_TYPE` 對 `types:` 的原行診斷，不用無界 typo regex 取代結構完整性。
- `spec/fixtures/ci_violations/doc_claim_missing_type_metadata/docs/specs/92-claim-index.md` 與
  `scripts/test-doc-claims-rules.rb`：新增兩區塊 failure-first case，並提高只增不減的 case 下限。
- `docs/specs/92-claim-index.md`、`docs/dev/m0-review-convergence.md`：同步每個活性 CLAIM 的 type
  必填契約、fixture 表與突變清單。
- `docs/specs/91-pit-register.md`：被點名的舊識別字改為 `CLAIM_TYPE`；兩條範圍外觀察只登 §3，
  A.1 補本 worklog。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 最後 repo edit 後仍須從 setup 起跑完整 Windows production-equivalent 閘門；正式 fixture runner
   必須證明新 case exit 1、原合法 cases 不被誤傷，doc-claims warning 必須為 0。
2. commit 後仍須重跑 doc-claims、六條 CLAIM 原文 recheck、鐵律 15.1／15.2，再 push 並重取
   exact-head Claude／Codex；舊 head 的三綠不得外推。
3. #65 修改 `scripts/`，命中鐵律 18.3；即使 exact-head 雙零與 threads 收口，也只能通知使用者
   人工合併，不得由互動式 Codex 代行。
