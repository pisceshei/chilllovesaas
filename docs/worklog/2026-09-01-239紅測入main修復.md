# 2026-09-01 #239 紅測入 main 修復

## 已完成的工作 (Done)

- 修 `spec/graphql/notification_settings_spec.rb`：模板清單斷言由硬編碼三支改為
  對 `Notifications::Catalog::KINDS` 全集斷言（新 kind 自動納入，斷根同型staleness）。
- 事故全記錄＝`docs/specs/91-pit-register.md` §3.58（雙防線失守：管道吞退出碼
  再犯＋合併鏈未閘 CI 判定；固定處理兩條已生效）。

## 修改的檔案與核心邏輯 (Changes)

- spec/graphql/notification_settings_spec.rb（斷言對 KINDS）
- docs/specs/91-pit-register.md §3.58

## 尚未完成或需注意的風險 (Pending / TODO)

- 無，理由：修復＋事故登記＋流程固定處理三件都在本包；main 綠以本 PR 合併後的
  main run conclusion 複驗。
