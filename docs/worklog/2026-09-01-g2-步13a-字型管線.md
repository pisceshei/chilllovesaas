# 2026-09-01 G2 步 13a：字型管線真實作

## 已完成的工作 (Done)

- 三源開場：官方研究（agent，font 三 filter＋font object＋font_picker＋字型庫
  逐字）＋真店親點（chill.deals DOM 抓取 live @font-face 形／body computed
  font／15 faces jost+poppins）＋本地復現（Ella global-style 消費鏈）⇒
  `docs/research/97-theme-engine-fonts-blocks-teardown.md`。
- 字型庫（config/storefront_fonts.yml＋FontLibrary）＋OFL woff2 自 host 八檔＋
  FontDrop 官方七屬性＋font_face/font_url/font_modify 真實作
  （97 §1.2 live 形對錨；缺變體回 nil 官方逐字）。
- 突變 MF1-MF5 全紅＋canary；引擎相鄰 93 例迴歸綠。

## 修改的檔案與核心邏輯 (Changes)

- 新：`config/storefront_fonts.yml`、`app/liquid/theme_engine/font_library.rb`、
  `public/fonts/{jost,poppins}/*.woff2` ×8、`spec/liquid/font_pipeline_spec.rb`（5 例）、
  `docs/research/97-*.md`、本檔＋dev doc。
- 改：drops.rb（FontDrop 重寫——BaseDrop 假 stub → 真七屬性；SettingsDrop
  font_picker → FontLibrary.drop）、filters.rb（三 filter 真實作＋modify_weight
  private 表）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.62 四 ⚪（單 woff2 src／'woff' 同回 woff2／latin subset／庫僅 2 家族）。
- 13b 待做：`{% render child_block %}` parse-fatal（97 §2 已實測 FATAL）＋
  `block.blocks` 巢狀＋色階群組 drop 化（97 §3）。
- 生產煙測待部署（demo body 字型從 Times New Roman 轉 Jost 是本包可見交付）。
