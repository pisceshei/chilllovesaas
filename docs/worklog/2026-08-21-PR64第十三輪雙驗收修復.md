# PR #64 第十三輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `233d021209a89279a96ca6804e1c8030b005b0a7`：Claude 判詞
  `5363469305`、wrapper `5363470866`，為 🟡2／⚪4；Codex review `4988472500` 的 inline
  `3826183941` 指出中央 merge-only witness 漏 `-r`。兩方均有待清意見，沒有把 workflow
  success 或未留言誤報為雙清。
- 依鐵律 16 以 `git log -p` 追第五輪現行 validator、91 §3 與 external-facts B9／B10 沿革。
  production D／R 呼叫與 A 類 canary 原為兩組獨立 pathspec 字面量；只把 production 字面量改成
  `docs/worklogs` 時，production 空輸出被放行，而未改的 canary 仍非空，故 Claude 的事實成立。
- production D／R 與 A canary 改由同一個 `validate_history_status` 入口接收同一 pathspec；正常流程
  與 typo mutation 都重跑完整配對。合法的空 D／R 仍可通過，但同射程 A canary 回空必須非零。
- 91 的 Markdown API 事故保留 request／response 與復發形狀；`-f`／`-F @path` 的官方 URL、逐字
  及證據邊界只留 external-facts B9，消除與終態註釋不符的第二份契約副本。
- Git 官方 <https://git-scm.com/docs/git-diff-tree>（取證 2026-08-21）對 `-r` 的逐字定義為
  "Recurse into sub-trees."。本倉庫 immutable merge 實跑：不加 `-r` 只得到 `M scripts`；加上
  `-r` 才得到目標 `.pyc` 的一筆 `D`。B10 的中央 witness 已改成完整可重跑命令。
- Claude 四條 ⚪ 只登記／去重：P-8 第五次散文 supersede、external-facts 不在 destructive guard
  射程、fail-closed 分支未全做 mutation、A.1 對帳未進 CI。另把第十二輪 worklog 漏 `-r` 的
  未點名同型副本登記到 91 §3，不回寫歷史檔。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 第十二輪 `5363200002` 要求零掃描 canary；本輪 `5363469305` 證明 canary 與 production wiring 未結構綁定；`3826183941` 證明中央 witness 的巢狀路徑命令仍不可重現 |
| 既有防線失效原因 | 兩次呼叫各自手寫相同 pathspec，只測 canary 自己的 typo；fixture 敘事又省略 `diff-tree` 遞迴子樹所需的 `-r` |
| 被漏掉的固定步驟 | canary 必須與 production 呼叫共用同一輸入入口；外部契約的中央 witness 必須逐字複製實跑命令，而非只記結果 |
| 可重跑反向複驗 | 第五輪現行 block 對 `docs/worklogs` 呼叫完整 validation pair，必須命中 `zero-scan producer`；B10 同一 merge 命令移除／加入 `-r` 分別得到頂層 tree／目標檔案 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取 | exact-head issue、review body、paginated inline、GraphQL threads 分源重取 | Claude 兩個 comment ID 與 Codex review／inline 均綁 `233d021…`；舊 head 不外推 |
| ② producer／consumer／終態 | W5 production pair、external-facts B9／B10、91 §3／A.1 與本 worklog 同步 | typo mutation 走 production 共用入口；91 不再保留 B9 第二份逐字；A.1 同 commit 補本檔 |
| ③ 完成性聲明 | 只發布已實跑的 `diff-tree` 對照，不把待跑閘門寫成成功 | `-r` 對照為 `M scripts`／目標 `D`；新 head 29 閘門與 GitHub 驗收仍待取得 |
| ④ 狀態空間 | 正常空 D／R、非空 A、pathspec typo、merge 巢狀／非巢狀分格 | typo 必須非零；不加 `-r` 不得宣稱取得巢狀目標紀錄 |
| ⑤ fail-open | production 與 canary 接受同一個 pathspec 參數，由一個 helper 成對執行 | 只改該參數為 `docs/worklogs` 時，A canary 空輸出必須觸發 `zero-scan producer` |
| ⑥ workflow 本機假綠 | 本輪不改 workflow／scripts／config，且不把 A.1 人工 fence 說成 CI gate | 受限路徑差異必須空；A.1 無 CI 閘門已登 91，不偽裝已補 |
| ⑦ Markdown／Windows | 變更文件做表格直線、圍欄與 GitHub `/markdown` render；最後 repo edit 後跑 Windows gates | 渲染與 29 道閘門待最終編輯完成後實跑，任一非零即停止 |

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-21-PR64第五輪雙驗收修復.md`：production D／R 與 A canary 改走同一
  pathspec helper，zero-scan mutation 也重跑完整 pair。
- `docs/dev/external-facts.md`：B10 新增 Git 官方 `-r` 逐字，中央 witness 補完整 `-r` 命令與
  有／無遞迴的實跑差異。
- `docs/specs/91-pit-register.md`：移除 B9 外部契約副本，去重／新增四條 ⚪、登記未點名同型
  worklog 副本，並補 A.1。
- `docs/worklog/2026-08-21-PR64第十三輪雙驗收修復.md`：保存本輪輸入、查證、20.4／20.3 與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 最後 repo edit 後須實跑第五輪現行 validator、Markdown 結構／GitHub render 與連續 29 道
   Windows production-equivalent gates；任一非零即停止，不得用先前 head 結果代替。
2. commit 後須重跑 doc-claims warning 0 與鐵律 15.1／15.2 全量攝取；若有晚到意見，先處置再
   宣稱 exact-head 雙清。
3. #64 為一般 PR；只有新 exact-head CI、Claude、Codex 雙零、approved threads resolved、
   mergeable 與 evaluator `1111` 同時成立，才可依 D31／D32 代行 squash merge。
