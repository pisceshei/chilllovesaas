# 2026-08-23 — UI-1：design token 單一來源（含 23 號 §1 升版）

## 已完成的工作 (Done)

- 抓出 token 的**三份拷貝已漂移**：`docs/design/23` §1 與 `app/assets/tokens.css`
  停在 2026-07 的估計值（30 顆，`--bg:#f4f4f5`、`--focus:#2a78d6`），原型 `:root`
  已是研究 47／64 的**實測值**（223 顆，`#f1f1f1`、`#005bd3`，含間距七階、字級九階、
  字重四階、導航色、語意 5×5×3、`--hairline:.66px`）。
- 定 producer：原型 `:root` 區塊；`app/assets/tokens.css` 改為機械副本
  （`scripts/sync-tokens.rb` 產生）；23 §1 改為指標＋家族一覽（不再放值）。
- 新增 CI quality 閘門 `scripts/check-tokens-sync.rb`（兩條規則）＋共用擷取邏輯
  `scripts/lib/token_block.rb`。
- 三向 canary 實跑：漂移 rc=1、23 §1 貼回 CSS 宣告行 rc=1、原型缺檔 rc=2、乾淨 rc=0。

## 修改的檔案與核心邏輯 (Changes)

- `app/assets/tokens.css`：由 30 顆換成原型的 223 顆（**嚴格超集**——實跑比對過
  `admin.css`／`application.css` 用到的 token 全部都在新集合裡，故不會有未定義變數）。
- `docs/design/23-interaction-css-spec.md` §1：值區塊 → 指標表＋家族一覽＋dated 裁定註。
- `scripts/check-tokens-sync.rb`：規則 1＝tokens.css 與原型逐位元組相同；
  規則 2＝23 §1 不得出現**行首**的 CSS 宣告。
- `scripts/sync-tokens.rb`／`scripts/lib/token_block.rb`：同步端與擷取邏輯。
- `.github/workflows/ci.yml`：quality job 新增一步。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **本包只換了 token 的值，沒有改任何元件 CSS**。`admin.css` 仍有大量裸值
  （`#3f4144`／`#ededee`／`6px 10px`／`52px`），它們現在**有對應的 token 了**
  （`--text-nav`／`--surface-hover`／`--sp-150 --sp-200`／`--topbar-h`）但還沒替換。
  下一包（UI-2 側欄對齊）會連同處理側欄那一段；其餘元件段落待排。
- 實作端 CSS 的「不得出現裸值」尚未機械化（原型端有 `lint-prototype.py`）。登記 91。
- 兩個判準的教訓已寫進註釋：①「全文不得出現 X」會被寫下這條規則的那句話推翻
  ⇒ 判準改行首；②Ruby 在 Windows 是 text mode，讀寫兩次翻譯互相抵銷，
  「逐位元組相同」會在行尾其實不同時全綠 ⇒ 一律 binread／binwrite。
