# PR #64 第四輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收基準固定為 head `eb1afbadea06e2fbf042e39e02f0e079aeef13d5`。鐵律 15.2 三端點
  全量重取後，Claude issue comment `5359997378` 有一條 🟡；Codex review `4985973166`
  的 current-head inline 為 `3824178998`。Claude Review run `32401231340` completed success，
  沒有 validation skip；CI 的 quality／test 也對該 head completed success。
- 依 `git log -p --follow` 確認 P-8 worklog 的 Changes 終態更正於第二輪只列到
  `docs/worklog/2026-08-21-PR64第二輪雙驗收修復.md`；本輪只補入第三輪 worklog，並明示其
  `docs/specs/91-pit-register.md` A.1 索引，沒有改寫歷史 Done 敘事。
- Claude 點名 PR 描述的「累積變更」不是全集。該描述屬 GitHub 外部狀態，不在 repo commit；
  push 後以 `gh pr diff 64 --name-only` 取得新 exact-head 集合，逐檔列入描述，不再用「重點」
  冒充完整清單，也不拿本輪 push 前的舊集合外推新 head。
- 本輪四條範圍外觀察只登記到 91 §3：歷史層原文不改與刪除非必要數字的規則衝突、A9 程式碼
  引用改排、首組 pageInfo 的更強停止邊界，以及首輪事後 20.3 未逐列交代 ④～⑦；沒有擴修
  那些未被阻擋意見點名的實物。
<!-- 🔴 2026-08-21 取證補註（Codex inline `3824363616`）：上句「四條」只描述 immutable
commit `7c10b6a` 的輸入快照；複驗為 `git diff --unified=0 eb1afba 7c10b6a -- docs/specs/91-pit-register.md | grep -E '^\+- \*\*'`，不得外推後續 HEAD。 -->

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取 | current head 三端點全部 `--paginate`，逐則讀 review body 與 inline | 上列 issue comment／review／inline／run ID 均對應 `eb1afbadea…`；review run completed success |
| ② 生產者與消費者同步 | 以 PR64、第三輪 worklog、A.1 與累積變更反查 P-8 Changes、91、PR 描述與本 worklog | P-8 終態更正已列第三輪及 A.1；第四輪 worklog 已進 91 A.1；PR 描述列為 push 後動作 |
| ③ 完整性聲明 | PR 描述的檔案全集只能由新 exact head 的 `gh pr diff 64 --name-only` 產生 | push 前不發布新 head 檔數；push 後逐檔對照，缺一即不送驗 |
| ⑦ Markdown／歷史層 | 歷史 Done 不靜默改寫；終態 Changes 直接回寫；範圍外規則衝突只登 §3 | `git diff --check` 須為零；本 worklog 不發布未保存 response 的 GitHub Markdown 衍生計數 |

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-P8證據來源與合併後文件債收斂.md`：終態 Changes 補齊第三輪
  worklog 與其 A.1 關係。
- `docs/specs/91-pit-register.md`：只登記 Claude 本輪被列為範圍外的觀察，並同步 A.1。
- `docs/worklog/2026-08-21-PR64第四輪雙驗收修復.md`：保存本輪三端點、沿革查證、20.3 與待辦。
- PR 描述（GitHub 外部狀態）：push 後以新 exact head 的 name-only 集合逐檔重建累積清單。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本段定稿後仍須從 setup 跑完整 Windows production 等價閘門；任一非零即停止。其後才可
   commit、跑 commit 後 doc-claims、執行鐵律 15、push 與更新 PR 描述。
2. push 後須強制移動 `pr64-last-push` 到新 head，重新取得 CI、Codex、Claude、unresolved
   threads、mergeability 與 evaluator；舊 head 的 completed success 不得外推。
3. #64 是一般 PR；只有 exact-head CI、Codex、Claude 雙零、threads resolved、mergeable 與
   evaluator `1111` 同時成立，才可依 D31／D32 代行 squash merge。
