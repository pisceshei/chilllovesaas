# worklog 2026-08-24 — D13 與 on_hand 公式的文件對帳

## 已完成的工作 (Done)

- `docs/specs/71-admin-parity-sweep.md` 的 **71-R8-V4** 由 ⬜ 改 ✅：指向 `docs/DECISIONS.md` D13
  （已裁定採 sources 模型），並補記 2026-08-24 實機複驗結果與「UI 四型 ≠ 資料模型四型」的區分。
- `docs/specs/84-m1-gate-triage.md` 的 **B-4** 標記作廢（刪除線 ＋ 紅字說明原判為何是錯的分類）。
- `docs/plans/2026-08-24-三方向執行順序.md` §6 的三條「未解問題」全部更新：
  第 1 條作廢（早已裁定）、第 2 條改寫（不是二選一，是 schema 錯）、第 3 條補上本尊的明確答案
  與「與第 2 條共用同一張表」的警告；並在 §6 開頭加上 migration 閘門與「每步可見」約束解除的更新。

## 修改的檔案與核心邏輯 (Changes)

- `docs/specs/71-admin-parity-sweep.md`：R8-V4 一列。
- `docs/specs/84-m1-gate-triage.md`：B-4 一列。
- `docs/plans/2026-08-24-三方向執行順序.md`：§6 三條 ＋ 開頭警語。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **`13` §F4 與 `config/limits.yml` 的五處模型形狀尚未修**——那需要先確認 `CollectionSource`
  家族的確切型別名與 exclusion 區塊語義，而該值域正由 enum 導出研究處理中。
  本輪刻意只做「不依賴新事實」的對帳，避免把未複驗的型別名寫進 limits.yml。
- 🔴 **`limits.yml` 既有的數字上限不得被覆蓋**：`max_with_variants_per_shop: 100`／
  `max_containing_collection_per_shop: 50`／`max_excluding_collection_per_shop: 5` 等都帶 help 逐字出處（P23），
  與研究提出的 per-source 數字**不是同一個口徑**。修形狀時只動形狀鍵，不動這些。
- 本輪暴露的模式值得記住：**D13（裁定）與 71-R8-DOC1（on_hand 公式）都是「記錄了但沒被下游吸收」**。
  兩者都不是研究缺口。專案目前沒有任何機制保證「裁定 → 規格 → schema」這條鏈不斷。
  這一條是觀察，不是本輪的待辦。
