# PR #62 第二輪驗收修復

## 已完成的工作 (Done)

- 對 head `90d38c58d39f07f9d86aff1455967123bbafe4f5` 全量重取
  `issues/62/comments`、`pulls/62/reviews`、`pulls/62/comments` 與 GraphQL `reviewThreads`。
  Codex review `4983038329` 的未清輸入為 inline `3821829610`；Claude Review run
  `32372029926`／job `96434580654` 實跑完成，通過判詞為 issue comment `5356457527`，其 ⚪
  與該 Codex inline 是同一個 REST 枚舉缺口。
- 依歷史層規則保留首輪 worklog 原句，緊鄰追加日期更正；明列三個 REST endpoint，並提供包含
  `pageInfo { hasNextPage endCursor }` 的 paginated GraphQL `reviewThreads` 完整重取命令。首輪
  worklog 末項原有的「輸入全集可由首項重取」因此已有實際入口，不再互相空回指。
- 已通讀本 worklog；輸入集合就是上述 Codex inline 與 Claude comment，且 Claude 明定兩者同件
  事、隨 inline 修復即收口，因此本輪沒有另增 `91` §3 暫存項。本檔同步列入附錄 A.1。

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-PR62首輪驗收修復.md`：保留歷史原文並追加 REST／GraphQL 全集與
  可執行重取命令。
- `docs/worklog/2026-08-20-PR62第二輪驗收修復.md`：保存本驗收修復輪的 exact-head 輸入、修法、
  射程與待辦。
- `docs/specs/91-pit-register.md`：只把本 worklog 列入附錄 A.1；未把已直接修復的同一問題重複
  登記成暫存坑。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本輪 staged patch 已實跑完整 29 閘門且 29/29 通過；commit 後仍須重跑 doc-claims、鐵律 15
  兩點 diff 與四個 GitHub 取證入口。`90d38c5` 的既有遠端綠燈不得外推到下一個 head。
- push 後須回覆並 resolve inline `3821829610`，再取得下一個 exact head 的 CI、Codex 與 Claude；
  本 PR 屬鐵律 18.3，雙清後仍只能通知使用者人工合併。
