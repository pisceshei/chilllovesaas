# 2026-09-01 Ella 修復 PR-6：編輯器 block 級操作

## 已完成的工作 (Done)

- 六軸診斷 editor 軸 critical ②收口（「功能 block 沒做」的直接回應）：
- 後端 `sectionSchemas` 補 **blocks 面**：本地 block defs（type/name/limit/
  settings 經 translate_defs）＋`max_blocks`＋🔴 `@theme` 展開 blocks/*.liquid
  全集（24 §2.4）；`@app` 跳過（無 app 層）。E8/MB6-1 紅證。
- 前端 block 級全操作（24 §3 同語義向下一層，全走 applyOp band 快照棧＝
  可 undo）：樹 block 列可選取（帶+section+block 三元選中）→ **block 設定
  面板**（def 驅動 SettingControl＋fallback）；add-block（＋按鈕 per def、
  尊重 max_blocks；🔴 新 block 帶 def defaults——MB6-2 紅證）；remove／
  上下移；setBlockSetting（MB6-3 紅證）。
- ED12 全流紅證：選 block→改值→add-block→save payload 兩 block 齊
  （原改值＋新帶 default）。
- 突變 3/3 殺。

## 修改的檔案與核心邏輯 (Changes)

- 改：theme_type（block_defs_for/theme_block_defs）、ThemeEditorPage
  （block ops＋樹 block 列＋設定面板 block 分支）、editor 測試 11→12
  （ED12；ED1 補 block 節點）、bootstrap spec 7→8（E8）、promo/blocks-iter
  fixture（本地 blocks 定義＋@theme）、i18n 3 鍵 ×5、admin.css。

## 尚未完成或需注意的風險 (Pending / TODO)

- block 巢狀（block 內 block——Ella _parent/_leaf 兩層）樹只展開一層；
  toggle block disabled 未做（本尊 block 也有眼睛）——隨 PR-8 UI 對位補。
- PR-7 即時預覽次之。
