# 2026-08-20 — PR #63 第二輪 Codex 驗收修復

## 已完成的工作 (Done)

對 exact head `665e9030215cde6ae18965f833a8f009df8bf8df` 的 Codex review
`4984304467`，已全量拉取 issue comments、review body、paginated inline 與 GraphQL threads；
外層與各 thread 的 `hasNextPage` 都是 false，新輸入只有兩條 unresolved inline：

1. `3822823460` 指出 91 附錄把 D36 寫成此樹已有的現行決策。以
   `git grep -n D36 HEAD -- docs/DECISIONS.md AGENTS.md CLAUDE.md` 與同式查
   `origin/main` 均為空，另以 `git show 5209087:docs/DECISIONS.md` 才找到 D36，證實驗收方的
   事實層成立。已把附錄改為使用者 2026-08-20 裁定，並明載 D36 尚只在未合併 PR #62 head
   `5209087`，避免本 PR 讀者尋找不存在的 current-tree 決策。
2. `3822823484` 指出初始 worklog 的 code-derived 驗證數字沒有相鄰 snapshot 邊界。數字本身
   來自實跑，但不能因此外推為後續 head 現值。已在數字前追加日期更正，將整段明確綁定
   2026-08-20（Asia/Taipei）與 immutable head `ec4eb1d7f4c91cb2ca091bc3062340fe15028cf7`，
   並明載不得跨 head 沿用；歷史原文保留。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `docs/worklog/2026-08-20-Claude-Fable5額度回退Opus5.md` | 在驗證輸出前加日期／immutable-head snapshot 邊界，不改歷史數字 |
| `docs/specs/91-pit-register.md` | 將 D36 標成未合併 PR #62 head 的條件性引用，並補列本輪 worklog |
| `docs/worklog/2026-08-20-PR63第二輪驗收修復.md` | 記錄 exact-head 輸入、沿革查證、兩項精準修復與下游邊界 |

## 尚未完成或需注意的風險 (Pending / TODO)

1. 這些 tracked 變更仍須從 setup 起跑完整 production 等價閘門、commit、commit 後 doc-claims
   warning 0，再 push 一次新 HEAD；不得沿用 `665e903` 的 CI 或 Codex review。
2. 新 HEAD 要重新取得 CI、Claude validation-skip 原文與只召喚一次的 Codex review；若產生新
   inline，仍須四端全量拉取後逐條查證，不能只讀 review 摘要。
3. 本 PR 修改 workflow，命中鐵律 18.3；即使新 HEAD 驗收收斂，仍只能通知使用者人工合併。
