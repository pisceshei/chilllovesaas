# 2026-08-28 — D69 rem token 靜態完整性檢查落地

D68 裁定②的後半（B 道）。🔴 **觸鐵律 18.3**（`scripts/` ×2 ＋ `ci.yml` ＋ `config/ci.rb`）
⇒ 人工審閱與人工合併，不走 D40 自合。

## 已完成的工作 (Done)

- `scripts/check-rem-tokens.rb`（規則、射程、退出碼三分、誠實聲明見檔頭與 D69）。
- `scripts/test-rem-token-rules.rb` ＋ `spec/fixtures/rem_tokens/` 7 份 fixture，8 格全過。
- 接線 `ci.yml` quality job ＋ `config/ci.rb`（`check-ci-parity` 通過；執行位元已設）。
- **五個活突變全部轉紅**（M1 判定反轉／M2 canary 刪除／M3 刪一族／M4 不剝註釋／M5 exit 2→0），
  突變後 checker 以備份還原、`diff` 確認無殘留。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `scripts/check-rem-tokens.rb` | 新增（100755） |
| `scripts/test-rem-token-rules.rb` | 新增（100755） |
| `spec/fixtures/rem_tokens/*.html` | 新增 7 份 |
| `.github/workflows/ci.yml` | quality job 加兩步（Check rem token integrity／Regression-test rem token rules） |
| `config/ci.rb` | 對應兩步（同批，維持 ci.yml ⊆ ci.rb） |

## 20.2 適用類型／固定處理／反向複驗輸出

| 類型 | 適用 | 固定處理已跑 | 反向複驗輸出 |
|---|---|---|---|
| ⑤ fail-open／自我證明閘門 | 是 | 正常（clean=0）／違規（px、em=1）／輸入缺失（no_root=2）／**零掃描 canary**（family_empty=2）四類 fixture 齊；**五個活突變逐一實跑** | 五個突變全轉紅；真 producer 過 |
| ② 生產者／消費者 | 是 | ci.yml 與 config/ci.rb 同批改（同步條款） | parity exit 0 |
| ③ 易腐數字 | 是 | 顆數（13／8）只出現在 OK 訊息（每次重算），文檔不寫死 | — |

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **消費端硬編 px 本檢查看不到**（`91` §3.42 W-3）——擾動法未機械化。
- fixture 的 `:root` 形態是手寫簡化版；若日後 `TokenBlock.extract` 的邊界規則改變
  （例如允許巢狀 `}`），fixture 要跟著複驗——回歸表末的「真 producer 必過」是這條的哨兵。
