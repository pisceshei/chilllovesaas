# PR #64 第十二輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `67c9bcbe245e7f09ebf8a5e6017baab0b0078771`：Claude 判詞
  `5363200002`、wrapper `5363201431`，為 🟡1／⚪4；Codex review `4988295763` 的三則 inline
  為 `3826028167`、`3826028172`、`3826028174`。同 head quality／test／Claude Review 均
  completed success，但兩方都有未清意見，沒有把 workflow 綠誤報成雙清。
- 依鐵律 16 用 `git log -p` 追第五輪現行 validator、P-8 終態 pointer、第十一輪 20.4／20.3、
  external-facts 與 91 §3 沿革。Claude 的事實成立：正常 D／R 輸出本來為空，把 pathspec 改成
  `docs/worklogs` 仍是空且 exit 0；兩條歷史 fixture 的非空 canary 使用不同 range／pathspec，
  不能證明生產呼叫有掃到輸入。
- failure-first 對 immutable merge `59cfaf44bd2d71cef6d54d8e1b63aa8b8b602890` 與 first parent
  `76751e4162a79bbb28860b545e673ee1d9ee1bea` 實跑：目標
  `scripts/__pycache__/lint-prototype.cpython-311.pyc`（current tree 已刪除，只存在於上述
  immutable merge 歷史）在舊 range log 出現 1 次；加
  `--diff-merges=separate` 後為 2 次，`git diff-tree -m` 的 merge-only witness 為 1 次。這證明
  Codex 所述 merge diff 漏掃成立，且可用倉庫既有物件承重，不需假造 fixture。
- Git 官方 <https://git-scm.com/docs/git-log>（取證 2026-08-21）逐字包含
  "merge commits will not show a diff"；`separate` 定義為
  "Show full diff with respect to each of parents."。現行 producer 明示
  `--diff-merges=separate`，merge fixture 要求上述目標恰出現 2 次，移除選項就非零。
- destructive guard 新增同 base／HEAD／`docs/worklog` 的 A 類非空 canary；pathspec typo mutation
  同樣走 `history_status_for`，必須命中 `zero-scan producer`。D／R 本身仍允許空，不能把合法零值
  與零掃描混為一談。
- P-8 的兩處 historical rendering 句各加相鄰日期更正：未保留 exact request／response 的舊
  Boolean 與計數全部撤回為「未取得」，不以後來 current response 反向證成舊 staged patch。
- GitHub CLI 官方 <https://cli.github.com/manual/gh_api>（取證 2026-08-21）重新取得 `-f` 為
  static string、`-F` 的 `@path` 才讀檔；可重用契約集中於 `docs/dev/external-facts.md` B9，
  merge diff 契約集中於 B10，91 事故條目只留復發形狀並回指 B9。
- Claude 四條 ⚪ 沒有擴修：pointer 第四次 supersede 只補既有條目來源；correction validator
  讀工作樹、tracked worklog 的最終證據 bootstrapping、第三輪／P0 rendering 歷史副本都只登
  91 §3。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 第九輪 `3825320726` 指出 D／R 狀態漏分；第十一輪 `5362492718`／`3825640332` 指出 producer 仍走 endpoint；本輪 `5363200002`／`3826028167` 再指出合法空值遮住零掃描與 merge diff 漏掃 |
| 既有防線失效原因 | 真實 deletion／rename fixtures 證明的是另外兩組 range／pathspec；正常 D／R 期望空，沒有 paired nonempty canary；`git log` 未明示 diff-merges 時又不輸出 merge diff |
| 被漏掉的固定步驟 | 預期空的 producer 必須有同 range／pathspec 的非空 canary；歷史 destructive guard 必須逐列 merge 狀態；兩者都要有從 production helper 入口開始的反向案例 |
| 可重跑反向複驗 | 第五輪 worklog 的第十二輪現行 block：A 類 canary、`docs/worklogs` typo mutation、真實 deletion／rename guard、immutable merge multiplicity 一起執行，任一漏件即非零 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取 | exact head 的 issue、review body、paginated inline、GraphQL threads 分源全量重取 | Claude 兩個 ID 與 Codex review／三 inline 均綁 `67c9bcb…`；舊 unresolved threads 不算新意見 |
| ② producer／consumer／終態 | W5 producer、P-8 pointer、W11 更正、external-facts、91 §3／A.1 與本 worklog 同步 | pointer 指第十二輪唯一 heading；B9／B10 各有官方來源；A.1 同 commit 補本檔 |
| ③ 完成性聲明 | 不發布未留存 render 結論；舊 head workflow 綠不外推 | P-8 兩處相鄰更正明標未取得；新 head 結果仍待取得 |
| ④ 狀態空間 | 正常空 D／R、非空 A canary、delete、rename、merge、pathspec typo 分格 | typo 必須轉紅；merge fixture 目標必須恰出現 2 次；正常 D／R 可空但 canary 不准空 |
| ⑤ fail-open | 同 range／pathspec 非空 canary 與 production helper mutation 成對 | `docs/worklogs` 回空 exit 0 的 failure-first 不再能通過 `require_nonempty` |
| ⑥ workflow 本機假綠 | 本輪不改 workflow／scripts／config，以有 canary 的 pathspec 證明不適用 | commit 後受限路徑差異必須空，移除 pathspec 的同範圍差異必須非空 |
| ⑦ Markdown／Windows | historical correction、表格、圍欄與 UTF-8 中文檔名分別複驗 | GitHub current render、表格直線與 29 道 Windows gates 待最後 repo edit 後實跑 |

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-21-PR64第五輪雙驗收修復.md`：第十二輪現行 validator 加 merge diff、
  同射程 A 類 canary、pathspec typo mutation與 immutable merge multiplicity fixture。
- `docs/worklog/2026-08-20-P8證據來源與合併後文件債收斂.md`：兩處 historical render 結論
  相鄰撤回，terminal pointer 與 guard 說明同步第十二輪。
- `docs/worklog/2026-08-21-PR64第十一輪雙驗收修復.md`：原處更正「任一 producer 空」的過度
  概括，明列合法空 D／R 必須配對同射程非空 canary。
- `docs/dev/external-facts.md`：新增 B9 `gh -f/-F @path` 與 B10 Git merge-diff 官方契約。
- `docs/specs/91-pit-register.md`：回指 B9、登記四條 ⚪、補 current Codex rendering 來源與 A.1。
- `docs/worklog/2026-08-21-PR64第十二輪雙驗收修復.md`：保存本輪輸入、獨立查證、failure-first、
  20.4／20.3 與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 最後 repo edit 後須實跑第十二輪 validator、表格直線、GitHub current Markdown render 與連續
   29 道 Windows production-equivalent gates；任一非零即停止。
2. commit 後須重跑 validator、doc-claims warning 0、鐵律 15.1／15.2 全量攝取；若有晚到意見，
   必須先處置，不能把本 worklog 當成雙清證據。
3. #64 是一般 PR；只有新 exact-head CI、Codex、Claude 雙零、threads resolved、mergeable 與
   evaluator `1111` 全成立，才可依 D31／D32 代行 squash merge。
