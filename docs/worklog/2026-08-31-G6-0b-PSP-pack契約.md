# 2026-08-31 — G6-0(b)：PSP pack 契約落地（65 §D 第三格式＋airwallex/paypal 真 pack）

## 已完成的工作 (Done)

- 🔴 **Airwallex 金額格式更正（一手複驗，取證 2026-08-31）**：wire form＝十進位主單位
  **JSON number**（data_types 逐字 "$9.99 is represented as 9.99"＋create schema
  `amount: number`）——推翻 69 號（2026-08-12）的 decimal_string 結論；69 號依 19.5
  追加有日期更正，65 §D.4 實證表更正並補 PayPal 列（value＝string、pattern 逐字）。
- **65 §A 增 R7（`Money::PspNumber`，`amount_format: decimal_number`）**＋§B X7c/X8c
  ＋§C 後綴 `_psp_number`＋§D.1 分流＋§D.2 **A6c**（decimal 側的 A3 等價物：cents 無法
  無損表達為生效位數 ⇒ raise 不 round——收掉 A6b 自註的那條不對稱）＋**A7**
  （per-currency 位數覆蓋；PayPal HUF/JPY/TWD＝0 位「帶小數即 error」官方逐字、
  Airwallex payments 零小數 20 幣逐字清單——🔴 兩家都覆蓋 ISO：HUF 在 ISO 是 2 位）
  ＋§D.5 追記（當年留白的 sub-2 位＝今日 A7+A6c 落法）＋§H T20–T24＋§L **V-132 結案**。
- **Money 實作**：`PspNumber` 值物件（三條 Data 後門全關）；`to_psp_amount` 三分流＋
  `roundtrip_selfcheck!` 掛上轉換路徑（原 TODO 收掉）；`declared_places_with_guard!`
  （A7+A6c）；`from_psp_amount` 唯一入向閘門（形態不符宣告＝TypeError、**Float 恆拒**
  ——JSON.parse 必須 decimal_class: BigDecimal）；PspDecimal#to_storage 修「無小數點
  字串被 `split('.')` 誤數成 4 位」＋per-currency 位數。
- **Psp::Pack**：`decimal_places_for`（覆蓋優先）＋`validate_decimal_places!` 共用驗證
  ＋覆蓋值域 0..2 載入即拒（>2 等到轉換層會 `% 0` ZeroDivision）；amount_value_class
  三型；`decimal_places_overrides` 進 upcase 正規化清單。
- **limits.yml**：`psp_amount_formats` 三值；`psp_pack_required_keys_when_decimal_number`；
  兩格式必填 `decimal_places_overrides`（空表＝已查證無例外）；`test_matrix_amount_formats_required`
  三值；**真 pack `airwallex`（decimal_number＋零小數 20 幣覆蓋）／`paypal`
  （decimal_string＋HUF/JPY/TWD 0 位）**，逐值帶一手出處＋取證日期，`enabled: false`
  （enable 時點＝G6-1/G6-2 adapter＋sandbox 端到端後）；`enable_gates_registered` 清空
  （V-132 結案）。
- **測試**：矩陣 spec 增 T20–T24＋A7/A6c＋from_psp_amount 閘門格（100 examples 全綠）；
  fixture packs 增 number_two／zero_override_string／zero_override_number；invalid 增
  missing_places_overrides／override_too_many_places。
- **突變輪 MUT-A~G 全紅後復原**：拔 A6c／to_psp_number 直送儲存值（selfcheck 亦鳴）／
  入向收 Float／split 回舊寫法／覆蓋表被忽略／拔覆蓋值域驗證／矩陣格式清單塞假格式。
- 消費者同步（20.2②）：15／58／63／62／90-blueprint 各補有日期更正註；
  docs/dev/m1-money-unit-boundary.md 全面同步（含兩條 TODO 收掉）。

## 修改的檔案與核心邏輯 (Changes)

- `docs/specs/65-money-unit-boundary.md`（R7/A6c/A7/X7c/X8c/§D.4/§H/§L 全套增修）
- `docs/research/69-cross-platform-practice.md`（V-188 有日期更正）
- `config/limits.yml`（0b/0c 區塊；真 pack 兩枚）
- `app/models/money.rb`（PspNumber/from_psp_amount/selfcheck/A6c/A7）
- `app/models/psp.rb`／`app/models/psp/pack.rb`／`app/services/psp/base_adapter.rb`
- `spec/models/psp_amount_matrix_spec.rb`／`money_spec.rb`／`psp/registry_spec.rb`
- `spec/fixtures/psp_packs/{matrix,invalid}.yml`
- `docs/specs/{15,58,62,63}`＋`docs/research/90-*`＋`docs/dev/m1-money-unit-boundary.md`（更正註）

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **CLAUDE.md 鐵律 3 摘要句仍寫「兩種都存在／Airwallex＝字串」**＋
  `scripts/check-money-boundary.rb` C2 後綴白名單未含 `_psp_number`——兩者依 D46 判例
  歸 **18.3 人工合併 PR（PR B）**，本包不動規範本文與 scripts/。C2 現況 fail-closed
  （誤擋不誤放），G6-1 adapter 落地前 PR B 必須先合入。
- Airwallex sandbox charge＋webhook 實測（V-132 結案的最後一哩）＝G6-1 enable 前必做；
  `env` demo/sandbox 文檔矛盾一併實測定值。
- 65 §K.8–9 可觀測（轉換結構化日誌＋P1 告警）仍未實作——G6-1 adapter PR 前必落
  （dev 篇章既有 TODO，本包未動）。
- PayPal 25 幣支援清單未入 pack（pack 不管「支援哪些幣」只管「位數怎麼算」；
  幣別可用性走 G6-3 的 provider capability 面）。
