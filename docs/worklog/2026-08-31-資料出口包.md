# 2026-08-31 — 資料出口包（A′5–7）＋json parity

## 已完成的工作 (Done)
- 順序分析後六項計畫的第 1–5 步（第 0 步對表併入 #203 後續、第 6 步 cart 另包）：
- 實測（83 §12.4）：S9-CAP 佈置（category＋fecify.product_id metafield＋
  雙系列一顯一隱）後真引擎逐格——category 四屬性、collections 管道過濾
  排除格（**先存成員再重測**，乾淨）、metafields 三層契約與 json 黑名單、
  admin 佈置面三發現（s-* shadow DOM／+Exclude 一級卡／加品即時提交）。
- 實作：JsonSerializer（drop 感知 `| json`；黑名單＋to_s 兜底）；
  CategoryDrop／MetafieldDrop／MetafieldNamespaceDrop／MetafieldsRootDrop；
  ProductDrop.collections（memberships ∩ published_on＋published_at 一次撈）／
  .metafields／.category／.published_at；Variant name/public_title＋
  selling-plan 變體級 stub；as_storefront_json 全家（product 25 鍵、
  variant 21 鍵、option 壓平、collection 9 鍵、image=ours 登記）；
  Metafield／MetafieldDefinition 薄模型；renderer 佈線 publication。
- spec：C1–C2／MF1–MF2／CT1／J1–J5（46 examples 全綠）；突變 O1–O5 全紅
  ——O4 首輪假綠暴露「濾鏡 wiring 未測」，補 J5（Liquid 渲染面）後轉紅
  （20.2⑤ wiring 教訓的又一實例）。

## 修改的檔案與核心邏輯 (Changes)
- `app/liquid/theme_engine/json_serializer.rb`（新）＋ filters.rb json 改線。
- `app/liquid/theme_engine/drops.rb`：四個新 drop＋三出口＋json 面。
- `app/liquid/theme_engine/page_renderer.rb`：publication／collection
  published_at 佈線。
- `app/models/metafield.rb`／`metafield_definition.rb`（新，讀取面薄模型）。
- `spec/liquid/drops_spec.rb`＋`docs/research/83` §12.4。

## 尚未完成或需注意的風險 (Pending / TODO)
- 帶圖商品 image-json 形未測（ImageDrop json＝ours；探針對表待補）。
- category.name 需 taxonomy 名稱字典（平台字典表候選，鐵律 2 ③ 程序）。
- product/collection 的 published_scope 語義、sort_order "most-relevant"
  新值域對表——隨系列前台包。
- 下一步＝cart 端點包（specs/15 F1–F3 已預裁；含 bundled section rendering
  POST 面）。
