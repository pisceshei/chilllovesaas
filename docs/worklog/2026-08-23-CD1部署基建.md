# 2026-08-23 — CD-1 部署基建（walking skeleton 管線）

## 已完成的工作 (Done)

- bt3 伺服器環境建置（apt）：MySQL 8.4.10／nginx 1.28.3／Node 22.22.1／編譯依賴；
  rbenv＋ruby-build 裝 Ruby 3.4.10（`/opt/rbenv`，對齊 `.ruby-version`）。
- 拓撲實測並登記（`docs/specs/120` §1）：WAN 80／443 由前置機器佔用，bt3 對外
  HTTP 入口待使用者給一條路由器轉發或前置 vhost。
- `production.rb` 加 `DISABLE_FORCE_SSL` 開關（fail-secure，註釋寫明為何兩個旗標
  必須同開同關）。
- `scripts/deploy.sh`（七步冪等部署＋`/up` 健檢）、`config/deploy/` 兩配置檔、
  `docs/specs/120-deploy-pipeline.md`（CD-1 版）。

## 修改的檔案與核心邏輯 (Changes)

- `config/environments/production.rb`：assume_ssl／force_ssl 改 `ENV["DISABLE_FORCE_SSL"] != "1"`。
- `scripts/deploy.sh`：新增；取碼（detach checkout）→bundle→pnpm＋assets→db:prepare→
  同步 systemd／nginx→重啟 puma→健檢，失敗非零退出。
- `config/deploy/chilllove-puma.service`：新增；EnvironmentFile 注入秘密，Solid Queue in puma。
- `config/deploy/nginx-chilllove.conf`：新增；80 反代＋`/vite/` immutable 靜態直出。
- `docs/specs/120-deploy-pipeline.md`：新增；拓撲／組件／env 契約／程序／待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

- ⏸ 對外入口待使用者輸入（120 §1）；TLS／異地備份／CD-2 Actions 觸發／CD-3 rollback
  演練全列 120 §6，各標缺什麼。
- `DISABLE_FORCE_SSL=1` 是明文過渡態：登入 cookie 在 WAN 上未加密——**只用假資料的
  demo 店可接受，接真資料前必須先完成 TLS**。
- deploy.sh 的 `nginx -t` 失敗會中止但不還原已 install 的配置檔（下次成功部署會覆蓋；
  單機 walking skeleton 接受此殘留）。
