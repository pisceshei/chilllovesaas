# 本機環境部署與 M0 分支全套實測（Windows 開發機）

> 對應規格：docs/dev/m0-rails-skeleton.md §開發環境需求 ｜ 驗證對象：`m0/rails-foundation` @ `00bb928`

## 已完成的工作 (Done)

- **環境部署（全部免管理員）**：
  - Node 22.17.1（fnm 1.39 via winget，user-scope；PowerShell profile 已加 `fnm env --use-on-cd`，自動吃 `.node-version`）＋ corepack/pnpm 11.16.0（照 `packageManager` 欄位）。
  - MySQL 8.4.9：**發現早已裝在 `C:\Program Files\MySQL`**（winget 判定已安裝）但從未初始化——以使用者空間 datadir（`C:\Users\pisce\chilllove-mysql\data`）`--initialize-insecure` 初始化，mysqld 以背景程序跑、**只綁 127.0.0.1**；開機自啟走 Startup 資料夾 `start-chilllove-mysql.vbs`（刪檔即取消），手動啟動 `C:\Users\pisce\chilllove-mysql\start-mysql.cmd`；MySQL bin 已入使用者 PATH。root 空密碼（僅本機 dev，與 database.yml 的 `DB_PASSWORD` 未設預設吻合）。
  - mysql2 gem 編譯依賴：修復 MSYS2 pacman 過期鑰匙圈（`pacman-key --init/--populate`＋重裝 msys2-keyring）後裝 `mingw-w64-ucrt-x86_64-libmariadbclient`；`bundle install` 29 依賴/134 gems 全過，mysql2 0.5.7 載入驗證 OK。
- **M0 分支全套實測（第一次有本機證據）**：
  - `db:create db:migrate`（test）全過；Rails 重 dump schema 與手工同步版**只差兩行 `limit: 255` 冗餘**（varchar 預設值，dump 省略）——已以 dump 為準修正並 push（`00bb928`）。
  - `bundle exec rspec`：**51 examples, 0 failures**（30.1s；含真 Chrome system spec，驗收截圖 `tmp/m0-admin-shell.png` 產出並人工檢視——CHILL LOVE shell＋商品空狀態 ✅ ＝ HANDOFF §5 M0 驗收標準）。
  - `pnpm test`：Vitest 3/3 ✅；`pnpm typecheck`：tsc 無錯 ✅。
  - `bundle exec rubocop`：71 檔 0 違規 ✅；`brakeman --exit-on-warn`：0 錯誤 0 警告 ✅。

## 修改的檔案與核心邏輯 (Changes)

- 倉庫內：僅 `db/schema.rb` 兩行（`m0/rails-foundation` 分支，commit `00bb928`）——依「schema 漂移以實跑 dump 為準」的既定約定。
- 倉庫外（機器狀態）：fnm＋Node＋pnpm、MySQL datadir 與自啟、MSYS2 keyring 修復＋mariadb client、PowerShell profile、使用者 PATH。
- **為什麼 MySQL 不裝 Windows 服務**：非管理員環境下服務安裝要 UAC；使用者空間 datadir＋Startup 自啟達成同樣效果、完全可逆（刪 vbs＋刪資料夾即卸乾淨），且 datadir 不在 Program Files 反而好備份。
- **為什麼 root 空密碼可接受**：只綁 127.0.0.1、純本機 dev；CI 用的是獨立的 service 容器。要收緊時 `ALTER USER` 一句話＋設 `DB_PASSWORD` 即可。

## 尚未完成或需注意的風險 (Pending / TODO)

- **`pnpm build`（production Vite build）本機未跑**——CI 會跑；本機驗收以 test/typecheck 為準。理由：build 需 `CHILLLOVE_BASE_HOST`，本機無部署需求。
- mysqld 目前這一次是我手動起的背景程序；**下次重開機才會第一次走 Startup vbs**——若屆時連不上，先跑 `start-mysql.cmd` 看 console 錯誤。
- MSYS2 是 Codex 舊安裝（路徑在 `Documents\Codex\...\ruby34`）——能用但位置怪；日後要搬正規位置（如 `C:\Ruby34`）時 mysql2 要重編。
- 骨架 docs/dev 的「2026-08-11 實測結果表」現在有了移植後對照：51/51（原 49/49，+2 為法域與 HK 防回退新斷言）——PR 描述可引用本 worklog 作自測證據。
- 本機與 CI 僅剩一個已知差異：CI 另跑 `bundler-audit`／`pnpm audit`（依賴漏洞掃描，本機未跑，無阻塞疑慮）。
