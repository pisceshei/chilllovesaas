# 廢止雙 bot 驗收改制（D40）

> 2026-08-23 使用者裁定：「從現在開始你直接開發，取消 github 的 claude 和 codex 驗收。
> 只保留 ci 兩個測試」。全文與射程見 `docs/DECISIONS.md` D40。

## 已完成的工作 (Done)

- 刪除 `.github/workflows/claude-review.yml`（1599 行；Claude bot 判詞自此不再產生）。
- `docs/DECISIONS.md` 新增 **D40**：改制內容、Codex 停用方式（OpenAI 側設定，倉庫內無從關閉，
  停用前其 review 一律不構成義務）、合併前提收斂為 CI `quality`＋`test`、
  「不隨之廢止的」清單（技術鐵律 1–14、文檔分層、worklog 紀律照舊）。
- `CLAUDE.md` 頂部分工段改寫（含舊制沿革一行）；`AGENTS.md` 頂部加停用通告。

## 修改的檔案與核心邏輯 (Changes)

- `.github/workflows/claude-review.yml`：刪除。
- `docs/DECISIONS.md`：新增 D40。
- `CLAUDE.md`：分工段改寫。
- `AGENTS.md`：頂部通告。
- `docs/worklog/2026-08-23-廢止雙bot驗收改制.md`：本檔。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **Codex 自動 review 需使用者到 chatgpt.com/codex/cloud/settings 停用**——倉庫內做不到；
  停用前它仍可能對 PR 發 review，依 D40 一律不構成處置義務。
- CLAUDE.md／AGENTS.md 內文仍散落大量以驗收循環為前提的條款（鐵律 15–21 細則、20.x），
  本輪只在頂部宣告停用、未逐條改寫——理由：它們是歷史裁定的載體，逐條改寫工程大且
  D40 已明確其停用狀態；若日後閱讀噪音過大，另開清理包。

## 2026-08-23 補遺：刪檔改為「保留＋僅手動觸發」（PR #72）

🔴 **PR #71 的刪檔做法錯了兩層**：
① `check-doc-claims.rb` 的 R1 是**全樹路徑保真**——刪 `claude-review.yml` 使數十份歷史
   worklog 的路徑引用全部轉紅（quality job fail）。正解＝檔案保留、`on:` 觸發改為僅
   `workflow_dispatch`——等效取消自動驗收，R1 全綠、歷史引用不變。
② 🔴 **流程事故（記下防再犯）**：#71 的 quality 是紅的，但等待腳本偵測到 failure
   後 `break`，其後以 `&&` 串接的 merge **照樣執行**——CI 紅的 commit 進了 main，
   違反 D40 自己立的合併前提。固定處理＝等待與合併分成兩個獨立步驟，
   合併前以獨立命令複驗兩 job 的 conclusion 皆 success。
