# PR #64 第三輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收基準固定為 head `a10afadbba0b8da77e5dcf3d04b92fe89b50010f`。鐵律 15.2 三端點
  全量重取後，Claude issue comment `5359558626` 有三條 🟡；Codex review `4985595726`
  的 current-head inline 為 `3823859926`／`3823859928`／`3823859932`，沒有以 review 外殼
  代替 inline。Claude Review run `32397232652` completed success，沒有 validation skip。
- A9 不再用手冊單數 `a collection` 推論排他契約。2026-08-21 直接取 GitHub CLI 官方 pinned
  commit `fadd4efb7daddd8afd8a5517a0cb5f5f39af6ada`：`pagination.go` 的 JSON token decoder 在
  `foundNextPage`／`foundEndCursor` 同時成立即 `break loop`，`api.go` 下一頁只設定
  `params["endCursor"] = endCursor`。來源：
  <https://github.com/cli/cli/blob/fadd4efb7daddd8afd8a5517a0cb5f5f39af6ada/pkg/cmd/api/pagination.go#L30-L88>、
  <https://github.com/cli/cli/blob/fadd4efb7daddd8afd8a5517a0cb5f5f39af6ada/pkg/cmd/api/api.go>。
  因此 external-facts 只發布「JSON token stream 首組完整 pageInfo＋單一 endCursor」的官方實作事實，
  不再以未證推論當標題；巢狀 connection 不會各自獲得獨立游標是該實作的直接結果。
- A10 標題已把邊界從「250 以上」改為「超過 250」，並明示替代者是 repository commits
  endpoint；來源仍是 GitHub Docs 的逐字「Lists a maximum of 250 commits for a pull request.」
  與 more-than-250 指引（取證 2026-08-21）：
  <https://docs.github.com/en/rest/pulls/pulls#list-commits-on-a-pull-request>。
- 首輪 worklog 追加 2026-08-21 事後 20.3 三欄稽核，明示對象是 `e20cfa1`、執行日較晚，
  不冒充首輪 push 前證據。第二輪 worklog 撤回沒有鄰近查法的更正次數；PR 描述的 `th=4`
  則由 GitHub `POST /markdown` 反證為 `<thead>` substring 誤算。
- 2026-08-21 GitHub Markdown API `@file` 實跑：首輪 worklog 為
  `table=1 / tr=4 / th=3 / td=9`，第二輪 worklog 為 `table=1 / tr=5 / th=3 / td=12`。
  複驗入口：`gh api -X POST markdown -F text=@<worklog-path> -f mode=gfm`，標籤計數使用
  `<tr\b`／`<th\b`／`<td\b`，不再用會把 `<thead>` 算進去的 `<th` substring。
- Claude 本輪 ⚪ 已依 17.2 登記到 91 §3：HTML comment 更正不可見／切列表、A9／A10 章節
  編排不符、坑簿缺證偽撤回 tombstone；已被證偽或已由本輪阻擋修復吸收的舊觀察不重建為假債。

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取／外部證據 | current head 三端點全量拉取；外部語義只取官方手冊、Docs 或 pinned 官方實作 | 上列 issue comment／review／inline／run ID 均對應 `a10afadbba…`；A9 原始碼由官方 `cli/cli` commit 重取 |
| ② 生產者與消費者同步 | 以 `endCursor`／`pageInfo`／`250`／`20.3` 反查 external-facts、兩份舊 worklog、91、PR 描述與本 worklog | A9／A10、首輪事後稽核、第二輪計數撤回、91 §3／A.1 均列入 Changes；PR 描述列為 push 後動作 |
| ③ 易腐數字／完成聲明 | 移除無鄰近查法的次數；PR 描述的 HTML 計數改用單字邊界 | 第二輪 worklog 不再發布更正次數；兩份 worklog 的 GitHub API 輸出與可重跑入口見 Done |
| ⑦ Markdown | 表格每列驗未跳脫直線，並以 GitHub `POST /markdown` 的 `@file` 實際渲染 | `gh api -X POST markdown -F text=@docs/worklog/2026-08-21-PR64第三輪雙驗收修復.md -f mode=gfm` 實跑成功；以 `<table\b`／`<tr\b`／`<th\b`／`<td\b` 計得 `1/5/3/12`，裸直線檢查通過 |

## 修改的檔案與核心邏輯 (Changes)

- `docs/dev/external-facts.md`：A9 改用 pinned 官方實作直接支持的單一游標邊界；A10 修正
  超過 250 的嚴格閾值與 repository endpoint 名稱。
- `docs/worklog/2026-08-21-PR64首輪Claude驗收修復.md`：追加具日期／對象邊界的 20.3 事後稽核，
  不改寫首輪原文或冒充當時證據。
- `docs/worklog/2026-08-21-PR64第二輪雙驗收修復.md`：撤回無鄰近複驗的更正次數。
- `docs/specs/91-pit-register.md`：只登記本輪未點名的同型／規則層問題，並同步 A.1。
- `docs/worklog/2026-08-21-PR64第三輪雙驗收修復.md`：保存本輪三端點、官方證據、20.3 稽核與待辦。
- PR 描述（GitHub 外部狀態）：push 後改綁新 exact head，`th` 計數改用 `<th\b`，並列齊本輪清況。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本 worklog 的 GitHub Markdown API 與裸直線檢查已完成；本段回填後仍須從 setup 跑完整
   Windows production 等價閘門、commit、commit 後 doc-claims、鐵律 15 與 push。
2. push 後須更新 PR 描述、強制移動 `pr64-last-push` 到新 head，重新取得 CI、Codex、Claude
   與 evaluator 四條件；舊 head 的任何核准不得外推。
3. #64 是一般 PR；只有 exact-head CI、Codex、Claude 雙零、threads resolved、mergeable 與
   evaluator `1111` 同時成立，才可依 D31／D32 代行 squash merge。
