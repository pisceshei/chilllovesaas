# worklog 2026-08-24 — 商品伺服器端搜尋與篩選（排程第 1 包）

## 已完成的工作 (Done)

- `Products::SearchScope`：Shopify search syntax 的 v1 白名單子集（裸詞/片語→title CONTAINS、
  `status:`/`vendor:`/`product_type:` 等值；未知 prefix 當字面文字）編譯成 Arel 條件。
- `products` query 加 `query: String` 參數；filter 先於 keyset cursor 套用。
- 前端 `ProductsPage`：**刪除記憶體過濾**（第 51 筆之後搜不到的缺陷根源），
  搜尋改打伺服器（300ms 去抖）；新增狀態篩選下拉（四值全列）；
  `PRODUCTS_QUERY` 補選 `vendor`/`productType`（修掉恆「—」的兩個死欄）；
  空態分流（店空 vs 查無）。
- i18n：`products.filter.statusLabel`／`statusAll` × 5 語。
- 測試：`spec/requests/products_search_spec.rb` 7 例（複驗：
  `bundle exec rspec spec/requests/products_search_spec.rb`）；前端改寫 1 例＋新增 1 例
  （複驗：`npx vitest run app/frontend/admin/pages/ProductsPage.test.tsx`）。
  **突變驗證**：移除 `sanitize_sql_like` ⇒ LIKE 跳脫測試轉紅；還原 ⇒ 綠。
- 全套快照：rspec `408 examples, 0 failures`；vitest `Tests 42 passed`；rubocop 0（-a 後）；
  brakeman `Security Warnings: 0`；tsc 無錯。

## 修改的檔案與核心邏輯 (Changes)

- 新增：`app/graphql/products/search_scope.rb`、`spec/requests/products_search_spec.rb`、
  `docs/dev/m1-products-search.md`
- 修改：`app/graphql/types/query_type.rb`（products field/resolver）、
  `app/frontend/admin/pages/ProductsPage.tsx`、`ProductsPage.test.tsx`、
  `app/frontend/admin/i18n/messages/*.json` × 5

## 尚未完成或需注意的風險 (Pending / TODO)

- v1 未支援 `tag:`／`created_at:`／否定／OR／括號／萬用字元——全部落在「字面文字」路徑，
  行為可預測；`tag:` 等第 9 包（product_tags 正規化）再上。
- title LIKE `%…%` 無索引；商品量大時要 FULLTEXT 或外部索引——demo 店量級（數十筆）不構成問題，
  登記不阻擋。
- `sortKey` 刻意留給第 21 包（keyset 排序鍵一般化）。
- 狀態下拉的樣式沿用原生 select ＋ `cl-status-filter` class；與原型的 listbar 樣式細對齊
  屬 UI 對齊輪，不夾在本包。
