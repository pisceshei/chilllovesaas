# PR #61：Codex `44ebd39` 延遲意見修復查證

## 已完成的工作 (Done)

- 攝取 exact-head Codex review `4980284182` 的兩則延遲 inline：`3819608325`（P1，會員分級
  語義）與 `3819608329`（P2，Q-1 固定計數）。GraphQL 在合併前由 36 threads／0 unresolved
  變為 38／2，合併守衛 exit 4，`gh pr merge` 未執行；遠端攝取交接為 PR comment
  `5352994087`。
- P1 依鐵律 16 查官方英文來源。SHOPLINE 官方英文《Set Up Membership Tiers》逐字列出
  "Single purchase with the total spent a certain amount." 與
  "Purchase within a specific period with a certain amount."；URL：
  <https://support.shoplineapp.com/hc/en-us/articles/205657286-Set-Up-Membership-Tiers>，
  取證日期 2026-08-20。故驗收意見所述「只有中文來源」已被證偽；但 `git log -p -S
  'single_purchase'` 顯示我方 enum／事件鏈只在初始方案 commit `4b31b75` 出現，沒有 Shopify
  實測或另項選案。`docs/DECISIONS.md` D23 又明定 SHOPLINE 只作輔助、交互取證以 Shopify 為主，
  因此真正缺口是把輔助研究直接升格成 `specs/110` 輸入。
- P2 以 `git log -p -S '35→全量 token'` 與 blame 證實三個數字同樣只來自初始方案
  `4b31b75`；原格的 `wc -l` 只能得到文件行數。對現有權威檔重跑三個集合導出：
  `docs/design/23-interaction-css-spec.md` 的 `:root` 自訂屬性為 35 個、§3 元件表為 16 列，
  `docs/design/48-component-contract.md` 的連續數字元件節為 §1–§37，共 37 節。
- 修法選擇：P1 保留已取得的 SHOPLINE 英文研究，但把 enum／事件／limits 全部降為「輔助候選、
  我方未選案」，在 Shopify 實測或使用者差異裁定前禁止輸入 `specs/110`；P2 保留 2026-08-20
  快照數字供理解，但加入輸出精確集合與數量的三個 fail-closed 命令，Q-1 開工必重跑，DoD 不以
  固定數字判斷。

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-PR61-Codex-44ebd39延遲意見修復查證.md`：保存兩則意見、外部英文
  一手來源、D23 邊界、內部提交沿革、現值導出與選案理由。
- `docs/handoff/2026-08-20-PR61-Codex-44ebd39延遲意見修復查證.md`：交接實作入口、未採方向、
  兩條 thread 與重驗命令。
- `docs/specs/91-pit-register.md`：只登記「Codex 無重大問題總結可早於同輪 inline」的本次實例，
  並把本步 worklog／handoff 補進附錄 A；未修改 poller、workflow 或合併判準。
- `docs/handoff/2026-08-20-階段一開場包.md`：終態入口新增本步配對文件。

## 尚未完成或需注意的風險 (Pending / TODO)

- 兩則 inline 尚未實作、回覆或 resolve；本步只完成修法前查證。
- SHOPLINE 官方英文頁證明的是 SHOPLINE 自身語義，不是 Shopify 或 CHILL LOVE 已選業務規則；
  即使補齊英文逐字，也不能直接恢復我方 enum／狀態機宣稱。
- Q-1 的三個現值只綁 2026-08-20 這棵樹；實作時需加入動態命令，後續 Q-1 開工仍必須重跑，
  不得把 35／16／37 當永久上限。
- 實作後會形成新 head；既有 CI、Codex 結論與 38/2 threads 快照不得冒充新 head 終態。
