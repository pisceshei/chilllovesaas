# 2026-08-31 — 結帳線第二包：運送資料層＋RateResolver＋買家前台全接

## 已完成的工作 (Done)

- **實測取證輪**（先行，鐵律 12/14/16/19）：測試店 2026 費率編輯器（Rate type 恰四值／
  Transit 秒制五值必填）、zone 建立面⊆market、真 checkout 全流程（免運門檻以費率
  自身幣別比較的 $62.73 實錘）、**split shipping 2026 現行形**（per-shipment 獨立
  radio＋Lowest price 聚合）、profile 刪除回落 General 的逐字語義；官方 combined-rates
  ／zones×market 交集句同日網查。正典＝`docs/research/85`（五件套＋V 登記）。
- **運送三表接上消費者**（表建於 M0、至今零 model 零消費者）：ShippingProfile／
  ShippingZone／ShippingRate 三 model；商品→設定檔歸屬欄（NULL＝General 補集、
  FK nullify＝刪檔回落）；建店預設運送鏈 `Shipping::ProvisionDefaults`（General＋
  primary market zone＋免運費率）＋既有店回填 migration。
- **`Checkouts::RateResolver`**：participant 分組（v1 單地點＝profile）→ per-participant
  條件判定（金額／重量級距雙端含；重量加店預設包裹重）→ split 形 shipments ＋
  46c 合併形 merged_options（同名相加／無共同名取最便宜相加）雙輸出；
  Rates(p)=∅ 整車擋；market guard（:not_sellable）；`sellable_countries`＝
  active market ∩ 有費率 zone（官方交集句）。
- **買家前台全接**（使用者指示「必須考慮到和買家前台的對接，完善所有的接口對接」）：
  checkout 頁出國家下拉（值域＝sellable_countries）＋運送選項（split per-shipment
  fieldset／合併單列）；`POST /checkouts/:token/delivery` 選國即預選最便宜入 Total、
  改選走 **server 重驗**（F3-3：名稱＋價格對不上 ⇒ 422＋重選警示＋落當前價）；
  `shipping_lines` 快照落庫、總額經 `Checkouts::Calculator` 同源重算（鐵律 7）。
- **🔴 checkout 狀態 enum 更正**：第一包誤用 `active`；依 90-blueprint/03 §B.2 改
  三值 `open/completed/deleted`（migration 同步預設值＋既有列；前台 find 只認 open）。
- 突變輪 MUT-1～7 全紅（交集→聯集／空費率 fail-open／拿掉包裹重／重驗砍價格比對／
  拔 market guard／split 恆退化／拔 status 過濾），全數復原後 88/88 綠。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260831210000_wire_shipping_into_checkout.rb`：products.shipping_profile_id
  （🔴 單欄 FK on_delete:nullify——複合形會 NULL 掉 shop_id）、rates transit 秒欄、
  checkouts.shipping_lines json、shops.split_shipping_enabled、status 預設 active→open。
  每步帶存在性 guard（MySQL DDL 非交易式，中途失敗重跑不得炸 Duplicate column——本輪實錘）。
- `db/migrate/20260831211000_backfill_default_shipping.rb`：薄呼叫端。
- `app/models/{shipping_profile,shipping_zone,shipping_rate}.rb`：General 唯一＋不可刪；
  zone 國家不重疊（同檔）；級距雙端含（算例 4 的 2000g→1–2kg 語義；邊界同名雙命中
  由取最便宜收斂＝免運門檻值正確取免運側）。
- `app/services/shipping/provision_defaults.rb`＋`app/models/shop.rb`（callback 排
  provision_default_market 之後；三表 delete_all 反序清理）。
- `app/services/checkouts/rate_resolver.rb`（核心）；`create_from_cart.rb` 快照加
  weight_grams/requires_shipping/shipping_profile_id＋status open；`checkout.rb` 三值 enum。
- `app/controllers/storefront/checkouts_controller.rb`：show 運送段渲染＋delivery 動作
  （重驗→落庫→Calculator 重算）；`config/routes.rb` delivery 路由；
  `config/initializers/rack_attack.rb` throttle 收 /checkouts/*。
- `config/limits.yml` shipping 區塊：default_package_weight_grams（V）、split 預設、
  rate_types 四值、transit 檔位；V-15 顯示名半格關閉註記（官方字面 `Shipping`）。
- specs：`rate_resolver_spec`（算例 1–5＋必測 1–7＋免運級距＋幣別＋正規化＋TypeError）、
  `shipping/provision_defaults_spec`（P1–P4）、`storefront_checkout_delivery_spec`
  （D1–D9 端到端）、第一包 C1 status 斷言更正。

## 尚未完成或需注意的風險 (Pending / TODO)

- **split Off 生效範圍未取得**（85 §5.3 V）：本尊切 Off 對行中 checkout 未生效，
  我方實作為即時生效（split? 每次請求判）——語義差異已登記，取證後對齊。
- **金額條件基數之爭**（85 §5.3 V）：官方句「total price of the cart」vs spec 15
  F2.1(b) per-participant；實測未判別。實作取 spec 規範層（per-participant），
  需構造跨門檻雙 participant 車再實測收口。
- **v1 縮邊**（逐項登記，非遺漏）：單地點 ⇒ participant=profile（location_groups
  未建；算例 5 以 v1 形入測）；單幣別 ⇒ 異幣費率不產出選項（換匯隨 markets 幣別包）；
  carrier 型費率不產出（58 物流商對接包）；州級 zone 粒度未做（85 §4 記 62 states）；
  admin 運送設定 UI／GraphQL 未做（本包資料層由 provision＋console 寫入）。
- 店預設包裹重量的本尊值未量測（limits 預設 0，V）；合併形 transit 呈現未實測（ours＝取最慢）。
- 後續包：F3 one-page 欄位三態（85 §5.1 已取證）→ F4 付款 → F5 訂單成立（吃同一
  Calculator Result＋shipping_lines 快照）。
