# PR #65 第十二輪雙驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `243765c17051eb48ae8da1b215deba7ab99ed1cc`：Codex review
  `4988736695` 的三條 inline `3826415179`／`3826415182`／`3826415185`，GraphQL threads
  `PRRT_kwDOT0QjlM6bAF4w`／`PRRT_kwDOT0QjlM6bAF4z`／`PRRT_kwDOT0QjlM6bAF41`；晚到 Claude
  判詞 `5363844402` 與 wrapper `5363846108` 也綁同一 head，未把 CI 雙綠外推成雙清。
- 依鐵律 16 以 `git log -p` 追第九／十一輪 worklog、R6 type-like／recheck cardinality、fixture
  runner 與 m0 契約沿革。type metadata 已先解析 key＋value 並拒絕 `type*`，recheck 卻仍只用
  exact matcher 抽 value，兩個語義欄位的 fail-closed 結構不對稱。
- failure-first fixture 含一筆合法 `recheck:` 與一筆 `rechecks:`；舊 checker實跑 exit 0。
  修法把 recheck metadata 解析為 key＋value，先拒絕非精確語義的 `recheck*` 鍵，再只用精確
  `recheck` 做 cardinality 與命令檢查；同 fixture 修後在畸形鍵原行 exit 1。
- 第十一輪 Markdown 結果補 GitHub 官方 endpoint、逐字、exact request 與 exact response body；
  response 逐字為 `### CLAIM-001` 且沒有 `<h3>`。沒有把只保留衍生 count 的舊快照冒充 raw
  evidence。
- 第九輪四個 endpoint totals 沒有 raw response，且 live collections 已追加；相鄰更正把它們
  降為未能獨立重現的歷史快照，不再當 current total 或完成性證據。
- Claude 指出的 inline comment fail-open 已獨立證實：CommonMark 0.31.2 §4.6 官方
  <https://spec.commonmark.org/0.31.2/#html-blocks>（取證 2026-08-21）逐字為
  "The block begins with a line that meets a start condition (after up to three optional spaces of
  indentation)."、"Start condition: line begins with the string `<!--`."。failure-first fixture 的
  `- type: count <!-- x --> qualitative` 在舊碼 exit 0；block／inline opener 分流後於原行 exit 1，
  合法 inline comment 的相反方向 fixture 維持 exit 0。另依 GitHub 官方 Render a Markdown
  document endpoint <https://docs.github.com/en/rest/markdown/markdown#render-a-markdown-document>
  （取證 2026-08-21）用 exact JSON `{"text":"- type: count <!-- x --> qualitative","mode":"gfm","context":"pisceshei/chilllovesaas"}`
  實跑，raw response 為 `<ul dir="auto">\n<li>type: count  qualitative</li>\n</ul>`；後綴確為可見正文。
- Claude 同則 ⚪「`NUM` 不含兩」經 checker 原文與沿革核對屬實，但不是本輪引入；只登記到
  `docs/specs/91-pit-register.md` §3，不擴修 R4。
- 本輪最後一版 repo edit 後完整 Windows production-equivalent runner 的實跑輸出快照（取證
  2026-08-21）為
  `GATES_ALL_GREEN=29`：Rails 284 examples／0 failures、前端 5 tests／0 failures、RuboCop
  實跑輸出快照：162 files／0 offenses、Brakeman 0 errors／0 warnings、doc-claims 42 個 fixture 全過。GitHub
  Markdown API 對 external-facts／m0／91／本 worklog 的實際渲染也均成功；Node v24.19.0 高於
  專案宣告的 Node 22 範圍，僅記環境邊界，不外推為受支援版本。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 第七／八輪已補 type-like 與 recheck 鍵變體，但 `3826415185` 證明 recheck-like typo 仍在 exact matcher 前消失；`5363844402` 又證明第十一輪新增 closer 規則未同時補 start-condition 正反格；另兩條 inline 是未保存 raw evidence／易腐 totals 再犯 |
| 既有防線失效原因 | recheck parser 只回 value、不保留 key；comment parser 沒區分 block／inline opener；worklog 只保存衍生 count 或總數，沒有 raw response／snapshot 邊界 |
| 被漏掉的固定步驟 | 同形 metadata 欄位須對稱枚舉 exact／like-key／缺件；parser 的 start／end condition 必須成對實作並各放正反 fixture；外部結果保存 request＋raw response，無 artifact 的歷史 totals 只稱未取得快照 |
| 可重跑反向複驗 | `doc_claim_bad_recheck_key` 修前 0／修後 1；`doc_claim_inline_comment_suffix` 修前 0／修後 1，`doc_claim_inline_comment_ok` 維持 0；GitHub Markdown exact response 為 `### CLAIM-001`；第九輪 totals 明標不可由 live collection 回推 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗 |
|---|---|---|
| ① 驗收攝取 | issue comment、review body、paginated inline、GraphQL threads 綁 exact head | Claude issue comment、三個 Codex inline 與三個 thread ID 可逐一重取；同 head 晚到意見先納入再推 |
| ② producer／consumer／終態 | checker、runner、fixture、external-facts、m0、W9／W11 更正、91 同步 | `rg` 反查 `recheck*`、block／inline 契約、四支本輪 fixture 與兩段相鄰更正 |
| ③ 完成性聲明 | raw response 已保存才稱取得；歷史 totals 無 raw artifact 就稱未能重現快照 | W11 有 exact JSON／body；W9 明標不可作 current total |
| ④ 狀態空間 | exact recheck、合法鍵變體、重複語義鍵、recheck-like 畸形鍵；block opener、行中 inline comment 合法／畸形分格 | 合法 variant 0、duplicate 1、`rechecks:` 1；inline 合法 0、畸形 suffix 1、block closing suffix 1 |
| ⑤ fail-open | 一筆合法命令不得遮住第二筆畸形 metadata；block end 規則不得丟棄 inline comment 後綴 | 兩支 failure-first 舊碼皆 0；修後分別在 `rechecks` 與 `count  qualitative` 原行非零 |
| ⑥ workflow 本機假綠 | 本輪不改 workflow；script 包仍跑完整 parity／syntax 與 exact-head CI | #65 命中 18.3，雙零後仍由使用者人工合併 |
| ⑦ Markdown／Windows | W9／W11／W12 與 m0／91 走 GitHub render；最後 edit 後跑 29 閘門 | table／tr／th／td 非空，完整 Windows runner任一非零即停止 |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：`CLAIM_RECHECK` 捕捉 recheck-like key＋value；畸形鍵先 fail-closed，
  精確語義 entries 再做 cardinality／命令驗證；comment state 新增 block／inline 分流，只有行首
  0–3 空格後的 opener 套用整行 block closer，inline comment 後綴繼續解析。
- `scripts/test-doc-claims-rules.rb`、`doc_claim_bad_recheck_key` 與兩支 inline comment fixture：保存
  failure-first 與合法相反方向，runner 下限提高；m0 的契約、fixture 表與 mutation 清單同步。
- `docs/dev/external-facts.md`：以 CommonMark start/end 官方逐字取代 opener-only 的已知偏差與
  〔推論〕；只發布 checker 已由正反 fixture 證明的窄邊界。
- `docs/worklog/2026-08-21-PR65第九輪雙驗收修復.md`：相鄰更正無 raw response 的 totals。
- `docs/worklog/2026-08-21-PR65第十一輪雙驗收修復.md`：補官方 endpoint、逐字與 exact
  request／response，撤回未保存舊 response 的證據地位。
- `docs/specs/91-pit-register.md`：相鄰標記 opener 坑已收割；「`NUM` 不含兩」依 17.2 只登記，
  A.1 維持納入本 worklog。

> 🔴 **2026-08-21 第十三輪相鄰更正（Claude comment `5364105225`）**：Changes 第一條末句
> 「只有行首 0–3 空格後」在本輪 head `cf9fa51` 並非 raw-line 判定；當時以
> `visible.empty?` 與 masked 餘段代理行首，且跨行 inline comment 沒有把 prefix／suffix 重組。
> 第十三輪改為每個 raw line 只判一次 block start，並保存跨行 inline comment 的邏輯活性行；
> 原 bullet 保留作第十二輪時點記錄，不再作現行契約。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本輪最後 repo edit 後已取得 29 道全綠；若 commit 前再改任何 repo 檔，必須從第一道重跑，
   不得把本次結果外推到新內容。
2. commit 後須跑 doc-claims warning 0、六條 CLAIM 原文 recheck 與鐵律 15.1／15.2；晚到
   Claude／Codex 意見必須先處置。
3. #65 修改 `scripts/`，命中鐵律 18.3；即使 exact-head 雙零與 threads 收口，也只能通知
   使用者人工合併，互動式 Codex 不得代行。
