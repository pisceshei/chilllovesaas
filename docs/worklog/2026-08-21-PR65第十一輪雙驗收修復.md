# PR #65 第十一輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `ccf073b6e72cc0a90e64cb41d72725d0b9ca1806`：Claude 判詞
  `5363573988`、wrapper `5363575225`，為 🟡1／⚪3；Codex review `4988556633` 的 inline
  `3826259856` 指出 HTML comment closing-line suffix 被錯當活性標頭。CI 三綠不等於雙清。
- 依鐵律 16 以 `git log -p` 追 `active_markdown_lines`、R6 全域 ID namespace、fixture runner、
  external-facts 與 m0 契約沿革。CommonMark 0.31.2 §4.6 官方
  <https://spec.commonmark.org/0.31.2/#html-blocks>（取證 2026-08-21）逐字為
  "It ends with the first subsequent line that meets a matching end condition" 與
  "End condition: line contains the string `-->`."。
- 「closing line 整行仍屬 block」是由官方以 line 定義終點推出的〔推論〕；另用 GitHub
  `/markdown` 對 `-->### CLAIM-001` 實跑 h3 count 0，沒有把推論冒充官方逐字。
- closing-line failure-first fixture 在修前 checker exit 0；修後不再把 `-->` 後綴重新餵回行首
  結構 parser，改為 `R6 宣稱索引沒有任何` 並 exit 1。GitHub `/markdown` 對同形輸入實跑
  h3 count 0，證明後綴不是活性 heading。
- 跨索引唯一性補相反方向 fixture：兩份各自合法索引使用 CLAIM-001／CLAIM-002 時 exit 0；原
  重複 ID fixture 仍 exit 1。第十輪 20.3 ④ 原稱單檔生產索引可承重多檔 happy path，已用相鄰
  更正撤回，沒有改寫歷史表。
- Claude 三條 ⚪只登 91 §3：未納 git index 的 92 新檔不進 listing、92 自我契約是完整 R6 的
  子集、診斷「首次」是字典序而非撰寫先後。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | Claude `5363573988` 指出跨索引唯一性漏 happy path；Codex `3826259856` 指出 comment closer 漏 closing-line suffix；複驗：`ruby scripts/test-doc-claims-rules.rb` |
| 既有防線失效原因 | 全域 ID 只有重複方向與單檔 production 正向；comment fixture 只測未收尾及 closer 是否生效，沒有測 closer 同行後綴 |
| 被漏掉的固定步驟 | 新增拒絕規則時須另放合法多實體 happy path；逐行 parser 的 terminator 必須測 terminator 所在行及下一行的恢復邊界 |
| 可重跑反向複驗 | `doc_claim_duplicate_id_across_indexes` exit 1、`doc_claim_distinct_ids_across_indexes_ok` exit 0、`doc_claim_comment_closer_suffix_header` exit 1；移除任一側即 runner 轉紅 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗 |
|---|---|---|
| ① 驗收攝取 | issue、review body、paginated inline 與 GraphQL thread 分源綁 exact head | Claude 兩個 comment ID、Codex review／inline 均綁 `ccf073b…`；舊 head 不外推 |
| ② producer／consumer／終態 | checker、runner、兩組 fixture、external-facts、m0、W10 更正、91／A.1 同步 | fixture 表與 mutation 清單含兩個新 case；W10 原錯句有相鄰更正 |
| ③ 完成性聲明 | 只發布本輪實跑的 failure-first、runner 與 GitHub render，不把待跑閘門寫成成功 | closer 修前 0／修後 1、GitHub h3 0 已取得；最終 29 閘門仍待最後 edit 後跑 |
| ④ 狀態空間 | 跨索引重複／相異、comment closer 是否有同行 suffix 分格 | 重複 ID 1、相異 ID 0、closing suffix 假 header 1 |
| ⑤ fail-open | closing suffix 不得滿足 header canary；全域唯一性不得退化成拒絕第二份索引 | `doc_claim_comment_closer_suffix_header` 釘拒絕方向；`doc_claim_distinct_ids_across_indexes_ok` 釘放行方向；MIN_CASES 只增不減；複驗：`ruby scripts/test-doc-claims-rules.rb` |
| ⑥ workflow 本機假綠 | 本輪不改 workflow，但 script 包仍完整跑 parity／syntax 與 exact-head CI | 受限包須雙零後由使用者人工合併；本機綠不能代替獨立審查 |
| ⑦ Markdown／Windows | 官方逐字、GitHub render、表格直線、UTF-8 中文路徑及 Windows runner 分別複驗 | h3 count 0；Markdown render 與 29 閘門待最後 repo edit 後重跑 |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：comment closer 命中後丟棄 closing-line suffix，下一個 raw line 才
  恢復活性解析；契約註釋同步。
- `scripts/test-doc-claims-rules.rb` 與兩組新 fixture：新增 closing-line 負向與跨索引相異 ID
  正向 case，runner 下限同步提高。
- `docs/dev/external-facts.md`、`docs/dev/m0-review-convergence.md`：補 closing-line 官方證據、兩支
  fixture 與 mutation 射程。
- `docs/worklog/2026-08-21-PR65第十輪雙驗收修復.md`：相鄰更正未承重的多索引 happy path 宣稱。
- `docs/specs/91-pit-register.md`：只登三條 Claude ⚪並補 A.1；本 worklog 保存本輪證據與稽核。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 最後 repo edit 後須跑兩支新 fixture、完整 runner、Markdown 結構與連續 29 道 Windows
   production-equivalent gates；任一非零即停止。
2. commit 後須重跑 doc-claims warning 0、六條 CLAIM 原文 recheck 與鐵律 15.1／15.2；晚到意見
   必須先處置，不得把舊 head 三綠外推。
3. #65 修改 `scripts/`，命中鐵律 18.3；即使 exact-head 雙零與 approved threads 收口，也只能
   通知使用者人工合併，互動式 Codex 不得代行。
