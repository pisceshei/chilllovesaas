# 2026-09-01 G6 步 9a：折扣引擎核心

## 已完成的工作 (Done)

- Engine（解析半場：候選/條件/組合雙向同意/best-wins/shipping 硬規則/枚舉防護
  統一文案）＋Calculator `discounts:` 多級管線（F2.1 同基數不複利＋鉗制＋
  可交換律＋最大餘數＋先折後稅）——legacy `discount:` 原樣、既有 70 例零改動全綠。
- F3 用量硬保證：claim_discounts!（條件式 UPDATE＋redemption 唯一索引＋
  applications 回放）進 CreateFromCheckout 交易。
- 結帳 delivery 步 recompute 接 Engine→Calculator→快照。
- 資料層：M0 discounts/discount_applications 採用＋discount_redemptions 表＋
  checkouts 兩欄；Discount model（status 推導制/code 正規化/17-F1.4 shipping
  旗標驗證）。
- 突變輪 9/9 殺＋canary：複利/鉗制/跨級/雙向同意/shipping 硬規則（E5 以
  update_all 打穿旗標層單測縱深）/枚舉洩漏/無條件 +1/email 未正規化/回放漏寫。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260901090000_create_discount_redemptions.rb`＋schema（語句級守衛）
- 新：`app/models/{discount,discount_application,discount_redemption}.rb`、
  `app/services/discounts/engine.rb`
- 改：`app/services/checkouts/calculator.rb`（Result +2 欄＋evaluate_discounts
  私有管線；`discount:`/`discounts:` 互斥）、
  `app/services/orders/create_from_checkout.rb`（claim_discounts!；Failure 帶碼）、
  `app/controllers/storefront/checkouts_controller.rb`（recompute 接線）
- 測試：`calculator_discounts_spec`（10 例含 property）＋`engine_spec`（8 例）＋
  `storefront_discount_claim_spec`（4 例）；checkout 線回歸 108 例全綠

## 尚未完成或需注意的風險 (Pending / TODO)

- 步 9b：discountAutomaticBasic/CodeBasic Create/Update＋Activate/Deactivate/
  Delete＋admin 折扣頁（22 §4）＋前台折扣碼輸入欄＋/discount/:code 分享連結。
- 91 §3.54 五 ⚪（entitlements 表/BxGy 多 code/min_subtotal 分層/threads 併發
  實測/limits 折扣鍵全量消費）。
- 併發超發防線＝條件式 UPDATE（refunds C1 同構已證序列化）；本包 U1 以
  「快照繞過軟檢」模擬 TOCTOU 窗，threads 實跑未做（誠實登記）。
