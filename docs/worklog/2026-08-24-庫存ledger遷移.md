# worklog 2026-08-24 — 庫存 ledger 形狀 migration（含 G28 登記）

## 已完成的工作 (Done)

- **71 §A G28 登記**（使用者 2026-08-24 裁定原文「idempotencyKey上string,批准進 71 §A」）：
  庫存 mutation 的 `idempotencyKey` 上 GraphQL 契約層 `String!`，加嚴理由與 G-code 同構。
  ⚠️ 同案的 `changeFromQuantity required: :nullable` **刻意不在 G28 內**——等第 17 包寫 mutation 時再定。
- **migration `20260824100000_reshape_inventory_ledger`**（總裁定 §三的落地）：
  ①新表 `inventory_adjustment_groups`（冪等鍵＋reason＋mutation_kind＋參考文件＋actor 兩欄＋changes_count）
  ②`inventory_adjustments` 改 group 子行：刪 `idempotency_key`/`reason`/`reference_*`（上移 group）、
    刪兩個衍生量實體欄，加四個 leaf delta＋`ledger_document_uri`＋`position`，
    `unavailable_delta`/`on_hand_delta` 改 **STORED GENERATED**，唯一鍵 `(shop_id, group_id, level_id)`
  ③`inventory_levels` 補四個 leaf 欄，`unavailable`/`on_hand` 改 STORED GENERATED，
    加 `(shop_id, on_hand)`/`(shop_id, available)` 索引。
- 動工前依總裁定要求**兩端實查資料量**：本機與 bt3 生產三張表皆 0 列（safety_assured 的依據）。
- **雙向驗證**：dev 與 test DB 遷移成功；`db:rollback:primary` → 再 migrate 往返無誤。
- `spec/db/inventory_ledger_schema_spec.rb` 5 例（複驗：`bundle exec rspec spec/db/inventory_ledger_schema_spec.rb`）：
  恆等式（incoming 不在內）／🔴 直接 UPDATE generated 欄被 DB 拒絕（機制不是紀律）／
  狀態間移動一列自含（on_hand_delta=0）／(group, level) 唯一鍵／冪等鍵唯一性在 group 頭。
- 全套快照：rspec `413 examples, 0 failures`；rubocop 0；brakeman 0；`check-tenant-isolation.rb` 通過。

## 修改的檔案與核心邏輯 (Changes)

- 新增：`db/migrate/20260824100000_reshape_inventory_ledger.rb`、`spec/db/inventory_ledger_schema_spec.rb`
- 修改：`db/schema.rb`（產出）、`docs/specs/71-admin-parity-sweep.md`（§A G28）、
  `docs/plans/2026-08-24-庫存ledger形狀總裁定.md`（§六閘門結案）

## 尚未完成或需注意的風險 (Pending / TODO)

- 模型（InventoryLevel／InventoryAdjustment／InventoryAdjustmentGroup／Location）、`Inventory::Adjust` 服務、GraphQL mutation、禁直寫 cop、
  併發測試全部屬排程第 15–17 包，本輪**刻意不建**——本輪交付的是表形狀與 DB 層機制。
- V-96.1（同呼叫重複 item×location 的本尊行為）／V-96.3（Initial inventory 的 reason 映射）仍開放；
  服務層落地時照 fail-closed 預設（reject／correction）。
- `quantity_name`／`reason`／`mutation_kind` 的值域驗證在服務層（DB 不建 CHECK，與全倉慣例一致）；
  在第 16 包模型落地前，這三欄的值域**只有文件在守**。
- MySQL DDL 非交易性：本 migration 中途失敗會留半成品（空表所以重跑前手動 DROP 即可）——
  部署到 bt3 時 deploy.sh 的 db:prepare 是冪等入口，單次執行即完成。
