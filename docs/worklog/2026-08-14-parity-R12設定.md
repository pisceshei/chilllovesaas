# Parity R12：設定（RBAC 架構揭露＋權限 115 全窮舉＋通知 47 範本）

> 對應規格：docs/specs/71 §E R12、§F（2 STRUCT＋1 MISS 修；4 DOC＋10 V 登記）｜
> 研究：**docs/research/81**（433 行，實測＋help 雙源）｜help：設定模組九主題（147K）｜
> commit：見本檔所在 commit

## 已完成的工作 (Done)

- **🔴 本輪最重發現：權限模型是整代落差**。本尊 2026 已改 **RBAC 且使用者掛在組織層**
  （`/settings/organization-account`，底下有「角色」「安全性」兩子頁；橫幅原文「**以角色為基礎的
  存取控制現已啟用**」），完整模型是 **使用者 ↔（群組）↔ 角色 ↔ 權限** 四段。
  我方是商店級 staff checkbox＝舊模型。已登記 R12-STRUCT1（M1 前必答）。
- **層③架構**：①設定是**全螢幕覆蓋層**（`/settings` 302→`general`，右上 ✕）——我方 `openSettings()`
  與本尊同構 ②側欄**組織 2 ＋ 商店 18** 兩層 ③完成**設定樹映射表**（本尊 20 ↔ 我方 23）。
- **層②值域窮舉（本輪規模）**：角色 **10 個/4 類別**；**權限 115 個/17 群組/3 層**（群組→子標題→
  權限→縮排子權限，逐項落檔）；角色類別 4；安全登入規定 2；語言狀態 2 與 `⋯` 5 項；
  **顧客通知 11 組 47 個範本**（help 明說「請去後台看」不給清單，**本輪實測補齊**）；
  匯出/匯入/新增使用者等頁首動作；一般設定 **9 張卡片**逐張。
- **層⑥條件控件**：語言 `⋯` 的「設為網域預設」**未發布時禁用**；一般設定的「開發商店」卡與
  「組織和商店轉移」文案是**條件性**的；權限樹在未選角色類別前不渲染。
- **🔴 父子權限是真連動（實測＋help 雙證）**：勾縮排子項「套用折扣」→ 自動勾父項「編輯訂單」
  **和**群組基線「檢視」；help 補完另一半——**撤銷會往下連動移除**，且
  **Inventory/Catalogs 任一權限 → Products>View 且不可取消**。⇒ 依賴是**圖不是樹**（R12-V6）。
- **🔴 step-up auth（help 未載，實測發現）**：儲存角色彈出「請驗證您的帳號以繼續／為加強安全性，
  請輸入您的密碼」；移除使用者也要輸入自己的密碼。已入 limits。
- **層⑤help 雙源，14 節**：補完實測拿不到的——敏感權限 7 條、座位上限依方案
  （**Starter/Basic 是 0**）、邀請 **7 天**過期且**無重寄操作**、Groups 是 **Plus 專屬第四段**、
  政策 **6 份**（是 Return policy 不是 Refund）、品牌欄位限制與**顏色是陣列配對**、
  活動記錄 **250 筆唯讀**、Translate & Adapt 的 **30 種可翻譯資源型別**、語言上限 **20（無方案階梯）**。
- **原型修正**：permCats 由 6 類彙總 → **實測 17 群 115 權限全量**；補**角色目錄卡**（10 角色/4 類別）；
  補**安全登入規定**二選與**使用者活動記錄**入口；邀請文案 72 小時 → **7 天**（並註明無重寄）；
  一般設定補**商店資產卡**（中繼欄位／品牌，建立品牌的正確路徑）與**資源卡**＋`storeActivityLog()`
  彈窗（三條硬約束）；幣別與時區欄位加註本尊事實；禮品卡分頁加註歸屬爭議。
- **DOCS ＋9 條**，每條依鐵律 12.4 寫滿四件事（是什麼／完整值域／怎麼做／🔴 跨功能·跨頁·前端影響）。
- **limits.yml ＋7 節 63 鍵**：`settings`／`users_and_roles`／`notifications`／`brand_assets`／
  `store_activity_log`／`languages`／`policies`。
- **驗證**：lint ERROR 0／WARN 13（基準線）；煙霧 12/12；原型 console 零錯誤。

## 修改的檔案與核心邏輯 (Changes)

- `docs/research/81-admin-settings.md`（新檔，本輪主產出）：§0 架構＋設定樹映射表／§1 使用者與權限
  （角色目錄・權限 115 全表・父子連動・step-up auth・安全性頁）／§2 一般設定九卡／§3 通知 47 範本／
  §4 語言與 Translate & Adapt／§5 CSS 三段式 8 條／§6 裁定面／§7 help 層 14 節（含矛盾表 6 條、未載 12 項）。
- `docs/design/chilllove-admin-v2.html`：
  - `permCats` 重寫（6→17 群，115 權限）；新增 `ROLE_CATALOG`（10 角色/4 類別）
  - `SET_PAGES.users`：補角色目錄卡、安全登入規定 select、使用者活動記錄入口；邀請文案修正
  - `SET_PAGES.general`：補「商店資產」與「資源」兩張卡；幣別/時區欄位加註
  - `SET_PAGES.giftcards`：加註本尊歸屬（付款＋一般）與文檔矛盾
  - 新函式 `storeActivityLog()`（250 筆／唯讀／執行者三型）
  - DOCS ＋9 條（set-roles／set-role-cat／set-sec-signin／set-user-activity／set-store-assets／
    set-asset-metafields／set-asset-brand／set-resources／set-activity-log／set-shortcuts）
- `config/limits.yml`：新增 7 節 63 鍵。
- `docs/specs/71`：§E R12 ✅；§F 新增 17 條（2 STRUCT＋1 MISS＋4 DOC＋10 V）。

## 尚未完成或需注意的風險 (Pending / TODO)

- **M1 前必答（5 條）**：STRUCT1（RBAC 資料模型——🔴 **角色是組織層資源可跨店，與鐵律 2「全表帶
  shop_id」直接衝突**，這是本輪最需要裁定的一條）／STRUCT2（品牌歸屬）／V1（禮品卡設定歸屬）／
  V3（時區語言改 per-user）／V6（權限依賴圖雙向傳播＋不可取消邊）／V9（Groups 第四段要不要做）。
- **M2 前必答（3 條）**：V4（通知範本進 jurisdiction pack）／V5（翻譯編輯器＝獨立 app 邊界，
  與 R9-V1 同源）／V10（品牌顏色是陣列配對，非一主一副）。
- **V2：角色 create→delete 未走完**——被 step-up auth 擋下，我不輸入密碼。這是本輪唯一沒滿足
  鐵律 12.2「完整操作流程」的地方，已明確登記而非隱瞞。要補需使用者本人操作。
- **本輪未實測的設定頁（10 個）**：付款／結帳／顧客帳號／運送與配送／稅額與關稅／地點／
  應用程式／銷售管道／網域／顧客事件／顧客隱私／政策／中繼欄位。本輪聚焦在薄弱點⑨（使用者權限）
  ＋通知＋語言＋一般，其餘頁面的按鈕級拆解**尚未做**，R14 收斂前需補或明確登記為遞延。
- **員工通知／出貨要求通知／Webhook 三個子區**只取到入口與說明，未進入內層（範本清單、
  webhook 建立表單值域）。
- 既有未決：R8-V2、R9-V1、R10-V2/V3、R11 的 11 條（含鐵律 7 的 AOV 例外）、B-8（RTE）、
  POS 範圍（R13 前）、PR #12 與 R11 PR 待合併。
