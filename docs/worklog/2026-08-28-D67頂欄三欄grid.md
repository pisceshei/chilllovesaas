# 2026-08-28 — D67 頂欄改成本尊的三欄 grid

`docs/specs/91` §3.40 W-1。🟢 CSS ＋ 兩處 markup wrapper，未動判準面，不觸鐵律 18.3。

## 已完成的工作 (Done)

- 頂欄由 flex 改成 `grid-template-columns: minmax(max-content, 1fr) minmax(0, 640px) minmax(max-content, 1fr)`，
  三個 zone（左／中／右，右槽 `justify-content: flex-end`）。
  本尊實測是 `960px 640px 960px` ＠ viewport 2560 ⇒ 等效 `1fr 640px 1fr`。
- 原型與 `AdminShell.tsx` 各包三個 zone wrapper。中槽同時容納搜尋列與存檔列。
- 🔴 **溢出判準改寫**：舊的只比視口邊緣（fail-open），改成
  「每個 zone 的子元素必須落在自己 zone 的矩形內」，並排除定位祖先脫離流的子代。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `docs/design/chilllove-admin-v2.html` | `.topbar` 改 grid ＋ 新增 `.tb-zone` / `.tb-zone--r`；markup 包三個 zone |
| `app/assets/stylesheets/admin.css` | `.cl-topbar` 同上 ＋ `.cl-topbar__zone` / `--right` |
| `app/frontend/admin/layout/AdminShell.tsx` | 三個 `cl-topbar__zone` wrapper |
| `docs/DECISIONS.md` | D67 |
| `docs/specs/91-pit-register.md` | §3.41（W-1…W-4） |

`app/assets/tokens.css` 未變動。

## 20.2 適用類型／固定處理／反向複驗輸出

| 類型 | 適用 | 固定處理已跑 | 反向複驗輸出 |
|---|---|---|---|
| ① 證據不完整 | 是 | 三欄比例、槽角色、savebar 槽位全部實測，未取得處明寫 | 本尊 `960px 640px 960px` |
| ② 生產者／消費者同步 | 是 | 原型與 `admin.css` 同一提交、**各自量測** | 兩份三寬度表 |
| ④ 狀態空間漏分 | 是 | 中槽三態全列：搜尋顯示／savebar 取代／窄版摺疊或隱藏 | savebar `.show` 時 static、320w640、中線 640 |
| ⑤ **fail-open** | 是 | 🔴 舊判準只比視口邊緣，抓不到 zone 內重疊；已改判準並加 canary | 純 `1fr` ⇒ **28 處**溢出；`minmax(max-content,1fr)` ⇒ **0** |
| ⑦ Windows 工具鏈 | 是 | 改檔腳本寫成檔案再跑；先 `encode()` 再 `wb` | 無事故 |

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **原型在 1280 中欄偏 76.6px**：右槽 max-content 381 > 可分到的 304。
  **這是資訊架構問題（原型頂欄控件比實作多），不是版面 bug**（`91` §3.41 W-1）。
  `admin.css` 在 1280 是精確置中（偏 0）。
- **兩份檔窄版的搜尋形態不同**（原型摺疊成 36px 圖示鈕、`admin.css` 隱藏），
  兩者都不是對齊本尊的結果——**本尊窄版形態未取得**（`91` §3.41 W-2）。
- 🔴 **新的溢出判準只存在於本輪的臨時量測腳本**，沒有進倉庫。
  `scripts/rwd-check.mjs`（鐵律 13.3 要求）仍未建立（`91` §3.41 W-3）。
- **本尊中央槽的 `padding: 0 14px` ＋ −14px 外距未照抄**（純命中區、無視覺效果，W-4）。
- **hover 態仍全部未取得**（沿用 D66 的登記）。
