# 2026-09-02 Ella 修復 PR-28：預覽內 hover 工具列（⑤e）

## 已完成的工作 (Done)

- 編輯器 parity 軸 ⑤e「hover 顯示 ops」：橋（design_mode 才注入）在
  hover section 時右上浮出 ↑↓⧉✕ 四鈕（視覺自有——鐵律 9），點擊
  postMessage `cl:op`；父頁映射既有 moveSection/duplicateSection/
  removeSection（band 以 cl:select 同法跨帶解析）——op 全走 applyOp
  ⇒ undo/改即見/儲存/URL 免費繼承。
- remove 的選取清理走 functional setSelectedId（onMessage effect 掛載
  一次、selectedId 閉包會舊——直接比較是 stale 陷阱）。
- 測試 BR3（橋內容三錨＋公開店面不帶）＋ED27（remove＋undo 直達／
  duplicate／異 origin 忽略）；突變 1/1 殺（映射掏空）。

## 修改的檔案與核心邏輯 (Changes)

- `app/liquid/theme_engine/runtime.rb`：EDITOR_BRIDGE_JS 工具列＋
  mouseover 定位＋cl:op 發送。
- `app/frontend/admin/pages/ThemeEditorPage.tsx`：cl:op 分支。
- specs：BR3／ED27。

## 尚未完成或需注意的風險 (Pending / TODO)

- 工具列的 disabled 態（首/末位 ↑↓）未做——盲按越界＝moveSection 自身
  邊界 no-op，安全但無視覺回饋。
- 本尊工具列鈕集/位置未逐格量測（V）——功能對位（四 op）先行。
- srcdoc 換頁（draft_page）後 DOMContentLoaded 已過——bar append 用
  DOMContentLoaded 在 srcdoc 重載時機下仍觸發（新文件新事件）✓；
  但若日後改 partial morph 記得 bar 生命週期。
