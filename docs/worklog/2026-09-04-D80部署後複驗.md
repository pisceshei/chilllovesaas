# 2026-09-04 D80 部署後複驗（bt3 mirror 店本尊形＋八頁三寬 computed 對表）

配對 handoff：`docs/handoff/2026-09-04-D80語言前綴本尊形.md`（同一工作包，§①③ 終態同步更新）。規範：`docs/dev/e12-computed-parity.md` §2b、
`docs/dev/d80-url-structure.md`。

## 已完成的工作 (Done)

- PR #326 squash 合併 → main `a9c68526`（首候選 `5ada3d09`＋docs-only 整合修復 `13907111`：quality job 的 Check doc claims 抓到 handoff 一句可算數字）；
  bt3 `scripts/deploy.sh origin/main` → head `a9c6852`；無 migration；`render_parity:mirror[mirror]`：主市場 prefixes `,/zh-hant,/en,/fr,/ja`、
  美國／香港／日本／欧盟四市場各 `presence=none removed=1`；`Rails.cache.clear`。
- bt3 本機與公開站複驗（curl，2026-09-04 13:20 UTC）：`/` 200、`Shopify.locale = "zh-CN"`、`Shopify.country = "TW"`、`Shopify.routes.root = "/"`、
  hreflang 六條（`zh-Hans`／x-default → `/`，`zh-Hant`／`en`／`fr`／`ja` → `/zh-hant/`…）、頁首語言鈕 1；`/zh-hant/` 200 `routes.root = "/zh-hant/"`；
  `/en/collections/all` 200；`/zh-hans-tw/`、`/en-us/` 404；`POST /localization country_code=US` ⇒ 302 `/collections/all`＋
  `set-cookie: localization=US; path=/; expires=（一年）; samesite=lax; secure`；`GET /` 帶該 cookie ⇒ `Shopify.country = "US"`。
- computed 對表 8 頁 × 3 寬（hoko.vip live vs `https://mirror.chilling.com.hk` 根路徑，`--open-details 1 --wait 3000`）：逐頁逐寬＝E15 部署後基線
  （表在 e12 §2b「D80 部署後」）；非全同段只有 `__root__`（平台注入的 body 第三個 div／grid rows）與商品頁 `template--T__main`（countdown 寬 1px 級）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `docs/dev/e12-computed-parity.md` | §2b 加「D80 部署後（bt3）」表與 mirror 店實測 |
| `docs/handoff/2026-09-04-D80語言前綴本尊形.md` | §① 終態（PR／main／bt3／mirror／對表）、§③ 移除「bt3 部署與對表尚未做」 |

docs-only；本機跑 doc-claims 兩支（`--base origin/main`，先 `git add` 讓 R4／R5 掃到新檔），完整閘門由 PR 的 CI `quality`＋`test` 執行。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.84 的 V 照舊（子資料夾／自有網域市場本尊形、每語言一組 sitemap 子表、`language.root_url`、country-only 的語言、未知國碼、`pt-BR`）；
  ⚪ 舊形 URL 無 301、市場靠 cookie 但無 `Vary: Cookie`。
- bt3 的 `http://127.0.0.1/up` 無 Host 回 403（平台 host 保護；公開 `https://demo.chilling.com.hk/up` 200）——部署腳本的本機探針改帶 Host 較準，未改。
- 頁首區段 fetch 內容（31 國／5 語言清單）尚未做 SRA HTML 段 diff（下一包候選）。
