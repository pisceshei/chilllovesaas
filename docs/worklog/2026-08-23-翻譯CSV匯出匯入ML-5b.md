# worklog 2026-08-23 — 翻譯 CSV 匯出／匯入 ML-5b

## 已完成的工作 (Done)

- `Translations::CsvExport`：8＋1 欄（對齊本尊欄序＋我方 `source_digest`）；未翻譯欄位也出列（`status=untranslated`）；來源語言不出現；`market_handle` 恆空；可選語言／欄位。
- `Translations::CsvImport`：四種「不變更」語義（列缺席／欄位缺席／儲存格空白／未勾 overwrite）＋`__CLEAR__` 唯一清空＋缺 digest 欄整檔拒絕＋digest 不符仍寫入但標 outdated/review_required＋逐行 transaction 與逐行錯誤＋清空/覆寫稽核日誌。
- `Admin::TranslationsController`：`GET /admin/translations/export`、`POST …/preview`（dry_run）、`POST …/import`；路由排在 SPA catch-all 之前。
- UI：設定 › 語言新增「匯入／匯出翻譯」卡片（匯出語言選擇、上傳、預覽四數字＋digest 不符提示＋逐行錯誤、覆寫勾選框與說明）。
- 五語訊息（`errors.translation_csv.*`）與 15 個 UI i18n key；Gemfile 補 `csv`（Ruby 3.4 bundled gem 不再預設 require）。
- 測試：`spec/requests/translation_csv_spec.rb`（複驗：`bundle exec rspec spec/requests/translation_csv_spec.rb`）11 例，涵蓋匯出欄序/status/選擇性匯出與匯入七種語義＋往返；全套 rspec 快照 `389 examples, 0 failures`；vitest 快照 `Tests 36 passed`；rubocop／tsc 無 offense。
- `docs/dev/m1-translation-csv.md`。

## 修改的檔案與核心邏輯 (Changes)

- 新增：`app/services/translations/csv_export.rb`、`app/services/translations/csv_import.rb`、`app/controllers/admin/translations_controller.rb`、`app/frontend/admin/pages/TranslationCsvCard.tsx`、`spec/requests/translation_csv_spec.rb`、`docs/dev/m1-translation-csv.md`
- 修改：`config/routes.rb`、`Gemfile`／`Gemfile.lock`、`config/locales/*.yml`（五語）、`app/frontend/admin/i18n/messages/*.json`（五語）、`app/frontend/admin/pages/SettingsLanguagesPage.tsx`、`app/frontend/admin/pages/SettingsLanguagesPage.test.tsx`（查詢收窄到清單容器）、`app/assets/stylesheets/admin.css`

## 尚未完成或需注意的風險 (Pending / TODO)

- 匯出同步產出；資料量大時要改非同步＋email（`i18n.export.async_delivery`）。
- 稽核軌是結構化日誌不是資料表（`clear/overwrite_writes_audit_trail` 的最小實作）。
- 預覽與實際匯入是兩次上傳（前端各送一次檔案）；若檔案很大應改成一次上傳＋暫存 token。
- 射程 PRODUCT／COLLECTION × 四欄位；日後加資源型別只要擴 `resource_type` 值域，**CSV 格式不變**。
- 前端測試教訓：頁面加了新卡片後「日本語」同時出現在語言列與匯出下拉 ⇒ 查詢一律指定容器（第三次踩同型）。
