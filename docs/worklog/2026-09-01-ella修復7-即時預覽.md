# 2026-09-01 Ella 修復 PR-7：右側即時預覽（draft section 渲染）

## 已完成的工作 (Done)

- 使用者點名「沒有右邊的即時預覽效果」收口第一階段（editor 軸 high ③）：
- `PageRenderer` 收 `draft_sections`（sid→未儲存 entry）——SRA 單 section
  渲染的資料覆蓋最優先（DS1/MP7-1 紅證：draft 值出現在片段、且不落 DB）。
- `POST /admin/store/preview/:theme_id/draft_section`（staff 閘＋noindex；
  缺參 422）；🔴 路由插位事故（regex 插進跨行 get 語句中間 ⇒ routes 全掛
  9 紅）——已修＋教訓：路由插入錨定完整語句不是首行。
- EDITOR_BRIDGE_JS 增 `cl:replace`：片段換 `#shopify-section-{id}`（保留
  選中描邊）。
- 編輯器 debounce 400ms：選中 section 的 entry 一變（設定/block 操作）⇒
  POST draft 片段 ⇒ cl:replace 進 iframe（ED13/MP7-2 紅證——未儲存值
  「改即見」）。失敗靜默（增強面不擋儲存路徑）。

## 修改的檔案與核心邏輯 (Changes)

- 改：page_renderer（draft_sections）、storefront_preview_controller
  （draft_section action）、routes、runtime（cl:replace 橋）、
  ThemeEditorPage（activeDraftRef＋debounce effect）、bootstrap spec 8→9
  （DS1）、editor 測試 12→13（ED13）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 片段渲染語境 v1＝首頁（非 index 模板的資源語境未接——product 模板的
  section 即時預覽會缺 product assigns）；佈景設定即時預覽（CSS 變數層）
  未接；結構操作（add/remove/move section）仍靠 save 後 reload。
- 橋 JS（iframe 內半場）單元測試覆蓋不到（jsdom 無真 iframe 文檔）——
  誠實登記；生產親測補。
