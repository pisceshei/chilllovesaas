# Worklog — 鐵律 15 提交前復核（PR-0e，2026-08-17）

背景：PR #52 驗收循環中兩起事故——①19:27 判詞與 19:29 push 時序交錯、整批 🔴 漏收
一輪；②🔴-7 整項漏做而 worklog 稱「19 項全收」（同型第二次）。使用者裁定立為鐵律。

## Done

- CLAUDE.md 鐵律 15（四款）：逐項對照（每意見有對應 diff hunk）／時序復查（push 前
  重拉 PR 最新留言）／宣稱與實物同刻（「N 項全收」N＝清單實數逐項打勾）／順序
  （復核→閘門親眼看碼→commit→push；閘門後動檔＝重來）。
- AGENTS.md 同步摘要。

## Changes

| 檔案 | 改動 |
|---|---|
| `CLAUDE.md` | 鐵律 15（**14.4 之後**、驗收基準之前（2026-08-17 更正，PR #53 首輪）：原寫 12.4——13/14 已隔在中間，沿用上一規約 PR 的句式未更新） |
| `docs/worklog/2026-08-17-鐵律15提交前復核.md`＋同名 handoff | **新增**（本三件套自身，Changes 表原漏列） |
| `AGENTS.md` | 驗收基準節補摘要 |
| `docs/dev/m0-review-convergence.md` | 終態補「第五機制：鐵律 15」可見段（第 4 輪誤放註釋、第 5 輪移出） |

## Pending / TODO

- 無。本鐵律即刻適用於 PR #52 後續各輪。
  <!-- 🔴 2026-08-17 更正（PR #53 首輪，Codex）：「無」與同 commit handoff §③ 矛盾——
       機制化（腳本比對判詞清單 vs diff）確為已知待辦，原文保留、以本註記為準。 -->

## 驗收後修正（PR #53 首輪，2026-08-17）

### Done（bot 🔴1＋🟡2、Codex 4 條，重疊後計 4 項）

- 🔴 Changes 表「12.4 之後」→14.4＋補列三件套自身兩列（終態層）。
- 15.4 重排：15.2 重拉留言移為 **push 前最後一個動作**＋「有新判詞⇒回 15.1」——
  原順序在閘門（分鐘級）期間貼出的判詞照樣漏，防不住立法事故①。
- 15.1／AGENTS 摘要收窄：hunk 要求僅限「宣稱已修復」項；②③⚪＝核對登記存在
  （原文會令合法清法無法 push）。
- Pending「無」加歷史層更正註記（與 handoff §③ 機制化待辦矛盾）。

## 驗收後修正（PR #53 第 2 輪＝Codex 三條，2026-08-17）

- 15.1 明訂「diff」＝已 commit 差異（HEAD vs base），非工作樹；
- 15.2 補：重拉涵蓋 bot 判詞＋Codex inline 兩種形式；首次 push（PR 未存在）豁免；
- ⚪ 順手：PR 描述「改了什麼」更新為修訂後條文措辭（瀏覽器端）。

## 驗收後修正（PR #53 第 3 輪，2026-08-17）

- 🟡（雙方同指）：15.1「diff＝已提交差異」與 15.4「核對在 commit 前」互斥——
  15.4 重排：修復→閘門→commit→**逐項核對 `git diff base...HEAD`**→重拉→push
  （核對缺項＝補修復重跑閘門再 commit）。
- ③證偽補「證據已附」要求（第 2 輪收窄時誤刪）；handoff §① 終態摘要同輪回寫。

## 驗收後修正（PR #53 第 4 輪＝Codex 五條，2026-08-17）

- AGENTS 摘要③補證據要求（第 3 輪只補了 CLAUDE.md 側）；15.3 明訂宣稱位置
  （commit 後的 worklog／PR 留言，commit message 不作全收宣稱）；
  m0-review-convergence.md 終態補鐵律 15 機制段；handoff G-xx 行加錨定註記；
  15.4 奇數粗體星號修復。

## 驗收後修正（PR #53 第 5 輪＝Codex 七條，2026-08-17）

- 15.3 統一：全收定論只寫核對後 PR 留言（第 4 輪版本自相矛盾）；15.1 ③判準改
  「證據存在」（僅登記＝缺項）；convergence 段移出註釋成可見終態＋Changes 補列；
  handoff §① 已含修訂摘要；G-xx ⚪ 同步登記 PR 描述。

## 驗收後修正（PR #53 第 6 輪＝bot 第 4 輪＋殘項，2026-08-17）

- 🔴 handoff §① 回寫至五輪＋補 convergence 檔（Changes 表已於上一 commit 補列）。
- 🟡 殘項：handoff §④ 清單檔標**倉庫外**（倉庫內檔 commit 後打勾＝死循環）；
  15.1 diff 定義句位自「此三類」中段移出接回；AGENTS :205 疊放註記合併。
- 15.3 主句死循環與 ③ 判準對齊已由上一 commit（第 5 輪）覆蓋，本輪複核成立。
- ⚪：PR 描述措辭更新至終態＋⚪ 清單併入（瀏覽器端，本輪執行）。

## 驗收後修正（PR #53 第 7 輪，2026-08-17）

- 🔴 同型三犯終治：handoff §① 輪數**不寫死**（grep 指標句）——每輪遞增使前輪計數
  過期，寫死必腐。
- 15.3 括號成對修復；15.4 `base` → `origin/main`（可解析 ref）；m0 第五機制段移至
  文末獨立段＋檔頭概述同步「五項機制」；本 PR 文檔欄更正：**有** docs/dev 變更
  （m0-review-convergence.md）——PR 描述同步。

## 驗收後修正（PR #53 第 8 輪，2026-08-17）

- 🔴 m0 段 `base...HEAD` 副本同步 `origin/main...HEAD`（同缺陷修 CLAUDE 漏本檔——
  複製時未帶第 7 輪修正）；段落層級 ###→##（自「已知限制」子層升獨立段）。
- Codex 五條：概述五項句已在檔頭 ✓；可執行 PR-base 指令統一；坑登記補記
  （handoff 行不替代 PR 描述 ⚪）；本 PR docs/dev 變更已記（第 7 輪 Changes 列）；
  輪次修復核對基準＝上輪已審 HEAD（15.1 適用範圍註記於此登記，機制化時一併考慮）。

## 驗收後修正（PR #53 第 9 輪＝條文整併，2026-08-17）

- 使用者點名「越修越差」成立：第 2 輪後的 🔴 全為修復自身引入。根因＝條文裡
  疊了多枚行內更正註記（複驗：`git show 整併前一 commit:CLAUDE.md | grep -c "更正，PR #53"`，當時快照＝8）（括號套括號、註記插句中），文本結構劣化為每輪新缺陷的
  土壤。本輪**整併**：鐵律 15 重寫為乾淨終態文本（語義嚴格保持六輪收斂結果、
  逐款比對無變更），沿革改一句指標指向本檔；AGENTS 摘要同步重寫。
  <!-- 🔴 2026-08-17 更正（Codex 第 8／10 輪）：上行複驗指令的「整併前一 commit」非可解析
       ref——可執行式＝`git show 4c75066:CLAUDE.md | grep -c "更正，PR #53"`＝8
       （4c75066＝整併前末輪 commit）。「全為修復自身引入」＝當時判讀快照，列舉七項、
       各對應第 3–8 輪判詞：14.4 位置漂移／15.4 未閉自身時序窗／hunk 要求與清法②③⚪
       矛盾／diff 定義與 commit 順序矛盾／「五輪」計數過期／m0 base...HEAD 副本漏同步／
       括號不成對。「逐款比對無變更」機械複驗＝`git diff 4c75066 66a02d4 -- CLAUDE.md`
       （語義等價為當時人工結論快照）。原文保留。 -->
- 此後修約紀律（記憶亦存）：修前全文互動分析／最小爆炸半徑（不新增會腐化的
  輪次・位置・計數）／提交前整體重讀最終文本。

## 驗收後修正（PR #53 第 10 輪＝Codex 第 8 輪七條，2026-08-17）

- 核對基準精化（回應輪對**上輪已審 HEAD**——累計 diff 會讓原始 hunk 冒充本輪修復）；
  首輪命令補 fetch＋非 main base 條款；15.2 補兩類留言完整取法；15.4 重來點改回
  款起點（僅回 15.1 會放行未提交改動）；m0 概述納第五項＋補變更記錄段＋核對句
  同步；本檔上節裸數字補複驗指令（歷史層原文語義不變）。

## 驗收後修正（PR #53 第 11 輪＝Codex 第 9 輪五條，2026-08-17）

- 核對基準統一設計解（三條合併）：回應輪＝對**上輪 push 的 HEAD** 取**兩點** diff
  ——兩點治 rebase 後三點退回 merge-base（原始 hunk 冒充修復）；「作者 push 時自記
  SHA」治判詞不帶 SHA（claude-review.yml 明載不內嵌）；「含首輪意見」治撤回型修復
  在累計 diff 無 hunk 被誤判缺項。初始盤點（對 base）與修復證據分離。15.4 push 末步
  增記 HEAD；AGENTS 摘要與 m0 段同步。
- worklog 複驗指令換真 SHA（4c75066）；兩個全稱句各補列舉（七項自引缺陷）與機械查法
  （`git diff 4c75066 66a02d4 -- CLAUDE.md`），語義等價標當時人工快照。
  <!-- 🔴 2026-08-17 更正（Codex 第 10 輪）：上行所述當時以**就地改寫**第 9 輪節實施，
       違反歷史層規則；第 12 輪已還原第 9 輪原文、改為其下追加更正註記。本行敘述保留當刻認知。 -->
- handoff §① 回寫為指標式終態摘要（條文唯一權威＝CLAUDE 15；修訂主軸四類），
  不再逐輪羅列而漂移。

## 驗收後修正（PR #53 第 12 輪＝Codex 第 10 輪三條，2026-08-17）

- 歷史層違規自糾：第 9 輪節就地改寫還原為 856d40e 原文，可執行指令、七項列舉與
  機械查法改為其下追加 HTML 更正註記（帶日期／來源／查法）；第 11 輪節補違規註記。
- 15.1② 限定**僅 🟡**（🔴 不適用裁定不修，同驗收基準節——原文缺限定使紅燈可經②
  無 hunk 通過缺項公式）。
- 15.4 push 末步增**輕量基準 ref**（`git tag -f pr{N}-last-push`）：僅記 SHA 字串在
  rebase／force-push 後換 clone 接手時 object 不可達（fatal: bad object）；新環境以
  基準 ref 或 `git fetch origin pull/{N}/head` 取回。AGENTS／m0 同步。

## 驗收後修正（PR #53 第 13 輪＝Codex 第 11 輪一條，2026-08-17）

- 基準 ref 由本地 tag 改**推送遠端**（`git push -f origin pr{N}-last-push`，合併後刪）：
  本地 tag 在「force-push 後換 clone 接手」情境同樣不可達，且 `pull/{N}/head` 只指向
  現任 head、取不回上一輪——上輪修法自身不覆蓋其宣稱情境的第二例（第一例＝僅記 SHA）。
  新環境取法＝`git fetch origin tag pr{N}-last-push`。AGENTS／m0 同步。

## 驗收後修正（PR #53 第 14 輪＝bot 第 13 輪 🔴1🟡1，2026-08-17）

- 🔴 終態層封閉補課：第 13 輪只改了 CLAUDE 條文＋AGENTS 順序句＋m0 變更記錄行，
  m0 第五機制段本體與 handoff §① 主軸①仍停「由 push 時自記」——兩處各補
  「並推送遠端（合併後刪）」；AGENTS 15.1 摘要句同補（bot 固定核對項「輕量 ref
  在庫文本一致」）。「自記」語義的在庫副本以 `grep -rn "由 push 時自記"` 複驗一致。
- 🟡 m0 變更記錄行裸 `pull/{N}/head` 包 code span（渲染保真——花括號裸置進 HTML 解析）。
- ⚪ PR 描述陳舊四點（④命令／「六輪」／文檔欄／⚪ 節缺輪次）本輪以網頁編輯更新
  （bot 明示不觸 15.4 動檔條款）。

## 驗收後修正（PR #53 第 15 輪＝bot 第 14 輪 🟡1，2026-08-17）

- 上輪 🟡 修錯對象自糾：被點名的是 CLAUDE 15.1 的裸 `<sha>`（「審 `<sha>`」——尖括號
  進 HTML 解析被吞），我卻包了 m0 的 `pull/{N}/head`。本輪包對目標，並對四檔全掃
  「反引號外的尖括號 token」＝僅此一枚（HTML 註釋行除外）。教訓：渲染保真類意見
  必須回到**被點名的檔與行**核對，不得憑掃描結果推定對象。
  <!-- 🔴 2026-08-17 更正（bot 第 15 輪）：本節引文自身原含一枚裸 <sha>（已包 code span）；
       「四檔全掃＝僅此一枚」當刻為假——掃描時點在本節寫入之前、且未含本檔。原敘事保留。 -->

## 驗收後修正（PR #53 第 16 輪＝bot 第 15 輪 🟡1，2026-08-17）

- 第 15 輪節引文的裸 `<sha>` 包 code span；「僅此一枚」假宣稱加更正註記（根因＝掃描
  先於本節寫入、且掃描範圍不含 worklog 自身）。本輪起：裸 token 掃描＝commit 前
  最後一步、含全部五檔（worklog 在內）、在本輪全部文字寫完後執行。
  <!-- 🔴 2026-08-17 更正（Codex 第 12 輪）：上行「全部五檔」未列舉——五檔＝CLAUDE.md、
       AGENTS.md、docs/dev/m0-review-convergence.md、docs/handoff/2026-08-17-鐵律15提交前復核.md、
       本檔。原文保留。 -->

## 驗收後修正（PR #53 第 17 輪＝Codex 三條，2026-08-17）

- 15.4 新判詞分支「回 15.1」改「回本款起點」（缺陷 hunk 自身可滿足 15.1 公式）。
- fetch 基準 tag 加 -f（本地同名 tag 已存在時 would clobber 失敗）。
- 上節「全部五檔」全稱補列舉更正註記（歷史層原文保留）。
