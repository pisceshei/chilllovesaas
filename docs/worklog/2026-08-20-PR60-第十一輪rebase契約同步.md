# PR #60 第十一輪：rebase 與契約同步

> 對應規格：`CLAUDE.md` 鐵律 15–18 ｜ commit：本檔同提交

## 已完成的工作 (Done)

- 將 PR #60 的 10 個提交由舊 main `31d9337` rebase 到 PR #58 的合併提交 `46f6133`；原遠端 head `8a2350c` 由 `pr60-last-push` 保留，rebase 後起點為 `8159a2c`。
- 兩個衝突檔依已裁定契約合併：保留第五參數 `DEADLINE_S`，同時沿用鐵律 17.1 的 INTERVAL 900–1500 秒，並將 MAX_POLLS 上限按 35 天 ÷ 900 秒收斂為 3360。
- 將 `CLAUDE.md` 的操作式補成五參數；將 workflow 內 external-facts 的臨時分支 fallback 改為 main 已落地後的直接讀取，保留「被審 PR 刪除或破壞時查官方來源」的 fail-closed 降級。
- 已實跑 poller 語法與參數邊界；workflow checker 確認 2 份 YAML、33 個 `run:` 區塊與 11 條回歸情境通過。Windows 本機須用 Git Bash PATH；否則會誤叫 WSL bash 而讀不到 Windows temp 檔。

## 修改的檔案與核心邏輯 (Changes)

- `scripts/await-verdict.sh`：把 #60 的 deadline 能力與 #58 的等待間隔裁定合成單一契約；MAX_POLLS 先用十進位字串長度比較上限，避免預設乘積進入 Bash 算術前溢位。
- `CLAUDE.md`、`docs/dev/m0-automation-infra.md`：同步五參數用法、900–1500 秒 INTERVAL、3360 MAX_POLLS，以及「exit 5 只界定等待、不界定 API 呼叫」的誠實範圍。
- `.github/workflows/claude-review.yml`：PR #58 已進 main，移除只為「檔案尚未落地」存在的遠端分支 fallback；不採其他替代檔，避免被審內容指定新的事實來源。
- `docs/worklog/2026-08-19-P8補審-approve綁定斷言更正.md`：保留歷史原句，追加 rebase 現值錨與第十一輪 Changes 索引。
- `docs/worklog/2026-08-20-PR60-第十一輪rebase契約同步.md`、`docs/handoff/2026-08-20-PR60-第十一輪rebase契約同步.md`：本可驗收單位的三件套文件。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本 PR 修改 `.github/workflows/claude-review.yml`；Claude action 會因 default-branch workflow validation mismatch 結構性跳過，success check 不等於 Claude 判詞，依鐵律 18.3 仍須人工合併。
- `fetch_state` 的 API 呼叫本身仍不受 `DEADLINE_S` 約束；本輪只處理已點名的契約一致性，沒有擴修該已登記缺口。
- push 後仍須取得 GitHub Codex 對新 exact head 的審查與 GitHub CI；rebase 前 `8a2350c` 的 Codex 無重大問題結論不得沿用。
