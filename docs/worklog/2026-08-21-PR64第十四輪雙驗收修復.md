# PR #64 第十四輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `26fc683e40bb8ad6466d082c6887876345f84646`：Claude comment
  `5363665327`／wrapper `5363667739` 為通過、🟡0／⚪3；Codex review `4988636859` 的兩條
  inline `3826327584`／`3826327592` 分別指出 A10 沒寫 repository fallback endpoint／query，
  以及 B9 把 `-F @path` 說成唯一 file-content 供給形態。CI 三綠不等於雙清。
- 依鐵律 16 以 `git log -p` 追 A10 與 B9 的引入沿革；兩段都曾補官方逐字，但 A10 沒保留
  List commits 的連結目標與 `sha` 參數，B9 又把「literal `@path` shorthand 的差異」外推成
  所有 request body 供給的絕對要求。
- GitHub REST 官方 <https://docs.github.com/en/rest/commits/commits#list-commits>（取證
  2026-08-21）對 `sha` 逐字為 "SHA or branch to start listing commits from."。A10 現明列
  fallback `GET /repos/{owner}/{repo}/commits?sha={pull_head_sha}`；對本 exact head 實跑第一筆
  SHA 逐字相同。
- A10 同時 fail-closed：官方只定義從 SHA 開始列 ancestor stream，沒有給只取 PR delta 的
  停止參數或 merge-base exclusion 算法；endpoint／起點已取得，PR-only 裁切仍明標未取得。
- GitHub CLI 官方 <https://cli.github.com/manual/gh_api>（取證 2026-08-21）除 `-F @path` 外，
  另明列 `-F key=@-` 與 `--input file/-`。兩路對 Markdown API 實跑均 exit 0，並各自取得
  預期 `<h1>` canary；B9 已收窄為 literal `@path` shorthand 的規則。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | A10 已在第八輪補超限逐字，卻漏可執行 fallback endpoint／query；B9 已在第十三輪集中契約，卻把單一 shorthand 寫成絕對要求 |
| 既有防線失效原因 | 只保存「改用某 endpoint」的連結文字，沒有同時保存目標 path／必要 query／回應邊界；只對照 `-f` 與 `-F`，沒有枚舉同頁的 stdin／whole-body 入口 |
| 被漏掉的固定步驟 | 外部操作契約須列 producer、endpoint、必要參數、輸出集合邊界及同頁替代入口；缺任何一格即標未取得 |
| 可重跑反向複驗 | repository commits 以 `sha=26fc683…` 回第一筆同 SHA；`-F text=@-`／`--input -` 各回預期 `<h1>`；不得把 ancestor stream 直接叫 PR-only 集合 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗 |
|---|---|---|
| ① 驗收攝取 | issue、review body、paginated inline 與 GraphQL threads 綁 exact head | Claude／Codex IDs 與 threads `PRRT_kwDOT0QjlM6a_3yE`、`PRRT_kwDOT0QjlM6a_3yJ` 可重取 |
| ② producer／consumer／終態 | external-facts A10／B9、本 worklog、91 A.1 同步 | A10 有 endpoint＋sha＋PR-only 邊界；B9 有 shorthand＋替代入口 |
| ③ 完成性聲明 | endpoint 與起點稱已取得，PR-only 裁切稱未取得 | 不把 head ancestor stream 冒充 exact PR commit set |
| ④ 狀態空間 | PR endpoint ≤250／超限 fallback、file field／stdin field／whole body 分格 | 三種 body 供給形態不再互相取代；超限路徑有明確 endpoint |
| ⑤ fail-open | HTTP exit 0 仍須釘第一筆 SHA 或 HTML canary | head SHA 相等、兩個 `<h1>` 內容相等才算入口取得 |
| ⑥ workflow 本機假綠 | 本輪不改 workflow／scripts／config；仍須 exact-head GitHub CI | 受限路徑差異必須空，舊 head 三綠不可外推 |
| ⑦ Markdown／Windows | 外部 facts 與 worklog 走 GitHub `/markdown` 結構複驗；最後 edit 後跑 29 閘門 | 表格 tr／th／td 非空、完整 Windows runner 任一非零即停止 |

<!-- 🔴 2026-08-21 第十五輪更正（Claude comment `5363892357` 🟡1）：本表第三欄誤刪鐵律 20.3
     固定名稱中的「輸出」，七列也只寫待驗宣稱。
     🔴 **2026-08-22 第十六輪再更正（Codex inline `3826627161`）——連更正的方式本身也錯了**：
     上一輪是**就地改寫**這張歷史表（表頭與七列第三欄全換），`git log -p` 可見改寫發生在
     commit `82e2fa8`。歷史層 worklog 記的是**那一輪當時說了什麼**，就地改寫會把稽核軌跡
     抹掉——讀者再也看不到第十四輪原本寫的是待驗宣稱。
     ⇒ 本輪**還原上表為第十四輪原文**，更正後的內容改以下方附加表呈現。 -->

#### 上表的第十五／十六輪更正版（附加，不取代原表）

| 20.2 適用類型 | 固定處理 | 反向複驗**輸出** |
|---|---|---|
| ① 驗收攝取 | issue、review body、paginated inline 與 GraphQL threads 綁 exact head | `claude=【驗收結論】通過`；`codex_review=COMMENTED`；`inline=3826327584,3826327592`；`threads=30,hasNext=false,targets=PRRT_kwDOT0QjlM6a_3yE,PRRT_kwDOT0QjlM6a_3yJ` |
| ② producer／consumer／終態 | external-facts A10／B9、本 worklog、91 A.1 同步 | `A10=1`；`B9=1`；`W14=1`；`A1=1` |
| ③ 完成性聲明 | endpoint 與起點稱已取得，PR-only 裁切稱未取得 | `repo_first=26fc683e40bb8ad6466d082c6887876345f84646`；A10 輸出仍逐字含「PR-only 集合仍為未取得」 |
| ④ 狀態空間 | PR endpoint ≤250／超限 fallback、file field／stdin field／whole body 分格 | `pr64_count=17`；field stdin 回 `<h1 dir="auto">field stdin canary</h1>`；whole-body stdin 回 `<h1 dir="auto">stdin body canary</h1>` |
| ⑤ fail-open | HTTP exit 0 仍須釘第一筆 SHA 或 HTML canary | raw `-f` 回 `<p dir="auto">@docs/worklog/2026-08-21-PR64第十四輪雙驗收修復.md</p>`；typed `-F` 為 `typed_tr=13` |
| ⑥ workflow 本機假綠 | 本輪不改 workflow／scripts／config；仍須 exact-head GitHub CI | `restricted_count=0`；非空對照組 `docs_count=20` |
| ⑦ Markdown／Windows | 外部 facts 與 worklog 走 GitHub `/markdown` 結構複驗；最後 edit 後跑 29 閘門 | 修正前 HEAD 的 GitHub render 為 `table=2,tr=13,th=5,td=29`；第五輪現行 validator 輸出 duplicate／drop／missing-section／off-date／zero-scan／delete／rename 各自拒絕，normal paths＝15 |

<!-- 🔴 2026-08-22（Claude comment `5364180385` 🟡-2）：上表 ⑦ 列的 `th=` 本輪由 7／8／14
     改為 **5／6／11**，且**明文錨定在受驗樹 `82e2fa8`**。
     🔴 **錨定是必要的，不是修飾**：這幾個數描述的是「這幾個檔渲染出來長什麼樣」，
     而本輪**正在改這幾個檔** ⇒ 不錨定的話，寫下數字的那次編輯自己就會讓它過期
     （實測：本輪替本檔加上附加表之後，本檔在最終樹的 render 變成 `table=3,tr=21,th=8,td=50`）。7／8／14 是 `<th` **前綴**命中數，會一併算進 `<thead>`（各檔 2／2／3 個），
     不是表頭儲存格數。🔴 **這是第三輪已證偽並固定的計數式再次復發**——
     `docs/worklog/2026-08-21-PR64第三輪雙驗收修復.md` 逐字寫過「標籤計數使用 `<tr\b`／`<th\b`／
     `<td\b`，不再用會把 `<thead>` 算進去的 `<th` substring」。復發紀錄見本輪 worklog 的 20.4。
     產生命令：`gh api -X POST /markdown --input <(jq -n --rawfile t <path> '{text:$t,mode:"gfm"}')`
     後以 `<tr\b`／`<th\b`／`<td\b` 計數；同列其餘四個數（table／tr／td／EF blockquote）本輪重算相符。 -->

## 修改的檔案與核心邏輯 (Changes)

- `docs/dev/external-facts.md`：A10 補 repository List commits endpoint、`sha` 起點、實跑與
  PR-only 未取得邊界；B9 收窄 literal `@path` shorthand，補 stdin field／whole-body 入口。
- `docs/specs/91-pit-register.md`：A.1 納入本輪 worklog，不擴修 Claude 三條白項。
- `docs/worklog/2026-08-21-PR64第十四輪雙驗收修復.md`：保存 exact-head 輸入、官方證據、
  20.4／20.3 與本輪待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 最後 repo edit 後須跑 Markdown 結構／GitHub render 與連續 29 道 Windows
   production-equivalent gates；任一非零即停止。
2. commit 後須跑 doc-claims warning 0、鐵律 15.1／15.2 全量攝取；若有晚到意見先處置。
3. #64 為一般 PR；只有新 exact-head CI、Claude、Codex 雙零、approved threads resolved、
   mergeable 與 evaluator `1111` 同時成立，才可依 D31／D32 代行 squash merge。
