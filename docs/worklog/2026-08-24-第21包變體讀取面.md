# worklog：第 21 包（讀取面 options/selectedOptions＋variants connection＋排序鍵一般化）

## 已完成的工作 (Done)

- **KeysetCursor 泛化**：`ORDER_KEYS` 白名單制（created_at／position 各配編解碼器，
  白名單外 KeyError fail-closed）；🔴 預設鍵 payload 形與舊版逐位元組同構
  ⇒ 線上既有 admin 分頁 cursor 繼續可解（spec 以手工造舊 cursor 釘住）。
- **KeysetConnection 泛化**：`(order_key, direction)` 參數化——canonical／backward／
  apply_after／apply_before／cursor 全走同一組鍵；after 的比較子依方向翻轉
  （desc⇒lt、asc⇒gt）。預設 `(created_at, :desc)`＝舊行為零回歸
  （products/collections/inventory 三個既有消費者不動）。
- **變體 connection**：`ProductType.variants` list→connection
  （`(position asc, id asc)`——position 是語義序，排程 §2.1③ 的缺陷正面關掉）；
  `connection: false` 禁 graphql-ruby 內建 Relay extension（會自動注入分頁引數撞名
  ——DuplicateNamesError 實抓）。
- **options／selectedOptions 讀取面**：ProductOptionType／OptionValueType／
  SelectedOptionType 三型別＋ProductVariantConnection/Edge；selectedOptions 走
  connection resolver 的 preload（N+1 守衛 spec：`product_variant_option_values`
  SELECT ≤1 條）。
- **前端同步**：ProductDetailPage query 改 `variants(first: 1) { nodes {…} }`＋
  TS 型別＋兩處 fixture（🔴 送出 payload 的 variants 斷言維持 list 形不動——
  那是 productSet input 不是查詢回應）。
- 測試：新增 7 例（cursor 4＋讀取面 3）；既有 request spec 一處 query 形同步。
  注入回歸：排序鍵退回 created_at ⇒ 2 紅（反序 fixture 如設計轉紅）→還原綠。
- 全套：rspec **494/494**、vitest 60/60、typecheck、rubocop 0、brakeman 0、
  tokens-sync、lint-prototype ERROR 0。

## 修改的檔案與核心邏輯 (Changes)

| 檔 | 變更 |
|---|---|
| `app/graphql/products/keyset_cursor.rb` | ORDER_KEYS 白名單泛化 |
| `app/graphql/products/keyset_connection.rb` | (order_key, direction) 參數化 |
| `app/graphql/types/product_type.rb` | variants→connection＋options 欄 |
| `app/graphql/types/product_variant_type.rb` | selected_options |
| `app/graphql/types/{option_value,product_option,selected_option,product_variant_edge,product_variant_connection}_type.rb` | 新建五型別 |
| `app/frontend/admin/pages/ProductDetailPage.tsx`（＋test） | query/型別/fixture 改 connection 形 |
| `spec/graphql/keyset_cursor_spec.rb`／`spec/requests/variant_read_surface_spec.rb` | 新建 |
| `spec/requests/product_set_spec.rb` | 一處 query 形同步 |

## 尚未完成或需注意的風險 (Pending / TODO)

- 隱含變體頁面零回歸由 vitest 60/60 蓋；**多變體 UI 在第 23 包**才有人類可見面。
- collections／products 兩個既有 connection 未改排序鍵（預設路徑零觸碰）；
  inventory_items connection 用自己的 keyset（第 18 包產物）——三者收斂到同一支
  屬未來重構，登記不做（17.2）。
- `selectedOptions` 排序依 option.position——選項重排（第 23 包拖曳）後的順序
  一致性由 22 的寫入面保證，本包只讀。
