# 2026-09-05 動態結帳按鈕（portable-wallets）逐字對位 E18（路線圖 T4）

> 規範＝`docs/dev/e18-dynamic-checkout.md`；取證＝`docs/dev/external-facts.md` §G26；未取得／範圍外＝`docs/specs/91-pit-register.md` §3.87；
> handoff＝`docs/handoff/2026-09-05-動態結帳按鈕E18.md`。裁定依據 D84（2026-09-05：任何不同即修，不再等裁定）。

## 已完成的工作 (Done)

- **取證（hoko.vip，headless Chrome＋curl，2026-09-05）**：①`{{ form | payment_button }}` 骨架逐字；②JS 升級後光 DOM（`shopify-accelerated-checkout` 拆
  `disabled`、加 `requires-shipping=""`；`shopify-buy-it-now-button` 8 屬性；`button.shopify-payment-button__button.shopify-payment-button__button--unbranded`；
  兩層 closed shadow root）與 computed；③head bootstrap 七個節點的順序與本體；④注入的 `<style id="global-shopify-accelerated-checkout-styles">`
  （`5rem`／`none`）與 bundle 的樣式回溯演算法（Io／xa／La／Oa／ip／op／ko）；⑤按下流程：`POST /api/unstable/graphql.json?operation_name=cartCreate`
  （標頭四個、變數、回應全形）→ `location = checkoutUrl` → `302 /checkouts/cn/{token}/zh-hans?_r=…`；⑥五語言 bundle 的 `buy_now` 文案；
  ⑦官方句（help dynamic-checkout／compatibility、shopify.dev payment_button／accelerated-checkout／cartCreate／MoneyV2）。
- **實作**：骨架（filter＋registers）、head bootstrap（`Storefront::DynamicCheckoutHead`，接在 SEO head 段後、只在 200 頁）、`origin:` 進
  `PageRenderer`、模組 `app/assets/storefront/portable-wallets.js`（自訂元素三個、樣式回溯、cartCreate 按下流程、五語言文案代入）、光 DOM 樣式表
  `accelerated-checkout-backwards-compat.css`（規則集對位、我方自寫）、`AssetsController#portable_wallets`／`#accelerated_checkout_css`、
  `Storefront::ApiController#graphql`（cartCreate 一支；token 驗證；形同回應含 `extensions`）、`CartController#checkout_link`（`/cart/c/:token?key=`）、
  `Storefront::AccessToken`／`Storefront::CartKeys`、`CartWriter.add(allow_sold_out:)`、Normalizer 三條規則、四條路由、五語言 `_platform.accelerated_checkout.buy_now`。
- **驗證**：P1–P3（Liquid）、H1–H6（request）綠；本機 mirror 店 headless Chrome：升級後光 DOM／屬性序／文案／`global-…-styles` 文字與 hoko 逐字相同，
  computed 三寬的 payment 子樹只剩 `button opacity 1 vs 0.5`——來源＝本機 mirror 店庫存資料（`data-inventory-quantity="0"`、submit `disabled`）與 hoko
  （`data-inventory-quantity="99"`、submit 未 disabled、`available:false`）不同（見 Pending）。
- 路線圖：T4 改「進行中（本包）」並寫實際範圍；新增 T10（`content_for_header` 完整本尊形）、T11（Storefront API 執行器）。

## 修改的檔案與核心邏輯 (Changes)

| 檔 | 變動 |
|---|---|
| `app/liquid/theme_engine/filters.rb` | `payment_button`：本尊骨架逐字；token 改 `Storefront::AccessToken.for` |
| `app/liquid/theme_engine/runtime.rb` | registers `shop_id`／`buyer_country`／`buyer_locale` |
| `app/liquid/theme_engine/page_renderer.rb` | `origin:` kwarg；200 頁 content_for_header 接 `DynamicCheckoutHead` |
| `app/controllers/storefront/pages_controller.rb` | 傳 `origin: request.protocol + host_with_port` |
| `app/services/storefront/dynamic_checkout_head.rb`（新） | head bootstrap 七節點（script 本體我方自寫） |
| `app/assets/storefront/portable-wallets.js`／`accelerated-checkout-backwards-compat.css`（新） | 模組與光 DOM 樣式表 |
| `app/controllers/storefront/assets_controller.rb` | 兩個新 action（常量預載；語言反查；文案代入；5 分鐘 cache） |
| `app/controllers/storefront/api_controller.rb`（新） | `POST /api/:version/graphql.json` cartCreate |
| `app/controllers/storefront/cart_controller.rb` | `checkout_link` |
| `app/services/storefront/access_token.rb`／`cart_keys.rb`（新） | HMAC 導出的權杖／兩把 key |
| `app/services/storefront/cart_writer.rb` | `add(allow_sold_out:)` |
| `app/services/render_parity/normalizer.rb` | `access-token`／`shop-id` 抹值；bootstrap script 替身；bundle 語言檔名 `LANG` |
| `config/routes.rb` | 模組／CSS／API／結帳連結四條 |
| `config/storefront_locales/{en,zh-Hans,zh-Hant,fr,ja}.yml` | `_platform.accelerated_checkout.buy_now` |
| `spec/liquid/payment_button_spec.rb`（新）、`spec/requests/storefront_dynamic_checkout_spec.rb`（新） | P1–P3、H1–H6 |
| `spec/fixtures/theme_engine/minimal-1.0/templates/product.e18.json`＋`sections/main-product-form.liquid`（新） | 帶商品表單的替代模板（`?view=e18`） |
| `docs/dev/e18-dynamic-checkout.md`（新）、`docs/dev/external-facts.md` §G26、`docs/specs/91-pit-register.md` §3.87、`docs/plans/2026-09-05-全對齊路線圖.md` | 規範／取證／V／路線圖 |

## 對表結果（本機 dev server `mirror.lvh.me:3000` vs hoko.vip 快照 2026-09-05；scratchpad `e18/`）

| 項 | hoko | 本機 | 結果 |
|---|---|---|---|
| 升級後 `.shopify-payment-button` outerHTML（`probe_payment_button.js`） | §G26 | 同（差：`shopify-accelerated-checkout`／buy-it-now／button 多 `disabled`＋`aria-disabled`——本機 submit 鈕 `disabled`） | 結構／屬性序／文案同 |
| `<style id="global-shopify-accelerated-checkout-styles">` | `--…-button-block-size: 5rem; --…-button-box-shadow: none;` | 逐字相同 | ✅ |
| computed（`shopify-accelerated-checkout`／`shopify-buy-it-now-button` 28 屬性子集） | display inline… | 全同 | ✅ |
| computed button | opacity 1 | opacity 0.5（`disabled` ⇒ Ella `[disabled]{opacity:var(--opacity-50)}`） | 資料態差 |
| computed 三寬 diff（`report-products_acme-tee-{1280,768,390}-open.md`）payment 子樹 | — | 各只剩 1 列（同上 opacity） | 其餘段差＝本機 dev 資料／時序（header 語言選單、promotion popup、multitasking bar），bt3 部署後再對 |

## 閘門（凍結 tree （首候選；commit 前的工作樹＝本 commit 內容） 後全跑，本機，dev server 已停；scratchpad `gates_e8b.py` 以 `PYTHONIOENCODING=utf-8` 逐支以 bash.exe 執行 config/ci.rb 步驟）

| 閘門 | 命令 | 退出碼 | 摘要 |
|---|---|---|---|
| Style: Ruby | `bin/rubocop` | exit=0 | 998 files inspected, no offenses detected |
| Security: Gem audit | `bin/bundler-audit` | exit=0 | No vulnerabilities found |
| Security: Frontend audit | `pnpm audit --audit-level high` | exit=0 | No known vulnerabilities found |
| Security: Brakeman code analysis | `bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error` | exit=0 | No warnings found |
| Test: Rails | `bundle exec rspec` | exit=0 | 總數行不在 log 尾段（尾段被 rack 棄用警告佔滿）；總數以 CI run 為準 |
| Test: Frontend | `pnpm test` | exit=0 |  |
| Type check: Frontend | `pnpm typecheck` | exit=0 |  |
| Build: Frontend | `pnpm build` | exit=0 |  |
| Invariants: Exec bits | `bash scripts/check-exec-bits.sh` | exit=0 |  |
| Invariants: Exec bit rules regression | `bash scripts/test-exec-bits-rules.sh` | exit=0 |  |
| Invariants: Prototype lint | `python scripts/lint-prototype.py` | exit=0 | ERROR 0 / WARN 136 |
| Invariants: Lint rules regression | `python scripts/test-lint-rules.py` | exit=0 |  |
| Invariants: Dead-control baseline | `python scripts/check-baseline-raise.py` | exit=0 |  |
| Invariants: Doc claims | `ruby scripts/check-doc-claims.rb` | exit=0 |  |
| Invariants: Doc claim rules regression | `ruby scripts/test-doc-claims-rules.rb` | exit=0 |  |
| Invariants: Tenant isolation | `ruby scripts/check-tenant-isolation.rb` | exit=0 |  |
| Invariants: Design token single source | `ruby scripts/check-tokens-sync.rb` | exit=0 |  |
| Invariants: Reversal naming | `ruby scripts/check-reversal-naming.rb` | exit=0 |  |
| Invariants: Money unit boundary | `ruby scripts/check-money-boundary.rb` | exit=0 |  |
| Invariants: Money rules regression | `ruby scripts/test-money-rules.rb` | exit=0 |  |
| Invariants: Limits key types | `ruby scripts/check-limits-keys.rb` | exit=0 |  |
| Invariants: Limits key rules regression | `ruby scripts/test-limits-key-rules.rb` | exit=0 |  |
| Invariants: Rem token integrity | `ruby scripts/check-rem-tokens.rb` | exit=0 |  |
| Invariants: Rem token rules regression | `ruby scripts/test-rem-token-rules.rb` | exit=0 |  |
| Invariants: CI parity (ci.yml ⊆ config/ci.rb) | `ruby scripts/check-ci-parity.rb` | exit=0 |  |
| Invariants: CI parity rules regression | `ruby scripts/test-ci-parity-rules.rb` | exit=0 |  |
| Invariants: Workflow syntax | `ruby scripts/check-workflow-syntax.rb` | exit=0 |  |
| Invariants: Workflow syntax rules regression | `ruby scripts/test-workflow-syntax-rules.rb` | exit=0 |  |
| doc-claims 兩支（worklog 閘門表寫入後補跑） | `ruby scripts/check-doc-claims.rb`／`ruby scripts/test-doc-claims-rules.rb` | 見 PR body | |

### 突變（commit `84abd2a6` 後跑 scratchpad `e18/mutate_e18.py`；每格改一處 → 目標規格轉紅 → `git checkout --` 還原；工作樹還原後 clean）

| 突變 | 改處 | 目標 | 結果 |
|---|---|---|---|
| M193 `payment_button` 恆回空字串 | filters.rb | P1 | RED ✓ |
| M194 bundle 語言碼不小寫（`zh-TW.js`） | dynamic_checkout_head.rb | H1 | RED ✓ |
| M195 200 頁不接 bootstrap | page_renderer.rb | H1 | RED ✓ |
| M196 模組不代入文案 | assets_controller.rb | H2 | RED ✓ |
| M197 `amount` 不除以 100（cents 直出） | api_controller.rb | H3 | RED ✓（第一版突變「`to_s` 取代 `to_s("F")`」殺不掉＝等價突變：ActiveSupport `core_ext/big_decimal/conversions.rb` 已把 `BigDecimal#to_s` 預設成 `"F"`；換成 cents 直出） |
| M198 售罄不建 cart（`allow_sold_out: false`） | api_controller.rb | H4 | RED ✓ |
| M199 結帳連結不驗 key | cart_controller.rb | H5 | RED ✓ |
| M200 Normalizer 不抹 bootstrap 本體 | normalizer.rb | H6 | RED ✓ |
| M201 token 不驗（恆 true） | access_token.rb | H4 | RED ✓ |
| M202 cart id key 取 16 hex | cart_keys.rb | H3 | RED ✓ |

## 尚未完成或需注意的風險 (Pending / TODO)

- **bt3 部署後複驗**（同 E17 收尾 PR）：mirror.chilling.com.hk 三寬 computed vs hoko、升級後 DOM probe、按下 → `cartCreate` → `/cart/c/{token}` → `/checkouts/{token}` 全鏈路。
- **庫存狀態模型差**（新發現，登記 91 §3.87）：hoko 同一變體 Liquid `inventory_quantity`＝99、`inventory_policy: deny`，而 `.js`／`available`＝false ⇒ Ella
  不出 `hidden`／`disabled`；我方 mirror 店資料 `inventory_quantity`＝0。本尊的「available ≠ inventory_quantity > 0」（地點可履行／不可用量）尚未建模，
  歸 T5 資料集擴充＋庫存包。
- 91 §3.87 其餘 V（enabled-flags 語義、錯 token 回應形、discountCodes／多行、`cart_changelog` 全文、zero-decimal amount、key 長度、302 目標形、非 200 頁 head、
  失敗 UI、cart 面元素、media 分支）。
- 🔴 dev server 不 reload theme_engine／模組常量：本機對表期間改檔（含 `touch`）＝下一請求 500；一律重啟。
- 自訂元素 MutationObserver 自迴圈（已修：過濾自己子樹＋屬性只在改變時寫；登記 91 §3.87）。
