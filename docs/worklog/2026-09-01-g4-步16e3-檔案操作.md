# 2026-09-01 G4 步 16e3：code editor 檔案操作

## 已完成的工作 (Done)

- `themeFileDelete`：刪 overlay 列——語義雙形（🔴 被覆寫 base 檔＝**還原原始
  版本**、overlay-only 新檔＝**刪除檔案**；F9/MF-1 紅證含 touch theme）；
  無列 NOT_FOUND。base 檔本體不可刪（tombstone＝91 §3.71 ⚪ 不變）。
- `overlayState` 讀面：path→overlaid|new 圖（base list 對照；F10/MF-2 紅證
  ——全標 new 的退化被殺）。
- code editor UI：目錄列「新增檔案」（官方「選資料夾→New file→輸入檔名」形；
  templates/ 不給建——雙真相源禁令）→ 空 tab 即 dirty → 首存 lockVersion
  null（CE4）；被覆寫檔出「還原原始版本」／新檔出「刪除檔案」→
  themeFileDelete＋關 tab（CE5/MF-3 紅證）；儲存後新檔入樹＋標狀態。
- 突變 3/3 殺（MF-1 touch／MF-2 分類／MF-3 前端 mutation 斷線）。

## 修改的檔案與核心邏輯 (Changes)

- 新：theme_file_delete.rb。
- 改：theme_type（overlayState）、mutation_type、CodeEditorPage（New file＋
  revert/delete＋save 後入樹）、CodeEditorPage.test（3→5 例）、
  theme_file_overlay_spec（8→10 例）、i18n 6 鍵 ×5、admin.css。

## 尚未完成或需注意的風險 (Pending / TODO)

- rename 未做（官方右鍵 rename——我方 overlay rename 對 base 檔需 tombstone
  先行）＝91 §3.71 軸；window.prompt 素 UI（modal 化後補）。
- 步 16 至此七包（16a shell／16b op-stack／16c picker／16d 控件／16d2 佈景
  設定／16e1 覆寫層／16e2+16e3 code editor）——視覺編輯器＋佈景設定＋code
  editor 主線齊；殘餘豐富化（資源選擇器/undo 整合/即時預覽/語法高亮/
  Timeline）全在 91 §3.69-72 登記，依 20 步方案先進下一步。
- 生產煙測：部署後 overlay 建→還原→自清（16e1 煙測型擴 delete）。
