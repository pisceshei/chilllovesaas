# 2026-08-21 — P-2 宣稱索引與 doc-claims R6

## 已完成的工作 (Done)

- 依 `git log -p`、來源文件結構與可重跑 Ruby 命令，逐組核對階段一'方案點名的六組假數字；
  沒有把驗收敘述直接當修法。
- 新建 `docs/specs/92-claim-index.md`，以六個 `CLAIM-NNN` 區塊記錄原宣稱、更正值、
  baseline 與可執行 `recheck:`。歷史 token 計數固定在 commit
  `e50b9120cc9b2514fde4995a5ff4f6ff15332bff`，明文禁止外推目前 HEAD。
- `scripts/check-doc-claims.rb` 純增量加入 R6：只納管 `docs/specs/92-*` 的結構化
  `type: count` 區塊；缺可辨識複驗命令即阻擋。R1–R5 既有對象與判定語義不變。
- 新增 R6 正、反 fixture。定向回歸實跑為十二個 fixture case 與三個 git 情境全綠
  （複驗：`ruby scripts/test-doc-claims-rules.rb`）；
  六條索引命令分別輸出 16/10/26/314、十個 P0 ID、76、32/33、82/4/78、162/7/11/180。
  > 🔴 2026-08-21 驗收更正（Codex review `4985224623`、Claude comment `5359074865`）：
  > 上述為首推時的歷史輸出，不能代表修復後終態。C-16 是正面對照、P0-14／P0-18 是部分修復、
  > 混合狀態仍含未結案標記，180 只是未去重列次；終態值與可執行斷言見
  > `docs/specs/92-claim-index.md` CLAIM-001／002／003／006。R6 測試終態以
  > `ruby scripts/test-doc-claims-rules.rb` 的當前輸出為準。
- Windows production 等價完整閘門已在本 worklog 寫入後重跑，29／29 全綠：
  Ruby 162 檔零違規、
  兩側 audit 零漏洞、Brakeman 0 errors／0 warnings、Rails 284 examples／0 failures、前端
  5 tests／0 failures、原型 lint `ERROR 0 / WARN 136`、doc-claims warning 0、workflow
  2 份 YAML／33 個 run 區塊與其回歸測試全綠。Node v24.19.0 超出專案宣告的 Node 22
  範圍；本結果只證明此環境實跑，不宣稱專案已支援 Node 24。
- 對本包改動的三個表格片段呼叫 GitHub `/markdown` API：50 附錄為
  `tr/th/td=7/3/18`、53 總表為 `11/5/50`、本 worklog Changes 表為 `11/2/20`，均與來源列／欄
  計數一致；沒有用本機 YAML／Markdown 猜測代替 GitHub 實際渲染。
- 已通讀本 worklog；提交前 Markdown 全檔結構複驗另發現總方案、53、83 的既有裸直線／
  圍欄問題。這些位置未被 P-2 點名，未順手修改，已依鐵律 17.2 集中登記到 91 §3。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `docs/specs/92-claim-index.md` | 建立六組結構化計數宣稱與各自 baseline／複驗命令 |
| `docs/specs/50-logic-gap-register.md` | 表 3 依語義分為 15 條 C 衝突＋1 條正面對照＋10 條 T 衝突，缺口 25、全簿 313；P1 區分 32 個來源分組與 33 個下游項 |
| `docs/specs/83-admin-1to1-audit-round3.md` | §1.1 明列十筆 P0 複驗為 8 已修復／2 部分，不再把列數當完成數 |
| `docs/specs/84-m1-gate-triage.md` | 把 81 限定為建檔文字，現值改由 71 最後一欄含未結案標記的列重算為 77 |
| `docs/specs/51-token-conformance.md` | 82／4／78 加 immutable historical snapshot 邊界 |
| `docs/specs/53-ui-gap-recheck.md` | 162 正名為表 1–5 小計，180 明定為未跨章去重的原始列次，不宣稱 distinct gap 總數 |
| `scripts/check-doc-claims.rb` | 新增 R6、畸形／游離標頭阻擋與生產索引零供給 canary；射程仍只到 `docs/specs/92-*` |
| `scripts/test-doc-claims-rules.rb`、`spec/fixtures/ci_violations/doc_claim_*` | 補正常、違規、假命令、零標頭、標頭前計數、畸形標頭與生產零供給的反向證明 |
| `docs/dev/m0-review-convergence.md`、`docs/plans/2026-08-18-總方案.md`、`docs/specs/91-pit-register.md` | 同步 R6 規則、納管、fixture、突變與窄例外終態；保留 specs 其餘部分未廣泛納管的既有邊界 |
| `docs/worklog/2026-08-21-P2宣稱索引與R6.md` | 本工作單位證據、改動與待辦；附錄 A 同 commit 收割 |
| `docs/worklog/2026-08-21-PR65首輪雙驗收修復.md` | 首輪 Codex／Claude 意見、鐵律 20.3 稽核與修復證據 |

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本 worklog 寫入閘門證據後，已依鐵律 15.4 從 setup 起完整重跑 29 支閘門且全綠；其後仍須
   commit、commit 後 doc-claims warning 0、push 與 GitHub exact-head CI／Codex／Claude 驗收。
2. 本包修改 `scripts/`，命中鐵律 18.3。即使雙零與評估器 1111 成立，也只能通知使用者人工合併，
   不得代行 `gh pr merge`。
3. R6 是 `docs/specs/92-*` 的窄例外，不代表 research／design／其餘 specs 已被 R1／R3
   全面納管；91 既有 F12 缺口仍保留。
4. Markdown 複驗發現的總方案／53／83 既有破版候選只在 91 §3 登記；本包沒有獲得修改
   那些未點名位置的授權，後續須獨立立包處理。
