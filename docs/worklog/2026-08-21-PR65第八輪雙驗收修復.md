# PR #65 第八輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `eb282ab48bf9ca856e2af8f167d08c57d9789206`：Claude comment
  `5362007581`、wrapper `5362012265`；Codex review `4987453495` 的新 inline 是
  `3825428475`。quality／test／Claude workflow 在該 head completed success，但兩方仍有
  未清意見，故沒有把綠 workflow 誤報成雙清。
- 修法前用 `git log -p` 追 checker、runner、fixtures、92 與 m0 的演進。把既有 duplicate
  fixture 第二個鍵改成 `Recheck :` 後，舊 checker exit 0，runner 精確輸出以下 dated
  failure-first snapshot：

~~~text
doc_claim_duplicate_recheck：期望 exit 1，實得 0
文檔引用保真檢查器的回歸測試失敗（1/33）
~~~

- Claude 🟡1 成立：`CLAIM_RECHECK_CMD` 的 optional `bundle exec` 前置組與工具 alternation 的
  `bundle` 分支語言等價。刪除前置組並同步註釋；`bundle exec ...` 仍由「以 bundle 起頭」
  規則涵蓋，不宣稱 wrapper 有獨立承重。
- Claude 🟡2 成立：`CLAIM_RECHECK` 改為鍵大小寫／冒號前空白不敏感。保留既有精確重複
  fixture，另加單一 `Recheck :` 合法反向案例與精確鍵加變體鍵的重複拒絕案例；checker
  檔頭、92 契約與 m0 終態層同步。
- Claude ⚪只登記到 91 §3：只有工具名的 code span 仍合近似語法，不在本輪擴寫 command
  grammar。
- Codex inline `3825428475` 經獨立 Git 物件複驗證偽：baseline commit 是 `origin/main` 祖先，
  `git cat-file -e` 對兩個 source path 都 exit 0；從 92 抽出 CLAIM-001 至 CLAIM-006 原文命令
  逐條實跑均 exit 0，其中 CLAIM-005 原始輸出如下，故不換 baseline、不改 claim：

~~~text
CLAIM-005 exit=0 stdout="{declarations: 82, overlap: 4, new_tokens: 78}" stderr=""
~~~

- 修後定向實跑 snapshot：新 duplicate-key-variant fixture exit 1 且命中 duplicate 訊息；
  單一 key-variant fixture exit 0；完整 runner 通過並輸出
  `35 條 fixture case / 35 個 fixture ＋ 3 條 git 情境＋2 條供給情境`。
- 第一輪 production-equivalent 內容複驗已完成：逐支 runner 的 gate 01–27 都輸出
  `GATE_PASS`；workflow checker 初次誤選 WSL bash，外部 PATH 釘到 Git for Windows 後，
  gate 28／29 原命令分別 exit 0。Rails 為 `284 examples, 0 failures`、frontend 5/5、
  RuboCop 162 files／0 offenses、Brakeman 0 warnings／0 errors、prototype ERROR 0、doc-claims
  warning 0。因本 bullet 是其後的最後 repo edit，仍不能拿該輪當提交前正式末輪。
- GitHub Markdown API current response 取證日期 2026-08-21，端點
  <https://api.github.com/markdown>；本檔回應中的逐字標記包含
  `<table role="table">`、`<pre lang="text" class="notranslate">`、`<th>`、`<td>`，且
  current response 無 raw fence 洩漏。這只證明本次送出的 current body，不外推歷史回應。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 第七輪已把 type key 拆成正常／變體／畸形，本輪 `5362007581` 又證明同組 recheck metadata 仍只認精確鍵；同 comment 另抓到 regex 重複分支讓註釋失真 |
| 既有防線失效原因 | 20.3 寫了「結構化欄位分開列 key 狀態」，實作與 fixture 卻只套 type；命令 grammar 又同時用 wrapper 與工具清單表示同一字串 |
| 被漏掉的固定步驟 | 同一 metadata schema 的每個鍵都須套正常／合法變體／畸形／multiplicity；regex 每個分支須有能讓回歸轉紅的獨立語言差集 |
| 可重跑反向複驗 | `ruby scripts/test-doc-claims-rules.rb`；單一 `Recheck :` 必須綠，與精確鍵併存必須紅；wrapper 移除後既有 bundle 語言不變 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取／exact head | issue comment、review body、paginated inline、GraphQL thread 分源重取 | Claude 與 Codex ID 均綁 `eb282ab…`；歷史 inline 不因 current commit 欄位被改寫而誤算成新意見 |
| ② 生產者／消費者／契約層 | checker、檔頭、runner、fixtures、92、m0、91 與本 worklog 同步 | `rg` 核對 recheck 鍵語義與 fixture 名；A.1 同 commit 補本檔 |
| ③ 易腐數字／完成聲明 | runner 數量只作 dated output snapshot；不外推新 head 驗收 | 上方 snapshot 保存實跑原文；GitHub 舊 head 結果明標失效邊界 |
| ④ 狀態空間／multiplicity | recheck 精確鍵、合法變體、單筆、混合重複與命令值分開 | positive key-variant 與 duplicate key-variant 各走獨立 fixture；既有 exact duplicate 保留 |
| ⑤ fail-open／happy path | failure-first 先證明混合鍵重複 exit 0；修後同輸入非零且合法單筆仍綠 | 定向兩個 fixture 與完整 runner 共同通過 |
| ⑥ workflow 假綠／Windows | 本輪不改 workflow／config；scripts 變更仍命中 18.3，只能人工合併 | 完整 Windows runner 待最後 repo edit 後實跑；GitHub 新 head 仍須確認 Claude run 未 skip |
| ⑦ Markdown／歷史層 | 新表格欄數固定；歷史 worklog 原文不回寫 | 本檔經表格直線與 GitHub Markdown API current response 複驗後才送驗 |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：移除語言等價 wrapper pattern，讓 recheck 鍵大小寫／冒號前
  空白不敏感，並同步檔頭與契約註釋。
- `scripts/test-doc-claims-rules.rb` 與 fixtures：保留 exact duplicate，新增合法 recheck key
  variant 與 mixed-key duplicate，更新最低案例下限。
- `docs/specs/92-claim-index.md`、`docs/dev/m0-review-convergence.md`：同步 recheck key 終態語義
  與 fixture 消費者；CLAIM-005 不因已證偽意見而改動。
- `docs/specs/91-pit-register.md`：只登記 bare-tool 近似判定，並補本 worklog 的 A.1 關係。
- `docs/worklog/2026-08-21-PR65第八輪雙驗收修復.md`：保存輸入、沿革、failure-first、證偽、
  20.4／20.3 與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本 bullet 是最後 repo edit；須從 gate 01 重新跑一輪連續 29 項，並重跑六條 CLAIM 原文，
   不能把上方第一輪內容複驗冒充正式末輪。提交後再跑 doc-claims 並要求 warning 0。
2. commit 後須按鐵律 15 對 `pr65-last-push` 逐項核對，三端點與 GraphQL threads 全量重拉；
   Codex 已證偽 thread 要附實跑證據，不能把無改檔誤寫成未處置。
3. #65 修改 `scripts/`，命中鐵律 18.3；即使 exact-head 雙清與 evaluator `1111` 成立，
   也只能通知使用者人工合併，不得代行。
