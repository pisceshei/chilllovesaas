# 工作記錄：主題引擎缺口批次 T14（門市取貨／系列語境上下商品／圖片焦點／format_address）——2026-09-05

> 路線圖 T14（新增列）；規格章節＝`docs/dev/t14-theme-engine-gaps.md`；取證＝`docs/dev/external-facts.md` §G30；未取得＝`docs/specs/91-pit-register.md` §3.91；
> handoff＝`docs/handoff/2026-09-05-主題引擎缺口T14.md`。真店變更清單：無（只讀官方文檔與倉內主題 fixture）。

## 已完成的工作 (Done)

- **取證方法**：新增 conformance 乾跑（scratchpad `t8/conformance_probe.rb`）——以 mirror 店資料＋主題 fixture 逐模板渲染（含 `?view=`），收 HTTP 狀態、
  Liquid error 與 `ThemeEngine::MISSES`。Kalles 75 頁／Minimog 57 頁皆零例外；Minimog 的 18 頁 `image_url` nil 錯與本尊同型。
- **門市取貨**：`locations` 三個取貨欄位；`variant.store_availabilities`（官方兩種情況才有定義，其餘 nil）；`StoreAvailabilityDrop`／`LocationDrop`／`AddressDrop`；
  `format_address` 真實作（原本回空字串）。三套主題（Ella／Kalles／Minimog）的 pickup-availability 區塊自此有資料。
- **系列語境上下商品**：`/collections/{handle}/products/{p}` 帶 `collection` 進 assigns；`collection.previous_product`／`next_product`（序＝系列生效排序，首尾 nil）。
- **圖片焦點**：`files.focal_point` 欄位；`FocalPointDrop`（未設回官方預設 50）／`ImagePresentationDrop`；`ImageDrop`／`FileImageDrop`／`PlaceholderImageDrop#presentation`。
- **非官方屬性顯式宣告 nil**（主題誤用，本尊同回 nil）：`collection.terms`、`search.url`／`id`／`current_vendor`／`current_type`、`shop.taxes_included`。
- **假缺口排除**：`FormDrop.*` 的 miss 來自 Kalles `product-ask-question` 的 `assign name = '…'`＋`form[name]` 慣用法，探針證實輸出與本尊同（§G30）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `db/migrate/20260906090000_add_pickup_settings_and_focal_point.rb`、`db/schema.rb` | `locations.pick_up_enabled`／`pick_up_time`／`pick_up_instructions`；`files.focal_point`（🔴 StoredFile 的表名是 `files`） |
| `app/models/location.rb` | `pickup_enabled` scope＋`pick_up_time` 長度驗證 |
| `app/liquid/theme_engine/drops.rb` | 新 `AddressDrop`／`LocationDrop`／`StoreAvailabilityDrop`／`FocalPointDrop`／`ImagePresentationDrop`；`VariantDrop#store_availabilities`（整段包 `with_tenant`）；`CollectionDrop#previous_product`／`next_product`／`neighbour_ids`／`terms`；三個 image drop 的 `presentation`；`SearchDrop` 四個 nil；`ShopDrop#taxes_included` nil |
| `app/liquid/theme_engine/filters.rb` | `format_address` 依官方例的順序輸出 `<p>…<br>…</p>` |
| `app/liquid/theme_engine/page_renderer.rb` | `within_collection`：系列語境商品頁帶 `collection` |
| `config/limits.yml` | `collection.neighbour_scan_limit`（ours） |
| `spec/requests/storefront_pickup_and_context_spec.rb`（PU1–PU6）、fixture `sections/t14-context-probe.liquid`＋`templates/product.t14.json` | 規格 |
| `docs/dev/t14-theme-engine-gaps.md`、`docs/dev/external-facts.md` §G30、`docs/specs/91-pit-register.md` §3.91、路線圖 T14 列 | 文檔 |

## 閘門（凍結 tree （首候選；commit 前的工作樹＝本 commit 內容） 後全跑，本機，dev server 已停；scratchpad `gates_e8b.py` 逐支以 bash.exe 執行 config/ci.rb 步驟）

| 閘門 | 命令 | 退出碼 | 摘要 |
|---|---|---|---|
| Style: Ruby | `bin/rubocop` | exit=0 | 1012 files inspected, no offenses detected |
| Security: Gem audit | `bin/bundler-audit` | exit=0 | No vulnerabilities found |
| Security: Frontend audit | `pnpm audit --audit-level high` | exit=0 | No known vulnerabilities found |
| Security: Brakeman code analysis | `bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error` | exit=0 | No warnings found |
| Test: Rails | `bundle exec rspec` | exit=0 | （未解析） |
| Test: Frontend | `pnpm test` | exit=0 |  |
| Type check: Frontend | `pnpm typecheck` | exit=0 |  |
| Build: Frontend | `pnpm build` | exit=0 |  |
| Invariants: Exec bits | `bash scripts/check-exec-bits.sh` | exit=0 |  |
| Invariants: Exec bit rules regression | `bash scripts/test-exec-bits-rules.sh` | exit=0 |  |
| Invariants: Prototype lint | `python scripts/lint-prototype.py` | exit=0 | ERROR 0 / WARN 136 |
| Invariants: Lint rules regression | `python scripts/test-lint-rules.py` | exit=0 |  |
| Invariants: Dead-control baseline | `python scripts/check-baseline-raise.py` | exit=0 |  |
| Invariants: Doc claims | `ruby scripts/check-doc-claims.rb` | exit=0 |  |
| Invariants: Doc claim rules regression | `ruby scripts/test-doc-claims-rules.rb` | exit=0 |  |
| Invariants: Tenant isolation | `ruby scripts/check-tenant-isolation.rb` | exit=0 |  |
| Invariants: Design token single source | `ruby scripts/check-tokens-sync.rb` | exit=0 |  |
| Invariants: Reversal naming | `ruby scripts/check-reversal-naming.rb` | exit=0 |  |
| Invariants: Money unit boundary | `ruby scripts/check-money-boundary.rb` | exit=0 |  |
| Invariants: Money rules regression | `ruby scripts/test-money-rules.rb` | exit=0 |  |
| Invariants: Limits key types | `ruby scripts/check-limits-keys.rb` | exit=0 |  |
| Invariants: Limits key rules regression | `ruby scripts/test-limits-key-rules.rb` | exit=0 |  |
| Invariants: Rem token integrity | `ruby scripts/check-rem-tokens.rb` | exit=0 |  |
| Invariants: Rem token rules regression | `ruby scripts/test-rem-token-rules.rb` | exit=0 |  |
| Invariants: CI parity (ci.yml ⊆ config/ci.rb) | `ruby scripts/check-ci-parity.rb` | exit=0 |  |
| Invariants: CI parity rules regression | `ruby scripts/test-ci-parity-rules.rb` | exit=0 |  |
| Invariants: Workflow syntax | `ruby scripts/check-workflow-syntax.rb` | exit=0 |  |
| Invariants: Workflow syntax rules regression | `ruby scripts/test-workflow-syntax-rules.rb` | exit=0 |  |
| doc-claims 兩支（本表寫入後補跑，commit 後再跑一次——R4／R5 只掃已提交的新增行） | `ruby scripts/check-doc-claims.rb --base origin/main --require-base`／`ruby scripts/test-doc-claims-rules.rb` | 見 PR body | |

### 突變（commit 後跑 scratchpad `t14/mutate_t14.py`——v2 紀律：rspec 不走管線、finally 還原、還原後斷言乾淨）

| 突變 | 改處 | 目標 | 結果 |
|---|---|---|---|
| M226 非合格變體也回陣列 | drops.rb | PU1 | RED ✓ |
| M227 逐地點 available 改用整體庫存 | drops.rb | PU2 | RED ✓ |
| M228 `address.street` 不合併兩行 | drops.rb | PU3 | RED ✓ |
| M229 `format_address` 略過 company | filters.rb | PU4 | RED ✓ |
| M230 系列語境不帶 collection | page_renderer.rb | PU5 | RED ✓ |
| M231 焦點未設回 0（非官方預設 50） | drops.rb | PU6 | RED ✓ |

## 尚未完成或需注意的風險 (Pending / TODO)

- bt3 部署後複驗（收尾 PR）：migration 上線、mirror 店建一個取貨地點後商品頁出 pickup 區塊、系列語境商品頁上下商品。
- 91 §3.91 V：Expected pickup date 值域、非合格變體的實際回傳、`format_address` 逐國順序、latitude／longitude、metafields、
  country 物件、焦點 admin 面、系列成員變動不旋轉商品頁快取、系列查無時的 404 語義。
- 🔴 `store_availabilities` 必須整段在 `ActsAsTenant.with_tenant` 內（`first_available_variant` 也會讀 tenant-scoped 的 inventory_item）。
- 🔴 多變體規格一律顯式給 `position:`——工廠的 `sequence(:position)` 是全域的，單跑與整套跑順序會不同（PU1 踩過）。
- admin 面（取貨設定、圖片焦點）＝後續包；`address` 欄位在既有資料是空 JSON，`format_address` 會出 `<p></p>`（T5 補資料）。
