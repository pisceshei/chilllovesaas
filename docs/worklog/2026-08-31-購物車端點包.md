# 2026-08-31 — 購物車端點包（specs/15 F1 落地＋bundled section rendering）

## 已完成的工作 (Done)
- 六項順序計畫的最後一項。specs/15 F1 的預裁全數落地：
  - `carts`＋`cart_line_items` 遷移（merge_key_hash 唯一索引以 shop_id 開頭
    〔鐵律 2〕；變體刪除 CASCADE；90 天 purge 掃描鍵就位）。
  - `Storefront::CartWriter`：add＝MySQL upsert 撞唯一索引 `quantity+VALUES`
    （F1 #5 併發收斂）；同 variant 不同 properties＝合法多行；售罄閘與
    VariantDrop 同軸；兩層上限（max_quantity_per_line／max_lines）；
    clear 清行保留 note/attributes（官方語義）。
  - `Storefront::CartSerializer`：頂層 14 鍵（83 §3.3 live 對照）＋item 核心
    子集；金額 integer cents 直通；顯示價＝即時、unit_price_cents＝快照
    （F1 #3 雙層）。
  - `Storefront::CartController`＋路由：`.js` 與裸路徑同義（Ella 用裸形）；
    JSON items 形與 FormData 形都收；錯誤形＝真店 422 三鍵
    `{status,message,description}`（83 §12.5 逐字結構；文案繁中）；
    `_cl_buyer` 簽名 cookie **host-only**（F1 ⚠️坑防線）；
    **bundled section rendering**：POST 帶 `sections` ⇒ 回應加 map
    （複用 #203 fragment 機制）。
  - CartDrop 升級：有買家車 ⇒ serializer 直餵（Liquid cart 契約＝/cart.js
    同形）；預覽 controller 讀 cookie 車進渲染。
- 實測補格（83 §12.5）：售罄 add 422 三鍵形逐字捕獲；壞 id／超量兩格被
  Shopify bot 防護（429 Verifying your connection）攔截＝未取得（登記，
  不硬闖）。
- spec：writer W1–W9＋request R1–R7（16 格）；突變 Q1–Q6 全紅。

## 修改的檔案與核心邏輯 (Changes)
- `db/migrate/20260831120000_create_carts.rb`＋schema.rb。
- `app/models/cart.rb`／`cart_line_item.rb`（merge_key=SHA-256 canonical）。
- `app/services/storefront/cart_writer.rb`／`cart_serializer.rb`。
- `app/controllers/storefront/cart_controller.rb`＋`config/routes.rb`。
- drops.rb（CartDrop 真車模式）＋runtime/page_renderer（cart_json 佈線）＋
  preview controller（cookie 車）。

## 尚未完成或需注意的風險 (Pending / TODO)
- 🔴 vehicle：cart 端點目前掛 staff session（預覽面）；公開店面的
  host→shop 解析與買家匿名 session 歸 W6 hosting 包（controller 檔頭③登記）。
- item json 為 live 30+ 欄的核心子集；折扣／selling plan 欄恆空形。
- cart_item_limit（商家可調總件數上限）未接（欄位／設定面未建；兩層防呆
  上限已接）。90 天 purge job 隨排程包。
- Q-44（bundle parent_id 入鍵）維持暫定；離島格（updates 陣列形位置語義）
  以行序實作。
