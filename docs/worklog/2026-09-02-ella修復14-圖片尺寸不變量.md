# 2026-09-02 Ella 修復 PR-14：圖片尺寸不變量（StoredFile 驗證＋回填＋遙測）

## 已完成的工作 (Done)

- 6 軸艦隊 divided-by-0 軸④收口：官方 image 物件 width/height/aspect_ratio
  恆 number、文檔無 nil 態（shopify.dev objects/image 取證 2026-09-01）；
  我方 files 表兩欄 nullable ⇒「有圖無尺寸」是本尊不可能、我方會炸的態
  （Ella `if image` 守衛內除法 30 位點審計，blocks/logo.liquid:44/57 等
  8 處無守衛位點只在我方炸）。三件套：
- **StoredFile 驗證**：`image/* ∧ ready ⇒ width/height 必須為正整數`；
  非圖片與 pipeline 中間態（uploaded/processing/failed）不受限——
  ProcessFile 本來就在 ready 轉場寫入尺寸，封的是繞管線直寫路徑。
- **BackfillDimensions 服務**：存量「ready 圖片缺尺寸」列從 blob 讀實際
  尺寸補寫；blob 缺失/解碼失敗 ⇒ 轉 failed（同 ProcessFile 終態語義）；
  冪等（條件式 scope）。backend 可插拔（本機/CI 無 libvips，同
  process_file_spec 分工）。生產執行證據見 Pending。
- **ImageDrop 遙測**：width/height nil ⇒ count_miss（不改 nil 契約——
  修法在資料層，drop 層只監控不變量是否被繞過）。
- 既有 spec 的 ready 圖片建檔點（7 檔 12 處）補尺寸——它們表示的正是
  不變量要禁止的態。
- 另兩條軸①②結論照登：首頁兩條 `divided by 0`＝主題無守衛除法撞
  「adapt_image＋空背景圖」資料態，**本尊同 gem 語義同樣出錯**；空
  image_picker 回 nil 是官方契約（取證 2026-09-01），不得改成佔位圖。
- 測試 ID1（驗證矩陣）／BF1（回填＋failed 轉場＋冪等）；突變 2/2 殺
  （拔驗證、回填 fail-open 化）。

## 修改的檔案與核心邏輯 (Changes)

- `app/models/stored_file.rb`：requires_dimensions? 條件驗證。
- 新 `app/services/media_pipeline/backfill_dimensions.rb`。
- `app/liquid/theme_engine/drops.rb`：ImageDrop width/height nil 遙測。
- 7 個既有 spec 檔的 ready 建檔點補 width/height。
- 新 spec `spec/services/media_pipeline/backfill_dimensions_spec.rb`。

## 尚未完成或需注意的風險 (Pending / TODO)

- 生產回填待本 PR 部署後以 runner 執行一次並把輸出補登於此（預期 0 列
  ——demo 資料全走管線/seed 皆帶尺寸；斷言前不寫死）。
- search_controller image_aspect 同形 nil（艦隊同軸點名）＝呼叫面有守衛，
  暫不動，登記於此。
- assign 位除零被 Liquid 靜默吞（軸⑤機制實錘）：回歸判準不能只掃
  'Liquid error'——已入 handoff 注意事項。
