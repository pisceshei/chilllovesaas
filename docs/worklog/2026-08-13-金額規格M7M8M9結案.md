# 金額規格 M-7／M-8／M-9 結案（登記 5 處，grep 實改 17 處）

> 對應規格：`docs/specs/65` §D／§J（結案紀錄全文＝65 §J.2）｜使用者授權：2026-08-13 四選項問答，選「批准全 8 檔 16 處」｜ commit：見本檔所在 commit

## 已完成的工作 (Done)

- **M-9（15 號六處）**：§F4-5 唯一出口改 `Money::Storage#to_psp_amount(psp:)`＋`amount_format` 分流、斷言改引 65 §D.2 A0–A6（逐條標適用格式）；§F4-6 退款、§F5-2 入向（改 65 §E.1-1 逐字語義）、§F2.1（**連續兩輪被登記表漏掉的那處**）、§F4/§F5 兩處「PI 是 R5」單一格式假設（Stripe 專章處加註不替換、通用流程處一般化）。
- **登記表縫隙兩處（最重要的補獲）**：16 §F5.1 ③ 與 55 §A.0 ③——它們是 M-2 結案時用「當時正確」的 `to_psp_minor` 改寫的，同日 V-188 改名後**二度過時**，而 65 §J 沒有任何一列涵蓋。已改 `to_psp_amount`＋A3/A6 分支澄清。
- **M-7（52:39、57:6）**：交叉引用改指 65 §D＋「該點不是捨入點」澄清。🔴 **刻意不照抄 16/55 的 M-2 版文字**——照抄會把舊名再複製一次，M-7 修完即刻變成下一個 M-9。
- **M-8（63 號五處）**：§G.4「依 ISO 4217」世界觀改寫；`to_psp_minor_unit` 裸簽名代碼塊**整個刪除不留簡化版**；同段的 exponent=3「market 建立時擋、回 INCLUSION」句依 68 §D-3 一併修正（**兩個根因、兩條獨立追溯註釋**）；63:175 註釋；63 §L 驗收清單點名的已廢函式。
- **58:198**：「物流商字串 vs PSP 整數」的字面反推陷阱——補「R6 與物流商字串長得一模一樣，判別依據是去向不是字面」，對齊鐵律 3 現行措辭。
- **62 §L.5 兩句**：收款列補 A0/A6 維度；「A2 是唯一執法點」改為「minor_units 分支唯一」。**順帶核實 62:1117 的三項待修清單並結案**（63 本輪修；65 T11／55 經 grep 無殘留——該清單對這兩項的判斷成文即過時）。
- **limits.yml 鍵改名**：`psp_minor_unit_follows_iso4217` → `psp_amount_format_declared_per_pack`（兩個字面在 V-188 後都不成立；比照 D-2 前例不做別名、原位留 deprecation）；63 §9 鍵表同輪改。
- **65 §J 結案**：M-7/M-8/M-9 三列標 ✅；新增 **§J.2 結案紀錄**（17 處實改清單＋grep 詞表＋已知盲區＋方法論數據「登記 5 → 實改 17，第三次應驗 2–3 倍少算比例」）。
- **SESSION-EXPORT §4** 的「需要授權才能動」三條標結案（防下一個接手者重新請求授權）。

## 修改的檔案與核心邏輯 (Changes)

- `docs/specs/15`、`16`、`52`、`55`、`57`、`58`、`62`、`63`、`65`、`config/limits.yml`、`docs/handoff/2026-08-13-SESSION-EXPORT.md`（11 檔）。
- **為什麼以 65 §D 現行文字為唯一藍本**：M-7 的兩處若照抄 16/55（它們自己就是待修項），舊簽名會再繁殖一輪——這正是「修正本身成為下一輪待修項」的形態，本輪用藍本紀律切斷它。
- **為什麼 63 的代碼塊整個刪**：裸 `(Integer, String)` 簽名正是 65 §C L1–L3 要禁的形態（無 psp 綁定、無值物件、可拿 ISO 當基數重現 100 倍）；留簡化版＝留一份可複製的錯誤示範。
- **為什麼 Stripe 專章的 R5 具體值不刪**：格式中立化該去 65 §D 做；PSP 專章的價值就是「這家長什麼樣」，抹掉等於把落地資訊變回抽象契約。
- **追溯註釋紀律**：既有 M-1/M-2 註釋一字未動，新註釋疊加——沿革鏈不斷（67 §M-9 教訓）。

## 尚未完成或需注意的風險 (Pending / TODO)

- **grep 詞表有已知盲區**（65 §J.2 已明文）：中文措辭轉述舊契約（如「PSP 回報的整數金額」）任何詞都不命中；根治是 65 §B 末的 X1–X11 完整性檢查機械化，落地時點應在 M3 金額引擎動工前。
- **65 §J M-8 行內文仍留著「鍵值未動」的歷史敘述**——該行已整列標 ✅ 並指向 §J.2（有改名紀錄），視為歷史紀錄不再改寫；若有人只讀行內文不讀 §J.2 可能誤解，風險低。
- **62 §L.4 的 V-115 與 65 的 V-132/V-133/V-204/V-206 均未結案**，本輪刻意未替它們補答案（65 §L「無明確出處不自補規則」）——V-132 是 Airwallex pack enable 的前置，A-3 選型若選 Airwallex 會擋在它上面。
- 手上無 Ruby 環境，65 §D.1 參考實作未實跑——但本輪全部是文檔修正，無代碼行為變更；等 M0 骨架合併後 65 §C.3 的 CI 靜態檢查才有掛載點。
