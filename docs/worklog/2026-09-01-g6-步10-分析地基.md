# 2026-09-01 G6 步 10：分析地基＋首頁 KPI

## 已完成的工作 (Done)

- daily_rollups 表＋RollupDaily（shop 時區日界線／upsert 覆蓋制冪等／80 §3 三紅線
  落口徑）＋RollupJob（每 15 分「今日＋昨日」）。
- analyticsOverview(from,to)（一支回全卡片＋series；AOV 分子/分母獨立、不反推）。
- /admin/home 佔位轉正（四 KPI 卡＋範圍鈕＋逐日簡表）。
- 突變輪 5/5 殺＋canary：UTC 日界線／累加制／AOV 扣退款（🔴 equality-trap 實例：
  首輪隔日退款 fixture 殺不掉「分子扣退款」——returns=0 使錯對同值；補同日退款格
  才紅。equality-trap-in-tests 記憶的教科書案例）／clamp 非負／查詢反推。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260901100000_create_daily_rollups.rb`＋schema（FK 語句級守衛）
- 新：`app/models/daily_rollup.rb`、`app/services/analytics/{metrics,rollup_daily}.rb`、
  `app/jobs/analytics/rollup_job.rb`、GraphQL overview/series 兩 type、
  `app/frontend/admin/pages/HomePage.tsx`
- 改：query_type（analyticsOverview）、recurring.yml、App.tsx、i18n ×5（+12 鍵）
- 測試：rollup_daily_spec（4 例）＋analytics_overview_spec（2 例）
- 🔴 MySQL 坑：upsert_all 不收 :unique_by（adapter 明文拒）——ON DUPLICATE 由
  uq 索引驅動，update_only 照用。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.56：sessions /collect 漏斗／16 指標挑選器／Top products dimension／
  compareRange／rebuild rake／nightly 抽樣對帳／HomePage FE 測試（頁為薄讀出層，
  本輪未寫 vitest——誠實登記）。
- 折扣歸因（步 9 applications 聚合的 performance 區）掛本引擎 dimension 列。
