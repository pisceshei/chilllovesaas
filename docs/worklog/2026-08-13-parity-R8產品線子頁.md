# Parity R8：產品線子頁（STRUCT1 結案：採購單/轉移升獨立頁）＋庫存/系列/禮品卡

> 對應規格：docs/specs/71 §E R8、§F（71-R0-STRUCT1 ✅ 結案、71-R8-DOC1 ✅；V1-V5 遞延）｜
> 研究：**docs/research/77**｜實測：庫存（欄集/檢視/篩選）＋採購單空態與建立式＋轉移空態與建立式
> ＋系列列表與 2026 新版建立式（來源卡＋條件目錄）＋禮品卡空態｜
> help：wf_9367bc63-f1b（4 主題＋2 critic，6/6）｜commit：見本檔所在 commit

## 已完成的工作 (Done)

- **🔴 R0-STRUCT1 結案**（掛了 8 輪的結構差）：實測坐實本尊產品導航子項＝**商品系列/庫存/採購單/
  轉移/禮品卡** 五項，採購單與轉移是**獨立頁**非庫存 tab。已修：NAV kids 五項＋GROUP_OF 補兩鍵＋
  新增 `poPage()`/`transfersPage()` 兩頁殼（空態文案＝本尊原文，內容復用 invPO/invTransfers）＋
  模組註冊 m-po/m-transfers＋庫存頁 tab 由 4 降 2（庫存/調整歷史）＋舊 INVTAB 深連結自動轉導。
- **實測**：庫存欄集 8（含**箱名稱**）＋檢視三（All/Incoming/Not Fulfillable，**本尊無 tab**）；
  採購單建立式（供應商→收件地＋商品搜尋含匯入/條碼＋成本摘要＋參考編號 255/備註 5000/付款條件/
  幣別/標籤）；轉移建立式（出貨地→🚚→收件地＋日期/參考名稱/備註/標籤/**連結採購單**）；
  系列 2026 新版建立式（**「來源」卡**：新增條件+新增商品同卡＋**排除**＋多來源組；條件屬性 9 可見）；
  禮品卡空態雙鈕（建立禮品卡/新增禮品卡商品）。
- **help 六路**：庫存狀態公式（On hand=Committed+Unavailable+Available，**Incoming 不在內**）；
  不可用手動子狀態 4（損壞/品質控管/安全庫存/其他）；調整原因 7 值全表；調整歷史 8 欄/180 天/
  子類逐一；**批量編輯器不建立稽核歷程**（與我方 ledger 唯一入口衝突→V2）；CSV 19 欄僅 4 可寫＋
  防誤覆寫（On hand current 比對）；追蹤/超賣（超賣不適用 POS）；多地點獨立不可合併。
- **DOCS**：5 條（po-empty/xfer-empty/xfer-new 新增；doc-m-inventory/doc-m-giftcards 改寫）。
- **驗證**：lint ERROR 0/WARN 13（途中補 xfer-new 漏登）；煙霧 8/8（導航兩子項/兩新頁/tab 降 2/
  歷史 tab/兩建立式）；console 零錯誤。測試店草稿全部捨棄、無殘留。

## 修改的檔案與核心邏輯 (Changes)

- `docs/research/77-admin-products-sub.md`（新檔）：§1 庫存/§2 採購單/§3 轉移/§4 系列/§5 禮品卡/
  §6 裁定面。
- `docs/design/chilllove-admin-v2.html`：NAV products kids 五項＋GROUP_OF＋inventoryPage tab 降 2＋
  深連結轉導＋poPage/transfersPage 新函式＋MODULES 註冊兩頁＋DOCS 5 條。
- `docs/specs/71`：§E R8 ✅、§F R0-STRUCT1 標結＋R8 六條。
- **為什麼保留 INVTAB 的 po/xfer 分支**：舊 DOCS 與商品詳情頁的「快速轉移」按鈕仍以 INVTAB 深連結
  進入——改成轉導（`go('m-transfers')`）比全域搜改引用安全。

## 尚未完成或需注意的風險 (Pending / TODO)

- 71-R8-V2 **最重要**：本尊批量編輯器不寫稽核歷程，我方 13-F5 要求 ledger 唯一入口——**M1 庫存
  實作前必須裁定**（照抄=有稽核缺口；不照抄=偏離需入 §A 保護清單）。
- V1（箱名稱欄/Not Fulfillable 檢視）、V3（轉移連結採購單）、V4（2026 系列「來源卡」vs 我方
  手動/智慧二分）、V5（CSV 19 欄與 4 可寫欄）。
- 系列條件目錄僅取到 9 個屬性（清單可捲、DOM 掃描未穿透該虛擬列表）——R9 或 M1 前補全量。
- 採購單收貨流（receive inventory）未實測（空店無單）；狀態機第三態以 help 為準。
