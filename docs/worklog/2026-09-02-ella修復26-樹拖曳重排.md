# 2026-09-02 Ella 修復 PR-26：編輯器樹拖曳重排（⑤e 核心）

## 已完成的工作 (Done)

- 編輯器 parity 軸 ⑤e 的核心件：本尊 sections 樹可拖曳重排（24 §1.2
  親點實測）；我方原本只有 ↑↓ 逐格移動。
  16c 當時的「dnd＝⚪ 不引入」是內部 scoping，被 2026-09-01 exact-parity
  裁定覆蓋 ⇒ 本包補上。
- **moveSectionTo**：任意位置插入 op（splice 出→bounded splice 入），走
  applyOp ⇒ undo/redo、改即見（draftsVersion）、儲存、URL 全免費繼承。
- **HTML5 drag&drop**：樹列 li draggable；dragOver 只在同帶 preventDefault
  （跨帶不可放＝本尊同帶語義）；drop 取 dragRef 放到目標列 index。
- 測試 ED25（同帶重排＋undo 還原＋跨帶忽略）；突變 1/1 殺（drop no-op）。

## 修改的檔案與核心邏輯 (Changes)

- `app/frontend/admin/pages/ThemeEditorPage.tsx`：moveSectionTo＋dragRef＋
  li 三事件。
- `ThemeEditorPage.test.tsx`：ED25。

## 尚未完成或需注意的風險 (Pending / TODO)

- block 子層拖曳（block_order 重排）未做——同構擴充點（moveBlockTo＋
  block li 掛事件），登記。
- 拖曳視覺回饋（插入線/抓取影）未做——功能語義先行，視覺打磨另輪。
- 本尊「拖到目標列上/下半決定插前插後」細粒度未量測（V）——現語義＝
  放到目標列位置（目標前插）。
