# 2026-08-25 第 25 包後修：mutation 取租戶改 context（線上驗收抓到）

對應規格：第 25 包（PR #125）後續；28 §0 GraphQL context 契約。

## 已完成的工作 (Done)

- `Mutations::StagedUploadsCreate`／`Mutations::FileCreate` 取租戶由 `Current.shop`
  改 `context.fetch(:current_shop)`——與其餘 9 處 GraphQL 用點一致
  （複驗：`grep -rn "context.fetch(:current_shop)" app/graphql | wc -l`）。
- 新增 `spec/graphql/mutation_context_shop_spec.rb` 兩道：①靜態掃描 `app/graphql`
  下不得出現 `Current.shop` ②直呼 `ChillloveSchema.execute`（無 controller）冒煙。

## 修改的檔案與核心邏輯 (Changes)

- `app/graphql/mutations/{staged_uploads_create,file_create}.rb`：兩行。
- `spec/graphql/mutation_context_shop_spec.rb`（新，2 例）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **為什麼 532 例全綠卻仍有這個缺陷**：request spec 一律經 controller，
  controller 會設 `Current.shop` ⇒ 兩種寫法都綠；**直呼 schema 的路徑
  （rails runner 線上驗收、未來背景 job／內部呼叫）才會 nil**。
  教訓＝**線上驗收走的路徑與 request spec 不同，本身就是一道獨立防線**；
  新增的靜態掃描例把這個形態釘死在 CI。
- 無其他待辦。
