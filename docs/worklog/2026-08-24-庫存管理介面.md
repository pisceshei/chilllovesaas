# 2026-08-24 庫存管理介面（排程第 18 包）

## 已完成的工作 (Done)

**讀取面（GraphQL）**——庫存在此之前**完全沒有查詢面**，寫入端（第 17 包）做完卻看不到。

- `inventoryItems(first/after/last/before, locationId, query, productId)`：cursor 分頁，
  一個 JOIN 撈齊五個數量（unavailable/committed/available/on_hand/incoming）。
- `locations`：地點選擇器來源。
- `inventoryHistory(inventoryItemId, locationId, first)`：ledger 歷程，含
  `changes { name delta quantityAfterChange }`。
- 三支都過 `authorize_inventory!`（D42 的 `inventory.view`）。

**第八式（期後值）落地**：`quantityAfterChange` 用 window function 的 running sum，
**CTE 先在整條 ledger 上開 window、外層才套保留期過濾**。反過來寫（日期條件進 CTE）
會讓期後值變成「窗內累計」——實測窗外 +100、窗內 +5 時，正確 105、錯誤 5。

**A 塊 `/admin/inventory` 列表**：八欄、地點選擇器、搜尋、行內調整（Available / On hand
兩欄可點）、SaveBar 批次送出、CSV 鈕 disabled（D43）。

**B 塊 商品頁庫存卡**：`ProductDetailPage` 的庫存區塊從 disabled 佔位換成真卡——
每個 (變體 × 地點) 一列，Available / **Total**（本尊商品頁對 on_hand 的用語，94 §2b⑤），
行內調整用**同一個** `InventoryAdjustPopover`，卡內自己的儲存鈕，連到歷程頁。
新增 `productId` 參數給它用——**不靠標題搜尋**（同名商品會撈錯）。

**C 塊 `/admin/inventory/:itemId/history`**：七欄，Incoming 為條件欄（整份歷程沒有
incoming 變動就不顯示——help 列 8 欄、實測 7 欄的成因）。儲存格「(+1) 10」＝delta ＋期後值，
無變動的欄**留白不顯示 0**。

**可複用元件**：`app/frontend/admin/components/InventoryAdjustPopover.tsx`（A/B 塊共用，第 29 包變體子頁的
驗收條）＋ `app/frontend/admin/lib/inventoryLimits.ts`（reason 值域前端鏡像，檔頭寫明它是鏡像不是第二真相）。

**i18n**：77 個 key × 5 語言（en / zh-Hant / zh-Hans / ja / fr）。

**測試**：`spec/requests/inventory_read_spec.rb` 8 例、
`app/frontend/admin/pages/InventoryPage.test.tsx` 11 例、`app/frontend/admin/components/InventoryCard.test.tsx` 3 例。
全套綠：rspec 446 例 0 失敗／vitest 56 例／tsc clean／rubocop 0／brakeman 0 警告／
tokens 同源通過／`lint-prototype.py` ERROR 0。

**注入回歸驗證（紅了才算守衛）**——五個守衛逐個做：

| 注入的回歸 | 結果 |
| --- | --- |
| 拿掉 SELECT 的 `level_available` 別名（改走 association） | N+1 守衛紅 |
| 保留期日期條件搬進 CTE 的 WHERE | 第八式紅（105 → 5） |
| 卡的 `.then` 改設 `selection`（回歸雙抓形態） | 單次抓取紅（2 → 3） |
| 卡的 `onStage` 直接送出（拿掉兩段式） | 兩段式紅 |
| 卡的查詢丟掉 `productId` | B 塊紅 |

🔴 第一次做 M1/M2 兩個注入時**都拿到綠**，但那是注入本身壞的（M1 造成 TDZ 讓整檔收不到，
vitest 印「no tests」；M2 在 state updater 裡呼叫閉包住舊 `pending` 的 `save`，
`pending.size === 0` 直接 return）。**「注入後綠」要先懷疑注入而不是先下結論**——
改成 M1′／M2′ 兩個語義正確的注入後，兩者都紅。

**順手修掉一個真 bug**（測試計數對不上而發現，不是猜的）：商品頁卡 mount 時抓兩次
（`locationId` 從 `""` → 抓 → 設成 L1 → 又抓）。除了白抓，**第二次回應會蓋掉使用者在
等待期間 stage 的東西**。改成「選取地點」與「資料屬於哪個地點」兩個 state，
後者由回應自己說（第一列的 `locationId`），不由前端猜。

**CSS**：`admin.css` 新增庫存區塊（15 個 class）。20 個 token 全部逐一驗過在
`tokens.css` 有定義（鐵律 8：不自創色值與尺寸）。數量欄一律 `tabular-nums`。

**文檔**：`docs/dev/m1-inventory-ui.md`（鐵律 12.4 四件事）。

## 修改的檔案與核心邏輯 (Changes)

**新增（後端）**
- `app/services/inventory/items_query.rb`：列表讀取 scope。`SELECT_COLUMNS` 帶 `level_*`
  別名；LIKE 用 `sanitize_sql_like` 轉義。
- `app/services/inventory/history_query.rb`：第八式所在。CTE 開 window、外層過濾；
  `Row = Data.define(...)`。
- `app/graphql/types/`：`location_type` / `inventory_quantities_type` / `inventory_item_type`
  / `inventory_item_edge_type` / `inventory_item_connection_type` /
  `inventory_history_change_type` / `inventory_history_row_type`（Zeitwerk 一檔一類）。
- `spec/requests/inventory_read_spec.rb`

**修改（後端）**
- `app/graphql/types/query_type.rb`：新的 query field ＋ resolver ＋ `authorize_inventory!`
  （逐項複驗：`grep -n "field :inventory_items\|field :locations\|field :inventory_history" app/graphql/types/query_type.rb`）
  ＋ `resolve_location` ＋ `empty_connection`。

**新增（前端）**
- `app/frontend/admin/pages/InventoryPage.tsx`（A）、`app/frontend/admin/pages/InventoryHistoryPage.tsx`（C）、
  `app/frontend/admin/components/InventoryCard.tsx`（B）、`app/frontend/admin/components/InventoryAdjustPopover.tsx`（共用）、
  `app/frontend/admin/lib/inventoryLimits.ts`（鏡像）、兩支 `.test.tsx`。

**修改（前端）**
- `app/frontend/admin/App.tsx`：`IMPLEMENTED` 加 inventory ＋ 歷程路由。
- `app/frontend/admin/pages/ProductDetailPage.tsx`：庫存區塊——`isNew` 留欄位形狀，既有商品渲染 `<InventoryCard>`。
- `app/frontend/admin/i18n/messages/*.json` ×5、`app/assets/stylesheets/admin.css`。

**核心邏輯三條**
1. **兩段式**：✓ 只 stage，不打 API。送出時**一個 pending ＝ 一次 mutation ＝ 一個新
   `idempotencyKey` ＝ 一列 `inventory_adjustment_groups`**（第 17 包的契約）。
2. **CAS 基準 ＝ 畫面上當時看到的值**，原封不動送 `compareQuantity` / `changeFromQuantity`。
   前端**不預擋** stale——預擋等於前端自己跑一次 race，還是會輸。
3. **儲存後一律重讀**。寫入後的真值只有伺服器知道。

## 尚未完成或需注意的風險 (Pending / TODO)

**執行規格 §5「明文不做」逐條**（不靜默）：
1. CSV 匯入／匯出（D43）——鈕保留在本尊的位置但 disabled。
2. 批量編輯器（D43）。
3. Move／狀態間移動 UI（origin→destination 選擇器）——登記為 V，v1 不做 Move 語義。
4. 「檢視 Incoming」與「Not Fulfillable」兩個視圖。
5. 箱名稱欄（71-R8-V1 既有登記）。
6. 篩選器 10 項——v1 只有搜尋。
7. 行動版。

**本輪新產生的**：
8. 商品頁卡只取第一頁變體（`DEFAULT_PAGE_SIZE`＝50），而 `product.max_variants`＝2048。
   超過 50 個變體的商品，卡上看不到後面的。卡內分頁留給第 20–23 包（變體）。
9. 🔴 **商品頁卡用卡內自己的儲存鈕，本尊走頁面同一個 Save——這是刻意差異**。
   理由：商品頁 SaveBar 屬於 `productSet` 那棵樹，併進去會讓一顆按鈕觸發**兩條非原子的
   API**，其中一條失敗時商家看不出哪一半存進去了。要合併需另開一輪（改的是
   `InventoryCard` 與 `ProductDetailPage` 的 save handler，資料契約不動）。
10. 歷程頁的參考文件目前直接印 URI 字串，本尊是 popover。形態差異，資料已在手上。
11. `.cl-status-filter` 這個 class 在 `ProductsPage` 與 `InventoryPage` 都用、
    `admin.css` 裡卻沒有定義。**既存狀態、非本包引入**——依「修復只針對點名問題」不擴大範圍，
    登記在此。
12. `lint-prototype.py` 有 136 個 WARN（ERROR 0），全部是既存的原型死控件警告，非本包產生。
