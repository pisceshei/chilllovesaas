# 2026-09-01　G6-8（步 5）：履約與退款線

20 步計畫的步 5。研究資產＝步 5 交接 §5（ord-0～ord-3）＋本包缺口補查（ord-4）；
dev doc＝`docs/dev/g6-fulfillment-refund.md`。

---

## 已完成的工作 (Done)

### 研究（六道關卡的①③④⑤——②③已由前 session 的 88 號實測涵蓋）

- 缺口補查（背景研究輪，取證 2026-09-01）：`FulfillmentOrderLineItemInput`（id/quantity
  皆 non-null 單值）；`FulfillmentStatus` 現行 4 值（OPEN/PENDING deprecated）；
  delivered 走 `FulfillmentEventStatus.DELIVERED`（不在 FulfillmentStatus）；
  兩支 mutation 的 userErrors 都是**裸 UserError**；`Order.fulfillments`/`refunds`
  都是 **list 非 connection**；Airwallex `POST /api/v1/pa/refunds/create` 全欄
  （status 4 值 RECEIVED/ACCEPTED/SETTLED/FAILED——**沒有 SUCCEEDED**）。
- `fulfillmentTrackingInfoUpdate` 的取代/合併語義＝官方未取得（兩頁複核零命中）
  ⇒ 我方裁定整組取代（ours，dev doc §6 #6）。

### 交付

- migration ×2：`orders.captured_total_cents`／`refunded_total_cents`（16 F5.1 物化欄）
  ＋回填；FO 回填（每單一張）；`fulfillments.line_items_snapshot`。
- models ×4：FulfillmentOrder／Fulfillment／Refund／RefundLineItem。
- 服務：`Fulfillments::Create`／`UpdateTracking`／`Cancel`；
  `Orders::FulfillmentStatus`（rollup 唯一推導器）；
  `Refunds::Calculator`（純函式；預覽與實退同一份——鐵律 7）；
  `Refunds::Create`（條件式 UPDATE 軟上限＋restock 兩型＋financial 推導）；
  `Psp::Airwallex::Refunds`＋`Refunds::ProcessPspRefundJob`（失敗補償累計欄）。
- GraphQL：4 mutations（fulfillmentCreate/TrackingInfoUpdate/Cancel/refundCreate）
  ＋`Order.suggestedRefund`＋7 個 type＋7 個 input＋4 組 error 三件套；
  `LineItemType.fulfillableQuantity`；OrderType 加 fulfillments/fulfillmentOrders/refunds。
- 建單物化 FO（`CreateFromCheckout#materialize_fulfillment_order!`）。
- 🔴 **補步 4 的整合缺口**：`MarkAsPaid`／`MarkPaidFromPsp` 翻 success 時遞增
  `captured_total_cents`（漏掉它退款上限恆 0）。
- FE：OrderDetailPage 出貨卡（88 §3 形：Unfulfilled 卡＋Mark as fulfilled 對話框
  ＋fulfillment 卡＋cancel）＋退款入口；RefundPage 新頁（88 §4 骨架：步進器＋
  restock 自動判定＋shipping＋summary 動態預覽＋上限句＋金額活字主鈕）；
  i18n 五語各 +41 鍵。
- topics：`ORDER_FULFILLED`/`ORDER_REFUNDED`（內部點號形——本尊 webhook topic
  名未取證，不冒名；Topics 檔頭登記）。

### 🔴 本輪抓到的真缺陷（整合面）

1. **`captured_total_cents` 增量路徑缺**（見上）——`mark_as_paid_captured_spec` 釘。
2. **`Money::Storage.new` 是 private**——job 初版用 `new` 生產路徑會炸；
   `process_psp_refund_job_spec` 抓到，改 `from_cents`。
3. 🔴 **spec 頂層常數撞名＝雙向順序依賴**：`RSpec.describe` block 無 lexical class
   scope ⇒ `CREATE = <<~GQL` 落在 Object 頂層；與 `external_video_media_spec` 的
   `CREATE`（及 `product_set_spec` 的 `MUTATION`）撞名，後載入覆蓋先載入
   ⇒ **全套紅、單獨綠，且紅的是誰取決於載入順序**。修法＝我的三檔改檔案前綴
   常數名；全倉同型風險登記 91 §3。

## 修改的檔案與核心邏輯 (Changes)

| 檔 | 核心 |
|---|---|
| `db/migrate/20260901010000_*.rb` | 兩累計欄＋回填（safety_assured；防重入 column_exists?）＋FO 回填（NOT EXISTS 冪等） |
| `db/migrate/20260901020000_*.rb` | fulfillments.line_items_snapshot json |
| `app/models/{fulfillment_order,fulfillment,refund,refund_line_item}.rb` | 新增 |
| `app/models/order.rb` | FULFILLMENT_STATUSES 擴 in_progress/on_hold；has_many 三條 |
| `app/services/fulfillments/{create,update_tracking,cancel}.rb` | 出貨線（條件式扣減＋committed 釋放/回加） |
| `app/services/orders/fulfillment_status.rb` | rollup 唯一推導器 |
| `app/services/refunds/{calculator,create}.rb` | 最大餘數分攤＋per-unit 游標；條件式 UPDATE 軟上限 |
| `app/services/orders/{mark_as_paid,mark_paid_from_psp}.rb` | captured 累計補齊 |
| `app/services/psp/airwallex/refunds.rb`＋`app/jobs/refunds/process_psp_refund_job.rb` | PSP 退款＋失敗補償 |
| `app/graphql/...` | 4 mutations＋types＋inputs＋error 三件套×4＋OrderType/LineItemType 擴欄 |
| `app/services/orders/create_from_checkout.rb` | 建單物化 FO |
| `app/services/events/topics.rb` | ORDER_FULFILLED/ORDER_REFUNDED（INTERNAL） |
| `app/frontend/admin/pages/{OrderDetailPage,RefundPage}.tsx`＋App.tsx＋i18n×5 | 出貨卡＋退款頁 |
| spec ×7 新檔 | 見 dev doc §8 表 |

## 突變驗證

見本檔末尾補記（突變輪在全套綠之後跑；結果與轉紅對照逐條記錄）。

## 尚未完成或需注意的風險 (Pending / TODO)

1. **FO hold/release 無寫入入口**（on_hold 有守衛與讀出；UI 分裂鈕的
   Mark as on hold 隨後續包）；SCHEDULED 不出值（無 fulfill_at 入口）。
2. **退款 RECEIVED 停 pending 的收斂**（退款 webhook／輪詢）未做。
3. **Return/exchange 線**（16 §F7）＝獨立步驟；**orderCancel**（16 §F4）不在本步。
4. **免運折扣 ⇒ 運費不可退**的判定無資料來源（v1 無免運折扣概念）。
5. **對外 webhook topic**：本尊出貨/退款 topic 名未取證 ⇒ 內部點號形先行。
6. **restock 是否記 ledger**：本尊行為未取證；v1 與建單扣減同紀律（不記）。
7. spec 頂層常數撞名的**機制化**（rubocop cop 或 lint）：判準面，登記 91 §3 候選。
8. 生產部署後煙測：出貨→取消→退款全鏈在 demo 店實跑（自我清理）。
