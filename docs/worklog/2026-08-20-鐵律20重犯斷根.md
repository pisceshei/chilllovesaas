# 鐵律 20：重犯斷根

## 已完成的工作 (Done)

- 依 2026-08-20 使用者裁定，完整讀取 `docs/specs/91-pit-register.md` 的 F1–F12 正典形態；
  以 `git ls-files` 建立 worklog／handoff 路徑集，再依候選根因回讀對應事故檔與
  `docs/dev/m0-review-convergence.md` 沿革；另以 GitHub CLI 全量讀取
  PR #58、#60、#61 的 issue comments、reviews、review body 與 paginated inline comments，
  用 review／comment id 交叉核對，不以記憶歸類。
- 將「同一系統性根因有獨立復發證據或修後再犯，且已有固定處理與反向複驗」定為升格門檻；
  F1–F12 逐類寫下升格或不重複立法的理由，避免把單次事故或待裁定修法硬編成永久規則。
- 新增鐵律 20：送驗前一次掃完適用重犯類型；驗收攝取、consumer 同步、易腐聲明、狀態空間、
  fail-closed、workflow 平台實跑與 Markdown／Windows 工具鏈各有固定處理。復發時須記防線失效
  與反向複驗，不接受只補眼前一行或「下次小心」。
- 保留鐵律 17.2 的射程：同型但未被點名的既有問題只登記 `91`；若斷根需改 checker、workflow
  或 CI 判準，先登記候選與代價、另取使用者裁定，不以本鐵律擴修。
- 2026-08-20 本地驗證快照：全部機械閘門逐支退出 0（最後彙總 `TOTAL=17 FAILED=0`；複驗入口
  以 `ruby scripts/check-ci-parity.rb` 的 production 清單為準），doc-claims 警告 0，原型 lint
  `ERROR 0 / WARN 136`。新章節以 RDoc Markdown 實際渲染，與提交前版本的結構差為
  `DELTA: [1, 13, 5, 48, 1]`（依序為 table／tr／含 thead 的 th 前綴／td／pre）；同時以
  未跳脫直線計數確認 F1–F12 表各列欄數一致。GitHub Markdown API 因本機安全層禁止把文件內容
  外傳而未取得，不以本地結果冒充 GitHub 平台結果。
- 證據帳的 GraphQL 分頁命令已對 PR #61 實跑，2026-08-20 快照輸出
  `THREADS=23 HAS_NEXT=false`；這只證明命令與當時分頁結果，後續 PR 仍須重跑，不能外推現值。

## 修改的檔案與核心邏輯 (Changes)

- `CLAUDE.md`：新增鐵律 20 的升格門檻、固定處理矩陣、送驗前清單、復發處理與 17.2 邊界。
- `AGENTS.md`：新增鐵律 20 的操作摘要與權威指向。
- `docs/dev/m0-review-convergence.md`：新增證據邊界、F1–F12 全型態判定、跨 PR 事故錨、根因、
  固定處理、反向複驗與送驗順序。
- `docs/DECISIONS.md`：新增 D34，落籍本次使用者裁定與機制擴張邊界。
- `docs/plans/2026-08-18-總方案.md`、`docs/plans/2026-08-20-階段一執行方案.md`、`HANDOFF.md`：
  同步鐵律現值、開工驗證與防重犯入口。
- `docs/specs/91-pit-register.md`：登記本次跨輪根因稽核，並把本 worklog 與配對 handoff 列入附錄 A。
- `docs/worklog/2026-08-20-階段一開場包.md`、`docs/handoff/2026-08-20-階段一開場包.md`：回寫
  PR #61 終態，避免開場包仍停在 D33／鐵律 19。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本單位尚須跑完整本地閘門、commit 後 doc-claims、鐵律 15 兩點 diff 與 GitHub 三端點重拉；
  在這些證據完成前不得宣稱已通過或已推送。
- PR #61 修改 `CLAUDE.md`、`AGENTS.md` 與 workflow，仍命中鐵律 18.3；驗收完成後只能通知
  使用者人工審核與合併，不能走 D31 代行合併。
- 使用者本次授權的是立法與整理，未授權擴張 `scripts/`／workflow／CI 判準；後續同根復發若
  顯示需要機械化，須先依鐵律 20.4 登記 `91` §2 的候選與代價。
- GitHub GFM 的本輪精確渲染證據未取得；若後續獲准使用 `/markdown` API，應以新章節原文重跑。
  本輪已完成本地真實 Markdown render 與欄位／fence 結構檢查，但不把兩者描述成 GitHub 結果。
