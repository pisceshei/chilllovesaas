# 2026-09-01 G2 步 12a：/collections 線＋真分頁＋?view=

## 已完成的工作 (Done)

- 三源方法開場（使用者 2026-09-01 裁定的常設方法律）：官方深潛（agent，shopify.dev
  逐字）＋真店親點（chill.deals：/collections、/collections/all、sort 值域 DOM 抓取、
  ?view= 兩態實測）＋live payload ⇒ `docs/research/96-storefront-g2-teardown.md`。
- /collections 清單頁（list-collections 模板）＋`collections`／`all_products`
  全域真 drop（原 runtime nil stub 之首二）＋`collection.products` 商品格
  ＋`{% paginate %}` 真分頁＋/collections/all 虛擬系列＋`?view=` 替代模板。
- 突變輪 7/7 殺＋canary（M1 發布閘/M2 頁窗/M3 discoverable/M4 排序/M5 快取 key/
  M6 all 優先序/M7 parts 前綴）；引擎相鄰 95 例迴歸綠。
- 🔴 新坑實錘：Liquid drop 覆寫 `[]` 會劫持屬性派發（Drop#key? 恆 true）——
  handle 查詢一律走 liquid_method_missing（dev doc §3）。

## 修改的檔案與核心邏輯 (Changes)

- `app/liquid/theme_engine/drops.rb`：CollectionDrop＋products/counts/sort_by；
  新 CollectionsDrop/AllProductsDrop/CollectionProductsDrop/VirtualAllCollection/
  DropTranslations/PartDrop；PaginateDrop 真欄位＋窗式 parts；TemplateDrop suffix。
- `app/liquid/theme_engine/tags.rb`：PaginateTag 真分頁（by 右值渲染期求值、
  clamp 1..250、page 參數、parts URL builder 濾 sections/section_id）。
- `app/liquid/theme_engine/runtime.rb`：publication/params/template_suffix 三參數；
  collections/all_products 接線；registers 加 request_params/request_path（帶前綴）。
- `app/liquid/theme_engine/page_renderer.rb`：resolve 加 /collections 與虛擬 all；
  `template_key_for`（?view= 存在切換／不存在 fallback——真店實證形）；section
  請求同吃 view 語境。
- `app/models/product.rb`：`has_many :collection_products`（join 用；無 dependent）。
- `app/controllers/storefront/pages_controller.rb`：CACHE_PARAMS + view。
- fixture 主題四檔＋`spec/requests/storefront_collections_spec.rb`（8 例）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.60 五 ⚪（best_selling 排名／window_size／storefront filter／collection.image
  ／list 頁逐卡 count N+1）。
- 12b（搜尋線：/search＋suggest 雙形＋recommendations 雙形）接續本包。
- 生產煙測待部署後（demo /collections 從 404 轉 200 是本包可見交付）。
- ⚠ 過程坑：以 `RAILS_ENV=test bundle exec ruby -e` 除錯時建的列**不在交易內、
  會殘留**，污染並發型 spec 的 purge 順序（全套一度 4 紅）；除錯腳本要自清或
  改用 rspec 內聯例。
