# 2026-08-28 — S6c 系列的銷售管道 popover（實測 → 落地）

🟢 不觸鐵律 18.3。teardown＝`docs/research/82` §17；裁定＝`docs/DECISIONS.md` D71。

## 已完成的工作 (Done)

- **鐵律 12 全六層實測**（本機 Chrome、測試店、寫入已全部還原並重載複驗）：
  觸發鈕（h28/r8/13/20/450、aria 計數即時、tooltip `Manage publishing`）；
  popover（215×162/r12、標頭三態總開關、三管道列 h40、switch 32×16）；
  🔴 排程入口只在 `supportsFuturePublishing` 列且 **hover 才現身**；
  🔴 **toggle＝表單級 dirty**（保存列 Save 才送、Discard 還原、關 popover 不丟）；
  總開關 **mixed→全開→全關**；排程子視圖（原地換頁、GMT+8、Remove schedule）。
- **鐵律 14 抓包**：`DeleteCollectionPublications` request body 全文 ＋ response
  （data key＝**`publishableUnpublish`**＝S5 同名）；回讀 query 全文
  （`catalogType: APP`）。Add 方向 variables 未取得（攔截器晚於該次觸發）標 V。
- **12.3⑤ help 雙源**：Save 步驟、排程 Online Store only、兩條官方規則原文（82 §17.6）。
- **落地**：`CollectionChannelsControl` ＋ save/discard/dirty 接線；
  共用機制由 ProductDetailPage export（不複製）；i18n ×5；CSS token 化；測試 4 格。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `docs/research/82-admin-channels.md` | 新增 §17（八小節 teardown） |
| `app/frontend/admin/pages/ProductDetailPage.tsx` | 16 個識別字加 `export`（零邏輯變動）＋共用聲明註釋 |
| `app/frontend/admin/pages/CollectionDetailPage.tsx` | 查詢×3、狀態、dirty/save/discard 接線、`CollectionChannelsControl` |
| `app/frontend/admin/pages/CollectionDetailPage.test.tsx` | fixture 補發布列與管道路由；新增 4 格 |
| `app/assets/stylesheets/admin.css` | `.cl-chpop*` 一組（值全走 token） |
| `app/frontend/admin/i18n/messages/*.json` ×5 | `collection.channels.*` 五鍵 |
| `docs/DECISIONS.md` | D71 |

## 20.2 適用類型／固定處理／反向複驗輸出

| 類型 | 適用 | 固定處理已跑 | 反向複驗輸出 |
|---|---|---|---|
| ① 證據 | 是 | 語義全部真觸發實測＋抓包＋help 三源；未取得處逐項標 V（Add variables、Escape 焦點內行為、新建表單形態） | 82 §17 逐條帶取證條件 |
| ② 生產者／消費者 | 是 | 發布語義單一來源（export 共用不複製）；i18n 五語系同批 | messages.test 綠 |
| ④ 狀態空間 | 是 | dirty 三路（Save／Discard／關 popover）全測；salesChannelsOf 的 handle=null 濾除有 fixture | 測試 4 格 |
| ⑤ fail-open | 是 | 「零 mutation」斷言配正向對照（Save 後恰 1 次）；順序斷言（collectionSet < publishing） | 4 格全綠 |
| ⑦ 工具鏈 | 是 | 改檔腳本檔案化、先 encode 再 wb | 無事故 |

## 尚未完成或需注意的風險 (Pending / TODO)

- **層④ CSS 逐項對齊未做完**：popover 內距／列距我方以 token 近似（8/12/6），
  本尊逐 px 值只量到面板與列的外框幾何。與其他頁的 CSS 量測涵蓋排查同一包。
- **Add 方向 variables 形狀未取得**（標 V）；排程 Done 送出的 payload 未抓
  （會產生真排程，寫入面已用 Cancel 收斂——它與商品排程共用 S5 的 publishDate 語義，
  該語義 82 §15.8 已抓過）。
- **本尊新建系列表單的管道形態未取得**（我方 isNew 隱藏，登記於 82 §17.8）。
- **量測方法坑**：`computer` 座標框架＝最近一次截圖（`91` §3.43 W-2 適用本輪，
  本輪已照固定處理：先截圖、乘比例、hover 後驗 rect）。
