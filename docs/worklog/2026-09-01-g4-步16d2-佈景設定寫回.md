# 2026-09-01 G4 步 16d2：佈景設定寫回

## 已完成的工作 (Done)

- `themeSettingsUpsert`：佈景設定（settings_data current 的 DB 覆寫層＝
  ThemeSetting）整份寫回——🔴 寫入必 touch theme（步 2 紅字，TS3/MT-A 紅證）
  ＋樂觀鎖與 template upsert 同構（theme_settings.lock_version 新欄；顯式比對
  client 底版＋AR 原生鎖雙層，TS2/MT-B 紅證）＋Hash 形閘。
- 讀面三件：`settingsSchema`（settings_schema.json 分組，🔴 theme_info 首項
  跳過＝24 §2.5，MT-D 紅證；defs 經 SchemaLocale t: 解析——與 sectionSchemas
  共用 translate_defs）／`themeSettingsJson`（DB 覆寫 → 檔案 current，與
  Runtime 同讀序，E6 雙態證）／`themeSettingsLockVersion`。
- 編輯器「佈景主題設定」入口（24 §1.3 面板底部連結）：分組面板重用
  SettingControl；獨立 settingsDraft＋settingsDirty；Save 雙寫（模板 dirty 才發
  templateUpsert、設定 dirty 才發 settingsUpsert——ED9 斷言模板未動不發，
  MT-C 紅證）；選中 section（樹點擊或 postMessage cl:select）退出 themeMode。
- 突變 4/4 殺（MT-A touch／MT-B 鎖比對／MT-C 前端儲存分支／MT-D theme_info 跳過）。

## 修改的檔案與核心邏輯 (Changes)

- 新：migration（theme_settings.lock_version）、theme_settings_upsert.rb、
  theme_settings_upsert_spec.rb（TS1-3）。
- 改：theme_type（三讀面＋translate_defs 抽共用）、mutation_type、
  ThemeEditorPage（themeMode＋settingsDraft＋save 雙寫）、bootstrap spec 5→6 例
  （E6）、editor 測試 8→9 例（ED9）、settings_schema.json fixture（加 theme_info
  首項＝skip 反向格）、i18n 1 鍵 ×5。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 佈景設定不進 undo/redo 快照棧（本尊 ↺ 涵蓋 theme settings——24 §1.5 實測；
  我方獨立 draft 無 undo）＝91 §3.70 登記，16e 併 code editor 輪整合。
- 設定變更無 client-side 即時預覽（本尊改色即時重渲染＝24 §1.5；我方 Save 後
  iframe 重載才見）——同登 91 §3.70。
- color_scheme_group 型在佈景面板走 default 分支唯讀（scheme 編輯器＝16e）。
- 生產煙測：部署後開佈景設定改 brand 色→Save→前台重載驗證。
