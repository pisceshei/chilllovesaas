# PR #64 第七輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收基準固定為 exact head `db555efbea08fb8528b8e8d66f50780806866cf4`。Claude Review
  run `32409270436` completed success、沒有 validation skip；判詞 comment `5360974435` 有兩條
  🟡，wrapper 為 `5360976226`。Codex review `4986687378` 的 current-head inline 為
  `3824777619`／`3824777624`／`3824777629`；CI quality／test 同 head completed success。
- 修法前依 `git log -p` 追到第五／第六輪命令與 P-8 終態沿革。failure-first 實跑證明舊定位式
  回兩個檔；A.1 原集合 6 列，內存複製其中一列後 raw 變 7，但尾端 `.uniq` 仍回 6 並成功，
  精確輸出含 `OLD_DUPLICATE_SURVIVED`。這不是從驗收敘述推定，而是對舊 head 的實跑結果。
- P-8 終態不再用會自我命中的全文 grep；直接指定第五輪 worklog 與第七輪 heading，並明列 diff
  與 A.1 共用同一個完整路徑 pattern。未提交狀態不再拿舊 HEAD diff 與新工作樹比較，現行命令
  明定只在 commit 後、乾淨工作樹執行。
- 第五輪 worklog 保留第六輪 fenced command 原文並相鄰加 dated correction；新現行 block 把
  P-8／A.1 讀自 HEAD、驗收 worklog 集合取 preserved base 到同一 HEAD，以
  `git status --porcelain` 另擋 dirty worktree，不用 `.uniq` 消除證據。duplicate、drop、
  missing-section 三個內存 mutation 各須命中特定錯誤。
- 第三輪 worklog 的「首組完整 pageInfo」按歷史層保留，原處追加 Codex
  `3824777619` 更正並回指 external-facts A9 終態證據邊界。91 原「制度衝突」條目改回真實缺口：
  曾靜默改寫歷史數字，正解是保留原文＋相鄰更正，與終態刪除非必要數字並不互斥。
- Claude 本輪與上一輪未落籍的範圍外觀察已按根因併入 91 §3.6：可移動 tag 未保存解析 SHA、
  PR 內 commit 的 main-only clone 前置、B5 缺 code span 等長規則、第四／第五輪 HTML comment
  list breakage 位置。外部規則獨立取自 CommonMark 0.31.2 §6.1：
  <https://spec.commonmark.org/0.31.2/#code-spans>（取證 2026-08-21）；只登記，未擴寫 B5。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | head `1041c03` 的 `5360596028` 已要求違規 mutation；head `db555ef` 的 Claude `5360974435` 與 Codex `3824777624` 又證明前輪只打到 manual-list guard，集合比較與 multiplicity 仍可放行 |
| 既有防線失效原因 | mutation 在集合比較前就 abort，沒有逐一驗 comparator 的 duplicate／drop／missing 狀態；正常路徑又混用 HEAD diff 與工作樹文件，pre-commit 假警報和 worktree 刪檔放行都未被測到 |
| 被漏掉的固定步驟 | 鐵律 20.2.4／⑤要求先列 commit 前、commit 後、dirty tree、缺輸入與違規分支，再讓每個承重分支有對應反向案例；前輪只證明單一 regex guard 會紅 |
| 可重跑反向複驗 | commit 後執行第五輪 worklog「現行可重跑反向複驗（2026-08-21 第七輪更正）」block；正常 HEAD 必須輸出集合，duplicate／drop／missing-section mutation 必須分別命中 `duplicate A.1`／`set mismatch`／`missing A.1` |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 證據／當前 head／驗收攝取 | issue comments、review body、paginated inline 與 GraphQL threads 全量重取；每項綁 ID／head | 上列 Claude／Codex 五條均綁 `db555ef…`；Claude run completed success 且沒有 skip |
| ② 生產者／消費者／終態／歷史同步 | 同步 P-8 終態、第五輪現行入口、第三輪歷史更正、91 §3／A.1、本 worklog 與 PR 描述 | 直接入口與 command heading 一致；歷史原文未覆寫；第七輪 worklog 同 commit 進 A.1 |
| ③ 易腐數字／完成性聲明 | failure-first 數值綁 exact old head 與輸出；終態集合不手抄，直接由 HEAD 重算 | 舊輸出 6→7→`.uniq` 6 只作 `db555ef` failure snapshot；新 block 不發布固定集合數 |
| ④ 狀態空間／競態窗口 | 分開 pre-commit dirty、post-commit clean、缺 A.1、drop、duplicate、新 head 與 base drift | dirty tree 明確報 `commit first`；三個內存 mutation 各有不同退出原因；新 head 全套重驗 |
| ⑤ fail-open／happy path／自我證明 | 正常 HEAD 與 duplicate／drop／missing-section 全部由同一 validator 執行；不以 `.uniq` 丟 multiplicity | failure-first 舊 duplicate mutation 成功；新 block 提交後須證明正常成功且三 mutation 非零 |
| ⑥ workflow 本機假綠／字面塊 | 不適用：本輪與本 PR 都沒有修改 `.github/workflows/` | `git diff --name-only db555ef..HEAD -- .github/workflows` 提交後須為空；Claude run 仍按 ① 查 skip |
| ⑦ Markdown／Windows 工具鏈 | command 放 bash fence；表格直線與 GitHub render 實測；中文路徑用 `core.quotepath=false` | `git diff --check`、未跳脫直線計數、GitHub `/markdown` HTML 結構與 Git Bash 實跑均須通過 |

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-P8證據來源與合併後文件債收斂.md`：現行入口改為直接路徑＋heading，
  共用 exact path pattern，明示 post-commit／dirty-tree 邊界。
- `docs/worklog/2026-08-21-PR64第五輪雙驗收修復.md`：保留第六輪歷史命令，新增 HEAD-only
  validator、multiplicity 與三個 violation mutation 的第七輪現行 block。
- `docs/worklog/2026-08-21-PR64第三輪雙驗收修復.md`：原處追加 A9「首組完整」撤回更正。
- `docs/specs/91-pit-register.md`：制度衝突改回歷史靜默改寫；補齊前兩輪 scope-out 根因與來源，
  並同步 A.1。
- `docs/worklog/2026-08-21-PR64第七輪雙驗收修復.md`：保存 exact-head 輸入、failure-first、
  20.4／20.3、修改射程與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 新 validator 刻意要求 clean HEAD，須在本輪 commit 後才執行正常與三個 mutation；提交前先跑
   Ruby syntax、Markdown render、doc-claims 與完整 Windows production-equivalent runner。
2. commit 後仍須跑現行 block、immutable-base doc-claims warning 0、鐵律 15、push／tag，並取得
   新 exact-head CI、Codex、Claude 與 threads 證據；`db555ef` 的結果不得外推。
3. #64 不命中 18.3；只有 exact-head CI、Codex、Claude 雙零、threads resolved、mergeable 與
   evaluator `1111` 同時成立，才可依 D31／D32 代行 squash merge。若 #65 先由使用者合併導致
   base 改變，須先整合 main 並從頭重驗。
