# PR #65 第五輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收輸入固定為 head `b2df2423b990f799380cbb047903be008c5c5167`。Codex review
  `4986560215` 的 inline 是 `3824670871`／`3824670875`／`3824670885`／`3824670890`；
  Claude Review run `32407607863` completed success、沒有 validation skip，issue comment
  `5360765482` 與 wrapper `5360768407` 綁同一 head。CI quality／test 同 head completed success。
- 修法前先以 `git log -p -- scripts/check-doc-claims.rb scripts/test-doc-claims-rules.rb` 追到
  `98189f7` 的圍欄收尾與 `b2df242` 的 comment EOF 沿革；沒有把驗收方建議直接當實作。
  外部語義依 `docs/dev/external-facts.md` B5 保存的 CommonMark 0.31.2 §6.1 官方逐字判定
  code span 內 delimiter 是字面內容，來源 <https://spec.commonmark.org/0.31.2/#code-spans>
  （取證 2026-08-21）；沒有重複轉述成另一套規格。
- 2026-08-21 failure-first snapshot：在 base `b2df242…` 加入本輪 fixture、但尚未改 checker 時，
  `ruby scripts/test-doc-claims-rules.rb` 精確失敗 `4/27`：重複 count、count 零供給、合法 code span
  誤擋與 code span scope 繞過各一。修法後同一命令全綠，且四個新 case 分別驗證違規／零輸入／
  happy path／scope 不得靜默截斷。
- `active_markdown_lines` 現在先辨識 block fence，再只為 comment opener 搜尋遮掉同一行成對
  code span；原始文字仍交給 metadata 判定。每個活性 CLAIM 區塊另限制只能有一筆 count，整份
  索引至少要有一個活性 count；未成對或跨行 code span 不被本修法宣稱支援。
- 主 P-2 worklog 撤回無相鄰 current-head 證據的「29 支」完成句；第四輪 worklog 保留原表並
  就地補正鐵律 20.2 的 ④／⑥ 分類。Claude 明列的範圍外觀察只登記到 91 §3，未擴改總方案、
  checker 成功訊息、workflow 或 production ROOT wiring。
- 2026-08-21 本輪文件定稿後，Windows production-equivalent runner 已從 bundle／pnpm／Rails
  setup 起跑到 workflow syntax regression 全綠；Rails、前端、RuboCop、Brakeman 與兩側 audit
  均通過。Node v24.19.0 超出專案宣告的 Node 22 engine，本結果只證明該環境實跑，不發布
  Node 24 支援。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 既有 `doc_claim_no_headers`／`doc_claim_count_before_header` 已處理結構輸入為空或落在區塊外；本輪 Codex inline `3824670871`／`3824670885` 再證實「區塊內 count 的零／多供給」仍可借用一筆 recheck 或完全不進 count 契約 |
| 既有防線失效原因 | checker 只驗「每個被認出的含 count 區塊有一筆可用 recheck」，沒有把 count cardinality 的零／一／多狀態列完；header canary 也被誤當成 count 供給 canary |
| 被漏掉的固定步驟 | 鐵律 20.2⑤要求違規、零掃描與 happy path；前輪只驗 header、區塊位置及已認出的 count，沒有對同一層輸入做 zero／one／many mutation |
| 可重跑反向複驗 | `ruby scripts/test-doc-claims-rules.rb` 必須讓 `doc_claim_duplicate_count`／`doc_claim_no_count` 各自 exit 1，並讓 `doc_claim_count_ok` exit 0；code span opener 是 Claude 明示的另一根因，以另兩個正反 case 固化，不冒充本列復發錨 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 證據／當前 head／驗收攝取 | 先鎖 `headRefOid`，再全量讀 issue comments、review body、inline 與 GraphQL threads；run 查 completed 與 skip | 上列 Codex／Claude／run ID 均綁 `b2df242…`；Claude run completed success 且未 skip |
| ② 生產者／消費者／終態／歷史同步 | 同步 checker 註釋、fixture runner、終態契約、外部證據、主 worklog Changes、91 §3 與 A.1；歷史錯句只加相鄰更正 | R6 contract、fixture 表、突變清單與主 Changes 同步；第四輪原表仍在且有 dated correction |
| ③ 易腐數字／完成性聲明 | 刪除主 worklog 無 current-head 複驗的閘門總數；fixture 集合以 runner 實跑，不在終態手抄總數 | 主 Pending 改為 runner setup＋序列；`ruby scripts/test-doc-claims-rules.rb` 是現值入口 |
| ④ 狀態空間／競態窗口 | 分開 #64 未合併、#64 先合併造成 base drift、#65 新 head、證據未取得與 18.3 人工合併 | #64 若先進 main，整合後新 head 必須重跑全套；#65 雙清後仍只通知使用者人工合併 |
| ⑤ fail-open／happy path／自我證明 | 對 count 做 zero／one／many；對 code span opener 做合法與繞過正反 fixture；保留 production wiring 查核 | failure-first snapshot 為 `4/27` 失敗；修後同一 suite 全綠，四個新 case 均命中各自預期 |
| ⑥ workflow 本機假綠／字面塊 | 不適用：本輪與本 PR 沒有修改 `.github/workflows/`；GitHub run／skip 查核歸 ① | `git diff --name-only b2df242..HEAD -- .github/workflows` 在提交後必須為空；不得用本機 YAML 綠代替遠端結果 |
| ⑦ Markdown／Windows 工具鏈 | 用官方 CommonMark 逐字定義 code span；表格直線與實際 GitHub render 複驗；Windows 顯式 UTF-8 與 Git Bash PATH | `git diff --check`、表格未跳脫直線計數、GitHub `/markdown` 的 `<tr>/<th>/<td>` 與完整 Windows runner 均須在提交前通過 |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：block fence 先於 comment opener；同一行成對 code span 只在 opener
  搜尋時遮罩；每個 CLAIM 區塊只允許一筆 count，索引 count 零供給轉紅。
- `scripts/test-doc-claims-rules.rb` 與四個 `spec/fixtures/ci_violations/doc_claim_*` 目錄：新增
  duplicate count、no count、code span 合法及 scope 繞過正反案例，並同步 suite 下限。
- `docs/dev/m0-review-convergence.md`、`docs/dev/external-facts.md`：同步 R6 終態、fixture／mutation
  清單與 CommonMark code span 的直接應用邊界。
- `docs/worklog/2026-08-21-P2宣稱索引與R6.md`：終態 Changes 加入 count cardinality／code span，
  Pending 撤回無相鄰 current-head 複驗的閘門總數。
- `docs/worklog/2026-08-21-PR65第四輪雙驗收修復.md`：保留歷史原表，追加 ④／⑥ 正確分類更正。
- `docs/specs/91-pit-register.md`：前輪重複 count 候選更新為部分結案；只登記本輪 Claude 明列的
  external-facts 分流、runner 摘要與總方案摘要範圍外問題，並同步 A.1。
- `docs/worklog/2026-08-21-PR65第五輪雙驗收修復.md`：保存本輪 exact-head 輸入、查證、
  failure-first、20.4／20.3、終態射程與未完成事項。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本檔定稿後的定向 suite、六條 CLAIM recheck、Markdown render 與完整 Windows
   production-equivalent runner 已通過；仍須 commit，並以 immutable base `b2df242…` 跑
   post-commit doc-claims warning 0，再執行鐵律 15、push 及 exact-head 雙驗收。
2. PR #64 仍可能先合併且同樣修改 91；若 base 改變，必須先整合最新 main、保留兩批來源邊界，
   對整合後新 head 重跑所有本地與 GitHub 證據，舊 head 結果不得外推。
3. PR #65 修改 `scripts/`，命中鐵律 18.3。即使 exact-head 雙零、threads 全清與評估器成立，
   仍只能通知使用者人工合併；不得代行 `gh pr merge`。
