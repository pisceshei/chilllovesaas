# 2026-09-03 引擎缺口 PR-7：section 包裝三件（id 來源前綴／群組 class／群組 BEGIN-END 註解）＋三套主題金標本入倉

分支 `engine/section-wrapper-7`（base main `54879d64`）。配對 handoff：
`docs/handoff/2026-09-03-引擎缺口-section包裝三件.md`。本包同時把真店 pnrjnw-sy（hoko.vip）本日切換發布抓到的
Kalles 5.4.2／Minimog 6.0.0／Ella 7.2.0（含商品）金標本入倉 `tools/theme-conformance/golden/`。

## 已完成的工作 (Done)
- 真店操作（使用者本日裁定：Publish＋建產品皆授權）：依序 Publish Kalles → 抓金標本 → Publish Minimog → 抓金標本
  → 建三個產品（Acme Tee／Bolt Mug／Cosy Lamp，vendor Acme／Bolt／Acme，type Tee／Mug／Lamp，Lamp 有庫存）
  → 重抓 Minimog → Publish Kalles 重抓 → **Publish Ella 復原**（現行發布＝`ella-7-2-0-theme-source`）→ 抓
  Ella（含商品）。三套主題 id：Kalles 143475343463、Minimog 143475048551、Ella 143469576295（`Shopify.theme`）。
  金標本已做金鑰掃描（`grep -rE "sk[.]eyJ|pk[.]eyJ|shpat_|sk_live_" tools/theme-conformance/golden/` 零命中）。
- 金標本逐字實證（三套主題一致）：JSON 模板 section `id="shopify-section-template--{N}__{key}"`、群組 section
  `id="shopify-section-sections--{N}__{key}"`、群組 section class 緊接 `shopify-section` 後帶
  `shopify-section-group-{group}`、群組輸出前後 `<!-- BEGIN sections: {group} -->`／`<!-- END sections: {group} -->`。
- `Runtime#render_section(key, data, scope:)`：wrapper id 與 Liquid `section.id` 同帶前綴（模板鍵／群組名代本尊
  數字 id——形同值 ours，登記）；群組 class；`render_section_group` 包 BEGIN／END；`PageRenderer` 模板 sections
  傳 template scope；SRA（`section_id`／`sections`）收裸 key 與完整 id（`section_key_from_id`）、map 鍵照請求原樣，
  檔名直渲染無前綴；編輯器橋 JS 以裸 key 定址（`sectionKey`／`findSection`）。
- spec `spec/requests/storefront_section_wrapper_spec.rb` SW1–SW4；fixture 加 `sections/sid-probe.liquid` 與
  `index.json` 的 `probe`；既有 spec 期望改前綴形（page_renderer E11／E13、liquid_view_templates LV2／LV3、
  theme_editor_bootstrap DS1／CC1）。
- 突變輪：M1 拔前綴 ⇒ SW1／SW2／SW3／SW4 紅；M2 拔群組 class ⇒ SW2／SW4 紅；M3 拔 BEGIN／END ⇒ SW2 紅；
  M4 SRA 不收完整 id ⇒ SW3 紅；M5 `section.id` 仍裸 key ⇒ SW1 紅。
- 回歸：`bundle exec rspec spec/requests/storefront_section_wrapper_spec.rb spec/liquid/page_renderer_spec.rb
  spec/requests/storefront_liquid_view_templates_spec.rb spec/requests/theme_editor_bootstrap_spec.rb
  spec/requests/storefront_search_spec.rb spec/requests/storefront_cart_spec.rb spec/liquid/theme_conformance_spec.rb` 綠。

## 修改的檔案與核心邏輯 (Changes)
- `app/liquid/theme_engine/runtime.rb`：`render_section` 加 `scope:`、`section_full_id`、`Runtime.template_scope`、
  `Runtime.section_key_from_id`；`render_section_group` 的 BEGIN／END 與 scope；`EDITOR_BRIDGE_JS` 的 `sectionKey`／`findSection`。
- `app/liquid/theme_engine/page_renderer.rb`：`render_template_sections` 傳 scope；`section_data_for` 回 `[data, scope]`
  並正規化 sid；`render_single_section`／`render_sections_json` 用之。
- `spec/requests/storefront_section_wrapper_spec.rb`、`spec/fixtures/theme_engine/minimal-1.0/sections/sid-probe.liquid`：新檔。
- `tools/theme-conformance/golden/{kalles-5.4.2,minimog-6.0.0,ella-7.2.0-products}/`：新金標本（既有 `ella-7.2.0-baseline` 不動）。

## 尚未完成或需注意的風險 (Pending / TODO)
- 前綴的數字段＝本尊內部 id，我方用模板鍵（`product.alt` ⇒ `product-alt`）／群組名——**形同值 ours**；主題若把
  `section.id` 當數字解析會不同（三套主題無此用法：`grep -rhoE "section\.id \| (split|remove|plus)" test/fixtures/themes` 零命中）。
- 靜態 `{% section %}` 與檔名直渲染維持裸名（官方頁未逐字；真店首頁未見靜態 section 樣本）。
- `.liquid` 模板（非 JSON）內 `{% section %}` 同靜態形。
- 前端 `ThemeEditorPage.test.tsx` 的 mock 片段仍用裸 id（不影響：前端不解析 wrapper id）。
