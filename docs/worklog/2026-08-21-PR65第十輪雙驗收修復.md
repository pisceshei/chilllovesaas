# PR #65 第十輪雙驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `381d904986f653e870c642f68adc8ce7cc64021c`：Claude comment
  `5363275846`、wrapper `5363277702`；Codex review `4988342175`／inline `3826068567`／
  GraphQL thread `PRRT_kwDOT0QjlM6a_N8F`。quality／test／Claude Review 均 completed success，
  但兩方都有未清意見，故未把 workflow 綠誤報成雙清。
- 三個 REST 端點全量重拉並讀 review body；Codex 的新 inline 證明 `seen_claim_ids` 在逐檔迴圈
  內重置。新增兩份各自合法、但共用 `CLAIM-001` 的索引 fixture，修前 checker exit 0，證明
  原本「ID 唯一」只在單檔承重，沒有覆蓋 `CLAIM_INDEX` 明確允許的多檔狀態。
- 依鐵律 16 以 `git log -p` 追 R6 checker、fixture runner、92 索引、m0 終態規則與 91 坑簿沿革；
  現行 namespace 是 `docs/specs/92-*` 集合，因此保留分片索引形態，將 ID 集合移到逐檔迴圈外，
  並在重複訊息同時報出首次檔案與行號。
- Claude 🟡1 已用兩個本地隔離 fixture 複驗：活性 CLAIM 區塊內的 4 空格 metadata 現為 exit 1；
  第一個標頭前的同形 metadata 仍 exit 0。91 原條目因此收窄到後者，沒有把仍成立的邊界整條刪掉。
- Claude 🟡2 由 checker 的零標頭 violation 與既有 `doc_claim_no_headers` fixture 互證；檔頭契約補回
  「每份索引須有活性標頭」。四條 ⚪ 依 17.2 只登記／去重於 91 §3，未擴修 parser、訊息或 changelog。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | 既有 `doc_claim_duplicate_id` 只測同一檔兩個區塊；本輪 Codex `3826068567` 證明跨兩份索引仍可重複 |
| 既有防線失效原因 | `seen_claim_ids = {}` 位於 `targets.each` 的 R6 分支內；每換一份索引就丟掉前一份 ID，fixture 也只有單檔形態 |
| 被漏掉的固定步驟 | 20.2④⑤ 沒有枚舉「同一檔／跨檔」分區狀態，也沒有把宣稱的唯一性 namespace 寫進契約層 |
| 可重跑反向複驗 | `doc_claim_duplicate_id_across_indexes` 修前 exit 0、修後 exit 1 且同時報 `92-alpha.md:3` 與 `92-beta.md:3`；單檔重複與乾淨 fixture 仍守原方向 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗 |
|---|---|---|
| ① 驗收攝取 | 三個 REST 端點全量分頁、review body 與 GraphQL thread ID 都綁 exact head | issue `5363275846`、review `4988342175`、inline `3826068567`、thread `PRRT_kwDOT0QjlM6a_N8F` 可逐一重取 |
| ② producer／consumer／終態 | checker、runner、fixture、92、m0、主 worklog Changes、91 §3／A.1 同步 | `rg` 反查活性標頭、ID 唯一與新 fixture 名；不把舊「唯一」模糊句留在終態層 |
| ③ 易腐數字／完成聲明 | runner 下限只增不減；驗收 IDs 與 exit code 明標為本輪快照 | 精確 case 集合以 runner 實際輸出為準，不從第九輪數字外推 |
| ④ 狀態空間／競態 | 單檔重複、跨索引重複、跨索引不重複三種狀態分開 | 新負向 fixture、既有單檔負向與生產索引正向共同承重 |
| ⑤ fail-open／happy path | failure-first 固定兩份合法索引各自通過、合併 namespace 後才應失敗的路徑 | 修前 0、修後 1；拿掉外層 `seen_claim_ids` 即由新 CASE 轉紅 |
| ⑥ workflow 本機假綠 | 本輪不改 workflow；仍須跑 parity／workflow syntax 與 GitHub exact-head CI | push 前完整 production-equivalent runner 任何非零即停止 |
| ⑦ Markdown／Windows | 中文路徑、表格直線、UTF-8 與明確 Ruby／Python／Git Bash 入口分開處理 | `git diff --check`、GitHub `/markdown` 表格計數、doc-claims warning 0 與完整 Windows runner |

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：`seen_claim_ids` 移到全索引逐檔迴圈外，重複訊息記錄首次 path／line；
  檔頭與 R6 註釋同步「每份索引有活性標頭、CLAIM ID 跨索引全域唯一」。
- `spec/fixtures/ci_violations/doc_claim_duplicate_id_across_indexes/docs/specs/92-alpha.md`、
  `92-beta.md` 與 `scripts/test-doc-claims-rules.rb`：保存跨索引 failure-first，case 下限同步提高。
- `docs/specs/92-claim-index.md`、`docs/dev/m0-review-convergence.md`、
  `docs/worklog/2026-08-21-P2宣稱索引與R6.md`：同步全域 namespace、fixture 表、突變清單與終態 Changes。
- `docs/specs/91-pit-register.md`：收窄巢狀 metadata 坑的現行射程；四條 Claude ⚪ 只登記／去重，
  A.1 補本 worklog。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 最後 repo edit 後仍須從 setup 起跑完整 Windows production-equivalent 29 閘門；正式 fixture runner
   必須證明新跨索引 case exit 1、既有合法 cases 不被誤傷，doc-claims warning 必須為 0。
2. commit 後仍須重跑 doc-claims、六條 CLAIM 原文 recheck、鐵律 15.1／15.2，再 push 並重取
   exact-head Claude／Codex；`381d904…` 的三綠不得外推。
3. #65 修改 `scripts/`，命中鐵律 18.3；即使 exact-head 雙零與 threads 收口，也只能通知使用者
   人工合併，不得由互動式 Codex 代行。
