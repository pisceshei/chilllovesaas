# 2026-08-30 — 包 30：Liquid 主題引擎生產化（D77）

## 已完成的工作 (Done)

- 鐵律 16 研究：liquid gem 5.13.0（rubygems，2026-06-29）＋themePublish 官方形
  （逐字 `Publishes a theme.`／`ThemePublishUserError`）；78 §4 既有 help 錨沿用。
- 鐵律 12 實測：`/themes` 本尊頁複驗＝跨域 iframe（78 §0.2 既登記的工具限制再現，
  CDP 截圖 OOPIF 不合成）⇒ 控件事實照舊以 help 為準，不重測。
- Gemfile：liquid ~>5.13＋strscan >=3.1.1。
- 模型六支（Theme/Template/ThemeSetting/Page/Menu/MenuItem；表＝M0 既建，零 migration
  ——首稿誤起新 migration 撞既有表，發現後即棄）。
- 引擎移植 `app/liquid/theme_engine/`：environment（limits 三鍵）/file_source
  （路徑逃逸防線）/sources（fixture 僅 dev/test，鐵律 9）/drops（DB-backed 白名單委派）
  /filters/tags/runtime（DB 覆寫讀取序＋process AST cache）/page_renderer
  （路由→template→layout；可見性＝Storefront::Lookup）。
- 三反例改掉＋**第四反例新抓**（static_environments——{% render %} 隔離吃掉普通
  assigns，實測 snippet 內 {{ shop }} 空）。
- 預覽端點（staff＋noindex）＋ThemePolicy＋routes；themes query＋themePublish
  （SOURCE_MISSING=ours fail-closed）；StorePage（分區清單＋預覽＋發布）＋i18n×5＋CSS。
- CacheStampBumper＋SaveProduct 內部事件產生端（同交易）。
- Ella 7.2.0 整頁冒煙（E9）通過；單檔 SyntaxError 轉 warning 不炸頁。

## 修改的檔案與核心邏輯 (Changes)

- `app/liquid/theme_engine{,.rb,/…}` 八檔（引擎本體）；`config/initializers/theme_engine.rb`
  （Zeitwerk 例外）。
- `app/models/`：theme/template/theme_setting/page/menu/menu_item/theme_policy。
- `app/controllers/admin/storefront_preview_controller.rb`＋`config/routes.rb` 兩路由。
- `app/graphql/`：theme_type／theme_publish（+error type/enum）／query_type.themes／
  mutation_type 掛載。
- `app/services/catalog/cache_stamp_bumper.rb`＋consumers.rb 兩行＋save_product.rb
  內部事件三行。
- `app/frontend/admin/pages/StorePage{,.test}.tsx`＋App.tsx 掛載＋i18n×5＋admin.css。
- `config/limits.yml` theme_engine 五鍵；locales×5 errors.theme.*。
- spec：page_renderer（E1–E9）/theme_preview（P1–P4）/cache_stamp_bumper（B1–B3）。

## 突變表（20.2⑤；全部實跑）

| # | 突變 | 結果 |
|---|---|---|
| M1 | ProductDrop 加 status | E2 紅 |
| M2 | PageRenderer 繞過 Lookup | E3 紅 |
| M3 | 預覽拿掉 noindex 頭 | P2 紅 |
| M4 | registry 兩條都拆 | B2 紅（🔴 單條不紅＝變體事件冗餘遮蔽；端到端 drain 格
  被 ResyncConsumer bump 遮蔽 ⇒ 改接線斷言＋行為隔離，兩輪 triage 記在 spec 檔頭） |
| M4b | bumper 改 bump 全店 | B2c 紅 |
| M5 | DB 覆寫序倒置 | E5 紅 |
| M6 | 前端拿掉 role 分支 | T2 紅 |

## 全套閘門抓到的三處連動（20.2②，修畢）

- 🔴 **內部事件重跑 StatusTransition**（真 bug）：首版直接沿用 `enqueue_event!`
  ⇒ status 變更時重複 publication.changed＋重複副作用；producers_spec `.sole`
  當場抓到。修＝`enqueue_internal_event!` 精簡 payload。
- relay_delivery_spec 的「零消費者」例改用 `inventory.level.changed`
  （product.updated 自本包起有消費者——spec 內註明留位僅存一個）。
- ProductsPage S6c-2 測試導航到 /admin/store 後 StorePage 真的掛載，
  stub 缺 themes 欄 ⇒ `undefined.filter` 炸 render；修＝`?? []` 防禦兜底。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.48 七條 ⚪（AST cache 偏離／render 動態名／money 符號／miss 遙測面／
  Zeitwerk 例外／預覽語義 ours／paginate 單頁）。
- 主題上傳管線（25 §4）與程式碼編輯器＝後續包；templates/theme_settings 目前無
  UI 寫入端（引擎讀取序已就位）。
- 公開店面（帶前綴路由、頁級快取、robots 紀律）＝包 33；本包預覽端點不對外。
