# PR #64 第十七輪雙驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `53d346bb86c7c4049440ffc3aab578f20f8ff317`：Claude 判詞
  `5379467830`（🔴 2 條、🟡 2 條、⚪ 1 條）、Codex review `4999795981`（六則 inline：
  五則 P1、一則 P2）。兩者綁同一 head。判詞條目以**自足機械列舉**取得
  （`gh api … --jq .body | grep -nE '^[[:space:]]*(#{1,6} |[-*] )?\*{0,2}(🔴|🟡|⚪)-[0-9]+'`
  ＋區段邊界式），實得 2🔴／2🟡／1⚪，其中 🟡-2 落在「✅ 已符合」段（`th` 數值已複驗相符）。
- **🔴-1 ＝ Codex `3835660393`（W15 的 ⑦ 列被就地改寫）**：還原第十五輪原文，正確值改由
  相鄰更正註承載。🔴 這是本 PR 內「修 A 的同時對 B 犯同一件事」的**第二次**——第十六輪
  在修「W14 歷史表被就地改寫」的**同一個 commit** 裡，對 W15 做了同型改寫，且該 commit
  之前 W15 全檔 `<!--` 數為 **0**。
- **🔴-2 ＝ Codex `3835660386`（W16 沒進 `91` A.1）**：補列第十六、十七兩輪 worklog，
  並附可重跑的漏列反向式。
- **Codex `3835660389`（R5 警告）**：把 W16 的「唯一的規範性防線」改為帶射程複驗的敘述，
  警告清掉。🔴 **同時逐字更正 Codex 給的成因**——它不成立，真正的成因是時序（見下）。
- **Codex `3835660390`（窗口實測自相矛盾）**：重跑一次分類器，只留**一份**輸出並附完整指令。
- **Codex `3835660396`（P2，`request payload` ≠ `request body`）**：`91` 該條改用官方措辭並
  保留 `--method GET` 邊界；本輪實跑 `gh api --help` 取得逐字。
- **🟡-1（PR 描述在 current head 仍鎖 `82e2fa8`，本 PR 第 3 次）**：PR 描述的 head、累積
  diff range、實跑證據段同批改綁本輪 head。
- **⚪-1**：依 17.2 只登記 `91` §3，**但把判詞標記為「未取得」的渲染證據補上了**。

### 鐵律 20.4 同根因復發記錄

| 要求 | 「修 A 時對 B 犯同一件事」第二次 | 「commit 前跑 doc-claims」第二次（跨 PR 同日） |
|---|---|---|
| 復發錨 | 第十五輪為修 🟡1 就地改寫 W14 的 20.3 表；第十六輪修好 W14 的**同一個 commit `53d346b`**，對 W15 的 ⑦ 列做了同型改寫（`git log -L 39,39:<W15> HEAD` 可見該 commit 的一對 `-`／`+`） | 第十六輪在 **commit 之前**跑 doc-claims 得 0 警告並發布；commit 後實有 1 條。同日 PR #66 第 34 輪**獨立踩到同一件事**，兩次都是我 |
| 既有防線失效原因 | 第十六輪的 20.4 只寫了「更正歷史層時先問原文還在不在」——那是**針對被修的那一個檔**的提問，沒有要求先列出**同類落點的集合**。於是修 W14 時完全沒看 W15 | `AGENTS.md` 只說「commit **之後**再跑一次」，沒說「commit **之前**那次不算數」。我把它讀成「跑兩次比較保險」，而不是「只有後面那次有效」 |
| 被漏掉的固定步驟 | **修一類違規之前，先用一道 grep 把同類落點的完整集合列出來**，逐處判分層，再動手 | **凡發布 doc-claims 結果，必須是 commit 後、工作區乾淨時那一次**；commit 前的輸出只能當作草稿自查，不得寫進 worklog 或 PR 留言 |
| 可重跑反向複驗 | `git -c core.quotepath=false grep -nE '\bth=[0-9]' -- docs`：逐處判「歷史層／終態層」，歷史層有值被改動就必須有相鄰更正註（本輪實跑輸出見下） | `git status --porcelain` 必須為空之後才跑 `ruby scripts/check-doc-claims.rb`；把同一組 base 在 commit 前後各跑一次，數字不同即證明本條成立（PR #66 第 34 輪已實測：0 → 1） |

**本輪實跑**

- 🔴 **Codex `3835660389` 的成因我獨立查證後否證，逐字記於此**：它寫「預設跑法回報 0 是因為
  `origin/main` 取不到、R4／R5 被跳過」。本工作樹 `git rev-parse origin/main` 回
  `0fbe520502588b34f9b9cad6ae9b3a282d4db643`——**與顯式 base 同一個 commit**；兩種跑法都回報
  **同樣的 1 條警告**，掃描範圍同為「20 份、1893 個新增行」。
  ⇒ **finding 成立、成因不成立**。真正的成因是 `check-doc-claims.rb` 的 R4／R5 掃描集合取自
  **committed diff 的行號**、讀的卻是 **working tree 的內容**（PR #66 第 34 輪實測：腳本自報
  137 個新增行、`git diff -U0` 實得 184），未提交的插入會讓兩者錯位。
- `gh api --help` 逐字（本輪實跑）：
  `"To send the parameters as a `GET` query string instead, use `--method GET`."`
  ⇒ `-f` 的值在預設 POST 下進 request body、在 `--method GET` 下進 **query string**，
  故 `request payload` 不可寫成 `request body`。
- `91` ⚪2 那一行的 GFM 渲染（判詞記為「未取得」，本輪取得）：實得 **2 個 `<strong>`**，
  邊界落在「官方同頁／另／明列」之間 ⇒ 作者意圖的「整句粗體、內部再強調」沒有發生，
  實際是兩段粗體夾一個普通字。
- A.1 完整性複驗（本輪實跑，用 `91` 本身既有的那組 canonical 全量雜湊，不另立新判準）：

  ```
  git -c core.quotepath=false ls-files docs/worklog docs/handoff | sort | md5sum
  grep -E '^- \[.\] ' docs/specs/91-pit-register.md \
    | grep -oE 'docs/(worklog|handoff)/[^`]+' | sort | md5sum
  ⇒ 兩式皆 09e617f61e72161e58428179ceacbdda  *-   （相等）

  comm -23 <(git -c core.quotepath=false ls-files 'docs/worklog/*.md' | sort) \
           <(grep -oP '^- \[[x ]\] `\K[^`]+' docs/specs/91-pit-register.md \
               | grep '^docs/worklog/' | sort)      ⇒ 空集合
  同式 comm -13                                      ⇒ 空集合
  （tracked worklog 160、A.1 條目 160）
  ```

  🔴 **本輪把上一版的「射程收窄」整個撤回**（來源＝PR #64 Codex inline `3835780547`）：
  上一版寫「A.1 是**漸進收割清單**，實測 tracked 159 份、A.1 條目 279 條，所以射程只能是
  本 PR 新增的 worklog」。**那個理由是我自己量錯**——`grep -oP '^- \[[x ]\] …'`
  **沒有篩 `docs/worklog/` 前綴**，把 A.2 的 handoff 條目一起數了進來
  （本輪重現：不篩＝279、篩了＝160）。
  🔴 **A.1 就是全量索引**，勾選符號才代表收割進度；收窄射程**會讓「刪掉一列」不被察覺**，
  等於把閘門改弱。⇒ 已還原為全量比對。
  🔴 教訓：**收窄一道既有閘門之前，先確認要收窄的理由不是自己的測量誤差。**

- 🔴 **窗口掃描：一支可原樣執行的腳本，指令與輸出都由它產生後直接貼上**
  ——上一版那段「完整指令」**根本不能跑**：`<本檔>`／`<PR描述落檔>` 是未展開的佔位符、
  `W` 從未賦值也沒有跑遍 5/8/12/16、迴圈裡沒有分類也沒有輸出
  （來源＝PR #64 Codex inline `3835780549`）。

  <details><summary>指令（原樣，可執行）</summary>

  ```bash
  # 窗口分類器：對每一處 render 計數宣稱，判斷同窗口內是否同時有「計數式」與「錨定」。
  # 用法：window_scan.sh <PR描述落檔>
  # 🔴 本腳本可原樣執行；worklog 貼的是它的原文與 stdout，不重打。
  BODY="${1:?需要 PR 描述落檔路徑}"
  W17='docs/worklog/2026-08-22-PR64第十七輪雙驗收修復.md'

  collect() {
    git -c core.quotepath=false grep -nE '\bth=[0-9]' -- docs
    [ -f "$W17" ] && grep -nE '\bth=[0-9]' "$W17" | sed "s|^|$W17:|"
    grep -nE '\bth=[0-9]' "$BODY" | sed "s|^|$BODY:|"
  }

  for WIN in 5 8 12 16; do
    pass=0; fail=0; exempt=0; total=0
    while IFS= read -r line; do
      [ -z "$line" ] && continue
      f=$(printf '%s' "$line" | cut -d: -f1)
      n=$(printf '%s' "$line" | cut -d: -f2)
      [ -f "$f" ] || continue
      lo=$(( n > WIN ? n - WIN : 1 )); hi=$(( n + WIN ))
      ctx=$(sed -n "${lo},${hi}p" "$f")
      fm=$(printf '%s' "$ctx" | grep -c '<th.b')
      an=$(printf '%s' "$ctx" | grep -cE '82e2fa8|53d346b|660fc89|終態樹')
      total=$((total+1))
      case "$(basename "$f")" in
        *第三輪*) exempt=$((exempt+1)); continue ;;
      esac
      if [ "$fm" -ge 1 ] && [ "$an" -ge 1 ]; then pass=$((pass+1)); else fail=$((fail+1)); fi
    done < <(collect)
    printf '窗口 ±%-3s PASS=%-3s FAIL=%-3s EXEMPT=%-3s 總計 %s\n' "$WIN" "$pass" "$fail" "$exempt" "$total"
  done

  echo '--- ±12 的 FAIL 明細（應為空）---'
  WIN=12
  while IFS= read -r line; do
    [ -z "$line" ] && continue
    f=$(printf '%s' "$line" | cut -d: -f1); n=$(printf '%s' "$line" | cut -d: -f2)
    [ -f "$f" ] || continue
    case "$(basename "$f")" in *第三輪*) continue ;; esac
    lo=$(( n > WIN ? n - WIN : 1 )); hi=$(( n + WIN ))
    ctx=$(sed -n "${lo},${hi}p" "$f")
    fm=$(printf '%s' "$ctx" | grep -c '<th.b')
    an=$(printf '%s' "$ctx" | grep -cE '82e2fa8|53d346b|660fc89|終態樹')
    if [ "$fm" -lt 1 ] || [ "$an" -lt 1 ]; then
      printf '  FAIL %s:%s fm=%s an=%s\n' "$(basename "$f")" "$n" "$fm" "$an"
    fi
  done < <(collect)
  ```

  </details>

  實跑輸出（stdout 原文）：

  ```
  窗口 ±5   PASS=8   FAIL=4   EXEMPT=2   總計 14
  窗口 ±8   PASS=11  FAIL=1   EXEMPT=2   總計 14
  窗口 ±12  PASS=12  FAIL=0   EXEMPT=2   總計 14
  窗口 ±16  PASS=12  FAIL=0   EXEMPT=2   總計 14
  --- ±12 的 FAIL 明細（應為空）---
  ```

  ⇒ **±12 是「零 FAIL」的最小窗口**（±16 沒有再多救到任何一處，撐大它沒有收益）。
  兩條 EXEMPT 都在 `2026-08-21-PR64第三輪雙驗收修復.md`：歷史層、無錨定 commit、
  依 19.5 不得就地補，且本輪無人點名 ⇒ 依「只修點名的」不動（已入 Pending）。
  ⚠️ **總計會隨編輯漂移**（本輪實得 14），這是正常的——判準是 **FAIL 必須為 0**，不是總數固定。

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗**輸出** |
|---|---|---|
| ① 驗收攝取 | 判詞 body 全文落檔後以**自足指令**機械列舉，逐條打勾；review body 與 paginated inline 綁 exact head | 實得 🔴 2／🟡 2／⚪ 1（🟡-2 在「✅ 已符合」段）；Codex `4999795981` 六則 inline 綁 `53d346b` |
| ② producer／consumer／終態 | W15 正文與 `Changes`、W16、`91` §3 與 A.1、PR 描述同批 | W15 ⑦ 列還原＋相鄰更正註；W15 `Changes` 改為描述 HEAD；A.1 補兩列；`91` 兩條更正＋一條新 ⚪ |
| ③ 完成性聲明 | **只發布 commit 之後、工作區乾淨時跑出來的閘門結果** | 見下方「送驗前閘門實跑」 |
| ④ 狀態空間 | 歷史層 vs 終態層；`request payload` vs `request body` vs GET query string；A.1 的「漸進收割」vs「全量索引」 | 三組都逐一寫出判準與實測，不靠推論 |
| ⑤ fail-open | 反向式若射程寫錯會噴出上百個假違規（A.1 那條初稿即是） | 已收窄射程並附突變證明 |
| ⑥ workflow 本機假綠 | 本輪不改 workflow／scripts／config | `git status` 僅 docs 檔 |
| ⑦ Markdown／Windows | 變更 Markdown 走 GitHub `/markdown`；Windows 展開逐步 exit 0 | 見下方 |

### 送驗前閘門實跑

🔴 **本段是 20.3 ③／⑦ 指向的那個段落。**上一版把 ③／⑦ 的輸出欄寫成「見下方『送驗前閘門實跑』」，
但本檔**根本沒有這個段落**（來源＝PR #64 Claude issue comment `5379763381` 🔴-2）——條文
（「只發布 commit 之後、工作區乾淨時跑出來的閘門結果」）是本輪剛立的，卻沒有在自己身上執行過一次。

以下全部取自 **commit 之後、`git status --porcelain` 為空**時那一次：

```
bin/ci                         原生 26 步：19 綠／7 紅
  7 紅全為 Windows 環境缺口（bin/* shebang → Errno::ENOEXEC；本機無 python3），逐步展開：
  ruby bin/rails db:prepare                      exit=0
  ruby bin/rubocop                               exit=0
  ruby bin/bundler-audit                         exit=0
  ruby bin/brakeman --quiet --no-pager …         exit=0
  PYTHONIOENCODING=utf-8 python scripts/lint-prototype.py       exit=0
  PYTHONIOENCODING=utf-8 python scripts/test-lint-rules.py      exit=0
  PYTHONIOENCODING=utf-8 python scripts/check-baseline-raise.py exit=0

check-doc-claims.rb（四種 base 各一次）
  預設                                            rc=0 警告 0 錯誤 0
  --base 0fbe520… --require-base                  rc=0 警告 0 錯誤 0
  --base 53d346b  --require-base                  rc=0 警告 0 錯誤 0
  --base 660fc89  --require-base                  rc=0 警告 0 錯誤 0

git diff --check                                  無輸出

A.1 完整性（91 既有的 canonical 全量 md5，兩式相等）
  09e617f61e72161e58428179ceacbdda  *-

Changes 覆蓋（本輪新加的那道）
  git -c core.quotepath=false show --name-only --format= 53d346b 的 5 個路徑
  在 W16 的 Changes 段命中數 ⇒ 5／2／1／2／1（皆 ≥1）
```

⚠️ 以上為本 tree 在 2026-08-22 的命令輸出快照，不外推到後續 head。

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-21-PR64第十五輪Claude驗收修復.md`：**還原**被第十六輪就地改寫的
  20.3 ⑦ 列，正確值改由相鄰更正註承載；`Changes` 段兩則改為描述 HEAD 的實況
  （W14 的就地改寫已於第十六輪還原 ⇒ HEAD 上不存在該編輯）。
- `docs/worklog/2026-08-22-PR64第十六輪雙驗收修復.md`：「唯一的規範性防線」改為帶射程複驗的
  敘述（清掉 R5 警告），並更正 Codex 對該警告成因的歸因。
- `docs/specs/91-pit-register.md`：①**B13**（本輪當時編為 `B9`）條目的 `request body` 改為官方
  措辭 `request payload` 並保留 `--method GET` 邊界；②新增一條 ⚪（四個 `**` run 的渲染），
  附本輪取得的渲染證據；③A.1 補列第十六、十七兩輪 worklog，並附可重跑的漏列反向式與其射程說明。
  <!-- 🔴 2026-08-22 更正（來源＝PR #64 Codex inline `3836295486`）：本則原寫「B9 條目」。
       合併 PR #66 進 main 之後，`external-facts` 的 `B9` 已是**另一條規則**
       （「指令碼檔內的 PowerShell 函式必須先定義才能呼叫」），本 PR 的 `gh api @path` 契約
       改編為 **`B13`**。`Changes` 是終態層、必須等於 HEAD ⇒ 照現值改，否則讀者會被送到
       不相干的規則。舊編號保留在括號裡供對照。 -->
- `docs/DECISIONS.md`：新增 **D39**（「一份 worklog」對規則生效前已開的 PR 不追溯）；
  判準經兩次改判——先補 `--first-parent`、再整個換成 PR 的 `createdAt`（譜系答的不是「PR 何時被開」）——
  最後改為 fail-closed（比較前斷言兩值整串合法 ISO8601，含值域與換行）並附**輸入矩陣**與**零掃描 canary**。矩陣的判準是「每新發現一種失敗輸入就進表」，**不宣稱它已完整**，故此處不列格數也不列類別——逐格見 D39 該表。
- `docs/dev/external-facts.md`：合併 PR #66 後與 main 的 `A9`／`A10`／`B9`／`B10` 編號碰撞，
  本 PR 側改編為 **`A16`／`A17`／`B13`／`B14`**，各節加原編號自註；main 側四節各加反向指標。
  第 20 輪另新增 **B15**（`gh pr view --json mergedAt --jq .mergedAt` 對未合併 PR 回**空值**、不是字面 `null`），
  帶實測命令、`gh` 版本與取證日期——D39 的 fail-closed 判準以它為據。
- `docs/DECISIONS.md`（第 22 輪）：D39 更正註**撤掉自帶的 `od -c` 量測**，改成只留結論與
  指向 `external-facts` **B15** 的指標（同一份量測只能有一個發布點）；可攜性邊界把 BSD／macOS 的
  實際行為**降級為未取得**，平台相關那半改寫成明標的條件推論，「已知邊界」限縮到 GNU 側。
- `docs/worklog/2026-08-20-PR62首輪驗收修復.md`、`docs/worklog/2026-08-20-handoff工作單位節奏與本地保存更正.md`：
  兩份在本 PR 早輪被改動過（合併 PR #66 後的引用同步），而它們**自己的 `Changes` 段都沒有記到自己**
  ⇒ 依「被改動過就必須進某一份終態清單」補列於此。
  🔴 **第二份是本輪 commit 前跑逐檔判時自己抓到的**——驗收方（Codex inline `3837086325`）只點名了第一份。
  這一組的完整導出式見下方「累積 range 的分類對帳」，**不要照被點名的檔名逐個補，要跑那道迴圈**。
- `docs/DECISIONS.md`（第 23 輪）：D39 的「本輪矩陣**十一格**全部實跑」改為「**每一格**」＋指向 worklog 的逐格輸出段
  （同檔 18 行之上剛禁止裸寫格數）。
- `docs/specs/91-pit-register.md`（第 23 輪）：「既有表格欄數不符」那條補上第 ③④ 兩種形態
  （`總方案` 的 `P-8` 那列被擠出表格、`91` 的「（其餘待收割輪填入）」末欄為空），兩者皆在 base 上已存在。
- `docs/specs/91-pit-register.md`（第 21／22 輪）：第 21 輪落籍兩條 ⚪（更正註手寫列舉會漏／同檔兩張 20.3 表射程重疊）；
  第 22 輪把「20.3 閘門缺口」條目的禁區 pathspec 補寬成與 worklog ⑥ 列**逐字相同**（原本漏 `spec`、`package.json`，
  照舊式實作出來的檢查器會重現已被點名的缺口），並附兩式同步複驗指令。
- `docs/dev/external-facts.md`（第 21 輪）：B15 的證據塊換成 `set -o pipefail` 下重跑的**真值**
  （前兩版一版是重打、一版是子行程裡 `gh` 不在 PATH 而抓到的失敗管線輸出），並補上 `pipefail` 的官方依據；
  A17 把「官方上限／官方參數語義／我方組合的查詢」三件事拆開，不再把我方組合當成 GitHub 逐字指示。
- `docs/DECISIONS.md`（第 21 輪）：兩則 dated 更正註各自收口（原本沒有結束標記 ⇒ 把 D39 的現行指引整段吞成不可見）；
  可攜性邊界由「擋下所有值」改為「擋下所有**非空**值」並標明空字串反例。
- `docs/specs/91-pit-register.md`：第 20 輪把「表格欄數不符」那條拆成兩種形態各自的正解（形態①補第三欄／形態②補空行，後者以 `/markdown` 實跑三個變體確認），
  補上 GFM 官方逐字與取證日期，行號跨檔指標改內容錨；另新增兩條 ⚪：`check-doc-claims.rb` R4 的 `NUM` 不含「兩」、20.3 稽核表無任何機器讀取（同一張表四次被點名）。
- `AGENTS.md`：§6 加 D39 的就地限定指標，並**刪掉自帶的判準副本**改為只指向 D39；
  §工作單位交接補指標。
- `CLAUDE.md`：鐵律 21.4 落點補指標。
- `docs/plans/2026-08-18-總方案.md`、`docs/plans/2026-08-20-階段一執行方案.md`、
  `docs/worklog/README.md`、`docs/dev/m0-review-convergence.md`：各自的規則陳述句相鄰處補指標。
- `docs/worklog/2026-08-22-PR64第十七輪雙驗收修復.md`：本檔。第 18 輪起的每一輪處置都追加在本檔（依 D39 射程邊界③不另建），
  含各輪處置表、20.3 稽核表與送驗前閘門實跑段；第 23 輪把 ⑦ 表的「末欄 sentinel」列換成「表列完整性」集合判準。
  🔴 **不列輪次範圍**——本檔會隨每輪成長，寫死範圍下一輪就過期；輪次入口看檔內 `### 第 N 輪處置` 標題。
- PR 描述：head、累積 diff range、實跑證據段同批改綁本輪 head。
  <!-- 🔴 2026-08-23 補記（來源＝本輪 push 前對抗式複驗，非驗收方點名）：
       本條列原本只有三筆，**不含 `docs/DECISIONS.md`**——而本 PR 改最多的正是 D39。
       🔴 更要命的是我為「Changes 覆蓋」定的判準是「basename 在**本檔任一處** `grep -F`」，
       而 `DECISIONS.md` 的命中全部落在下方的**處置表**裡 ⇒ 檢查回綠，它名義上保護的性質卻不成立。
       這是「量了 X 當成關於 Y 的事實發布」的**判準版**：我把要問的 Y（終態層有沒有記）換成了好量的 X（全檔有沒有出現）。
       ⇒ 判準改為「basename 必須出現在 `## 修改的檔案與核心邏輯 (Changes)` **到第一個 `###` 之前的逐檔條列段**」。
       🔴 **這個射程本身也修過一次**：初稿寫「到下一個 `##` 之間」，而本節下面全是 `###` 子節
       （處置表都在裡面）⇒ 那個射程幾乎等於整份檔案，等於沒收緊。收緊到第一個 `###` 之後重跑，
       **立刻抓到 `docs/dev/external-facts.md`**——它本 PR 早輪就改了，卻從來沒進過終態條列。 -->

### 合併輪處置（`bbf5f3b7` 併入 main 之後）

🔴 **本節補的是「本輪改了 tracked tree 卻沒有任何 worklog 記到」**
（來源＝PR #64 Claude issue comment `5381104120` 🔴-5）。依 `AGENTS.md` §6，
finding 需要改 tracked tree ⇒ 在**同一份** worklog 追加處置段並回寫終態層。

| 來源 | 動作 | 落點 |
|---|---|---|
| PR #66 合併進 main（`bbf5f3b7`） | 解兩個純追加衝突，兩側並存（main 側在前） | `docs/specs/91-pit-register.md`（§3.5 在前、§3.6 在後；A.1 兩側並存）、`docs/dev/external-facts.md` |
| 同上（編號碰撞） | 本 PR 的 `A9`／`A10`／`B9`／`B10` 改編為 **`A16`／`A17`／`B13`／`B14`**，各節加原編號自註 | `docs/dev/external-facts.md` |
| 同上（消費者同步） | `91` §3.6 的 22 行引用同批改號＋說明註 | `docs/specs/91-pit-register.md` |
| 使用者 2026-08-22 裁定（選項 A） | 新增 **D39**：「一份 worklog」對規則生效前已開的 PR 不追溯 | `docs/DECISIONS.md` |
| Codex `3836295490` | D39 指標傳播到規則落點（原本只加在 AGENTS §6 ⇒ 等於沒生效）。🔴 **本輪只傳了一部分**，第 18 輪才補齊 — 見下節 | `AGENTS.md`、`CLAUDE.md`、`docs/worklog/README.md`、`docs/plans/2026-08-18-總方案.md`、`docs/plans/2026-08-20-階段一執行方案.md` |
| Codex `3836295485` | D39 判準加 `--first-parent`（原式可能把側枝的 pre-rule commit 選成 `FIRST`） | `docs/DECISIONS.md` |
| Codex `3836295488` | D39 的「19 份」補可重跑導出指令 | `docs/DECISIONS.md` |
| Codex `3836295486` | 本檔終態 `Changes` 的 `B9` 指標改為 `B13`（合併後 `B9` 已是另一條規則） | 本檔 |
| Claude 🔴-3（安全相關） | 描述三處「不命中 18.3」改正——本 head 已改 `AGENTS.md`／`CLAUDE.md` ⇒ **命中 18.3、必須人工合併** | PR 描述 |
| Claude 🔴-1 | 描述的 range 改綁 immutable SHA、補列 `AGENTS.md` 與 `docs/DECISIONS.md`；**兩向差集皆空**（檔數不寫死——`git diff --name-only bbf5f3b73971b35d23c253a68bb2554d14eff1bc..HEAD \| wc -l`，會隨每輪修復增加） | PR 描述 |
| Claude 🔴-6（第 17 輪） | `91` §3 續登記三條 ⚪（範圍外觀察落籍） | `docs/specs/91-pit-register.md`（複驗：`grep -n "5381104120" docs/specs/91-pit-register.md`） |
| Claude 🟡-2（第 17 輪） | main 側 `A9`／`A10`／`B9`／`B10` 四節各加**反向指標**，指向本 PR 改編後的 `A16`／`A17`／`B13`／`B14` | `docs/dev/external-facts.md`（複驗：`grep -n "反向指標" docs/dev/external-facts.md`） |
| Claude 🟡-3（第 17 輪） | 改編號自註裡被我的腳本 `[:40]` 截斷的 main 標題，改回**逐字完整標題** | `docs/dev/external-facts.md`（複驗：`grep -n "逐字標題：" docs/dev/external-facts.md`） |

<!-- 🔴 2026-08-23 補記（來源＝PR #64 Claude issue comment `5381302078` 🔴-3；上一輪 🔴-5 同根因復發）：
     上面三列是**第十七輪就已經改進 tracked tree、卻沒有記進本表**的變更。
     驗收方的證據是三道 grep 全部零命中（`反向指標`／`落籍`／本表無對應列）。
     🔴 20.4 四欄：
     ① **復發錨**：`AGENTS.md:157-159`「同一整合 commit 回寫 worklog `Changes` 與受影響的 `docs/dev`」，第 2 次。
     ② **既有防線為何沒攔住**：`check-doc-claims.rb` 驗的是「已寫下的引用忠不忠實」，**漏寫**不產生任何引用 ⇒
       它結構上就看不見這一類；CI 也沒有任何一步比對「本輪 diff 的檔案集合」與「worklog `Changes` 提到的檔案集合」。
     ③ **固定處理的哪一步被漏掉**：處置每一條 finding 時是**逐條**改檔，而回寫 `Changes` 是**最後一次性**做的——
       於是「改了檔」與「記下改了檔」之間隔著整輪工作，被 🔴 條吸走注意力的🟡／⚪ 條就漏了。
       🔴 **改法**：回寫 `Changes` 不從記憶寫，改從 `git status --porcelain` 的**實際檔案清單**逐檔反查。
     ④ **可重跑的反向複驗**（本輪對本 head 實跑過）：
       ```bash
       # 左＝本輪真的改了哪些檔；右＝worklog 終態層提到哪些檔。左有右無 ⇒ 漏記。
       git diff --name-only <上一個候選 head>..HEAD | sort -u
       grep -o '`docs/[^`]*`\|`AGENTS\.md`\|`CLAUDE\.md`' "docs/worklog/2026-08-22-PR64第十七輪雙驗收修復.md" | tr -d '`' | sort -u
       ```
       🔴 這一式不能做成閘門：PR 描述、`91` 登記等落點本來就可能不在 diff 裡，兩集合不是相等關係，
       只有「左有右無」那一側需要人工看。 -->

<!-- 🔴 2026-08-22 更正（來源＝PR #64 Claude issue comment `5381104120` 🔴-4）：
     `c9090d4`（第十八輪）對本檔做了**就地改寫**：`git show c9090d4 -- <本檔> | grep -c '^-[^-]'`
     ⇒ **30 行**被刪，而本檔當時全檔只有 1 則更正註。被替換的兩處是
     ①那段不可執行的窗口分類器「完整指令」（佔位符未展開、`W` 未賦值、無分類無輸出）
     ②A.1 射程收窄那段（理由是我自己量錯：grep 沒篩 `docs/worklog/` 前綴，把 A.2 一起數了）。
     🔴 兩處都是**被驗收方點名要換掉的壞東西**，但依 `AGENTS.md` 分層規則，
     歷史層原文不改、只能原處追加更正註——**替換的動作本身仍是違規**，這是同根因第 4 次。
     ⇒ 被替換的原文**兩段都在 `660fc89`**（本檔誕生的那個 commit）：
       `git show 660fc89:<本檔> | grep -n '窗口掃描：本輪重跑一次'`   ⇒ 命中
       `git show 660fc89:<本檔> | grep -n '初稿的射程寫錯'`           ⇒ 命中（`:58`–`:61`）
     本註作為指路，不再回頭重貼。
     🔴 **本註的取回 ref 自己先寫錯過一次**（初稿寫「`53d346b` 與 `660fc89`」，
     而 `53d346b` 上本檔根本不存在——本檔由 `660fc89` 建立）。實跑四個 ref 逐一對照才發現。
     ⇒ 又一次「量了 X 當成關於 Y 的事實發布」；**凡寫 ref，先跑一次 `git show <ref>:<path>` 確認**。
     ---- 以下三欄為 20.4 補齊（2026-08-23，來源＝Claude issue comment `5381302078` 🟡-3）----
     ② **既有防線為何沒攔住**：`scripts/check-doc-claims.rb` 查的是「引用是否忠於原文」，
       它對**刪掉整段歷史層原文**沒有意見——刪掉的東西不會產生不忠實的引用。
       分層規則（歷史層不就地改寫）目前**只寫在 `AGENTS.md`，沒有任何閘門執行**，
       所以四次復發全部是「規則存在、機器不看」。
     ③ **固定處理的哪一步被漏掉**：`AGENTS.md:112`「在**原處**加更正註」的
       前半步——先判斷「這段屬於歷史層還是終態層」。四次都直接跳到「把它改對」，
       因為被點名的內容**讀起來就是錯的**，而「錯的東西該改掉」的直覺蓋過了分層判斷。
       🔴 **判別法**：問「這段話描述的是**當前狀態**還是**當時做了什麼**」——
       後者一律歷史層，不論它現在對不對。
     ④ **可重跑的反向複驗**（攔的是下次同型改寫，不是取回原文）：
       ```bash
       # 本檔任何一次新 commit 都應該只增不減；有刪除行即需人工判斷是否動到歷史層
       git show <新 commit> -- "docs/worklog/2026-08-22-PR64第十七輪雙驗收修復.md" | grep -c '^-[^-]'
       # ⇒ 期望 0。非 0 ⇒ 逐行看被刪的是不是「當時做了什麼」的敘述。
       ```
       🔴 這一式**故意不做成 CI 閘門**：worklog 的終態層（`Changes`）本來就必須
       隨 HEAD 改寫，一律禁刪會與 19.2 衝突。它是**動本檔前的人工檢查清單**。 -->

### 第十八輪處置（Claude `5381302078` 🔴6／🟡3／⚪3 ＋ Codex review `5000486801` 2×P1）

🔴 **依 D39 射程邊界③，本輪不另建 worklog**——D39 只豁免「規則生效前已開的 PR 的既有逐輪 worklog」，
生效後新建的一律受規則約束，所以處置追加在同一份。

| 來源 | 動作 | 落點 |
|---|---|---|
| Codex inline `3836378178`（P1） | D39 判準**整個換掉**：從 commit 譜系（`git log --first-parent` 取第一個 commit）改為 PR 的 `createdAt`。譜系答的是「這條分支多早」，本條要問的是「**這個 PR 何時被開**」——規則生效後才開、但從過期 checkout 長出來的 PR 會**誤得豁免** | `docs/DECISIONS.md` |
| Claude 🔴-4 | **既有指標逐字改寫**（不寫處數——改寫的與新增的是兩件事，合成一個數字就是 🔴-5 那個坑）：把先行詞從「本條」換成**點名的規則本身**，並明寫「其餘條文照舊不豁免」。原句插在涵蓋範圍大於該規則的段落裡 ⇒ 可被讀成連分層、更正註、閘門、ledger 都豁免。現況導出：`grep -rn '判準與射程邊界見' --include=*.md docs/ AGENTS.md CLAUDE.md` | `AGENTS.md`、`CLAUDE.md`、`docs/plans/2026-08-18-總方案.md`、`docs/plans/2026-08-20-階段一執行方案.md`、`docs/worklog/README.md` |
| Claude 🔴-5 | D39 指標補上原本沒有指標的規則落點，逐一列名：**D38 本文**、**Convergence Protocol v2 正典表**、**`AGENTS.md` §工作單位交接**、**總方案鐵律 17**。讀這些地方的人原本拿到的是未豁免版 | `docs/DECISIONS.md`、`docs/dev/m0-review-convergence.md`、`AGENTS.md`、`docs/plans/2026-08-18-總方案.md` |
| Claude 🔴-5（後半） | D39 的「落點同步：`AGENTS.md` §6 就地加一句」改成**導出指令**，不寫死落點數 | `docs/DECISIONS.md` |
| Claude 🔴-2＝Codex inline `3836378184`（P1） | 本檔終態層「25＝25」（`df506749` 的值）改成導出指令，不寫死檔數 | 本檔 |
| Claude 🔴-3 | 第十七輪三筆漏記的 tracked-tree 變更補進 `合併輪處置` 表，並補 20.4 四欄 | 本檔 |
| Claude 🔴-5／⚪-2 | 本檔「傳播到規則的**全部 5 個落點**」全稱句撤掉 | 本檔 |
| Claude 🟡-2 | `AGENTS.md` 不再自帶一份 D39 判準，改為只指向 D39——判準已連改兩次，第二份就是第二個會腐化的來源 | `AGENTS.md` |
| Claude 🟡-3 | 第十七輪那則更正註補齊 20.4 的 ②③④ 欄 | 本檔 |
| Claude 🔴-1／🔴-6／🟡-1 | 描述層：證據改綁 current head、合併通道的互斥句收斂為單一結論、鐵律 15 攝取水位重取 | PR 描述 |
| Claude ⚪-1／⚪-2／⚪-3 | 見 `91` §3 續登記 | `docs/specs/91-pit-register.md` |

<!-- 🔴 2026-08-23 自查更正（無驗收方來源；本輪 push 後自己掃出來的）：
     被更正的是**本表 `Claude 🔴-4` 那一列**（內容錨：「既有指標逐字改寫」開頭的那一列），
     不是緊鄰本註的 ⚪ 列。
     位置說明：依 `AGENTS.md:112`「在原處加」，本註理應緊貼該列；放在表末是因為表格列
     中間插不進 HTML 註釋（會把該列切斷）⇒ 改以**內容錨**點名被更正的列。
     該列初稿逐字寫「D39 指標**九處**逐字改寫」，而同列落點欄只列得出五個檔。
     🔴 9 是「改寫 5 ＋ 新增 4」的和，而該列只講改寫那一半——**與被點名的 🔴-5 是同一個坑，在修那個坑的同一個 commit 裡又犯一次**。
     🔴 **20.4 四欄**：
     ① 復發錨：「手寫落點數與同位置清單不符」，本 PR 第 2 次（前一次＝Claude `5381302078` 🔴-5「全部 5 個落點」）。
     ② 既有防線為何沒攔住：`scripts/check-doc-claims.rb` R4 的易腐數字 pattern **只認阿拉伯數字與部分中文數詞**，
        「九處」這種寫法落在它的字元類外；而本倉庫沒有任何閘門比對「宣稱的處數」與「同位置清單的長度」。
     ③ 固定處理哪一步被漏掉：處數是我從**剛改過的檔案憑記憶加總**得來的，不是導出的；
        而「改寫既有」與「新增」在我腦中是同一件事、在文件上不是。
     ④ 可重跑的反向複驗：
        ```bash
        # 凡是「N 處／N 個」型的句子，同一列必須有導出指令；否則該數字就是憑記憶寫的。
        grep -nE '[0-9一二三四五六七八九十兩]+[個處則條份張]' "docs/worklog/2026-08-22-PR64第十七輪雙驗收修復.md"
        # 逐筆判：①活的宣稱（要有導出指令或 head 錨）②19.5 逐字保留的撤回引文③帶 head 錨的歷史陳述
        ```
     🔴 **同一個錯也在 issue comment `5381400805` 的處置表裡**（該表逐字寫「九處逐字改寫」）。
        倉庫內不宣稱倉庫外的動作已完成——該留言的更正另以一則新留言發出，本註不代它宣告。 -->

<!-- 🔴 2026-08-23 更正（來源＝PR #64 Claude issue comment `5381930022` 🔴-3）：
     上面兩張處置表是**歷史層**——它們記的是「第 17／18 輪當時做了什麼」。
     `AGENTS.md:112` 逐字要求：`docs/worklog/` 原文不改；發現寫錯就在原處加一則
     帶日期與來源的 HTML 更正註（格式見該條原文；此處不重貼標記，因為註釋內
     出現結束序列會**提前關閉本註**——這一點本輪實測過，見下）。
     🔴 **第 19 輪對這兩張表做了五處就地改寫，只有其中一處（🔴-4 那列）附了更正註。**
     以下逐字保留改前原文（`f8d72fa` 上的值），並說明各自的改動與理由：

     ① `Codex 3836295490` 列的落點欄，原文逐字：
        「`AGENTS.md` ×2、`CLAUDE.md`、`docs/worklog/README.md`、兩份執行方案」
        → 改為逐檔列名並去掉「×2」。理由＝該欄用集合代稱使檔名 grep 不到（第 19 輪 🔴-1），
          「×2」是寫死的落點計數（同輪自查）。

     ② `Claude 🔴-1` 列（合併輪處置表）動作欄裡的導出指令，原文的管線符號是**未跳脫**的 `|`
        → 改為 `\|`。理由＝GFM 把該列切成四格、末欄「PR 描述」依 "the excess is ignored"
          被丟棄（第 19 輪 🔴-2）。⚠️ 這一處改的是**渲染**不是內容，但它仍然改動了已發布的行。

     ③ `Claude 🔴-5` 列的動作欄，原文逐字：
        「D39 指標補上**四個原本沒有的規則落點**：D38 本文、Convergence Protocol v2 正典表、
          `AGENTS.md` §工作單位交接、總方案鐵律 17。讀那四處的人原本拿到的是未豁免版」
        → 改為「補上原本沒有指標的規則落點，逐一列名：…」。理由＝「四個」是活的手寫計數。

     ④ `Claude 🔴-2` 列的來源欄，原文逐字：「Claude 🔴-2」
        → 改為「Claude 🔴-2＝Codex inline `3836378184`（P1）」。理由＝第 19 輪 🟡-1 指出
          該 id 的落點被寫在 ⚪ 列而實為本檔。

     ⑤ `Claude ⚪-1／⚪-2／⚪-3、Codex inline 3836378184` 列的來源欄，原文逐字即上引
        → **刪掉**「、Codex inline `3836378184`」。
        🔴 這一處最嚴重：被刪掉的正是驗收方上一輪 🟡-1 **證偽過的假落點宣稱**
          （該 id 在 `91` 零命中）。撤回一個已發布且已被證偽的陳述卻不留原文與撤回理由，
          就是 19.5「禁止靜默改寫事故紀錄」擋的形態——事故軌跡被抹掉，
          下一個人讀不出「這裡曾經寫錯過」。

     ⚠️ **驗收方同輪撤回了自己上一輪的修法措辭**：它上一輪寫「`:227`／`:297` 改成兩個檔名」，
     而 `AGENTS.md:118` 逐字「驗收方不得要求回頭改歷史層的原文」——合規做法是保留原文＋
     相鄰 dated 更正註承載現值。⇒ **本輪不再還原**（再改一次就是第六處就地改寫），
     改以本註逐字承載全部五處的改前原文。

     ---- 🔴 20.4 四欄（本 PR 同根因**第 3 次**）----
     ① 復發錨：**第十五輪**為修 🟡1 就地改寫 W14 的 20.3 表；**第十六輪**修好 W14 的同一個 commit 又對 W15 就地改寫；本輪五處。
        （輪次以本檔 `## 鐵律 20.4 同根因復發記錄` 那張表為準——本項初稿寫成「第 16／17 輪」，晚了一輪且指到「還原」的那一輪。）
     ② 既有防線為何沒攔住：`check-doc-claims.rb` 驗的是「引用忠不忠實」，
        對**就地改寫自己的歷史層**沒有意見——改完之後引用仍然忠於改後的原文。
        分層規則只寫在 `AGENTS.md`，沒有任何閘門執行。
     ③ 固定處理哪一步被漏掉：驗收意見說「修法＝把 X 改成 Y」時，我直接照做，
        **沒有先問「X 在哪一層」**。三次都是這個順序問題：先判層、再決定「改」還是「加註」。
        🔴 判別法：被點名的那行描述的是**當前狀態**（終態層 ⇒ 直接改）
        還是**當時做了什麼**（歷史層 ⇒ 只能加註）。處置表記的永遠是後者。
     ④ 可重跑的反向複驗：
        ```bash
        # 🔴 判準是**類別**不是位置：本檔**所有歷史層敘事段**都算，不只「合併輪處置」那一張表。
        # 歷史層＝記「當時做了什麼」的段落（處置表、20.3 稽核表、閘門實跑段、更正註）；
        # 終態層＝記「當前狀態」的段落（`## 修改的檔案與核心邏輯 (Changes)` 的逐檔條列）。
        PREV=<上一個已發布 head 的 SHA>   # 這是變數不是佔位符；不賦值下面會被 shell 當重導向
        F="docs/worklog/2026-08-22-PR64第十七輪雙驗收修復.md"
        git diff "$PREV"..HEAD -- "$F" | grep -c '^-[^-]' || true   # grep -c 零命中時 rc=1，不是失敗
        # ⇒ 這個數字**不是**判準：終態層本來就會有合法刪除行。
        #    判準是：把每一條刪除行對回它所在的段落，凡落在**歷史層**者，都必須有逐字保留。
        git diff "$PREV"..HEAD -- "$F" | grep -n '^-[^-]'   # 逐行看，人工歸屬
        ```
        🔴 **本式在引入它的那個 commit（`daa04de`）上就不成立**：初稿寫「期望 0」而實跑得 7，
        且那 7 行沒有一行落在初稿宣告的射程（合併輪處置表）內——**修法被綁在上一次違規的「位置」，
        而不是違規的「類別」**。這正是本 PR 反覆復發的第二個根因（finding 是集合謂詞不是位置）。
        ⇒ 改為「導出刪除行 → 逐行歸屬到層 → 歷史層的必須有逐字保留」，不設數字期望。
        🔴 這一式不能做成 CI 閘門：同檔的終態層（`Changes`）本來就必須隨 HEAD 改寫、
        一律禁刪會與 19.2 衝突。它是**動本檔前的人工檢查清單**。
        🔴 **射程＝本檔所有歷史層敘事段**（處置表、20.3 稽核表、閘門實跑段、既有更正註），
        **不是只有處置表區塊**——初稿末句把射程窄回「處置表區塊」，與它上面三行剛寫下的定義矛盾，
        而那正是本條要修的「把修法綁在上一次違規的位置」。（來源＝PR #64 Codex inline `3836905819`。） -->

### 鐵律 20.3 送驗前稽核（射程＝`bbf5f3b73971b35d23c253a68bb2554d14eff1bc..HEAD`）

🔴 **本表不綁輪次、只綁 range。** 初稿標題寫「第 18／19 輪合併一張」，而 ① 列承載的是
第 20 輪的證據 id、下游段標又寫「第 19 輪」——讀者無法判定它稽核的是哪一輪、哪一個 head。
⇒ 改綁 range；輪次是敘事，range 才是可驗證的。
🔴 **上一版只把括號裡的「合併一張」拿掉，輪次列舉還留著**（「涵蓋第 18／19／20 輪」），
於是標題違反它自己下一行剛立的規則，而且該列舉在後續輪次立刻過期。
⇒ 本輪把輪次從標題整個刪掉（來源＝PR #64 Claude `5382552421` 🟡-2）。
**射程一律由 range 決定：`HEAD` 是誰，這張表就管到誰。**

| 20.2 適用類型 | 固定處理 | 反向複驗**輸出** |
|---|---|---|
| ① 驗收攝取 | 三端點 `--paginate` 全量重拉；每個被處置的 id 必須①**是該端點全集的成員** ②**其 head 綁定等於被審 head**。🔴 **本列的輸出必須每輪重取**——它是唯一綁具體 id 的一列，`HEAD` 一變就過期 | **違規 0**。逐端點的 head 綁定欄位**不同**，判準也不同：`reviews` 用 `.commit_id`、`pulls/comments` 用 `.original_commit_id`（實得皆 `679ebd45d4f2e336d0da97c71323ae5736bab7b8`）；🔴 **`issues/comments` 端點沒有任何 head 欄位**，Claude 判詞的 head 只宣告在 **body 文字**裡（`5381930022` 全文唯一的 40-hex ＝ `679ebd45d4f2e336d0da97c71323ae5736bab7b8`）⇒ 該端點只能驗「成員資格 ＋ body 自宣告的 head」，**這比另兩個端點弱，不得含混報成同一條判準** |
| ② producer／consumer／終態 | D39（producer）改判準時同批改 `AGENTS.md`（consumer）與 worklog 終態層 | **違規 0**。式（雙 grep，全文在 `docs/DECISIONS.md` D39「落點同步」）：左式導出規則陳述句全集、右式導出已帶指標者，逐筆判「每個**規則陳述句**相鄰處是否拿得到射程限定」。FAIL＝任一規則陳述句相鄰處零命中。🔴 **不登記命中數**——本檔自己含該 pattern 的引述，寫下數字的那一行就會改變數字 |
| ③ 易腐計數／全稱句 | 不寫處數；必要時附導出指令或 head 錨 | **違規 0**。式：`grep -nE '[0-9一二三四五六七八九十兩]+[個處則條份張]' <本檔>`，逐筆判三類（活宣稱／19.5 撤回引文／帶 head 錨的歷史陳述），只有**活宣稱且同位置無導出指令無 head 錨**算違規。⚠️ 該式有已知缺口：`check-doc-claims.rb` R4 的 `NUM` 不含「兩」（已登記 `91` §3）⇒ 本列另以人工對抗式複驗補掃 |
| ④ 狀態空間 | D39 判準的輸入矩陣**每新發現一種失敗輸入就進表**，不宣稱已完整 | **違規 0**。見下方「D39 判準輸入矩陣實跑」，逐格 rc 與 stdout；格數不寫死（本輪已擴充過兩次） |
| ⑤ fail-open | 比較前斷言兩值整串合法 ISO8601（年份下界、值域、日曆、換行），任一不符 rc 2 | **違規 0**。同上；另含 20.2.5 要求的**零掃描 canary**（兩個斷言迴圈都刪掉後，「輸入缺失」那格必須翻回 `APPLIES`），以及「只刪其一仍 rc 2」的備援說明 |
| ⑥ workflow 本機假綠 | 本輪不改 workflow／scripts／config／閘門驅動器 | **違規 0**。式（🔴 **pathspec，不用 regex 交替**）：`git -c core.quotepath=false diff --name-only bbf5f3b73971b35d23c253a68bb2554d14eff1bc..HEAD -- .github scripts script config bin spec package.json Rakefile Gemfile .rubocop.yml`；**FAIL＝輸出非空**（不數數、不用管線、不看退出碼）。**canary**：同一式套 `abafcc2^..abafcc2` 必須輸出 `.github/workflows/claude-review.yml` — 實得如此 ⇒ 這一式證明得了自己抓得到東西。🔴 射程本輪再補 `spec` 與 `package.json`：18.3 把「ci.yml 的 step 所引用的其他判準檔」列為同一類，而 `ci.yml` 直接呼叫 `bundle exec rspec` 與 `pnpm test`／`typecheck`／`build`⇒ 少了這兩個，把 `pnpm test` 改成 no-op 或刪掉 `spec/` 仍會報「違規 0」 |
| ⑦ Markdown／Windows | 每個改動表格跑欄數斷言＋末欄 sentinel；一切「會不會顯示」的結論以 GitHub `/markdown` 實跑為準 | **違規 0**。見下方「表格與渲染實跑」 |

### 送驗前閘門實跑（射程同上表）

🔴 **只登記「違規數」，不登記「命中數」。** 命中數會被「寫下它」這個動作本身改變
（本檔一寫下某個 pattern，該 pattern 的命中數就 +1）；違規數在合規時恆為 0，寫下來還是 0。
🔴 **一切以 `git show <HEAD>:<path>` 對已提交的樹取得**，不讀工作樹。

#### D39 判準輸入矩陣實跑

判準本體逐字取自 `docs/DECISIONS.md`，只把兩行 `gh` 呼叫換成參數注入（其餘一字不動）。
除非該列另行標明，`RULE_AT` ＝ `2026-08-22T14:21:47Z`。
🔴 **上一版把這句寫成「`RULE_AT` 固定為⋯」，結構上排除了矩陣裡唯一一格變動 `RULE_AT` 的輸入**，
於是「補齊輸出」的那個 commit 同時把該格既有的輸出刪掉了。判準沒錯，錯的是取樣框。

```
正常·適用              APPLIES                                                        rc=0
正常·不適用            NOT_APPLIES                                                    rc=0
輸入缺失（PR_AT 空）   EVIDENCE_NOT_OBTAINED: 非 ISO8601: []                          rc=2
工具失敗（RULE_AT 空） EVIDENCE_NOT_OBTAINED: 非 ISO8601: []                          rc=2
多行汙染               EVIDENCE_NOT_OBTAINED: 值含換行                                rc=2
值域荒謬 0000-00-00    EVIDENCE_NOT_OBTAINED: 非 ISO8601: [0000-00-00T00:00:00Z]      rc=2
非閏年 2/29            EVIDENCE_NOT_OBTAINED: 不存在的日曆日期: [2026-02-29T00:00:00Z] rc=2
4/31                   EVIDENCE_NOT_OBTAINED: 不存在的日曆日期: [2026-04-31T00:00:00Z] rc=2
閏年真日期 2024-02-29  APPLIES                                                        rc=0
年份 0000-01-01        EVIDENCE_NOT_OBTAINED: 非 ISO8601: [0000-01-01T00:00:00Z]      rc=2
年份 1999-01-01        EVIDENCE_NOT_OBTAINED: 非 ISO8601: [1999-01-01T00:00:00Z]      rc=2

零掃描 canary
  兩個 for 迴圈都刪 + PR_AT 空   APPLIES                                              rc=0  ← 翻成危險側
  只刪 ISO 迴圈 + PR_AT 空       EVIDENCE_NOT_OBTAINED: 不存在的日曆日期: []          rc=2  ← 備援生效
  只刪日曆迴圈 + PR_AT 空       EVIDENCE_NOT_OBTAINED: 非 ISO8601: []                 rc=2  ← 備援生效
```

🔴 **canary 的兩次選錯，都寫在這裡當教訓**：
① 初稿選「工具失敗」那格，實跑回 `NOT_APPLIES` 而不是 `APPLIES`——那格空的是 `RULE_AT`，
   字串比較恰好落在**安全側**。**一個永遠回安全值的輸入，拿掉防線也不會變色 ⇒ 等於沒做 canary。**
② 第二版寫「刪掉**那個** `for` 迴圈」，而同一個 commit 已把迴圈從 1 個增為 2 個
   ⇒ 「那個」無唯一解。**兩種單刪法逐一實跑過，都仍 rc 2**——只刪 ISO 迴圈得「不存在的日曆日期」、
   只刪日曆迴圈得「非 ISO8601」，兩者皆 rc 2（輸出見上方矩陣的 canary 段）。
   這是 20.2.2 的 producer／consumer 不同步：改了被 canary 指涉的對象，沒改 canary。

#### 表格與渲染實跑

| 檢查 | FAIL 判準 | 本輪違規數 |
|---|---|---|
| 表格欄數 | 本輪**新增／改動**的表格列，未跳脫直線數 − 1 必須等於該表 header 欄數 | **0** |
| 表列完整性 | **射程＝本輪 commit 動到的檔**（不是整個 `<base>..HEAD`）：原始碼裡每一行像表列的（`\|` 起訖、≥3 個未跳脫直線、非分隔列），都必須渲染成 `<tr>`；且欄數等於 header、末欄非空。🔴 **既有違規另計**——`91` §3 已登記四種形態，它們在 base 上就存在、本 PR 未動該區塊 | **0** |
| `Changes` 覆蓋 | 本輪 commit 動到的每個檔，在 `## 修改的檔案與核心邏輯 (Changes)` **到第一個 `###` 之前的逐檔條列段**內，必須有一則**描述本輪動作**的條目（不是只要 basename 出現過）| **0** |
| HTML 註釋 | 本輪動過的檔渲染後：該隱藏的不可見、該可見的可見 | **0** |

🔴 **20.4：「動了檔沒回寫 `Changes`」在本 PR 是第 4 次**（第 16／19／21 輪各一次，第 22 輪又一次——
第 22 輪為 `91` 補了條目，唯獨 `DECISIONS` 沒補，與第 21 輪「為另兩個檔各加條目、唯獨 `91` 沒加」是同一形狀）。
① **復發錨**：`AGENTS.md:113`／`:120` 終態層必須等於 HEAD。
② **既有防線為何沒攔住**：第 22 輪才把謂詞從「basename 出現」換成「有描述本輪動作的條目」，
   **新謂詞第一次上場就漏**——因為它**不可機械判定**，只能靠人工逐檔對照，而我對照時看的是
   「這個檔名有沒有出現」而不是「這一則講的是不是本輪」。
③ **固定處理哪一步被漏掉**：我把 `git diff --name-only` 的輸出**逐檔看過了**，卻沒有逐檔去讀
   對應條目的**內容**。看檔名是 O(1)，讀內容是 O(n)——我選了便宜的那個。
④ **可重跑的反向複驗**（本輪對已提交樹實跑）：
```bash
PREV=<上一已發布 head>
F="docs/worklog/2026-08-22-PR64第十七輪雙驗收修復.md"
SEG=$(git show "HEAD:$F" | awk '/^## 修改的檔案與核心邏輯/{f=1;next} /^#{2,3} /{if(f)exit} f')
for p in $(git -c core.quotepath=false diff --name-only "$PREV"..HEAD); do
  # 🔴 判準不是「檔名出現」，是「出現的那一則有沒有標本輪輪次或描述本輪動作」
  echo "$SEG" | grep -n -F "$(basename "$p")"
done
```
🔴 這一式**仍然只能列出候選**，最後一步（判斷那則講的是不是本輪）沒有機器可以代勞。⇒ 見下一段。

🔴 **20.4：第 5 次，而且就發生在寫下「第 4 次」記錄的同一個 commit 裡。**
（來源＝Claude `5382825871` 🔴-1。）
② **既有防線為何沒攔住——這一格必須正面回答**：
   第 4 次的處置是「補上缺的那三則條目」＋「寫一段 20.4 記錄」。
   🔴 **兩者都不會在下一次改檔時被執行**：條目是資料、記錄是散文，沒有任何一步發生在
   「我又改了一個檔」那個時刻。所以第 5 次不是「忘了教訓」，是**教訓從來沒有掛到觸發點上**。
   更精確地說：第 4 次我把反向複驗**寫進了 worklog**，卻沒有把它**跑進本輪的處置流程**——
   我在 push 之後才第一次真跑它，於是它抓到的東西只能進下一個 commit。
③ **固定處理哪一步被漏掉**：反向複驗的執行時機。它必須在 **commit 之前**跑，而不是 push 之後。
   🔴 本輪起，`Changes` 覆蓋的反向複驗與其他閘門同批、在 commit 前跑，輸出進本節。
④ **可重跑的反向複驗**（本輪在 commit 前實跑，輸出見下方閘門段）：見 ⑦ 表下方。

🔴 **這一列的謂詞第 22 輪才換掉，換之前它是恆綠的。** 舊謂詞是「basename 出現在該條列段」，而 `91` 的 basename 第 20 輪就在了 ⇒
第 21 輪動了 `91` 卻沒回寫 `Changes`，這一格照樣報 0（來源＝PR #64 Claude `5382552421` 🔴-2）。
🔴 **我上一次修這一格時「收緊了射程卻沒換掉謂詞」**——射程從整份檔案收到條列段，問的仍然是「有沒有出現」
而不是「有沒有記下本輪做了什麼」。**這正是本檔自己寫過的「把要問的 Y 換成好量的 X」，第 2 次。**
⚠️ 新謂詞**不可機械判定**（「描述本輪動作」是語義），所以它是**人工檢查清單**；可機械化的部分只有「basename 存在」，
兩者的差距就是這一格的殘餘風險，已登記 `docs/specs/91-pit-register.md` §3。

🔴 **這一格的「0」綁的是本輪 commit 的增量 range，不是 squash 後的單一 commit。**
（來源＝PR #64 Codex inline `3836968552`：受驗的 merge ref 是一個把整個 PR 壓成一筆的 commit，
照它的 `^..` range 去跑，本條列段會漏掉十幾個路徑。）
🔴 **兩種 range 要分開講，因為它們問的不是同一個問題**：

| range | 這一格在問什麼 | 判準 |
|---|---|---|
| 增量（`<上一已發布 head>..HEAD`）| 本輪改的檔有沒有被本輪的 `Changes` 記下 | 每個檔都要有描述本輪動作的條目 |
| 累積／squash（`<base>..HEAD`）| 本 PR 改的檔有沒有被**某一份** worklog 記下 | 見下方分類對帳 |

累積 range 的分類對帳（本 head 實跑，不寫死總數）：

```bash
ALL=$(git -c core.quotepath=false diff --name-only <base>..HEAD)
echo "$ALL" | grep -v '^docs/worklog/'   # 非 worklog：必須全部在本檔條列段內
echo "$ALL" | grep '^docs/worklog/'      # worklog 類：逐輪 worklog 各自帶 Changes，
                                          # 本檔只負責自己與 README
```

本 head 實得：**非 worklog 那一組逐檔命中、無 MISS**；本檔自己與 `docs/worklog/README.md` 亦命中。
🔴 **其餘逐輪 worklog 不能整類排除，必須逐檔判**（來源＝PR #64 Codex inline `3837086325`）：
D39 豁免的是「**整併**」，不是「盤點實際的 tracked-tree 變更」。一份逐輪 worklog 只要在本 PR 內**被改動過**，
就必須出現在**某一份**終態清單裡——它自己的 `Changes` 段，或本檔的條列段。
逐檔判的導出式：

```bash
for p in $(git -c core.quotepath=false diff --name-only <base>..HEAD | grep '^docs/worklog/'); do
  # 該檔自己的 Changes 段有沒有記到自己？
  git show "HEAD:$p" | awk '/^## 修改的檔案與核心邏輯/{f=1;next} /^## /{if(f)exit} f' | grep -qF "$(basename "$p")" && continue
  # 沒有 ⇒ 必須出現在本檔條列段
  echo "需在本檔條列段列出：$p"
done
```

🔴 **本 head 實跑抓到一筆**：`docs/worklog/2026-08-20-PR62首輪驗收修復.md` 在累積 range 內被改動，
而它自己的 `Changes` 段列的是別的檔、本檔條列段也沒有它 ⇒ 已補進條列段。
🔴 初稿寫「其餘逐輪 worklog 不在本檔射程⋯是規則允許的」——**那是把「不要求整併」讀成了「不要求盤點」**。

⚠️ **本 PR 動過的檔裡另有既有的欄數不符列**（`docs/plans/2026-08-18-總方案.md` 的 X 系列、
`docs/dev/m0-review-convergence.md` 表後那段被誤吸進表格的句子）——兩者在 base `bbf5f3b73971b35d23c253a68bb2554d14eff1bc`
上就已存在、本 PR 未動該區塊，依「只修點名的」不在本包修，已登記 `docs/specs/91-pit-register.md` §3。
🔴 **不在此列舉筆數**：初稿寫「僅 8 列」，複驗立刻找到第 9 列。例外集合一旦手寫列舉就會漏。

🔴 **本輪把「末欄 sentinel」整個換掉，因為它對本輪的缺陷結構性盲目。**
舊判準是「我手挑幾列，斷言它們渲染後末欄非空」。第 23 輪的缺陷是**一次插入把 ⑦ 表的第四列擠出表格**——
① 沒被我挑到的列本來就不檢查；② 就算挑到了，判準是**數 `<td>`**，而整列不再產生任何 `<td>` 時一格都數不到。
🔴 新判準是**方向相反的集合謂詞**：原始碼裡每一行像表列的，都必須渲染成 `<tr>`。

🔴 **新檢查器自己也做了 canary，而且第一版沒亮**：
- 第一版只掃「被解析器認出的表」，而孤立的表列行根本不會進那個集合 ⇒ 對故障樣本回 0。
- 改成掃「所有像表列的行」之後故障樣本轉紅；**但同時對兩列正常渲染的表列報了假陽性**
  ——`text()` 沒解 `&lt;`／`&gt;`，含尖括號的儲存格在原始碼側與渲染側鍵不同。
- 解完實體後：故障樣本 1 違規、正常樣本 0 違規，雙向都對。
🔴 **檢查器的誤報與漏報同樣要先自證**——一個只證明過「會紅」的檢查器，可能整天在紅。

🔴 **20.4：表與相鄰散文的邊界，本 PR 第 2 次**。第一次是 `m0-review-convergence.md` 表後那段被**吸進**表格（下方 ⚠️ 段已登記），
本輪是反方向——一列被**擠出**表格。①復發錨＝同一個邊界規則；②既有防線＝那條登記只給了「補空行」的正解，
沒有給檢查器；③被漏掉的一步＝插入散文時沒有重跑渲染；④反向複驗＝上述新檢查器（雙向 canary 已實跑）。

🔴 **掃描器是前置過濾，渲染才是權威。** 本輪實測到三條 GitHub 行為，三條都不是推論：

- 行中（非行首）的註釋開啟標記不會開啟 **HTML block**（block 只在行首、≤3 空格起算），
  但**內容照樣被隱藏**——它走的是 inline raw HTML。
  實測：一行寫成「前面 ＋ 行中開啟標記 ＋ X ＋ 結束標記 ＋ 後面」，渲染後段落只剩
  「前面」與「後面」，中間整段消失（連空白都被吃掉）。
  🔴 這個例子**不能**用字面標記寫出來——寫了就會被它自己描述的行為吃掉。
  🔴 初稿只寫了前半句，讀者會推出「行中開啟 ⇒ 會顯示」，與實測相反。
  可操作的結論是：**位置不影響隱藏，只影響「哪些行被吃進去」**。
- 🔴 一旦進入註釋，**結束序列在任何位置都會關閉它，包在反引號裡也照樣關閉**
  ——註釋內部不做 inline 解析。⇒「在註釋裡引用註釋語法」是本倉庫反覆發生的事故形態。
- ≥4 空格縮排的開啟標記變成**縮排代碼塊**、整段字面可見；
  🔴 **但縮排要算「相對於所在容器」**，不是絕對欄位。

🔴 **最後一條是我的掃描器踩的坑**：它用絕對縮排判定，於是把 `CLAUDE.md` 裡三則位於
list item 內、相對縮排其實 <4 的註釋報成違規。逐則送 GitHub `/markdown` 渲染後
**三則都正常隱藏 ⇒ 全是誤報**。沒有先渲染就登記的話，我等於發布了三個假 finding——
**這與「量了 X 當成關於 Y 的事實發布」是同一件事，只是這次 X 是我自己寫的掃描器。**

<!-- 🔴 2026-08-23 更正（來源＝本輪 push 前對抗式複驗，11 個 agent；非驗收方點名）：
     本節（20.3 表 ＋ 閘門實跑段）在 `daa04de` 上的版本被本輪**整段重寫**。
     🔴 **不列處數**：初稿寫「有六處被就地改寫」並只逐字保留那六處，而實際被改動的段落多於六處
     （④⑤⑦ 三列第三欄、「第 18 輪沒做這張表」整段、`daa04de` 那則更正註、段標與前言、canary 段、
     表格與渲染實跑表、掃描器三段、既有欄數段）——**手寫列舉就會漏，這是本 PR 第三次犯**。
     ⇒ 改為：①下面逐字保留驗收方點名的那幾處 ②**其餘一律以取回指標為準**——
     `git show daa04de:docs/worklog/2026-08-22-PR64第十七輪雙驗收修復.md` 可取得完整改前原文，
     `git diff daa04de ac5eeb6 -- <本檔>` 可導出**全部**改動，不依賴我的列舉。
     （來源＝PR #64 Claude `5382422505` ⚪-1。）
     被點名的那幾處逐字保留如下：

     ① 表標題原文逐字：
        「### 鐵律 20.3 送驗前稽核（第 18／19 輪合併一張；🔴 第 18 輪缺席，本輪補做）」
        → 改綁 range。理由＝該標題與 ① 列承載的第 20 輪證據、下游「第 19 輪」段標三者互斥。

     ② ⑥ 列的第三欄原文逐字（🔴 **這是本輪最嚴重的一條**）：
        「**違規 0**：`git -c core.quotepath=false diff --name-only <base>..HEAD
          \| grep -cE '^\.github/\\|^scripts/\\|^config/'` ⇒ **0**」
        🔴 該式**是 fail-open**：ERE 裡被跳脫的直線是**字面直線**不是交替符。
        實測（取一個真的動過禁區的 range，53 筆異動）：正確式得 **53**、
        原始檔形態得 **0**、GitHub 渲染後形態也得 **0**。
        ⇒ 那格登記的「違規 0」與被檢查的內容無關，**用一個只會印 0 的式子去證明沒有違規**。
        🔴 而它取代的舊式（`grep -vc '\.md$'`，中文路徑誤判）雖然會噴假陽性，
        至少**吵**——人會被逼去看。新式是**靜**的假陰性，比舊式更糟。
        ⇒ 改為 **pathspec**（完全不用 regex 交替）＋ FAIL＝輸出非空 ＋ 一格 canary。

     ③ ② 列第三欄原文逐字：「**違規 0**：左式列出的每一個規則陳述句，相鄰處都拿得到指標
        （雙向逐筆核過）。」——「左式」在改後的列裡已無指涉對象（指令被我一併刪掉了）。
     ④ ③ 列第三欄原文逐字：「**違規 0**：導出式的每一筆逐筆判過三類⋯」——同上，「導出式」懸空。
        ⇒ ②③ 兩列把指令補回，欄名「反向複驗輸出」要求的是**輸出**，不是要求刪掉指令。

     ⑤ ① 列第三欄原文只寫「`5381930022` 見於 issue comments 全集」就整格報「違規 0」，
        而該列宣告的判準有兩個合取項（成員資格 ∧ head 欄等於本 head）。
        🔴 `issues/comments` 端點**根本沒有 head 欄位** ⇒ 第二個合取項在該筆上不可能成立。
        ⇒ 明文分端點寫，並承認 issue-comment 端點的驗證強度較弱。

     ⑥ 「D39 判準六格＋canary 實跑」段標與其下的六行輸出原文。
        同一個 commit 已把 D39 的輸入矩陣擴成九格（新增三格日曆），
        段標與輸出都沒跟 ⇒ 20.3 要求的「全部實跑並寫入本輪 worklog」對那三格不成立。
        ⇒ 段標改為不寫格數，輸出補齊為十格＋兩種 canary。

     🔴 **20.4：這是同一張表第 4 次被點名**（第 15 輪欄名被改、第 18 輪指標懸空、
     第 20 輪欄名再被改＋四列無輸出、本輪 ⑥ 列 fail-open）。
     前三次的固定處理都是「把這一格補上」，而第四次證明了問題不在某一格：
     **這張表的每一格都是我自己寫的散文，沒有任何機器會讀它。**
     ⇒ 能斷根的處理只有一種——把其中可機械化的三件事做成檢查器：**①⑥ 的禁區 pathspec ②⑦ 的表格欄數斷言 ③⑦ 的末欄 sentinel 渲染斷言**（
     做成 `scripts/` 下的檢查器並掛 CI。**本包不做**（改 `scripts/` 屬射程外），
     已登記 `docs/specs/91-pit-register.md` §3 §閘門缺口，並在此標明代價：
     在它被機制化之前，這張表的可靠度等於「我這一輪有沒有真的逐格跑過」。 -->
### 第 24 輪處置

| 來源 | 動作 | 落點 |
|---|---|---|
| Claude 🔴-1 | 第 23 輪三則 `Changes` 條目補進條列段；20.4 記第 5 次並正面回答「為何寫下第 4 次後同一 commit 仍復發」 | 本檔 |
| Claude 🔴-2 | 撤回「Codex 以 👍 表示零意見」——見下 | PR 描述、本檔 |
| Claude 🔴-3 | 描述的自帶斷言清單改列本輪實際跑的四項並附射程，刪掉已撤除的「末欄 sentinel」與命中數 | PR 描述 |
| Claude 🔴-4 | `check-doc-claims` 的 base 數兩份發布互斥（描述 5 次／留言 3 次）⇒ 統一為同一份實跑輸出 | PR 描述、留言 |
| Claude 🟡-1／🟡-3 | 20.3 ① 列標明每輪重取；⑦ 表「表列完整性」列宣告射程 | 本檔 |
| Claude 🟡-2 | 15.1 檔數的 20.4 記錄搬進本檔並補第 ④ 欄 | 本檔 |
| Claude ⚪ | `check-doc-claims.rb` 的 `VOLATILE_NUM` 單位清單缺「格／列／欄／輪」⇒ 落籍 `91` §3 | `docs/specs/91-pit-register.md` |
| Codex inline `3837086325` | 累積覆蓋的「worklog 類整體排除」過寬 ⇒ 改為逐檔列舉例外 | 本檔 |

🔴 **本輪最重的一條是 🔴-2，因為它直接餵給合併條件判斷。**

我在上一輪的處置留言與 PR 描述都寫了「**Codex 對本 head 以 👍 reaction 表示零意見**」。
這句話**三重錯誤**，逐項有可重跑的證據：

| 我發布的 | 事實 | 證據 |
|---|---|---|
| 👍 可以當零意見 | `CLAUDE.md` 逐字「任何 reaction 都只作觸發／排隊訊號⋯**不得把 👀、👍 或其他 reaction 當 completion**」 | `grep -n "當 completion" CLAUDE.md` |
| 該 reaction 綁本 head | reaction 在 `21:08:12Z`，head `5bfb1da` 在 `21:37:01Z` — **早 28 分 49 秒** | `git log -1 --format=%cI 5bfb1da` |
| Codex 零意見 | Codex **對本 head 實發了 finding review** `5001114795`（`21:43:22Z`） | `gh api .../pulls/64/reviews --jq 'select(.commit_id=="5bfb1da…")'` |

🔴 **20.4 四欄**：
① **復發錨**：這不是計數型復發，是**規則理解錯誤**——鐵律明文禁止的事，我做了。
   本 PR 對「把非受控載體當 completion」是第 1 次，但與「宣稱 vs 實物」同族。
② **既有防線為何沒攔住**：`CLAUDE.md` 那三行是**給 workflow 的判準**（0e 的 C1 evaluator），
   我讀它時把它當成「機器怎麼判」，沒有意識到**我自己在留言裡下的結論也受同一條約束**。
   ⇒ 規則寫給機器，人就以為自己不在射程內。
③ **固定處理哪一步被漏掉**：我看到一個 👍 就停止查證了。
   🔴 正確順序是**先查受控載體**（review `.commit_id` ／ issue comment 的受控前綴），
   reaction 連看都不該看。而且我甚至沒有比對時間戳——**一個早於 head 的訊號不可能回應該 head**。
④ **可重跑的反向複驗**：
```bash
H=$(gh pr view 64 --repo pisceshei/chilllovesaas --json headRefOid --jq .headRefOid)
# Codex 對本 head 到底有沒有 review、是不是 finding review
gh api repos/pisceshei/chilllovesaas/pulls/64/reviews --paginate   --jq ".[]|select(.commit_id==\"$H\")|{id,submitted_at,body}"
# 🔴 判準：有 finding review ⇒ 未清；完全沒有受控載體 ⇒ 「未取得」，**不是**零意見。
# 🔴 reaction 一律不看。
```

🔴 **Codex review `5001114795` 的逐條處置**（inline `3837086325`）：
它指出累積覆蓋的分類對帳把「worklog 類」**整體排除**過寬——
`docs/worklog/2026-08-20-PR62首輪驗收修復.md` 在累積 range 內被改動，
而它**自己的 `Changes` 段**與**本檔的條列段**都沒有記到它。
🔴 D39 豁免的是「整併」，不是「盤點實際的 tracked-tree 變更」。
⇒ 分類對帳改為**逐檔列舉例外**而不是整類排除，並把該檔補進本檔的條列段。

🔴 **🟡-2：15.1 檔數的 20.4 記錄**（上一輪只寫在 PR 描述，20.3 要求落點是 worklog）：
① **復發錨**：同一欄位連續兩輪出錯（第 23 輪寫 4 實得 3；修它時寫 0 實得 3）。
② **既有防線為何沒攔住**：第一次的處置是「改成正確數字」，那不會防止第二次；
   第二次我改用 `subprocess` 自動產生，以為自動化就不會錯——
   **而子行程解不到 ref，`wc -l` 回 0，管線照樣退出 0**。
③ **固定處理哪一步被漏掉**：貼上之後沒有回頭看數字**是否荒謬**。
   一個有 commit 的 range 不可能是 0 files；這一眼比任何自動化都便宜。
④ **可重跑的反向複驗**：
```bash
# 三個值必須來自同一次實跑，且在能解到 ref 的環境
PREV=<上一已發布 head>
git diff --name-only "$PREV"..HEAD | wc -l     # ⇒ N
git diff --shortstat "$PREV"..HEAD             # ⇒ 逐字貼上
# 🔴 荒謬檢查：N 為 0 而 git log "$PREV"..HEAD 非空 ⇒ 量測環境有問題，不是真的沒改檔
git log --oneline "$PREV"..HEAD | wc -l
```


## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **「commit 前跑 doc-claims」目前只靠紀律，沒有機械防線**：腳本不會拒絕在髒工作區執行，
  也不會提示行號來源與內容來源不同步。同日兩個 PR 各踩一次 ⇒ 這不是偶發。
  真正的斷根是讓腳本自己檢查 `git status` 並在非乾淨時 fail-closed，需改 `scripts/`
  （命中 18.3，另包）。
- 🔴 **`<th` 前綴計數法與歷史層就地改寫這兩族，至今都只有「可 grep 的體例」而非閘門**。
  本輪補的是「修之前先列同類落點集合」這個步驟，仍是紀律。
- ⚠️ 兩條舊 ⚪ 與本輪新 ⚪ 只登記未修（**B13**〔本輪當時編為 `B9`〕的截斷無省略號／
  「另明列」框架失去對象／四個 `**` run 的渲染）。依 17.2 不順手改；
  下一個本來就要動 `external-facts` 或 `91` 敘事的工作包一併處理。
- ⚠️ 本 PR 改 `docs/dev/external-facts.md` 等規範性文件但未動 `scripts/`／`workflows/`；
  合併仍依 D38 過渡期覆寫由**使用者人工執行**。
