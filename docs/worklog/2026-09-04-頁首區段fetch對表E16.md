# 2026-09-04 E16：頁首區段 fetch 內容（31 國／5 語言清單）的 Section Rendering HTML 段 diff

配對 handoff：`docs/handoff/2026-09-04-頁首區段fetch對表E16.md`。規範：`docs/dev/e16-header-sra-parity.md`；e8 §2d；取證：`docs/dev/external-facts.md` §G24；
未取得／範圍外：`docs/specs/91-pit-register.md` §3.85。前置：`docs/handoff/2026-09-04-總交接-語言前綴本尊形後.md` §9 第 1 包。

## 已完成的工作 (Done)

- 對 hoko `https://hoko.vip/?section_id=sections--19763396837479__header_default` 與 bt3 mirror 店同段（`sections--header-group__header_default`，
  head `a9c6852`）跑 `render_parity:diff`（檔案輸入，Normalizer 把段 id 抹成 `G__`）：`header_default` 相似度 0.989、5 處 differ；`/collections/all` 脈絡 0.984、
  另 4 處 differ（報告在 scratchpad `hdr/report-header-sra*.md`）。差異分類：3 處＝本尊新版顧客帳戶連結（e8 §3 平台功能列、91 §3.83 ⚪）；其餘全是引擎缺口：
  1. **section 形沒有請求頁脈絡**：`PageRenderer#build_runtime` 傳 `path: nil` ⇒ `linklists` current 全對空字串（首頁項誤標 active、`/collections/all`
     脈絡下「目錄」不標）、`request.path` nil、`return_to` 只剩前綴（`""`／`/en`）。改為傳請求路徑。
  2. **`{% form 'localization' %}` 預設 `return_to` 缺 query**：本尊＝路徑＋原始 query（順序、編碼、`&` 不轉義逐字；前綴根 `/en?…` 不帶尾斜線）。
     `Runtime` 新 `query_string:` ⇒ registers `request_query`；`FormTag#default_return_to`；預設值只轉 `"<>`（`RawAmp`）。整頁快取分支只餵進快取鍵的
     query 對（`cache_relevant_query_string`），`/search`／`/cart`／預覽／section 形餵原始 query。
  3. **`/localization` 不收 PUT**：本尊表單自帶 `_method=put` ⇒ Rack::MethodOverride 改成 PUT ⇒ bt3 mirror 店真表單提交 **404**（同日 curl 實測；D80 期
     用不帶 `_method` 的 curl 沒抓到）。routes 裸與帶前綴兩形改 `via: %i[post put]`。
  4. **`return_to` 絕對 URL 一律回根**：Ella `localization-form.js:170` 提交前把 `return_to` 改寫成 `window.location.href`；本尊只取路徑＋query（外站 host 丟掉、
     `back` ⇒ `/back`）⇒ 我方先前每次切語言都落回首頁。`LocalizationController#safe_return_to` 改為 URI 解析取 path＋query。
- 本尊取證（§G24）：SRA 六個脈絡的 `return_to`、整頁零 `return_to`、主選單 current 跟請求頁、`POST /localization` 帶 `_method=put`／直打 PUT／絕對 URL／
  外站／`back`／換語言＋絕對 URL 的 302 目標與 cookie。
- `.claude/launch.json` 的 rails 預覽**不是改 `runtimeExecutable` 能修的**：本輪把它改成 Git Bash 後 preview_start 仍失敗，harness 自己先以 WSL bash 執行
  `cd /c/Users/…/p2-claim-index-r6`（log 逐字：`/bin/bash: line 1: cd: /c/Users/pisce/Documents/ChatGPT/CHILL LOVE SYSTEM/worktrees/p2-claim-index-r6: No such file
  or directory`）⇒ 改動已還原、不入庫；本機驗證改用背景 `bundle exec rails server -p 3001`（用完以 Windows pid 停）。
- 規格：F6b（form tag）、SR6／SR7（section rendering；fixture 新 `sections/sra-probe.liquid`＋`templates/collection.sra.json`）、L2b／L5／SR8（i18n）。
- 修法後本機複驗（背景 `rails server -p 3001`，本機 mirror 店 shop 5、Ella 7.2.0）：兩個脈絡的 `render_parity:diff` 皆 `header_default` 0.993、
  differ 只剩 3 處新版顧客帳戶連結（scratchpad `hdr/report-header-local*.md`）；`return_to` 出 `/?section_id=…`／`/collections/all?section_id=…`；
  `POST /localization` 帶 `_method=put`＋絕對 URL return_to ⇒ 302 `…/collections/all?sort_by=price-ascending`＋`localization=US; path=/`（部署前 bt3 為 404）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `app/liquid/theme_engine/page_renderer.rb` | `query_string:` 貫穿；`build_runtime(page_type, assigns, path)` 傳請求路徑（先前 nil） |
| `app/liquid/theme_engine/runtime.rb` | `query_string:` ⇒ `base_registers[:request_query]` |
| `app/liquid/theme_engine/tags.rb` | `FormTag#default_return_to`（路徑去尾斜線＋原始 query）；`RawAmp` 值物件只轉 `"<>` |
| `app/controllers/storefront/pages_controller.rb` | `render_page(query_string:)`；section／cart／預覽／search 餵原始 query；快取分支 `cache_relevant_query_string` |
| `app/controllers/storefront/localization_controller.rb` | `safe_return_to` 只取路徑＋query（絕對 URL／外站／`back` 本尊形） |
| `config/routes.rb` | `/localization`（裸＋`:locale_prefix`）`via: %i[post put]` |
| `spec/fixtures/theme_engine/minimal-1.0/{sections/sra-probe.liquid,templates/collection.sra.json}` | 探針 section（`request.path`／localization form／linklist current）＋`?view=sra` 模板 |
| `spec/liquid/form_tag_spec.rb`、`spec/requests/storefront_section_rendering_spec.rb`、`spec/requests/storefront_i18n_spec.rb` | F6b／SR6／SR7／L2b／L5／SR8 |
| docs | `docs/dev/e16-header-sra-parity.md`（新）、e8 §2d（#67–#69）、external-facts §G24、91 §3.85 |

## 閘門（凍結 tree （首候選；commit 前的工作樹＝本 commit 內容） 後全跑，本機，dev server 已停；scratchpad `gates_e8b.py` 以 `PYTHONIOENCODING=utf-8` 逐支以 bash.exe 執行 config/ci.rb 步驟）

| 閘門 | 命令 | 退出碼 | 摘要 |
|---|---|---|---|
| Style: Ruby | `bin/rubocop` | exit=0 | 987 files inspected, no offenses detected |
| Security: Gem audit | `bin/bundler-audit` | exit=0 | No vulnerabilities found |
| Security: Frontend audit | `pnpm audit --audit-level high` | exit=0 | No known vulnerabilities found |
| Security: Brakeman code analysis | `bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error` | exit=0 | No warnings found |
| Test: Rails | `bundle exec rspec` | exit=0 | （未解析） |
| Test: Frontend | `pnpm test` | exit=0 |  |
| Type check: Frontend | `pnpm typecheck` | exit=0 |  |
| Build: Frontend | `pnpm build` | exit=0 |  |
| Invariants: Exec bits | `bash scripts/check-exec-bits.sh` | exit=0 |  |
| Invariants: Exec bit rules regression | `bash scripts/test-exec-bits-rules.sh` | exit=0 |  |
| Invariants: Prototype lint | `python scripts/lint-prototype.py` | exit=0 | ERROR 0 / WARN 136 |
| Invariants: Lint rules regression | `python scripts/test-lint-rules.py` | exit=0 |  |
| Invariants: Dead-control baseline | `python scripts/check-baseline-raise.py` | exit=0 |  |
| Invariants: Doc claims | `ruby scripts/check-doc-claims.rb` | exit=0 |  |
| Invariants: Doc claim rules regression | `ruby scripts/test-doc-claims-rules.rb` | exit=0 |  |
| Invariants: Tenant isolation | `ruby scripts/check-tenant-isolation.rb` | exit=0 |  |
| Invariants: Design token single source | `ruby scripts/check-tokens-sync.rb` | exit=0 |  |
| Invariants: Reversal naming | `ruby scripts/check-reversal-naming.rb` | exit=0 |  |
| Invariants: Money unit boundary | `ruby scripts/check-money-boundary.rb` | exit=0 |  |
| Invariants: Money rules regression | `ruby scripts/test-money-rules.rb` | exit=0 |  |
| Invariants: Limits key types | `ruby scripts/check-limits-keys.rb` | exit=0 |  |
| Invariants: Limits key rules regression | `ruby scripts/test-limits-key-rules.rb` | exit=0 |  |
| Invariants: Rem token integrity | `ruby scripts/check-rem-tokens.rb` | exit=0 |  |
| Invariants: Rem token rules regression | `ruby scripts/test-rem-token-rules.rb` | exit=0 |  |
| Invariants: CI parity (ci.yml ⊆ config/ci.rb) | `ruby scripts/check-ci-parity.rb` | exit=0 |  |
| Invariants: CI parity rules regression | `ruby scripts/test-ci-parity-rules.rb` | exit=0 |  |
| Invariants: Workflow syntax | `ruby scripts/check-workflow-syntax.rb` | exit=0 |  |
| Invariants: Workflow syntax rules regression | `ruby scripts/test-workflow-syntax-rules.rb` | exit=0 |  |
| doc-claims 兩支（worklog 閘門表寫入後補跑） | `ruby scripts/check-doc-claims.rb`／`ruby scripts/test-doc-claims-rules.rb` | 見 PR body | |

### 突變（commit 後跑 scratchpad `mutate_e16.py`；每格改一處 → 目標規格轉紅 → `git checkout --` 還原）

M174 section 形退回 `path: nil` ⇒ SR6／M175 `return_to` 預設丟 query ⇒ F6b／M176 預設值的 `&` 也轉義 ⇒ F6b／M177 快取分支餵原始 query ⇒ SR7／
M178 routes 只收 POST ⇒ L5／M179 `safe_return_to` 退回「非 / 開頭回根」⇒ L2b。結果表登記於 PR body（commit 後才跑，不為登記刷新 head；17.4）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.85 的 V：`return_to` 預設值對 `"<>` 的本尊處置（本尊觀測到含 `"` 的參數整個消失，規則未取得）、整頁形（非 section）帶 query 的本尊值（Ella 不可觀測）、
  section 形的 `request.path` 本尊值、fragment／無法解析 return_to 的本尊處置。
- ⚪ 頁首 SRA 剩餘 3 處 differ＝本尊新版顧客帳戶連結（91 §3.83），待顧客帳戶包裁定。
- bt3 部署後須重跑兩個脈絡的 `render_parity:diff`（hoko 檔在 scratchpad `hdr/`；mirror 段 `curl 'https://mirror.chilling.com.hk/?section_id=sections--header-group__header_default'`）
  與 `POST /localization` 帶 `_method=put` 的 302 複驗（部署前 404）。
- 其他 `PageRenderer.new` 呼叫端（search／recommendations／cart sections／admin preview）未傳 `query_string` ⇒ 其 section 形的 `return_to` 只出路徑；
  Ella 這些段不含 localization 表單，未對表。
