# 2026-09-02 引擎缺口 PR-5：Shop 欄位（money_with_currency_format／description／features）＋字型庫家族

分支 `engine/shop-fonts-5`（自 PR-4 分支長出，PR-4 合併後 rebase 到 main）。配對 handoff：
`docs/handoff/2026-09-02-引擎缺口-shop欄位與字型庫.md`。收口 D78 triage：`ShopDrop.money_with_currency_format／
description／features`、`font_library.roboto_condensed`（顯示名誤為 "Roboto"）／`libre_baskerville`／`helvetica`。

## 已完成的工作 (Done)
- `ShopDrop#money_with_currency_format`＝money_format＋空白＋幣別碼（官方 objects/shop 句＋
  filters/money_with_currency 例 `$10.00 CAD`，取證 2026-09-02）；`#description`＝nil 宣告（shops 表無欄）；
  `#features`＝`ShopFeaturesDrop`（未文檔化；`login_with_shop_classic_customer_accounts?`／`follow_on_shop?` 皆 false）。
- 字型庫：`config/storefront_fonts.yml` 新 `library` 段（官方 settings/fonts 表列的 Assistant／Karla／
  Libre Baskerville／Lora／Roboto Condensed／Arial／Times New Roman／Helvetica，含官方 handle 碼），
  `system` 段補 `system_ui`；`FontLibrary` 解析 library 家族（system?=false、file=nil）、handle style
  字元接受 `o`（oblique，官方 deprecated Helvetica 形）、未知家族顯示名逐字 titleize；`variant`／
  `variants_for` 同步支援 library 家族。
- spec `spec/liquid/shop_fonts_gap_spec.rb` H1–H6（H6＝Kalles 5.4.2 真主題 conformance 不再出現
  `font_library.helvetica`）；`font_pipeline_spec` F4 的「未知 handle」樣本由 `helvetica_n4` 改為永不入表的
  `nope_font_n4`（入表即失效——keyset_cursor_spec 同款教訓），斷言語義不變。
- 突變輪：M1 拔 library 解析分支 ⇒ H3 紅；M2 顯示名改回 `split("_").first` ⇒ H4 紅；
  M3 `money_with_currency_format` 少幣別碼 ⇒ H1 紅；M4 `features` 回 nil ⇒ H2 紅。
- 回歸：`bundle exec rspec spec/liquid/shop_fonts_gap_spec.rb spec/liquid/font_pipeline_spec.rb` 綠。

## 修改的檔案與核心邏輯 (Changes)
- `app/liquid/theme_engine/font_library.rb`：`HANDLE_RE`＋`STYLES`、`library`、`drop`／`variant`／`variants_for`
  的 library 分支、`build_library`、`system_drop` 顯示名 titleize。
- `config/storefront_fonts.yml`：`system.system_ui`、新 `library` 段。
- `app/liquid/theme_engine/drops.rb`：`ShopDrop#money_with_currency_format／description／features`、新 `ShopFeaturesDrop`。
- `spec/liquid/shop_fonts_gap_spec.rb`：新檔；`spec/liquid/font_pipeline_spec.rb`：F4 樣本鍵。

## 尚未完成或需注意的風險 (Pending / TODO)
- library 家族**無自 host woff2**：`font_face`／`font_url` 空輸出，瀏覽器退 fallback 家族——視覺上與本尊
  不同（登記）；補檔方式＝OFL 檔進 `public/fonts/` 並把家族從 `library` 搬到 `families`。
- `shop.features` 的本尊 runtime 值形＝未取得（未文檔化）；`shop.description` 無資料欄。
- 官方字庫表列其餘家族（非三套主題預設用到者）未登記，仍走 system fallback＋miss。
- `font.baseline_ratio` 維持常數 0.1（既有 ours）。
