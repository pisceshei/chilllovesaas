# 2026-08-30 — CLI 探針輪＋真引擎分歧修正

## 已完成的工作 (Done)
- 使用者完成 Shopify CLI OAuth（裝置流；CAPTCHA＋密碼紅線由使用者親手）。
- `theme pull` 全量普查（884 檔）＋極簡探針主題 S9-Probe（#166056231147，
  unpublished）push 到真店，用受控模板逐格量真引擎 drop 語義——
  全部結果落 `docs/research/83` 新 §12。
- #200 的三個未取證格：壞 ?variant=（已證與實作同款）、全售罄 fallback
  （單變體證據支持）、compare_at nil 混值（varies 仍開；但發現 min/max
  全 nil 印 0 的分歧）。
- 修正兩處真引擎分歧：`compare_at_price_min/max` 0 fallback、
  `variant.weight_unit` 常數 "kg"（ours，無單位存儲）。spec 同步。

## 修改的檔案與核心邏輯 (Changes)
- `app/liquid/theme_engine/drops.rb`：兩處量測值修正（帶 83 §12 錨）。
- `spec/liquid/drops_spec.rb`：P4 補 min/max=0 斷言、V6 補 weight_unit。
- `docs/research/83`：新 §12（上傳驗證文案／JSONC 頭註／平台改寫 changelog／
  正規化普查／真引擎逐格表）。
- `docs/plans/2026-08-30-商品模塊-Liquid對接缺口分析.md` §D 更新註。

## 尚未完成或需注意的風險 (Pending / TODO)
- 仍未取證：多變體全售罄的 sofav、compare_at nil 混值的 varies、
  `compare_at_price`（單數形）的輸出形——探針主題還在，隨時可補。
- W6 json-filter parity 兩項登記（product|json 鍵差、owv|json 壓平字串）。
- S9-Probe 主題與 live-ella pull 快照留存（scratchpad；主題在店端長存）。
