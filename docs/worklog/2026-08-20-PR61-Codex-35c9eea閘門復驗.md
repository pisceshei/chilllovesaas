# PR #61：Codex `35c9eea` 修復後閘門復驗

## 已完成的工作 (Done)

- 對實作提交前的工作樹從 setup 起執行 production 等價閘門。Windows 上依 `bin/setup` 原始碼
  將 setup 等價拆成 `bundle check`、`pnpm install --frozen-lockfile`、
  `ruby bin/rails db:prepare`、`ruby bin/rails log:clear tmp:clear`，再逐項執行 `config/ci.rb`
  的其餘 gate，總數由該拆分與 `Select-String '^\s*step '` 現值導出。
- 第一輪的真實結果是前 27 項通過、第 28 項 workflow syntax 失敗、第 29 項因 fail-fast 未執行；
  先前口頭速記成「27/2」不精確，不能當正式證據。第 28 項的 33 個 `run:` 同時報
  `/bin/bash: C:/Users/.../Temp/*.sh: No such file or directory`，與既有 Windows shell 路徑坑同型，
  沒有證明 33 段 shell 同時語法錯誤。
- 第二輪在同一 PowerShell process 前置 `C:\Program Files\Git\bin`；smoke check 證實
  `Get-Command bash` 為 Git Bash、`pwd` 為 `/c/Users/pisce/Downloads/shopifysystem/chilllovesaas`，
  且兩個目標腳本存在。之後從 setup 起完整重跑，輸出 `GATES_ALL_GREEN=29`。
- 第二輪關鍵結果：Rails `284 examples, 0 failures`；frontend `5 passed`；RuboCop 162 檔零違規；
  Brakeman 0 errors／0 warnings；Ruby 與前端 audit 均無已知漏洞；prototype 三份合計
  `ERROR 0 / WARN 136`；doc-claims warning 0；workflow syntax 解析 2 份 YAML、33 個 `run:`，
  regression 11 條全綠。MySQL 由 `ruby bin/rails db:prepare` 的 exit 0 證明應用連線可用。

## 修改的檔案與核心邏輯 (Changes)

- 本 worklog 與 `docs/handoff/2026-08-20-PR61-Codex-35c9eea閘門復驗.md`：保存第一次環境假紅、
  shell A/B 證據、完整第二輪命令契約與 29 項結果。
- `docs/handoff/2026-08-20-階段一開場包.md`：終態入口加入本步。
- `docs/specs/91-pit-register.md`：附錄 A 納入本步兩檔；shell 路徑根因已由既有條目覆蓋，通讀後
  沒有另造重複坑項。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本步文件尚待 commit 與 commit 後 doc-claims；若 commit 後文檔檢查出現 error 或 warning，
  必須停止 push 並另立修復交接。
- Node v24.19.0 超出 `package.json` 宣告的 `>=22.12 <23`；前端 gate 的綠只證明這次環境實跑，
  不代表專案已支持 Node 24。
- 完整 29 項發生在實作 commit `ee7835e` 前的同內容工作樹；本步只新增交接文件。後續仍須依
  15.1／15.2 核對提交差異與 GitHub 意見，push 後由機械 CI 重新驗證新 head。
- PR #61 命中 workflow validation-skip 與鐵律 18.3；本機全綠不補足 Claude 判詞，也不授權代行合併。
