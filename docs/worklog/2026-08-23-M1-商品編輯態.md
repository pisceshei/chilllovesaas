# 2026-08-23 — M1：商品編輯態（productSet 更新分支＋lockVersion＋前端同元件雙態）

## 已完成的工作 (Done)

- **後端更新分支**（`Catalog::SaveProduct#update`，63 §B.4 同一支 mutation）：
  GID 解析→`FOR UPDATE` 行鎖→lockVersion 比對（缺=BLANK、過期=STALE_OBJECT
  field null）→宣告式覆寫 product＋隱含變體→恆 bump 版本（全樹鎖：只改變體
  也 touch product）→`products/update` outbox 事件。handle 變更暫拒
  （301 基建屬 URL 包；同值 no-op），未帶 status＝保持現值。
- **讀取面**：`Types::ProductVariantType`（金額 R4 出向走 `Money::Storage#to_decimal`，
  不手算除法）；ProductType 補 descriptionHtml/variants；QueryType 補
  `product(id:)`（28 §1 對應；tenant-scoped object_from_id）。
- **前端編輯態**（同元件 isNew 分流，59 §7）：載入填表（htmlToDescription 反向）、
  handle 唯讀＋hint、右欄狀態卡（四態 select＋可購買/可被發現兩維讀值，13 §F1.2
  真值表）、儲存帶 id＋lockVersion 不帶冪等鍵（更新天然冪等，D-PS5）、成功留頁
  吸收新 lockVersion、STALE_OBJECT toast、查無商品空態。
- 測試：後端 +6 例（更新快樂路徑/STALE/缺 lockVersion/handle 拒與 no-op/
  NOT_FOUND/product(id:) 讀取形）＝全套 330；前端 +4 例＝14，全綠。

## 修改的檔案與核心邏輯 (Changes)

- `app/services/catalog/save_product.rb`：call 依 id 分流；update／
  update_implicit_variant!／enqueue_event! 泛化；normalize 的 status 語義改
  「建立缺席=draft、更新缺席=保持」。
- `app/graphql/types/product_variant_type.rb` 新增；`product_type.rb`／
  `query_type.rb`／`inputs/product_set_input.rb`（lock_version 引數）擴充。
- `app/frontend/admin/pages/ProductDetailPage.tsx`：雙態整合（載入/狀態卡/
  儲存分流）；`App.tsx` 的 :id 路由換真頁。
- spec：`product_set_spec.rb` 更新態 describe＋讀取形；v1 守門測試改寫為
  「缺 lockVersion=BLANK」（行為演進在測試內註明）；`ProductDetailPage.test.tsx` +4。

## 尚未完成或需注意的風險 (Pending / TODO)

- handle 變更（需 url_redirects 301 基建）；具名選項/多變體（B.5）；
  狀態改 ACTIVE 不觸發 auto_publish（88 §5 #2 照舊）；編輯態內容語言切換器
  （多語言包）；SEO 文案欄位仍 disabled（input 無 seo）。
- 更新態併發防線＝FOR UPDATE＋版本比對；**跨請求丟失更新已測**（過期版本輸），
  執行緒級同時儲存未加測——行鎖序列化下第二個必然讀到新版本吃 STALE，
  邏輯上被既有測試覆蓋，登記於此供庫存包參照。
