# 2026-08-15 — 驗收失敗「看得到症狀、看不到原因」的修復

> 起因：使用者「PR 41 驗收失敗」。查下去發現 PR #41 沒有錯，
> 而且**這個 bug 之所以卡了 11 小時、跨四個工作階段，是因為錯誤原文從一開始就被藏起來**。

---

## 已完成的工作 (Done)

### 1. 先確認：不是 PR #41 的問題，也不是任何 repo 側的問題

PR #41 兩次 push 的驗收都失敗，指紋與 08-15 凌晨起的全滅一模一樣
（`is_error=true num_turns=1 total_cost_usd=0 duration_ms≈569–716`）。

逐項用**時間戳**排除，不用推測：

| 候選原因 | 證據 | 結論 |
|---|---|---|
| 模型是 `claude-fable-5`、帳號沒權限 | `git log -S`：模型自 **00:14** 起就是它；PR #33 的驗收 **01:41／01:54 兩次成功** | ❌ 同一份設定稍早才成功過兩次 |
| `--effort max` 不被接受 | `git log -S '--effort'`：**00:01 引入、00:10 之後沒再動** | ❌ 成功那兩次也帶著它 |
| workflow 被改壞 | `claude-review.yml` 最後一次改動 **01:00** | ❌ 🔴 **最後成功（01:54）與第一次失敗（01:55）之間，這個檔一個字都沒改** |
| `CLAUDE_CODE_OAUTH_TOKEN` 被換掉 | repo Settings → Secrets：**Last updated Aug 14** | ❌ 不在邊界上 |
| 暫時性抖動 | 01:55→12:46 共 **11 小時**、所有 run 皆 ≤47s | ❌ |
| 5 小時滾動用量視窗 | 同上（早該恢復） | ❌ |

⇒ 🔴 **repo 這一側已經沒有變數可以解釋它。**
⇒ 🔴 **換模型（不論換成哪個）都不會修好它**——這一點 PR #37 的結論相同。
仍未排除的都在帳號側：用量上限、OAuth token 在 CI 這條路徑上的獨立額度、訂閱狀態。

### 2. 真正的根因：**錯誤原文被 action 白名單式砍掉了**

失敗時 log 只印：

```
Running Claude Code via SDK (full output hidden for security)...
Rerun in debug mode or enable `show_full_output: true` in your workflow file for full output.
```

而 result 物件只有 6 個欄位——**`result` 欄（錯誤訊息就在裡面）整個不見**：

```json
{"type":"result","subtype":"success","is_error":true,
 "duration_ms":626,"num_turns":1,"total_cost_usd":0,"permission_denials_count":0}
```

🔴 **這就是為什麼四個工作階段都在猜**：大家拿到的是同一組**被拿掉了原因的數字**，
於是只能對指紋，而指紋對不出「額度」與「權限」的差別——**兩者長得一模一樣**。

### 3. 🔴 實測證偽：`Rerun in debug mode` 這條建議是死的

在 PR #41 的 run `31865047628` 勾 **Enable debug logging** 重跑 attempt #2：

- raw log **11,704 行**、`##[debug]` 滿地都是 ⇒ debug mode 確實開了
- **那句「full output hidden for security」照樣印**
- result 物件**照樣只有 6 個欄位**

⚠️ 而 action 的 `base-action/README.md` 明寫
「`show_full_output` is automatically enabled when GitHub Actions debug mode is active」
——**在 `@v1` 上不成立**。⇒ 兩條建議只有 `show_full_output: true` 是真的。

### 4. 修法（兩層，先便宜的再貴的）

**① 先從存檔撈** —— 失敗分支多撈一個 `ERRTEXT`（result 物件的 `result` 欄），撈到就貼原文。
console 是白名單式輸出，但**存檔 `claude-execution-output.json` 有沒有被砍是未知的**
⇒ 撈得到就賺到（噪音為零），撈不到就明說撈不到並指向 ②。

**② 明文開 `show_full_output: "true"`** —— 保證下一次失敗一定看得到原因。

🔴 **開之前先驗證它不是「會被靜默忽略的未知 input」**（本檔上方 `--model` 那段記過這個形態）：

- `anthropics/claude-code-action@v1` 的 `action.yml` **156 行宣告**、
  **325 行 `INPUT_SHOW_FULL_OUTPUT: ${{ inputs.show_full_output }}` 明文映射**。
- 🔴 325 行那個映射**非有不可**：composite action **不會**把 `with:` 自動傳給內層步驟，
  內層讀的是 `process.env.INPUT_SHOW_FULL_OUTPUT`。少了映射，寫了等於沒寫。
- 宣告型別是**字串**（`default: "false"`）⇒ 照字串寫，不寫 YAML 布林。

⚠️ **查證過程本身踩了一個坑，值得記下**：第一次用 GitHub 的 blob 檢視搜 `show_full_output`
得到「找不到」，差點據此寫下「v1 沒有這個 input」。
🔴 **blob 檢視是虛擬捲動**，只渲染前約 150 行（我抓到的 `innerText` 只有 7,119 字元，
實際檔案 460 行 / 23,685 字元），而 `show_full_output` 在**第 156 行**——就差在渲染邊界外。
**「頁面上搜不到」是渲染結果，不是事實**（與鐵律 12 §0 對空白頁的處置同型）。
正解：`raw.githubusercontent.com` 的純文字，或 GitHub code search。

---

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `.github/workflows/claude-review.yml` | ①驗收步驟加 `show_full_output: "true"`（含 v1 已驗證的宣告／映射行號、debug mode 的證偽、移除條件）；②失敗分支加 `ERRTEXT`／`ERRBLOCK`，撈得到就貼「錯誤原文」程式碼區塊，撈不到就明說原因並指向 ①；③判準表末尾補「不要勾 debug 重跑」「不要靠換模型試」兩條，附證偽依據 |

### 自測（不是只看 diff）

`ERRTEXT` 的 jq 五條 fixture，`jq 1.8.2` 實跑：

| case | 輸入 | 期望 | 實得 |
|---|---|---|---|
| A | 有 `result` 欄 | 撈出原文 | `Credit balance is too low` ✅ |
| B | **PR #41 實際觀察到的形狀**（無 `result` 欄） | 空 | 空 ✅ |
| C | 完全沒有 result 物件 | 空且不爆 | 空、exit 0 ✅ |
| D | 單一 JSON 陣列格式（非 JSONL） | 同 A | `Invalid API key` ✅ |
| E | `result` 不是字串（物件） | `tostring` 不爆 | `{"code":429,...}` ✅ |

整個失敗分支再用 **stub `gh`** 端到端實跑 A／B 兩種輸入，確認：

- A 貼出「**錯誤原文**」程式碼區塊
- B 貼出「**錯誤原文：取不到**」並指向 `show_full_output`
- 🔴 `printf` 的格式字串與參數個數對得上（**10 個 `%s` / 10 個引數**）
  ——這是本次改動最容易錯的地方，因為我在中間插了一個 `%s`

另：`YAML.safe_load` 通過；解析步驟抽出跑 `bash -n` 通過；
`show_full_output` 解析後的實際值確認為字串 `"true"`。

---

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **本 PR 自己驗證不了**（反竄改：動 `.github/workflows/**` 的 PR，驗收步驟整個跳過）。
   **合併進 main 之前不生效**。⇒ 合併後第一個 PR 的失敗留言就是驗證點。
2. 🔴 **這不是「修好驗收機器人」，是「讓它失敗時說得出原因」。**
   ⚠️ 合併後驗收**很可能還是失敗**——那是預期的。差別在於這次留言裡會有錯誤原文，
   而**那句原文才是修復的起點**。不要把「合併了還在失敗」讀成這個改動沒用。
3. ⚠️ **`ERRTEXT` 撈不撈得到是賭的**——console 被砍是實測，**存檔有沒有被砍沒有實測過**
   （檔案在 runner 上，跑完就沒了）。若下一次留言印「取不到」，
   代表存檔同樣被砍 ⇒ `show_full_output` 是唯一手段，**那時不得把它關掉**。
4. ⚠️ **`show_full_output` 會讓每輪 log 多出完整審核串流**（含工具執行結果、檔案內容）。
   本倉庫是 **private**、log 僅協作者可見、secrets 仍被 GitHub 遮罩 ⇒ 可接受，
   但這是**診斷用的臨時設定**，移除條件已寫在該行上方註釋裡。
   🔴 若本倉庫日後改為 public，**必須先關掉這一行**。
5. ⚠️ **ERRTEXT 截斷在 1500 bytes**，沒有做「截斷了要講」的標記。
   多數 API 錯誤遠短於此，但若真被截斷，讀的人不會知道。（低優先，未做。）
6. ⚠️ 本次沒有把這段 shell 抽成 `scripts/` ＋ fixture 回歸測試——
   五條 case 是手動構造的，**日後改壞不會有人知道**。
   正解與 PR #41 對執行位元閘門做的同型（`scripts/check-*.sh` ＋ `test-*-rules.sh`），
   本次沒做，理由是它與 PR #37 原本的 Y7／Y8 共用同一段、應該一起抽，
   而那要動更大的結構。**登記在此，不是忘了。**
