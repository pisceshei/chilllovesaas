# PR #64 第十五輪 Claude 驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `874eade9e58df099323013c9693e3f19e21debe4`：Claude issue
  comment `5363892357`／wrapper `5363893749` 為需修改，含 🔴1、🟡2、⚪3；Codex exact-head
  issue comment `5363805191` 為零意見。三端點與 GraphQL threads 另行全量重取，沒有把 CI 綠
  或單一 reviewer 外推成雙清。
- 依鐵律 16 以 `git log -p` 追第十四輪 worklog、external-facts A10／B9 與 91 §3 沿革。
  事實成立：上一輪 comment `5363665327` 的三條 ⚪ 在 §3 為零；W14 的 20.3 表頭與七列輸出
  也都漂移。本輪只補點名範圍，不改 Claude 另外列出的六條白項原文。
- `91` §3 已把 `5363665327` ⚪1／2 各立條目，⚪3 併入既有 destructive guard 射程條目；
  同時把本輪 `5363892357` 的 ⚪1／2／3 各自登記，令 PR 描述與 A.1 的「已處置」重新符合實物。
- W14 的第三欄恢復鐵律 20.3 固定名稱「反向複驗輸出」，七列改存本輪實跑 stdout／集合計數，
  並以相鄰 dated correction 說明這次歷史層修訂，避免再出現無沿革的實質覆寫。
- GitHub REST 官方 <https://docs.github.com/en/rest/pulls/pulls?apiVersion=2022-11-28>
  與 GitHub CLI 官方 <https://cli.github.com/manual/gh_api> 均於 2026-08-21 獨立重取。A10 已補完整
  250 上限逐字；B9 已補 `-F ... @-` 與 `--input -` 的英文逐字，沒有只採驗收方轉述。

### 鐵律 20.4 同根因復發記錄

| 要求 | G-01：⚪ 落籍第三次漏做 | 20.3 表格機制第三次漂移 |
|---|---|---|
| 復發錨 | PR #55 第十輪與 PR #58 第三輪已各漏搬一次；本輪再把三條未搬寫成已處置 | 第十輪曾把④⑥換成不存在的類型，第十一輪曾重編號並漏⑥；W14 又刪掉「輸出」兩字並只留宣稱 |
| 既有防線失效原因 | G-01 仍為待裁候選，送驗前只有人工 checkbox，沒有逐 comment id 對 §3 的 cardinality | 只看七列外形，沒有逐字驗表頭，也沒有要求每格含已執行 stdout／計數 |
| 被漏掉的固定步驟 | 每次有 ⚪，先按 comment id 建清單，再逐項對 §3；PR 描述與 A.1 只能在對帳相等後寫已處置 | 逐字複製 20.3 表頭；七列逐一實跑，第三欄只能放輸出或明確不適用證據，不放未來式 |
| 可重跑反向複驗 | 只截 §3 後，`5363665327` 必須恰 3 筆、`5363892357` 必須恰 3 筆 | `rg` 表頭只准命中「反向複驗輸出」；W14 七列逐格可對回本輪 CLI／API／render stdout |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取 | issue、review body、paginated inline、GraphQL threads 全量重取並綁 `874eade…` | Claude `5363892357`＝需修改；Codex `5363805191`＝零意見；舊 head 結果不外推到下一 push |
| ② producer／consumer／終態 | 同步 external-facts、91 §3／A.1、W14 與本 worklog | §3 `old_white=3,new_white=3`；W14 good header＝1、bad header＝0；A.1 本 worklog＝1 |
| ③ 完成性聲明 | 逐字證據、⚪ cardinality、歷史修訂註記各自取證 | `a10_quote=1`；`field_quote=1`；`input_quote=1`；W14 相鄰 correction＝1 |
| ④ 狀態空間 | 上一輪三條 ⚪ 與本輪三條 ⚪ 分開；新增官方逐字與範圍外原文分開 | `5363665327` 在 §3＝3；`5363892357` 在 §3＝3，兩個集合沒有互相代替 |
| ⑤ fail-open | 先保存 §3 零命中的 failure-first，再驗新 comment id 恰三筆 | failure-first＝`5363665327` 在 §3 為 0；修後輸出 `old_white=3,new_white=3` |
| ⑥ workflow 本機假綠 | 本輪不改 workflow／scripts／config；用非空 docs 對照守空查詢 | `restricted_count=0`；`tracked_docs_vs_head=3`；`status_count=4`（三修改＋一新增） |
| ⑦ Markdown／Windows | W14、W15、91、external-facts 走 GitHub `/markdown`；最後 edit 後跑 29 閘門 | 於受驗樹 `82e2fa8` 實測（計數式＝`<tr\b`／`<th\b`／`<td\b`；🔴 `<th` **前綴**會一併命中 `<thead>`，第三輪已證偽、不得使用）：W14 `table=2,tr=13,th=5,td=29`；W15 `2,13,6,33`；91 `3,25,11,72`；EF `blockquote=29,table=0` |

## 修改的檔案與核心邏輯 (Changes)

- `docs/dev/external-facts.md`：補 A10 的 250 上限完整逐字，以及 B9 的 field stdin／whole-body
  stdin 官方逐字。
- `docs/specs/91-pit-register.md`：補上一輪三條 ⚪、本輪三條 ⚪，並在 A.1 納入本 worklog。
- `docs/worklog/2026-08-21-PR64第十四輪雙驗收修復.md`：修正 20.3 固定表頭與七列實跑輸出，
  加相鄰 dated correction。
- `docs/worklog/2026-08-21-PR64第十五輪Claude驗收修復.md`：保存本輪輸入、20.4 復發閉環、
  20.3 實跑與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 補齊上方 20.3 實跑輸出後，最後 repo edit 後須跑 GitHub Markdown 結構與連續 29 道
   Windows production-equivalent gates；任一非零即停止。
2. commit 後須跑 doc-claims warning 0、鐵律 15.1／15.2 三端點＋review body＋GraphQL threads
   全量攝取；若有晚到意見先處置。
3. #64 為一般 PR；只有新 exact-head CI、Claude、Codex 雙零、approved threads resolved、
   mergeable 與 evaluator `1111` 同時成立，才可依 D31／D32 代行 squash merge。
