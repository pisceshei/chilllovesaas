# 2026-09-02 Ella 修復 PR-20：storefront filtering（facets）v1

## 已完成的工作 (Done)

- 91 §3.61 收口（6 軸艦隊 high 級「系列頁篩選器整組缺席」）。官方契約
  取證 2026-09-02：objects/filter（label/param_name/type 三值/values/
  active_values/min_value/max_value/range_max/operator/url_to_remove）、
  objects/filter_value（label/value/param_name/count/active/url_to_add/
  url_to_remove）、support-storefront-filtering。
- **Facets 服務**（`app/liquid/theme_engine/facets.rb`）：
  - v1 過濾器集合＝availability(boolean)／price(price_range)／變體選項
    (list, per option)／vendor／product_type（ours：全開，配置面後續）。
  - param 形：`filter.v.option.{name}`＝官方逐字例；其餘採本尊已知形標 V
    （主題端 param 自我一致——Ella 表單 name 取自 drop 的 param_name）。
  - 🔴 單位邊界（鐵律 3）：URL 參數＝主單位十進位（Ella price-facet 以
    money_without_currency 填 input）；drop `min_value.value`＝integer
    cents（Ella `| money` 直餵）——換算只在 parse 層一處。
  - 🔴 多值 OR 從 **query string** 解析（Rack parse_query 保留重複鍵；
    Rails params 對重複裸鍵 last-wins 靜默丟值——MF2 殺手格）；跨過濾器
    AND。counts＝套用其他過濾器後（標準 faceting；官方口徑未細＝V）。
  - URL 重建：url_to_add/remove 官方語義（分頁參數恆剝、sort_by 保留）。
- **佈線**：CollectionDrop.filters 語境齊備出真 drop（片段/editor 舊呼叫
  面照舊 []）；CollectionProductsDrop.relation 套 facets（base_relation
  抽類方法——雙處漂移即「數字對不上列表」bug）；products_count／
  all_products_count 官方語義真分家；pages_controller `_facets_qs`
  正規化子串（🔴 進頁快取鍵——MF3 殺手格：漏鍵＝過濾頁互汙）。
- 測試 FA1–FA5；突變 3/3 殺（apply no-op／多值塌陷／快取鍵漏）。
- minimal fixture main-collection 加 facets 渲染塊（斷言面）。

## 修改的檔案與核心邏輯 (Changes)

- 新 `app/liquid/theme_engine/facets.rb`（Facets＋FacetFilterDrop＋
  FacetValueDrop）。
- `drops.rb`：CollectionDrop filter 語境／facets／counts 分家；
  CollectionProductsDrop base_relation 類方法＋facets 套用＋unfiltered_total。
- `page_renderer.rb`：兩個 CollectionDrop 建點傳 filter_query/request_path。
- `pages_controller.rb`：facets_query_string（排序重建＝快取鍵穩定）。
- fixture main-collection.liquid＋新 spec storefront_facets_spec.rb。

## 尚未完成或需注意的風險 (Pending / TODO)

- search.filters 仍 []（v1 只接 collection——96 §4 搜尋面另輪）。
- 過濾器配置面（本尊 Search & Discovery：啟停/排序/合併值/swatch）未做；
  presentation 恆 text、operator 恆 OR。
- 官方 5000/1000 上限（大集合不出 filters）未做——demo 量級不觸及，登記。
- availability param 精確字串等四個 V（worklog 首段）；counts 口徑 V。
- Ella facets 的 sort「most-relevant」值只在 filter 語境出現——sort 對映
  既有（96 §2），未加新值。
