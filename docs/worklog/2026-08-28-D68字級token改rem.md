# 2026-08-28 — D68 字級 token 改 rem ＋ 介面語言保留裁定落地

使用者裁定：①介面語言留在頂欄；②px→rem 執行、連 B 道靜態檢查一起做。
本包＝①的登記 ＋ ②的轉換本體。🟢 不觸 18.3；靜態檢查腳本另開 D69（18.3 人工合併）。

## 已完成的工作 (Done)

- **裁定①登記**：`91` §3.41 W-1 處置＝裁定保留；後果（原型 <1400 視口中欄不正中）如實記錄。
- **轉換**：`--t-*` 13 顆 ＋ `--lh-*` 8 顆，px → rem，全部整除無捨入。
  producer＝原型 `:root`，`sync-tokens.rb` 機械同步；`--sp-`/`--r-`/`--h-`/`--sz-` 維持 px（另案）。
- **驗收兩道**（本機 Chrome、同源 iframe、擾動污染已停用）：
  - **擾動法（A）**：root 32px（×2）。🔴 **canary 先行**——轉換前 12 個代表元素比值全 **1.0**；
    轉換後 token 消費端全 **2.0**，唯二 1.0 是 kbd 的 10px 階外字面值（本來就該不動）。
  - **預設恆等**：`git show HEAD:` 對照組並排，35 條路由 × 2792 元素 × 12 個 computed 屬性
    ——**差異 0**。

## 🔴 事故：同一個坑踩兩次——註釋裡的 `*/`

兩段新註釋都寫了 `--t-*/--lh-*`：**`*/` 提早終結 CSS 註釋**，殘骸吞掉緊鄰宣告。

| 次 | 吞掉什麼 | 症狀 | 誰抓到 |
|---|---|---|---|
| 1 | `--t-275` 宣告 | `--t-2xs` 的 var() 無定義 ⇒ `.dev-tag` 11→12px | 擾動法的 **at16 基線欄**（不是比值欄） |
| 2 | `html,body` 基準規則前半 | 全站 body 字重 450→400（200 個元素） | 全路由等價比對 |

🔴 兩次都是**行為級驗收**抓到的：`lint-prototype`（只查大括號平衡）與
`check-tokens-sync`（逐位元組、兩邊一起壞）都結構上看不到。
修法＝改寫成「--t- 與 --lh- 兩族」；掃描判準＝`\*/\S`（`*/` 後緊接非空白），
掃完全檔僅剩 6 處合法密排形（註釋正常結束後緊接宣告）。登記 `91` §3.42 W-2。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `docs/design/chilllove-admin-v2.html` | `:root` 21 顆 token px→rem ＋ 轉換說明註釋（含事故記錄）；D63 註釋更新 |
| `app/assets/tokens.css` | `sync-tokens.rb` 機械同步（382→394 行） |
| `docs/DECISIONS.md` | D68 |
| `docs/specs/91-pit-register.md` | §3.41 W-1 處置更新（於 D68 條目內登記）＋ §3.42（W-1…W-3） |

`admin.css` 本包未動（它的消費端全走 var()，值變了自動跟）。

## 20.2 適用類型／固定處理／反向複驗輸出

| 類型 | 適用 | 固定處理已跑 | 反向複驗輸出 |
|---|---|---|---|
| ① 證據不完整 | 是 | 換算逐顆列出並驗整除；本尊同形引 D63 原文 | 21 顆對照表 |
| ② 生產者／消費者 | 是 | 只改 producer，同步機械複本；JS 消費者掃描＝0 | `check-tokens-sync` 394 行相同 |
| ④ 狀態空間 | 是 | 三種失敗形態各有覆蓋：漏轉（擾動 1.0）／轉錯值（整除驗算）／預設變化（等價比對） | 三道全綠 |
| ⑤ **fail-open** | 是 | 🔴 **擾動法 canary 先行**：轉換前全 1.0 的基線先取得，才能宣稱「2.0 是轉換造成的」。at16 基線欄本輪真的抓到回歸（11→12） | 前 1.0 → 後 2.0；等價差異 0 |
| ⑦ 工具鏈 | 是 | 先 encode 再 wb；改檔腳本檔案化 | 無截斷 |

## 尚未完成或需注意的風險 (Pending / TODO)

- **D69 靜態檢查未落地**（本包的下一步，18.3 人工合併）。
- **擾動法是臨時腳本**，未進倉庫、未進 CI（`91` §3.42 W-3）；
  消費端硬編 px 的殘留（C-3 那批）只有它照得出來。
- **`--sp-`/`--r-`/`--h-`/`--sz-` 仍是 px**（`91` §3.42 W-1，需另一裁定）。
- **`10px`（kbd）刻意留字面值**——階外值，擾動下比值 1.0 是預期行為不是漏轉。
