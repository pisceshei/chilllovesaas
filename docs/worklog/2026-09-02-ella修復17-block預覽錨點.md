# 2026-09-02 Ella 修復 PR-17：block 級預覽錨點（橋 blockId 點選/高亮）

## 已完成的工作 (Done)

- fleet editor-live 軸⑤收口：本尊 block 級錨點（官方 `block.shopify_attributes`
  ＝「The data attributes for the block for use in the theme editor」、編輯器外
  無值——shopify.dev objects/block 取證 2026-09-02；我方 BlockDrop 的
  design_mode 閘與 `data-shopify-editor-block` JSON attr 已於 PR-6 落地）：
- **橋點選**：click 先找 section wrapper，再 `closest("[data-shopify-editor-
  block]")`（限 section 內）解析 JSON 取 block id ⇒ `cl:select` 帶 `blockId`。
- **橋高亮**：`cl:highlight` 帶 `blockId` 時在 section 內逐一 parse attr 匹配
  ⇒ outline 縮到 block 元素（無匹配退 section 級）。
- **前端**：cl:select 的 blockId 直開 block 設定面板（含清空——無 blockId
  ⇒ null 清 stale 選取）；樹選 block 的 highlight postMessage 帶 blockId。
- 測試 BR1（後端橋內容三錨）＋ED20（訊息反選開面板／樹選 block 高亮帶
  blockId）；ED2 斷言更新（highlight 訊息自本包起帶 blockId 欄）。
- 突變 2/2 殺（拔前端反選、拔橋點選 blockId）。

## 修改的檔案與核心邏輯 (Changes)

- `app/liquid/theme_engine/runtime.rb`：EDITOR_BRIDGE_JS 兩處。
- `app/frontend/admin/pages/ThemeEditorPage.tsx`：cl:select blockId 反選＋
  highlight 帶 blockId。
- specs：theme_editor_bootstrap（BR1）、ThemeEditorPage.test.tsx（ED20＋ED2）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本尊 block 級的 hover 浮動工具列／拖曳重排（⑤e）仍未做，照舊登記。
- 巢狀 block（block.blocks 子層）點選會命中**最內層** blockEl 的 id——
  與樹的子層選取一致；本尊行為未逐格量測（V）。
