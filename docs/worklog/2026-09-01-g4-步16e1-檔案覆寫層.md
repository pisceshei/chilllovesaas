# 2026-09-01 G4 步 16e1：主題檔案覆寫層（code editor 資料層）

## 已完成的工作 (Done)

- `theme_file_overlays` 表＋`ThemeFileOverlay` model（一列＝一個被編輯的主題檔；
  uq (shop,theme,path)）；Theme has_many dependent destroy；themeDuplicate 一併
  拷列（F7/ME-4 紅證）。
- `OverlaySource`：overlay 列優先、缺列回落 base；list＝併集；🔴 **AST cache
  租戶安全**——overlay 檔在 `Runtime#compiled` 改 per-row 版本鍵
  （shop/theme/lock+updated_at 戳），未覆寫檔維持共用鍵（15a 跨租戶汙染同軸；
  F2/ME-1 紅證：A 店編輯 section 不汙染 B 店＋A 再編輯即時生效）。
- `Sources.resolve` 自動包裝（有覆寫列才包）；🔴 without_tenant＋顯式 shop_id
  （鐵律 2 條款②——resolve 會在租戶 context 外被叫）。
- `themeFileUpsert`：路徑白名單（allowed_top_dirs＋恰兩段＋檔名 A-Za-z0-9._-；
  🔴 **templates/ 與 config/settings_data.json 拒收＝雙真相源禁令**，F4/ME-3
  紅證）＋單檔上限依型引 limits `theme_editor`（官方 Theme limits 頁逐字：
  liquid 256KB/JSON 512KB/locale·data 1.5MB，取證 2026-09-01；asset 2MB=ours
  防呆）＋樂觀鎖＋🔴 touch theme（F3/ME-2 紅證）。
- 官方研究取證：help edit-theme-code＋dev code-editor＋dev architecture/limits
  （per-file save/Timeline/檔名規則/上限值——16e2 UI 的契約底）。
- 突變 4/4 殺（ME-1 共用鍵汙染／ME-2 touch／ME-3 templates 白名單／ME-4 拷列）。

## 修改的檔案與核心邏輯 (Changes)

- 新：migration（theme_file_overlays）、theme_file_overlay.rb、overlay_source.rb、
  theme_file_upsert.rb、theme_file_overlay_spec.rb（F1-F7）。
- 改：sources.rb（resolve→base_resolve＋包裝）、runtime.rb（compiled overlay 鍵）、
  theme.rb（has_many）、theme_duplicate.rb（拷列）、mutation_type、
  limits.yml（theme_editor 四鍵）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 16e2＝code editor UI（檔案樹分型資料夾＋多 tab＋unsaved dot＋per-file save
  ——官方形已取證）；16e3＝rename/delete/new file＋跨檔搜尋。
- ⚪ 覆寫層無版本歷史（本尊 per-file Timeline，官方「finite、asset 無」）；
  ⚪ binary asset 上傳不支援（mediumtext 文字檔限定）；⚪ base 檔刪除
  （官方 code editor 可刪檔——我方 overlay 無 tombstone）——登記 91 §3.71。
- 🔴 migration 首輪踩 91 §3.52 家族（add_foreign_key 被 strong_migrations 擋、
  create_table 已落地）——已改逐語句 guard＋safety_assured；新表 migration
  一律逐語句守衛。
- 🔴 本尊 Edit code 親點受阻（V 項）：online-store-web 跨域 iframe＋Chrome
  視窗最小化（viewport 0×0）；官方雙源＋41 §634 先行，視窗恢復後補實測。
- 生產煙測：部署後 upsert 一檔→前台渲染變→自清。

