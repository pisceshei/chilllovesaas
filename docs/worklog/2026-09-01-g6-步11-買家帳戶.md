# 2026-09-01 G6 步 11：買家帳戶線

## 已完成的工作 (Done)

- passwordless OTP 全流程（74 §7 形）：customer_otps/customer_sessions 表＋
  四防線（attempts/cooldown/expiry/consumed）＋自動建 profile（normalize_email
  同定義點）＋365 天 session（limits customer.session_days）。
- OTP 信接步 6 通知鏈（catalog kind customer_otp＋自寫模板＋DeliverJob 分支）。
- /account 家族（非主題化）：訂單史/地址簿唯讀/login/verify/logout；
  結帳 Sign in 死鏈收口＋登入態預填（email 空才填＋customer_id＋預設地址）；
  runtime `customer` 顯式 nil stub（頁快取正確形）。
- 突變輪 7/7 殺＋canary：MO5 兩輪教訓——T6 的 logout 刪列遮蔽過期檢查、
  T6b 的 travel 讓 rack-test cookie 先過期再遮蔽一次 ⇒ model 級 T6c 直測
  （遮蔽鏈要逐層剝——mutation-not-red-triage 的新變種：**雙防線互遮**）。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260901110000_create_customer_sessions_and_otps.rb`＋schema
- 新：`app/models/{customer_session,customer_otp}.rb`、
  `app/controllers/storefront/accounts_controller.rb`、
  `app/controllers/concerns/storefront/customer_auth.rb`、
  `config/notification_templates/customer_otp.liquid`
- 改：routes（/account 六路由）、limits（customer 四鍵——OTP 三值 ours 保守，
  官方未取得誠實標註）、notifications catalog/DeliverJob、
  checkouts_controller（include CustomerAuth＋show 預填）、runtime.rb（customer
  nil stub）、通知設定兩頁 kind 標籤＋i18n ×5（+2 鍵）
- 測試：`spec/requests/storefront_account_spec.rb`（10 例）

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.57：主題頁 customer 注入（快取鍵分票）／storefront 地址 CRUD／
  Shop·社群·passkey·OIDC 登入形／OTP code 過境 queue arguments 風險面／
  sessions purge job（過期列不刪＝留痕，量大再清）。
- 生產煙測待部署後（發碼→信渲染→驗碼→session→/account→清理）。
- SMTP 未配置照舊 ⇒ 生產 OTP 信只渲染不外寄——**買家帳戶線在 SMTP 接上前
  實際不可登入**（demo 紅線的已知後果；使用者給 SMTP 四鍵即解）。
