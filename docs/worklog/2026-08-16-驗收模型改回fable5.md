# 2026-08-16 — 驗收模型改回 Fable 5（使用者裁定⑤）

## 已完成的工作 (Done)

`claude-review.yml` 的 `--model` 由 `claude-opus-5` 改回 `claude-fable-5`
（使用者 2026-08-16 指示；`--effort max` 原本就是現值，未動）。

裁定沿革補第 ⑤ 條，並把 ④（改回 Opus）的根因寫清楚：當時是 Fable 5 對本帳號
**中途變成不可用**（2026-08-15 09:30–10:14），不是模型名錯誤；
③ 的理由（Fable 5 ＝官方標示 most capable）從未被推翻。

🔴 風險與偵測寫進註釋：若 Fable 5 再次不可用，症狀是 `num_turns:1`／零花費／秒殺；
失敗留言（`show_full_output` ＋ ERRTEXT）現在會貼出錯誤原文——看到即照 ④ 先例換回。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `.github/workflows/claude-review.yml` | `--model claude-opus-5` → `claude-fable-5`；裁定沿革 ④ 補根因、新增 ⑤ 與復發偵測指引 |

自測：`YAML.load_file` 通過；`ruby scripts/check-doc-claims.rb` 通過；
`ruby scripts/check-workflow-syntax.rb` 不在 main（PR #42），本機不可跑——
**該閘門合併後會在 #42 的分支上驗到本檔**。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **本 PR 改 `claude-review.yml` ⇒ 反竄改會跳過自己的 Claude 驗收**（設計如此），
   只有 Codex 一個驗收方＋CI。實效（Fable 5 能不能跑）要等合併後第一個 PR 的驗收。
2. ⚠️ 判準：合併後第一輪驗收若出現 `num_turns:1`＋零花費，即 Fable 5 仍不可用，
   照 ④ 先例回退——**不要重複 2026-08-15 那次 11 小時的排查**，失敗留言會有原文。
