# P-8 證據來源與合併後文件債收斂

## 已完成的工作 (Done)

- 以 PR #62 exact-head Codex review `4983737311` 的 inline `3822393091` 為輸入，保留既有
  worklog 原文並追加日期更正；把 pre-squash commit
  `2ed2403d06eb50bba0f82e74fcacf44643a81bd8` 錨定到 PR #61、2026-08-20 與可重跑的
  `pulls/61/commits` API 查法，不再要求後繼者依賴 main clone 的物件可達性。
- 以同一 review 的 inline `3822393104` 為輸入，查核 GitHub CLI 官方 `gh api` 分頁契約後，
  明定三個 paginated REST 集合負責留言正文全集，GraphQL `reviewThreads` 只負責
  `isResolved`／`isOutdated` 狀態與首則 inline ID 對應；舊命令保留為歷史，緊鄰追加修正命令。
- 依 `git log -p` 與 GitHub PR metadata 查得 PR #62 已於 2026-08-20 合併，merge commit 為
  `0fbe520502588b34f9b9cad6ae9b3a282d4db643`；將 `docs/specs/91-pit-register.md` 附錄 A.1
  的過渡期說明更新為 main 終態，沒有改寫當時為真的 PR #63 歷史 worklog。
- 已通讀本 worklog；輸入皆是已點名問題，沒有發現需另轉入 `docs/specs/91-pit-register.md` §3
  的範圍外同型問題。本檔已在同一變更列入附錄 A.1。

<!-- 🔴 2026-08-21 更正（PR #64 Claude comment `5358544615`、Codex inline
`3823620181`）：上一句的「沒有發現」只代表當時被點名檔內的反查結果，不能證明全樹閉合。
後續語義複驗同時證明 PR62 第二輪所稱「完整重取」修飾的是有 `$endCursor`／`pageInfo` 的外層
`reviewThreads`，沒有把巢狀 `comments(first:100)` 稱為正文全集；先前暫登的 §3 債因此已撤回。
官方契約與證據邊界集中於 `docs/dev/external-facts.md` A9／A10。 -->

### 鐵律 20.3 重犯類型複驗

- **適用類型**：20.2.1（exact-head review／GitHub 集合取證）、20.2.2（歷史更正與終態索引
  同步）、20.2.3（SHA／日期／完成性聲明）、20.2.7（Markdown comment 與 code fence）。
  20.2.4～20.2.6 不適用：本包不修改流程狀態機、checker、workflow、scripts 或 CI 判準。
- **固定處理**：先以 PR metadata 及三個 REST 集合重取 review 與合併終態，再用 `git log -p`
  判斷歷史層／終態層；原句不靜默改寫，緊鄰追加有日期更正。正文與 thread 狀態分源後，用
  識別字反查舊 query；敏感與凍結路徑另取 staged name-only 差集；Markdown 送 GitHub GFM
  renderer 驗證 comment 不出現在 HTML、fence 仍產生 code block。

<!-- 🔴 2026-08-21 射程更正（PR #64 Claude comment `5358544615`）：上句「用識別字反查
舊 query」在 2026-08-20 staged patch 的實跑射程只到被點名的
`docs/worklog/2026-08-20-PR62首輪驗收修復.md`，不是全樹影響面閉合；不得用它證明其他
worklog 無同型副本。後續全樹字面掃描只用來建立候選集合，是否矛盾仍須按
`docs/dev/external-facts.md` A9 的外層連線／正文邊界逐句判讀。 -->
- **反向複驗輸出（2026-08-20 staged patch）**：
  `git -c core.quotepath=false diff --cached --name-only origin/main -- .github/workflows scripts config/ci.rb CLAUDE.md AGENTS.md docs/handoff`
  無路徑輸出；`rg -n -F 'comments(first:' docs/worklog/2026-08-20-PR62首輪驗收修復.md`
  只取得歷史命令的 `comments(first:100)`、緊鄰更正對它的否定說明，以及狀態命令的
  `comments(first:1)`；`git diff --cached --check` exit 0。兩份含更正的 worklog 經 GitHub
  `/markdown` API 以 GFM 模式渲染，輸出為 `PR62_CODE_BLOCKS=2`、
  `PR62_CORRECTION_COMMENT_VISIBLE=False`、`HANDOFF_CORRECTION_COMMENT_VISIBLE=False`、
  `PR62_STATUS_QUERY_VISIBLE=True`；此為 2026-08-20 staged patch 快照，不外推到後續 head。

<!-- 🔴 2026-08-21 複驗邊界更正（PR #64 Claude comment `5358544615`）：本列的 `rg`
命令帶單一檔案路徑，只能證明該檔三個命中及其更正相鄰，不能證明 `docs/` 其他檔案沒有
同義候選，更不能只靠字面命中判定候選與外部契約矛盾。 -->
- **20.4 復發記錄**：第一次完整 runner 的 workflow syntax 與其 regression gate 分別 exit 1；
  診斷逐項皆是 `/bin/bash: C:/Users/pisce/AppData/Local/Temp/wf-run-...: No such file or directory`。
  既有防線失效不是 checker 漏規則，而是本輪 runner 只在直接 shell gate 指定 Git Bash，漏做
  「把 Git Bash 目錄放在子程序 PATH 首位」的 Windows 固定步驟，讓 Ruby checker 呼叫的裸
  `bash` 解析到 `C:\Windows\system32\bash.exe`。反向複驗：`Get-Command bash -All` 的修正前
  首項為該 System32 路徑；加入 `C:\Program Files\Git\bin` 到 PATH 首位後，`Get-Command bash`
  輸出 `C:\Program Files\Git\bin\bash.exe`，`bash --version` 輸出 GNU bash 5.3.15。修正後必須
  從第一支完整重跑，不以補跑兩支冒充全綠。

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-handoff工作單位節奏與本地保存更正.md`：追加 PR #61 pre-squash
  commit 的可取回來源與日期錨。
- `docs/worklog/2026-08-20-PR62首輪驗收修復.md`：追加 REST 正文全集／GraphQL thread 狀態的
  邊界、官方分頁原文與修正後查詢。
- `docs/specs/91-pit-register.md`：把 PR #63 worklog 索引的 D36 過渡敘述更新為 PR #62
  已合併終態，並索引本 worklog。
- `docs/worklog/2026-08-20-P8證據來源與合併後文件債收斂.md`：記錄本一般 PR 的輸入、證據、
  射程與後續 P-8 證據責任。

<!-- 🔴 2026-08-21 終態更正（PR #64 Claude comments `5358544615`／`5359209200`／
`5360279873`、Codex reviews `4985307122`／`4985973166`／`4986192842`）：後續驗收修復曾修改
本檔的射程敘述與更正格式、`docs/specs/91-pit-register.md` 的 §3／A.1，以及
`docs/dev/external-facts.md` 的 A9／A10；本段不宣稱 PR 累積檔案全集，也不再手抄驗收輪次。
驗收 worklog 集合定義為 preserved main base `0fbe520502588b34f9b9cad6ae9b3a282d4db643`
到 HEAD 的 diff 中，檔名符合 `PR64.*驗收修復.md` 的子集；A.1 集合定義為 A.1 到 A.2 區段內
符合相同檔名的子集。集合相等與驗收輪次檔名的手抄回歸斷言，以
`git grep -l -F '#### 現行可重跑反向複驗（2026-08-21 第六輪更正）' HEAD -- docs/worklog`
定位的 fenced block 為唯一入口。 -->

## 尚未完成或需注意的風險 (Pending / TODO)

- 2026-08-20 在 Git Bash PATH 修正後，上一個 staged snapshot 的完整 runner 輸出
  `GATES_TOTAL=29`、`GATES_PASSED=29`、`GATES_FAILED=0`；Node v24.19.0 仍被 pnpm 明示超出
  `>=22.12 <23` engine 範圍，因此該綠燈不構成 Node 24 支援聲明。本 worklog 隨後又加入這段
  記錄，故該 snapshot 不外推成最終 commit 證據，提交前仍須從第一支重跑。
- 尚未取得 commit 後 doc-claims、GitHub current-head CI、Codex、Claude Opus 5 與評估器
  `1111` 證據；取得前只能稱 P-8 evidence-source 候選，不得宣稱四條件齊或 P-8 單元二完成。
- 本 PR 合併並固定 source head 後，仍須另開 pure-doc evidence-tail PR，只引用本 PR 的 run URL、
  current-head 驗收與 `1111` 留言；不得把 evidence-tail 自身結果寫成自己的合併前證據。
