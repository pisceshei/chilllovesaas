# 2026-09-01 Ella 修復 PR-2：真圖鏈

## 已完成的工作 (Done)

- 六軸診斷 image 軸收口（0 <img> 根因 A＋B）：
  ①**買家面媒體端點** `GET /media/:id/:filename`（查找憑 shop_id＋id——租戶
  隔離 M1/ME2-1 紅證；Cache-Control public 1 天；binread 位元組完整）——此前
  買家面**沒有任何**媒體路由（唯一 blob 端點在 /admin 且需授權）；
  ②**ImageDrop#url 接端點**（原：探測不存在的 LocalDisk.url_for ⇒ 恆 nil＝
  fail-open 靜默降級——M2/ME2-2 紅證）＋新 FileImageDrop（直包 StoredFile，
  settings 鏈用）；
  ③**settings image_picker 解引用**：非空值解析 `shopify://shopify/files/{name}`
  ／裸檔名 → StoredFile（Runtime#resolve_settings_file，經 Liquid context
  register 注入——四個 SettingsDrop 建構位零改動全覆蓋）；命中真圖、未命中
  佔位（M3/ME2-3 紅證）；🔴 **空值維持 nil＝官方契約**（除零軸取證：主題
  `!= blank` gate 靠它；ME2-4 紅證翻轉會被抓）；
  ④image_url/image_tag nil 防線（半殘鏈不出 `<img src="">`）；
  ⑤FileSource binread＋合法 UTF-8 才標記（Windows 文字模式譯壞二進位；
  純 binread 又會讓 Liquid 模板 BINARY 撞中文插值——雙全形）。
- M5 端到端紅證：section 設定帶真檔值 ⇒ 頁面出 `<img src="/media/{id}/...">`。
- 突變 4/4 殺（ME2-1 租戶／ME2-2 url 回 nil／ME2-3 resolver 斷線／ME2-4 空值
  翻佔位）。⚠ ME2-2 首次施加錨未命中（assert 擋下、綠無效）——重做以
  count==1 驗錨後真紅；突變施加一律 assert 錨存在。

## 修改的檔案與核心邏輯 (Changes)

- 新：media_url.rb、storefront/media_controller.rb、
  storefront_media_chain_spec.rb（M1-M5）。
- 改：routes（/media/:id/:filename）、drops.rb（ImageDrop#url／FileImageDrop／
  image_picker coerce＋resolve_settings_image）、runtime.rb
  （resolve_settings_file）、filters.rb（nil 防線）、file_source.rb（binread 雙全）。

## 尚未完成或需注意的風險 (Pending / TODO)

- URL 形 `/media/{id}/{filename}`＝ours（本尊 CDN 形）；derivatives 縮圖
  variant 參數未接（image_url width 仍原圖——coverage 軸 finding，隨 PR-4）。
- demo 內容種子（系列＋商品掛系列＋settings 圖真值＋檔案走 ProcessFile 補
  width/height）＝PR-2b 資料工——沒有它首頁仍是佔位 SVG（空店的「正確」輸出）。
- 生產煙測：部署後 /media 端點打真檔＋前台商品頁出真 <img>。
