# 2026-09-01 G4 步 16d：schema 驅動控件對映

## 已完成的工作 (Done)

- `Theme.sectionSchemas`：全部區段（🔴 不過濾 presets——樹上選中的 main-* 也要
  控件）的 settings 定義，含 id/type/label/info/content/default/placeholder/
  min/max/step/unit/options/limit 子集。
- `ThemeEngine::SchemaLocale`：schema 內 `t:` 鍵解析（26 §4.6：editor 翻譯檔＝
  `locales/*.default.schema.json`，與 storefront locales 兩層不混用）。缺檔缺鍵
  回原鍵（顯示層 fail-open）。sectionCatalog 名稱同管線（生產實錘：Ella 40 個
  preset 區段 name 全是 `t:names.*`，16c picker 會顯示原鍵——本包收口）。
- 設定面板改 schema 驅動（26 §5 對映）：checkbox/number/range（unit 顯示）/
  select/text_alignment（固定三值）/radio/textarea 族（richtext/html/liquid 先
  textarea）/color（hex→color picker，非 hex 退 text）/text 族（url/video_url/
  color_background/font_picker）；header/paragraph 結構元素；資源選擇器
  （image_picker/product/…）唯讀＝16e 射程。🔴 值解析＝實例值 ?? default
  （default 只補顯示、不物化落庫——ED8 斷言 spacing 不進 payload）；
  schema 未覆蓋鍵與缺 schema 型退回 16b typeof 分派（向後相容，ED1-7 不動）。
- 突變 3/3 殺（MD1 t: 解析／MD2 default 補位／MD3 schema 驅動整段）。

## 修改的檔案與核心邏輯 (Changes)

- 新：`app/liquid/theme_engine/schema_locale.rb`、
  `spec/fixtures/theme_engine/minimal-1.0/locales/en.default.schema.json`。
- 改：`theme_type.rb`（sectionSchemas field＋each_section_schema 共用枚舉＋
  catalog 接 t: 解析）、`ThemeEditorPage.tsx`（SettingControl/FallbackControl
  雙元件）、promo.liquid fixture（name/label 改 t: 鍵）、editor 測試 7→8 例
  （ED8）、bootstrap spec 4→5 例（E5）、i18n 3 鍵 ×5、admin.css。

## 尚未完成或需注意的風險 (Pending / TODO)

- 16d2（下一包）＝佈景設定寫回：settings_schema.json 分組面板＋ThemeSetting
  current 值＋themeSettingsUpsert（🔴 同樣必 touch theme）。
- 資源選擇器控件（image_picker/product/collection/*_list/metaobject）＝16e；
  visible_if 條件顯示未評估；font_picker 先 text（字型庫 handle 下拉可後補）；
  schema locale 只取 `*.default.schema.json`（多語系挑選後補）——四項登記 91 §3。
- 生產煙測：部署後選 Ella 區段看 schema 控件（range/select/checkbox）與
  picker 中文名。
