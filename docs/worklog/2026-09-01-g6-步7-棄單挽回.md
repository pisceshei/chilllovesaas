# 2026-09-01 G6 步 7：棄單挽回

## 已完成的工作 (Done)

- 89 §8 入庫（admin 清單七欄＋詳情頁＋本尊挽回 URL 形實測；V-237 登記）。
- 判定：MarkAbandonedJob（官方 10 分鐘規則→limits `checkout.abandoned_after_minutes`；
  recurring 每 5 分鐘；集合式 UPDATE 冪等）。
- 挽回連結：RecoveryUrl＋`GET /checkouts/recover/:recovery_token`
  （活單回結帳頁／已成單去 thank-you／查無 404）。
- 寄送：abandonedCheckoutSendRecovery（三前置：已標棄單/有 email/未成單）→
  步 6 DeliverJob 新 abandoned_checkout 分支 → 寄出回填 recovery_email_sent_at。
- 讀面：abandonedCheckouts keyset connection（KeysetCursor 登記 abandoned_at 鍵）
  ＋AbandonedCheckoutType（官方 abandonedCheckoutUrl 同名）。
- admin：/admin/orders/abandoned 佔位轉正（七欄＋徽章＋每列寄送鈕）＋i18n ×5（+19 鍵）。
- 突變輪 7/7 殺＋canary：MA1 email 前置／MA2 閾值／MA3 成單分流／MA4 判定前置／
  MA5 成單擋寄／MA6 sent_at 回填／MA7 連結 token。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260901060000_add_recovery_email_sent_at_to_checkouts.rb`＋schema
- 新：`app/jobs/checkouts/mark_abandoned_job.rb`、
  `app/services/notifications/recovery_url.rb`、
  `app/graphql/types/abandoned_checkout{,_edge,_connection}_type.rb`＋errors 兩檔、
  `app/graphql/mutations/abandoned_checkout_send_recovery.rb`、
  `app/frontend/admin/pages/AbandonedCheckoutsPage.tsx`（＋test）
- 改：`app/jobs/notifications/deliver_job.rb`（abandoned 分支＋回填）、
  `app/controllers/storefront/checkouts_controller.rb`（recover 端點）、
  `config/routes.rb`（🔴 recover 在 :token 前）、`config/limits.yml`、
  `config/recurring.yml`、`app/graphql/products/keyset_cursor.rb`（abandoned_at 鍵）、
  query/mutation type、`App.tsx`、i18n ×5
- 文檔：89 §8＋`docs/dev/g6-abandoned-checkouts.md`

## 尚未完成或需注意的風險 (Pending / TODO)

- 自動排程寄送（Send after）＝⚪ 後置（本尊已遷 Messaging automation；89 §6）。
- 詳情頁（挽回 URL 拷貝框／Notes 卡）＋Export ⚪ 後置；V-237（未寄態的寄送控件形）
  待本尊複測。
- recovered 不追連結歸因（91 §3.51 登記）；SMTP 未配置照舊（步 6 Pending 首條）。
- 官方六種抑制情形（付款錯誤/不可運送/只留電話/無可購商品/全免費/已完成）v1 只
  實作「已完成」與「無 email」兩種（其餘依賴的資料面尚未到位；91 §3.51）。
