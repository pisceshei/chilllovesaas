# 2026-08-15 — 對等性檢查器補上反向證明（並當場抓到兩個活 bug）

> 承接 `2026-08-15-CI對等性機制化.md`（PR #39）。
> 來源：多代理驗收（2026-08-15）指出 `check-ci-parity.rb` 是同批新增檢查器中
> **唯一沒有回歸測試**的一支。

---

## 已完成的工作 (Done)

### 1. 先問「為什麼只有它沒有測試」——答案是它**寫不了**測試

<!-- 🔴 2026-08-15 兩度更正（PR #39 第 3、4 輪驗收）。本段初稿逐字是
     「同批的 check-limits-keys.rb／check-money-boundary.rb／**check-exec-bits.sh／
     check-workflow-syntax.rb** 都有回歸測試，只有 check-ci-parity.rb 沒有」。
     ①第 3 輪：後兩支**根本不在樹上**（分別在未合併的 PR #41／#42）。
     ②第 4 輪：換上去的新說法「已合併的檢查器都各有一支反向證明」
       **本身又是不成立的全稱句**——實際有三支沒有。
     🔴 兩次都是同一個病：**沒有回頭核對剩下那句斷言**。
     第 3 輪只換掉虛構的檔名，卻讓一個更大的全稱句留在原位。 -->

<!-- 🔴 2026-08-16 第三次更正（PR #39 第 5 輪）：本行原寫「已合併的檢查器共 **7 支**」，
     實際 **6 支**——第 7 支 `check-ci-parity.rb` 是本 PR 自己新增的，不算「已合併」。
     同一句的數字已錯三次，每次都是「修數字」的那一手寫錯
     ⇒ 從此不寫死支數，`git ls-files scripts/` 為唯一事實。 -->
本倉庫已合併的檢查器（`git ls-files scripts/` 可列全）中，**只有三組**配了反向證明：
`check-limits-keys.rb` → `test-limits-key-rules.rb`、
`check-money-boundary.rb` → `test-money-rules.rb`、
`lint-prototype.py` → `test-lint-rules.py`。
🔴 **另外三支至今零反向證明**：`check-tenant-isolation.rb`／`check-reversal-naming.rb`／
`check-baseline-raise.py`（實跑 `git ls-files scripts/` ＋ grep 確認，沒有任何 `test-*` 配對）。
`check-ci-parity.rb` 是本 PR 才新增的，之前也沒有反向證明（現在有了）。

⇒ 「只有它沒有」這句話**本來就不成立**，真正的理由不是「別人都有」，
而是 **`docs/specs/65` §K.7 要求檢查器要有反向證明**，而這一支**寫不出來**：
它把 `ROOT` **寫死**成倉庫根目錄

```ruby
ROOT = File.expand_path("..", __dir__)
```

⇒ **沒有辦法**餵給它一個故意違反的 fixture。想寫測試的人會發現寫不了，然後就不寫了。

🔴 **教訓：可測性是檢查器的設計需求，不是寫完之後才想的事。**
一支只能對真倉庫跑的檢查器，等於宣告「我永遠不會被反向證明」。
修法＝接受 `ARGV[0]` 當 ROOT，形態比照另外兩支 `check-*.rb`（三行改動）。

### 2. 🔴 加上 ROOT 參數後第一件事就是拿 fixture 打它——**兩個都沒打紅**

| fixture | 內容 | 修前 |
|---|---|---|
| `parity_runner_collapse` | ci.yml 跑 `npm audit`，ci.rb 只有 `pnpm audit` | **exit 0** 🔴 |
| `parity_name_spoof` | `step "TODO: 之後要加 ruby scripts/check-limits-keys.rb", "true"` | **exit 0** 🔴 |

兩個都是活的 bug。**這支測試不是補文件，它在被寫出來的當天就抓到兩個缺陷。**

#### bug ①：兩側用兩套邏輯，而 `"pnpm audit".include?("npm")` ＝ true

ci.yml 那側算出識別字 `key`，ci.rb 那側卻是**另一套**——去「找找看」有沒有哪一行像：

```ruby
ci_rb_commands.lines.any? do |line|
  next false unless line.include?(runner)                                    # ← 子字串
  arg.nil? || line.match?(/#{runner}\s+(?:-\S+\s+)*#{arg}\b/)                # ← 備援正則
end
```

`npm` 是 `pnpm` 的子字串，兩道防線**同時**失效：正則 `/npm\s+(?:-\S+\s+)*audit\b/`
也命中 `pnpm audit` 的後半段。同型還有 `bundle` 命中 `bundler-audit`。

**修法**：抽出 `command_key` lambda，**兩側都用它算 key，再做集合差**。
這樣「寬鬆」不可能只發生在一側——不對稱本身就是這個 bug 的根因。

#### bug ②：`step` 的名稱與指令在同一行，掃整行就等於讓名稱也算數

`step` 的簽名是 `step "名稱", "指令"`。舊寫法把**整行**丟給 `SCRIPT_REF` 掃 ⇒
腳本名寫在**名稱**裡就算「有跑」。

⚠️ 這不是假想的攻擊，是**現存的形態**：ci.rb 今天就有一步叫
`step "Invariants: CI parity (ci.yml ⊆ config/ci.rb)", "..."`，名稱裡真的帶著檔案路徑。
今天沒中只是因為它不含 `scripts/`。

**修法**：`STEP_CALL` 正則只取**第二個引號字串**。
配套兩條 fail-closed：解析不出兩段引號的 `step` 行**報錯**（不得靜默跳過，
靜默跳過＝那一步從比對中消失而症狀是「通過」）；`step` 行掃到 **0 行**也報錯。

### 3. 新增 `scripts/test-ci-parity-rules.rb`（寫下時的快照＝10 條 / 9 個 fixture；現值以 `ruby scripts/test-ci-parity-rules.rb` 實跑為準）

| fixture | 期望 | 守什麼 |
|---|---|---|
| `parity_clean` | exit 0 | 反向斷言；**另加一條斷言「認出 2 組指令」**——只斷言 exit 0 的話，把 `KNOWN_RUNNERS` 清空仍全綠 |
| `parity_missing_script` | exit 1 | 規則 1 差集 |
| `parity_runner_collapse` | exit 1 | 🔴 bug ①（token 邊界） |
| `parity_name_spoof` | exit 1 | 🔴 bug ②（名稱冒充） |
| `parity_guard_disabled` | exit 1 | 規則 3 自我約束 |
| `parity_no_quality` | exit 1 | quality job 解析不到 ⇒ 來源側空 ⇒ 差集恆空 ⇒ 永遠通過 |
| `parity_unparsed_step` | exit 1 | `step CHECKS[:parity]` 這種寫法 fail-closed |
| `parity_no_steps` | exit 1 | canary：ci.rb 一行 `step` 都不剩 |
| `parity_comment_only` | exit 0 | 整行註釋不得算「有執行」；斷言用**腳本計數**不是 exit 0 |

並掛進 `ci.yml` quality job 與 `config/ci.rb`（否則對等性檢查自己會紅——它守的就是這件事）。

### 4. 突變測試 8/8

| 突變 | 結果 |
|---|---|
| P1 退回子字串比對 | 被抓 ✅ |
| P2 退回掃整行 | 被抓 ✅ |
| P3 `KNOWN_RUNNERS` 清空 | 被抓 ✅ |
| P4 規則 3 自我約束拿掉 | 被抓 ✅ |
| P5 quality 空陣列改成靜默放行 | 被抓 ✅ |
| P6 unparsed step 改成靜默跳過 | 被抓 ✅ |
| P7 註釋剝除拿掉 | 被抓 ✅ |
| P8 規則 1 差集恆空 | 被抓 ✅ |
| 對照組（未突變） | 通過 ✅ |

🔴 **P5 一開始報「存活」，那是假的**：我用 `perl -0pi -e 's/…\n  exit 1\nend/…/s'` 做突變，
而這個檔是 **CRLF** 行尾 ⇒ `\n` 前面還有 `\r`，正則**根本沒匹配**，
檔案原封不動，測試當然通過。改用 `sed` 重做即被抓到。
⚠️ **差一點就把一個不存在的缺口寫進 Pending。**
教訓：**突變測試必須先確認「突變真的發生了」**——
「改壞了還通過」與「根本沒改壞」在結果欄裡長得一模一樣。

### 5. 修掉四處過時的「八步」與一句會誘導出有害修法的錯誤註釋

**「八步」**：`config/ci.rb` 前面那批通用步驟**建檔時是 8 步、現在是 9 步**
（`50a2745` 起算）。四處敘述仍寫「前八步」。
⇒ 改成不寫死步數的描述（「bin/setup／rubocop／audit／rspec／pnpm 等」），
只在明確標為歷史的地方保留數字並註明日期與 commit。

**🔴 `config/ci.rb:25` 的「＋ schema drift」是錯的，而且錯得有害**：

`Verify schema.rb has no drift`（`git diff --exit-code db/schema.rb`）在 **`test` job**
（`ci.yml:100`），**不在 quality job**；`services: mysql` 也只掛在 test job 上。

照原文理解的人會推論「parity 檢查應該也涵蓋 schema drift」，
於是往 quality job 加一支 `check-schema-drift.rb`——**而 quality job 連不上資料庫**，
那支腳本的「連不上就 exit 0」分支會**每次都命中** ⇒ 得到一個
**結構上不可能失敗的步驟**，比沒有這一步更糟：它看起來像有在守。

⇒ 刪掉那三個字，並把「為什麼不該補」寫進註釋，免得下一個人再推導一次。

---

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `scripts/check-ci-parity.rb` | ①`ROOT` 吃 `ARGV[0]`（可測性）②抽出 `command_key` lambda，**兩側共用**（修 bug ①）③`STEP_CALL` 只取第二個引號字串（修 bug ②）④新增兩條 fail-closed：`step` 解析不出兩段引號／`step` 行 0 行 ⑤「前八步」更正 |
| `scripts/test-ci-parity-rules.rb` | **新增**；條數不寫死（10/9 → 13/12，實跑 `ruby scripts/test-ci-parity-rules.rb` 為準） |
| `spec/fixtures/ci_violations/parity_*` | **新增 9 份** |
| `.github/workflows/ci.yml` | quality job 加 `Check CI parity rules regression` |
| `config/ci.rb` | 加對應 step；刪掉錯誤的「＋ schema drift」並寫明為什麼不該補；「八步」更正 |
| `docs/handoff/2026-08-15-CI對等性機制化.md`、`docs/worklog/2026-08-15-CI對等性機制化.md`、`docs/worklog/2026-08-15-金額CI執法.md` | 「八步」更正 |

**自測**：全閘門綠（check-ci-parity／check-limits-keys／check-money-boundary／
check-reversal-naming／check-tenant-isolation／test-ci-parity-rules 10 條／
test-limits-key-rules／test-money-rules／lint-prototype ERROR 0／test-lint-rules 23 項）；
`ci.yml` YAML 解析通過；突變 8/8。

---

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **`command_key` 的取樣面仍然很窄，而且這是刻意的。**
   只認 `KNOWN_RUNNERS`（pnpm／npm／bundle／rake）與 `bin/*` 開頭的行；
   `node scripts/x.js`、`docker run …`、`make …` 一律看不到。
   ⚠️ 症狀與這次修掉的一樣：**檢查通過**。加新形態的執行器時要記得同步這張表，
   而**沒有任何機制提醒**（`parity_clean` 的「認出 2 組」斷言只守住既有的，不守新增的）。

2. ⚠️ **`STEP_CALL` 只認雙引號**。`step 'name', 'cmd'`（單引號）會落進 fail-closed 分支報錯——
   這是安全的方向（寧可紅），但錯誤訊息會讓人以為是自己寫錯而不是檢查器不支援。
   要支援單引號就擴充正則，別把 fail-closed 拿掉。

3. ⚠️ **規則 2 仍不看參數**。`pnpm audit --audit-level high` 與 `pnpm audit` 的 key 都是
   `pnpm audit` ⇒ ci.yml 收緊門檻而 ci.rb 沒跟上，本檢查看不到。
   這是檔頭誠實聲明既有的取捨（參數本來就可以不同），不是本輪引入的，但**沒有被測試涵蓋**。

4. ⚠️ **inline shell 步驟仍不在檢查範圍**（檔頭誠實聲明第 1 條）。
   🔴 該條文寫著「正解是把它抽成 `scripts/check-exec-bits.sh`」——**那件事在 PR #41 已經做了**。
   ⇒ #41 合併後，檔頭那段與 `config/ci.rb` 的對應註釋**必須一起更新**，
   否則會留下一句「已知缺口」指著一個已經補好的洞。**這是 #41 的前置條件，不是本 PR 的。**

5. 🔴 **新增腳本忘了執行位元，被 CI 擋下（run #133，39s 紅）。**
   `scripts/test-ci-parity-rules.rb` 進版控時是 `100644`，執行位元閘門直接打紅。
   根因是本機 Windows 的 `core.filemode=false` ⇒ **本地怎麼跑都不會發現**。
   修法 `git update-index --chmod=+x`，並順手稽核 `bin/` `scripts/` 全部為 100755。
   ✅ **閘門運作正常——這正是它存在的理由**，登記在這裡是為了讓下一個在 Windows 上
   新增腳本的人先做這一步，而不是等 CI 紅了才知道。

6. ⚠️ **`parity_*` fixture 的 ci.yml 都是手寫的最小 workflow**，
   與真實 `ci.yml`（含 anchors、多行 `run: |`、matrix）差距不小。
   本輪只覆蓋到「解析得動的簡單形態」，真實檔的複雜寫法沒有 fixture 對應。

---

## 驗收後修正（PR #39 第 3 輪）

### 🔴 1. 論證的前提裡有一半是不存在的檔案——**而且我今天才剛在 #40 修過同一種錯**

`scripts/test-ci-parity-rules.rb:8-10` 初稿逐字：

> 同批新增的檢查器中唯一沒有的一支——`check-limits-keys.rb`、`check-money-boundary.rb`、
> **`check-exec-bits.sh`、`check-workflow-syntax.rb`** 都有。
> （後兩支分別在 PR #41／#42，送審當時尚未合併——本引文就是那個錯誤本身。）

實查 `git ls-files scripts/`（12 檔）：**後兩支根本不在樹上**——
`check-exec-bits.sh` 在 PR #41、`check-workflow-syntax.rb` 在 PR #42，兩者當時都還 OPEN。

⇒ 四支裡只有兩支成立，而這句話是整段敘事的**論證前提**
（同批都有 → 只有它沒有 → 一定有結構性原因 → `ROOT` 寫死）。**前提虛構了一半。**

🔴 更難看的是它與**同一份 PR** 的誠實聲明直接牴觸：
`check-ci-parity.rb` 的「不檢查什麼」第 1 條當時還寫著
「正解是把它抽成 `scripts/check-exec-bits.sh`」——**用未來式講同一個檔**。
同一份 PR 裡，一處當它不存在、一處當它已存在。

🔴🔴 **這與我今天稍早在 PR #40 修掉的是同一種錯**（那邊是 worklog／handoff 用現在式
斷言 PR #39 的 `check-ci-parity.rb`）。修完別人那一份的幾小時後，
我在**這一份**又寫了一次。⇒ 「引用前先打開來看一眼」顯然還沒變成反射動作，
它現在是本批工作內第四次指錯出處。

**修法**：清單只留**實際存在**的三組（`check-limits-keys.rb`→`test-limits-key-rules.rb`、
`check-money-boundary.rb`→`test-money-rules.rb`、`lint-prototype.py`→`test-lint-rules.py`），
另外兩支改成明確的「PR #41／#42 進行中，尚未合併」＋一句
「合併後請回來把它們加進清單」。

### 🔴 2. handoff 沒跟上本 PR 後半段的交付

`commit 4a60920` 新增了回歸測試與 9 份 fixture，但那個 commit **只改了 handoff 的「前八步」一行**。於是：

| 位置 | 問題 |
|---|---|
| §④ 第 2 條 | 逐字仍寫「`check-ci-parity.rb` **沒有**配 fixture 回歸測試⋯一併補比較划算」——**已經補好了**。handoff 是接手入口，它在叫下一個人去補一個補好的洞 |
| 抬頭 worklog 清單 | 只列 1 份，實際 2 份。CLAUDE.md 逐字要求「列出本輪產生的**所有** worklog 檔名」 |
| §① 我改了什麼 | 三條裡完全沒有回歸測試、9 份 fixture、兩個活 bug——**那是本 PR 後半段的主要交付** |

🔴 **本 PR 開篇那張表**（「ci.yml 9／ci.rb 6／註釋自稱 5，三個數字互不相同」）
講的就是**敘述沒跟著交付走**。修好了機制，卻在自己的交接文件裡復發同一型。

⇒ §① 補到 6 條、抬頭補第 2 份 worklog、§④ 那條改成「已補上」並寫明
**真正的教訓不是「要補測試」，是「補之前先確認它寫不寫得出來」**。

---

## 本輪新增的 Pending

6. 🔴 **「指出處前先打開來看」仍然只是紀律，沒有任何機制。**
   本批工作內已經第四次指錯出處，其中兩次是**同一天、同一種形態**
   （現在式引用未合併分支的檔）。
   ⇒ 可機制化的最小版本：一支掃 `docs/` 與 `scripts/` 註釋裡的 `scripts/*.{rb,py,sh}`
   路徑、確認它在 `git ls-files` 裡的檢查器。**本輪未做**，
   因為它會誤報所有「刻意提及未來檔案」的段落（本檔就有兩處），
   要先想清楚豁免怎麼標。登記在此，不要當成已解決。

7. 🔴 **三支已合併的檢查器至今零反向證明**（本輪查證時發現，本輪不補）：
   `check-tenant-isolation.rb`／`check-reversal-naming.rb`／`check-baseline-raise.py`。
   實跑 `git ls-files scripts/` ＋ `grep -rn` 確認：沒有任何 `test-*` 配對，
   只命中各自檔頭的用法字串與 `parity_missing_script` fixture 的引用。
   ⇒ **這三支改壞了不會有人知道**，正是 65 §K.7 要防的「看起來像有在守」。
   ⚠️ 其中 `check-tenant-isolation.rb` 守的是**鐵律 2（多租戶隔離）**，
   它靜默失效的後果比本 PR 修的任何一條都嚴重。
   本輪不補（範圍），但**登記成必辦**，不要當成「順帶一提」。

---

## 驗收後修正（PR #39 第 5 輪）

### 🔴 1. `bundle exec X` 全部塌成同一個 key——我只修了塌陷的一半

驗收方實測復現成立：`bundle exec rspec`／`bundle exec rubocop`／`bundle exec erb_lint`
在舊算法下 key 全是 `bundle exec`，而 `config/ci.rb` 本來就有 `bundle exec rspec`
⇒ ci.yml 加**任何** `bundle exec Y` 都被判「已涵蓋」——**這條 false-green 當時就是活的**。
與我修掉的 bug ①（`npm` 命中 `pnpm`）同類：識別字塌陷 ⇒ 假性涵蓋。
共用 `command_key` 讓兩側「一樣寬鬆」，但**沒有讓 key 變得忠實**。

**修法**：`exec` 是**轉接詞**不是目標——命中 `WRAPPER_SUBCOMMANDS`
（bundle/pnpm/npm 的 exec、pnpm dlx，逐項列舉不得口頭擴充）就再吃下一個非旗標 token，
`bundle exec rspec` 整組當 key。fixture＝`parity_exec_collapse`（修前 exit 0、修後點名
`bundle exec erb_lint`）；突變（轉接詞處理拿掉）被抓 ✅。

### 🔴 2. SCRIPT_REF 的兩個擴充分支零覆蓋（第 3／5 輪連續指出）

檔頭用 🔴 宣稱「帶引號含空白／不帶引號含子目錄都要認」，但 9 份 fixture 的路徑
全是扁平無引號 ⇒ 把 SCRIPT_REF 退回舊字元類，10 條照樣全綠。

**修法**：fixture `parity_path_forms`——ci.yml 同時用兩種寫法、ci.rb 只放子目錄那支，
斷言點名帶空白那支。

🔴🔴 **突變驗證時又踩了一次「突變沒套用」的坑**：第一次用 shell sed 做 M-A，
「確認突變套用」用的是 `grep -c 'A-Za-z0-9_'`——**而 :82 的註釋本身就含這個字串**，
sed 實際沒生效、grep 卻回 1，我差點把「存活」當真。
改用 ruby `String#include?` 精確比對整行、失敗即 abort，重做後**被抓 ✅**。
⇒ 教訓升級：**「確認突變套用」的檢查本身也要能失敗**——
檢查的字串必須是「只有突變後才會出現、且不出現在任何註釋裡」的。

### 🔴 3. 規則 3 只護自己，不護自己的反向證明

把 `Check CI parity rules regression` 從 ci.yml 刪掉 ⇒ 規則 1 差集空、規則 3 不看它
⇒ 反向證明被靜默移出 CI。「單向差集擋不住把這一步刪掉」正是規則 3 的立論，升了一層。

**修法**：`self_ref` 改成 `SELF_REFS` 兩支清單；fixture＝`parity_regression_dropped`；
突變（退回單支）被抓 ✅。既有 10 份 fixture 的 ci.yml／ci.rb 同步補回歸測試那步
（規則 1 要求 ci.yml ⊆ ci.rb，兩邊都補），`parity_comment_only` 的計數斷言隨之調整（現值見 `grep "用到" scripts/test-ci-parity-rules.rb`）。

### 4. 「已合併的檢查器共 7 支」（引號內＝被更正的**錯誤原文**）——同一個數字第三次寫錯

實際數字＝當時 `git ls-files scripts/` 的快照（6 支）：所謂第 7 支 `check-ci-parity.rb` 是本 PR 自己新增的，不算「已合併」。
這個數字的更正史：4（虛構兩支）→ 7（多算自己）→ 6。**每次都是「修數字」的那一手寫錯。**
⇒ 三處（本檔、test-ci-parity-rules.rb 檔頭、handoff §④）全部改成
**不寫死支數**，`git ls-files scripts/` 為唯一事實；歷史行加更正註記。
`金額CI執法.md` 同句的「五個」也補了指標註記（歷史層不改，指向三數互斥表）。

### 5. 誠實聲明補第 5 條：複合行

`command_key` 只讀行首 token，`A && B` 後段整段看不見；行內含 `scripts/` 時整行回 nil，
連同行的 `pnpm foo` 一起跳過。目前 ci.yml 無此寫法，但「不檢查什麼」既然自稱契約，
就必須列全。

### 本輪自測

CASES 全綠（當時快照 13 條 / 12 個 fixture，現值以 `ruby scripts/test-ci-parity-rules.rb` 為準）；突變 M-A（SCRIPT_REF 退回）／M-B（SELF_REFS 退回單支）／
M-C（轉接詞拿掉）全部被抓，且每條先以「整行精確比對」確認突變真的套用；
全閘門綠；rubocop no offenses；exec bits 全 100755；ci.yml YAML 解析通過。
PR 描述已在上一輪更新（11 支／已補上／九步），本輪驗收跑在更新前，該條不再適用。

### 本輪新增的 Pending

8. ⚠️ `WRAPPER_SUBCOMMANDS` 是列舉式的：`rake` 沒有轉接詞（正確），但日後若引入
   `yarn`／`npx` 這類新 runner，KNOWN_RUNNERS 與 WRAPPER_SUBCOMMANDS 要一起評估，
   漏了症狀仍是「檢查通過」。
9. ⚠️ 「確認突變套用」目前靠人自律用精確比對。要機制化得把突變清單固化成腳本
   （Pending 1 的老問題），本輪仍未做。

---

## 驗收後修正（PR #39 第 6 輪）

### 🔴 commit 宣稱交付的兩項，實際根本沒進檔——「宣稱交付而未交付」

第 5 輪的 commit message 寫「誠實聲明補第 5 條：複合行」、「金額CI執法的『五個』補指標註記」，
**兩項都沒有落地**。根因：那一輪的批次 python 腳本在第 ① 步 assert 失敗、
**整批未寫入**，我補做時只補了記得的 ①②③，④⑤ 漏了——而 commit message 是照原計畫寫的。

🔴 教訓（比缺陷本身重要）：**批次腳本中途失敗後，「補做」必須逐項對照原清單打勾，
不能靠記憶**。commit message 宣稱的每一項都要在 diff 裡指得出對應行——
這正是本 PR 開篇「三個數字互不相同」的形態：宣稱與交付分岔，而讀者只看得到宣稱。

本輪補齊：①誠實聲明第 5 條（複合行靜默失明）②金額CI執法「五個」註記。
順帶依驗收方指正**收窄第 1 條的全稱句**——「不檢查 inline shell 步驟」過寬，
規則 2 檢查的 `pnpm audit` 本身就是 inline shell；本腳本看不到的是「多行腳本邏輯」。

### 🔴 handoff／worklog 的「10 條 / 9 個 fixture」（引號內＝被更正的**過期快照**）停在第 4 輪

實際已 13 / 12。四處全改成**不寫死條數**（「寫下時 10/9、已長到 13/12，實跑為準」）——
與 #40 的「終態層不寫死數字」同一個裁定。

### 🟡 一併處理

- `parity_comment_only` 的理由欄寫錯（「兩邊抵銷成綠」不成立——`command_lines`
  只作用在 ci.yml 側）：斷言不動，理由改正並留更正註記。
- `RUNNER_EXEMPT` 零 fixture：`parity_clean` 的 ci.yml 加 `pnpm install --frozen-lockfile`
  （ci.rb 沒有）——豁免壞掉該條立刻紅；**突變（豁免清空）實測被抓** ✅。
- `docs/dev/m1-money-unit-boundary.md:118` 補機制指向（紀律 → `check-ci-parity.rb` 機器擋）。
- `config/ci.rb` 的 `pnpm audit` 補離線行為說明（刻意不優雅略過：安全檢查靜默略過比紅燈糟）。

### 本輪未做（登記理由）

- 「腳本數下限斷言」：與 MIN_CASES 同型，但 checker 會被 fixture 以小數字呼叫，
  下限只能對真倉庫生效 ⇒ 需要「fixture 模式 vs 真倉庫模式」的區分，引入的複雜度
  超過它擋的風險（SCRIPT_REF 兩個形態已有 parity_path_forms 守著）。登記不做。

---

## 驗收後修正（PR #39 第 7 輪）

### 🔴 parity_path_forms 的「子目錄」分支零承重——共用解析器的 fixture 陷阱

驗收方用突變後的正則對 fixture 兩側實掃，證明：`scripts/ci/check.rb`（假想路徑，只存在於 fixture 內容裡）在 fixture
**兩側都有**，而 SCRIPT_REF 是兩側共用的 ⇒ 砍掉子目錄支援時它**同時**從兩側消失
⇒ 差集不變 ⇒ 該 CASE 照樣綠。我宣稱「兩個擴充分支終於有斷言在守」，實際只守了一支。

🔴 教訓（一般化）：**共用的解析器，fixture 必須讓目標只出現在一側**——
否則解析器的任何劣化都對稱地作用在兩側、在差集裡互相抵銷。
這與 bug ①（兩側寬鬆度不對稱）恰好是鏡像：那邊是「不對稱造成假綠」，
這邊是「對稱造成零承重」。

修法：fixture 的 ci.rb 刻意**不放**子目錄那支（讓它成為缺項）；CASE 拆成兩條
（空白支、子目錄支各一）。突變（字元類排除 `/`，先驗證套用）**被抓** ✅。
CASES 13 → **14 條 / 12 fixture**。

### 🔴 第 6 輪收窄的全稱句只改了檔頭——印給人看的那行還是舊的

`:266` 每次成功都印「inline shell 步驟不在檢查範圍」，指向的檔頭卻已明說這句過寬。
使用者看得到的是 :266 不是檔頭。ci.yml:283 同句同病。兩處都收窄成
「**多行 inline shell 的腳本邏輯**不在檢查範圍（單行指令由規則 2 涵蓋）」。
與第 4 輪「同一條只修一半」同型——**修全稱句時要 grep 它的所有複本**。

### 🔴 PR 描述「文檔」欄與 diff 相反 ＋ 變更記錄漏補

描述寫「無 docs/dev/ 變更」，而 diff 有 `docs/dev/m1-money-unit-boundary.md | 2 +-`
（第 6 輪補的機制指向）。描述已改；該檔 §變更記錄 補一行（docs/dev/README.md:44 要求）。
