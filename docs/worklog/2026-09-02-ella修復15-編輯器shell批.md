# 2026-09-02 Ella 修復 PR-15：編輯器 shell 批（行動版/badge/快捷鍵/URL 化/移除區段）

## 已完成的工作 (Done)

- 6 軸艦隊編輯器 parity 軸的 ④⑤b⑤d⑤g 四項收口（證據＝24 §1.1 頂列實測）：
- **📱 行動版預覽切換**（④）：頂列切換鈕，iframe 收窄 390px 置中；再按還原。
- **作用中 badge**（⑤g）：published 主題在主題名旁出綠色 pill
  （24 §1.1「`作用中` 綠色 pill 標明編輯的是已發佈主題」）。
- **undo/redo 鍵盤快捷鍵**（⑤g）：Cmd/Ctrl+Z／Cmd/Ctrl+Shift+Z；輸入控件
  內不攔（原生文字 undo 先行）；經 ref 取最新函式、effect 只掛一次。
- **編輯器狀態 URL 化**（⑤d）：選中 section ⇒ `?section={id}`、佈景設定
  ⇒ `?context=theme`（24 §1.1「編輯器狀態可 URL 化——復刻時用 query param
  同步器實現」；本尊 section 值是 `template--{id}__{sectionId}` 實例形，
  我方用 section 鍵——與 §12.3 登記的 id 語義差異同源）。初載還原一次，
  跨帶定位（群組 section 也能還原選中）。
- **設定面板底部「移除區段」**（⑤b）：selected section 面板尾部 critical
  鈕，移除＋清選取＋清 URL param。
- i18n 三鍵 × 5 語（editor.active／mobilePreview／removeSection）。
- 測試 ED16–ED19；突變 2/2 殺（掏空快捷鍵 handler、拔樹點選 URL 同步）。

## 修改的檔案與核心邏輯 (Changes)

- `app/frontend/admin/pages/ThemeEditorPage.tsx`：mobilePreview 狀態＋
  badge＋快捷鍵 effect（undoRef/redoRef）＋syncStateParams＋初載還原＋
  面板底部移除鈕。
- `app/assets/stylesheets/admin.css`：`.cl-editor__badge`。
- `app/frontend/admin/i18n/messages/{en,fr,ja,zh-Hans,zh-Hant}.json` 三鍵。
- `ThemeEditorPage.test.tsx`：ED16–ED19。

## 尚未完成或需注意的風險 (Pending / TODO)

- 軸剩餘未做（登記）：⑤a picker 搜尋/分類/tabs、⑤b custom_css 與面板
  header 動作、⑤c 頁面切換器固定項、⑤e 預覽內 inline ＋插入點/拖曳、
  ⑤f 市場覆寫入口、三顆面板切換器（sections/settings/blocks 現以樹＋
  入口鈕形替代）。
- 本尊行動版預覽寬度未量測（V）——390 取我方 RWD 驗證階的手機寬。
- 分支基底教訓：`gh pr merge` 不動本地 ref——**切新分支前必先
  `git fetch origin main`**，否則從舊 main 開工（本輪 ella/editor-shell
  首切即中，anchors 全失配後發現）。
