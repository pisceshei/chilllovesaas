# 2026-08-24 部署失敗補救（PR #113）

<!-- 🔴 本 worklog 是**事後補寫**（2026-08-24 接手面稽核發現 #113 三件套缺 worklog 與 handoff）。
     內容以當時的 PR body、commit 訊息與部署輸出為據，非事後追憶。 -->

## 已完成的工作 (Done)

PR #112（D46 租戶閘）合併後部署 bt3 失敗：backfill migration 的 `execute` 被
`strong_migrations` 擋下，`db:prepare` abort。

**修復**：`safety_assured { execute(...) }` ＋ 註釋寫明保證內容（小表 `INSERT ... SELECT`、
不改結構、`NOT EXISTS` 冪等）。順帶修掉一行說謊的部署日誌（原本印
「補上既有 staff 的指派（ 列）」，`affected_rows` 是空的；改成前後 COUNT 相減）。

**驗證**：在「有一間店＋有 staff」的資料狀態下實跑該分支——新增 1 列、
再跑一次新增 0 列（冪等）、rollback 不留資料。rspec 457 全綠。

**合併後重新部署成功**：migration 於正式站實跑「新增 1 列，總計 1」，puma 重啟，
`/up` 綠；兩道閘的判定式實機驗過（含反向案例 `can_access_shop?(不存在的店)=false`）。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260824130000_backfill_user_store_assignments.rb`：
  `safety_assured` 包裹 ＋ 日誌改前後 COUNT。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **教訓（本次的根因）：只在特定資料狀態下才執行的 migration 分支，CI 不會替你驗**。
   CI 與本地的資料庫 `shops=0`，走的是 `shop_ids.length != 1` 早退分支，
   `execute` 從未被執行到；正式站有一間店才第一次踏進那行。
   同型風險的自查法：migration 裡任何 `if <資料狀態>` 分支，都要在該狀態下實跑一次。
2. 🔴 **「部署失敗且看起來沒事」比「部署失敗」危險**：本次 deploy 在 `[4/7]` abort、
   `[5/7]`～`[7/7]` 未跑 ⇒ puma 仍跑舊碼、站台完全正常，但 git 檢出已是新碼、
   `assignments=0`——當時任何一次 puma 重啟都會載入租戶閘並鎖死全部帳號。
   已於重新部署後解除；此形態往後要當作部署失敗的**預設假設**去查。
3. 無其他待辦——本 PR 只含這一項修復，範圍刻意最小。
