# 2026-08-16 — T-1 實測：刪除「已被訂單引用的變體」，本尊到底成不成功

> 依鐵律 12.1／12.2 在測試店 `chill-love-u5q5mnzq` 走完整操作流程。
> **背景**：parity 查證（2026-08-15）判定本尊允許硬刪，但那是**證據方向**——
> `productVariantsBulkDelete` 的 mutation 頁對「已被訂單引用的變體」**完全沉默**，
> shopify.dev 與 help 都找不到逐字保證。
> 🔴 而這一條做錯的代價是**不可逆的**：我方一旦把 `fk_line_items_product_variant_id`
> 改成 `ON DELETE SET NULL`，就**再也擋不回來**（已裁定不做軟刪）。

---

## 已完成的工作 (Done)

### 結論：**成功刪除，訂單完全不受影響。硬刪成立。**

### 實測步驟（每一步都親自點擊）

| # | 動作 | 結果 |
|---|---|---|
| 1 | 建商品「T-1 探測商品（變體刪除語義）」，選項 `尺寸 = A / B` | 商品 `9891043115243`，兩個變體 |
| 2 | 建草稿訂單，加入**變體 A**，**顧客留空** | 草稿 `#D3` |
| 3 | 「標示為已付款」→ 轉成正式訂單 | 訂單 **#1006**（`7116407570667`） |
| 4 | 回商品頁確認 A 的可售數量 | **−1**（訂單確實吃掉庫存 ⇒ 引用關係成立） |
| 5 | 勾選 A → ⋯ → **刪除子類** → 儲存 | ✅ **成功**，無任何錯誤 |
| 6 | 回訂單 #1006 | **完全不受影響**（見下） |

🔴 **零費用**：確認 modal 逐字「這將建立一筆**免費訂單，無需付款**」。
🔴 **零對外發信**：刻意不掛顧客。

### 步驟 5 的兩個 UI 觀察（值域窮舉）

- 刪除是**暫存**的：勾選後顯示「此子類將被刪除／**復原**」，要按儲存才生效。
- 確認 modal 逐字：**「選項值為「A」的子類將從您的商店中刪除。此動作無法復原。」**
  ⚠️ **完全沒有提到訂單**——沒有「此變體有 N 筆訂單」之類的警示或阻擋。
  （這順帶回答了 parity 清單的 **T-11**：admin UI 對已售出變體**沒有**額外警示 modal。）

### 步驟 6：訂單 #1006 刪除後的狀態

| 欄位 | 刪除後 |
|---|---|
| 商品名稱 | **「T-1 探測商品（變體刪除語義）」照常顯示** |
| 變體標題 | 🔴 **「A」照常顯示**——變體已經不存在了 |
| 單價／數量／小計／總計 | 全部不變 |
| 訂單狀態 | 已付款／未出貨，不變 |

⇒ **line item 是快照**，變體標題不是即時從 variant 讀的。這與 parity 查證的
`CalculatedLineItem.variant` 官方逐字（「The value is null for … items where the
variant has been deleted」）一致：關聯沒了，但顯示用的資料留在 line item 自己身上。

---

## 修改的檔案與核心邏輯 (Changes)

本輪**沒有改代碼**，只在測試店操作 ＋ 記錄。
規格側的對應修正已在前一輪（`2026-08-16-變體選項join表.md`）完成，本檔是它的**實測補強**：
把「證據方向」升級成「親自跑過」。

---

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **T-8 沒有被本次實測回答，而我差點誤報。**
   我看到刪除後訂單頁的商品名與變體名都**不是連結**，一度想寫成「變體刪除後
   `LineItem.product` 也拿不回來」。
   ⚠️ **跑了對照組才發現是錯的**：另一張變體完好的舊訂單 #1002，
   `a[href*="/products/"]` 同樣是 **0 個**——這個 admin 版本的訂單行項目**本來就不是連結**。
   ⇒ **「沒有連結」與刪除無關，證明不了任何事。**
   🔴 **教訓：觀察到「A 之後出現 B」時，先問「沒有 A 的時候是不是也有 B」。**
   T-8 要另外用 GraphQL 查 `LineItem.product` 才能回答，UI 看不出來。
2. ⚠️ **本次走的是 admin UI，不是 `productVariantsBulkDelete` 直呼**
   ⇒ 拿不到 `userErrors` 的原始 JSON。
   測試店 admin 走內部 persisted-query API（`/api/operations/{hash}/{Op}/`），
   不能當公開 Admin API 的 parity 依據（PR #29 假設清單已登記過同一件事）。
   ✅ **但這不削弱本結論**：要證明的是「會不會被擋」，而它**沒有被擋**。
3. 🔴 **這解除了 `fk_line_items_product_variant_id` 改 `ON DELETE SET NULL` 的前置條件。**
   D12 的 worklog Pending #1 可以往下走了。
   ⚠️ 但**仍未做**——那要改 `line_items` 的外鍵，屬訂單域，應該獨立一輪。
4. ⚠️ **測試資料刻意留著不清**：商品 `9891043115243`（現在只剩變體 B）與訂單 #1006
   是本結論的**證據**，刪掉就沒有了。名稱已標明是探測用。
5. ⚠️ **只測了「兩個變體刪其中一個」這一種情境**。parity 清單的 **T-4**
   （`CANNOT_DELETE_LAST_VARIANT` 的名稱與描述不一致：last variant vs default variant）
   要另外測「一次刪光全部」與「單變體商品刪那一個」兩種——
   🔴 **而單變體測試永遠是綠的**，與鐵律 3 的 exponent=2 假綠同型。
