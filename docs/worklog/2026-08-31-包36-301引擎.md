# 2026-08-31 包 36：301 引擎＋後台重導管理

## 已完成的工作 (Done)

- **消費引擎 `Storefront::RedirectResolver`**（62 §B.5；14 §F5-3）：掛在 PagesController
  **404 之後**（活頁面永遠先贏——manual 列結構上不可能遮蔽現任資源，建立端因此免跨表
  活性檢查）；查表用**無前綴正規路徑**、命中後**保留 locale 前綴** 301 並帶原 query
  （HDL-9：/en-hk/products/舊 ⇒ /en-hk/products/新）；410 列 ⇒ head gone；manual 鏈
  跟至 `seo.redirect_max_chain`(10)、環停在已知最遠目標（handle_change 列由鏈坍縮
  不變量保證單跳——producer 第 6 包既有）。
- **🔴 DOC-5 裁定（第 6 包遷移註釋懸置的表級語義，本包收口）**：manual 列與
  handle_change 列**統一存無前綴正規形**；帶前綴輸入 ⇒ `PREFIXED_PATH_FORBIDDEN`。
  62 §B.5 的「帶前綴＝presence 內重導」形態不採（逐語言 N 列維護災難＋與 producer
  正規形不變量打架）；per-presence 需求出現時另立顯式欄位（登記）。
- **後台 GraphQL**：`urlRedirects`（keyset ≤250、query 過濾）＋`urlRedirectCreate/
  Update/Delete`（path/target 欄名對齊本尊；code enum 含 PREFIXED_PATH_FORBIDDEN／
  SELF_REDIRECT／TAKEN／NOT_EDITABLE）。系統列（handle_change）**不可改**（鏈坍縮
  不變量的守門）、**可刪**（HDL-8：刪除＝商家釋放舊 handle 的官方動作）。
- **admin 頁** `SettingsRedirectsPage`（/admin/settings/redirects；設定索引掛卡）：
  列表（來源→目標＋系統/手動標記）＋新增＋刪除；i18n 鍵五語言檔全補
  （messages parity 測試自然覆蓋）。
- specs：E1–E3（引擎）＋G1–G4（管理）7 例；vitest 3 例（列表／新增變數形／刪除 GID）。

## 修改的檔案與核心邏輯 (Changes)

- `app/services/storefront/redirect_resolver.rb`、`app/services/url_redirects/normalize.rb`（新）
- `app/controllers/storefront/pages_controller.rb`（404 → 查表 → 301/410 掛載）
- GraphQL：`types/url_redirect{,_edge,_connection}_type.rb`、`types/errors/url_redirect_user_error_{code,type}.rb`、
  `mutations/url_redirect_{create,update,delete}.rb`、query_type／mutation_type 註冊
- 前端：`pages/SettingsRedirectsPage.tsx`（新）＋`SettingsPage.tsx`（掛卡）＋`App.tsx`（route）
  ＋`i18n/messages/{en,zh-Hant,zh-Hans,ja,fr}.json`（redirects 鍵組）
- specs：`spec/requests/url_redirect_engine_spec.rb`（新；producer 側仍在
  `url_redirects_spec.rb`）＋`SettingsRedirectsPage.test.tsx`

**突變驗證（20.2⑤／20.3）**：
| # | 突變 | 預期紅 | 結果 |
|---|---|---|---|
| MUT-r1 | 301 不保留前綴 | E1（＋E3） | 2 failures ✅ |
| MUT-r2 | 重導先於活頁（404 條件拿掉） | E2 | 1 failure ✅ |
| MUT-r3 | Normalize 拿掉前綴閘 | G2 | 1 failure ✅ |

## 尚未完成或需注意的風險 (Pending / TODO)

- **manual 410 列的建立面未開**（mutation 只發 301；引擎已支援 410——`redirect_status_codes`
  含 410 的下架語義由商品下架流程接，登記）。
- 重導**編輯 UI** 未做（Update mutation 已備；頁面 v1＝建立＋刪除）；`query` 過濾輸入框未接
  （API 已備）；分頁 UI 未做（250 上限內先行）。
- domain_move／import 兩個 source 值域無寫入者（62 §B.5 既定；平台子網域→自訂網域
  301 全量歸 hosting 後續，§B.1 行 8）。
- `canonical_overrides` 表（62 §B.5 第二張）未建——極少用、需審計，待真需求。
- sitemap 與重導：來源路徑天然不進 sitemap（discoverable 判準不含已改名 handle）；
  來源＝活資源的 manual 列不生效（活頁先贏）——兩處引擎註釋已記。
