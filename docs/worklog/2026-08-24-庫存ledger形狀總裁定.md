# worklog 2026-08-24 — 庫存 ledger 形狀總裁定（含測試店實機走流程）

## 已完成的工作 (Done)

- **實機走完整調整流程**（使用者本日鐵律重申：每一個步驟都親自走一次）：
  測試店庫存頁 → Available 儲存格 → `Adjust by` +1 → origin `Inventory addition` →
  destination `Shop location` → reason `Received` → ✓ → Save → 商品頁確認 10/10 →
  由 `View adjustment history` 真實 href 進歷程頁（路由 `/products/inventory/{item_id}/inventory_history`）。
- **V-94.3 結案**（`docs/research/94` §2.5）：一次調整＝歷程頁一列（7 欄），每個受影響數量欄
  各顯示 delta＋期後值；參考文件在 Activity popover（實測連手動調整都掛 `TransferAdjustment` gid）；
  期初庫存本身是一列 ledger 事件；`Created by` 是 group 級（staff 或 app）。
- **API 互證**：`inventoryAdjustQuantities` 官方範例調 available −4 回兩筆 change
  （available ＋ on_hand）——UI 與 API 是同一份資料的兩個投影。
- **合併裁定落檔** `docs/plans/2026-08-24-庫存ledger形狀總裁定.md`：
  ledger 行身分＝(group, level) 一列帶六 leaf delta 欄＋兩個 STORED GENERATED；
  四條對撞逐條消解（from/to 兩欄都不存在／冪等鍵上 group／actor 兩欄在 group／
  ledgerDocumentUri 在行、referenceDocumentUri 在 group）。

## 修改的檔案與核心邏輯 (Changes)

- `docs/research/94-collections-inventory-live-teardown.md`：新增 §2.5、V-94.3 結案。
- 新增 `docs/plans/2026-08-24-庫存ledger形狀總裁定.md`。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **仍不寫 migration**：V-96.1（同呼叫重複 item×location 的本尊行為）、
  V-96.3（Initial inventory 映射哪個 reason）未結案；`idempotencyKey` 上不上 `String!` 待使用者拍板。
- V-96.4：71-R8-DOC1 記「歷史 8 欄」，本次實測 7 欄——以親自點擊為準，差異可能是欄位設定或方案差；
  下次實測順手點欄位設定按鈕確認。
- 測試店留下一筆真實調整（+1，Received）——刻意保留，它就是歷程頁的實測樣本。
