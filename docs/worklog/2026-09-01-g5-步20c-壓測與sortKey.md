# 2026-09-01 G5 步 20c：F5 壓測欠帳＋products sortKey＋91 補正

## 已完成的工作 (Done)

- 🔴 F5 併發壓測欠帳收口（20 步方案步 20 原文：「50 執行緒同 checkout：訂單
  恰一＋顧客統計不漂」）：ST1＝50 執行緒相異冪等鍵打同一 checkout——訂單恰一
  ＋每個成功形結果指向同一張（completed 分支回放）＋輸家全為已定義形
  （CHECKOUT_NOT_OPEN/Guard Conflict，無未知例外）＋顧客統計恰一次
  （orders_count/total_spent_cents）＋庫存單次（committed+2/available−2）＋
  order_counter +1＋outbox 恰一顆 orders/create。3.4s 內跑完（pool 5 夠用：
  輸家在贏家 commit 後快速失敗釋放連線）。
- products sortKey V 項收口：官方 ProductSortKeys 九值 enum 逐字（取證
  2026-09-01）；v1 支援 TITLE/CREATED_AT/UPDATED_AT/ID（升冪為底＋reverse
  官方語義）；🔴 未支援鍵 fail-closed（SORT_KEY_NOT_SUPPORTED——SK3/MS-3
  紅證：不得靜默退預設）；不帶 sortKey＝created_at desc 零回歸（SK1）；
  cursor 續頁沿用排序鍵（SK4 三連頁）。ORDER_KEYS 補 title/id 編解碼
  （🔴 nullable 欄不得進 keyset——PRODUCT_TYPE 因此進 91 ⚪）。
- 91 §3.74：sortKey 未支援子集登記＋roadmap /collections 缺漏條有日期收口
  （已由 #242/#244 同日交付）＋壓測防線分層（單層突變預期內殺不掉）登記。
- 突變 3/3 殺（MS-1 複合＝鎖＋條件轉移同時退化 ⇒ ST1 紅／MS-2 reverse 吞
  ⇒ SK2 紅／MS-3 靜默退預設 ⇒ SK3 紅）。

## 修改的檔案與核心邏輯 (Changes)

- 新：product_sort_keys_enum.rb、products_sort_key_spec.rb（SK1-4）、
  order_creation_stress_spec.rb（ST1）。
- 改：query_type（products sort_key/reverse＋PRODUCT_SORT_KEY_MAP）、
  keyset_cursor.rb（title/id 編解碼）、91 §3.74。

## 尚未完成或需注意的風險 (Pending / TODO)

- 步 20 殘件：dunning 引擎首版（37 W1 retry_offsets——需先讀 37 號研究）；
  87 號 V 項補測（🔴 需使用者在測試店配合觸發——user-gated）。
- ⚠ 部署驗證教訓（本輪未出倉自捕）：deploy 鏈上游 `git stash pop` 失敗令
  ssh 根本沒跑、/tmp 舊檔的 ✅ 行被 grep 撿到差點誤判「已部署」——**部署
  宣稱一律以伺服器 `git log -1` 現值驗**，不以本地日誌 grep。
- 生產煙測：sortKey 走 admin GraphQL（登入態）——CI 全綠＋SK1-4 已覆蓋，
  生產抽測 TITLE 一頁即可。
