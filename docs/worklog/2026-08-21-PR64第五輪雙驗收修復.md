# PR #64 第五輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收基準固定為 head `7c10b6a86155cf4fe43965fb574b6473e1939d78`。鐵律 15.2 三端點
  全量重取後，Claude issue comment `5360279873` 有終態層與復發閉環意見；Codex review
  `4986192842` 的 inline 為 `3824363605`、`3824363616`、`3824363620`、`3824363626`。
  Claude Review run `32403756499` completed success，沒有 validation skip；CI quality／test 同 head 綠。
- 終態 P-8 worklog 不再逐輪手抄驗收 worklog 名稱；改為明示不宣稱全集，並由 immutable base
  `7de4ab0` 到 HEAD 的 worklog 集合與 91 A.1 做集合相等複驗。這使新增本輪 worklog 不會再次
  讓同一個 commit 內的終態列舉過期。
<!-- 🔴 2026-08-21 第六輪更正（Codex inline `3824556128`）：上列 `7de4ab0` 是 PR 內 commit，
     squash 後不由 main 保存，不能作合併後入口；現行命令改用 preserved main base
     `0fbe520502588b34f9b9cad6ae9b3a282d4db643`，見本檔「現行可重跑反向複驗」。 -->
- 第四輪 Done 的「四條」按歷史層規則保留，緊鄰補上 `eb1afba..7c10b6a` immutable diff 查法；
  Changes 終態層則移除非必要計數。91 §3 新增 PR #64 批次標題，並把更強 pagination 候選降為
  「未取得官方證據」；沒有把驗收方敘述直接升成外部事實。
- Claude 的範圍外觀察按根因去重：可移動 `pr64-last-push` 另登 §3；20.3 未列不適用類型併入
  既有條目並擴到第四輪；附錄 A 的機械防線缺口已由 §2 G-01 家族涵蓋，不重複立條。

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | head `eb1afba` 的 Codex inline `3824178998` 已點名終態 Changes 漏第三輪；head `7c10b6a` 的 Codex inline `3824363605` 與 Claude comment `5360279873` 又點名同段漏第四輪 |
| 既有防線失效原因 | 既有修法只把「上一個漏項」補進手抄列舉，而補字的 commit 同時新增下一份驗收 worklog；列舉在結構上於 commit 完成時再次過期 |
| 被漏掉的固定步驟 | 鐵律 20.2.2 的生產者／消費者反查只核對當時漏項，沒有把「本輪新 worklog」視為同一集合的新生產者，也沒有做 A.1 的集合相等比較 |
| 可重跑反向複驗 | `ruby -E UTF-8:UTF-8 -e 'p="docs/worklog/2026-08-20-P8證據來源與合併後文件債收斂.md"; s=File.read(p,encoding:"UTF-8"); b=s[/<!-- 🔴 2026-08-21 終態更正.*?-->/m] or abort("missing terminal correction"); abort("manual round list returned") if b.match?(/PR64\u7b2c[\u4e00\u4e8c\u4e09\u56db\u4e94\u516d\u4e03\u516b\u4e5d\u5341]+\u8f2a/); a=%x[git -c core.quotepath=false diff --name-only 7de4ab0 HEAD -- docs/worklog].lines.grep(/PR64.*\u9a57\u6536\u4fee\u5fa9[.]md$/).map(&:strip); r=File.read("docs/specs/91-pit-register.md",encoding:"UTF-8"); x=r[/### A[.]1.*?(?=### A[.]2)/m] or abort("missing A.1"); c=x.scan(/`(docs\/worklog\/2026-08-21-PR64[^`]*\u9a57\u6536\u4fee\u5fa9[.]md)`/).flatten.uniq; abort({diff:a,appendix:c}.inspect) unless a.sort==c.sort; puts a'` |

<!-- 🔴 2026-08-21 第六輪更正（Claude comment `5360596028` 🟡1／2、Codex inline
     `3824556128`）：上表原命令保留作歷史層，但單反引號 code span 被命令內反引號切斷、
     `7de4ab0` 不會由 squash 後 main 保存，且手抄守衛漏了「首輪」。它不是現行入口；
     下方 fenced block 同時改用 preserved base、涵蓋「首輪／第 N 輪」並做過濾後集合相等。 -->

#### 現行可重跑反向複驗（2026-08-21 第六輪更正）

上表原字串保留作歷史；現行複驗只認下列 fenced block。正常樹須輸出 PR #64 驗收 worklog
集合；若在 P-8 終態更正內重加「首輪」或「第 N 輪」手抄字樣，或 A.1 漏／多一項，必須非零。

~~~ruby
ruby -E UTF-8:UTF-8 -e 'p="docs/worklog/2026-08-20-P8證據來源與合併後文件債收斂.md"; s=File.read(p,encoding:"UTF-8"); b=s[/<!-- 🔴 2026-08-21 終態更正.*?-->/m] or abort("missing terminal correction"); abort("manual round list returned") if b.match?(/PR64(?:\u9996\u8f2a|\u7b2c[\u4e00\u4e8c\u4e09\u56db\u4e94\u516d\u4e03\u516b\u4e5d\u5341]+\u8f2a)/); a=%x[git -c core.quotepath=false diff --name-only 0fbe520502588b34f9b9cad6ae9b3a282d4db643 HEAD -- docs/worklog].lines.grep(/PR64.*\u9a57\u6536\u4fee\u5fa9[.]md$/).map(&:strip); r=File.read("docs/specs/91-pit-register.md",encoding:"UTF-8"); x=r[/### A[.]1.*?(?=### A[.]2)/m] or abort("missing A.1"); c=x.scan(/`(docs\/worklog\/2026-08-21-PR64[^`]*\u9a57\u6536\u4fee\u5fa9[.]md)`/).flatten.uniq; abort({diff:a,appendix:c}.inspect) unless a.sort==c.sort; puts a'
~~~

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 驗收攝取 | current head 的 issue comments、review body、inline 全部 `--paginate` 重取 | 上列 Claude／Codex ID 均綁 `7c10b6a…`；Claude run completed success |
| ② 生產者與消費者同步 | 反查 P-8 終態段、各輪 worklog、91 §3／A.1 與 PR 描述 | 終態段改成動態集合；新增本 worklog同步進 A.1；20.4 命令須在 commit 後通過 |
| ③ 易腐數字／完成聲明 | 歷史計數綁 immutable diff；終態刪除非必要計數；外部未取證行為標未取得 | 第四輪「四條」綁 `eb1afba..7c10b6a`；pagination 候選不再發布為事實 |
| ④ 零值／空值 | 本輪只改文件，無產品資料邊界；集合比較的零輸入由 missing terminal／A.1 abort 與集合不等擋住 | 20.4 命令任一來源缺失皆非零 |
| ⑤ fail-open／happy path | 動態集合命令同時守「終態段不得恢復逐輪手抄」與 A.1 集合相等 | 命令在 commit 後實跑；任一集合漏項或重引入逐輪名即 abort |
| ⑥ 超大值／資源上限 | 不適用：本輪無數值解析、輪詢或資源上限變更 | `git diff --name-only 7c10b6a..HEAD` 僅允許本 worklog、P-8 worklog、第四輪 worklog與 91 |
| ⑦ Markdown／歷史層 | Done 原文不靜默改寫；Changes 可回寫終態；新表逐列維持等量未跳脫直線 | `git diff --check`、doc-claims warning 0 與表格結構複驗須全過 |

<!-- 🔴 2026-08-21 第六輪更正（Claude comment `5360596028` 🟡2）：上表第⑤列原稱
     「重引入逐輪名即 abort」，但原守衛只涵蓋「第 N 輪」、漏掉「首輪」。現行 fenced
     block 已擴成 `PR64(?:首輪|第 N 輪)` 的 Unicode 等價式；第六輪另以 mutation 實跑證明
     在終態更正插入「PR64首輪驗收修復」時命令非零。 -->

## 修改的檔案與核心邏輯 (Changes)

- `docs/worklog/2026-08-20-P8證據來源與合併後文件債收斂.md`：終態更正改為動態集合與 A.1
  反查，不再逐輪手抄。
- `docs/worklog/2026-08-21-PR64第四輪雙驗收修復.md`：歷史計數緊鄰補 immutable 快照查法；
  終態 Changes 刪除非必要計數。
- `docs/specs/91-pit-register.md`：新增 PR #64 批次標題、收窄未取證 pagination 候選、合併
  20.3 同根條目、登記可移動 base 缺口，並同步 A.1。
- `docs/worklog/2026-08-21-PR64第五輪雙驗收修復.md`：保存本輪三端點、20.4 四件事、20.3
  全類型稽核、修改射程與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 本段定稿後須先實跑 20.4 反向命令的 worktree 等價式與 Markdown 表格結構，再從 setup 跑
   完整 Windows production-equivalent 閘門；任一非零即停止。
2. commit 後須重跑 20.4 HEAD 命令、doc-claims warning 0、鐵律 15 與三端點增量；push 後所有
   舊 head 的 CI／Claude／Codex 結果失效，必須取得新 exact-head 雙清。
3. #64 是一般 PR；只有 exact-head CI、Codex、Claude 雙零、threads resolved、mergeable 與
   evaluator `1111` 同時成立，才可依 D31／D32 代行 squash merge。
