# 2026-08-15 — 補上 workflow 的語法閘門（全倉庫最沒保護的地方）

> 來源：多代理驗收的 **Y12**，複驗成立。

---

## 已完成的工作 (Done)

### 1. 問題：一個保護真空

`.github/workflows/claude-review.yml` 有一支 **240+ 行的閉環 shell 腳本**，而：

- 🔴 **動該檔的 PR 拿不到 Claude 驗收**（反竄改機制，見該檔檔頭 31–40 行）；
- 🔴 本倉庫的 CI **沒有任何一步**解析 workflow YAML 或跑 `bash -n`；
- 🔴 該檔註釋**自己記載**被同型語法錯誤咬過**兩次**（block scalar 續行落在第 0 欄、heredoc）。

⇒ 這是全倉庫**最需要機械語法檢查、卻最沒有保護**的地方。
本輪前面幾個 PR 又往它加了約 50 行 shell（新 jq、9 參數 printf），風險只增不減。

### 2. `scripts/check-workflow-syntax.rb`

兩條規則：

1. `.github/workflows/*.yml` 每份必須是**合法 YAML**
2. 每個 step 的 `run:` 區塊必須過 **`bash -n`**（只檢查語法，不執行）

三個實作決定：

- **GitHub 表達式先換成佔位符**。`${{ ... }}` 直接送 bash 會是 bad substitution ⇒ 誤報。
  目前倉庫的 run 區塊裡**一個表達式都沒有**（實測 25 個 run 區塊、0 個），
  但第一個用它的人不該撞到假警報。
- **只送 bash 系的 shell**（未宣告＝預設 bash）。宣告 `shell: pwsh`／`python` 的跳過。
- **掃到 0 份 workflow 就 fail**，不是印 OK ——同 `check-exec-bits.sh`（PR #41，尚未進 main）的 canary，
  防「空值長得像資料」。

🔴 檔頭有**誠實聲明**：**這不是 actionlint**。不驗 action inputs 是否存在、
不驗 `${{ }}` 內的表達式語義、不驗執行期行為。
（前例就在同一份 workflow 的註釋裡：`with: model:` 在 action v1 是**未知 input，
GitHub 靜默忽略**——那一類本腳本抓不到，要靠 actionlint 或讀 action.yml。）

### 3. `scripts/test-workflow-syntax-rules.rb`（初版四條，經兩輪驗收後**現為八條**）

| fixture | 期望 | 守什麼 |
|---|---|---|
| `wf_clean` | exit 0 | 🔴 反向斷言，且**刻意含 `${{ }}` 與 heredoc**——兩者都是最容易誤報的形態 |
| `wf_bad_yaml` | exit 1 | block scalar 續行掉到第 0 欄（**claude-review.yml 記載的 2026-08-14 真實事故**） |
| `wf_bad_bash` | exit 1 | 🔴 **YAML 合法但 bash 壞掉**——只驗 YAML 的檢查會放行，要到 CI 跑那步才炸 |
| `wf_empty` | exit 1 | 🔴 掃不到檔案時必須 fail |

`wf_clean` 那條是雙重用途：既是反向斷言，也**證明佔位符替換有效且不誤傷 heredoc**。

**自測**：真實倉庫 exit 0（2 份 workflow、25 個 run 區塊）；四條回歸測試全過；
`rubocop` 2 files no offenses；`ruby -c config/ci.rb` OK；兩支新腳本 100755。

---

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `scripts/check-workflow-syntax.rb` | **新增**。YAML 解析 ＋ 每個 run 區塊 `bash -n`；GH 表達式換佔位符；零掃描 canary |
| `scripts/test-workflow-syntax-rules.rb` | **新增**。初版四條 fixture 驅動；條數不寫死（初版四條起持續增加），實跑 `ruby scripts/test-workflow-syntax-rules.rb` 為準 |
| `spec/fixtures/ci_violations/wf_*/` | **新增**；份數不寫死（初版四份起持續增加，`git ls-files spec/fixtures/ci_violations/ \| grep wf_` 為準） |
| `scripts/check-doc-claims.rb` | 🔴 **掃描語義變更**（第 3 輪 🟡 實作）：R4 數字類擴**中文數字**（排除裸「一」）；R4/R5 由「掃整份有改動的檔」收窄為**只掃 diff 的 `+` 行**（`-U0` hunk 行號集合） |
| `scripts/test-doc-claims-rules.rb` | 補 `doc_volatile_cjk` case（中文數字承重；曾抓到自己的冗餘 pattern 讓突變測不出來，已修） |
| `spec/fixtures/ci_violations/doc_volatile_cjk/` | **新增**（中文數字計數的易腐宣稱形態，內容見該 fixture 檔） |
| `.github/workflows/ci.yml` | quality job 加 `Check workflow syntax` 與 `Regression-test workflow syntax rules` |
| `config/ci.rb` | 同步補兩步（本機跑得到特別有價值——見下） |

---

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **這不是 actionlint，不要當成 actionlint 用**。它抓不到「未知 input 被 GitHub 靜默忽略」
   那一類——而那正是 `claude-review.yml` 註釋裡記載過的坑（`with: model:` 在 v1 無效）。
   要補那一層得引入 `rhysd/actionlint`，**本輪沒做**（新增外部依賴要先討論，鐵律 1）。

2. ⚠️ **`bash -n` 只看語法**。變數拼錯、`set -e` 的交互、管線退出碼、命令不存在——全抓不到。
   本輪前面幾個 PR 抓到的 workflow bug（`core.quotePath`、零掃描、jq 濾鏡）
   **沒有一個**是語法錯誤 ⇒ 這道閘門守的是另一類問題，不能取代那些檢查。

3. ⚠️ **本 PR 加的兩步只在 quality job**，`test` job 沒加（沒必要重複）。
   但這表示若有人只看 test job 綠就合併，是看不到這兩步的。

4. ⚠️ **沒有對 `check-workflow-syntax.rb` 做突變測試**（同 `check-exec-bits.sh` 那輪的缺口——該檔在 PR #41，尚未進 main）。
   八條 case 是照已知事故與兩輪驗收意見設計的，不是用突變打出來的。
   <!-- 「八條」＝寫下當時的快照；現值 `ruby scripts/test-workflow-syntax-rules.rb` 實跑為準。 -->

5. ✅ **`config/ci.rb` 已同步**——本機跑得到這道閘門特別有價值：
   改 `claude-review.yml` 的 PR 拿不到 Claude 驗收，**本機這一步是它上線前唯一的機械檢查**。

6. ⚠️ **同批驗收剩 Y7／Y8 兩條**（`claude-review.yml` fallback 的假歸因與 null 垃圾）。
   那兩條要動 `claude-review.yml` 本身 ⇒ 該 PR 不會被驗收，
   計畫併進已開的 PR #37（同檔、同樣不可自我驗收），避免兩個 PR 改同一個檔造成衝突。

---

## 驗收後修正（PR #42 第 1 輪 Codex review，六條，逐條複驗）

### 🔴 P1：`bash -n` 對未閉合 heredoc **exit 0**

實測：

```
$ printf 'cat <<EOF\nnever terminated\n' > hd.sh && bash -n hd.sh; echo "exit=$?"
hd.sh: line 2: warning: here-document at line 1 delimited by end-of-file (wanted `EOF')
exit=0                     ← 🔴 只在 stderr 印警告，退出碼是 0
```

⇒ 只判 `status.success?` 的話，這一類**完全放行**。
🔴 而 `claude-review.yml` 記載的兩次語法事故，**其中一次正是 heredoc**
——**這道閘門原本擋不住它宣稱要守的兩件事之一**。

**修法**：`stderr` 有任何輸出就算違規。
實測本倉庫 **27 個 run 區塊 stderr 全空** ⇒ 不會製造誤報；
且它的失效方向是「吵」不是「靜默」，那是對的方向。
新增 `wf_bad_heredoc` fixture 守住。

### 🔴 P2：裸日期讓腳本崩掉

`YAML.load_file` 預設不許 `Date` ⇒ 含 `SINCE: 2026-08-15` 的 workflow 會丟
`Psych::DisallowedClass`。本腳本原本只 rescue `Psych::SyntaxError`
⇒ **帶著 Ruby backtrace 崩掉**，CI 上只看得到 backtrace，讀的人不知道是哪份 workflow。

**修法**：`permitted_classes: [Date, Time, Symbol]` ＋ 加 rescue `Psych::Exception`
（仍報成違規，**不讓腳本崩掉**）。

### 🔴 P2：自訂 shell 模板被靜默跳過

GitHub 的 `shell:` 可以是模板，例如 `bash -e {0}`、
`bash --noprofile --norc -eo pipefail {0}`（**後者正是 `run:` 的預設**）。
原本用 `[nil, "bash", "sh"].include?(shell)` 判斷 ⇒ 這些寫法**一個都不匹配**
⇒ 那些 run 區塊被**靜默跳過不檢查**——又是「檢查沒驗到目標」。

**修法**：`interpreter_for(shell)` 取第一個 token 的 basename 判斷；
順帶採納另一條 P2——**宣告 `sh` 就用 `sh -n` 檢查**，不要用 bash 的寬鬆語法放行。

### 未採納 / 未處理

- **`${{ }}` 內含 `}}` 字面**（例如 `fromJSON('{"a":{}}')`）會讓非貪婪 regex 截錯。
  目前倉庫 **0 個表達式**，且真出現時是 `bash -n` **誤報**（吵而不是靜默）
  ⇒ 可接受的失效方向，**本輪不處理**，已寫進腳本註釋。
- **重複 YAML 鍵**：Psych 預設不報錯，GitHub Actions 會。這需要自己走 AST 比對，
  **本輪沒做**，登記在下面 Pending。

### 補測後的回歸測試（3 條 → **6 條**）

新增 `wf_bad_heredoc`（exit 1）與 `wf_date_scalar`（exit 0）。
後者是**第二條反向斷言**，且刻意「合法但最容易被誤判」——
它同時覆蓋裸日期與自訂 shell 模板兩個坑。

自測：6 條全過；真實倉庫 exit 0（27 個 run 區塊）；rubocop no offenses。

> 🔴 **更正（2026-08-15，PR #42 的 Claude 驗收指出）**：原文接著寫
> 「比修前多 2 個——**那 2 個正是原本被靜默跳過的**」。**那句是假的。**
>
> 實查：`.github/workflows/` 只有 `ci.yml`、`claude-review.yml` 兩份；
> **`shell:` 宣告數 = 0**（`defaults:` 也 0）；`run:` ＝ ci.yml 25 ＋ claude-review.yml 2 ＝ 27。
> 而本 PR 往 `ci.yml` 新增的正好是 **2 個 `run:`**（Check workflow syntax／
> Regression-test workflow syntax rules）⇒ 修前就是 25。
>
> 既然全倉庫 0 個 `shell:` 宣告，舊白名單對 `nil` 就是 true ⇒ **一個 run 區塊都不可能被跳過**。
> 25→27 的 +2 是**本 PR 自己加的兩步**，不是「原本被跳過的」。
> ⇒ 🔴 **這是把自己新增的步驟當成修好的證據。**
>
> **事實是**：本倉庫 0 個 `shell:` 宣告，所以「自訂 shell 模板」這條修正
> **在真實倉庫上不改變任何結果**——唯一的證據只能來自 fixture，
> 而那正是本輪新增 `wf_custom_shell` 的理由。
> 出處：`AGENTS.md` 註釋與文檔 §2（引用要有出處且為真）。

---

## 驗收後修正（PR #42 的 Claude 驗收，四條 🔴，逐條實測）

### 🔴 1. 0 掃描 canary 只做了檔案層，沒做 run 區塊層

`files.empty?` 有 canary，但 `checked_runs == 0` 沒有 ⇒ 若所有 run 區塊都被
`interpreter_for` 跳過，會印「**0 個 run 區塊皆通過 bash -n**」並 exit 0。

🔴 **而那正是本 PR 第三條修正在修的回歸形態**——這道閘門對它自己剛修好的故障，會綠著通過。

修法：加第二層 canary ＋ 新 fixture `wf_no_runs`（合法 YAML、只有 `uses:` 步驟 ⇒ 必須 exit 1）。

⚠️ **第一版把 canary 放錯位置**：放在 `violations` 判斷之前 ⇒ `wf_bad_yaml`
（YAML 壞掉 ⇒ 0 個 run 區塊）被 canary 搶先開火，輸出不再含「YAML 解析失敗」，
回歸測試立刻紅。🔴 **canary 不得搶在真正的違規之前**——已移進 `violations.empty?` 分支。
（這個錯誤是回歸測試自己抓到的，不是我看出來的。）

### 🔴 2. `wf_date_scalar` 對它宣稱守的回歸不承重

它宣稱「同時覆蓋自訂 shell 模板」，但它**期望 exit 0**——
而 `interpreter_for` 回歸時該區塊只是被跳過，結果**仍然是 exit 0**。
🔴 **exit 0 分不出「檢查過而且乾淨」與「根本沒檢查」。**

真正承重的是 `wf_bad_heredoc/broken.yml` 的 `shell: bash --noprofile --norc -eo pipefail {0}`
那一行——但該 fixture 檔頭整段只講 heredoc，下一個人很可能覺得那行多餘而刪掉。

修法：①刪掉 `wf_date_scalar` 的假宣稱；②新增 `wf_custom_shell`（自訂 shell 模板下的壞 bash
⇒ **必須 exit 1**）作為正向斷言；③在 `broken.yml` 那一行上方標明它是承重的。

### 🔴 3. 自測結論不實（把自己新增的兩步當成證據）

原文：「27 個 run 區塊，比修前多 2 個——**那 2 個正是原本被靜默跳過的**」。

實查：全倉庫 **0 個 `shell:` 宣告** ⇒ 舊白名單對 `nil` 就是 true ⇒ **一個區塊都不可能被跳過**；
而本 PR 往 `ci.yml` 新增的正好是 2 個 `run:`。⇒ +2 是**本 PR 自己加的**。已更正為事實敘述。

### 🔴 4. 檔頭仍寫「四條」，實際六條（現為八條）

`test-workflow-syntax-rules.rb` 檔頭、handoff、worklog 多處未跟上。
已改寫成**八條的分組清單**（真實事故／判準本身／反向斷言／「什麼都沒驗到」兩層）。

### 突變測試

把 `interpreter_for` 改回舊白名單（精確成員比對）→ **8 條裡 3 條變紅**
（`wf_date_scalar`／`wf_custom_shell`／`wf_no_runs`），輸出逐字是
「掃到 1 份 workflow，但 **0 個 run 區塊**被檢查」⇒ 新增的兩條**是承重的**。還原後 8 條全過。
<!-- 🔴 2026-08-16 更正（第 2 輪驗收指出，重跑突變確認）：上表三個名字**錯了一個**——
     正確的三條是 `wf_date_scalar`／`wf_custom_shell`／**`wf_bad_heredoc`**（不是 wf_no_runs）。
     wf_no_runs 本來就期望 exit 1＋canary 訊息，突變下行為不變 ⇒ 仍綠；
     wf_bad_heredoc 的 step 帶自訂 shell，突變下被跳過 ⇒ 輸出不含「但有警告」⇒ 紅。
     2026-08-16 重跑輸出逐字：
       wf_bad_heredoc：exit code 對，但輸出不含「但有警告」
       wf_date_scalar：期望 exit 0，實得 1
       wf_custom_shell：exit code 對，但輸出不含「bash -n」
     本表是本 PR 唯一一份「我們證明了什麼」的紀錄，承重關係指錯比數字錯更傷。 -->

**自測**：8 條全過｜真實倉庫 exit 0（2 份 workflow、27 個 run 區塊）｜
`ruby -c` 兩支腳本｜`ruby -c config/ci.rb`。

---

## 驗收後修正（PR #42 第 2 輪 ＋ 2026-08-16 併 main）

### 🔴 三處「這道閘門守了什麼」的紀錄與事實不符

1. **`:58` 的免責聲明是反的**：寫「wf_date_scalar 不覆蓋自訂 shell 模板——跳過也是
   exit 0，分不出來」。那在 `checked_runs` canary 加入後**就不成立了**：
   該 fixture 只有一個 run 區塊，interpreter_for 回歸 ⇒ 跳過 ⇒ canary exit 1 ⇒ 變紅。
   驗收方用**我自己的突變表**證明了這件事——我的突變結果否證了我自己寫的註釋。
   ⇒ 上一輪被要求「修正」的那一份（fixture 檔頭）才是對的，:58 才是錯的那一份。
2. **突變表的名字錯了一個**：寫 `wf_no_runs`，實際變紅的第三條是 **`wf_bad_heredoc`**
   （重跑確認；wf_no_runs 本來就期望 canary 訊息，突變下行為不變 ⇒ 仍綠）。
   本表是本 PR 唯一一份「我們證明了什麼」的紀錄，承重關係指錯比數字錯更傷。
3. **handoff 寫「沒做突變測試」**，與 worklog 的突變表直接矛盾。
   改成真實邊界：「做了一次定向的（interpreter_for），尚未突變 stderr 判準／canary／YAML 分支」。

### 🟡 一併處理（第 2 輪建議）

- steps 迴圈補 `next unless step.is_a?(Hash)`——空元素 `-` 是合法 YAML，
  無防護時 `nil["run"]` 崩掉（與裸日期同形態）。**配 fixture `wf_nil_step`**（needle 用
  run 計數不用 exit 0），突變（防護拿掉）**被抓** ✅。CASES 8 → 9 條。
- `bash -n` 呼叫固定 `LC_ALL=C`（stderr 非空即違規的判準下，locale 告警會整片誤紅）。
- 成功訊息「皆通過 bash -n」改成「shell 語法檢查（bash/sh -n）」——走 sh 分支時原句不準。
- `wf_bad_yaml/broken.yml` 檔頭照實：fixture 是**第 4 欄**形態，
  真實事故是第 0 欄——同類不同形態，原檔頭會讓人以為第 0 欄被測過了。
- 檔頭「這八條」改為不寫死條數（已過期兩次）。

### 併 main（#46/#40/#39 進來之後）

- `config/ci.rb` 衝突：保留 main 的（limits＋parity 四步）＋本分支的 workflow 兩步，
  現 `check-ci-parity` 綠（15 支腳本全涵蓋）——第 2 輪 🟡「缺 3 條」隨 main 合併自動解決。
- 新閘門 `check-doc-claims` 首跑抓到本分支 3 條（引用 PR #41 的 `check-exec-bits.sh`
  ——該檔尚未進 main——未錨定），已補 PR 錨定。
  🔴 **然後這一段自己又犯了一次**（第 3 輪驗收抓到）：上面那句初稿寫「#41」沒有「PR」
  前綴 ⇒ ANCHORS 正則不認 ⇒ 閘門紅。根因是流程序錯了：**修完跑閘門 → 再追加 worklog
  （帶著新引用）→ 沒重跑就 commit**。⇒ 教訓：**閘門必須是 commit 前的最後一步**，
  任何在閘門之後動過文檔的 commit 都要重跑。

## 尚未完成或需注意的風險（本輪追加）

5. ⚠️ 尚未突變的判準不變：stderr 非空判定、零掃描 canary、YAML 解析分支。
6. ⚠️ `shell: sh` 分支沒有「dash 擋得住的 bashism」正向 fixture（[[ ]] dash 不擋，
   要用 process substitution 這類）；登記，未做。

---

## 驗收後修正（PR #42 第 3 輪）

### 🔴 1. 閘門紅著、紀錄寫它是綠的——流程序錯誤

第 2 輪的收尾順序是：修三條錨定 → 跑閘門（綠）→ **追加 worklog（新文字自己帶著
未錨定的「#41」引用——少了「PR」前綴，ANCHORS 正則不認）→ 沒重跑就 commit**。
於是 push 上去的分支閘門是紅的，而 worklog 與 commit message 都寫「現 exit 0」。

⇒ 教訓：**閘門必須是 commit 前的最後一步**；任何在閘門之後動過文檔的 commit 都要重跑。
本輪的收尾照這個順序執行。

### 🔴 2. 終態層兩處「八條」寫死（第 9 條進來後過期）

handoff §① 與 worklog Changes 表改為不寫死（4→8→9，實跑為準）。
歷史層的「八條」敘事**不動**，改用**鄰行快照註記**（原文保留、註記行含複驗指令）——
這同時滿足「歷史不回頭改」與 R4。

### 🟡 3. R4 對中文數字全盲（真閘門缺口，本輪實作）

`VOLATILE_NUM` 五條 pattern 全以 `\d+` 開頭，而本倉庫計數幾乎一律寫中文——
「八條」撐過兩輪人工審查＋閘門首跑，就是這個洞的直接後果。

- `NUM` 擴成中文數字；fixture `doc_volatile_cjk`；
- 🔴 突變測試先抓到**我自己的冗餘**：多加的獨立 CJK pattern 會把「NUM 退化回
  `\d+`」的突變接住 ⇒ 刪冗餘讓 NUM 承重，重測**被抓** ✅；
- 🔴 CJK 擴充引出兩個結構問題，一併修：
  ①**單獨「一」誤報**（「一支 240+ 行的閉環 shell」是量詞不是計數）——
    排除裸「一」，複合數詞（十一/一百）仍涵蓋；
  ②R4 原本掃「有改動的整份檔」⇒ 本 PR 這種**新檔**整份都算新增，歷史段也被打——
    改成**只掃 diff 的 `+` 行**（`-U0` hunk 頭算行號集合），
    「新寫的散文守規矩、歷史不動」從此有機械對應。

### 🟡 4. 其餘

- `wf_nil_step` 檔頭加承重註記（needle 依賴「恰好一個 run 區塊」）；CASE 說明同步。
- `test-workflow-syntax-rules.rb` nil_step 條目補續行排版（與鄰居一致）。

## Pending（追加）

7. ⚠️ 裸「一」排除是啟發式：「一條 case 都沒有」這種**真的在計數**的句子也會被放過。
   R4 的失效方向因此偏「少報」；量級可接受（計數句極少用單數），登記不隱瞞。

### 更正註記（2026-08-16，PR #42 第 5 輪）

- 上方「🟡 3」②段的敘述**歸因有誤，結論不動**（第 4 輪驗收指出）：對本 PR 這種
  **整份新增的檔**，所有行都是 diff 的 `+` 行 ⇒「只掃新增行」**救不了**新檔裡的
  歷史式敘事段——它們實際能通過，靠的是鄰近的快照標記／複驗指令豁免
  （SNAPSHOT／RECHECK_CMD），不是範圍收窄。範圍收窄真正救的是**既有檔案**的
  未改動段。原文依歷史層裁定不改，錯在歸因，特此更正。
- 上方「🟡 4」第二點「補續行排版」當時**並未實際改到**（第 4 輪驗收指出，
  宣稱與實物不符）；本輪已真的改齊，並把同形態的單行黏連字串一併清掉
  （兩支測試腳本，`grep -n '"     "' scripts/test-*.rb` 現為空）。

## 第 5 輪驗收修正（2026-08-16）

### Done

- 🔴 `dig` 的第三個入口：`defaults:` 掛**字串**是合法 YAML，`Hash#dig` 對 String
  丟 TypeError 帶 backtrace 崩掉——實測復現後加 `is_a?(Hash)` 防護，
  fixture `wf_bad_defaults`（doc 層＋job 層各掛一個字串）。
- 🔴 check-doc-claims 兩條新路徑補**承重**覆蓋（原本零 fixture／零 canary）：
  `test-doc-claims-rules.rb` 加**臨時 git 倉庫情境**（照 test-exec-bits-rules.sh
  的辦法，`git init` 後兩次 commit 造出「歷史層有髒數字、新增行才受檢」）——
  G1 反向：歷史髒數字＋乾淨新增行必須放行（範圍退化回「整份檔」立刻紅）；
  G2 正向：新增行的中文數字要抓到**該行**且不連坐歷史行（NUM 退化回 `\d+` 立刻紅）。
- `check-workflow-syntax.rb` 檔頭「25 個 run 區塊、0 個表達式」改標**快照**並註記
  過時（ci.yml 現已含 `${{ }}`，佔位符替換已實際生效）；現值以實跑輸出為準。
- 終態層補齊：本檔 Changes 表（wf_* 份數不寫死＋補 doc-claims 側三列）、
  handoff §① 補 doc-claims 側改動、上方兩條更正註記。
- 格式：兩支測試腳本的單行黏連字串改回行尾續行；`\d+` 補逃逸
  （原本雙引號吃掉反斜線，實跑輸出印成 `d+`）。

### Changes

| 檔案 | 改動 |
|---|---|
| `scripts/check-workflow-syntax.rb` | `defaults` 取值前加 `is_a?(Hash)` 防護（doc 層＋job 層）；檔頭快照化 |
| `scripts/test-workflow-syntax-rules.rb` | 加 `wf_bad_defaults` case；黏連字串改續行 |
| `spec/fixtures/ci_violations/wf_bad_defaults/` | **新增** |
| `scripts/test-doc-claims-rules.rb` | 加 git 情境 G1／G2；黏連字串改續行；`\d+` 逃逸 |
| 本檔＋handoff | 終態層補齊與更正註記（見上） |

### Pending

8. git 情境測試依賴環境有 `git` 可執行檔：CI 必有；極端本機環境沒有時會以
   「git 情境測試自身失敗」形式紅掉（吵而不是靜默），不另做降級。
9. `defaults` 防護只涵蓋「非 Hash ⇒ 當成沒有」；`defaults.run.shell` 掛非字串
   （如列表）會流進 interpreter_for 而被當未知 shell 跳過——方向是少檢不是崩潰，
   暫不處理。
