# 2026-08-15 — 補上 workflow 的語法閘門（全倉庫最沒保護的地方）

> 來源：多代理驗收的 **Y12**，複驗成立。

---

## 已完成的工作 (Done)

### 1. 問題：一個保護真空

`.github/workflows/claude-review.yml` 有一支 **240+ 行的閉環 shell 腳本**，而：

- 🔴 **動該檔的 PR 拿不到 Claude 驗收**（反竄改機制，見該檔檔頭 31–40 行）；
- 🔴 本倉庫的 CI **沒有任何一步**解析 workflow YAML 或跑 `bash -n`；
- 🔴 該檔註釋**自己記載**被同型語法錯誤咬過**兩次**（block scalar 續行落在第 0 欄、heredoc）。

⇒ 這是全倉庫**最需要機械語法檢查、卻最沒有保護**的地方。
本輪前面幾個 PR 又往它加了約 50 行 shell（新 jq、9 參數 printf），風險只增不減。

### 2. `scripts/check-workflow-syntax.rb`

兩條規則：

1. `.github/workflows/*.yml` 每份必須是**合法 YAML**
2. 每個 step 的 `run:` 區塊必須過 **`bash -n`**（只檢查語法，不執行）

三個實作決定：

- **GitHub 表達式先換成佔位符**。`${{ ... }}` 直接送 bash 會是 bad substitution ⇒ 誤報。
  目前倉庫的 run 區塊裡**一個表達式都沒有**（實測 25 個 run 區塊、0 個），
  但第一個用它的人不該撞到假警報。
- **只送 bash 系的 shell**（未宣告＝預設 bash）。宣告 `shell: pwsh`／`python` 的跳過。
- **掃到 0 份 workflow 就 fail**，不是印 OK ——同 `check-exec-bits.sh` 的 canary，
  防「空值長得像資料」。

🔴 檔頭有**誠實聲明**：**這不是 actionlint**。不驗 action inputs 是否存在、
不驗 `${{ }}` 內的表達式語義、不驗執行期行為。
（前例就在同一份 workflow 的註釋裡：`with: model:` 在 action v1 是**未知 input，
GitHub 靜默忽略**——那一類本腳本抓不到，要靠 actionlint 或讀 action.yml。）

### 3. `scripts/test-workflow-syntax-rules.rb`（四條）

| fixture | 期望 | 守什麼 |
|---|---|---|
| `wf_clean` | exit 0 | 🔴 反向斷言，且**刻意含 `${{ }}` 與 heredoc**——兩者都是最容易誤報的形態 |
| `wf_bad_yaml` | exit 1 | block scalar 續行掉到第 0 欄（**claude-review.yml 記載的 2026-08-14 真實事故**） |
| `wf_bad_bash` | exit 1 | 🔴 **YAML 合法但 bash 壞掉**——只驗 YAML 的檢查會放行，要到 CI 跑那步才炸 |
| `wf_empty` | exit 1 | 🔴 掃不到檔案時必須 fail |

`wf_clean` 那條是雙重用途：既是反向斷言，也**證明佔位符替換有效且不誤傷 heredoc**。

**自測**：真實倉庫 exit 0（2 份 workflow、25 個 run 區塊）；四條回歸測試全過；
`rubocop` 2 files no offenses；`ruby -c config/ci.rb` OK；兩支新腳本 100755。

---

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `scripts/check-workflow-syntax.rb` | **新增**。YAML 解析 ＋ 每個 run 區塊 `bash -n`；GH 表達式換佔位符；零掃描 canary |
| `scripts/test-workflow-syntax-rules.rb` | **新增**。四條 fixture 驅動 |
| `spec/fixtures/ci_violations/wf_{clean,bad_yaml,bad_bash,empty}/` | **新增** |
| `.github/workflows/ci.yml` | quality job 加 `Check workflow syntax` 與 `Regression-test workflow syntax rules` |
| `config/ci.rb` | 同步補兩步（本機跑得到特別有價值——見下） |

---

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **這不是 actionlint，不要當成 actionlint 用**。它抓不到「未知 input 被 GitHub 靜默忽略」
   那一類——而那正是 `claude-review.yml` 註釋裡記載過的坑（`with: model:` 在 v1 無效）。
   要補那一層得引入 `rhysd/actionlint`，**本輪沒做**（新增外部依賴要先討論，鐵律 1）。

2. ⚠️ **`bash -n` 只看語法**。變數拼錯、`set -e` 的交互、管線退出碼、命令不存在——全抓不到。
   本輪前面幾個 PR 抓到的 workflow bug（`core.quotePath`、零掃描、jq 濾鏡）
   **沒有一個**是語法錯誤 ⇒ 這道閘門守的是另一類問題，不能取代那些檢查。

3. ⚠️ **本 PR 加的兩步只在 quality job**，`test` job 沒加（沒必要重複）。
   但這表示若有人只看 test job 綠就合併，是看不到這兩步的。

4. ⚠️ **沒有對 `check-workflow-syntax.rb` 做突變測試**（同 `check-exec-bits.sh` 那輪的缺口）。
   四條 case 是照已知事故設計的，不是打出來的。

5. ✅ **`config/ci.rb` 已同步**——本機跑得到這道閘門特別有價值：
   改 `claude-review.yml` 的 PR 拿不到 Claude 驗收，**本機這一步是它上線前唯一的機械檢查**。

6. ⚠️ **同批驗收剩 Y7／Y8 兩條**（`claude-review.yml` fallback 的假歸因與 null 垃圾）。
   那兩條要動 `claude-review.yml` 本身 ⇒ 該 PR 不會被驗收，
   計畫併進已開的 PR #37（同檔、同樣不可自我驗收），避免兩個 PR 改同一個檔造成衝突。
