# Worklog — P-8 補審：approve 綁定斷言更正（2026-08-19）

> 起因：使用者裁定「補審」。PR #59 合併的 head `325ce62` **從未被任何驗收方看過**
> （Codex 最後一次 review 是前一個 head `fd9753b`；本 PR 因防竄改機制零則 bot 判詞）。
> 補審的第一件事就查出一個我自己寫錯的斷言。

## 已完成的工作 (Done)

1. **更正 `claude-review.yml` 中該斷言的全部出現處**（複驗：`git grep -c -F -e '不再需要賭時間窗' HEAD -- ':/.github/workflows/claude-review.yml'`）——隨 `325ce62` 進 main、未經任何驗收方（該 commit 的輪次以 `git log --oneline 31d9337 | grep 325ce62` 查）。
   原文：「直接把核准釘在 `$HEAD_SHA` 上：head 若已前進，這則核准就屬於舊 commit，
   分支保護的 dismiss-stale 會自然忽略它，**不再需要賭時間窗**。」
   🔴 **官方文檔不支持這個推論**。`POST /pulls/{n}/reviews` 的 `commit_id` 逐字定義是
   「The SHA of the commit that needs a review. Not using the latest commit SHA may render
   your review **comment** outdated if a subsequent commit modifies the line you specify as
   the position.」（來源：docs.github.com/en/rest/pulls/reviews，取證 2026-08-19）
   ⇒ 它管的是**留言的行定位**，不是核准對分支保護的效力；而 dismiss-stale 是
   **推播事件撤銷全部核准**，與 `commit_id` 指向哪個 commit 無關。
   ⇒ **check-then-act 的競態沒有被關掉**，只是窗變小＋留下「評估的是哪個 commit」的可稽核紀錄。
2. **登記現況實測**：`gh api repos/pisceshei/chilllovesaas/branches/main/protection` 回
   **404 Branch not protected** ⇒ `main` 未設任何分支保護、**核准目前不構成閘門**，
   本項風險現在是**惰性**的；一旦啟用 required approving review 就會活過來。
3. `docs/dev/m0-automation-infra.md` §2.2 同步更正（此前該篇也照錯誤斷言描述）。

## 修改的檔案與核心邏輯 (Changes)

🔴 **本輪整體不是 docs-only**：含一條 fix（fail-open → fail-closed）與一條 feat
（`DEADLINE_S`／exit 5／`nap()`）。判準取自 Conventional Commits——type 依**改動的性質**
而非「動了哪些檔」決定，Angular 對 `docs` 的定義逐字是「Documentation **only** changes」。

<!-- 🔴 本表以 `git show <sha> --stat` 逐 commit 核對後填寫，不靠回想。
     第一輪＝badf316、第二輪＝cdc2230、第三輪＝本次提交。
     複驗：`git -c core.quotepath=false diff --name-only $(git merge-base origin/main HEAD)...HEAD`
     的每一個路徑都必須在本表出現（`core.quotepath=false` 不可省，否則中文檔名回八進位轉義）。 -->

| 檔案 | 輪次 | 內容 |
|---|---|---|
| `.github/workflows/claude-review.yml` | 1·2·3 | **1**：兩處（`AUTO_MERGE` 開／關路徑）approve 註釋更正——保留原文、標明錯在哪、附官方定義逐字與取證日期（此輪代碼行為確未動）。**2**：C3 時間預算守衛 fail-open → fail-closed，新增 `CHECKS_STATE=pending-nobudgetclock`；approve 註釋再更正。**3**：撤回「`require_last_push_approval` 對時序缺口免疫」並反向登記官方原文、補 required status check 才是真解；**把水位時間戳守衛提到評估器入口（`TS_OK`）**、C2 刪掉 `${WATERMARK_TS:-0}` 的 fail-open 預設、C3 在無時間戳時整個迴圈不進、刪除第 2 輪那個實際不可達的分支；`allowedTools` 加 `WebSearch`／`WebFetch` ＋ prompt 新增「第三類證據：外部服務語義」段。**4**（#60[5]）：`docs/dev/external-facts.md`（**PR #58**，2026-08-19 **尚未進 main**）在本分支不存在（它是 #58 的產物）⇒ 四處懸空引用改為**條件式＋三段式降級**（先 `Read`，失敗改 `git show origin/m0/p0-plan-and-laws:<path>`，再失敗就直接查官方來源並在判詞標明），並註明 #58 進 main 後可拿掉條件式 |
| `scripts/await-verdict.sh` | 2·3 | **2**：新增第 5 位置參數 `DEADLINE_S`（預設 `MAX_POLLS×INTERVAL×2`）＋新退出碼 5；非正整數走 exit 2；限流仍不消耗輪次。**3**：預算宣告從主輪詢迴圈上方**移到第一個可能等待的動作之前**（起跑重試與限流等待原本全在預算外）；新增 `nap()`（夾到 `min(想睡, 剩餘)`）與 `deadline_exit()`；三處裸 `sleep` 全改走 `nap`；`wait_for_reset` 夾到剩餘預算且夾斷即 exit 5（不得繼續發請求）；退出碼表補記行為變更。**4**：#60[3] `DEADLINE_S` 補前導零拒絕（`08`／`09`／`00`／`010`／`0900` 五形實測全部改回 exit 2；🔴 實測證明 Codex 把後果講輕了——放行後算術展開失敗只讓 `$(deadline_left)` 成空字串、下游比較全部 `integer expected`，於是**deadline 被靜默關掉**、跑到 exit 4，不是它說的「中止」）；#60[4] 檔頭簽名＋預設值句＋缺參數 usage 補第 5 參數；#60[2] 退出碼表與 `deadline_exit` 訊息**契約誠實降級**為「只約束等待（sleep），不含 API 呼叫耗時」 |
| `docs/dev/m0-automation-infra.md` | 1·2·3 | **1**：§2.2 補「approve 綁 commit 的真實效力」段。**2**：補 C3 兩個新狀態與 exit 5 的契約。**3**：C2 狀態補 `no-watermark-ts`；C3 的 `pending-nobudgetclock` 改為 `skipped-no-watermark-ts` 並記錄舊分支為何不可達；exit 5 補起算點變更、`nap()` 語義與已知殘留。**4**：#60[4] 操作命令補第 5 參數；#60[2] 契約範圍同步降級（與腳本第 45／159 行同義） |
| `docs/handoff/2026-08-18-P8-自動化基建.md` | 2·4 | **2**：§① approve 前 head 複驗改標 best-effort，指向 `docs/dev` §2.2 與本 worklog。**4**（#60[1]）：§① 仍把「`--match-head-commit` ＋尚未啟用的 `require_last_push_approval`」寫成真正的閘門——那是第三輪已證偽的方案，就地改為 required status check 的現值，**並帶「尚未實作」**（不帶就是把第三輪的錯誤形態原樣複製一次：拿未落地的機制當「真正的閘門」） |
| `docs/worklog/2026-08-19-P8補審-approve綁定斷言更正.md`（本檔） | 1·2·3 | **1**：新建。**2**：追加「補審第二輪」節。**3**：追加「補審第三輪」節；Changes 表依 git 實物重建（原表缺 `await-verdict.sh` 與 handoff 兩列、且把整輪寫成「代碼行為未動」）；第二輪的 `require_last_push_approval` 斷言加撤回註。**4**：追加「補審第四輪」節；Changes 表補第 4 輪；Pending 第 1 項改寫（數量級由「一次網路呼叫」更正為 22500 秒並記處置＝契約降級）、第 2 項結案劃線 |

**自測**：`ruby` YAML parse OK；全部 run 區塊 `bash -n` OK；run 區塊零 `${{ }}`；
全閘門 selector FAIL=0。複驗斷言：`gh api repos/OWNER/REPO/branches/main/protection`（應回 404）。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **競態未關閉**：核准與評估之間仍有時間窗。真正的關法有兩條——①啟用分支保護的
   dismiss-stale ②不在合併前送核准（把核准移到合併動作本身）。**兩者都屬 18.4 啟用裁定
   的範圍，本輪不動**（`AUTO_MERGE` 仍 `"false"`，核准目前只是訊號）。
2. **`fd9753b..325ce62` 其餘改動仍未經第三方審查**：C3 時間預算（`JOB_BUDGET_S=1320`）、
   退出碼契約補正、兩處 doc-claims 觸發源。已在 PR #59 貼出補審請求（若 Codex 不受理
   已合併 PR，則由本 PR 的審查涵蓋——本 PR 含同一段代碼的上下文）。
3. **本 PR 改 `.github/workflows/` ⇒ 依防竄改機制不會有 bot 判詞**，且依鐵律 18.3 一律人工合併。

## 補審第二輪（2026-08-19；#59 對合併提交 4 條＋#60 3 條，**先上網研究再修**）

🔴 **上一版的修正本身又被推翻一次。** 我把「`commit_id` ⇒ dismiss-stale 會忽略舊核准」
更正掉，卻在改寫時寫下「真正關掉要靠 dismiss-stale」——**那個也是錯的**，而且同樣沒查證。

**研究結論（取證 2026-08-19，來源逐條入註釋）**：
- **核准端不存在原子解法，這是 GitHub 的刻意設計**：head 前置條件原語只掛在合併
  （REST merge 的 `sha`「must match to allow merge」＋**409**、GraphQL `expectedHeadOid`、
  `gh pr merge --match-head-commit`），**一個都沒掛在 approve**；`commit_id` 是
  「the commit the review **pertains to**」，端點狀態碼只有 200/403/422；POST 不支援條件式請求。
- **dismiss-stale 押不得**：官方 API 層是事件式（`_on_push`）、概念層是狀態式
  （「records the state of the diff…」），**求值時機從未公布 ⇒ 未取得**。
- ✅ **真正的解**＝`require_last_push_approval`（純狀態謂詞、作為**合併閘門**求值，
  不依賴撤銷是否及時發生 ⇒ 對時序缺口免疫）＋合併時帶 SHA。⚠️ 前者**尚未啟用**
  （main 無分支保護，實測 404），屬 18.4 裁定範圍。
  <!-- 🔴 2026-08-19 補審第三輪更正（原文保留，見下方「補審第三輪」節）：這一條**也是錯的**，
       方向還與官方敘述相反。官方逐字「with this option, "stale" reviews are not dismissed」
       ＋「If you are concerned about pull requests being "hijacked"…it is safer to dismiss
       stale reviews」⇒ 該規則明文容忍核准指向舊 commit，官方自承它在此情境比 dismiss-stale 弱。
       真正關得掉的是 required status check（依構造綁 latest commit SHA）。 -->
- **業界依據不是推測**：GitHub 官方 Dependabot 自動核准教學就是裸的
  `gh pr review --approve "$PR_URL"`、**零 head 複驗**——大家都不守核准那一步，因為守不住。

**本輪改動（七條點名，逐條對應）**：
- #59①／#60①：兩處斷言改寫為上述實據，並標明「head 複驗保留但是 best-effort」。
- #59③：主輪詢的限流**無界**（`i=$((i-1))` 只是軟界，起跑那個 `RATE_WAITS` 上限**主迴圈沒有**）
  → 加**總時鐘上限** `DEADLINE_S`＋**新退出碼 5**。形狀取自 gRPC A6 的 deadline
  「applies across all attempts」；**不採**次數上限（一次限流可差兩三個數量級 ⇒ 界不可預測）、
  **不採** retry budget／circuit breaker（單機單請求，重試放大不存在）、**不改**等待邏輯
  （現行「等到伺服器指定時點」比自選退避更合官方指引）。
- #59④：`WATERMARK_TS` 缺失時預算守衛被**停用**（fail-open）→ 改 fail-closed：
  取不到就當預算用盡、記 `pending-nobudgetclock` 並停等。
- #59②：docs/dev 補 C3 兩個新狀態與 exit 5 的契約。
- #60②：handoff §① 同 commit 回寫。#60③：裸數字改複驗指令。

## 補審第三輪（2026-08-19；#60 四條點名，**先派研究上網查證再修**）

🔴 **第二輪寫下的「✅ 真正的解＝`require_last_push_approval`」又被推翻——這是同一個
斷言連續第三次寫錯。** 前兩次錯在憑推論寫肯定句，這一次錯在**方向與官方敘述相反**。

**研究結論（4 個代理、183 次工具呼叫，取證 2026-08-19）**：

- **[1] 核准競態**：主張成立。官方在 branch protection 與 rulesets **兩頁逐字重複**：
  「with this option, "stale" reviews are **not dismissed**」——該規則的設計本意就是
  **容忍核准指向舊 commit**；同段結尾更直說「**If you are concerned about pull requests
  being "hijacked"**…**it is safer to dismiss stale reviews**」⇒ 官方自承它在本情境
  **比 dismiss-stale 弱**。⚠️ 它的判準（比 `submitted_at` 還是比 review 綁的 commit）
  **官方從未公布**（窮舉 docs 四頁＋兩則 changelog＋GraphQL schema）⇒ 兩種讀法都不得押注。
  🔴 但驗收方只說「這個也不成立」而沒給替代方案，照做會停在「無解」——**其實有解**：
  官方逐字「Required checks must pass on **the latest commit SHA**. **Checks from earlier
  commits don't satisfy the requirement.**」⇒ **required status check 依構造綁 commit
  且 fail-closed**，這正是核准做不到的事。已寫進註釋（含「不得直接把現有 `review` check
  當 required」——它的 success ≠ 驗收通過）。
- **[2] deadline 沒有真的有界**：三條全中，且量到反例——`DEADLINE_S=1`、`INTERVAL=20`
  實測 **21 秒**才 exit 5；預設值下最壞牆鐘約 11.8 小時 vs 檔頭宣稱的 4 小時。
  🔴 **修法形狀與驗收方描述的不同**：它寫「只在每輪開始時檢查」，最直覺的修法是加檢查點
  ——**那一秒也省不下來**，根因是**睡眠時長沒有被剩餘預算約束**，正解是 clamp。
  🔴 另一處若照字面修會踩官方紅線：夾斷 `wait_for_reset` 後若 `continue`，等於在還沒等到
  reset 時繼續發請求＝官方明文警告的封鎖條件 ⇒ 夾斷一律 exit 5。
  ⚠️ 研究另指出驗收方**漏了第四個洞**（單次 `fetch_state` 自身不受預算約束）——
  **本輪未修**（未被點名），已在檔頭與 `docs/dev` 明寫為已知殘留。
- **[3] Changes 表**：主張成立。原表把整輪寫成「代碼行為未動」，而第二輪確實改了代碼；
  另缺 `scripts/await-verdict.sh` 與 `docs/handoff/…` 兩列。已依 `git show --stat` 逐
  commit 重建。🔴 研究建議的機制化（加 CI 規則驗「該寫的有沒有寫」）**不採**——
  使用者已裁定不再機制化；改以表上方的複驗指令留痕。
- **[4] fail-closed 分支不可達**：主張 **PARTIAL**——「根本不可達」字面不成立
  （first-query 回 `pending` 時確實會執行），但要害那一半完全成立：
  `[ "$CHECKS_STATE" = "pending" ] || break` 讓 `allgreen` 第一輪就 break ⇒ 守衛零覆蓋。
  C3 的狀態共七個：`empty`／`pending`／`allgreen`／`notgreen`／`apifail`／
  `pending-timebudget`／`skipped-no-watermark-ts`，其中**只有 `allgreen` 令 C3=1**
  （複驗：`git grep -n -F -e 'CHECKS_STATE" = "allgreen" ] && C3=1' -- ':/.github/workflows/claude-review.yml'`），
  而 `allgreen` 正是那個 `|| break` 第一輪就跳出的分支。
  配上 C2 的 `${WATERMARK_TS:-0}` 退成 0（jq 實測 CAND 由 0 變 1，時間窗變 no-op），
  交叉表實測復現 `1111 → APPROVE`。
  已改為評估器入口統一前置（`TS_OK`），並刪除變成真正死碼的舊分支。

**自測（本輪實跑）**：workflow YAML parse OK；全部 run 區塊 `bash -n` 通過，且對
`shcheck.py` 做過**突變驗證**（注入未閉合 `if` ⇒ 確實轉紅，證明這道檢查有判定力）；
`await-verdict.sh` `bash -n` OK；`nap()` 行為測試——預算剩 3 秒時 `nap 100` **實測 3 秒**
返回且回 1（修前同型情境睡滿），`deadline_exit` rc=5。

## 尚未完成或需注意的風險（第三輪追加）

1. 🔴 **`fetch_state` 單次呼叫仍不受預算約束**（#60[2] 再次點名；本輪採「契約降級」而非實作）：
   3 支分頁端點 × `HARD_PAGE_CAP`(100) × 每次 `get()` 上界 75 秒（gh 45s 逾時後再回退匿名 30s）
   ⇒ 誤差上界 **22500 秒 > 預設預算 14400 秒**（原估「一次進行中的網路呼叫」低估約 300 倍）。
   **本輪處置＝把契約明文降級為「只約束等待（sleep）」**（腳本第 45／159 行、`docs/dev` 已同步），
   非實作收緊。待辦形狀（gRPC 型 deadline 傳遞）：fetch_state 加傳剩餘預算 → python 端算絕對
   deadline → 每個 `subprocess.run`／`urlopen` 的 timeout 取 `min(既有值, 剩餘)` → 三個 pager
   頁間檢查 → 夾斷回新哨兵 `DEADLINE`，bash 端 `DEADLINE*) deadline_exit;;`（🔴 **不得 continue**——
   夾斷時還沒等到 reset，再發請求即 GitHub 明文警告的封鎖條件）。
   ⚠️ `gh` 無 timeout 旗標（實測 gh 2.97.0），只能靠 python 端 subprocess timeout。
2. ~~**`await-verdict.sh` 三處用法字串仍寫四個參數、未列 `DEADLINE_S`**~~
   ——**已於本輪（#60[4] 點名）修畢**：`scripts/await-verdict.sh` 第 23–24 行與第 79 行、
   `docs/dev/m0-automation-infra.md` 第 133 行三處同步補上第 5 參數與其預設值。
3. **required status check 只寫進註釋，未實作**：真正關掉核准競態需要
   ①建立 `verdict/four-conditions` check-run ②設為 required ③main 目前**無任何分支保護**
   （帶認證實測回 404；🔴 未帶 token 一律 401，複驗必須帶 token）。屬 18.4 啟用裁定範圍。
4. **`require_last_push_approval` 的判準未取得**：官方從未公布比的是時間戳還是 commit。
   要升級成已證實，最小實驗需在**另一個測試倉庫**做（不得動本倉庫），屬使用者授權範圍。

## 補審第四輪（2026-08-19；#60 Codex 五條，**十條意見一次派五個代理查證**）

> 本輪與 #58 第 36 輪同批處理，研究一次派了 7 個代理／325 次工具呼叫。

### 五條逐條

- **[1] handoff 終態層沒跟上**：成立。第三輪在 workflow 與 worklog 撤回了
  `require_last_push_approval`，但 `docs/handoff/2026-08-18-P8-自動化基建.md` §① 仍寫它是真正的閘門。
  ⚠️ **研究校正了 Codex 兩處不精確**：①它說「已在 workflow、docs/dev 與 worklog 撤回」——
  **docs/dev 沒有**，該篇從頭到尾沒提過這個旗標；②它沒說 required status check **尚未實作**，
  照它字面寫成肯定句就會把第三輪的錯誤形態原樣複製一次。⇒ 替換文字必須帶「尚未實作」。
- **[2] deadline 仍不涵蓋 fetch_state**：成立，**而且比 Codex 講的嚴重**。實測算式：
  3 支分頁端點 × `HARD_PAGE_CAP`(100) × 每次 `get()` 上界 **75 秒**
  （gh 子行程 45 秒逾時後被 `except Exception: pass` 吃掉，**再往下跑匿名 urllib 30 秒**）
  ⇒ 單次 `fetch_state` 理論上界 **22500 秒**，比預設預算 14400 秒還大 1.56 倍。
  🔴 **本輪已登記的「已知殘留」有兩個硬問題**：①數量級寫錯（原寫「一次進行中的網路呼叫」，
  低估約 300 倍）②**位置錯**——埋在一段講「預算宣告位置」的註釋內部，而契約寫在退出碼表，
  讀契約的人不會讀到 90 行外的但書。Codex 讀了契約沒讀到但書，正好證明那個位置無效。
  ⇒ **採 (b) 契約誠實降級**，不採 (a) 實作收緊：Codex 自己把 (b) 列為合格解，
  說明它的抱怨是**契約與行為不符**而非缺少能力；鐵律 17.2 要最小者。
- **[3] `DEADLINE_S` 的八進位歧義**：**PARTIAL——Codex 把後果講輕了**。
  它說會「以 `value too great for base` 中止」，實測反證：**根本不會中止**。
  算術錯誤只讓 `$(deadline_left)` 展成空字串，下游三個比較全部因 `integer expected` 而 rc≠0，
  於是 `&&`／`||` 兩側分支都不觸發 ⇒ **nap() 不夾斷、sleep 睡滿、deadline 完全不生效**，
  一路跑到 exit 4。這比中止更嚴重——中止至少看得見，現況是把 `DEADLINE_S` 這個界
  **靜默關掉**（該用語出自腳本自身的立法註，查法：`grep -n "有界終止" scripts/await-verdict.sh`），外觀只是一堆 stderr 噪音加一個看似正常的 exit 4。
  🔴 **不採 Codex 的第二個選項（算術加 `10#`）**：實測 `10#00` = 0 ⇒ `DEADLINE_S` 變 0 ⇒
  仍然立刻 exit 5，`10#` 修不掉 `00`，還得再加一個非零檢查。拒絕前導零一行同時關掉五形。
  實測驗證：`08`／`09`／`00`／`010`／`0900` 全部 exit 2，`900` 正常接受。
- **[4] 用法字串未列第 5 參數**：成立，**而且本輪 worklog 的 Pending 第 2 項已經逐字登記過它**，
  連「三處」的拆法都一樣，押後理由寫的是「本輪未修，因未被點名」——**這輪被點名，押後理由自動失效**。
  ⚠️ 研究另指出第 23 行只是簽名、第 24 行是預設值句，只改 23 會讓同一個「用法」區塊自相矛盾
  ⇒ 兩行視為同一處一起改。
- **[5] prompt 引用了本分支不存在的檔**：成立，**Codex 還低估了一處**——
  `scripts/check-doc-claims.rb` 的 `IN_SCOPE` 不含 `.github/workflows/`，所以那四處懸空引用
  **連 R1 都掃不到**，不是「CI 沒紅是因為還好」，是**結構上永遠不會紅**。
  🔴 **不採把 `external-facts.md` 複製進 #60**：合併基礎上不存在該路徑 ⇒ add/add 衝突幾乎必然；
  更根本的是**語義**——該檔開頭自訂的第 1 條使用規則預設它**只有一份**，複製成兩份等於製造
  兩個都自稱權威、各自漂移的版本，正是它要防的東西。⇒ 採條件式，並把「#58 進 main 後可拿掉」
  寫在原地當到期條件。另依 Anthropic 的 reduce-hallucinations 建議，把**降級行為**寫進 prompt
  （不是只標錨定），否則驗收方讀到「該檔不在這裡」之後不知道要做什麼。

### 自測（本輪實跑）

`bash -n` 通過；workflow YAML parse OK；全部 run 區塊 `bash -n` 通過；
`DEADLINE_S` 前導零五形逐一實測（`08`／`09`／`00`／`010`／`0900` → exit 2，`900` → 接受）；
全閘門 FAIL=0；doc-claims commit 前後各跑一次、警告 0。

### Pending（第四輪追加）

- **(a) 實作收緊未做**（見上方 Pending 第 1 項的待辦形狀）——本輪只降級契約。
- 🔴 **`IN_SCOPE` 不含 `.github/workflows/`** ⇒ workflow 內的路徑引用永遠不會被 R1 掃到。
  這是結構缺口，但**未被點名**（Codex 只說引用懸空，沒說檢查器盲）⇒ 依鐵律 17.2 不動，登記待裁。
