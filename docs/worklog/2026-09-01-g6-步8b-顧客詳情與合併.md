# 2026-09-01 G6 步 8b：顧客詳情頁＋合併＋對帳

## 已完成的工作 (Done)

- Customers::Merge（保留規則官方對位＋三類 blockers＋附屬四搬移＋note/tags 官方
  上限＋統計由訂單重算）；customerMerge mutation。
- 顧客詳情頁 `/admin/customers/:id`（74 §4＋步 8a 實測 §5 對位）：KPI 四格／
  最近訂單卡／聯絡卡三 modal（contact 含 locale／marketing 勾選走 consent
  mutation／地址唯讀展示）／稅務／標籤／備註／More actions 四動作；列表列點擊
  導覽接上。
- customers.locale 欄＋create/update/type 佈線。
- ReconcileStatsJob（recurring 每日 4am；漂移 log＋修正）。
- 突變輪 6/6 殺＋canary：保留規則/blockers/統計相加/consent 不搬/對帳只 log/
  先寫後刪回歸。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260901080000_add_locale_to_customers.rb`＋schema
- 新：`app/services/customers/merge.rb`、`app/jobs/customers/reconcile_stats_job.rb`、
  `app/graphql/mutations/customer_merge.rb`、
  `app/frontend/admin/pages/CustomerDetailPage.tsx`（＋test）
- 改：customer_create/update（locale）、customer_type（locale）、mutation_type、
  `config/recurring.yml`、`App.tsx`（detail 路由）、`CustomersPage.tsx`（列導覽）、
  admin.css（KPI/雙欄）、i18n ×5（+57 鍵）
- 🔴 實紅教訓：merge 首版「先寫保留方再刪被併方」撞 phone uq 索引（G4 格抓到）
  ⇒ 序改「先算→刪→寫」＋MB6 回歸突變釘住。
- 測試：`spec/services/customers/merge_spec.rb`（5 例）＋
  `spec/jobs/customers/reconcile_stats_job_spec.rb`（1 例）＋
  customer_mutations_spec 追加 2 例＋FE 3 例

## 尚未完成或需注意的風險 (Pending / TODO)

- RFM 群組＝KPI 佔位「—」（分析線步 10 承接）；時間軸 composer／寄送 email／
  商店抵用金／Request customer data／建立訂單 ⚪（91 §3.53）。
- merge 的八類官方 blockers 對應子系統未建（gift cards/store credit/subscriptions/
  company/payment methods/multipass/async job/override）——子系統落地時必須回來
  補 blocker（91 §3.53 首條）。
- 地址管理 modal（增刪改+設預設 UI）後端四支已在 8a；詳情頁 v1 只展示預設地址，
  編輯 UI 隨下輪補（91 §3.53）。
