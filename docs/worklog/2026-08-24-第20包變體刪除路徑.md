# worklog：第 20 包（變體刪除路徑裁定與落地）

## 已完成的工作 (Done)

- **B1 方案② 落地**（整合規格 §5；RESTRICT＋應用層單一路徑）：
  - migration：`inventory_items.product_variant_id` 可空化＋`variant_deleted_at` 稽核欄
    （複合 FK 帶 NULL 依 MATCH SIMPLE 不檢查＝合法孤兒；unique 索引多 NULL 不衝突
    ——與 `event_outbox.dedupe_key` 同一 MySQL 依賴）。dev/test 兩庫實跑。
  - `Catalog::DeleteVariant`：guard（`product_min_variants` 引 limits）→ 同 transaction
    ①line_items 單欄置 NULL（欄本來可空；快照欄獨立成立）②media 置 NULL
    ③選項座標刪除 ④item 孤兒化 ⑤variant destroy。line_items/media 零 model
    ⇒ sanitized SQL，不越界建 model（17.2）。
  - 🔴 **`has_one :inventory_item` 的 `dependent: :destroy` 移除**＋
    `before_destroy :orphan_inventory_item!` 全域不變量——dependent 連鎖走**記憶體
    快取關聯**，先置 NULL 都繞不過（rspec 抓到）；改成任何 destroy 路徑自動孤兒化。
  - locale 五語補 `last_variant_required`。
- **釘板 v2**：`inventory_baseline_spec` 的「fail-closed 至第 20 包」如其自述轉紅，
  改寫成 B1 新語義（有 ledger 商品可刪且 ledger 保留；location 仍擋）。
- spec 5 例新增；注入回歸兩件（拿掉孤兒化步驟 ⇒ 5 紅／guard 恆放行 ⇒ 1 紅；
  還原＝反向字串替換，遵守整合規格 §8-1）。
- 全套：rspec **487/487**、rubocop 0、brakeman 0、vitest 60/60、typecheck、
  tokens-sync、lint-prototype ERROR 0。

## 修改的檔案與核心邏輯 (Changes)

| 檔 | 變更 |
|---|---|
| `db/migrate/20260824150000_allow_orphan_inventory_items.rb` | 新建 |
| `app/services/catalog/delete_variant.rb` | 新建（刪除路徑；複驗全庫呼叫面 `grep -rn destroy! app/services`） |
| `app/models/product_variant.rb` | dependent 移除＋before_destroy 孤兒化 |
| `config/locales/*.yml`（五語） | `last_variant_required` |
| `spec/services/catalog/delete_variant_spec.rb` | 新建 5 例 |
| `spec/models/inventory_baseline_spec.rb` | 釘板 v1→v2 |

## 尚未完成或需注意的風險 (Pending / TODO)

- 對外 mutation（`productVariantsBulkDelete` 類）不在本包——第 22 包宣告式 diff 的
  刪除分支是第一個呼叫端。
- 商品刪除（productDelete）仍無服務層路徑；`before_destroy` 回呼讓 model 級連鎖安全，
  但正式商品刪除包要補 media/redirect 連動——登記。
- 孤兒 item 的後續清理策略（保留多久／是否進 purge）未裁——登記，屬稽核保留政策。
