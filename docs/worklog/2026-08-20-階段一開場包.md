# 階段一' PR-1 開場包

## 已完成的工作 (Done)

- 依 `docs/plans/2026-08-20-階段一執行方案.md` §0.1 完成開工前提驗證；本輪快照為
  `origin/main` = `d9e6a45`、鐵律最大編號 = `18`，三個必備路徑皆存在。
- 將階段一'執行細則落入 `docs/plans/`，並依 §1 回寫伺服器解凍、Playwright 依賴、
  本階段射程與代行合併授權。
- 用 `git log -p` 追查 D25–D27 與總方案沿革後新增 D28–D31；D31 明確保留鐵律 18.3／17.3
  停點，不恢復 workflow 自動派修，也不翻 `AUTO_MERGE`。
- 以雙向集合比對核對附錄 A，補列 main 已追蹤但清單未登記的路徑，並同步列入本包新增的
  worklog 與 handoff。
- Markdown 結構自查發現總方案 §2.6 的既有表格欄位不齊；該段不在 PR-1 射程，已依鐵律 17.2
  登記到 `91` §3.5，未順手修正文。

## 修改的檔案與核心邏輯 (Changes)

- `docs/plans/2026-08-20-階段一執行方案.md`：階段一'的執行細則與自動化迴圈。
- `docs/DECISIONS.md`：現值新增 D28–D33；D32 補正互動式實作／代行邊界，D33 立零假設發布。
- `CLAUDE.md`、`AGENTS.md`：同步 D32 的互動式 Codex 窄例外與鐵律 19；PR #61 仍命中
  18.3，雙清或 workflow 證據不可得時都只走使用者獨立人工審核，不代行合併。
- `docs/plans/2026-08-18-總方案.md`：更新 CD 解凍狀態、首批序列與 §十二裁定狀態。
- `docs/specs/91-pit-register.md`：修復附錄 A 與已追蹤 worklog／handoff 的集合差異，並把
  Markdown 結構自查發現的範圍外問題登記於 §3.5。
- 本 worklog 與 `docs/handoff/2026-08-20-階段一開場包.md`：形成 PR-1 的工作記錄與交接。
- 後續 `docs/worklog/2026-08-20-PR61-*`、配對 handoff 與鐵律 19 三件套：記錄 PR #61
  各輪驗收、證據更正與終態規範；逐檔以 `91` 附錄 A 對帳。

## 尚未完成或需注意的風險 (Pending / TODO)

- PR-1 尚須跑完本地閘門、GitHub CI、Claude bot 與 Codex 當前 head 驗收；舊 head 結論不可沿用。
- P-8 單元二只有在本 PR 自然取得倒計時、格式驗證與評估器 `1111` 留言後才能登記收口證據。
- CD-1 開工時仍需取得 D28 列明的伺服器連線與部署環境輸入；任何私鑰或
  `RAILS_MASTER_KEY` 都不得寫入對話或倉庫。
- 總方案 §2.6 的既有表格欄位不齊已登記於 `91` §3.5；它不是 PR-1 的修復項，後續須另包處理。
