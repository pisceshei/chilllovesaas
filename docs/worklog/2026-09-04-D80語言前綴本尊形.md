# 2026-09-04 D80 方案 1：語言 URL 前綴、市場 cookie、hreflang 改回本尊形

配對 handoff：`docs/handoff/2026-09-04-D80語言前綴本尊形.md`。規範：`docs/dev/d80-url-structure.md`；裁定：`docs/DECISIONS.md` D80 末段；
取證：`docs/dev/external-facts.md` §G23。

## 已完成的工作 (Done)

- 使用者裁定 D80 方案 1（回覆「1」）後，把 2026-08-13「恆帶地區、恆有前綴、hreflang 逐國展開、根路徑 302」整組換成本尊形：
  共用網域預設語言無前綴、其他語言 `/{小寫語言碼}`、子資料夾市場 `/{語言碼}-{suffix}`；根路徑與無前綴路徑直接渲染；前綴查無 ⇒ 整條路徑當
  無前綴頁面；共用主網域上沒有自己 presence 的市場由 `localization` cookie（買家在 `{% form 'localization' %}` 選國）覆寫，語言仍只由 URL 決定；
  hreflang 共用網域一語言一碼、x-default 指無前綴 URL；`/localization` 依本尊三形（country／language／both）302＋Set-Cookie。
- 鏡像店同步本尊形：`hoko.json` 四個非主市場移除 `suffix`；`RenderParity::Mirror` 對無 suffix 市場不建 presence，並拆掉 E15 期建的 subfolder presence。
- 規範回寫：67 §F.1(b)(b-2)(c)(d)、62 §I.1／§I.2／§I.2-1／§I.2-2 註／§O REG-3／REG-4／limits 表、`config/limits.yml` `i18n.locale_prefix.*`／
  `seo.hreflang.*`（改名不留別名，沿革在鍵旁）、e8 §3／§4、e13 註、91 §3.84、DECISIONS D80、external-facts §G23、新 `docs/dev/d80-url-structure.md`。
- 規格：U1–U8／H1–H5／DH1–3＋DR1＋BC1／BC2／P5／M9／MR1–MR4／LC5／SF-*／L1–L4＋L3b／S1／S2／S4／S8／SEO2／SEO6／E1–E3／G2／PV2b／SG1；
  其餘 request spec 的 `/en-hk/…` 改 `/…`、`/zh-hant-hk/…` 改 `/zh-hant/…`（複驗：`git diff 7d60fd7b -- spec | grep -c "^-.*en-hk"`）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `app/services/markets/url_prefix.rb` | 本尊形前綴（空／裸語言段／子資料夾段）；`SEGMENT` 地區段可選；不看 regions |
| `app/services/markets/prefix_index.rb` | `Hit` 加 `country_code`／`effective_country_code`；`with_buyer_country`、`shared_market_for_country`、`prefix_segments`；查詢全在租戶脈絡 |
| `app/services/markets/hreflang_codes.rb` | `for_presence`（共用網域語言碼、子資料夾逐國）、`language_code` |
| `app/services/seo/hreflang_matrix.rb` | 改呼叫 `for_presence`；x-default 註 |
| `app/controllers/storefront/base_controller.rb` | `buyer_country_code`（cookie）、`apply_buyer_country` 套進 `locale_hit`／`default_hit` |
| `app/controllers/storefront/pages_controller.rb` | `root` 直接渲染；`show` 前綴查無 ⇒ 整條路徑；`serve`；renderer 帶 `market`／`country_code` |
| `app/controllers/storefront/localization_controller.rb` | 三形處置＋cookie（path＝語言根路徑、一年、Lax）；只剝命中前綴；語言保留當前 |
| `app/controllers/storefront/{cart,comments,search,recommendations}_controller.rb` | 前綴一律來自 `effective_hit`；cart `strip_locale_prefix` 只剝命中前綴 |
| `app/liquid/theme_engine/{page_renderer,runtime}.rb` | `market:`／`country_code:` 貫穿；`Shopify.country` 讀買家選國 |
| `app/services/storefront/localization_context.rb` | `drop(market:, country_code:)`；`root_url_for`（空前綴 ⇒ `/`） |
| `app/services/url_redirects/normalize.rb` | `prefixed?` 以本店真實前綴集合判定 |
| `app/services/render_parity/mirror.rb`、`spec/fixtures/render_parity/hoko.json` | 無 suffix 市場不建 presence、拆舊 presence；移除 suffix |
| `config/routes.rb`、`config/initializers/rack_attack.rb` | 前綴段正則地區可選 |
| `config/limits.yml` | `i18n.locale_prefix.*`／`seo.hreflang.*` 改鍵 |
| `app/frontend/admin/pages/SettingsRedirectsPage.tsx` | 註釋例子 |
| docs | 67／62／e8／e13／91／DECISIONS／external-facts／`docs/dev/d80-url-structure.md` |
| specs | 見 Done 段；新增 DR1／BC1／BC2／L3b／LC5，改寫 U／H／MR／P5／M9／S／SEO／G2／PV2b／SG1 |

### 閘門（凍結 tree 後逐支親眼看退出碼；runner＝`tmp/gates_e15.py`，Git Bash 非 login shell，`PYTHONIOENCODING=utf-8`）

跑於 2026-09-04、凍結 tree（本表以外無其他改動；填表後只重跑 Doc claims 兩支）。全套 rspec 在閘門內 exit=0
（凍結前另跑一次：`bundle exec rspec` 2153 examples, 0 failures，複驗指令同）。

| 步驟 | 指令 | 退出碼 | 秒 |
|---|---|---|---|
| Setup | `bin/setup --skip-server` | 1（Windows `bin/setup` 的 `Errno::ENOEXEC`——`system("bin/rails db:prepare")` 在 Windows 不可執行；bootstrap 步驟、非判準，同 E14b／E15） | 2 |
| Style: Ruby | `bin/rubocop` | 0 | 11 |
| Security: Gem audit | `bin/bundler-audit` | 0 | 7 |
| Security: Frontend audit | `pnpm audit --audit-level high` | 0 | 2 |
| Security: Brakeman code analysis | `bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error` | 0 | 275 |
| Test: Rails | `bundle exec rspec` | 0 | 394 |
| Test: Frontend | `pnpm test` | 0 | 70 |
| Type check: Frontend | `pnpm typecheck` | 0 | 2 |
| Build: Frontend | `pnpm build` | 0 | 2 |
| Invariants: Exec bits | `bash scripts/check-exec-bits.sh` | 0 | 0 |
| Invariants: Exec bit rules regression | `bash scripts/test-exec-bits-rules.sh` | 0 | 8 |
| Invariants: Prototype lint | `python scripts/lint-prototype.py` | 0 | 1 |
| Invariants: Lint rules regression | `python scripts/test-lint-rules.py` | 0 | 0 |
| Invariants: Dead-control baseline | `python scripts/check-baseline-raise.py` | 0 | 0 |
| Invariants: Doc claims | `ruby scripts/check-doc-claims.rb` | 0 | 1 |
| Invariants: Doc claim rules regression | `ruby scripts/test-doc-claims-rules.rb` | 0 | 14 |
| Invariants: Tenant isolation | `ruby scripts/check-tenant-isolation.rb` | 0 | 0 |
| Invariants: Design token single source | `ruby scripts/check-tokens-sync.rb` | 0 | 0 |
| Invariants: Reversal naming | `ruby scripts/check-reversal-naming.rb` | 0 | 2 |
| Invariants: Money unit boundary | `ruby scripts/check-money-boundary.rb` | 0 | 1 |
| Invariants: Money rules regression | `ruby scripts/test-money-rules.rb` | 0 | 4 |
| Invariants: Limits key types | `ruby scripts/check-limits-keys.rb` | 0 | 0 |
| Invariants: Limits key rules regression | `ruby scripts/test-limits-key-rules.rb` | 0 | 6 |
| Invariants: Rem token integrity | `ruby scripts/check-rem-tokens.rb` | 0 | 0 |
| Invariants: Rem token rules regression | `ruby scripts/test-rem-token-rules.rb` | 0 | 2 |
| Invariants: CI parity (ci.yml ⊆ config/ci.rb) | `ruby scripts/check-ci-parity.rb` | 0 | 0 |
| Invariants: CI parity rules regression | `ruby scripts/test-ci-parity-rules.rb` | 0 | 4 |
| Invariants: Workflow syntax | `ruby scripts/check-workflow-syntax.rb` | 0 | 2 |
| Invariants: Workflow syntax rules regression | `ruby scripts/test-workflow-syntax-rules.rb` | 0 | 4 |

### 突變（commit 後跑 scratchpad `mutate_d80.py`；NO-TALLY fail-closed；每格改一處 → 目標規格轉紅 → `git checkout --` 還原）

M1 預設語言加回前綴／M2 共用網域 hreflang 逐國展開／M3 根路徑退回 302／M4 cookie 不覆寫市場／M5 子資料夾 hit 被 cookie 拉走／
M6 `/localization` 不寫 cookie／M7 Mirror 仍建 subfolder presence／M8 重導驗證只看形狀／M9 routes 地區必填／M10 x-default 指帶前綴 URL／
M11 `language.root_url` 預設吐空字串／M12 `Shopify.country` 忽略買家選國。結果表登記於 PR body（commit 後才跑，不為登記刷新 head；17.4）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.84 的 V：子資料夾／自有網域市場的本尊前綴與 hreflang 形、每語言一組 sitemap 子表、`language.root_url` 本尊值、country-only 提交的語言處置、
  不屬任何市場的國碼、`pt-BR` 類前綴。
- ⚪ 舊形 URL（`/en-hk/…`、`/zh-hans-tw/…`）一律 404、無 301；⚪ 市場由 cookie 決定但回應未加 `Vary: Cookie`（市場獨立幣別落地前必須處理）。
- bt3 部署後須跑 `render_parity:mirror[mirror]`（拆掉 bt3 mirror 店的四個 subfolder presence）再清 cache；公開 mirror 站 URL 由
  `/zh-hans-tw/` 改為 `/`（E15 handoff 的對表指令要改 `CAND_BASE=https://mirror.chilling.com.hk`、不帶 `CAND_PREFIX`）。
