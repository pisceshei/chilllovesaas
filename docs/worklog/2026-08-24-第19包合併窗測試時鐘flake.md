# 2026-08-24 第 19 包合併窗測試的時鐘 flake 修復

對應規格：第 19 包執行規格 §4.5（合併窗）；63 §C.6。

## 已完成的工作 (Done)

- 修復 `producers_spec.rb` 合併窗三測試的**時鐘跨桶 flake**：dedupe_key 的時間桶
  取 `Time.current`（`adjust.rb` `enqueue_adjust_event!`），不凍結時多次呼叫踩到
  桶邊界就併成 2 顆事件。2026-08-24 於第 4 包分支全套實跑抓到一次
  （`expected: 1 got: 2`，該分支零 Ruby 改動——證明與被測程式無關）。
- 修法：`rails_helper` 引入 `ActiveSupport::Testing::TimeHelpers`；合併窗
  describe 加 `around { |example| freeze_time { example.run } }`。
- 反向補測：新增「跨桶不併」一例——`travel` 越過窗寬後第二次呼叫開新列、
  各自 `coalesced_count=1`。把 flake 的機制（桶邊界）釘成明文語義，
  同時是 freeze_time 修法的反向複驗（時鐘真的可控）。

## 修改的檔案與核心邏輯 (Changes)

- `spec/rails_helper.rb`：`config.include ActiveSupport::Testing::TimeHelpers`。
- `spec/services/events/producers_spec.rb`：合併窗 describe 凍結時鐘＋新增跨桶例
  （12 例全綠；坑：AS 無 `Integer#milliseconds`，窗寬要 `/1000.0` 換 `.seconds`）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 無。生產程式零改動（flake 在測試層，時間桶語義本身正確且已由新例覆蓋雙向）。
