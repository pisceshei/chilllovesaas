# 2026-08-31 — G6-1d：信用卡＋Apple Pay／Google Pay（components-sdk 內嵌）

## 已完成的工作 (Done)

- **components-sdk 契約逐字取證（官方 .md 鏡像＋CDN bundle 實物，2026-08-31）**：
  CDN＝static.airwallex.com/components/sdk/v1/index.js（window.AirwallexComponentsSDK；
  舊 airwallex-payment-elements 官方宣告 deprecated）；🔴 init env 三源矛盾
  （'sandbox'|'prod' vs 'demo'|'prod' vs quickstart 'demo'）——CDN bundle enum 實物
  同認 demo/sandbox 五值 ⇒ 以 quickstart＋bundle 為準取 'demo'／'prod'（limits 註）。
  card quickstart 全文形：init → createElement('card',{intent_id,client_secret,
  currency}) → mount('card') → card.confirm({intent_id,client_secret}) promise。
- **卡片付款頁**：/pay 分流（card/applepay/googlepay→SDK 頁；alipayhk/fps 維持
  server confirm QR）；同一把 `pi-<token>-<cents>` request_id ⇒ 兩型重按恆同 intent；
  🔴 confirm resolve 後不信客戶端——導 /pay/status?html=1 由 server 權威重取→
  Finalize（與 QR/webhook 同終點同冪等鍵）；authFormContainer 掛 3DS 容器。
- **錢包頁**（applePayButton/googlePayButton）：amount{currency, value:number}＋
  countryCode 必填（參考頁逐字；amount_value 以原文注入 JSON number——殺引號形）；
  on('success')/on('error') 事件制。⚠️ 端到端留 sandbox；本包＝頁面＋契約落位。
- **Apple Pay 網域驗證檔上線**：官方下載連結（Contentful 資產，取證 2026-08-31）
  的檔案入庫 `public/.well-known/apple-developer-merchantid-domain-association`
  （9122 bytes；static file server 對無副檔名回 application/octet-stream——官方要求
  的 content-type）。🔴 檔案 URL 帶 __5_ 版本尾碼＝Apple 換發會輪替，**不得**把
  下載 URL 硬編產品流程（worklog 記提供源即可）；網域註冊（add_items，body＝
  {"items":["<裸網域>"]}，guide 級 99/次）＝部署後以 API 對 demo 網域執行。
- checkout_supported_methods 擴為五式（Q1 同步改：card 現身；白名單外仍 hide）。
- 測試：Q1 改＋Q8（card 頁：CDN/config/client_secret/env=demo/mount，且不打
  confirm_qr）＋Q9（wallet 頁：element 名／amount JSON number 無引號／countryCode）
  ＋Q10（production→env=prod 映射殺手格）；MUT-A/B/C 全紅（env 恆 demo／card 掉回
  QR confirm／錢包金額變字串）。

## 修改的檔案與核心邏輯 (Changes)

- `app/controllers/storefront/checkouts_controller.rb`（pay 分流＋pay_sdk＋
  card_pay_page_html＋wallet_pay_page_html＋sdk_env/sdk_cdn_url）
- `config/limits.yml`（checkout_supported_methods 五式＋sdk 三鍵）
- `public/.well-known/apple-developer-merchantid-domain-association`（新；官方資產）
- `app/assets/stylesheets/checkout.css`（.ck-pay-btn／mount 容器）
- `spec/requests/storefront_psp_payment_spec.rb`（Q1 改＋Q8–Q10）

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **卡付端到端**（真輸卡→3DS→SUCCEEDED）需 sandbox 帳號或使用者授權正式小額；
  wallet 端到端另需真機（Apple Pay=Safari＋已註冊網域；Google Pay=Chrome 登入帳號）。
- CardElementOptions 參考頁未列 intent_id/client_secret（quickstart 有傳）——文檔
  內部張力照登；confirm rejection 形狀官方未文檔化（catch 走通用文案）。
- 沙盒/正式是否同一份 well-known 檔＝官方未明文（單一下載連結旁證；標未取得）。
- Apple Pay 網域註冊對 demo.chilling.com.hk＝部署後執行（正式帳號 config 級、
  可 remove_items 反悔）。
