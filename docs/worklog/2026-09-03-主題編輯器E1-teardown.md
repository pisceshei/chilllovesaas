# 2026-09-03 主題編輯器 E1：本尊逐面板實測 teardown（D79）

## 已完成的工作 (Done)

- 依 D79 在真店 pnrjnw-sy 以「Duplicate 副本」方式對本尊主題編輯器做逐面板、逐控件實測，寫成
  `docs/research/100-theme-editor-live-teardown.md` §0–§8＋§V：頂欄每個控件的 tooltip 與快捷鍵、四個下拉的完整值域
  （模板選擇器含各子清單）、左樹列解剖與右鍵選單、鍵盤快捷鍵實測、右欄設定面板逐型別形態（range／select／radio／
  checkbox／color／color_scheme／color_scheme_group／font_picker／image_picker／link_list／url／text／textarea／code／
  header／paragraph）、區段與區塊 picker 兩欄結構與分類、預覽的 hover／點選／右鍵／插入點／inspector／手機檢視／
  全寬預覽、儲存／發布／離開三個對話框逐字、URL 狀態參數全表、主題設定分類與 app embeds 面板、截圖換算尺寸表。
- §9 官方雙源（上一提交已入）逐條加「實測對照」句，登記 help 與實測的差異（drag handle 圖示）。
- 副本主題保留（theme id 143506604135）供 E2–E7 續用；副本終態與原主題一致（一次隱藏後已還原並儲存）；
  hoko.vip 現行發布主題未動、未發布副本。
- 記錄 D79 的執行入口：`docs/DECISIONS.md` D79（上一提交已入）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 變更 |
|---|---|
| `docs/research/100-theme-editor-live-teardown.md` | §0–§8 由骨架填成實測內容；§9 加實測對照；§V 十三項待驗證／工具限制 |
| `docs/worklog/2026-09-03-主題編輯器E1-teardown.md` | 本檔 |
| `docs/handoff/2026-09-03-主題編輯器E1-teardown.md` | 交接四段 |

本包純文檔，無程式碼、無測試變動；閘門只跑 `ruby scripts/check-doc-claims.rb --base origin/main --require-base`。

## 尚未完成或需注意的風險 (Pending / TODO)

- 截圖未落倉（工具限制，§V V1）：本檔以逐字轉錄替代；E2 起改本機 Chrome 手動截圖入倉。
- 編輯器 CSS bundle 與精確色值／字級未取得（§V V2）：§8 只有尺寸換算（±2px），E2 動手前須用本機 DevTools 補 token 值表。
- 帶 `⊞` 的快捷鍵實際鍵位、拖曳重排、Pages 子清單尾段、區段 picker Products 之後的分類、richtext／video／product 等
  設定型別、手機預覽精確寬度與平板檔位、Undo 到乾淨態的 Save 狀態、主題設定手風琴互斥——全部登記在 §V，E2–E4 逐項補。
- 本輪一次誤存：隱藏 Announcement bar 後的 Ctrl+Z 未生效即 Ctrl+S，副本被存成隱藏；已點眼睛還原並再儲存。
  教訓：對副本做破壞性操作前先確認 Undo 有生效（截圖看眼睛 icon），不能只靠快捷鍵回報。
