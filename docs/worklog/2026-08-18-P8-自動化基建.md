# Worklog — P-8 自動化基建（2026-08-18）

> 背景：使用者本日裁定「PR #58 掛熔斷停驗收，轉做 P-8」。#58（方案落庫＋鐵律 16/17/18
> 立法）仍開啟、意見留存；本包從 main 開分支，實作鐵律 17/18 的機制面。
> 條文本文與總方案在 PR #58（2026-08-18 尚未進 main）——本檔引用其語義時一律帶此錨定。

## 已完成的工作 (Done)

1. **判詞格式機械驗證（18.1④）**：claude-review.yml 判詞處理步驟新增——第一行恰為
   兩種合法結論形之一＋標記全文唯一，違者留言＋job 紅（exit 1）；檔尾「無法判讀」
   分支隨之成為不可達縱深防線，同步改 exit 1。
2. **18.1 四條件評估器**：「通過」分支 approve 後計算 C1–C4（全部存在型、fail-closed），
   `AUTO_MERGE="false"`（現況）只留言存證；`"true"` 時缺一拒合。C1 用代理判準
   （Codex review 錨定 head＋無建議聲明句），限制誠實寫在留言與 docs/dev 篇章。
3. **熔斷 label 修復**：實測證偽舊註釋「`--add-label` 會自動建立 label」——label
   從未存在、add-label 歷輪靜默失敗，熔斷從未生效。修法＝先冪等 `gh label create`
   再 add-label 且不再吞錯；label 本體已手動補建（`review:需人工裁定`）。
4. **`scripts/await-verdict.sh`**（17.1 倒計時輪詢）：判詞增量＋Codex head 錨定雙條件、
   INTERVAL 下限 300 秒（API 限額）、逾時升級 exit 4；JSON 落檔 UTF-8 解析（cp950 教訓）。
5. **doc-claims 淺 clone 雙重洞修復**：ci.yml fetch 改 `--unshallow`＋base fetch 去
   depth 去 `|| true`；checker 新增 `--require-base`（取不到 diff ⇒ exit 3）；
   harness 新增 git-G3 情境並**突變驗證**（壞→紅、還原→綠）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 內容 |
|---|---|
| `.github/workflows/claude-review.yml` | 18.1④ 格式驗證步驟＋四條件評估器（通過分支）＋熔斷 label create/add 修復＋檔尾不可達分支 exit 1＋頂部 AUTO_MERGE 註解補評估器語義 |
| `.github/workflows/ci.yml` | doc-claims base fetch：`--unshallow`＋全深 base fetch（fail-loud）＋`--require-base` 旗標；契約註釋寫明三件一組 |
| `scripts/check-doc-claims.rb` | 新旗標 `--require-base`：base diff 算不出 ⇒ exit 3（原 warning 保留為無旗標行為）；用法與退出碼表同步 |
| `scripts/test-doc-claims-rules.rb` | git-G3 情境（--require-base＋不存在 base ⇒ exit 3）；`GIT_SCENARIOS` 2→3；摘要列印補 G3 行 |
| `scripts/await-verdict.sh` | 新檔（見 Done 4；不匹配閘門 selector，刻意——是操作工具不是閘門） |
| `docs/dev/m0-automation-infra.md` | 新檔：交付逐件行為（見其 §1 表）＋驗證紀錄＋跨功能影響（含與 #58 的檔案交集與合併順序注意）；第 2 輪隨修復回寫 C1/C3/await-verdict/W1 段 |
| 本 worklog＋handoff | 三件套 |

**自測**：全閘門 selector 全集 FAIL=0（含新 G3 的 harness）；兩支 workflow ruby YAML
parse OK＋全部 run 區塊 `bash -n` OK；`await-verdict.sh` `bash -n` OK；G3 突變證據
（壞→harness exit 1 且失敗訊息含 git-G3；還原→exit 0）。複驗指令：
`ruby scripts/test-doc-claims-rules.rb`（看「3 條 git 情境」與 G3 行）。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **本 PR 自身無 bot 判詞**（反竄改：改 claude-review.yml 的 PR 自跳驗收）——
   驗收＝Codex review＋人工；依鐵律 18.3（改 workflows）一律人工合併。
2. 🔴 **兩件真實 PR 取證未取**（本 PR 取不到）：①下一個「通過」的常規 PR 要看到
   四條件評估留言（P-8 機械判準「倒計時→判詞格式驗證→四條件→止於合併」的後半）
   ②下一次超輪要看到熔斷 label 真的掛上。取到證據前，P-8 只算「機制已落地」，
   不算「機制已實測全鏈路」——18.4 啟用的前置沒齊。
3. **18.4 信任邊界待使用者裁定**（啟用 AUTO_MERGE 的前置）：建議形態＝限作者
   allowlist（僅 repo owner 的 PR 可自動合併）＋真實 PR 全鏈路實測後才翻值；
   C1 代理判準屆時需重新評估（見 docs/dev 篇章 §2.2 限制）。
4. **與 PR #58 的檔案交集**：`check-doc-claims.rb`／`test-doc-claims-rules.rb` 兩檔
   雙分支各改不同 hunk——後合併方必重跑 `test-doc-claims-rules.rb`；#58 回頭收官時
   其 91 §3.4 的「修法屬 P-8」條目要補「已由 P-8 落地」註記。
5. **91 落籍延後**：本輪新坑（熔斷 label 從未生效）與 P-8 修洞結案註記都該進 91
   §3.4，但該節在 #58 分支上尚未進 main——為免同檔雙分支衝突，落籍隨 #58 收官
   或收割輪補，本檔與 docs/dev 篇章先行承載全文。

## 驗收後修正（PR #59 第 2 輪：Codex r1 十條 P1×4 P2×6；bot 判詞＝反竄改自跳屬預期）

- r1-1（C1「內文沒有某句話」不算零建議證據，P1）：改**正向證據**——最新 Codex review
  錨定 head ∧ 以 `pull_request_review_id` 歸戶實查該 review 的 inline 意見數＝0；
  查詢失敗/無 review/未錨定一律 0。EVAL 文案同步。
- r1-2（格式 regex 未錨定行尾＋grep -c 數行不數次，P2）：regex 補 `[[:space:]]*$` 錨
  （容行尾空白/CR）；出現次數改 `grep -o | wc -l`。
- r1-3（await-verdict 不分頁，留言破百永遠等不到，P2）：改 python 直抓逐頁（不足 100
  則停，上限 PAGES_MAX 頁）；順帶把 curl+落檔改為 python urllib 直取（cp950 紀律不變：
  UTF-8 顯式解析、輸出只回 ASCII）。
- r1-4（判詞計數不限作者不限首行，任何人引述標記即可觸發 exit 0，P2）：鏡射
  claude-review.yml 收斂條件——作者允許清單 ∧ 第一行以標記開頭。
- r1-5（docs/dev「共五件」手抄計數，P1）：去數量化改「逐件見下表、數量以表列為準」；
  本 worklog Changes 表同型「五件交付」一併去（同缺陷同輪自查）。
- r1-6（C3 一次性查詢把「還在跑」永久記 0，P1）：有界等待——pending 每 30 秒重查
  至多 20 次；job 結束後才落定的 check 屬 18.4 自動合併 workflow 的 `workflow_run`
  再評路徑，誠實限制寫進註釋與 docs/dev。
- r1-7（G3 只測消費端、拿掉 ci.yml 旗標照綠，P2）：harness 新增 **W1 供給斷言**——
  直接斷言 ci.yml 的 doc-claims 調用行含 `--require-base`（突變驗證：拿掉旗標→紅）。
- r1-8（HEAD_SHA 截斷接受任意非空值、短前綴誤中舊 review，P2）：參數驗證——十六進位
  9–40 位，違者 exit 2。
- r1-9（INTERVAL/MAX_POLLS 非數字滑進循環變假逾時/燒限額，P2）：正整數驗證、違者
  exit 2（含 INTERVAL≥300 下限保留）。
- r1-10（跨 PR 錨定缺日期，P1）：docs/dev 三處與本檔頂部錨定補「2026-08-18」
  （AGENTS §4：PR 號＋日期缺一不可）；PR 描述「改動（五件）」同步去計數。
