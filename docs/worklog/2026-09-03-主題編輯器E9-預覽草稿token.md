# 2026-09-03 主題編輯器 E9：全頁草稿改 token 重載（修「改設定後預覽整頁錯亂」）

> 使用者實測（2026-09-03，截圖）：在編輯器調 Announcement bar 的 Spacing bottom／Section padding，live preview 整頁錯亂
> （無樣式、文字重疊）。同時回覆：路由前綴與貨幣格式跟隨本尊（D80／D81，本包只登記裁定，實作另開）；demo 店黑色方塊＝
> 早前 seed 的 `demo-logo.png`，已清掉 `settings.logo`（本尊新店無 logo ⇒ 顯示店名）。

## 已完成的工作 (Done)

- **根因**：PR-11 的全頁草稿刷新把 `draft_page` 回的整頁 HTML 用 `iframe.srcdoc` 換入；srcdoc 文件的 policy container
  （含 CSP）**繼承 admin 頁**（`config/initializers/content_security_policy.rb`：`style-src 'self'`、`script-src 'self'`＋nonce），
  主題的 inline `<style data-shopify>`、`style="--…"`、inline `<script>` 全被擋 ⇒ 任何設定一改、600ms 全頁刷新後預覽退化
  成無樣式。片段路徑（`draft_section`＋`cl:replace`）沒事，所以看起來像「一調就壞」。
- **修法**：`draft_page` 改為把草稿（sections＋settings）存進 `Rails.cache`（鍵含 shop＋theme＋隨機 token，TTL 20 分）
  只回 `{token}`；`show` 在 `editor=1` 且帶 `draft=token` 時套用草稿渲染（主題面 CSP＝`ThemeCsp`、正確 base URL、inline
  script 正常執行）。前端 `ThemeEditorPage` 以 `previewSrc` 加 `&draft=token` 重載 iframe，載入後還原捲動位置；
  預覽內導航亦保留 token（未儲存草稿跨頁仍可見）。
- 規格：`spec/requests/theme_editor_draft_token_spec.rb` DT1–DT4（token 形、無／錯 token 不套、跨主題不套、非 editor 不套）；
  `theme_editor_bootstrap_spec.rb` DP1／DP2／CC1／TCC1 改走 `draft_page!` helper（token → GET）；vitest ED14 改斷言
  `iframe.src` 含 `?editor=1&draft=`、`srcdoc` 為空。突變 M91–M94 見 PR 描述。
- 裁定登記：`docs/DECISIONS.md` D80（URL 前綴跟隨本尊市場／語言設定）、D81（貨幣格式跟隨本尊店級設定）；
  `docs/dev/e8-render-parity.md` §4 對應改為「已裁定、待實作」。
- demo 店資料：`ThemeSetting`（theme 1）`logo` 由 `shopify://shopify/files/demo-logo.png` 清為空（bt3 runner，2026-09-03），
  頁快取已清；header 改顯示店名「CHILL LOVE Demo」。

## 修改的檔案與核心邏輯 (Changes)

- `app/controllers/admin/storefront_preview_controller.rb`：`draft_page` 存草稿回 token（`DRAFT_TTL`）；`show` 讀 `draft`
  （`draft_payload`／`draft_cache_key`，只在 design_mode）；render params 去掉 `draft`。
- `app/frontend/admin/pages/ThemeEditorPage.tsx`：`draftToken` state＋`pendingScrollRef`；全頁刷新 effect 改 POST 取 token →
  `previewSrc` 帶 `&draft=`；`onPreviewLoad` 還原捲動。
- 規格：`spec/requests/theme_editor_draft_token_spec.rb`（新）、`theme_editor_bootstrap_spec.rb`（helper）、
  `ThemeEditorPage.test.tsx` ED14。
- 文檔：本 worklog、`docs/handoff/2026-09-03-主題編輯器E9-預覽草稿token.md`、`docs/dev/e6-theme-editor-preview.md` §E9、
  `docs/DECISIONS.md` D80／D81、`docs/specs/91-pit-register.md` §3.76、`docs/dev/external-facts.md` §G13–G14。

## 尚未完成或需注意的風險 (Pending / TODO)

- 草稿存 cache（SolidCache）：TTL 20 分；token 只在 staff 閘後有效；draft 大小受 `config/cache.yml` `max_size` 影響
  （256MB 總量；單筆數百 KB 正常）。
- 未儲存草稿跨預覽導航靠 `draftToken` state；重新整理整個 admin 頁會丟（本尊亦要求儲存，行為一致）。
- D80（路由前綴）與 D81（貨幣格式）實作另開包：前者牽動 `Markets::UrlPrefix`、路由解析、hreflang／sitemap／canonical、
  localization 表單、頁快取鍵；後者加 `shops.money_format`／`money_with_currency_format` 兩欄與 `money*` 過濾器。
- 使用者提出「從 Announcement bar 起編輯器功能未與本尊一模一樣」：逐 section／逐控件的功能對照（E4b 資源 picker、
  Editing scheme、image picker 篩選上傳、E7 RWD）仍待做；本包只修「改設定就壞」這條擋路的。
