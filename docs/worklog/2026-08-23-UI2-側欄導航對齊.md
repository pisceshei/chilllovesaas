# 2026-08-23 — UI-2：側欄導航對齊原型 NAV

## 已完成的工作 (Done)

- 側欄導覽樹由 6 項扁平清單改為**原型 `NAV` 的完整結構**（chilllove-admin-v2.html:2225
  逐項對照）：首頁／訂單(3子)／產品(5子)／顧客(2子)／成長(2子)／折扣／內容(3子)／
  市場(2子)／財務／分析(2子)＋銷售管道群（線上商店(2子)／代理式／門市 POS）＋
  應用程式群＋底部工具（AI 助理對話／設定）。
- 手風琴語義照原型：**目前頁面屬於該群組時展開**（不是點擊切換）；群組歸屬由
  資料表決定（`GROUP_OF` 語義——`/admin/collections` 屬產品、`/admin/catalogs` 屬市場）。
- 側欄 CSS 整段改 token（--sidebar-w 240／--topbar-h 56／--hairline／--sh-raised／
  nav-subs max-height 手風琴），對照原型 :294-348；topbar 高度 52→56（47 §0 量測真值）。
- placeholder 路由**由導覽資料表派生**——側欄每一項都有頁面，不會出現「點得到、404」。

## 修改的檔案與核心邏輯 (Changes)

- `app/frontend/admin/layout/navigation.ts`：新增；NAVIGATION／SALES_CHANNELS／APPS
  資料表＋entryContains 歸屬判斷。每一項附原型出處註釋（71-R8/R9/R10/R4 裁定）。
- `app/frontend/admin/layout/AdminShell.tsx`：SidebarEntry 元件（項＋子項手風琴）。
- `app/frontend/admin/App.tsx`：路由由 navigation.ts 派生；IMPLEMENTED map 標記真頁。
- `app/assets/stylesheets/admin.css`：側欄段整段重寫為 token 引用；52px→var(--topbar-h)。
- `ProductsPage.test.tsx`：狀態文案斷言 scope 到表格內（側欄新增「草稿」連結造成
  全文件查詢多重命中）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 訂單的 `count` 徽章刻意不搬：原型值 5 是 demo 數字，實作端必須來自 rollup
  同源查詢（鐵律 7）。有資料源後再加。
- 財務子項是狀態函數（啟收款後出現「支付款項」），動態化留待導航資料驅動（71-R4-V4）。
- 新增的 20+ 路由全部是 placeholder；逐頁實作按里程碑排。
- 行動版抽屜寬度已對齊原型 min(272px,84vw)，但未實測窄螢幕互動（rwd-check 尚未進 CI）。
