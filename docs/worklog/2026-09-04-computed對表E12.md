# 2026-09-04 E12 computed-CSS 對表：量測工具＋由它抓到的引擎缺口（鐵律 22.1「精確到 CSS 級別」，D83）

分支 `parity/e12-computed-css`（自 main `7e20cc5e`，E8b 之後；D83 #317 只改規範，無衝突）。規範 `docs/dev/e12-computed-parity.md`；未取得／更正
`docs/specs/91-pit-register.md` §3.79；e8 doc #52 更正。`scripts/` 屬 18.3 清單 ⇒ 依 D83 由工作階段代合。

## 已完成的工作 (Done)

- 新工具 `scripts/computed-parity.mjs`（零新依賴：Node 24 內建 WebSocket／fetch；本機 headless Chrome 152 乾淨 profile ⇒ 無擴充污染）：
  `capture`（逐元素 89 個 computed 屬性＋幾何、診斷）、`diff`（逐段／逐元素／逐屬性、Markdown 報告）、`inspect`（命中規則）、`evaljs`（取證）、
  `selftest`；`--open-details 1`；svg 只比盒；px 容差 0.5；身分正規化同 `RenderParity::Normalizer`。
- 首輪量測（hoko.vip live vs mirror.chilling.com.hk，E8b 部署）：首頁 1280／768／390 皆 20/21 段逐屬性全同；collections 14/15×2、
  contact 15/16、search 11/12、cart 14/15、404 11/12——未全同的只有 `__root__`（本尊平台 iframe／`shop-cart-sync`）；商品頁 10/15。
- 商品頁抓到的根因逐項追到並修（清單＝dev doc §1b 表，複驗 `grep -c '^| [0-9] ' docs/dev/e12-computed-parity.md`）：①`image_url` 對 nil＝Liquid 錯誤「invalid url input」（hoko sticky-atc 逐字；E8b #52 更正——
  空字串是 `{% assign %}` 吞錯）；②Section Rendering API 在任何頁面（`PagesController#show`）＋完整 id 依宿主模板解析（`section_data_for`）；
  ③recommendations 共同系列不足時以其他可見商品補位；④三支 SRA 端點（recommendations／search suggest／cart sections）語言跟 URL 前綴
  （`BaseController#locale_hit`；先前 `locale: nil` ⇒ 英文按鈕寬 74／99 vs 本尊 32／80）。修後本機商品頁 13/15，餘 `main`（countdown 時間性＋
  本尊 payment button）與 `__root__`。
- 取證教訓入 91 §3.79：本尊 CDN 對主題 CSS 做壓縮＋前綴＋巢狀攤平（Chrome 152 對原檔巢狀計算相同）；關閉 `<details>` 子樹兩邊報告值可能不同
  （搜尋 modal 圖示 25 vs 18，開啟後一致）。

## 修改的檔案與核心邏輯 (Changes)

- `scripts/computed-parity.mjs`（新）。
- `app/liquid/theme_engine/filters.rb`：`image_url` nil ⇒ `raise Liquid::ArgumentError, "invalid url input"`。
- `app/liquid/theme_engine/page_renderer.rb`：`section_data_for` 先依 id 宿主（`template--{name}__`／`sections--{group}__`）找段。
- `app/controllers/storefront/pages_controller.rb`：任何路徑的 `section_id`／`sections`（no-store、404／400、JSON）；`render_page(extra:)`；
  `current_domain` 上提。
- `app/controllers/storefront/base_controller.rb`：`locale_hit`、`current_domain`。
- `app/controllers/storefront/recommendations_controller.rb`：補位（`related_by_collections`＋其他可見商品依建立序）；renderer 帶 locale／presence。
- `app/controllers/storefront/search_controller.rb`、`cart_controller.rb`：renderer 帶 locale／presence（cart 由 `sections_url` 前綴解）。
- 規格：`spec/requests/storefront_section_rendering_spec.rb`（新，SR1–SR5）、`storefront_search_spec.rb` R4、`storefront_i18n_spec.rb` SF-9、
  `render_parity_pages_spec.rb` PP14 更正；fixture `minimal-1.0/sections/related-products.liquid` 加 `#rhello`。
- 文檔：`docs/dev/e12-computed-parity.md`（新）、`docs/dev/e8-render-parity.md` #52 更正、`docs/specs/91-pit-register.md` §3.79、本 worklog／handoff。

## 對表結果

見 dev doc §2（首頁三寬 20/21；六頁 1280 只差 `__root__`；商品頁修後本機 13/15）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 合併部署後要以公開站重跑商品頁與其餘七頁的 768／390（dev doc §3）；本輪商品頁修後數字來自本機 dev server。
- recommendations 補位規則官方未逐字（V）；`image_url` 對空字串仍走佔位（V）。
- 主題編輯器 live preview 的 computed 對表（鐵律 22.1 兩者皆是）尚未做——工具可直接對 `/admin/themes/{id}/editor` 的預覽 iframe 量，待下一包。
- 部署：無 migration；bt3 `deploy.sh` → restart；不需 mirror 對齊。

## 閘門

（凍結 tree 後填。）
