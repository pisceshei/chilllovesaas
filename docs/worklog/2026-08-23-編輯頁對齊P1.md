# worklog 2026-08-23 — 商品編輯頁對齊 P1（組織分類＋SEO＋狀態 picker＋封存）

## 已完成的工作 (Done)

- productSet 契約增量：`vendor`／`productType`／`tags`／`seo{title,description}` 四路輸入
  ＋ Product type 讀取面＋`productVendors`／`productTypes` 建議清單 query（91 §18 G1/G2）。
- 服務層 `normalize_organization`：宣告式語義（缺席＝保持現值、空值＝清除）、上限全走 limits
  （新增 `product.vendor_max_chars`／`product_type_max_chars`）；SEO 160＝建議值可超過、320 才 reject。
- 前端：右欄組織分類卡（類型 search-or-create datalist、廠商 autocomplete、標籤 chips、
  範本 select 佔位）；SEO 卡重做（SERP 預覽＋✏️ 展開＋雙計數器）；狀態自訂 listbox
  （三選項帶描述副行、封存排除——91 §2）；頁首更多動作▾（封存⇄取消封存即時儲存，
  複製／刪除 disabled 佔位）；CSS 新增四組類（statusbox/serp/chips/actionsmenu，全 tokens）。
- 測試：後端 4 例新增（`spec/requests/product_set_spec.rb` 組織分類＋SEO describe）；
  前端整檔改路由式 fetch stub＋6 例新增，12/12；全套 rspec 334/0、vitest 17/17、
  rubocop 0、tsc 0。

## 修改的檔案與核心邏輯 (Changes)

- `app/graphql/types/inputs/product_set_input.rb`＋`app/graphql/types/inputs/seo_input.rb`（新）
- `app/graphql/types/product_type.rb`＋`app/graphql/types/seo_type.rb`（新）＋`app/graphql/types/query_type.rb`
- `app/services/catalog/save_product.rb`（normalize_organization；create/update 展開 org hash）
- `app/frontend/admin/pages/ProductDetailPage.tsx`（StatusListbox／TagsField 元件、SERP 預覽、
  更多動作選單、suggestions 載入、auto-save effect）
- `app/frontend/admin/pages/ProductDetailPage.test.tsx`（路由式 stub 重寫）
- `app/assets/stylesheets/admin.css`／`config/limits.yml`／`spec/requests/product_set_spec.rb`
- `docs/dev/m1-product-edit-parity-p1.md`（新，12.4 四件事逐控件）

## 尚未完成或需注意的風險 (Pending / TODO)

- 冪等 fingerprint 含新欄位：同 idempotencyKey 改組織欄位重放回 FINGERPRINT_MISMATCH（契約如此，
  非回歸）。
- CJK 建議清單排序＝DB collation 碼位序，與「字母序」直覺不同（spec 註記）。
- 標籤「編輯」對話框（V-91.10）、佈景範本值域（V-91.11）、複製／刪除（G8）留待後續包。
- SERP 預覽 fallback 規則目前只在前端；G12 前台渲染開工時必須抽同源模組，否則預覽會說謊。
