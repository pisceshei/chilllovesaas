# 2026-08-31 — G6-3a：PSP provider 憑證層（設定 › 付款最小可用面）

## 已完成的工作 (Done)

- 使用者裁定「部署到伺服器、我在網站裡輸入憑證」⇒ 把 G6-3 資料層前拉成本切片：
  `shop_payment_providers` 表（(shop_id, provider) UNIQUE；祕密欄密文）＋
  `shopPaymentProviders` query＋`shopPaymentProviderSet` mutation（祕密 write-only：
  省略＝不變、空字串＝清空；payload 只回指紋）＋admin「設定 › 付款」頁
  （Airwallex／PayPal 雙卡：test mode 開關＋Client ID＋祕密欄＋各自的
  webhook secret／webhook ID；五語言 i18n）。
- **全倉第一個 AR encryption 使用點**：initializer 接 env 三鍵（production 缺鍵
  boot 即 raise；dev/test 固定 dummy）；bt3 已伺服器端生成三鍵落 /etc/chilllove/env
  （值不經過對話）；120 §4 env 契約同步。
- limits 新增 `psp_credentials:` 區塊（37 §6.3＋carrier.credentials 慣例：
  non-deterministic／fingerprint 16 hex／job payload 禁攜密／environment_enum）。
- 兩個實測踩中的坑修掉並寫進 model 註釋：json 表達式預設不進未存檔實例
  （after_initialize 補 []）；`Limits.enum` 回大寫（downcase 後比對）。
- 測試：model 6＋request P1–P7＋前端 5（vitest 全套 293/293）；
  突變 MUT-1~5 全紅（拔 encrypts／拔指紋刷新／省略當清空／type 長祕密欄／拔字典檢查）。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260831260000_create_shop_payment_providers.rb`＋`app/models/shop_payment_provider.rb`
- `config/initializers/active_record_encryption.rb`（新）＋`config/limits.yml`（psp_credentials）
- `app/graphql/`：`types/shop_payment_provider_type.rb`＋`errors/shop_payment_provider_user_error_{code,type}.rb`
  ＋`mutations/shop_payment_provider_set.rb`＋query_type/mutation_type 註冊
- `app/frontend/admin/`：`pages/SettingsPaymentsPage.tsx`（＋.test.tsx）＋App 路由＋
  SettingsPage 索引項＋五語言 messages＋`app/assets/stylesheets/admin.css`（.cl-provider-head）
- `docs/dev/g6-payment-provider-credentials.md`（新）＋`docs/specs/120` §4 同步
- `spec/models/shop_payment_provider_spec.rb`＋`spec/requests/shop_payment_provider_settings_spec.rb`

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **等使用者在頁面輸入 sandbox 憑證**（demo 店 /admin/settings/payments）——
  G6-1 sandbox 實測的前置。
- 清空祕密無 UI／輪換記錄／連線測試鈕／activation 狀態機＝G6-3 本體與 G6-1。
- KMS 信封加密維持 37 §6.3「待定使用者裁定」；本切片＝AR encryption 最低標。
- settings 權限沿用 authorize_products!（M5 RBAC 換）。
- 🔴 換 ACTIVE_RECORD_ENCRYPTION_* 任一鍵＝既有密文永久不可解——bt3 的 env 三鍵
  不得重生成（120 §4 已寫明）。
