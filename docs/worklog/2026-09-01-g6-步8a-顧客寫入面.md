# 2026-09-01 G6 步 8a：顧客模組寫入面

## 已完成的工作 (Done)

- 官方深研（shopify.dev latest＝2026-07＋help 逐字）＋測試店親點（More actions／
  三通道 marketing modal／Manage addresses／Erase modal／Edit customer modal）——
  落 `docs/dev/g6-customer-mutations.md` §5；狀態機與排除清單逐字進各檔頭。
- 同意事件鏈：customer_marketing_consents（append-only、readonly? 機制擋改）＋
  customers 狀態快取欄＋Customers::UpdateMarketingConsent（可寫三值閘／通道前置／
  latest-wins 投影／legacy boolean 同步）；UpsertFromCheckout 生產端改走同鏈。
- 抹除鏈：RequestDataErasure（limits customer.erasure_cancel_days=10 官方逐字）→
  CancelDataErasure → RedactDueJob（recurring 每小時；PII 清空＋地址刪＋redacted
  事件；訂單保留）。
- mutation 面 11 支：customerCreate/Update/Delete＋地址四支＋consent 兩支＋
  erasure 兩支；共用 CustomerUserError 三件套；CustomerType 擴欄
  （兩狀態／redactionScheduledAt／anonymizedAt／addresses）。
- phone 唯一索引（ix→uq）；CustomerPolicy 補 update?/destroy?。
- 突變輪 9/9 殺＋canary：三值閘/前置/latest-wins/append-only/訂單擋刪/地址簿殘留/
  抹除窗/checkout 首源/預設讓渡。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260901070000_create_customer_marketing_consents.rb`＋schema。
  🔴 立法事故實錄：首版把 `add_foreign_key` 放在 `unless table_exists?` 塊內，
  strong_migrations 擋下 FK 後重跑被守衛整塊跳過 ⇒ **FK 靜默缺席**（實測
  foreign_keys=[] 抓到）。修法＝FK 獨立成步＋語句級守衛（檔內註記教訓：
  re-entry 守衛的粒度必須＝語句，不是區塊）；test DB 以 reset→fresh migrate
  複驗一把過＋FK 在案。
- 新：`app/models/customer_marketing_consent.rb`、
  `app/services/customers/{update_marketing_consent,request_data_erasure,cancel_data_erasure}.rb`、
  `app/jobs/customers/redact_due_job.rb`、mutations ×11、inputs ×2、errors ×2
- 改：`app/services/customers/upsert_from_checkout.rb`（consent 走事件鏈）、
  `app/graphql/types/customer_type.rb`、mutation_type、`app/models/customer_policy.rb`、
  `config/limits.yml`（customer.erasure_cancel_days）、`config/recurring.yml`
- 測試：`spec/services/customers/marketing_consent_spec.rb`（7 例）＋
  `spec/jobs/customers/redact_due_job_spec.rb`（2 例）＋
  `spec/graphql/customer_mutations_spec.rb`（7 例）

## 尚未完成或需注意的風險 (Pending / TODO)

- 步 8b：customerMerge（官方 11 hard blockers 已取證——CustomerMergeErrorFieldType
  逐字在研究報告；我方可判格先做 merge_in_progress/deleted/pending_redaction）＋
  詳情頁 FE（74 §4＋本輪實測 §5）＋nightly 統計對帳 job＋customers.locale。
- ⚪ order 層個資遮蔽（redact 後 orders.email/地址快照仍在）＝91 §3.52。
- WhatsApp 通道（實測 modal 第三通道）＝資料層 channel 欄已留位，v1 不做。
- GraphQL 端重複 email 的官方逐字 message 未取得（僅 REST 422）——我方 TAKEN 碼
  ＋model message；本尊 GraphQL 實測抓包待補（91 §3.52）。
