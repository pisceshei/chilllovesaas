# 鐵律 21：遠端終態收斂

## 已完成的工作 (Done)

- 在 `2ed2403d06eb50bba0f82e74fcacf44643a81bd8` 推送成功後，檢查逐步 handoff 的實際閉環，
  發現原寫法同時要求「push／當前 head 驗收是獨立步驟」與「每步都要新增倉庫 handoff 並
  commit」。若照字面執行，記錄 push 或驗收會產生新 head，使剛取得的 current-head 證據失效，
  並再次觸發 push／驗收，形成不能收斂的循環。
- 保留使用者要求的每步詳細交接，新增唯一窄邊界：push、current-head review、合併、deploy／
  healthcheck／rollback 等只能在遠端動作後取得的結果，使用對應 PR／deployment 上的四段式
  remote handoff，綁定 head／base、run／review／comment id 與時間，不為記錄而產生新 head。
- 同步 `CLAUDE.md` 鐵律 21、`AGENTS.md`、D35、階段一'執行方案、review convergence 契約、
  歷史更正、開場終態入口與 `91` 附錄 A；其後若有修復 commit，下一份倉庫 handoff 必須引用
  前一個 remote handoff。
- 使用 Git for Windows Bash 固定 shell 語義後，從 setup 起重跑 Windows 等價 production 清單，
  實跑彙總為 `PRODUCTION_GATE_COUNT=29`、`PRODUCTION_GATE_FAILED=0`；詳細的失敗撤回與復驗證據
  在 `docs/worklog/2026-08-20-鐵律21閘門Shell路徑復驗.md`。

## 修改的檔案與核心邏輯 (Changes)

- `CLAUDE.md`／`AGENTS.md`：加入遠端終態不以新 commit 自我失效的載體例外；原子收尾明列
  commit 後 doc-claims 與 push。
- `docs/DECISIONS.md`：在 D35 保存同一裁定的可執行邊界，明定只改載體、不降證據要求。
- `docs/plans/2026-08-20-階段一執行方案.md`／`docs/dev/m0-review-convergence.md`：把例外同步到
  實際 PR 迴圈、送驗清單與收斂操作契約。
- `docs/worklog/2026-08-20-鐵律21逐步交接落地.md` 與同名 handoff：只追加日期更正和新入口，
  不覆寫舊 head 上的歷史敘述。
- `docs/handoff/2026-08-20-階段一開場包.md`、`docs/specs/91-pit-register.md`：補入本單位入口
  與三件套對帳。
- 本 worklog 與 `docs/handoff/2026-08-20-鐵律21遠端終態收斂.md`：記錄本收斂單位。

## 尚未完成或需注意的風險 (Pending / TODO)

- 本文件中的 GitHub 狀態只證明輸入快照：2026-08-20 13:58:54 +08:00 時，`HEAD`、
  `origin/m0/phase1-opening` 與 `pr61-last-push` 都是
  `2ed2403d06eb50bba0f82e74fcacf44643a81bd8`；尚未證明本次修訂 commit 的本地閘門或 GitHub 驗收。
- 完整 production 閘門已於本次文件定稿前實跑全綠；最終文字回寫後仍須重跑 doc-claims，並在
  commit 後再跑一次 doc-claims、15.1／15.2，之後才可推送新 head。推送與 current-head 驗收
  結果依新規則寫入 PR #61 的四段 remote handoff。
- PR #61 修改 `CLAUDE.md`／`AGENTS.md` 與 workflow，仍命中鐵律 18.3，只能由使用者人工合併；
  remote handoff 不是代行合併授權。
