# 2026-09-01 G6 步 6：通知基座（mailer＋模板）

## 已完成的工作 (Done)

- 89 號 teardown 入庫（實測：/settings/notifications 全清單逐組逐列＋三模板
  preview/edit 路由與表單形；官方：模板編輯器語義／Liquid 變數攤平契約／sender
  email／棄單 10 分鐘判定／API 陰性）。
- 通知鏈全落地：outbox 消費者（orders/create→訂單確認；order.fulfilled 且
  notify=true→出貨通知）→ DeliverJob（Solid Queue）→ NotificationMailer →
  Renderer（overlay→平台預設；ThemeEngine::Filters money 族同源）。
- 模板 overlay：**採用 M0 既有 notification_templates 表**（key/channel/name/subject/
  body/enabled）；GraphQL query＋update（含 revertToDefault 刪列、Liquid strict
  parse 儲存閘）；sender email query＋update。
- admin UI：設定 › 通知主頁（sender email＋三模板列）＋模板編輯頁（兩欄＋Revert
  預設態 disabled）；settings hub 入口；i18n 五語系各 +23 鍵。
- 預設模板三支自寫（`config/notification_templates/*.liquid`；變數名照 89 §5 官方
  攤平契約；金額 cents 經 money filter）。
- production mailer 設定：SMTP_ADDRESS 有值走 :smtp；未設 perform_deliveries=false
  （demo「不對外發信」紅線的明文降級）。
- 突變輪 7/7 殺＋canary 綠：M1 攤平破壞→N1；M2 notify 閘→D2；M3 語法閘→G3；
  M4 忽略 overlay→N3；M5 revert no-op→G4；M6 registry 漏掛→D1；M7 空 email 照寄→D5。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260901050000_create_notification_templates.rb`：**只加
  shops.sender_email**。🔴 事故記錄：首版同檔帶 table_exists? 守衛的
  create_table（kind/subject/body_liquid 形），被 M0 core schema 既有的
  notification_templates（key/channel/body 形）靜默跳過——specs 以
  「unknown attribute 'kind'」爆出。處置＝採用 M0 形（它更完整：channel 預留
  SMS、enabled 預留停用），全鏈改 key/body；dead create_table 移除。
- 新：`app/services/notifications/{catalog,renderer,payloads,order_confirmation_consumer,shipping_confirmation_consumer}.rb`、
  `app/jobs/notifications/deliver_job.rb`、`app/mailers/notification_mailer.rb`、
  `app/models/notification_template.rb`、`config/notification_templates/`（3 檔）、
  `app/graphql/types/notification_template_type.rb`＋errors 兩檔＋
  `app/graphql/mutations/notification_{template_update,sender_email_update}.rb`
- 改：`app/services/events/consumers.rb`（掛 2 topic）、`query_type.rb`／
  `mutation_type.rb`、`config/environments/production.rb`（SMTP 分流）、
  `App.tsx`（2 路由）、`SettingsPage.tsx`（通知入口）、admin.css、i18n ×5
- 測試：`spec/services/notifications/{renderer,delivery_chain}_spec.rb`＋
  `spec/graphql/notification_settings_spec.rb`＋
  `SettingsNotificationsPage.test.tsx`（新，4 例）
- 文檔：`docs/research/89-notifications-teardown.md`＋`docs/dev/g6-notifications.md`

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 部署後 SMTP 未配置（/etc/chilllove/env 無 SMTP_ADDRESS）⇒ 生產通知信
  perform_deliveries=false（只渲染不外寄）。這是 demo 紅線的**刻意狀態**；
  真寄信需使用者提供 SMTP 憑證後在 env 補四鍵並重啟。
- 棄單挽回的觸發器（判定 job＋recovery URL 端點）＝步 7 射程；本步已備妥模板／
  payload／renderer。
- Send test／per-template Translate／全店 logo+accent 外觀層／sender 確認流／
  unsubscribe＝⚪ 後置（89 §7.5）。
- 重複寄送窗（at-least-once）已誠實登記於 DeliverJob 檔頭②——v1 接受。
