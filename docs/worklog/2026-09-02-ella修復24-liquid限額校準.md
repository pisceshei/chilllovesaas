# 2026-09-02 Ella 修復 PR-24：Liquid resource limits 校準＋zip 時戳釘死

## 已完成的工作 (Done)

- **生產實錘缺陷**：demo 商品頁（3 媒體含外部影片）帶
  「Liquid error: Memory limits exceeded」且外部影片整塊消失——bt3 插樁量測
  （攔 Liquid::ResourceLimits 實例＋大上限環境重放）：實際需求
  max_assign_score=**100,138**、max_render_score=405/context、輸出 481KB
  ——撞我方 D77 防 DoS 值 assign_score_limit=100,000（差 0.14%）。
- **校準**（limits.yml 帶量測沿革註釋）：assign 100,000→**1,000,000**、
  render_score 200,000→**2,000,000**（實測 ×10 餘裕；仍為 DoS 界；本尊值
  未公開＝ours）；render_length 2MB 不動（用量 481KB）。
- **回歸格 EG1（真 Ella 7.2.0 整頁）**：9 圖＋1 外部影片商品頁渲染——
  零 Liquid error＋youtube embed 出現。🔴 場景校準沿革：2 圖輕場景壓不破
  舊限（ML1 突變首輪不紅＝mutation-not-red「初始狀態選錯」型），gallery
  的 per-media 陣列 concat/where 平方成長 ⇒ 9 圖場景舊限必紅、新限綠
  （殺手閉環實跑證據）。
- **zip DOS-time 釘死**（theme_crud_api_spec）：2026-09-02 本地事故——
  spec 產 zip 的時戳 bytes 落特定值窗時，multipart 檔案內容在本地 Windows
  的 Rails 整合層被截斷成 11 bytes ⇒ INVALID_ZIP 假紅約 40 分鐘後自癒；
  三層插樁（service 見 11B／controller 見 591B 完整／Rack 隔離 roundtrip
  114/114 匹配）；CI Linux 不受影響。釘死 Zip::DOSTime.now（選值四 bytes
  避 0x0D/0x0A）根治時間相依。底層截斷機制未全定罪（時窗已過無法復現）
  ——登記於此，再現時憑三層插樁法續查。
- 另：PR-23 PR body 曾寫「rspec 1975/0」＝定稿未讀 tally 的錯值（實際
  1972/4＝本 flake），已在 #286 留言自首更正——**tally 必須讀完才寫**
  （步 11 教訓的復發，記憶 pr64-review-loop-lessons 同軸）。

## 修改的檔案與核心邏輯 (Changes)

- `config/limits.yml`：兩鍵校準＋沿革註釋。
- `spec/requests/theme_crud_api_spec.rb`：DOSTime 釘死＋事故註釋。
- 新 `spec/liquid/ella_product_gallery_spec.rb`（EG1）。

## 尚未完成或需注意的風險 (Pending / TODO)

- multipart 截斷底層機制未定罪（時窗過）；若再現：三層插樁（service/
  controller/Rack）＋比對 zip 時戳 bytes。
- 生產 demo 商品頁在本包部署前帶 Memory error（部署即癒——EG1 同構）。
- 更大主題頁（50+ 媒體）可能再逼近新限——EG1 場景可再加重複驗。
