# worklog 2026-08-23 — 多語言地基 ML-0（i18n 表＋語言種子＋來源語言 en）

## 已完成的工作 (Done)

- Migration `db/migrate/20260823100000_create_i18n_foundation.rb`：`platform_locales`（平台字典，無 shop_id）／`shop_locales`（生成欄位模擬部分唯一：每店恰一來源語言）／`translations`（六稽核欄、五欄 NOT NULL 唯一鍵、三條查詢索引）／`translation_status`；五語種子；既有店回填（en 來源已發布、其餘啟用未發布）；`staff_members.locale` 預設改 en。
- Models：`PlatformLocale`／`ShopLocale`／`Translation`／`TranslationStatus`＋`Locales::Tag` 正規化驗證；`Shop#enable_launch_locales`（after_create）與三個 `delete_all` 關聯。
- limits：首發五語、`source_locale_default`、`admin.create_only_in_source_locale` 依 C1 翻 false、`admin.ui_locales`。
- 鐵律 2 新增「平台字典表」類別（CLAUDE.md 註釋＋71 §A G24 登記＋`check-tenant-isolation.rb` NON_TENANT_TABLES）。
- 新增 spec：`spec/models/shop_locale_spec.rb`／`spec/models/translation_spec.rb`／`spec/services/locales/tag_spec.rb`（複驗：`bundle exec rspec spec/models/shop_locale_spec.rb spec/models/translation_spec.rb spec/services/locales/tag_spec.rb`）；全套 rspec 快照：`353 examples, 0 failures`；rubocop／brakeman 無 offense；check-tenant-isolation／limits-keys／money-boundary／reversal-naming／tokens-sync 全綠。
- `docs/dev/m1-i18n-foundation.md`（12.4 四件事）。

## 修改的檔案與核心邏輯 (Changes)

- `db/migrate/20260823100000_create_i18n_foundation.rb`、`db/schema.rb`
- `app/models/platform_locale.rb`／`app/models/shop_locale.rb`／`app/models/translation.rb`／`app/models/translation_status.rb`／`app/models/shop.rb`
- `app/services/locales/tag.rb`
- `config/limits.yml`、`scripts/check-tenant-isolation.rb`、`CLAUDE.md`、`docs/specs/71-admin-parity-sweep.md`
- `spec/models/shop_locale_spec.rb`／`spec/models/translation_spec.rb`／`spec/services/locales/tag_spec.rb`；`spec/models/idempotency_key_concurrency_spec.rb`／`spec/models/product_variant_spec.rb`（purge 順序加三表）
- `docs/dev/m1-i18n-foundation.md`

## 尚未完成或需注意的風險 (Pending / TODO)

- `translations` 表目前零寫入路徑（ML-2 接線）；`translation_status` 尚無重算器。
- 來源語言變更精靈未做（67 §C.3(c)）；現階段只能 migration 級切換。
- strong_migrations 教訓：新表 FK 要寫在 `create_table` 內（`t.foreign_key`），獨立 `add_foreign_key` 會被擋；種子 `execute` 要包 `safety_assured`。MySQL DDL 非交易，migration 中途失敗要手動 drop 殘表再重跑。
- 本機 MySQL 非服務、會隨程序結束：手動啟動指令見記憶檔（`mysqld --datadir=C:\Users\pisce\chilllove-mysql\data --bind-address=127.0.0.1 --port=3306`）。
