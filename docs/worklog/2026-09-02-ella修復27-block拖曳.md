# 2026-09-02 Ella 修復 PR-27：block 子層拖曳重排

## 已完成的工作 (Done)

- PR-26 pending 的同構擴充：block 列拖曳重排（同 section 內；
  `moveBlockTo` 走 applyOp＝undo/改即見/儲存全繼承）。
- 事件面三防線：block dragStart/dragOver/drop 全 `stopPropagation`——
  否則冒泡讓 section li 同時進入拖曳/接受 drop（巢狀 draggable 的通則）。
- 測試 ED26（重排以 Save payload 斷言真序＋跨 section 拖放無汙染單次
  儲存驗證）；stubFetch 擴 bootstrap 覆蓋參數（零既有測試 churn）。
  🔴 測試結構教訓：no-op 操作後儲存鈕禁用 ⇒「第二次 Save 斷言未變」
  結構性打不出去——改成「汙染嘗試在前、單次 Save 驗證」。
- 突變 1/1 殺（drop no-op）。
- 另收一枚探針教訓（PR-26 部署時的 BUNDLE-STALE 虛驚）：bundle 新鮮度
  檢查必須用**存活字面**（i18n 鍵最穩）——局部識別字被 minify 掉、
  伺服器時鐘是 UTC（本地 HKT +8）兩者疊加會誤判成「部署沒重建」。

## 修改的檔案與核心邏輯 (Changes)

- `app/frontend/admin/pages/ThemeEditorPage.tsx`：moveBlockTo＋
  blockDragRef＋block li 三事件。
- `ThemeEditorPage.test.tsx`：stubFetch bootstrap 參數＋ED26。

## 尚未完成或需注意的風險 (Pending / TODO)

- 拖曳視覺回饋（與 PR-26 同批登記）；跨 section 搬 block（本尊不支援
  ＝同帶語義，非缺口）。
