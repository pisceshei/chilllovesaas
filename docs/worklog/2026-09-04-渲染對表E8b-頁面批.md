# 2026-09-04 渲染對表 E8b：其餘頁面批（collections／products／search／pages／cart／404）

分支 `parity/e8b-pages`（自 main `17621f57`；E11／D82 合併後 main `6cc21f6e`，無衝突）。規範 `docs/dev/e8-render-parity.md` §2b（#28–#65）；
證據 `docs/dev/external-facts.md` §G17（含 E8b 追加）／§G18；未取得與範圍外 `docs/specs/91-pit-register.md` §3.75b；鐵律 22（D82）。

## 已完成的工作 (Done)

- 首輪對表（本機 mirror vs hoko 快照）：cart 12/15、frontpage 11/15、collections/all 10/15、search 8/12、product 7/15、contact／404 因 404 無法跑。
- 逐段追根因，修引擎／對表缺口三十八項（dev doc §2b #28–#65），分四批：
  - 第一批（#28–#43）：section 級動態來源、recommendations 初次渲染 stub、search 未執行 results_count、linklist 字串形與 `== empty`、
    link_to_vendor／type 帶 title、全商品集合標題「产品」、product 表單 product-id／section-id 尾接、篩選平台字串依語系、頁面 template_suffix、
    Normalizer 抹商品／變體 id（含 JSON 跳脫主機）、鏡像店頁面發布／模板／庫存、rake `ALLOW_404`。
  - 第二批（#44–#54）：`date` 吃時間戳、`cart.currency` symbol／name（`ThemeEngine::Currencies`）、預設變體 option1、無圖變體媒體、
    篩選集合預設只出 availability＋price、排序選項名稱依語系、`image_url` 對 nil、`url_escape`／`url_param_escape` 官方例逐字、
    集合頁卡片佔位框 `collection-apparel-3` 448×448。
  - 第三批（#55–#59）：`closest` 依模板資源填入（product／collection／article／blog／page）、動態來源解成 nil 不退回裸字串、
    過濾器啟用清單儲存位 `shops.storefront_filters`（migration）、availability 值 count 0 退後、Normalizer 商品卡 id。
  - 第四批（#60–#65）：自動系列預設排序 `most-relevant`（admin 實測）、預設變體 `options`＝["Default Title"]、傳統 block 的 `block.id` 裸 key、
    `link.current` 去市場前綴、Normalizer `ShareMessage-`／`NoMediaStandardBadge--`／一位數 `"id":`。
- 既有規格期望值依新證據更正：PT5／CT5（`/collections/all` zh 標題「产品」）、G2（無圖商品 featured_image 為 nil）、FA3／FA4（啟用 vendor／選項過濾器）。
- 規格：新 `spec/liquid/render_parity_pages_spec.rb` PP1–PP20；`render_parity_spec` RP7；`mirror_spec` MR1 追加（頁面發布、模板、庫存、sort_order）。
- 最終對表數字：見「對表結果」段（page14 報告；本機 mirror 店已跑 `render_parity:mirror[mirror]` 對齊）。

## 修改的檔案與核心邏輯 (Changes)

- `app/liquid/theme_engine/runtime.rb`：`render_section` 解析 section 級動態來源（以 `@closest`）；`resolve_dynamic` 純動態形解成 nil 即 nil；
  `ordered_block_drops` 傳統 block 不給 `instance_id`；全域 `recommendations` stub（performed false）。
- `app/liquid/theme_engine/drops.rb`：`ClosestDrop.from_template`（＋blog／page）；`SearchDrop#results_count` 未執行 0；`LinkListDrop#to_s`／`#empty?`；
  `LinkDrop#current` 去前綴比對；`CartDrop` 帶 money_format／currency；`VariantDrop#options`／`option1` Default Title、`featured_image` 退商品首圖；
  `ProductDrop#featured_image` 無圖 nil；`SORT_OPTION_NAMES` 依語系；`STOREFRONT_SORT`／`ORDER_SQL` 加 `most-relevant`／`most_relevant`；Facets 帶 locale／enabled。
- `app/liquid/theme_engine/page_renderer.rb`：`template_key_for` 吃資源 `template_suffix`；`runtime.closest = ClosestDrop.from_template(assigns)`（兩處）。
- `app/liquid/theme_engine/facets.rb`：平台字串表（en／zh）；`DEFAULT_ENABLED`／`ALL_FILTERS`／`enabled_for(shop)`；availability `zero_count_last`。
- `app/liquid/theme_engine/filters.rb`：`link_to_vendor`／`link_to_type` 帶 `title`；`date` 吃時間戳；`image_url` nil ⇒ 空；`url_param_escape`／`url_escape` 官方例。
- `app/liquid/theme_engine/tags.rb`：`FormTag` product 型尾接 product-id＋section-id。
- `app/liquid/theme_engine/currencies.rb`（新）：ISO ⇒ symbol／name 表（HKD／CAD 有真店證據，其餘 ISO 通用，V）。
- `app/liquid/theme_engine/page_titles.rb`：zh `products`＝产品。`app/liquid/theme_engine/placeholder_svg.rb`：FRAMES 加 `collection-apparel-3`。
- `app/models/collection.rb`：`SORT_ORDERS` 加 `most_relevant`。`db/migrate/20260904030000_add_storefront_filters_to_shops.rb`：`shops.storefront_filters` json（nil ⇒ 預設）。
- `app/controllers/storefront/recommendations_controller.rb`：`performed` 兩鍵。
- `app/services/render_parity/normalizer.rb`：id／JSON 跳脫主機與前綴規則；商品卡／分享／變體 JSON id 規則。
- `app/services/render_parity/mirror.rb`：頁面發布＋模板、`align_stock`、系列 `sort_order` 建立／對齊；`spec/fixtures/render_parity/hoko.json`：`template_suffix`、
  `inventory_quantity`、frontpage `sort_order: most_relevant`。
- `lib/tasks/render_parity.rake`：`ALLOW_404=1`。
- 規格：`spec/liquid/render_parity_pages_spec.rb`（新，PP1–PP20）、`spec/services/render_parity/render_parity_spec.rb`（RP7）、`mirror_spec.rb`（MR1）、
  `spec/requests/storefront_{page_title,collection_tags,drops_gap,facets}_spec.rb`（期望值更正／啟用過濾器）。
- 文檔：e8 doc §2b #28–#65、external-facts §G17（＋E8b 追加）／§G18、91 §3.75b、本 worklog／handoff。

## 對表結果（本機 mirror vs hoko 快照；最終 tree，報告 `report-page14-*`）

| 頁面 | 段落 1.000／總數 | 未達 1.000 的段落（相似度） |
|---|---|---|
| / | 18/21 | __head__ 0.927、header_default 0.984、cart_drawer_PFLQy3 0.988 |
| /collections/all | 12/15 | __head__ 0.926、header_default 0.983、cart_drawer_PFLQy3 0.988 |
| /collections/frontpage | 12/15 | __head__ 0.926、header_default 0.983、cart_drawer_PFLQy3 0.988 |
| /products/acme-tee | 11/15 | __head__ 0.922、main 0.954、header_default 0.983、cart_drawer_PFLQy3 0.988 |
| /pages/contact | 13/16 | __head__ 0.927、header_default 0.983、cart_drawer_PFLQy3 0.988 |
| /search | 9/12 | __head__ 0.927、header_default 0.983、cart_drawer_PFLQy3 0.988 |
| /cart | 12/15 | __head__ 0.926、header_default 0.983、cart-section 0.983 |
| /nope（404） | 9/12 | __head__ 0.926、header_default 0.983、cart_drawer_PFLQy3 0.988 |

未達 1.000 的段落全部屬已登記類別：`__head__`（平台 head 注入＋dev 主機）、header／cart_drawer／cart-section（本尊新版顧客帳戶 URL，91 ⚪）、
product `main`（本尊動態結帳按鈕 `shopify-accelerated-checkout` 骨架，91 ⚪）。首頁 header 0.984、其餘 0.983 皆同一組顧客帳戶 URL 差。

## 尚未完成或需注意的風險 (Pending / TODO)

- 未取得（91 §3.75b）：linklist to_s 底層、link_to_type title、zh-Hant 全商品標題、search 未執行官方值、Price／Brand／Type 篩選 zh 字串、cosy-lamp 實際庫存量（取 10）、
  替代模板頁的 section 請求、過濾器設定面、`closest` 無資源官方值、其餘 `collection-apparel-N` 框、自動系列「Default sort」完整值域與 most-relevant 排序語義。
- 範圍外（91 ⚪）：動態結帳按鈕（平台 web component）、新版顧客帳戶 URL、pages/contact 自訂 section 實例資料（本尊 page.contact 設定值未含於快照）、
  collections 商品卡有圖／無圖資料差。
- PR-8 記錄更正：`/collections/all` zh 標題「商品」⇒「产品」（hoko 快照＋live 皆「产品」；91 §3.75b 有更正條）。
- 部署：**有 migration**（`storefront_filters`）——bt3 `deploy.sh` 後 `RAILS_ENV=production bin/rails db:migrate` 再 restart；之後跑 `render_parity:mirror[mirror]`
  （頁面發布／模板／庫存／sort_order 對齊）再跑公開 diff。
- computed CSS 量測（鐵律 22.2②）尚未開始。

## 閘門（凍結 tree 後全跑，2026-09-04 本機，dev server 已停；scratchpad `gates_e8b.py` 逐支以 bash.exe 執行 config/ci.rb 步驟，log `gates_e8b.log`）

| 閘門 | 命令 | 退出碼 | 摘要 |
|---|---|---|---|
| Style: Ruby | `bin/rubocop` | exit=0 | 982 files inspected, no offenses detected |
| Security: Gem audit | `bin/bundler-audit` | exit=0 | No vulnerabilities found |
| Security: Frontend audit | `pnpm audit --audit-level high` | exit=0 | No known vulnerabilities found |
| Security: Brakeman code analysis | `bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error` | exit=0 | No warnings found |
| Test: Rails | `bundle exec rspec` | exit=0 | （未解析） |
| Test: Frontend | `pnpm test` | exit=0 |  |
| Type check: Frontend | `pnpm typecheck` | exit=0 |  |
| Build: Frontend | `pnpm build` | exit=0 |  |
| Invariants: Exec bits | `bash scripts/check-exec-bits.sh` | exit=0 |  |
| Invariants: Exec bit rules regression | `bash scripts/test-exec-bits-rules.sh` | exit=0 |  |
| Invariants: Prototype lint | `python scripts/lint-prototype.py` | exit=0 |  |
| Invariants: Lint rules regression | `python scripts/test-lint-rules.py` | exit=0 |  |
| Invariants: Dead-control baseline | `python scripts/check-baseline-raise.py` | exit=0 |  |
| Invariants: Doc claims | `ruby scripts/check-doc-claims.rb` | exit=0 |  |
| Invariants: Doc claim rules regression | `ruby scripts/test-doc-claims-rules.rb` | exit=0 |  |
| Invariants: Tenant isolation | `ruby scripts/check-tenant-isolation.rb` | exit=0 |  |
| Invariants: Design token single source | `ruby scripts/check-tokens-sync.rb` | exit=0 |  |
| Invariants: Reversal naming | `ruby scripts/check-reversal-naming.rb` | exit=0 |  |
| Invariants: Money unit boundary | `ruby scripts/check-money-boundary.rb` | exit=0 |  |
| Invariants: Money rules regression | `ruby scripts/test-money-rules.rb` | exit=0 |  |
| Invariants: Limits key types | `ruby scripts/check-limits-keys.rb` | exit=0 |  |
| Invariants: Limits key rules regression | `ruby scripts/test-limits-key-rules.rb` | exit=0 |  |
| Invariants: Rem token integrity | `ruby scripts/check-rem-tokens.rb` | exit=0 |  |
| Invariants: Rem token rules regression | `ruby scripts/test-rem-token-rules.rb` | exit=0 |  |
| Invariants: CI parity (ci.yml ⊆ config/ci.rb) | `ruby scripts/check-ci-parity.rb` | exit=0 |  |
| Invariants: CI parity rules regression | `ruby scripts/test-ci-parity-rules.rb` | exit=0 |  |
| Invariants: Workflow syntax | `ruby scripts/check-workflow-syntax.rb` | exit=0 |  |
| Invariants: Workflow syntax rules regression | `ruby scripts/test-workflow-syntax-rules.rb` | exit=0 |  |
| 突變 M114–M147（scratchpad `mutate_e8b.py`＋`mutate_e8b_extra.py`，commit 後跑） | — | — | 輸出貼於 PR body |
| doc-claims 兩支（worklog 閘門表寫入後補跑） | `ruby scripts/check-doc-claims.rb`／`ruby scripts/test-doc-claims-rules.rb` | 見 PR body | |
