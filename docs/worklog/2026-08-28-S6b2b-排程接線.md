# 2026-08-28 · S6b-2b 排程發布接線

## 已完成的工作 (Done)

1. **把 S6b-2a 的三個原語接進發布 modal**：每個 `supportsFuturePublishing` 的管道列
   多一顆日曆 icon，開出 `SchedulePopover`，設定的時刻進 `values.publicationDelta`。
2. **`PublicationDelta.publish` 從 `string[]` 升為 `PublishEntry[]`**（`{ publicationId, at }`）
   ——排程不另存狀態，它是 publish 的一個欄位。連帶改 `channelIsOn`／`toggleChannel`／
   `sameDelta`／`PublishingCard` 的消費點。
3. **送出時依 `at` 決定帶不帶 `publishDate`**：`null` ⇒ 整個 key 省略（後端對明確 null reject）。
4. **`serverScheduleOf` 用 `isPublished === false` 判排程**，不是 `publishDate` 非空。
5. **發布卡樂觀顯示待送排程**（`排程於 {at}（待儲存）` badge）＋**卡標題列的排程計數**。
6. **修掉一個既有缺陷**：`setLockVersion` 從「publishing mutation 之後」移到
   「`productSet` 成功後立即」——原本發布 mutation 失敗會讓 `lockVersion` 永久落後，
   之後每次儲存都撞 `STALE_OBJECT`。
7. **兩個時間基準釘死**：`shopTimezone` 走 `Query.shop.ianaTimezone`；
   `publishingOpenedAt` 在開 modal 那一刻取一次（不是每次 render）。
8. **文件**：新增 `docs/dev/m2-schedule-wiring.md`（150 行）。

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 核心變動 |
|---|---|
| `app/frontend/admin/pages/ProductDetailPage.tsx` | `PublishEntry` 型別；`publishEntry()`／`serverScheduleOf()`／`scheduleChannel()` 三個新純函式；`ChannelScheduleButton` 元件；`sameDelta` 改比 `(id, at)`；`scheduledCount` useMemo；`shop { ianaTimezone }` 進 query；`setLockVersion` 位置修正 |
| `app/frontend/admin/pages/ProductDetailPage.test.tsx` | 新增 `S6b-2b 排程接線` describe（＋203 行） |
| `app/assets/stylesheets/admin.css` | `.cl-card__head-badge`（卡標題計數）；`.cl-pubmodal__row` 改 flex 容納行尾 icon；`.cl-icon-button--on` |
| `app/frontend/admin/i18n/messages/*.json`（5 語言） | `schedule.publishOn`、`product.publishing.state.pendingAt` |
| `docs/dev/m2-schedule-wiring.md` | 新增 |


⚠️ `110` 是同一棵工作樹上另一個工作包的產物，**不隨本包提交**。

## 尚未完成或需注意的風險 (Pending / TODO)

1. **本尊在「已有暫存排程」時按 `Cancel` 的行為未取得**（§14.10）。我方沿用 S6b 的處置
   （丟棄整份 draft）。要取證需在測試店造出該狀態並抓包。
2. **多管道各自排程不同時刻的呈現形態未經對照**：測試店只有 Online Store 一個
   `supportsFuturePublishing` 的管道，本尊的多管道形態測不到。資料結構已支援。
3. **排程 badge 的文案是我方措辭**，非本尊原文（§15.9 只取到 tooltip 的 `Publish on: …`）。
4. **本包三個新畫面沒有本尊 CSS 量測**（層④缺口），登記於 `110`。
5. `MW2` 突變一開始殺不掉任何測試——fixture 缺「已發布 ∧ 可排程」的組合。已補第三個
   publication 修正。**同型風險**：任何「兩個布林欄位的組合判準」都要檢查 fixture
   是否真的涵蓋四格，不然錯的判準會全綠。

## 20.2 適用類型掃描（鐵律 20.3）

| 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ⑤ fail-open／happy-path-only／自我證明閘門 | 五個突變 `MW1`–`MW5` 逐一實跑 | `MW2` 首次未轉紅 ⇒ 補 fixture 後殺 4 格；其餘首次即轉紅 |
| ② 生產者已改、消費者未同步 | `PublicationDelta` 改型別後以識別字掃全部消費點 | `channelIsOn`／`toggleChannel`／`sameDelta`／`PublishingCard`／`FormValues` 五處同批改 |
| ③ 易腐計數與行號 | dev 文件不寫行號，改用函式名錨 | 本 worklog 的「75 格」附測試命令可重跑 |
