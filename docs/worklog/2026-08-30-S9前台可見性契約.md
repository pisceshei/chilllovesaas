# 2026-08-30 — S9 前台消費介面契約（D75）

## 已完成的工作 (Done)

- 鐵律 16 研究：四條官方錨逐字取證（Storefront API 未發布＝null＋connection 省略；
  Liquid product 44 屬性無 `status`；private mode 三句；UNLISTED direct-link——
  後者沿用 PR-C 裁定書既有 A 級錨）。
- 鐵律 12/14 實測（本地 Chrome＋curl，82 §20）：自建 `S9-VIS-Lifecycle-Test`
  走 A（active）→B（draft）→C（取消發布 OS）→D（archived）同 handle 四格；
  D53-QC fixture 唯讀補 unlisted 三格；密碼模式路徑矩陣（robots 200 / sitemap 404+location）；
  詳情頁狀態下拉值域窮舉（恰三值＋逐字副文案）；封存確認框逐字。
- `Storefront::Lookup`（app/services/storefront/lookup.rb）：W6 直連入口三方法，
  判準＝`purchasable`（unlisted 可直連）；查無回 nil（404/null 由消費端包裝）。
- 契約文檔 `docs/specs/93-storefront-visibility-contract.md`（§A–§F）。
- spec 9 格全綠；突變 M1–M4 實跑各自轉紅後還原。

## 修改的檔案與核心邏輯 (Changes)

- `app/services/storefront/lookup.rb`（新）：product_by_handle／product_by_id／
  collection_by_handle；Collection 單閘（無 status 欄）。
- `spec/services/storefront/lookup_spec.rb`（新）：U1/B1/C1/S1/N1 殺手格＋id/系列格；
  取消發布一律走真實生產者 `Publications::Write`。
- `docs/specs/93-…`（新）、`docs/research/82` §20（新節）、`docs/specs/91` §3.46、
  `docs/DECISIONS.md` D75。

## 尚未完成或需注意的風險 (Pending / TODO)

- 預覽站 publication 閘、unlisted 真店面 noindex、系列頁排除面＝**未取得**
  （91 §3.46 ⚪ 三條；W6 依官方錨實作，不依預覽站行為）。
- 店級閘門欄位（development／密碼、B2B-only）schema 未建——包 30/33 射程，
  行為契約已定於 93 §E。
- 測試店留存 fixture：`S9-VIS-Lifecycle-Test`（id 9917399335147）終態 archived、
  已取消發布 OS——供日後複測 C/D 格；未刪除。
