# 2026-09-01 Ella 整合修復 PR-1：主題面 CSP

## 已完成的工作 (Done)

- 🔴 使用者 2026-09-01 指正「前端完全沒有 Ella 的樣子／編輯器沒有即時預覽」
  ——六軸診斷開跑，本包收**根因 #1**：全域嚴格 CSP（style-src 'self'、
  script-src 'self'+nonce）把主題的 inline `<style>`／style 屬性／inline
  event handler／inline `<script>` 全部封殺 ⇒ Ella 前台退化成
  Times New Roman、無 CSS 變數、互動全死（本地＋生產雙實錘：
  document.body fontFamily＝"Times New Roman"、console 整排 CSP violation）。
- `ThemeCsp` concern：主題渲染面（Storefront::BaseController＋
  Admin::StorefrontPreviewController=編輯器 iframe）改
  style/script 'unsafe-inline'＋img/font 加 https:＋frame-ancestors 'self'
  （iframe 預覽）＋🔴 關 per-request nonce（CSP 規範：script-src 帶 nonce
  則 'unsafe-inline' 被忽略；inline event handler 只有 unsafe-inline 能放行）。
- admin SPA 不掛 concern＝嚴格 CSP 原樣（信任邊界：admin 是第一方代碼、
  主題是商家內容——本尊 storefront 對主題內容同樣不設此類 CSP）。
- 修復後本地實測：body/h1 fontFamily → "Jost, sans-serif"（inline 樣式全面
  生效）；C1-C3 spec 釘死三面。

## 修改的檔案與核心邏輯 (Changes)

- 新：app/controllers/concerns/theme_csp.rb、spec/requests/theme_csp_spec.rb
  （C1 storefront 放行＋C2 無 nonce＋C2b 編輯器 iframe＋C3 admin 維持嚴格）。
- 改：storefront/base_controller.rb＋admin/storefront_preview_controller.rb
  （include ThemeCsp）。

## 尚未完成或需注意的風險 (Pending / TODO)

- Ella 整合修復後續包（六軸診斷 wf_97765096-680 出爐後定序）：圖片管線
  （整頁 0 <img>）／divided-by-0（image aspect ratio）／color scheme→body
  背景鏈／字型庫覆蓋 Ella 預設字型／編輯器 blocks+section groups+即時預覽
  ／主題頁 preview 流。
- dev 熱重載踩 Zeitwerk 常量縫（ThemeEngine::Runtime::ClosestDrop
  NameError）——重啟即復，生產 eager-load 不受影響；如再現另查。
