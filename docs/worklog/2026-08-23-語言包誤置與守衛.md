# worklog 2026-08-23 — 語言包鍵誤置修正與缺鍵守衛

## 已完成的工作 (Done)

- 修正 `errors.collection.product_gid_invalid` / `product_not_found` 兩鍵在五個 yml 中被誤置到
  `errors.product` 底下的問題（`en.yml` 有兩個 `handle_taken:`，插入時配對到第一個 ⇒ 五檔一致地錯）。
- 重寫 `spec/config/locales_spec.rb`：
  ① parity 檢查**移除 `.slice(:product, :staff)` 白名單**，改成直接讀我方 yml 檔比整棵樹——
     白名單本身就是這次事故的成因（`errors.collection` 從未被檢查過）。
  ② 新增一條守衛：掃 `app/**/*.rb` 中所有字面 `I18n.t("…")` key，五語逐一解析，缺任何一個即 fail。
     **已做突變驗證**：五語同時移除該鍵後，該例確實轉紅並列出五筆缺鍵；還原後綠。
- 複驗：`bundle exec rspec`（快照 `401 examples, 0 failures`）、`bin/rubocop`（無 offense）。

## 修改的檔案與核心邏輯 (Changes)

- `config/locales/{en,zh-Hant,zh-Hans,ja,fr}.yml`：兩鍵自 `errors.product` 移到 `errors.collection`。
- `spec/config/locales_spec.rb`：改讀 YAML 檔（`tree_for`），新增字面 key 解析守衛。

## 尚未完成或需注意的風險 (Pending / TODO)

- 新守衛只掃**字面**key：`I18n.t(dynamic_key)` 或 `t(".relative")` 形式掃不到。
  目前 app/ 內沒有動態 key 的錯誤訊息，但這是已知盲區，不是「已覆蓋」。
- 前端語言包（`app/frontend/admin/i18n/messages/*.json`）由另一條路徑檢查，不在本 spec 射程。
- 🔴 教訓：這個 bug 三個閘門（rspec／rubocop／brakeman）全綠，**只有線上實測看得到**——
  因為 `I18n.t` 對缺鍵回傳 "Translation missing: …" 字串而不是 raise。
  斷言錯誤碼而不斷言訊息的測試，對這一類 bug 永遠是綠的。
