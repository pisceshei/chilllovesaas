# Parity R10：市場（R0-MISS1 結案＋G13 複核＋三套覆寫模型）

> 對應規格：docs/specs/71 §E R10、§F（R0-MISS1 ✅ 結案、R10-DOC1/DOC2 ✅；V1-V5）｜
> 研究：**docs/research/79**｜help：wf_eb481a44-705（4 主題＋2 critic，6/6，491K tokens）｜
> commit：見本檔所在 commit

## 已完成的工作 (Done)

- **規範再升級（使用者本輪追加）**：層⓪載入紀律補「**頁面有問題必須重新載入 1–3 次**」＋
  「**404 先懷疑自己猜錯 URL**（用側欄真實 href 驗證），不得斷言本尊無此頁」——三處同步
  （CLAUDE.md／AGENTS.md／71 §D），含使用者原文。
- **🔴 R0-MISS1 結案（掛 10 輪）**：實測揭曉「推出」**不是分階段推出市場**，而是
  **排程與 A/B 測試商店更新**（空態原文「測試並安排發布時間／為即將到來的銷售活動排程一組變更，
  或嘗試不同設計以查看何者成效最佳」）；路由是**頂層 `/rollouts`**（我先猜 `/markets/rollouts`
  得 404——是猜錯不是沒有）；建立式＝名稱 ≤255＋草稿 badge＋變更類型二選（網路商店佈景主題／
  結帳頁面和帳號）。已補 `rolloutsPage()`／`rolloutNew()`＋導航子項＋DOCS 3 條。
- **層③架構兩大發現**：①**四棵並行文檔樹**（/manual/markets、/manual/international、
  checkout-settings 的市場覆寫、themes 的 store-contextualization）——只走前兩棵會整段漏掉覆寫模型；
  ②**三套市場覆寫模型互不相同**（主題 4 型／結帳 3 型／一般逐項），粒度是「該覆寫類型停止繼承、
  其他類型續繼承」⇒ 資料模型鍵必須含 `override_kind`（V2，M2 前必答）。
- **層②值域窮舉**：狀態 2／更多動作 2／市場類型 4（含銷售管道市場，agentic 明文排除）／
  自訂項目 7／繼承的設定 6（實測）／稅額顯示 3／關稅收取 2／匯率模式 2／網域結構 3／
  B2B 配對 3／語言優先序 3 層＋多父層決勝序／方案分層矩陣全表。
- **🔴 G13 複核維持**：本尊市場級差異走「主題/結帳區段覆寫」而非「翻譯表 market 維度」——
  我方刪 `translations.market_id` 與本尊架構**不衝突**；日後市場差異化內容走主題區段覆寫。
- **🔴 撞鐵律 3**：Managed Markets 五段金額鏈（含 `1/(1-1.5%)` 換匯與**末端 ±2.5% 進位**）＋
  匯率 30 天保證與「退款沿用訂單建立日匯率」⇒ 已登記 V3（進 65 §H 測試矩陣＋匯率快照持久化）。
- **驗證**：lint ERROR 0/WARN 13；煙霧 6/6；**途中自己引入語法錯誤當場抓到並修**（NAV 註釋裡
  寫 `**/rollouts**`，`*/` 提前終止註釋→整個 script 掛掉；console 讀到 SyntaxError 後修復並
  以 fetch 驗證伺服器送出的檔案已乾淨）。

## 修改的檔案與核心邏輯 (Changes)

- `CLAUDE.md`／`AGENTS.md`／`docs/specs/71 §D`：層⓪補「重載 1–3 次」與「404 歸因規則」。
- `docs/research/79-admin-markets.md`（新檔）：§0 架構（路由樹/四棵樹/三套覆寫模型）＋§2 值域窮舉
  ＋§3 方案矩陣＋§4 金額鏈＋§5 硬限制＋§6 G13 複核＋§7 裁定面。
- `docs/design/chilllove-admin-v2.html`：NAV markets kids 補「推出」＋GROUP_OF＋
  `ROLLOUTS`/`rolloutsPage`/`rolloutNew` 新函式＋MODULES 註冊＋DOCS 3 條（doc-m-catalogs 改寫、
  rollout-empty、rollout-new）。
- `docs/specs/71`：§E R10 ✅、§F R0-MISS1 標結＋R10 七條。

## 尚未完成或需注意的風險 (Pending / TODO)

- **V2（三套覆寫模型資料設計）** 與 **V3（五段金額鏈＋匯率快照）** 皆為 **M2 前必答**。
- V1 推出第二層值域（層②本輪未完成項）；V4 方案閘門是否實作分層；V5 銷售管道市場 vs 我方
  AI 代理管道（R13）。
- 註釋寫法教訓：Markdown 粗體 `**` 緊貼 `/` 會構成 `*/` 提前關閉 JS 註釋——**註釋內不得出現
  `**/`**，已在本輪 NAV 註釋改寫避開。
