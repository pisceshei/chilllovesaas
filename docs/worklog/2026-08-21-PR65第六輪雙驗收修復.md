# PR #65 第六輪 Claude／Codex 驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `55b046f946a211cf528759046c224d2f93cd6423`。Claude Review run
  `32411285757` completed success、判詞 comment `5361142724`、wrapper `5361145255`；Codex
  review `4986877890` 的 inline 是 `3824945908`、`3824945918`、`3824945923`、
  `3824945934`、`3824945940`。同 head quality／test 都 completed success。
- 修法前以 `git log -p` 追到 R6 parser、fixture、CLAIM-005 與 P-2 終態表的每輪沿革；dated
  failure-first output snapshot 顯示 bad type、duplicate recheck、明確 ROOT 零索引都在舊 head
  exit 0，comment 已開啟後反引號內 `-->` 則會收尾並把第二個 CLAIM 暴露成重複 ID。
- Claude 所述 closer 行為經官方規格獨立查證。CommonMark 0.31.2 §4.6 逐字是
  "Start condition: line begins with the string `<!--`." 與
  "End condition: line contains the string `-->`."，來源
  <https://spec.commonmark.org/0.31.2/#html-blocks>（取證 2026-08-21）。因此採最小且相符的終態：
  code span 遮罩只作用於 opener；comment 開啟後任何 closer 子字串都收尾，不把 checker 擴成
  偏離 block end condition 的 parser。
- CLAIM-005 不再要求 delivered tree 之外的舊 Git object。官方 actions/checkout 說明逐字為
  "Only a single commit is fetched by default, for the ref/SHA that triggered the workflow."，來源
  <https://github.com/actions/checkout>（取證 2026-08-21）；本輪把 baseline 兩側 token 名單保存為
  `docs/specs/92-claim-index.md` 樹內快照，recheck 直接驗排序、去重、交集與分量。
  <!-- 🔴 2026-08-21 第七輪更正（Claude comment `5361587574` 🟡1）：上段修法已被證偽。
       CI 在 doc-claims 前實際執行 `git fetch --no-tags --unshallow origin || true`；樹內快照只驗
       自己可讓來源被改錯仍 exit 0。現行 CLAIM-005 先以 `git cat-file -e` fail-closed，再直接
       `git show` baseline 兩份來源重算，最後才與樹內名單交叉核對。 -->
- R6 現在拒絕未支援／拼錯的 `type:`、同區塊重複 `recheck:`；生產零索引 canary 不再因明確
  ROOT 關閉，只有帶 ROOT 的 `--fixture-mode` 可豁免，且 W2 明確禁止 CI 傳 fixture flag。
- P-2 主 worklog 的 Changes 終態表移除複製的易腐總數，改回 CLAIM-001～006 作精確重算入口。
  91 的方案摘要登記同時列回被點名的總方案與內部反查的階段一方案，不再把來源錯掛。
- Claude 本輪三條 ⚪ 只登記到 91 §3：opener 比 CommonMark start condition 更寬、畸形標頭時的
  零 count 訊息口徑、m0 已知限制未集中列跨行 code span／closer 邊界；沒有擴修未點名實物。
- 定向回歸已實跑：doc-claims runner 的 fixture／git／supply 情境全綠，CLAIM-005 樹內快照
  重算成功。GitHub Markdown API dated output snapshot：m0 rules `tr/th/td=8/3/21`、fixture fragment
  `31/3/90`、P-2 Changes `11/2/20`、本輪 20.4 `5/2/8`、20.3 `8/3/21`；各列未跳脫直線數也
  符合欄數。m0 fixture 表後既有缺空行仍只登記在 91，不把 fragment 結果外推為全檔已修。
  <!-- 🔴 2026-08-21 第七輪更正（Codex inline `3825176230`）：上列 GitHub Markdown API 的
       exact response／request artifact 沒有留存，故外部回應證據為「未取得」；數字只保留作
       當時本機診斷紀錄，不得發布為已驗證 GitHub 渲染事實或外推 current HEAD。 -->

### 鐵律 20.4 同根因復發記錄

| 要求 | 本輪證據 |
|---|---|
| 復發錨 | head `b2df242` 的 `3824670871`／`3824670885` 已要求 count cardinality 與零供給；head `55b046f` 的 `3824945940`／`3824945934` 又證明同一狀態空間仍漏 recheck cardinality 與明確 ROOT |
| 既有防線失效原因 | 前輪只對 count entries 與無 ROOT supply 寫負向案例，沒有列出 metadata 的每個 cardinality 維度，也把「明確 ROOT」錯當成「fixture」 |
| 被漏掉的固定步驟 | 鐵律 20.2.4／⑤要求列輸入來源與 multiplicity；本輪把 type 值、count 數、recheck 數、ROOT 模式拆成獨立狀態，再讓每一格有反向 fixture／supply scenario |
| 可重跑反向複驗 | `ruby scripts/test-doc-claims-rules.rb`；bad type、duplicate recheck、closer scope、無 ROOT supply、明確 ROOT supply 分別有具名輸出，任一路徑失配即整支非零 |

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗輸出 |
|---|---|---|
| ① 證據／當前 head／驗收攝取 | issue comments、review body、paginated inline 與 GraphQL threads 全量重取；每項綁 ID／head | 上列 Claude 與 Codex 輸入綁 `55b046f…`；push 前還要再做 cutoff 全量拉取 |
| ② 生產者／消費者／終態／歷史同步 | checker、runner、fixture、convergence、external-facts、92、91、主 worklog、PR 描述一起盤點 | repo 終態已同步；PR 描述待新 head push 後重建，歷史原文不覆寫 |
| ③ 易腐數字／完成性聲明 | 終態 Changes 不複製 CLAIM 數值；歷史快照改為樹內集合＋可失敗 recheck | CLAIM-005 recheck 輸出 declarations／overlap／new_tokens；其餘終態引用 CLAIM ID 而不手抄總數 |
| ④ 狀態空間／競態窗口 | type 值、count multiplicity、recheck multiplicity、ROOT／fixture、生產淺 clone 分開 | bad type、duplicate count、duplicate recheck、S1、S2、W2 各走獨立分支 |
| ⑤ fail-open／happy path／自我證明 | failure-first 舊洞與修後負向案例並存；乾淨 fixture 仍必須 exit 0 | `doc_clean` 與合法 count 為 happy path；新增負向 fixture 均要求特定非零原因 |
| ⑥ workflow 本機假綠／字面塊 | 本輪不改 workflow；官方 checkout 預設深度只作 recheck 設計證據，不把本機完整歷史成功外推 CI | `git diff --name-only 55b046f..HEAD -- .github/workflows` 提交後須為空；CLAIM-005 不再依賴 fetch depth |
| ⑦ Markdown／Windows 工具鏈 | 中文路徑固定 `core.quotepath=false`；表格做未跳脫直線與 GitHub render 檢查 | `git diff --check`、GitHub `/markdown` 表格計數與 Windows production-equivalent runner 待收尾 |

<!-- 🔴 2026-08-21 第七輪更正（Claude comment `5361587574` 🔴1／🟡1）：上表第 ② 列的
     同步盤點漏了「被改腳本自己的檔頭／用法／退出碼表」，第 ③ 列又把樹內快照當可失敗
     source recheck。現行固定處理已把契約層列入同 commit 消費者，CLAIM-005 改讀 baseline
     source；本段保留原表作歷史失效證據。 -->

## 修改的檔案與核心邏輯 (Changes)

- `scripts/check-doc-claims.rb`：加入明示 fixture mode、unsupported type、duplicate recheck 與明確
  ROOT 零供給 fail-closed；保留 closer 的 CommonMark block-end 行為。
- `scripts/test-doc-claims-rules.rb` 與新增 fixture：新增 bad type、duplicate recheck、closer scope、
  explicit-root supply 與 CI fixture-flag wiring 斷言；既有 fixture／git 情境改為明示 fixture mode。
- `docs/specs/92-claim-index.md`：CLAIM-005 保存 baseline 兩側樹內集合，recheck 不再 `git show`
  delivered tree 未保證取得的歷史物件。
- `docs/dev/external-facts.md`、`docs/dev/m0-review-convergence.md`：終態契約收窄成 opener-only
  遮罩，補 CommonMark HTML block start／end 逐字與 supply／metadata 契約。
- `docs/worklog/2026-08-21-P2宣稱索引與R6.md`：Changes 表刪除無相鄰 recheck 的精確總數，改指
  CLAIM 終態入口；同步 parser 與 fixture 的實際行為。
- `docs/specs/91-pit-register.md`：修正總方案／階段一方案來源歸屬，將已點名 supply 缺口轉結案
  候選，並登記本輪三條範圍外觀察。
- `docs/worklog/2026-08-21-PR65第六輪雙驗收修復.md`：保存本輪 exact-head 輸入、外部證據、
  failure-first、20.4／20.3 與待辦。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 最後 repo edit 後仍須跑完整 Windows production-equivalent runner、commit 後 doc-claims
   warning 0 與 CLAIM rechecks；定向回歸與 Markdown fragment render 不能代替全閘門。
2. commit 後須依鐵律 15 全量重拉三個 REST 端點與 GraphQL threads、更新 branch/tag、重建 PR
   描述並取得新 exact-head CI、Claude、Codex；`55b046f` 的綠燈不得外推。
3. #65 修改 `scripts/`，命中鐵律 18.3。即使雙零、threads resolved、mergeable 與 evaluator
   `1111` 全成立，也只能通知使用者人工合併，不得代行。
