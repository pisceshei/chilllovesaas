# PR #61：Codex `35c9eea` 修復查證

## 已完成的工作 (Done)

- 攝取並核對 current-head Codex review `4979980175`；它精確綁定
  `35c9eea83f2ea770013b1e383508ab240c09bb94`，共有五則 inline：`3819360600`、
  `3819360609`、`3819360615`、`3819360628`、`3819360637`。遠端驗收攝取已以 PR comment
  `5352622049` 四段交接，沒有為遠端結果另造會改變受驗 head 的 commit。
- 依 `git log -p` 與現行樹查證三條內部一致性：`HANDOFF.md` 的「鐵律 1–20」在同一樹已有
  鐵律 21，固定計數確實腐化；§2 的 A–F umbrella 沒有物化 §3.3／§3.4 已明載的輸入依賴；
  `.github/workflows/claude-review.yml` 的評估器只在該 run 內執行一次，Codex 晚到後不會自動重評。
- Shopify 官方 `fulfillmentOrderMerge` 只說 "Merges a set or multiple sets of fulfillment orders
  together into one"；`fulfillmentOrderSplit` 只說 "Splits `FulfillmentOrder` objects"。兩段均只
  證明履約訂單能力，不能證明銷售訂單層不存在拆併。
- Shopify 官方另明載 app 可建立 "a single discount code that includes more than one discount
  saving"。它證明「單一折扣、多種優惠」存在，但不是「一次領取多張券」；因此不能用來證明
  券包存在，也不能沿用有限檢索推出券包不存在。
- GitHub 官方明載 workflow re-run "will also use the same `GITHUB_SHA` (commit SHA) and
  `GITHUB_REF`"；GitHub CLI `gh run rerun` 可 "Rerun an entire run"。現行 review job 可能以
  success 結束，即使 C1 因 Codex 晚到而是 `noreview`，所以修復方案使用整個 run 重跑，禁止用
  `--failed`；current-head validation-skip 仍走既有人工停點，不重跑一個必然再次 skip 的 run。

外部來源均於 2026-08-20 取證：

- Shopify `fulfillmentOrderMerge`：
  <https://shopify.dev/docs/api/admin-graphql/latest/mutations/fulfillmentOrderMerge>
- Shopify `fulfillmentOrderSplit`：
  <https://shopify.dev/docs/api/admin-graphql/latest/mutations/fulfillmentOrderSplit>
- Shopify Creating discounts with apps：
  <https://help.shopify.com/en/manual/discounts/discount-types/discounts-with-apps>
- GitHub Re-running workflows and jobs：
  <https://docs.github.com/en/actions/how-tos/manage-workflow-runs/re-run-workflows-and-jobs?tool=cli>
- GitHub CLI `gh run rerun`：<https://cli.github.com/manual/gh_run_rerun>

## 修改的檔案與核心邏輯 (Changes)

- 本 worklog 與 `docs/handoff/2026-08-20-PR61-Codex-35c9eea修復查證.md`：保存五則意見的
  事實核對、修法選擇、被推翻假設與實作入口。
- `docs/specs/91-pit-register.md`：附錄 A 納入本步兩檔；另登記 PowerShell
  `ConvertFrom-Json` 日期二次解析受本地時區影響而假判「無新增」的未點名工具坑，不順手擴修。

## 尚未完成或需注意的風險 (Pending / TODO)

- 五則 inline 尚未實作、回覆或 resolve；本步只完成修法前查證。下一步只可修改被點名的
  HANDOFF 入口、A–F 依賴、晚到評估器處置、券包證據邊界與銷售訂單拆併證據邊界。
- `gh run rerun` 的方案只適用於 current-head Claude run 已產生合法 bot 判詞、Codex 與 CI
  之後到齊、但同一 run 的評估留言因 C1 晚到而非 1111 的常規 PR；validation-skip、head 變動、
  bot 無判詞或 CI 未綠都不得套用。
- 本研究 commit 之後仍須跑 post-commit doc-claims；實作完成後才跑完整 29 閘門與 GitHub 新 head
  驗收。不得把研究完成寫成五條已修。
