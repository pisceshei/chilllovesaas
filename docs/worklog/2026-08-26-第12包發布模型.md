# 2026-08-26 第 12 包：發布模型收口（生產者 ＋ 回填 ＋ 可購買／可發現）

## 已完成的工作 (Done)

1. **先對本尊做了完整實測，再動手**。使用者裁定：「我們要和 shopify 本尊一樣的處理方式」
   ⇒ 本包的每一條設計決策都有實測或官方取證支撐，不是專案裁定。
   憑證落 `docs/research/82-admin-channels.md` **§8**（新增章）：
   - §8.2 讀取面是**正向、稠密**（未觸碰的變體回全部管道、不是 0 列 ⇒ 不是「無列＝繼承」）
   - §8.3 寫入面是**逐 publication 增刪**（admin 送獨立的 `ProductVariantUnpublish`），不是整批覆寫集合
   - §8.4 四條生產者規則（建立即物化／變體集合／層與層不連動／status 正交）
   - §8.5 `ProductStatus` 值域窮舉，帶本尊下拉內的一行語義原文
   - §8.6 UI 形態八項（Channels 欄／Publishing 卡／兩種 modal／變體表 Publishing 欄⋯）
   - §8.7 誠實登記本輪未取得的四項
2. **`Publications::Materialize`**（新）：對所有 `auto_publish` 管道補齊發布列，冪等。
3. **三個 `after_create`**：`Product` / `ProductVariant` / `Collection` 各掛一個。
4. **回填 migration** `20260826060000`＋配對 spec（含 source-guard）。
5. **`Product.purchasable` / `.discoverable`**：解除了 `product.rb` 那段「刻意不做具名 scope」
   的封印——當時卡住的是**寫入端缺席**，本包補上後才打開。
6. **`Product.published_on`**：發布層的兩個 EXISTS（商品層 ∧ 至少一個變體層）。
7. **既有測試的連帶修復**：六支併發 spec 的 `purge!` 各補「先刪 `resource_publications`」；
   `resource_publication_spec.rb` 加 `without_auto_publications` 幫手。
8. 文檔：`docs/dev/m2-publication-model.md`（新）、`docs/plans/2026-08-26-第12包執行規格.md`（新）、
   `88` §5 待辦 #2／#5 結案、`91` §3 登記五條範圍外觀察。

### 🔴 本輪最值得記的一件事：官方文檔與實測衝突，且我一開始把實測寫得太滿

官方逐字說新變體「published to all channels **where the parent product is published**」，
而我實測到新變體拿到了父商品沒有的管道。我第一版把它寫成「本尊不跟父商品，實測為證」——
**那是過度解讀**：加選項時新變體可能由前身 `Default Title` 變體衍生，而它本來就有全部管道的列，
我這個實驗分辨不了這兩個假說。

回頭補做 UI 側觀察後才看清楚：變體在父商品未發布的管道上**確實有列**（toggle 是開的），
只是被灰掉並附提示「Product must be published to the channel before variants can appear」
⇒ 官方那句描述的是**生效狀態**，我量到的是**儲存狀態**，兩者可以並存、不衝突。

⇒ 最終處置：採「全部 auto_publish 管道」，但在 82 §8.4②、`m2-publication-model.md` §4、
model 註釋與 spec 訊息**四處**都標明這是**我方裁定（ours）而非照抄**，並登記那個排除不掉的
替代假說為未取得。**兩種選法對可見性的結果完全相同**（閘控在讀取層的 AND），所以這是低風險裁定。

### 🔴 當場複驗到第 11 包的部署事故機制

同一支回填 migration：本機**開發庫回填 3 列**（有既有資料）、**測試庫回填 0 列**（空庫），
**兩次都 exit 0**。這正是第 11 包 `NoTenantSet` 只在部署時才炸的結構原因——
迴圈本體在 CI 與開發庫都沒被執行到。⇒ 配對 spec 先種既有資料再跑回填，另加 source-guard
盯著 migration 檔案本身有沒有 `without_tenant`（spec 用的是邏輯副本，拿掉那行其他格全會綠）。

## 修改的檔案與核心邏輯 (Changes)

| 檔 | 變動 |
|---|---|
| `app/services/publications/materialize.rb` | **新**。整段跑在 `ActsAsTenant.without_tenant` 內、`shop_id` 一律取自 `publishable` 本身——因為 `after_create` 會在 seeds／factory／rake／migration 底下觸發，那些路徑沒有租戶；而 current_tenant 是別間店時 default scope 會把管道過濾成 0 列、**一列都不建又不拋錯** |
| `app/models/product.rb` | 加 `after_create :materialize_publications`；加 `purchasable`／`discoverable`／`published_on`；改寫「刻意不做 scope」那段註釋（保留原理由 ＋ 記錄解除條件） |
| `app/models/product_variant.rb` | 加 `after_create :materialize_publications` ＋ 一段長註釋說明為什麼**不讀父商品**（含官方原文、UI 提示原文、替代假說、選擇理由） |
| `app/models/collection.rb` | 加 `after_create :materialize_publications` |
| `db/migrate/20260826060000_backfill_resource_publications.rb` | **新**。逐店取 `auto_publish` 管道 → 三型別分批 → `insert_all`；`down` 為 `IrreversibleMigration` |
| `spec/models/product_spec.rb` | 「刻意未實作」區塊（斷言 `not_to respond_to(:purchasable)`）換成「發布層」組——生產者 7 格、讀取 9 格、層不連動 2 格；每一格的測資都刻意落在**兩種實作會分岔**的那一點 |
| `spec/migrations/p12_backfill_publications_spec.rb` | **新**，6 格含 source-guard |
| `spec/models/resource_publication_spec.rb` | 加 `without_auto_publications` 幫手 |
| 六支併發 spec | `purge!` 各補一行 `ResourcePublication.unscoped.delete_all` |
| `docs/research/82-admin-channels.md` | 新增 §8（本輪實測全文） |
| `docs/dev/m2-publication-model.md` | **新** |
| `docs/plans/2026-08-26-第12包執行規格.md` | **新**（鎖範圍） |
| `docs/specs/88-publication-model.md` | §5 待辦 #2／#5 結案 |
| `docs/specs/91-pit-register.md` | §3 新增子節 |

**閘門結果**（本地，凍結 tree 後逐支看退出碼）：
rspec 1040 例 0 失敗｜rubocop 434 檔 0 offense｜brakeman 0 warning｜
tenant-isolation OK｜doc-claims ERROR 0／警告 0。

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **`insert_all`／`upsert_all` 繞過 callback ⇒ 匯入器（第 20+ 包）是未來炸點**。
   匯進來的商品會**全部沒有發布列 ⇒ 前台全部看不到，而且不拋任何錯**。
   做匯入器的人必須逐列走 model，或批量寫入後明確呼叫 `Publications::Materialize.for`（冪等）。
   已寫進 `m2-publication-model.md` §6 的消費者影響圖。
2. **`Collections::RuleCompiler` 仍用 `p.status <> 'archived'` 而非 `purchasable`**。
   本包刻意不改——那是語義變更（智慧系列會開始排除未發布商品），要另外裁定。已登記 `91` §3。
3. **第三層 catalog 完全沒做**（88 §3.2 裁定延後到 M5）。本包的「三層 AND」實作只有兩層。
4. **publish／unpublish mutation 與 `Publishable` 讀取欄位不在本包**（超出權威計畫表第 12 列，
   且無 v1 消費者）。契約形態已取證完畢，見第 12 包執行規格 §2.4。
5. **未取得四項**（P12-B3／B4／B7 與變體生產者的替代假說 B5），逐條在
   `m2-publication-model.md` §8 與 `82` §8.7。
6. **測試店狀態刻意留在「三層彼此不一致」的形態**（`Product/9911273160939` 是 Unlisted、
   商品層只在 2 個管道、變體在 3 個），它是目前唯一的活體 fixture。刪掉要重做一次才能複驗。
