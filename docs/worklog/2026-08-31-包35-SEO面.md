# 2026-08-31 包 35：SEO 面——hreflang／canonical／sitemap／JSON-LD／robots 開放／llms

## 已完成的工作 (Done)

- **`Seo::HreflangMatrix`**（62 §I.1 唯一實作，`<head>` 與 sitemap 共用）：active 市場×
  presence×**開放∧已發布**語言（與 PrefixIndex／LocalizationContext 同一開放集，三處同軸）
  ×逐國展開碼（Markets::HreflangCodes）；primary 先行 dedupe；x-default＝主網域 primary
  預設語言帶前綴 URL（不指 /）。自指＋雙向＝同一函式同一集合（SEO2 兩語言頁 byte-equal）。
- **`Seo::HeadTags`**（62 §A.1 平台注入層，經 `content_for_header`——只新增節點）：
  canonical（絕對、自引、去 variant／utm、留 page——§B.1）＋hreflang link 群＋商品 JSON-LD；
  **UNLISTED ⇒ meta robots noindex，且 hreflang／JSON-LD 整段不出**（limits
  `product.unlisted_meta_robots`／`unlisted_excluded_from` 落地為生產行為）。
- **`Seo::JsonLd`**（62 §A.3/A.4/A.5/A.6）：price＝cents 直出兩位小數字串（divmod，
  無 float、無逆向 parse）；priceCurrency＝presentment；availability 五態對映
  （只看可用；continue⇒BackOrder）；priceValidUntil 無檔期即整段省略。
- **Sitemap**（62 §C；形態鏡射 83 §3.6：sitemapindex＋products_N 子表）：products＝
  **discoverable**（UNLISTED／draft 排除）、collections＝published、pages＝visible；
  `xhtml:link` 用同一矩陣函式；lastmod=updated_at；50k 上限以 PER_PAGE=10000 分片。
- **robots 開放**（B13 撤除，兩處一起摘）：全站 Disallow 與 X-Robots-Tag noindex 移除；
  改 §D.2 預設 disallow 集合（/cart /checkout /account /search＋facet／preview 參數）
  ＋平台保底 Sitemap 行。包 33 的 S3/S5 格同步改為新契約。
- **llms 三別名**（62 §H.2/H.3）：/agents.md＝/llms.txt＝/llms-full.txt 同一生成器
  （store_name／store_url／sitemap_url／currency；ucp／mcp 未實作 ⇒ **不輸出欄位**）。
- Fixture layout 補 `{{ content_for_header }}`（本尊 layout 必有的注入點）。

## 修改的檔案與核心邏輯 (Changes)

- `app/services/seo/{hreflang_matrix,head_tags,json_ld}.rb`（新）
- `app/controllers/storefront/{sitemaps,agents}_controller.rb`（新）；`pages_controller.rb`
  （robots 開放＋摘 noindex）；`config/routes.rb`（sitemap 分片＋llms 三路由）
- `app/liquid/theme_engine/page_renderer.rb`（resolve 回傳 record＋content_for_header 注入，
  只在公開店面——預覽面維持 noindex 牆後空注入）
- fixture `layout/theme.liquid`；`spec/requests/storefront_seo_spec.rb`（SEO1–SEO8）；
  `storefront_pages_spec.rb` S3/S5 改新契約

**突變驗證（20.2⑤／20.3）**：
| # | 突變 | 預期紅 | 結果 |
|---|---|---|---|
| MUT-s1 | 矩陣拿掉 published 閘 | SEO2（zh-Hans 漏入） | 1 failure ✅ |
| MUT-s2 | JSON-LD price float 化 | SEO3 | 1 failure ✅ |
| MUT-s3 | sitemap 改 purchasable | SEO6（UNLISTED 漏入） | 1 failure ✅ |
| MUT-s4 | HeadTags 拿掉 unlisted 分流 | SEO4 | 1 failure ✅ |
| MUT-s5 | canonical 保留 variant | SEO1 | 1 failure ✅ |

## 尚未完成或需注意的風險 (Pending / TODO)

- **published_in?(market) 過濾未接**（62 §I.1 演算法列）：market×catalog 掛接（29 §1.3）
  未落——單市場 v1 全發布等價；多市場＋market catalog 落地時**必須**補（否則未發佈到
  該市場的資源會列出 404 hreflang，違反不變量 4）。
- robots.txt.liquid 主題覆寫層（§D.1 本尊形態）／AI 爬蟲三組開關（§D.3）／IndexNow
  （§C 規則 3）未做；agents.md 的 Liquid 模板 fallback 鏈（§H.3(a)）未做。
- sitemap 無快取層（14 §F5「別即時生成」——v1 店量小；大店前補 key-based 快取）；
  lastmod=updated_at（「庫存數不算內容更新」的細分未做——庫存量本就不動該欄，
  但價格／發布動 updated_at 屬內容性，方向正確）。
- JSON-LD 只出 Product 型（§A.2 矩陣的 Organization／BreadcrumbList 等待後續）；
  price 用 variant.price_cents（presentment＝店幣，單市場）——多市場 price list 解析
  （PriceView/resolve_price_view）接上時本模組換輸入不換格式器。
- SeoPriceParityTest 的 feed／catalog 兩消費者（§A.3 表 3/5）未存在——SEO3 已釘
  SSR＋JSON-LD 兩處；feed 包落地時擴成四方全等。
- meta_title/meta_description 的 head 輸出（62 §E 樣板）未做（Fields overlay 已備）。
- 62 §J 平台子網域→自訂網域 301 全量（§B.1 行 8）＝步 5/hosting 後續。
