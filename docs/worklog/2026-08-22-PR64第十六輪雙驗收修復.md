# PR #64 第十六輪雙驗收修復

## 已完成的工作 (Done)

- 驗收輸入綁 exact head `82e2fa8fb4979c046bd57c1049f3be302bfc0fb5`：Claude 判詞
  `5364180385`（🔴 **0 條**、🟡 2 條、⚪ 2 條）、wrapper `5364180436`；Codex review
  `4988979665`（COMMENTED，兩則 P1：`3826627161`／`3826627165`）**綁同一 head**。
  🔴 **判詞的「未覆蓋-1」已被事實推翻**：它寫「Codex 尚未對 current head 出任何 review」，
  但該 review submitted `01:34:26Z`，比判詞（`01:44:55Z`）**早 10 分鐘** ⇒ 18.1① 在本 head
  其實已滿足，卡住的是那兩則 P1。判詞當時漏攝。
- 🟡-1（`EF` B9 歸屬句只有兩個槽位）以**逐句對應**修復：blockquote 三句分屬 `-f/--raw-field`
  ／`-F/--field` magic type conversion／`--input` 三個**不同**旗標；原句依 19.5 留更正註。
  🔴 保住的是本節的規範性防線——「不得把 `@filename` 外推到 `-f`」
  （射程複驗：`grep -n '不得外推到 \`-f\`\|不得把後句外推到 \`-f\`' docs/dev/external-facts.md`
  ⇒ B9 節內只有這一句是**禁止性**斷言，其餘為描述性逐字）。
  它擋的正是本倉庫已發生過的 `-f text=@path` 送出字面 `@path` 事故。
  <!-- 🔴 2026-08-22 更正（來源＝Codex inline `3835660389`）：本行原本用了一個沒有查法的全稱句，
       在 `ruby scripts/check-doc-claims.rb --base 0fbe520 --require-base` 下觸發 R5 警告。
       已補上射程複驗指令（`grep -n '不得外推到 \`-f\`' docs/dev/external-facts.md`）並把該全稱句
       改為可驗證的敘述。
       ⚠️ **Codex 給的成因不成立，另記於此**：它寫「預設跑法回報 0 是因為 `origin/main` 取不到、
       R4／R5 被跳過」。本輪實跑否證——本工作樹 `git rev-parse origin/main` 回
       `0fbe520502588b34f9b9cad6ae9b3a282d4db643`（與顯式 base 同一個 commit），
       兩種跑法都回報**同樣的 1 條警告**、掃描範圍同為「20 份、1893 個新增行」。
       🔴 **真正的成因是時序**：我上一輪是在 **commit 之前**跑的，當時是 0；
       `check-doc-claims.rb` 的 R4／R5 掃描集合取自 **committed diff 的行號**、讀的卻是
       **working tree 的內容**，未提交的插入會讓兩者錯位 ⇒ commit 前的結果不可信。
       這與同日 PR #66 第 34 輪踩到的是**同一個坑**（該輪 worklog 的 Pending 有全文）。 -->
- 🟡-2（20.3 ⑦ 列的 `th=`）以**清法①**修復，並補上計數式**與錨定 commit**：`th` 由 7／8／14
  改為 **5／6／11**，且明文錨定在受驗樹 `82e2fa8`。
  🔴 7／8／14 是 `<th` **前綴**命中數，會一併算進 `<thead>`（各檔 2／2／3 個）。
  🔴 **光改數字不夠**：這幾個數描述的正是本輪要改的那幾個檔，不錨定就會被自己的編輯改掉
  （見下方 20.4 實跑）。
- Codex `3826627161`（W14 的歷史表格被就地改寫）：**還原第十四輪原表**，更正後的內容改以
  附加表呈現，兩則更正註逐段說明。
- Codex `3826627165`（B10 的條件是中文轉述、無英文逐字）：補上兩句官方逐字，並寫明
  `{diff-merges-default}` 這個 asciidoc 屬性的展開依據。
- 兩條 ⚪ 依 17.2 只登記 `91` §3，不順手改其 consumer。

### 鐵律 20.4 同根因復發記錄

| 要求 | `<th` 前綴計數法第二次復發 | 歷史層被就地改寫 |
|---|---|---|
| 復發錨 | 第三輪已證偽 `<th` substring 並固定計數式為 `<tr\b`／`<th\b`／`<td\b`（該輪 worklog 逐字寫下）；第十四／十五輪與 PR 描述**三處**又退回舊算法 | 第十五輪為修 🟡1 而**就地改寫**第十四輪的 20.3 表（表頭＋七列第三欄），`git log -p` 可見改寫發生在 `82e2fa8` |
| 既有防線失效原因 | 第三輪的固定式**只寫在該輪 worklog 的敘述裡**，沒有進入任何送驗前檢查；下一輪換人寫表時，最順手的 `grep -c '<th'` 又回來了 | 「更正必須留下原文」這條分層規則，在本倉庫是**寫在別的檔**（AGENTS 分層節）；W14 自己的更正註格式沒有要求「原文保留」，於是改寫看起來像是在照規則做 |
| 被漏掉的固定步驟 | 凡發布 render 計數，**必須同格寫出計數式**；只寫數字時下一個人無從判斷用的是哪一種 | 更正歷史層時，**先問「原文還在不在」**；答案是否時，改成「原文保留＋附加更正」而不是替換 |
| 可重跑反向複驗 | `git -c core.quotepath=false grep -nE '\bth=[0-9]' -- docs`（加 PR 描述與未追蹤新檔）：**終態層**每一處都必須在**同區塊（實作為 ±12 行窗口，實測值，見下）**內同時出現計數式（`<th\b`）與**錨定 commit**；**歷史層原文豁免**（依 19.5 不得就地改），但同檔必須有一則帶齊兩者的附加更正；**當輪自跑的終態樹快照**只需標明「終態樹」——它由本 commit 自己定義，錨定一個尚不存在的 sha 在邏輯上做不到（🔴 推論：**描述終態樹的計數，只能發布在被計數的那棵樹「之外」的載體**，如 PR 描述或驗收留言）。🔴 兩個陰溝實測到：`quotepath` 預設把 CJK 路徑引號化使後續 `sed` 全部讀不到；不加 `\b` 會誤命中 `innerWidth=`／`--depth=1` | `git log -p --follow -- <歷史 worklog>` 逐段看：更正段以外的正文，其 `-` 行**只能是把先前的就地改寫還原回原文**；出現任何**不是還原**的 `-` 行即為就地改寫 |

**本輪實跑（兩道反向式當輪就跑）**

- 計數複驗（**錨定受驗樹 `82e2fa8`**，以 `git show 82e2fa8:<path>` 取檔後送 `/markdown`）：
  W14 `table=2 tr=13 th=5 td=29`、W15 `table=2 tr=13 th=6 td=33`、
  `91` `table=3 tr=25 th=11 td=72`、`external-facts` `table=0`。
  同時印出 `<th` **前綴**數為 7／8／14、`<thead>` 數為 2／2／3 ⇒ **差值恰為 `<thead>` 數**，
  根因由本輪自己的量測直接證實，不是引用判詞。
  🔴 **必須錨定**：這些數描述的是被本輪修改的那幾個檔，不錨定的話，寫下它們的那次編輯
  自己就會讓它們過期——實測本輪為 W14 加上附加表之後，W14 在**最終樹**的 render 已變成
  `table=3,tr=21,th=8,td=50`。要最終樹的值就重跑該命令，本檔不覆述。
- 歷史層複驗（`git diff -U0 -- <W14>` 實跑）：`-` 行共 **6 行**，分屬兩區——
  **正文（式子管轄範圍）2 行**，都是把第十五輪的就地改寫還原回第十四輪原文（表頭列與 ⑦ 列）；
  **更正註區（式子明文排除）4 行**，是舊更正註被新的兩段式更正註取代。
  ⚠️ 本輪初稿把這條反向式寫成「只有新增、沒有刪改」，**實跑立刻自打嘴巴**——還原本來就會刪行。
  已改為「`-` 行只能是還原」。

- 🔴 **反向式①自己先跑壞了一次，修式子後的實跑輸出**（終態樹）：

  ```
  命中總數 14（docs 全樹 ＋ 未追蹤新檔 ＋ PR 描述），窗口 ±12：
    PASS   11
    EXEMPT  3   ← 全部是歷史層，逐一列名如下
  EXEMPT ①② 2026-08-21-PR64第三輪雙驗收修復.md  L26／L29   fm=1 an=0
             （該輪有計數式、無錨定 commit；依 19.5 不得就地補，已入 Pending）
  EXEMPT ③  2026-08-21-PR64第十四輪雙驗收修復.md L61        fm=2 an=1
             （本輪還原回去的第十四輪原表；錨定與計數式由正下方的附加更正註承擔）
  ```

  🔴 **本檔自己的命中不列行號**：它們也是自指的，貼上這段輸出就會位移。
  要逐行看就在合併後的 commit 上重跑上面那條指令。

  🔴 **窗口大小是量出來的，不是拍的**：同一批資料掃 ±5／±8／±12／±16 四種窗口，
  通過數 6／9／10／11，剩下的 FAIL 逐一看過——±5 與 ±8 的 FAIL **全部是誤判**
  （計數式與錨定是每個區塊寫一次，不是每行重複）；±16 之後只剩兩條**歷史層豁免**。
  ±12 是「不再有誤判、又不必為遷就單一句子而放寬」的最小值 ⇒ 取 ±12，並把當時
  唯一一句真的缺計數式的話（W14 錨定樹 vs 終態樹的對照句）直接補上計數式，而不是撐大窗口。

  <!-- 重跑指令＝`git -c core.quotepath=false grep -nE '\bth=[0-9]' -- docs` 加窗口判定；
       輸出全文見第十七輪 worklog 的「本輪實跑」。
       🔴 2026-08-22 更正（來源＝Codex inline `3835660390`）：**上面兩份快照互相矛盾，兩份都逐字保留。**
       矛盾點：上方「命中總數 14／窗口 ±12」那份寫 `PASS 11 / EXEMPT 3`，本段的四窗口掃描卻給
       ±12 通過數 **10**，而且說「±16 之後只剩**兩條**歷史層豁免」——上方剛列了**三條**。
       🔴 **成因是它們不是同一次跑的**：四窗口掃描跑在「補計數式」**之前**，
       `PASS 11 / EXEMPT 3` 跑在**之後**。中間那次編輯正好讓一句從 FAIL 轉 PASS
       （±12 的 10 → 11），而 ±16 那一行的「兩條」是我當時把 W14 還原後的原表那條漏數。
       ⇒ **同一段文字裡放兩個不同時間點的輸出，讀者無法判斷哪個是真的**，這正是
       20.2 第 3 項（易腐聲明）要擋的形態。
       ⇒ 本輪**重跑一次**，只留一份輸出並附完整指令，見第十七輪 worklog 的「本輪實跑」。
       ⚠️ 上面兩份數字**不再作為任何結論的依據**；`±12` 這個選擇由重跑那一份重新支撐。 -->

  初版式子寫的是 `git grep -n 'th='`，實跑暴露兩個毛病：
  ① `git grep` 預設把 CJK 路徑輸出成 `"docs/...\350\207\252..."`，後續 `sed` 全部 `No such file`；
  ② `th=` 會命中 `innerWidth=2294`／`--depth=1`（`th` 前面是詞字元），實測 8 個檔中 **5 個是誤命中**。
  已改為 `git -c core.quotepath=false grep -nE '\bth=[0-9]'`，檔數 8→3。
  🔴 同時發現初版式子**少了層次界定**：歷史層原文（第三輪、W14 還原後的原表）依 19.5
  **本來就不得就地加錨定**，照初版式子它們永遠是紅的——兩條規則互相矛盾。已加入豁免條件。

### 鐵律 20.3 送驗前稽核

| 20.2 適用類型 | 固定處理 | 反向複驗**輸出** |
|---|---|---|
| ① 驗收攝取 | 判詞 body 全文落檔後**機械列舉**每一條 🔴／🟡／⚪，逐條打勾；review body 與 paginated inline 綁 exact head | 機械列舉實得 🔴 0／🟡 2／⚪ 2；Codex `4988979665` 兩則 inline 綁 `82e2fa8` |
| ② producer／consumer／終態 | `EF` B9／B10、W14、W15、`91` §3、PR 描述同批 | `th=` 三處全部改為 `<th\b` 實數，並各帶**計數式＋錨定 commit**；B9 歸屬句逐句化；B10 補條件逐字 |
| ③ 完成性聲明 | 只發布已實跑的輸出 | `bin/ci` 實測 **26 步：19 綠／7 紅**，7 紅全部是 Windows 環境缺口（見下方展開表），逐步展開後**全部 exit 0**；`doc-claims` 預設與 `--base 0fbe520 --require-base` 兩種形態皆 rc=0、🟡 0 條 |
| ④ 狀態空間 | `<th` 前綴 vs `<th\b`、`<thead>` 有無、歷史層改寫 vs 附加、**錨定樹 vs 最終樹** | 前綴／實數／`<thead>` 三組數同時印出；還原後 `git diff` 對原表的 `-` 行**只有還原**（非零，見 20.4 實跑）；錨定樹與最終樹的 W14 計數已分別記錄 |
| ⑤ fail-open | 計數若少印 `<thead>` 一欄，就無法判斷差值來源 | 本輪計數器固定同時輸出 `<th` 前綴數與 `<thead>` 數 |
| ⑥ workflow 本機假綠 | 本輪不改 workflow／scripts／config | `git status` 僅 docs 檔 |
| ⑦ Markdown／Windows | 變更 Markdown 走 GitHub `/markdown`；Windows 展開 11 步 | 見 ③ |

### 送驗前閘門實跑（本輪全部在終態樹上跑過才寫進來）

`bin/ci` 原生 26 步、19 綠 7 紅。🔴 **7 紅先與未改動樹對照過**：`git stash` 後在受驗樹
`82e2fa8` 上重跑，得到**完全相同的 7 步**失敗 ⇒ 與本輪 docs-only 改動無關。逐步展開：

| 原生紅的步驟 | 失敗原因（實測） | Windows 展開 | 結果 |
|---|---|---|---|
| Setup（`bin/setup --skip-server`） | `bin/rails db:prepare` 丟 `Errno::ENOEXEC`——Windows 無法直接 exec shebang 腳本 | `ruby bin/rails db:prepare` | exit 0 |
| Style: Ruby（`bin/rubocop`） | 同上，bin stub 不可直接執行 | `ruby bin/rubocop` | exit 0，`162 files inspected, no offenses detected` |
| Security: Gem audit（`bin/bundler-audit`） | 同上 | `ruby bin/bundler-audit` | exit 0，`No vulnerabilities found` |
| Security: Brakeman（`bin/brakeman`） | 同上 | `ruby bin/brakeman --quiet --no-pager --exit-on-warn --exit-on-error` | exit 0，`No warnings found` |
| Invariants: Prototype lint | `python3` 在本機不存在（本機是 `python`） | `PYTHONIOENCODING=utf-8 python scripts/lint-prototype.py` | exit 0，`ERROR 0 / WARN 136` |
| Invariants: Lint rules regression | 同上 | `… python scripts/test-lint-rules.py` | exit 0，回歸 23 項全過 |
| Invariants: Dead-control baseline | 同上 | `… python scripts/check-baseline-raise.py` | exit 0，`OK：基準線無調高` |

🔴 **`PYTHONIOENCODING=utf-8` 不是裝飾**：不加時三支 python 皆 `UnicodeEncodeError: 'cp950'
codec can't encode character '✅'`——**檢查本身是通過的，死在印結果**。只看退出碼會把
「通過但印不出來」讀成「檢查失敗」，與退出碼三分的立論同型（見 `limits_bad_yaml` 那條）。

`/markdown` 結構複驗（**終態樹**，計數式 `<tr\b`／`<th\b`／`<td\b`，同時印前綴與 `<thead>` 供對照）：

```
external-facts.md          table=0  tr=0  th=0  td=0    (th prefix=0,  thead=0)
91-pit-register.md         table=3  tr=25 th=11 td=72   (th prefix=14, thead=3)
W14（第十四輪）             table=3  tr=21 th=8  td=50   (th prefix=11, thead=3)
W15（第十五輪）             table=2  tr=13 th=6  td=33   (th prefix=8,  thead=2)
```

🔴 **本檔（W16）自己的計數刻意不列**：它是自指的——把數字寫進本檔的那次編輯就會改掉它
（實測：加上本節的展開表後，本檔由 `table=2` 變成 `table=3`）。要本檔的值，就在本 PR 合併後的
commit 上重跑同一支計數器；本檔不做任何會立刻過期的宣稱。

W14 由錨定樹 `82e2fa8` 的 `table=2,tr=13,th=5,td=29` 變成終態樹的 `3,21,8,50`
（兩組都用計數式 `<th\b`），差額正是本輪加的附加表——
**這就是「render 計數必須錨定 commit」的實證**，不是理論擔憂。

## 修改的檔案與核心邏輯 (Changes)

- `docs/dev/external-facts.md`：B9 歸屬句改為**逐句對應三個旗標**（原句依 19.5 留更正註）；
  B10 補 `--diff-merges` 預設條件的兩句官方逐字，並註明 `{diff-merges-default}` 屬性的展開依據
  與「渲染頁被截斷、改取 git 一手 asciidoc」的取證路徑。
- `docs/worklog/2026-08-21-PR64第十四輪雙驗收修復.md`：**還原**被第十五輪就地改寫的 20.3 表；
  更正內容改以「附加表」呈現；兩則更正註分別說明「更正什麼」與「連更正方式本身也錯了」。
- `docs/worklog/2026-08-21-PR64第十五輪Claude驗收修復.md`：⑦ 列 `th=` 改為實數，並補計數式與錨定 commit。
- `docs/specs/91-pit-register.md`：登記兩條 ⚪（B9 逐字截斷無省略號／「另明列」框架失去對象）。
- PR 描述：`th=` 同批改為實數，並補計數式與錨定 commit。

## 尚未完成或需注意的風險 (Pending / TODO)

- 🔴 **第三輪固定下來的計數式仍然只存在於文字裡**，沒有任何機械檢查會擋住下一次退回
  `grep -c '<th'`。本輪補的是「每處數字鄰接計數式」這個可 grep 的體例，**不是**一道閘門；
  真正的斷根要等有腳本檢查 render 計數宣稱時一併做（需新增 `scripts/`，命中 18.3，另包）。
- ⚠️ **第三輪 worklog 的兩處 `th=` 至今沒有錨定 commit**（反向式①實跑 L26 計數式=0／L29 錨定=0）。
  它是歷史層，依 19.5 不得就地補；且本輪沒有任何驗收意見點到它，依「只修點名的」不動。
  下一個**本來就要動該檔**的工作包，用附加更正註補上錨定即可。
- ⚠️ 兩條 ⚪ 只登記未修：B9 第一句的截斷仍無省略號、「另明列」框架仍指向上方三行。
  依 17.2 不順手改；下一個本來就要動 `external-facts` 的工作包一併處理。
- ⚠️ 本 PR 改 `docs/dev/external-facts.md` 等規範性文件但未動 `scripts/`／`workflows/`；
  合併仍依 D38 過渡期覆寫由**使用者人工執行**。
