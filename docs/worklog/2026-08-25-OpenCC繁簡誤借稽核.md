# 2026-08-25 OpenCC 字元表入庫＋繁簡誤借稽核轉實際執行（D49）

對應裁定：`docs/DECISIONS.md` D49｜採用登記：`docs/specs/107-external-adoption-register.md` OpenCC-1
配對 dev doc：`docs/dev/m2-translations-resolve.md` §7（已改記裁定結果）

---

## 已完成的工作 (Done)

1. **OpenCC 兩個字元表原樣入庫**（`lib/opencc/`）：`STCharacters.txt`（4012 資料行）、
   `TSCharacters.txt`（4148 資料行），連同上游 LICENSE 逐字副本與我方 NOTICE
   （版權人、來源 URL、抓取日期 2026-08-25、三檔 SHA-256——複驗＝`sha256sum lib/opencc/*`）。
   🔴 不含任何 OpenCC 原始碼；詞庫（TWPhrases）刻意不採（107 OpenCC-1 有明文）。
2. **`Translations::ScriptDetector`**：純函式判別核心。
   - 專用字推導：「鍵不在自己的映射值裡」＝該體專用（汉→漢 ⇒ 汉 簡專；台→…台… ⇒ 共用）。
     實測錨：汉门发＝簡專／漢門發＝繁專／的人大台后里干＝共用。
   - 🔴 五個雙邊矛盾字（緼苧藴輼醖）兩邊剔除（fail-safe：寧漏報不誤報）。
   - 🔴 零列 canary：字表讀出 0 專用字 ⇒ raise（路徑錯/檔案被清不得靜默變「零命中」）。
3. **`Translations::Audit` 的 `script_mismatch` 由明文棄權轉實際執行**：
   只對 zh-Hant*/zh-Hans* 跑（日文漢字也是 Han 字元，別的 locale 跑＝滿屏誤報）；
   html 欄取 parser 文字再判（實體解掉、屬性值不進來）；**僅登記不自動修**
   （簡→繁一對多，自動改字是 script_conversion／ML-5 的事）。
   `ABSTAINED` 機制保留為空清單（報告端與 rake 顯示不拆）。
4. **`docs/specs/107-external-adoption-register.md` 建檔**（早於原定 R-8 時點，因為採用
   先發生了）：OpenCC-1 採用列＋鐵律 9 紅線的具名拒絕表（Vendure/Spree≥4.10/TinyMCE
   develop/Medusa 未取證）。

## 修改的檔案與核心邏輯 (Changes)

- 新增：`lib/opencc/{STCharacters.txt,TSCharacters.txt,LICENSE,NOTICE}`、
  `app/services/translations/script_detector.rb`、
  `spec/services/translations/script_detector_spec.rb`、
  `docs/specs/107-external-adoption-register.md`、本檔
- 修改：`app/services/translations/audit.rb`（規則表加列、棄權敘事改沿革註、
  `inspect_row` 接 ScriptDetector、`ABSTAINED = []`）、
  `spec/services/translations/audit_spec.rb`（棄權斷言改為六格實際執行斷言，
  含「非 zh 不跑」「html 走 parser 文字」「fix 不動它」「規則可組合」）、
  `docs/DECISIONS.md`（D49＋D50）、`docs/dev/m2-translations-resolve.md` §7 與 P7-L11、
  `docs/specs/91-pit-register.md` §3.17 OpenCC 條目結案註

## 尚未完成或需注意的風險 (Pending / TODO)

1. **詞彙級誤借（软件↔軟體）不在射程**——字元級判別抓不到「兩邊都是合法字、用詞不同」；
   那是 ML-5（script_conversion／在地化）的事，107 OpenCC-1 已明文「要用詞庫＝新開一列」。
2. **上游表更新**＝手動流程：重抓兩檔＋更新 NOTICE 的 SHA-256＋重跑 spec 的固定點錨
   （汉门发/漢門發/共用字）。表隨版本部署，無執行期變動面。
3. **D50（第 11 包案 A）在本 PR 只落裁定文**，實作在包 11 的 PR。
