# 2026-09-03 引擎缺口 PR-6：filters（sort_by／within／link_to_*tag／default_pagination／color_to_hsl／md5）

分支 `engine/filters-6`（自 PR-5 分支長出，PR-5 合併後 rebase 到 main）。配對 handoff：
`docs/handoff/2026-09-03-引擎缺口-filters.md`。收口 D78 triage 已驗證 `link_to_add_tag／link_to_remove_tag／
sort_by`（`url_for_type` 已於 PR-4 收口）與未驗證 `default_pagination／color_to_hsl／md5`；
`within` 原為 no-op stub（三套主題用量最高的一支）。

## 已完成的工作 (Done)
- `sort_by`（官方例 `/collections/sale-potions?sort_by=best-selling`；既有 query 用 `&`）。
- `within`（官方例 `/collections/sale-potions/products/draught-of-immortality`；非系列物件原樣；
  locale 前綴沿用系列 URL）。
- `link_to_tag`／`link_to_add_tag`／`link_to_remove_tag`：href 以 `registers[:request_path]`＋`current_tags`
  組成（系列 `/collections/{handle}/{tags}`、部落格 `/tagged/{tags}`，多 tag `+` 連接）；title 依官方例
  "Show products matching tag X"／"Narrow selection to products matching tag X"；已啟用的 tag 在 add
  形只出純文字（官方句）。
- `default_pagination`（官方例逐字：`<span class="page current">…</span> <span class="page"><a …
  title="">N</a></span> <span class="next"><a … title="">Next &raquo;</a></span>`；next／previous 參數）。
- `color_to_hsl`／`color_to_rgb` 真實作（官方例 '#EA5AB9' ⇒ 'hsl(320, 77%, 64%)'／'rgb(234, 90, 185)'；
  alpha ⇒ hsla／rgba；收 #RGB／#RRGGBB／#RRGGBBAA／rgb(a)()）。
- `md5`（官方例 `'' | md5` ⇒ d41d8cd98f00b204e9800998ecf8427e）。
- spec `spec/liquid/filters_gap_spec.rb` T1–T8；突變輪：M1 sort_by no-op ⇒ T1 紅；M2 within no-op ⇒ T2 紅；
  M3 add_tag 無視 current_tags ⇒ T4 紅；M4 next 文案漂移 ⇒ T6 紅；M5 hue 恆 0 ⇒ T7 紅；M6 md5 原樣 ⇒ T8 紅。
- 回歸：`bundle exec rspec spec/liquid/filters_gap_spec.rb spec/requests/storefront_blog_spec.rb
  spec/requests/storefront_collections_spec.rb spec/liquid/theme_conformance_spec.rb` 綠。

## 修改的檔案與核心邏輯 (Changes)
- `app/liquid/theme_engine/filters.rb`：上述 filters＋私有 helper `tag_link／current_tags／tag_base_path／
  parse_css_color／rgb_to_hsl`（`private def` 逐支，不影響其後的 filter 可見性）。
- `spec/liquid/filters_gap_spec.rb`：新檔。

## 尚未完成或需注意的風險 (Pending / TODO)
- `link_to_remove_tag` 的 title 文字官方例未示 ⇒ 用 "Remove tag X"（登記）；`default_pagination` 的
  previous 側與省略號 markup 官方例未示 ⇒ 對稱形 `<span class="prev">`／`<span class="deco">`（登記）。
- 系列頁的 tag 路徑（`/collections/{handle}/{tags}`）**尚未路由**（PageRenderer 只有部落格 `/tagged/`），
  連結能生成、點進去仍是 404——系列 tag 過濾路由另包。
- `color_lighten／color_darken／color_mix／color_modify（非 alpha）` 仍是 stub（不在本包射程）。
- `format_code`（三套主題 gift_card 模板用）官方 filters 頁 404 ⇒ 未文檔化、gift card 頁未路由，未動。
