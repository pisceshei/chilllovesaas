# 2026-08-15 — 驗收機器人的模型與推理強度

> 使用者裁定：「將 github 上面的 claude bot 模型改成指定使用 opus 5 推理能力拉到最高，
> codex 的模型指定使用 sol 5 推理能力也拉到最高。用更高的能力去驗收得到更好的效果」

---

## 已完成的工作 (Done)

### 1. Claude 驗收機器人 → Fable 5 + 最高推理強度（已落地）

`claude-review.yml` 的 `claude_args` 加兩行：`--model claude-fable-5`、`--effort max`。

🔴 **模型由 Opus 5 改成 Fable 5（使用者二次裁定，2026-08-15）**。
初始指令是「opus 5」，但查證時查到官方明文：
「Claude Fable 5 (`claude-fable-5`) is Anthropic's **most capable widely released model**」。
使用者的目標句是「用更高的能力去驗收得到更好的效果」⇒ 把這個落差回報後，裁定改用 Fable 5。
🔴 **這是「照字面執行」與「照目標執行」衝突時的處理範例**：不自作主張換模型，
但也不假裝沒看到——把事實攤出來讓使用者裁定。

🔴 **關鍵事實（親自查證，不是推測）**：`anthropics/claude-code-action@v1` 的 `action.yml`
**沒有 `model` 這個 input**。抓 `raw.githubusercontent.com/anthropics/claude-code-action/v1/action.yml`
逐一列出全部 **39 個 input**：

```
trigger_phrase, assignee_trigger, label_trigger, base_branch, branch_prefix,
branch_name_template, allowed_bots, allowed_non_write_users, include_comments_by_actor,
exclude_comments_by_actor, prompt, settings, anthropic_api_key, claude_code_oauth_token,
anthropic_federation_rule_id, anthropic_organization_id, anthropic_service_account_id,
anthropic_workspace_id, anthropic_oidc_audience, github_token, use_bedrock, use_vertex,
use_foundry, claude_args, additional_permissions, use_sticky_comment,
classify_inline_comments, use_commit_signing, ssh_signing_key, bot_id, bot_name,
track_progress, include_fix_links, path_to_claude_code_executable,
path_to_bun_executable, display_report, show_full_output, plugins, plugin_marketplaces
```

⇒ 網路上常見的 `with: model: claude-opus-4` 寫法是 **v0 的**。在 v1 上寫它，
GitHub Actions 會把未知 input **靜默忽略**（不報錯、不警告），workflow 照跑、
用的卻是預設模型——**這是最難發現的失效形態**：一切看起來都成功了。
唯一正確的位置是 `claude_args`（`action.yml` 的描述原文：
"Additional arguments to pass directly to Claude CLI"）。

旗標值的來源＝**本機 CLI 的 `--help` 實測**，不是猜的：
- `--model <model>`：接 alias（`fable`/`opus`/`sonnet`/`best`）或全名（`claude-fable-5`）。
- `--effort <level>`：`low, medium, high, xhigh, max` 五級，`max` 為最高。
  官方：「The default effort is `high` on every model that supports effort」
  ⇒ 這一行**有實際作用**，不是裝飾。
  `max` **不能**寫進 settings 的 `effortLevel`（官方：session-only）⇒ 只能走 `--effort`。

🔴 **本輪內修正一次：alias → 完整名**（`--model opus` → `claude-opus-5` → `claude-fable-5`）。
第一版我寫 alias，理由是「升版自動跟上」。**這個理由是錯的，因為它假設 alias 一定解析到最新**：
官方明文「Opus 5 requires Claude Code v2.1.219 or later」，且
「Before v2.1.219, `opus` resolved to Opus 4.8」——而 action 內建 CLI 的版號
**官方未公布 SDK↔CLI 對應** ⇒ 我們無法事先知道拿到哪一個。
若它舊於該版，alias 會**靜默**解析成上一代模型：不報錯、log 看不出來、以為換好了。
釘完整名把「靜默降級」換成「會叫的失敗」。
代價是升版不會自動跟上，這是**刻意付的**——與本專案一貫的取捨方向一致
（寧可失效時大聲壞掉，也不要安靜地做錯事）。

⚠️ **同理，也沒有用官方 alias `best`**（「有 Fable 5 權限就用 Fable 5，否則用最新 Opus」）。
它會自動挑最強且不會因無權限而失敗，聽起來更好——但它**仍然是 alias**，
一樣有「不知道實際拿到哪一個」的問題。取捨方向相同：要能看見自己在跑什麼。
⇒ **代價是若帳號沒有 Fable 5 權限，第一次跑就會報錯**。這是已知且刻意接受的，
   處置見 Pending #5，**不要看到報錯就直接把模型改回去**。

### 三條明確不要做的（已寫進 workflow 註釋）

| 做法 | 為什麼不行 |
|---|---|
| `with: model: claude-opus-5` | v1 沒這個 input，GitHub **靜默忽略**未知 input |
| step `env:` 加 `CLAUDE_CODE_EFFORT_LEVEL`／`ANTHROPIC_MODEL` | action 的 env 白名單只轉發 `ANTHROPIC_DEFAULT_{SONNET,HAIKU,OPUS}_MODEL` |
| 同時用 `settings:` 設 model／effortLevel | 官方：`claude_args` takes precedence over settings ⇒ 永遠不生效 |

### 考慮過但**刻意不加**：prompt 裡的 `ultrathink`

官方確實有這個關鍵字（「Include `ultrathink` anywhere in your prompt to request deeper
reasoning on that turn **without changing your session effort setting**」）。
不加的理由：我們已經 `--effort max`，**那是上限，再深不上去** ⇒ 加了是裝飾。
唯一會有用的情境是「組織層 effort limits 把 max 靜默 clamp 掉」，
但那是 Claude Enterprise 方案專屬，本專案用個人訂閱，**不適用**。
🔴 登記於此是為了讓下一個人不用重新查一遍就知道「這個已經評估過、結論是不加」。

### 2. Codex 驗收機器人 → 只有一個深度旋鈕，已打開

在 `chatgpt.com/codex/cloud/settings/code-review` 把
**「全面程式碼審查」**（原文：「要求 Codex 持續尋找更多發現，直到不再找到新問題為止」）
由 **關 → 開**，並**強制重載頁面確認伺服器端已存**（`aria-checked="true"` 持續）。

語義上這就是 loop-until-dry：**它是 Codex 這邊唯一等價於「推理能力拉到最高」的設定**。

`pisceshei/chilllovesaas` 在該頁「儲存庫喜好設定」列表中，該列的「全面」欄位是
**「依個人設定」** ⇒ 個人開關一開，本倉庫立即生效，不需要再逐倉庫設定。

---

## 修改的檔案與核心邏輯 (Changes)

| 檔案 | 改動 |
|---|---|
| `.github/workflows/claude-review.yml` | `claude_args` 增 `--model claude-fable-5` ＋ `--effort max`；上方加 26 行註釋，寫明「為什麼不能寫成 `with: model:`」、「為什麼釘完整名不用 alias」與三條明確不要做的 |

**倉庫外的改動（無法進 git，登記於此）**：
ChatGPT 個人設定 → Codex → 程式碼審查 → 「全面程式碼審查」＝開。
🔴 **這一條沒有任何 git 紀錄能證明它做過**——下一個人只能靠這份 worklog 知道它被改過。
要查證就去那一頁看 `切換全面程式碼審查` 的 `aria-checked`。

---

## 尚未完成或需注意的風險 (Pending / TODO)

1. 🔴 **模型名已查實：官方識別字是 `gpt-5.6-sol`**（使用者口語稱「5.6 sol」／「sol 5」）。
   GPT-5.6 家族三層由弱到強＝ **Luna ＜ Terra ＜ Sol**；alias `gpt-5.6` 會 route 到 Sol。
   「5.6」是世代、「Sol」是層級，**順序與口語相反** ⇒ config 裡照抄「sol 5」一定失敗。
   顯示名「GPT-5.6 Sol」（大寫＋空格）**只能寫在敘述句裡，不得當設定值**。

   ℹ️ **使用者本機的 `C:\Users\pisce\.codex\config.toml` 已經是
   `model = "gpt-5.6-sol"` ＋ `model_reasoning_effort = "xhigh"`**——
   但那只影響**本機 CLI 與 IDE extension**，與 GitHub 上那個 bot 無關（見 #3）。

   但**這不改變結論**：`chatgpt-codex-connector` App 自動貼的那個 review
   **沒有任何 model／effort 設定**。兩路獨立佐證：
   - **UI 全面掃描（我親自點過）**：`settings/general`、`settings/environments`、
     環境詳情頁、`settings/code-review`、`codex/cloud` composer ＋「＋」選單
     ——**六個面全文比對模型關鍵字零命中**。環境詳情頁只有映像檔／網路存取／
     快取／設定指令碼／環境變數／密碼，沒有模型。
   - **官方 GitHub 整合文檔全文 grep**：`reasoning`／`effort` 出現 **0 次**，
     `model` 只出現 1 次而且是 "threat models"。
   ⇒ repo 端能改的是「審什麼」，不是「用多強的腦」。

   🔴 **措辭紀律（查核 workflow 特別反駁掉的一條）**：正確說法是
   **「沒有欄位可填」**，不是「官方規定 PR 審查不能換模型」。
   官方那句 "you can't change the default model" 的主詞是 **cloud chats**；
   對 **code review** 官方是**沉默**，不是明文禁止。
   （旁證：GitHub 上把任務指派給 Codex agent 時**是**可以選模型的——
   那是 task，不是 review，兩者不要混為一談。）
   ⚠️ 也不要說「Sol 以外的層級沒上架 cloud」——實抓官方表，Luna／Terra／Sol
   在 Codex cloud **三個都打勾**；cloud 選不了模型的原因是**不開放選**，與上架無關。

2. ✅ **已裁定不做**（使用者 2026-08-15）：官方 `openai/codex-action@v1`
   **確實有 `model` 與 `effort` 兩個 input**（官方描述：「Pick the Codex agent
   configuration you want; leave empty for defaults」）。那是**自架 workflow**，
   與 connector App 的自動審查是**兩條並行的路**，不是同一個東西。
   要真的在 GitHub 上指定 `gpt-5.6-sol` ＋ 最高 effort，這是唯一官方途徑。
   **裁定＝先不做，維持現狀**——理由是它要新增 workflow、可能要 OpenAI API key secret、
   且會與現有 connector 審查**重複貼兩份意見**。
   🔴 登記在此是為了讓下一個人知道**這條路存在且已評估過**，不是沒想到。

   🔴 **要走這條路的話，有一個必須先解掉的風險**：`effort` 的**合法值域官方沒有列舉**
   （是 `xhigh`？`max`？還是 CLI 那五級 `minimal|low|medium|high|xhigh`？）。
   而 GitHub Actions 對**未知 input 是靜默忽略**——本倉庫在
   `anthropics/claude-code-action@v1` 上**已經踩過同型事故**（`with: model:` 被吃掉、
   workflow 照跑、用預設模型、一切看起來都成功）。
   ⇒ **不得憑文檔推斷填值，必須實跑一次 PR 驗證。**

   ℹ️ 計費也要先算清楚：hosted connector 審查走 **Codex 方案 credits**
   （Sol 約 125 credits/1M in、750/1M out），自架 action 走 **OpenAI API 計價**
   （$5 in／$30 out per 1M，且 >272K input 的請求全額 2x in／1.5x out）。
   **兩者不是同一個錢包**，不要拿 API 價目去估現在的審查成本。

   同批裁定**不加** AGENTS.md 的 `## Code Review Rules` 區塊
   （官方支援、可控制 Codex「審什麼」、成本為零），一併維持現狀。

3. ⚠️ **`config.toml` 的 `model`／`model_reasoning_effort`／`review_model` 對 GitHub bot 無效**。
   官方明文只綁 CLI 與 IDE extension（「The CLI and IDE extension share the same
   configuration layers」），對 hosted connector 審查**完全沉默**。
   ⇒ 使用者本機那份（已是 `gpt-5.6-sol` ＋ `xhigh`）**只影響他自己的終端機**。
   本 repo 目前沒有 `.codex/` 目錄，**放一個進去屬於賭，不是設定**——
   `.codex/config.toml` 確實是官方支援的**倉庫層**設定層，但官方對
   「雲端審查讀不讀它」沉默，且改了**不會報錯**，是最容易自我欺騙的形態。
   ⚠️ 反過來也不要說成「config.toml 對雲端一律無效」：
   `openai/codex-action` **明文支援**在 `codex-home` 裡放 `config.toml`
   ⇒ 正確說法是「**hosted connector 審查沒有 config 注入面**」。
   🔴 另注意值域不同名也不同集合：config.toml 側是
   `minimal|low|medium|high|xhigh`（**沒有 `max`**），API request body 側才有 `max`。
   互抄會產生無效值。

4. ⚠️ **在 AGENTS.md 寫「請用最高推理強度」不是設定**，是賭它照做。
   官方沒有任何一頁說 AGENTS.md 能控制 model 或 effort。
   🔴 這一條要與 #2 分清楚：AGENTS.md 的 `## Code Review Rules` 區塊是**官方支援的機制**，
   但它控制的是「審什麼」；「用多強的腦」在 AGENTS.md 裡**沒有任何官方鉤子**。
   ⇒ 不要因為 #2 裁定不加規則區塊，就以為「那寫句話請它認真審」是替代方案——那是無效的。

5. 🔴 **`--effort max` 與 `--model claude-fable-5` 尚未在 CI 上實跑驗證過**。旗標由
   action 依賴的 `@anthropic-ai/claude-agent-sdk`（`^0.3.232`，caret ⇒ 安裝時取最新 0.3.x）
   內建的 CLI 解析，**與本機 CLI 未必同版**。若不認得，步驟會失敗——
   但 `continue-on-error: true` ⇒ 不擋 CI，會走「步驟失敗」分支貼診斷留言。
   🔴 **第一次跑到時要看那則留言的 stderr，不要直接把旗標拿掉。**
   這正是釘完整名要換來的效果：**失敗會叫**，而不是安靜地跑一個舊模型。
   🔴 **另有一個獨立的失敗來源：帳號權限**。若報的是「模型不存在／無權限」，
   那是訂閱沒有 Fable 5，不是 workflow 寫錯 ⇒ 確認權限後再決定是否回退 `claude-opus-5`。
   **兩種失敗的 stderr 不一樣，不要混為一談就把旗標刪掉。**

6. ⚠️ **本改動動到 `claude-review.yml` ⇒ PR #30 自己不會被驗收**（反竄改機制，
   見本檔頂部註釋）。真正的驗證機會是**合併進 main 之後開的第一個 PR**——
   也就是 PR #29 重新 push 的那一次。

7. ⚠️ **「啟用積分」開關刻意沒動**（維持關閉）。它是計費設定：
   「允許在超過速率限制後繼續使用積分進行程式碼審查」。
   開了「全面程式碼審查」之後單次審查更耗額度，**比較容易撞到速率上限**；
   撞到時的行為是審查停止，而不是自動扣積分。要不要開由使用者決定，我不代為變更計費。

8. ⚠️ **成本／時長會上升**。`--effort max` ＋ Fable 5 ＋ `--max-turns 200`，
   單次驗收的 wall-clock 與 token 都會比現在的 3–4 分鐘高。
   若之後出現 `timeout-minutes: 30` 被打滿的情況，先調 `--max-turns` 而不是降 effort。
