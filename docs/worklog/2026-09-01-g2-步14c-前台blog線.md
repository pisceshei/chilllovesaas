# 2026-09-01 G2 步 14c：前台 blog 線

## 已完成的工作 (Done)

- /blogs 路由三型（98 §2）：/blogs/:handle（blog 模板）＋/tagged/:tags（`+` 多
  tag AND＋current_tags 注入）＋/blogs/:blog/:article（visible 閘）。
- blog 家族 drops（98 §1 官方屬性對位）：BlogDrop（articles_count 不含 hidden
  ——官方句）／BlogArticlesDrop（新到舊＋tagged＋paginate）／ArticleDrop
  （🔴 handle/url＝複合形——官方範例值）／CommentsDrop（50/頁官方上限）／
  CommentDrop（status 恆 published）／blogs 全域。
- 留言 POST（98 §2 真店抓包形）：政策三分流（closed 404／moderated pending／
  auto published）＋302 回 #comment_form 錨。
- FormTag new_comment 吃位置參數 article → action=comment_post_url。
- linklist 型別擴充：官方 `*_link` 型值＋frontpage/search/catalog/collections/
  blog/article URL（article＝複合路徑）。
- 搜尋 article 型接線（12b 留空收口）：整頁混型第三段＋suggest articles 真查。
- 突變 MB1-7 全紅＋canary；全套 1873 例綠。

## 修改的檔案與核心邏輯 (Changes)

- drops.rb（六個新 drop＋LinkDrop 擴充＋SearchResultsDrop article 段）、
  tags.rb（FormTag 位置參數）、page_renderer.rb（三路由）、
  search_query.rb（articles）、search_controller.rb（article 分支 ×3）、
  comments_controller.rb（新）、routes.rb（留言 POST ×2 形）、
  fixture 四檔、spec（7 例）。

## 尚未完成或需注意的風險 (Pending / TODO)

- 91 §3.65：留言表單成功/失敗態 form drop 狀態機（本尊回應三層被 CAPTCHA 紅線
  擋住未取證——我方 302 錨形 ours）；CAPTCHA 不做（throttle 承擔）；
  blog.next_article/previous_article 未實作；sitemap blogs 分片未加。
- 生產煙測待部署（demo /blogs/news 從 404 轉 200＋建文章走 admin API）。
