# 2026-09-02 Ella 修復 PR-23：編輯器預覽內導航不逃逸（cl:navigate）

## 已完成的工作 (Done)

- 主題頁/編輯器軸的 high 級殘項：編輯器 iframe 內點任何主題連結會導去
  **裸 storefront 頁**（無 design_mode、無橋——「一點就逃出編輯器」，
  6 軸艦隊 axis 4 ②）。本尊行為＝預覽內導航停留編輯器語境並同步左欄。
- **橋**（design_mode 才注入）：攔 `a[href]` 站內連結（`/` 開頭、非
  /admin/*）⇒ preventDefault＋`cl:navigate` postMessage；外部連結與
  admin 連結不攔。
- **前端**：cl:navigate（同源驗證同 ED3）⇒ iframe.src 換
  `/admin/store/preview/{id}{path}?editor=1`＋`templateForPath` 推斷
  模板型同步左欄（?template= param——選中清空）；路徑推斷剝 locale
  前綴（防禦性；admin 預覽渲染本就無前綴）。
- 測試 BR2（橋內容＋公開店面不帶）＋ED24（換 src/模板同步/異 origin
  忽略）；突變 2/2 殺（掏空攔截、拔模板同步）。
- 已知限制（登記）：form submit（cart POST 等）不攔——預覽內交表單仍
  逃逸；onMessage effect 掛載一次 ⇒ templateKey 閉包舊值只影響
  「同值重設 param」的無害分支。

## 修改的檔案與核心邏輯 (Changes)

- `app/liquid/theme_engine/runtime.rb`：EDITOR_BRIDGE_JS 連結攔截。
- `app/frontend/admin/pages/ThemeEditorPage.tsx`：templateForPath（export
  供測）＋cl:navigate 分支。
- specs：theme_editor_bootstrap（BR2）／ThemeEditorPage.test.tsx（ED24）。

## 尚未完成或需注意的風險 (Pending / TODO)

- form submit 攔截（含 cart/search 表單）另輪；分享預覽連結（訪客 2 天/
  店主 30 天 signed URL）與主題頁大卡縮圖照舊登記（axis 4 ② medium）。
