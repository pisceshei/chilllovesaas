# 22 — Admin 按鈕級功能對照與實作註釋

> 來源：help.shopify.com 官方手冊 80+ 頁逐頁挖掘（2026-08）+ 21 號實測 teardown。**這是正式開發的快速對照表**：每個控制項標註「功能 → 邏輯規則 → 實作註釋」。實作註釋代號：`M`=資料模型（06 號文件）、`S`=生產級規格（11–19 號）、`API`=API 層（09 號）、`P0/P1/P2`=優先級（07 號）。v2 原型已內建「**開發註釋模式**」——開啟後點任何虛線框元素即顯示本文件的對應註釋。

## 0. 通用 Shell 與橫切機制

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| 全域搜尋（CTRL K） | 跨資源搜尋+快速跳轉；支援結構化語法（`title:xxx`、`tag:值`、萬用字元、AND/OR/NOT） | P1。搜尋服務：各資源 FULLTEXT（S14-F4 ngram）+ 前綴語法解析器；先做 訂單號/商品/顧客 三類 |
| 檢視系統（saved views） | 每列表：預設檢視+自訂檢視（儲存篩選+排序+**欄位設定**），可改名/複製/刪除/釘選 | P0。M：`saved_views(staff_id, resource, filters_json, sort, columns_json)`；預設檢視唯讀 |
| 篩選器引擎 | 條件間 AND、同條件多值 OR；訂單可按狀態/金額/管道/標籤/風險/卡末四碼/metafield 等 20+ 維度 | P0 核心維度、P1 長尾。查詢建構器把 filters_json 轉 SQL（白名單欄位，防注入 S11§1） |
| 匯出對話框 | 範圍四選一（本頁/全部/勾選/目前篩選）+ 格式；**>50 筆改寄 email 下載連結**（10 萬筆約 1 小時） | P1。S13-F6：streaming CSV + job + 簽名下載連結；小量同步、大量轉背景寄信 |
| 匯入 CSV | 15MB 上限；欄名區分大小寫；「覆寫相同 handle/email」勾選；**欄留空=清空、整欄省略=保留** | P1。S13-F6 逐行 job + 錯誤報告；「空值 vs 缺欄」語意要寫進測試 |
| 批次編輯器（bulk editor） | 類試算表：欄位增減、方向鍵、拖曳填滿、需按 Save | P1。前端表格（TanStack）+ 一支 bulk PATCH（逐列驗證回錯 S13-F1） |
| 時間軸留言 | @提及員工（寄通知）、# 引用資源、附件、emoji；發布後 5 分鐘可編輯、僅本人 | P0 基本留言；提及/引用 P1。M：events + mentions 表；S16-F2 append-only |
| 動作可見性 | 未授權的模組整個選單不可見（非灰化） | S12-F3：Pundit + 側欄按權限渲染 |

## 1. 訂單

### 1a. 列表

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| 批次動作 | 出貨類（標記出貨/處理中/未出貨/**on hold（選原因）**/變更地點）、capture（僅全額）、列印裝箱單、標籤、封存 | P0：批次出貨/封存/標籤。批次逐筆獨立 transaction+結果報告（S16-F1）；批次出貨不能填追蹤碼（事後逐筆補）——照抄此取捨 |
| 匯出交易歷史 | 獨立按鈕；只含已 capture 交易 | P2。transactions 表直出（M：order_transactions） |
| 風險警示 | 中高風險單號旁 icon | P0 欄位預留；模型 P2（S15-F4 假指標起步） |

### 1b. 訂單詳情

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| Refund | 逐行數量（**數量 0 的品項不退款**）、restock 勾選（追蹤庫存才顯示）、**可直接對商品項目套用折扣**、退運費（上限=可退運費；**訂單層級免運折扣 → 完全不可退運費**）、可改總額、多付款方式分配（**混合付款時先扣禮品卡至可退全額，餘額才走其他方式**）、通知勾選（預設勾）；**軟上限=netPayment，超額退款需 `orders.over_refund` 權限＋二次確認（不是 DB 硬擋）**；**退款一經發起絕對不可撤銷**（強制二次確認）；退款後不能再建退貨 | P0。金額**只走 S16-F5.1 的唯一公式**（含退貨費用扣減、換貨扣抵、floor 0）：先落 pending 再打金流、restock 以 refund_line_item 冪等 |

<!-- 依 46a:595–601 修正：退款公式必須含「− 退貨費用 − 換貨扣抵」與 floor 0（原本只有「行單價×數量 − 折扣分攤 − 稅」）。
     依 46c:223、46c:241 修正：🔴 此處原本寫錯——原寫「**不可超過實收**…上限=Σcaptured−Σrefunded」做成硬擋，
     但 help 明載「先前已發過商店抵用金者可對原付款方式**超額退款**」→ 硬擋會擋掉合法情境，改為軟上限＋權限＋二次確認。
     依 46c:218–221、46c:228 補寫：數量 0 不退、退款頁可加折扣、禮品卡優先扣抵順序、退款不可撤銷。任何人翻舊版都不要改回硬擋。 -->
| Edit（編輯訂單） | 加品項/自訂品項/調量/移除未出貨項/品項折扣 → 差額：多收→寄發票或收款、少收→退款；**不能改訂單層折扣**；已出貨項不可移除；分期付款單不可編輯；稅重算、運費不重算 | P1。金額引擎重算（S15-F2）+ 差額發票流；限制清單直接做成 guard 函式 |
| Duplicate | 以現單開新草稿：帶品項（取最新商品資料）、顧客、地址、備註、標籤；**折扣與運費不複製** | P1。`Orders::Duplicate` → draft_orders |
| Cancel | 對話框：退款方式（原路/店內額度/稍後）、**原因必選（6 值 enum）**、**restock non-null 必須表態**（UI 預設勾）、通知勾選（UI 顯式帶 true）、**staffNote ≤255（買家不可見）**；**五條不可取消 guard**：已取消／有待處理付款授權／**有進行中的退貨**／有無法履行的未結出貨／已（部分）出貨；未 capture→Voided、已收→Refunded；**非同步 job ＋ 進度輪詢** | P0。S16-F4.1/F4.2：五條聯集 guard + 狀態條件轉移 + 庫存釋放 + void/refund 分流 + 關閉所有未結 FO |

<!-- 依 46a:832–838、46a:842–877 修正：原本只有 help 側的「部分出貨後不可取消」一條，dev 的四條全缺
     （尤其「**有進行中的退貨不可取消**」——不做會雙重退款＋庫存重複回補）；且 `restock` 為 non-null、`staffNote` ≤255、mutation 為非同步。
     依 46a:853 補寫：停用地點 ＋ 已付款 ＋ restock:true → **整個 mutation 失敗**；未付款則成功但庫存不回補。 -->
| Archive/自動封存 | 手動或設定觸發（已付+已出貨或全退） | P0 手動；自動＝nightly job（S16-F4） |
| 裝箱單 | Liquid 模板可自訂、可還原預設；多段出貨=多張 | P1。notification_templates 同機制（S18-F2） |
| Fulfill 卡 | 數量（部分出貨）、追蹤碼（自動辨識物流商+可多筆）、通知勾選；**on hold 須先解除**；最多 10 個 active hold（**8 種 reason enum**）；拆分/合併/移動出貨；Mark as delivered；取消出貨（先 void 標籤）；**取貨點訂單改顯示門市卡、不顯示收件地址** | P0：數量+追蹤+通知。M：fulfillment_orders（**7 status × 8 requestStatus × 12 supportedActions**，見 S16-F3.1）+ fulfillment_holds；**所有按鈕啟用一律讀伺服器回傳的 `supportedActions`，前端不得另寫 guard**；carrier 自動辨識=正則表 P1 |

<!-- 依 46a:213–290、46a §2⑦-8 修正：原本只寫「最多 10 個手動 hold」，缺整條狀態機與 8 種 hold reason；
     且原本的按鈕 guard 是**前端另寫一套**（必然與伺服器漂移）——官方要求 `supportedActions` 做成伺服器端計算欄位、admin 按鈕啟用完全由它驅動。
     依 46a:236、46a:358–366 補寫：`fulfillmentOrderCancel` 產生**替代單**、部分 hold/move 產生**剩餘單**（S16-F3.2），不做會讓剩餘品項憑空消失。 -->
| 買運送標籤 | 包裹尺寸/總重/寄件日/服務選擇/跨國報關欄（HS+申報價值+產地）/保險 → 費用記平台帳單、自動標出貨+回填追蹤 | P2（接物流商 API）。介面先留 `Labels::Quote/Purchase` adapter |
| Capture | Authorized 區顯示；可部分 capture（可多次，依供應商）；**授權期 7 天**（到期前 2 天示警）；批次只能全額 | P1（Stripe manual capture）。S15-F4：授權到期提醒＝nightly job + banner |
| Collect payment | 寄請款信 / 手輸卡號 / Mark as paid（外部已收款，不再請款） | P0：Mark as paid + 請款信。draft/pending 訂單共用 |
| 顧客區編輯 | 換/移除顧客（僅解除連結）；改聯絡（可勾同步回顧客檔）；改收件地址→**重算稅並產生差額卡**；帳單地址不可改 | P1。地址改→金額引擎重算→差額流程（同 Edit） |

### 1c. 草稿訂單

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| 自訂品項 | 名稱/單價/數量/課稅/是否實體；不入目錄不影響庫存報表 | P1。M：line_items.custom=true |
| Reserve inventory | 設到期日，庫存轉 Unavailable | P1。inventory ledger reason=reservation（S13-F5） |
| Lock product pricing | 鎖價：商品改價不影響此單 | P1。快照原則本來就有（M line_items snapshot）——此鈕=重新快照開關 |
| 折扣/運費 | 碼或自訂（訂單層/品項層+原因）；運費選店費率（需地址）或自訂 | P0。重用折扣引擎+RateResolver |
| Payment due later | 帳期（收貨即付/出貨後/Net 7–90/固定日）；Plus 可要求訂金 | P2。M：draft_orders.payment_terms |
| Send invoice | 自訂訊息、允許輸碼開關、忽略結帳驗證勾選 → 顧客付款轉正式單 | P0。簽名結帳連結（S15-F3 checkout 複用） |
| 業務規則橫幅 | 未滿足 checkout 規則列出，可 Ignore 硬建 | P2。validation pipeline + override 權限 |

### 1d. 棄單 / 退貨

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| 棄單列表 | 只列：**留 email 後 10 分鐘未完成**、非 bot、線上通路；90 天自動刪；寄挽回信（手動/自動：**對象二選一〔任何未完成者／email 訂閱者〕＋延遲四檔 `1/6/10/24 小時`，預設 10 小時（官方標「建議」）**＋模板）；可轉草稿保留 | P0 列表+手動信；自動化 P1（S15-F7 全套）。三處門檻統一至 `limits.abandoned_checkout.*` |

<!-- 依 44:373、24:228 修正：棄單判定門檻＝「留 email 後 10 分鐘未完成」；挽回信延遲四檔 1/6/10/24 小時、預設 10 小時。
     🔴 我方原本三處門檻不一致：22:60「≥10 分鐘」（對）／15:94「active 超過 1 小時」（錯，且四檔延遲未寫）／24:228（對）。
     已統一至 config/limits.yml 的 `abandoned_checkout.qualify_after_minutes: 10` 與 `recovery_delay_hour_options`。任何人翻舊版都不要改回 1 小時。 -->
| Return 建立 | 選品項（**來源＝已送達的 fulfillment line item**）+原因（依品類動態）→ 退貨運送三選一（平台標籤〔**僅限美國地點**〕/上傳標籤/無需寄回）→ restocking fee（**%／per line item**）與退貨運費（**固定額／per return／presentment 幣別**）**自動帶入建議值、可逐筆覆寫** → Exchange 加購（算差額；不能自訂品項；**產生 `ON_HOLD`＋`AWAITING_RETURN_ITEMS` 的 FO**）→ 收貨 accept/restock → 退款 | P0（原 P1，因 P0-02/09 上調）。M：returns + return_line_items(FK→fulfillment_line_items) + exchange_line_items + return_rules + return_policy_snapshots；規格見 S16-F5.1/F5.3/F7 |
| Return rules | 退貨期限（**14/30/90/不限/自訂**；起算點＝**個別品項配送日 或 訂單最後一項配送日**）、restocking fee %、退運費三選一（免費/每次固定/顧客自購）、Final sale 排除品（**collection 或 product 粒度；bundles 不可設**）、**可有多條規則且可按市場切換**、**兩個獨立 toggle（退貨規則管已履行／取消規則管未出貨）** | P0（原 P1）。**規則綁購買時點快照**（`order_line_items.return_policy_snapshot_id`），改規則不追溯既往——見 S16-F7.4 |

<!-- 依 46a:595–601（dev `suggestedRefund` 自動扣抵）＋ 46c:218、46c:436（help 可逐筆覆寫）修正。
     🔴 此處原本寫錯：Return 建立列的註釋欄原寫「『退貨費不自動從退款扣』照抄」——與 dev 的自動扣抵行為直接衝突（50 號表 3 T-01）。
     正確語義是**兩層**：API 自動算出建議值（dev 權威），admin UI 允許覆寫（help 描述的能力）。任何人翻舊版都不要改回去。 -->
<!-- 依 46c:415–419、46c:422–432、44:422/431/437/439 補寫：退貨期限「不限」檔位、第二種起算點、退運費三選一、多條規則、兩個獨立 toggle、
     最終銷售粒度與 bundles 禁令、購買時點快照——原本皆缺 -->
<!-- 依 46a:519、46a:648 補寫：退貨來源是**已送達的 fulfillment line item**，不是 order line item（schema 級，見 S16-F7.2） -->
<!-- 依 46c:297 補寫：在 Shopify 建立退貨運送單**僅限美國地點** -->

## 2. 產品

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| 描述富文本 | 全工具列 + 圖/影/表格 + HTML 檢視；**64KB 上限**；HTML 自動消毒 | P0。TipTap + sanitize（S13-F1 白名單）+ 大小限制 |
| AI 生成描述 | 依關鍵字/語氣生成草稿 | P2（AI 助理家族） |
| 媒體 | **上限 250**；首位=精選圖；圖 ≤20MB/≤25MP；影 ≤1GB/10 分；3D ≤500MB；alt 可編；拖曳排序 | P0 圖片；影/3D P2。S13-F3 全套（direct upload/尺寸驗證/EXIF） |
| 類別 taxonomy | 標準分類 1 個；解鎖類別 metafields、影響稅則 | P2。M：products.category_id + 標準分類表 |
| 定價 | compare-at > price 才顯示特價；**系列頁 Sale 標籤需全變體 compare-at 一致**；cost 算毛利不對外 | P0。展示規則進 storefront helper（S14） |
| 庫存卡 | SKU/條碼（真實 GTIN 供通路）/track 開關/繼續銷售/五狀態量 | P0。M：inventory_*（S13-F5） |
| 變體 | ≤3 選項/≤2048 變體；選項與值可重排改名；**改選項會重建變體**（斷外部引用）；批改價格/圖片/庫存；依選項分組 | P0。S13-F1 diff 更新（不重建）——我們刻意優於本尊的點，註明差異 |
| SEO 編輯 | 標題 ≤70、描述 ≤320、handle 改時「建立轉址」勾選 | P0。S13-F2 redirect 表 |
| 狀態 | Active/Draft/Archived + **Unlisted**（可直連買、不可被發現 noindex） | P0 三態；Unlisted P1（publishing 加 flag） |
| 發布 | 管道與市場清單、**排程上線日期**、變體層級可單獨發布 | P0 管道；排程 P1（publish_at + job） |
| Duplicate | 對話框勾選複製範圍（圖/SKU/條碼/庫存量）+ 複本狀態；影片/3D 不複製 | P1。`Catalog::Duplicate` |
| 封存 vs 刪除 | 封存=下架保留（可回復）；刪除=永久 | P0。被 line_items 引用禁止硬刪（S13-F1） |
| 系列 | 手動 vs 智慧**不可互轉**；智慧：≤60 條件、全店智慧系列 ≤5,000、比對欄位+運算子枚舉（equals/greater/starts/contains/set…）；排序 8 種（手動才可拖曳）；每系列可選管道+排程+SEO | P0 手動+簡化智慧；S13-F4 物化策略；運算子枚舉照 API 清單 |
| 庫存頁 | Available/On hand 可就地編；**Adjust by vs Set to** 兩模式；原因 7 種；調整歷史（180 天，更久用報表）；匯入僅 On hand 可寫 + 防過期校驗欄 | P0。ledger（S13-F5）+ 歷史頁；「on hand(current) 防過期校驗」是漂亮的併發防呆，抄 |
| 轉移 | Draft→Ready（保留庫存）→In transit（記 Incoming）→收貨 accept/reject 可部分；Draft 才可刪 | P1。M：transfers；狀態機進 06 §4 |
| 採購單 | 供應商檔案+成本+付款條件；Mark as ordered 後連結轉移收貨 | P2 |
| 禮品卡 | 面額=變體；後台直接發卡（≤$2,000、需 email/電話）；**停用=永久**；改到期日；完整卡號僅顧客可見（後台只見末 4 碼） | P1。M：gift_cards（code digest 存法同 api_tokens S19-F5） |

## 3. 顧客

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| 匯入 | 覆寫勾選（同 email/電話）；訂單數/消費額唯讀不可匯入；≤15MB | P1。S13-F6 同管線 |
| 批次編輯 | 可批改名/姓/標籤/三通道行銷同意；地址/電話僅單筆 | P1 |
| 合併 | 不可逆；排除清單：訂閱合約/B2B/存卡/店內額度/遮蔽中…；email 衝突規則 | P1。S16-F6 merge service + 排除 guard |
| 刪除/遮蔽 | 有訂單→只能**匿名化**（保留訂單、遮蔽個資） | P0（隱私義務）。S16-F6 anonymize |
| 稅務豁免 | 收稅/不收/除豁免外收 + VAT 驗證 | P2。金額引擎讀 customer.tax_exempt |
| 存卡管理 | 「寄更新卡片連結」（不是商家改卡） | P2。Stripe customer portal 等價 |
| 分群語法 | filter+運算子+值；相對日期（-30d）；MATCHES 複合（products_purchased、orders_placed(count>=3,date>=-6m)、距離、開信）；RFM 群組；即時動態成員 | P1 簡化集（amount_spent/orders/last_order/tags/訂閱）；語法解析器白名單編譯成 SQL（S19 同思路）；RFM P2 |

## 4. 折扣

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| 方法 | 碼 vs 自動；**自動折扣全店同時 ≤25**；**每店累計碼 ≤2,000 萬**（刪碼可釋放） | P0。上限做成 DB 檢查 + 建立時預檢（S17-F1） |
| 值與範圍 | %或固定；固定+指定商品時「每單只套一次」勾選；適用商品/集合/顧客各 ≤100 個 | P0。S17-F2 |
| 顧客資格 | 全部/指定顧客/分群/市場；自動折扣 ≤5 分群、碼 ≤100 分群 | P1 |
| 組合 | class 三類；**每單 ≤5 個 product+order 碼、1 個 shipping 碼**；shipping 不互疊；同品項雙 product 折扣僅 Plus；順序 product→order→shipping；兩個 % 訂單折扣都按原始小計；POS/草稿手動折扣無視組合 | P0。S17-F2 已覆蓋——把「兩個 % 訂單折扣按原始小計」寫進測試表 |
| 上限 | 總次數/每人一次（以 email 或電話判定） | P0。S17-F3 原子保證 |
| 推廣 | 分享連結（可帶 UTM、導向頁）+ QR code | P2 |
| 列表管理 | 停用/重啟（批次有狀態限制）、複製、刪除、篩選存檢視、詳情時間軸 | P0 基本；時間軸掛 events |

## 5. 分析

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| Dashboard 自訂 | Customize 模式：指標庫拖入卡片、移除/重排/縮放、具名區段、Reset to default | P1。M：`dashboard_layouts(staff_id, cards_json)` |
| 日期+比較期 | 任意區間、多期比較 | P0。rollup（S19-F2） |
| 報表 | 分類庫；開啟後可改篩選/欄位/群組；**表格上限 1,000 列但總計含全部**；另存自訂；匯出取全部 | P1 三張 → P2 報表庫。1,000 列+完整總計的取捨照抄 |
| Live View | 地圖點（藍=工作階段、紫=訂單）、近 5 分訪客、午夜起指標、行為漏斗（近 10 分）、streamer mode 隱藏數字 | P1 簡版（S19-F3 sessions 資料） |

## 6. 線上商店與內容

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| 主題庫 | 已發佈+未發佈（**上限 20/Plus 100**）；新增：商店/上傳 ZIP/AI 生成；試用主題可自訂不可改碼；分享預覽連結（訪客 2 天/店主 30 天） | P1（M6）。M：themes（role+library）；預覽=簽名短效連結（S14-F3） |
| 主題動作 | 自訂/發佈（原主題退回庫）/複製（改版前備份）/下載/改名 | P0（M6）。發佈=原子切換（S14-F2） |
| 部落格編輯器 | 標題 ≤255、富文本、摘要、精選圖、作者（限員工）、所屬部落格（可新建）、標籤、模板、SEO、可見性（顯示/隱藏/**排程發佈**） | P1。M：blogs/articles；排程=publish_at job |
| 選單 | 項目=名稱+連結（八種資源型+URL）；拖曳縮排巢狀；轉址管理同區 | P0（M2）。M：menus/menu_items + url_redirects |
| 偏好設定 | 首頁 SEO、社群分享圖、**密碼保護**、表單 captcha 開關、國際自動導向、hreflang 開關 | P0：密碼保護（S14-F6）；其餘 P1 |
| 檔案庫 | 一次 ≤20 檔；圖 ≤20MB/影 ≤1GB；篩選（類型/大小/使用位置）；複製 CDN 連結；**刪除警告引用斷裂** | P1。M：file_assets + 引用計數（referenced_by 查詢） |
| 元物件 | 定義（名稱→type ID、欄位=型別+驗證、display name 欄）→ 條目；存取範圍開關（後台/API/主題） | P2。S05-1f/M metafield 系統延伸 |

## 7. 市場與財務

| 控制項 | 功能與邏輯 | 實作註釋 |
|---|---|---|
| 市場建立 | 名稱+國家群（**一國僅屬一市場**，加入會自動移出原市場）；Active/Draft；主要市場=商店幣別 | P2。M：markets |
| 幣別定價 | 當地幣結帳；自動匯率（轉換費 1.5–2% 內含於顧客價）或手動匯率；**價格捨入**（€9.73→€10.00）；catalogs 固定價覆蓋 % 調整 | P2。金額引擎的 presentment 層；捨入規則表 per currency |
| 市場預覽 | 商品頁「View as」看該市場價格與可售性 | P2。context 切換參數 |
| 財務總覽 | 交易卡/帳戶卡（payouts 待轉/餘額）/稅務卡/工具 | P1 殼。payout 對帳報表（選期+幣別→交易+費用+撥款拆解）=S19 對帳精神 |

## 8. 設定（16 分頁精華）

| 分頁 | 關鍵設定項與邏輯 | 實作註釋 |
|---|---|---|
| 一般 | 商家法定資訊（不對顧客顯示；影響稅/金流）；時區（報表切日）；幣別（**首單後改需支援介入**）；訂單編號：**只能改前後綴，流水號固定 1001 起不可變** | P0。S15-F5 每店計數器；幣別鎖（S19-F4） |
| 方案 | 升降級即時生效；**Pause and Build**：停結帳/折扣/禮品卡、可編商品、前台可看不可買；關店：結清→網域斷開→資料保留 2 年可重開 | P2。shops.plan + paused 狀態機；「可看不可買」＝storefront 攔 checkout |
| 使用者 | 邀請（CSV 批量）；角色權限按模組×動作；強制 2FA；協作者（4 碼申請碼、不佔名額）；停用保留紀錄 | P0 簡版→P1 全顆粒（S12-F3） |
| 付款 | 測試模式（測試卡體系；不入報表；**上線前必關**）；帳單敘述名（2-19 字元）；**請款四模式**：①結帳時自動（預設，全方案）②整張訂單出貨完成後自動（全方案）③**每次履行時自動（🔴 僅 Shopify Plus）**④手動（授權期 7 天、逾期失效）；手動付款方式（自訂名禁保留字；訂單入 Payment pending）；payout 排程 | P0：Stripe test+手動付款；capture 模式 P1。上限與方案差異一律引用 `config/limits.yml` 的 `capture.*` |

<!-- 依 46c:508–514（H33 zh-TW）修正，原文為四種請款模式的表格：結帳時自動請款（預設）／履行時自動請款／**每次履行時自動請款（僅 Shopify Plus）**／手動請款。
     🔴 此處原本寫錯：本行原寫「**請款三模式**：結帳自動/全單出貨後自動/手動」，並在註釋欄寫「**修正：舊文件寫四模式，官方現行三模式**（第四種是訂單處理的自動出貨開關）」——
     **這條「修正」本身是錯的**：help 2026-08 實抓仍為四模式，第四種是 Plus 專屬的「每次履行時自動請款」，不是出貨開關。
     後果：多次出貨的訂單會「該逐次請款卻整單請款」。任何人翻舊版看到「三模式」都不要改回去。 -->

**授權效期與附加費（46c:517–526，一併補進 `limits.capture`）**：Shopify Payments 預設 **7 天**；Plus 延長 Visa/MC/Amex **30 天**、Discover/JCB **10 天**、Diners/CUP **7 天**；未請款的 pending hold 在顧客銀行帳單上最長顯示 **30 天**；🔴 **逾授權期限後才請款需付 1.75% 附加費**；支援部分請款，金流商或 Plus 支援**多次**部分請款。
| 結帳 | 聯絡方式（email/電話）；姓名/公司/地址2/電話 三態配置（不含/選填/必填）；行銷同意（email 可預勾按地區、**SMS 永不預勾**）；小費三檔%；訂單處理：預設帳單=運送地址、付款後自動出貨（三選項）、自動封存開關 | P0。checkout 欄位配置=settings JSON（S19-F4 schema 驗證） |
| 顧客帳號 | 新版（OTP、託管頁、365 天 session）vs 舊版；登入入口開關；自助退貨；店內額度；帳號 URL 自訂 | P1（S12/16 對應） |
| 運送 | General 1 + Custom ≤99 方案（**預設設定檔的商品集合是「補集」**＝未包含於其他設定檔的所有商品）；**跨方案購物車運費相加**（合併鍵＝**費率名稱**：名稱相同→相加、名稱全不同→取最便宜相加）；zones；費率條件（重量/金額 min-max）；**其他配送方式三列**：`當地配送`（半徑 ≤160km 或郵遞區號 3000 字元；≤10 區/地點；基本費+3 層條件）／`到店取貨`（備貨時間+指示）／**`取貨點 (pickup points)`＝台灣超商取貨的對應設定**；包裹；裝箱單 Liquid | P0 zones+條件費率+**取貨點（P0-13，見 S15-F3.1／S16-F3.3／28 §11）**；本地外送/到店取貨 P2。「跨方案相加」與「名稱為合併鍵」進金額引擎測試 |

<!-- 依 44:322 補寫，原文：後台「其他配送方式」三列＝`🚚 當地配送`／`🏠 到店取貨`／**`📍 取貨點`（已停用）**，44 逐字標「這正是台灣超商取貨的對應概念，
     我們 42 號前台的超商取貨流程在 admin 側要對應此設定」。原本本列只有「本地外送/取貨」兩種，**第三種取貨點完全缺席** → 台灣最主要配送方式在 admin 無處設定。
     依 46c:863、46c:869–876 補寫：預設設定檔的「補集」語義、合併運費以**費率名稱**為合併鍵（原本只寫「跨方案相加」，不寫合併鍵會算錯運費）。 -->
| 稅 | 登記啟動；含稅開關；按顧客國含/排稅；運費課稅；**覆寫以集合為單位**（可用未發布集合）；顧客免稅優先於覆寫；關稅 DDP（需 HS+產地，缺漏規則） | P0 單率；覆寫 P1（S15-F2 引擎讀取順序：顧客免稅>覆寫>登記稅率） |
| 地點 | 上限（Basic~Advanced 10/Plus 200/app 地點不佔）；預設地點必有；**出貨優先序拖曳**＝分單邏輯 | P1 多地點時進 fulfillment routing |
| 市場/網域/通知/自訂資料/政策/隱私 | 見各節；網域：主網域+301 開關（國際網域可關）；通知：模板分類+個別開關+webhooks 入口；自訂資料：definition（namespace.key+型別+驗證+pin）；政策：六類+固定 URL /policies/*；隱私：cookie banner 按地區+GPC | 各對應 S12-F1/S18/S05-1f；政策頁 P0（storefront 需要） |

## 9. 修正與新發現（回寫既有文件）

1. ~~**付款請款是三模式不是四模式**（01/04 提到的 per-fulfillment 屬舊版）——已在本文件修正，實作按三模式。~~
   🔴 **此處原本寫錯，已作廢**：`docs/research/46c:508–514`（help.shopify.com H33 zh-TW，2026-08 實抓）明列**四種**請款模式，
   第四種「**每次履行時自動請款**」是 **Shopify Plus 專屬**（不是「訂單處理的自動出貨開關」）。
   **正確結論：請款是四模式**，見 §8 付款列與 `config/limits.yml` 的 `capture.modes`。此條「修正」是本文件自己引入的錯誤，任何人翻舊版都不要改回去。
2. 訂單編輯限制清單（不能改訂單層折扣、已出貨項不可移除、分期單不可編、運費不重算）→ 併入 S16 的 guard 清單。
3. 折扣組合細節：每單 ≤5 個 product+order 碼 +1 shipping；同品項雙 product 折扣是 Plus 專屬；POS/草稿手動折扣無視組合 → S17-F2 測試表補列。
4. 配額類常數表（實作直接引用）：自動折扣 25、折扣碼 2,000 萬、變體 2048、選項 3、媒體 250、智慧系列全店 5,000、條件 60、主題庫 20、theme 試用 19、地點 10/200、手動 hold 10、附加分群 5/100、適用清單 100、留言可編輯窗 5 分鐘、棄單保留 90 天、匯出同步門檻 50 筆、CSV 15MB、描述 64KB、標題類 255 字、訂單起始號 1001。
   ✅ **`config/limits.yml` 已建立**（2026-08-12），並補入原本缺漏的：冪等 TTL 24h、cursor/陣列 input 250、棄單信延遲四檔 1/6/10/24（預設 10）、`cart_item_limit` 建議 50、退貨/取消 staffNote 與 returnReasonNote 各 255、自助退貨 250 品項、批次取消 250、請款四模式與授權效期表、逾期請款 1.75%、庫存七原因與五態、取貨點 COD/材積、Function 資源限制。**每條都帶出處註解**，實作不得硬編碼（鐵律 6）。
5. 新狀態：商品 **Unlisted**；訂單匯出交易史；草稿 Reserve/Lock pricing；出貨 on-hold 原因；退貨 restocking fee——均已排入分級。
6. Academy 目錄結構（角色軌×主題路徑×認證）→ 第三階段做「平台學院」時的資訊架構參考，先不動工。

## 10. 開發方案補充（納入 07/10）

- **實作順序不變（M0–M6），本文件作為每一頁的「驗收清單來源」**：做訂單詳情頁時，逐行對照 §1b 打勾。
- 新增里程碑內容：M4 加「訂單編輯+Mark as paid+取消對話框」；M5 加「折扣推廣連結、檢視系統含欄位設定」；M1 加「批次編輯器簡版、匯出>50 筆轉背景」。
- `config/limits.yml` 常數表在 M0 建立；所有上限檢查引用它。
- v2 原型的「開發註釋模式」與本文件同步維護：**新畫面先寫註釋再寫代碼**——註釋即規格。
