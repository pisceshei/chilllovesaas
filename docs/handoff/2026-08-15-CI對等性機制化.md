# 2026-08-15 交接 — 一條條款在寫下的隔天就被違反了

**本輪產生的 worklog**（CLAUDE.md §工作方式：交接文件要列出本輪產生的**所有** worklog）：
- `docs/worklog/2026-08-15-CI對等性機制化.md`
- `docs/worklog/2026-08-15-對等性檢查器補上反向證明.md`
  <!-- 🔴 2026-08-15 補（PR #39 第 3 輪驗收指出）：本 PR 後半段（commit 4a60920）
       新增了回歸測試與 9 份 fixture 並自成一份 worklog，而本清單沒跟上。 -->

---

## ① 我改了什麼

1. `config/ci.rb` 補上三支它漏掉的檢查，並移除自稱「五個」的錯誤數字。
2. 新增 `scripts/check-ci-parity.rb`：斷言 `ci.yml` 用到的每一支 `scripts/*` 都在 `config/ci.rb`。
   同時掛在 `ci.yml` 與 `config/ci.rb`。
3. 三處事實錯誤：`config/limits.yml:2041` 的示範改用 `Limits.fetch`、
   worklog 出處改指 Pending 第 5 條、handoff 的改動範圍數字回寫。
4. 🔴 **後半段（commit `4a60920`）：給 `check-ci-parity.rb` 補上反向證明**——
   新增 `scripts/test-ci-parity-rules.rb`（10 條 / 9 個 fixture）並掛進 ci.yml 與 config/ci.rb。
   為此先把它的 `ROOT` 改成吃 `ARGV[0]`：**寫死 ROOT 正是它先前寫不了測試的原因**。
5. 🔴 **那份測試在寫出來的當天就抓到兩個活 bug**（兩者當時都讓對等性檢查 exit 0）：
   ①`"pnpm audit".include?("npm")` ＝ true ⇒ ci.yml 跑 `npm audit`、ci.rb 只有 `pnpm audit`
     也算通過（備援正則同樣命中後半段，兩道防線一起失效）。修法＝兩側共用 `command_key`；
   ②`step` 的名稱與指令**在同一行** ⇒ 腳本名寫在**名稱**裡就算「有跑」。
     修法＝只取第二個引號字串，並補兩條 fail-closed。
6. 修掉四處過時的「前八步」，以及 `config/ci.rb` 一句**會誘導出有害修法**的錯誤註釋
   （「quality job 另外跑⋯＋ schema drift」——schema drift 其實在 `test` job，
   而 quality job 沒有 mysql ⇒ 照原文去補 `check-schema-drift.rb` 會得到一個
   **結構上不可能失敗**的步驟）。

---

## ② 為什麼這樣改（含被推翻的假設）

**被推翻的假設：「把理由寫進註釋，下一個人就會照做」。**

`config/ci.rb` 那段條款寫得非常清楚——它甚至預先解釋了不同步的後果
（「本機全綠 → 推上去被擋，而擋下來的理由是本機**沒有機會發現**的」），
還特地註明「理由寫下來免得又被漏掉」。

**它在寫下的隔天就被違反了**，而違反它的是我：PR #33 加了兩步 CI，`config/ci.rb` 一行未動。

🔴 這不是「有人不夠細心」，這是**紀律型條款的固有失效率**。同一個倉庫已經有三個同型前例
（CLAUDE.md 鐵律 2 的白名單、AGENTS.md 的執行位元節、`config/ci.rb` 這條），
每一次的教訓都寫成「下次要記得同步」，然後下一次照樣漏。
⇒ 唯一有效的處置是**讓機器擋**。所以本輪的重點不是補那三步（那只是清現場），
而是 `check-ci-parity.rb`。

**一個實作決定值得記**：對等性檢查**只認 `step` 開頭的行，不認註釋**。
因為本腳本自己的檔頭就大量提到腳本名——若連註釋都算數，
任何人在 `config/ci.rb` 寫一句「以後要加 xxx.rb」就能讓檢查變綠。
這類「檢查器被自己的文字騙過」的形態，本輪驗收在別處也抓到了
（ERB fixture 的違規其實來自它自己的註釋，見 worklog Pending 2 的 Y3）。

**另一件事：三條事實錯誤裡有兩條是「宣稱與現況不符」，我選擇修現況而不是修宣稱。**
`config/limits.yml:2041` 那條特別清楚：PR #33 的 worklog 說「已全部改回 `Limits.fetch`」，
而 limits.yml 漏改 ⇒ 那句宣稱是假的。**修好代碼讓宣稱成真**，比把宣稱改成
「大部分改回」更有價值——後者是把標準降到現況。

---

## ③ 還有什麼沒解決

1. 🔴 **inline shell 步驟不在對等性檢查範圍**（執行位元閘門）。已知缺口，寫在誠實聲明裡。
   正解是抽成 `scripts/check-exec-bits.sh`，本輪沒做。
2. ⚠️ **本輪驗收另有 9 條 🟡 未處理**，全部已複驗成立，逐條列在 worklog Pending 2。
   其中三條我認為最該優先：
   - **Y3**：ERB fixture 證明不了它宣稱的事（違規來自它自己的註釋）——
     這是「機制看起來有效、實際沒驗到」的典型。
   - **Y2**：回歸測試只覆蓋 TrueClass，FalseClass 分支從未執行——
     而**鐵律 11 的挪威 pack 就是 `no:`**，那正好落在沒覆蓋的分支。
   - **Y7/Y8**：`claude-review.yml` 的 fallback 又把四種路徑收斂成一個假歸因，
     與 PR #36 才剛修掉的是同一型。
3. ⚠️ **`config/ci.rb` 前面那批通用步驟與 `test` job 沒做對等性**——刻意不做，理由見誠實聲明。
   （原文寫「前八步」，2026-08-15 更正：該區塊實際 9 步，且會再增加 ⇒ 敘述不寫死步數。）

---

## ④ 下一個人要注意什麼

- 🔴 **往 `ci.yml` 的 quality job 加檢查步驟時，同時加進 `config/ci.rb`。**
  忘了也沒關係——`Check CI parity` 會擋下來，訊息裡直接點名是哪一支、哪個步驟。
  這正是本輪要解決的問題：以前只有一句「兩邊要同步」的註釋，現在有機器擋。
- 🔴 **加了新檢查腳本，別忘了它自己也要有反向測試**（65 §K.7）。
  ✅ **`check-ci-parity.rb` 的那一份已經在本 PR 後半段補上了**
  （`scripts/test-ci-parity-rules.rb`，10 條 / 9 個 fixture，突變 8/8）。
  <!-- 🔴 2026-08-15 更正（PR #39 第 3 輪驗收指出）：本條原文逐字是
       「本輪的 check-ci-parity.rb **沒有**配 fixture 回歸測試，只做了手動正反向驗證⋯
       一併補比較划算」——那在 commit 4a60920 之後就不成立了，
       而 handoff 是專案指定的「這一輪的判斷與教訓」入口 ⇒
       它等於在叫下一個人去補一個**已經補好的洞**。
       🔴 本 PR 開篇那張「ci.yml 9／ci.rb 6／註釋自稱 5，三個數字互不相同」的表，
       講的就是**敘述沒跟著交付走**。修好了機制，卻在自己的交接文件裡復發同一型。 -->
  🔴 **真正的教訓不是「要補測試」，是「補之前先確認它寫不寫得出來」**：
  這支腳本先前寫不了測試，因為 `ROOT` 寫死。**可測性是設計需求，不是事後補的東西。**
  ⚠️ 同型缺口仍在 worklog Pending 2 的 Y6（執行位元閘門，PR #41 處理中）。
  🔴 **而且缺口比想像的大**（第 4 輪驗收查證）：已合併的 7 支檢查器裡，
  **三支至今零反向證明**——`check-tenant-isolation.rb`（守**鐵律 2 多租戶隔離**）／
  `check-reversal-naming.rb`／`check-baseline-raise.py`。
  它們改壞了不會有人知道。**不要以為「除了這一支，倉庫已全面覆蓋」**——
  我在本 PR 裡就這樣寫過一次，被打回。
- **驗收發現的完整清單在 worklog Pending 2**（9 條 🟡），不要重跑一次多代理驗收才發現它們。
