# Handoff：Ella 修復 PR-28（2026-09-02）

## ①我改了什麼

預覽 hover 工具列（橋四鈕 ⇒ cl:op ⇒ 既有 section ops）。逐檔＝worklog。

## ②為什麼這樣改

- op 不在橋內執行、只發訊息：單一真相源在父頁 op-stack，橋零狀態。
- functional setSelectedId：onMessage 掛載一次的 effect 內任何 state 直讀
  都是 stale——這是本頁第三次同型（undoRef／templateKey 註記同軸）。

## ③還有什麼沒解決

- ↑↓ 邊界 disabled 視覺；本尊鈕集/位置量測（V）。見 worklog Pending。

## ④下一個人要注意什麼

- cl:* 訊息家族現有 select/highlight/replace/navigate/op——新增訊息記得
  三件套：橋發送、父頁同源驗證分支、ED 測試（異 origin 忽略格照抄 ED3）。
- 橋 DOM 注入物一律 id 前綴 cl-——BR 系測試以此斷言、公開店面不帶。
