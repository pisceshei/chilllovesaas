# Handoff：G5 步 20c 壓測與 sortKey（2026-09-01）

## ①我改了什麼

步 20 第三件（散件包）。base＝main `bf047fac`，分支 `g5/step20c-stress-sortkey`。
ST1 壓測＋sortKey＋91 補正。逐檔＝worklog Changes。

## ②為什麼這樣改

- 壓測相異冪等鍵（不是同鍵）：同鍵被 Guard 一層擋掉＝測不到 checkout 層
  兜底；相異鍵才逼出 FOR UPDATE/條件轉移/唯一索引三層。
- sortKey enum 全九值進 schema（對外形完整）、resolver fail-closed 支援子集
  ——比「enum 只放支援值」誠實（呼叫端拿到明確 code 而非 schema 錯）。

## ③還有什麼沒解決

- dunning（37 W1）＝步 20 最後實作件；87 V 項＝user-gated。

## ④下一個人要注意什麼

- ORDER_KEYS 新鍵紀律：nullable 欄不得直進（NULL 消列）；新鍵必登
  KeysetCursor 白名單（unknown key 撞過——memory）。
- 壓測 spec 的 purge! 表序涵蓋 webhook 兩表（20a 新 FK）——新資料層落地
  記得回這裡補刪序。
- 部署宣稱以伺服器 git log 現值驗（worklog ⚠ 教訓）。
