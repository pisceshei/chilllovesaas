# Handoff — 包 32：Markets 資料層（2026-08-31）

## ① 我改了什麼

六步方案（`docs/plans/2026-08-31-W6下半與結帳線-六步方案.md`）**步 1** 全量：
Markets／Domains admin 實測取證（`docs/research/84`，店 chill-love-u5q5mnzq，2026-08-31）→
資料層五表 migration（`20260831150000`；四表＋`market_regions`——29 §1.4 明列獨立表且
`region_of()` 缺它接不起來，屬工作卡「四表」的必要補充）→ 模型五支 → 服務四支
（UrlPrefix／HreflangCodes／PrefixIndex／ProvisionDefaults）→ 建店 callback＋既有店回填
（`20260831151000`）→ limits 四組落鍵 → 67 §J 更正 → specs 29 例＋4 突變全紅記錄。
驗證輸出與處置矩陣＝配對 worklog `docs/worklog/2026-08-31-包32-markets資料層.md`。

## ② 為什麼這樣改

- 前綴規則、白名單 schema、hreflang 展開全部逐字取自 67 §F.1(b)／§C.8／62 §I.2（使用者
  2026-08-13 裁定），不是本尊行為——本尊「primary 預設語言無前綴／多國裸碼」是**被裁定
  換掉的**，62 §I.2-1 已有明知偏離登記，不得「修回去」。
- 值域三組（market status／condition types／domain types）全部**親測**（84 §3–§4 DOM 收割
  與對話逐字），不是從 29 抄——29 的 enum 得到正面複驗。
- ProvisionDefaults 單一實作＋兩個薄呼叫端＝复刻 `20260826060000` 檔頭教訓（三份實作、
  spec 守自己那份 ⇒ 殺傷性突變全綠）。
- 被推翻的假設：〔推論〕「market_web_presence_locales 可等步 3 再建」——67 §I L7b 明文
  「白名單是路由表的內容，與 L7 同批」；且 FK ③⊆② 要與表同時落，後補會產生無 FK 的歷史列。

## ③ 還有什麼沒解決

worklog Pending 段逐項（V-225 未裁定；pt-BR 前綴縫隙需裁定；lineage 重算器未落；
catalog_version bump 與 62 §I.3(b) 失效觸發留給步 2–4 消費端；host 快照需 subdomain 兜底；
hreflang 40 國上限執法在步 4）。另：submarket 保存後的繼承徽章行為＝未取得（84 §5）。

## ④ 下一個人要注意什麼

- **步 2（包 33 後半 hosting）入口**：host→shop 解析讀 `domains`（host 全域唯一索引）＋
  `shops.subdomain` 兜底；帶前綴路由直接消費 `Markets::PrefixIndex.resolve`（回 nil 一律 404，
  根路徑 302 到 `UrlPrefix.for(primary presence, presence.default_shop_locale)`）。
- 🔴 `url_prefix` 與 `hreflang_codes` **不得合併**、輸出不得互相借用（67 §F.1(a)；SF-9 已用
  TypeError 封死）。看到「一個對外轉換函式」的重構提案先讀 67 §F.1(a) 的判準表。
- 🔴 白名單寫入（開／關／排序／改預設）目前只有資料層語義；admin UI（§C.8(c) 兩分區、
  三數字確認）與失效管線接線是後續包——接線時記得 `close!`／`reopen!`／`set_default_locale!`
  是唯一入口，不要繞過去 update_column。
- 重跑驗證：`bundle exec rspec spec/services/markets spec/models/markets_data_layer_spec.rb`；
  突變矩陣見 worklog（四格手動重放即可複驗）。
- 停止條件：V-225／pt-BR 兩處縫隙**需要使用者裁定**，實作端不得自行選邊。
