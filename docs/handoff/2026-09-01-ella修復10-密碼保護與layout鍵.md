# Handoff：Ella 修復 PR-10（2026-09-01）

## ①我改了什麼

/password 標本對表三件（密碼保護整條／平台密碼頁 ERB／layout 鍵＋
current_page）。分支 `ella/password-layout`。逐檔＝worklog。

## ②為什麼這樣改

- 平台頁走 ERB：標本實錘零主題資產＋Rails form 形——與我方棧同構，
  不進 Liquid。
- cookie 綁 digest 雜湊：改密碼全域踢出（本尊語義的最小正確形）。

## ③還有什麼沒解決

- admin 密碼設定 UI；平台頁 vs 主題頁切換判準（未取得）；checkout 豁免
  （未取證）；PR-9 srcset／PR-11 編輯器 live 五格接續。

## ④下一個人要注意什麼

- require_storefront_password 在 BaseController——新增 storefront
  controller 自動被閘；平台頁類（password 自身）要 skip。
- render_layout 現在吃 template_key——新渲染路徑呼叫端帶上它才有
  layout 鍵語義。
