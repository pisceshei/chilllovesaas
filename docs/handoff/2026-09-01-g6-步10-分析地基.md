# Handoff：G6 步 10 分析地基（2026-09-01）

## ①我改了什麼

20 步方案步 10。base＝main `68882ab`，分支 `g6/step10-analytics`。
rollup 表＋計算服務＋15 分排程＋overview query＋首頁 KPI。
逐檔＝worklog Changes；口徑與紅證＝`docs/dev/g6-analytics-foundation.md` §2。

## ②為什麼這樣改

- 覆蓋制而非增量制：19-F2 允許兩者，覆蓋制的冪等性免費（重跑同值），
  增量制要冪等還得記帳；量級小時覆蓋制全贏。
- AOV 分子獨立存列：查詢端反推（total/orders）在退款期間必錯——官方例外
  （80 §3①）只有「分子單獨落列」才守得住（MA5 紅證）。

## ③還有什麼沒解決

- 91 §3.56 六 ⚪＋HomePage vitest 未寫（worklog Pending）。
- 生產煙測待部署後（跑 rollup→query→負數日驗證→清理）。

## ④下一個人要注意什麼

- 🔴 新指標三步：Metrics 辭典登記單位 → RollupDaily 算值 → 測試含「重跑冪等」
  與「該指標的官方例外」格；**永遠不要在查詢端做指標運算**（AOV 反推＝MA5 家族）。
- 🔴 突變 fixture 先過 equality-trap 檢查：錯實作與對實作在你的 fixture 上
  會不會同值（MA3 首輪存活＝隔日退款讓 returns=0）。
