# Handoff：Ella 修復 PR-13（2026-09-02）

## ①我改了什麼

Liquid 全域覆蓋批：pages／images 全域、shop.vendors/types、
collection.filters=[]、powered_by_link（ours）、page_image、
unit_price_with_measurement。逐檔＝worklog。

## ②為什麼這樣改

- 缺口來源＝6 軸艦隊對 Ella 7.2.0 全量 522 檔掃描（filter 89 種/tag 39 種/
  全域 45 種對照），本批取「中影響且不需新資料模型」的全部——影片鏈要
  media 資料模型、facets 要整包，都不擠進本 PR。
- collection.filters 給 [] 不給 nil：nil 在 Ella facets 是「有篩選器」分支
  （`nil != empty` 為真）——空容器會渲染出來。
- powered_by_link 品牌換我方＝鐵律 9（官方值是 shopify.com 署名連結）。

## ③還有什麼沒解決

- pages/images 的迭代（paginate）形；article featured image；social
  sharing image 後備；影片鏈；facets 整包。見 worklog Pending。

## ④下一個人要注意什麼

- PagesDrop/ImagesDrop 都是 by-key liquid_method_missing 形——未命中
  count_miss 遙測＋nil，別改成拋錯（主題對缺頁寬容是官方行為）。
- ImagesDrop 查 StoredFile 用 without_tenant＋顯式 shop_id（鐵律 2 條款②，
  同 resolve_settings_file）；別「順手」改成 default scope。
- unit_price_with_measurement 輸入是 integer cents——資料層接
  variant.unit_price 時別餵主單位浮點（鐵律 3）。
