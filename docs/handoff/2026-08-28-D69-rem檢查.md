# Handoff — D69 rem 靜態檢查（2026-08-28）

工作包＝D68 裁定②的 B 道。配對 worklog：`docs/worklog/2026-08-28-D69-rem檢查.md`。
輸入 ref：`main` = `9221326`（D68 已合併）。分支 `m2/d69-rem-check`。

## ① 我改了什麼

新增 `scripts/check-rem-tokens.rb`（rem 完整性）＋ `scripts/test-rem-token-rules.rb`
（8 格回歸）＋ 7 份 fixture；接線 `ci.yml` 與 `config/ci.rb`。
五個活突變逐一實跑、全部轉紅。

## ② 為什麼這樣設計

- **只掃數字後綴的 token 名**：別名（`--t-sm: var(--t-325)`）的單位由被指向者決定，
  框進來會誤傷所有間接層。
- **零掃描 canary 是 exit 2 不是 0**：token 改名／區塊搬走／regex 失配三種情況
  在「掃到 0 顆」這裡同形——沒有 canary 的話三種都靜默全綠。
- **掃描前剝註釋**：D68 的 `*/` 事故同根——說明文字與宣告同形。
- **M3（刪一族）是設計期預判會存活的突變** ⇒ fixture 表特意有 `px_lineheight`
  單獨一格＋ `family_empty` 的 needle 指名 `--lh-`。

## ③ 還有什麼沒解決

- 消費端硬編 px 看不到（`91` §3.42 W-3，擾動法未機械化）。
- `--sp-`／`--r-`／`--h-`／`--sz-` 維持 px（`91` §3.42 W-1，待另一裁定）。

## ④ 下一個人要注意什麼

- 🔴 **本 PR 觸 18.3，人工合併**；合併後任何人把 token 改回 px，CI 的
  `Check rem token integrity` 會紅——那是本意，不要用改 checker 的方式讓它綠。
- 🔴 **新增 `scripts/*.rb` 記得 `git update-index --chmod=+x`**——Windows 上
  檔案系統沒有執行位元，`git add` 後預設 100644，`check-exec-bits` 會紅。
  本輪就中了一次（先 chmod 再 add 會被 add 蓋回去；**順序＝先 add 再 chmod**）。
- 給 checker 加新家族時：FAMILIES 加一條 ＋ **至少兩格 fixture**（該族違規 ＋ 該族單獨為空）。
