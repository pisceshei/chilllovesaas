# Handoff — 2026-08-20：PR #61 首輪驗收修復

## ① 我改了什麼

PR #61 首輪的 Claude／Codex 意見已按根因收斂：D32 已同步到兩份鐵律與總方案；P-8 改成
來源 PR 先固定、再由 evidence-tail 登記；排程器加入原子節點物化與隔離 worktree；CD 路線
拆出可用 Actions secrets 的 bootstrap 通道，並補異地備份與不污染 main 的 rollback 測試。

本輪 worklog：`docs/worklog/2026-08-20-PR61-首輪驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：PR #61 仍是常規文檔 PR。** 使用者選案後它修改 `CLAUDE.md`／`AGENTS.md`，已命中
  18.3；因此只能在雙清後由使用者人工合併，不能拿它測 D31 代行合併。
- **被推翻：證據可在產生它的同一 PR 回寫。** 回寫會產生新 commit，使舊 run 不再對應
  current head；改為來源 PR 先合併、獨立 evidence-tail 引用固定證據。
- **被推翻：CD-1 能直接用只存在 Actions secrets 的 SSH key。** GitHub 官方要求
  `workflow_dispatch` workflow 先存在 default branch；因此先合併 CD-1a 通道，再 dispatch，
  CD-1b 才承接初始化與證據。
- **被推翻：同機 dump 足以抵禦伺服器失效，或壞版本可合進 main 測 rollback。** 前者沒有
  獨立故障域，後者讓 git head 永久停在壞版本；現行方案改為加密異地副本還原演練，以及
  未合併故障注入後重新驗健康 main SHA。

## ③ 還有什麼沒解決

- PR #61 的內容凍結後本地閘門已全綠，尚待 commit 後 diff、三端點全量重拉、push 與新 head
  雙清；完成後仍須使用者人工合併。
- P-8 單元二的 evidence source／tail 尚未執行，必須在 PR #61 合併後開始。
- CD-1 的伺服器與 object storage 輸入尚未取得，且任何可能產生費用的 provider／retention
  必須在 §7 輸入點由使用者裁定。
- `91` §3.5 已登記但未擴修的鐵律 13.3 舊錨與 S-1 射程疑義，必須各自另包處理。
- Windows Ruby `Tempfile`／Git Bash 的 workflow syntax 假紅只以倉庫外 adapter 複驗，
  未修改 `scripts/`；日後若另包修 portability，必須用承重 fixture 防止把閘門直接跳過。

## ④ 下一個人要注意什麼

1. 先以 `git diff pr61-last-push..HEAD` 確認本輪只含點名根因、D32 與三件套；不要修 §3.5
   登記的同型實物。
2. 外部語義只沿用本輪已落庫的官方 URL、逐字與 2026-08-20 取證日期；來源更新時重查，
   不把「11」或 retention 候選當永久平台契約。
3. PR #61 無論四條件是否齊都不得代行合併；通知使用者人工合併後，其依賴鏈才可解鎖。
4. 並行包必須一包一 worktree；合併後先確認工作樹乾淨再移除，不能在共用 checkout 切分支。
