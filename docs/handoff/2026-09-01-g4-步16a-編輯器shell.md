# Handoff：G4 步 16a 編輯器 shell（2026-09-01）

## ①我改了什麼

20 步方案步 16 第一包。base＝main `97acce0`，分支 `g4/step16a-editor-shell`。
三面板骨架＋橋＋bootstrap 讀取面。逐檔＝worklog Changes。

## ②為什麼這樣改

- templateJson 走 DB 覆寫優先＝與渲染同一讀序（Runtime#template_json）——
  編輯器看到的必須是「渲染會用的那份」（ME1 紅證：只讀檔＝編輯過的模板
  在編輯器裡裝沒事）。
- 橋同源嚴格比對兩端（14 §F3 坑）：我方預覽端點在 admin 同 host ⇒
  location.origin 精確等；未來預覽搬獨立網域時改白名單、不改語義。
- 唯讀先行：骨架與橋是後續全部子包的載體，先立穩再上寫入。

## ③還有什麼沒解決

- 16b op-stack（六原子 op 表＝24 §3）；儲存紅線（touch theme＋lock_version）。

## ④下一個人要注意什麼

- 16b 寫入落點：Template row upsert（DB 覆寫層）＋🔴 touch theme（頁快取鍵
  ——步 2 紅字前提）；storage 目錄不可變（15a 紅線）。
- 橋訊息 schema：{type:"cl:highlight"|"cl:select", id}——擴充時保持 type 前綴
  cl: 並兩端同步。
