# handoff 2026-08-23 — 商品模塊手冊全譜研究＋流程實測＋多語言方案（裁定落檔）

本輪 worklog：
- `docs/worklog/2026-08-23-商品模塊手冊全譜與流程實測.md`

## ① 我改了什麼

1. 六冊 help 來源分冊 `docs/research/92-sources/`（a–f，約 130+ 頁英文版逐頁取證，全帶 URL＋日期）。
2. `docs/research/93-product-flows-live-walkthrough.md`：親手走完多變體建立→變體子頁→庫存調整（雙模式＋七原因）→調整記錄→複製→封存→刪除，§8 修正表 W1–W8。
3. `docs/plans/2026-08-23-多語言方案.md`：需求 R1–R8 對照 67 號、業務邏輯、四張表、API、介面語言包、編輯頁 UI、設定頁、ML-0～ML-6 包切分；**使用者裁定 C1–C7 已落 §10**。

## ② 為什麼這樣改（含被推翻的假設）

- 使用者 2026-08-23 連續三道指令：手冊全譜研究＋全流程親手操作（全權授權）→ 後台切英文、數據英文為主、**多語言提前啟動**（五語、SHOPLINE 內嵌形態、Collection 同步、語言可動態增刪）。
- **被推翻**：我原以為多語言要從規格寫起——實際 67 號已有 1,649 行裁定，與新指令**幾乎逐條吻合**（handle＝D-67-H1、語言是資料、兩層語言、堆疊/分頁判準）。只需修訂一處（建立態可填多語，C1）。教訓：新需求先 grep 規格再設計，這次省下整份重寫。
- **被推翻**：C2 原擔心既有中文資料遷移——使用者說明 demo 無真實資料、正式資料以英文為主且本來多語 ⇒ 遷移邏輯不做。
- C6：語言包由我方在開發端翻譯完成再部署，**不接外部機翻 provider**——ML-5 砍掉機翻草稿，只留離線繁簡轉換＋DNT 詞彙表。
- 92 主檔合成 workflow 撞 session 上限中斷（資料層六冊已安全落庫）。

## ③ 還有什麼沒解決

- 92 主檔（差距總表合成層）待 `resumeFromRunId: wf_a6d592bc-990` 補跑；補跑前的 P2 清單以 91 §18＋93 §8 為準。
- 多語言 ML-0～ML-6 全部未開工（下一步＝ML-0 基礎表）。
- D-PS6（建立固定草稿）依 C7 作廢，需在 `docs/dev/m1-product-set-foundation.md` 補註（ML-2 改建立頁時一併）。
- SEO/GEO 前台落地（G12）仍在 storefront 包。

## ④ 下一個人要注意什麼

- **來源語言＝en**：base row 一律英文；測試時別再用中文標題建商品（會變成「英文版是中文」）。測試店現有中文測試品（9907126370539）可改英文標題或刪。
- `platform_locales` 豁免 `shop_id` 必須同 PR 改 CLAUDE.md 鐵律 2 白名單＋71 §A G24（配套條款③），否則 `check-tenant-isolation` 會擋。
- 翻譯表**不得**做 per-locale JSON blob（67 §E.2-1 硬規則 1）；兩種 UI 模式都寫同一張 translations 列。
- 建立頁依 C7 跟隨本尊：加狀態卡、預設啟用；API 語義「未帶 status ⇒ DRAFT」不變，UI 恆顯式送。
- worklog/handoff 裡的檔案路徑一律寫完整存在路徑（doc-claims lint R1），不得用 glob。
