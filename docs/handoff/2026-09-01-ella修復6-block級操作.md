# Handoff：Ella 修復 PR-6 block 級操作（2026-09-01）

## ①我改了什麼

editor 軸 critical ②。base＝PR-5 之上（合併後 cherry-pick 上新 main）、
分支 `ella/editor-blocks`。逐檔＝worklog Changes。

## ②為什麼這樣改

- add-block 白名單＝schema blocks 定義本身（presets 只管 picker——24 §2.4
  取證）；@theme 展開全集與本尊一致。
- block ops 重用 applyOp(band)：undo/redo 免費獲得、無新棧。

## ③還有什麼沒解決

- block 巢狀第二層／block 眼睛（PR-8）；即時預覽（PR-7）。

## ④下一個人要注意什麼

- 選中三元組（band, sectionId, blockId）——新增面板分支先判 blockId
  再判 section（面板分支序）。
- BOOTSTRAP mock 的 sectionSchemas["blocks-demo"] 有 blocks def——動 mock
  注意 ED12 依賴 default "P"。
