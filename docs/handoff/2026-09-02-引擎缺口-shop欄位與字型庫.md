# Handoff：引擎缺口 PR-5——Shop 欄位＋字型庫家族（2026-09-02）

> 工作包＝分支 `engine/shop-fonts-5`（自 PR-4 長出，PR-4 squash 後 `git rebase --onto origin/main <PR-4 head>`）。
> 依鐵律 21 四段。配對 worklog：`docs/worklog/2026-09-02-引擎缺口-shop欄位與字型庫.md`。
> 本包是 `docs/handoff/2026-09-02-主題無關conformance與真店切換.md` §④E 排定的第 5 包。

## ① 我改了什麼
- `shop.money_with_currency_format`／`description`／`features`；字型庫 `library` 段（家族清單以
  `config/storefront_fonts.yml` 該段為準）＋handle `o` 字元＋顯示名 titleize。驗證：H1–H6 綠（H6 Kalles 真主題）；四個突變各自轉紅；
  font_pipeline 回歸綠（F4 樣本鍵改為永不入表）；rubocop 綠。

## ② 為什麼這樣改
- 字型缺口的實害是**顯示名錯**（"Roboto" 取代 "Roboto Condensed" 進 CSS `font-family`）與 system?=true
  讓主題走 system 分支；先把官方表列登記進 registry，檔案另補——比等 woff2 才修更早止血。
- money_with_currency_format 用官方 filter 例（`$10.00 CAD`）反推形，而不是猜「HK$ 前後」。
- 被推翻的假設：「helvetica_n4 是未知 handle」（既有 F4 樣本）——官方表列有 Helvetica（deprecated），
  且 Kalles 預設就是它。

## ③ 還有什麼沒解決
- library 家族無 woff2（視覺退 fallback）、`shop.features` 值形未取得、`shop.description` 無欄——見 worklog Pending。
- 真店 Publish／金標本仍待（前幾份 handoff §③）。

## ④ 下一個人要注意什麼
- 下一包＝§④E 第 6 包（filters：sort_by／within／link_to_*tag／default_pagination／color_to_hsl／md5）——
  spec 已寫好暫存於 session scratchpad `filters_gap_spec.rb.pending`（未實作，全套 rspec 時先移出）。
- 補字型檔時：家族從 `library` 搬進 `families`（同 key），`variants` 用 `{weight, style, file}` 形，
  `font_pipeline_spec` F3 有「實體檔存在」斷言可照抄。
- `FontLibrary::STYLES.key(style)` 反查用在 `variant`——傳入 style 一律字串。
