# 2026-09-03 主題編輯器 E3b 交接：與本尊並排對照後的修正（D79）

## ① 我改了什麼

- 目標：E3 合併部署後，使用者並排本尊與 demo 店截圖點名差異（左樹排位、預覽、整體 UI 要百分百複製）；本包逐項查實
  並修：群組帶漏兩個（liquid 標籤行內形）、`t:` 實例名外露、佔位插圖只有色塊、demo 店無主選單、左樹尺寸與本尊 1:1
  量測不符。
- 輸入：main `c57b50b7`（E3 已合併）；分支 `editor/e3b-parity-fixes`。
- 改動：見 `docs/worklog/2026-09-03-主題編輯器E3b-對照修正.md` Changes 表。
- 驗證（本機，2026-09-03）：`pnpm -s typecheck`／`pnpm -s test`／`bin/rubocop`（全量）／`bin/brakeman --no-pager
  --exit-on-warn --exit-on-error`／`ruby scripts/check-tokens-sync.rb`／`bundle exec rspec spec/requests/
  theme_editor_bootstrap_spec.rb spec/liquid` 皆退出 0；`ruby scripts/check-doc-claims.rb --base origin/main
  --require-base` 於文檔提交後跑（結果補記於本節末）。
- 突變五格（M18–M22）各自轉紅（worklog 表）。
- 未動真店；本尊副本主題只做點選／開 picker（未儲存、未 Publish）。

## ② 為什麼這樣改

- 使用者裁定（D79＋2026-09-03 重申「完全百分百的複製」）。本尊分頁可見時截到 1:1 截圖（視窗 1573@125% ⇒ 截圖≈設計
  px），量測值取代 E1 §8 的 ±2px 估值；E1 §2.2「選中＝藍底白字」被今日截圖推翻（選中＝淺灰底），依鐵律 19.5 在 100
  原處加日期更正。
- 佔位插圖自繪：鐵律 9 不抄本尊圖片資產；class／viewBox／preserveAspectRatio 規則取自 hoko.vip 實測與官方文檔逐字。
- `nameTranslations` 只回實際出現的鍵：完整 schema locale 檔很大（Ella 數百 KB），且實例名只會用到 `names.*` 少數鍵。
- 被推翻的假設：①「群組只會用 `{% sections %}` 引入」——Ella 用 liquid 標籤行內形；②「本尊選中列是藍底」——是淺灰；
  ③「本尊左樹字級 12–13px」——量測 14px/20px；④「hover／右鍵可用擴充功能合成事件量測」——iframe 不吃，未取得。

## ③ 還有什麼沒解決

- worklog Pending 逐項：`preserveAspectRatio` 逐名值、插圖逐像素、右鍵／hover／右欄量測、帳號 icon、demo logo、
  滿版差異。
- bt3 部署後要跑 `SEED_DEMO_SHOP=1 bin/rails db:seed` 補 main-menu，並請使用者切到 demo 分頁讓我截圖複驗。

## ④ 下一個人要注意什麼

- 量測法：本尊 admin 分頁在使用者 Chrome 是 125% 縮放（DPR 2.1875＝1.75×1.25）；`computer screenshot` 的 1254px 寬
  截圖 ≈ 1:1 設計 px（1573／1254÷1.25）。分頁必須在前景（visibilityState visible）才截得到，否則整張灰。
- E4 動手前先量右欄（需視窗 ≥1400 設計 px 才出右欄）；picker（E5）今日已截到形態：貼左欄右緣、與該列同高、寬≈250
  清單＋灰預覽欄、Sections|Apps 分段、Generate、"(1/1)" 灰化、插入線藍 2px＋⊕。
- 不要把 Polaris token 值寫成「我方 tokens」以外的第二來源：`app/assets/tokens.css` 已是本尊量測值，E3b 只用既有 token。
- 真店副本 theme 143506604135 仍可開來對表，不要 Publish。
- 停止條件：改鐵律本文、產生費用、對外發信、動真店現行主題——停下來問。
