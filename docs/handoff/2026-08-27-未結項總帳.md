# 2026-08-27 本輪未結項總帳（逐條核對現況）

> 產生於 2026-08-27，main＝`328afa9`（S6a-2 合併後）。
> 🔴 **本檔是終態層**：它必須等於 HEAD 的事實。各包 worklog／handoff 的 Pending 段是
> **歷史層**（寫下當刻的認知，依 AGENTS.md §文檔分層**不回頭改**）——兩者不一致時**以本檔為準**。
> 來源＝本輪五個包（PR #153／#154／#155／#156／#157）的 Pending 段全量抽取。

---

## A. 已解決（本輪內結案，附證據）

| # | 原登記 | 現況 | 證據 |
|---|---|---|---|
| A1 | 關卡②③（後台實測＋抓包）未取得（Chrome 未登入） | ✅ **已補做** | 使用者登入後兩輪實測，全文＝`docs/dev/m2-publication-scheduling.md` §11／§12 |
| A2 | PR-C 線上驗證尚未執行 | ✅ **已做，且抓到真東西** | `backfill scanned=3 bumped=1`（正式庫真有一列 stamp 落後）；重跑 `bumped=0` |
| A3 | 關卡④⑤研究代理執行中 | ✅ **已併入** | `m2-publication-scheduling.md` §9（外部參考）／§10（dev API） |
| A4 | 本尊到點行為實測進行中 | ✅ **已完成並推翻 D53 兩處** | §11；D53 已加日期更正註記（PR #155） |
| A5 | `ARCHIVED` 到點行為未實測 | ✅ **已補測結案** | §12；兩次獨立實測＝與 DRAFT 同為 consume-and-drop ⇒ 我方 fail-closed **從 ours 推定升格為實測證實** |
| A6 | `published_at` 語義變更後未重跑線上驗證 | ✅ **已做** | 三條路徑逐一證實：①合格覆寫 ②不合格清 NULL ③補發布 `republished=1` |
| A7 | S6a／S6a-2 未做線上驗證 | ✅ **已做** | 正式庫實跑 GraphQL，三個商品的 `status`／`purchasable`／`discoverable`／`resourcePublicationsV2` 全部一致（見 §D 的複驗指令） |
| A8 | 正式庫 backfill 那 3 列來源未逐列追查 | ✅ **已追查** | 見下方 §B |
| A9 | `STATUS_DIMENSIONS` 禁令變成「活的」 | ✅ **已緩解** | S6a-2 由伺服器出 `purchasable`／`discoverable`；該表降級為 fallback（建立態／未儲存的狀態改動） |

### §A8 的逐列結果（正式庫，2026-08-27）

`backfill scanned=3` ＝ `resource_publications` 中 `publishable_type="Product"` 且
`published_at <= now` 的三列（另有 1 列 Collection、3 列 ProductVariant 依設計不掃）：

| 列 | status | stamp 落後？ | 處置 |
|---|---|---|---|
| `id=1` Product/1 | draft | 否（stamp 已在 published_at 之後） | 不補 |
| `id=2` Product/2 | active | 否（**stamp 恰等於 published_at**） | 🔴 **這就是當初 `bumped=1` 的那一列**——`at: row.published_at` 的簽名 |
| `id=3` Product/3 | draft | **是**（落後 3 天） | **正確地不補**（status 不合格，等它改回可購買時由 `status_transition` 路徑 bump） |

⇒ `scanned=3 bumped=1` 完全解釋清楚，無殘留疑點。

---

## B. 仍未解決，且**需要使用者裁定**（兩條）

| # | 項目 | 我的建議 | 阻塞什麼 |
|---|---|---|---|
| **B1** | **D53 的 Quartz 術語錨**：D53 F2 寫「業界術語＝Quartz `MISFIRE_INSTRUCTION_DO_NOTHING`」，但 Quartz 官方對 misfire 的逐字射程只涵蓋 scheduler 關機與 thread pool 無執行緒，**不涵蓋業務前置條件不成立**；且我方架構下事件永遠準時觸發 ⇒ 嚴格說兩者都不是 misfire | 換成 **K8s `concurrencyPolicy: Forbid`**（三句與 F1 三句逐句同側），或改寫成「無精確業界對位物」 | 不阻塞任何實作；只是 D53 的用詞精確度。證據＝`m2-publication-scheduling.md` §9.1 |
| **B2** | **測試店留下五個測試商品**：`D53-QA 9913006162155`／`D53-QB 9913007767787`／`D53-QC 9913009438955`／`D53-QD 9913120653547`／`D53-QE 9913130418411` | 建議**保留 QA/QB/QC**（它們是「排程到點後的三種終態」的活體樣本，S6b 用得到）、**刪掉 QD/QE**（ARCHIVED 實驗品，已結案） | 不阻塞；但測試店會越積越多 |

🔴 指定保留的兩個 fixture（`9907126370539`／`9911273160939`）**全程未被觸及**。

---

## C. 已確認**不需要補**（附理由，避免下一輪重開）

| # | 原登記 | 結論 |
|---|---|---|
| C1 | 「`Publications::Lookup` 的顯式 `with_tenant` 那一層沒有 spec 覆蓋」 | ✅ **不需要在本路徑補**。`ChillloveSchema.object_from_id` 在欄位解析**之前**就需要租戶（實測：無租戶直呼會 `ActsAsTenant::Errors::NoTenantSet`）⇒ 該層在 `product(id:)` 這條路徑上**結構性不可達**，寫 spec 只會測到一個測不到的東西。它是給**其他呼叫端**（mutations）的縱深防禦。⚠️ 順帶登記既有守衛的射程缺口：`mutation_context_shop_spec` 的靜態掃描只抓字面 `Current.shop`，**抓不到「靠 `acts_as_tenant` 隱式租戶」的寫法**——擴張它屬 `scripts/`／既有 spec 範圍（鐵律 20.5 不擴修），登記待裁定 |
| C2 | 「backfill 只掃 `publishable_type = "Product"`」 | ✅ **設計如此**。變體因 R12 不可能有排程；Collection 無 status 軸。若日後 R12 放寬才要改，已寫進該服務註釋 |
| C3 | 「補發布在我方是非同步（outbox），本尊是存檔同步」 | ✅ **已登記為架構差異，不是行為差異**。延遲上界≈一個 relay 輪詢間隔（production 5 秒） |

---

## D. 屬未來包（已登記，本輪確認不做）

| # | 項目 | 去處 |
|---|---|---|
| D1 | 🔴 **`publications_updated_at` 仍無讀取端**——stamp 有寫入面沒消費面，八個讀取面全部空掛或未來包 | **S7／S8**。🔴 **不得把「stamp 已 bump」講成「快取已失效」** |
| D2 | S6b（商品 modal 編輯面）／S6c（系列 popover）／S6d（列表唯讀 popover） | **下一步**；S6c 前要先做 **popover 原語**（現有三份 inline 實作關閉語義各異、都缺點外關閉；列表 popover 還會被表格容器裁切 ⇒ 走 `Modal` 的 portal 解法） |
| D3 | 排程時間未換算店鋪時區（現用瀏覽器 locale） | **S6b**（本尊實測輸入框右側常駐 `GMT+8`） |
| D4 | 兩維欄位在列表上會是 N+1 | 列表要顯示時才做批次解析 |
| D5 | 兩維答案是**上界**（第三層 catalog 未進判準） | **S10**；落地後要回頭複驗，期望值收窄**不是回歸** |
| D6 | §7.2 三條新事實（UNLISTED 不可發布到第三方管道／平台側 `Suspended`+`Pending suspension`／channel markets 的 catalog 閘門） | **S10** 與未來包 |
| D7 | §9.4 七條跨模組缺口（`collections` 缺 stamp 欄／stamp 兩個時鐘來源／智慧系列未訂閱本 topic／sitemap 等四模組零實作／第三層 catalog 恆真／`Publishable` 三個 field 未實作／平台後台凍結租戶無機制） | **S6–S10／M8**，逐條已在 §9.4 標明去處 |

---

## §D 複驗指令（本檔全部「已做」宣稱的驗法）

```bash
# A7：正式庫實跑 GraphQL（三個商品的兩維與發布列）
ssh bt3 'sudo -u chilllove -H env -i PATH="/opt/rbenv/shims:/opt/rbenv/bin:/usr/bin:/bin" \
  HOME=/home/chilllove RBENV_ROOT=/opt/rbenv bash -c \
  "cd /www/wwwroot/chilllove/app; set -a; . /etc/chilllove/env; set +a; exec bin/rails runner tmp/<腳本>"'

# A8：backfill 冪等（跑完應為 bumped=0）
bin/rails publications:backfill_scheduled_stamps

# C1：確認 object_from_id 需要租戶（無租戶直呼會 NoTenantSet）
bundle exec rspec spec/graphql/product_visibility_fields_spec.rb
```
