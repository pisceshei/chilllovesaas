# handoff：第 22 包 PR

## ① 我改了什麼
VariantSync 引擎（兩階段 diff）＋SaveProduct 接線＋GraphQL input/enum 面＋
五語 locale＋5 例 spec（63:1141 靈魂測試）。輸入＝整合規格 §1.1/§4-22＋63 §B.4/B.5。

## ② 為什麼這樣改（含被推翻的假設）
- 「Rollback 例外可以把 userErrors 帶出去」被推翻——它被 transaction 靜默吞掉，
  SaveProduct 會回成功而資料已滾（自查抓到，未上線）。改自訂例外鏈。
- 「digest 匹配可以直接用原座標」＝63:311-316 明文的事故形態——注入回歸證明
  跳過階段 A 時 63:1141 立即紅（id 被換）。
- 「三策略 enum 要掛 productSet args」不成立：productSet 宣告式語義自含策略
  （未列出即刪除），enum 曝露是 schema 契約先定、mutation 後補（B2/B5 並存不矛盾）。

## ③ 還有什麼沒解決
sync_options! 的 position 兩階段落位未套（23 拖曳前必補）；值改名＝刪＋加的
UI 級語義（productOption* 後補）；worklog Pending 四坑逐條。

## ④ 下一個人要注意什麼
1. 動變體寫入只准經 SaveProduct→VariantSync；階段 A 投影不可跳（注入回歸釘住）。
2. spec 頂層常數一律帶檔案前綴（跨檔污染坑④）。
3. initialQuantities 的衍生冪等鍵形＝`{productSet 鍵}:init:{variant_id}`——
   重試同 key 的 productSet 會撞 Adjust 的 key 已用 userError（D44 語義），
   屬預期 fail-closed，不是 bug。
