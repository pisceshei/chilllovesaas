# Handoff：Ella 修復 PR-17（2026-09-02）

## ①我改了什麼

編輯器預覽的 block 級錨點：橋 click 上報 blockId、highlight 縮到 block
元素；前端訊息反選直開 block 面板、樹選 block 高亮帶 blockId。
逐檔＝worklog。

## ②為什麼這樣改

- attr 載體（data-shopify-editor-block JSON）PR-6 已埋，本包只補「橋認得它」
  ——官方 shopify_attributes 的存在理由就是編輯器 JS 錨點。
- highlight 無匹配退 section 級：draft 換頁後 block 元素可能還沒渲染，
  fail-open 到 section 比空白高亮好。

## ③還有什麼沒解決

- hover 浮動工具列／拖曳重排（⑤e）；巢狀 block 點選命中最內層＝V。

## ④下一個人要注意什麼

- highlight 訊息形自 PR-17 起是 `{type, id, blockId}`——測 postMessage 的
  精確物件斷言要帶 blockId（ED2 已改；新格照抄 ED20）。
- 橋內 JSON.parse 全部 try/catch——主題可能自帶同名 attr 的殘缺值，
  別拿掉防護。
