# Handoff — S6c 系列管道 popover（2026-08-28）

工作包＝S6c（分步方案 §S6 的第三種 affordance：系列＝輕量 popover）。
配對 worklog：`docs/worklog/2026-08-28-S6c系列管道popover.md`。
輸入 ref：`main` = `35d1712`（D70）。分支 `m2/d70-topbar-hover` 之後的 `m2/s6c-collection-channels`。

## ① 我改了什麼

按鐵律 12 全六層對本尊系列詳情頁的管道 popover 完整實測（82 §17，含 14 抓包與 help 雙源、
寫入全還原），然後落地 `CollectionChannelsControl`：觸發鈕＋popover＋三態總開關＋
排程入口，delta 進 SaveBar dirty，Save 套 publishablePublish/Unpublish 後重讀。
測試 11/11 綠（新增 4 格）。

## ② 為什麼這樣改

- 抓包證實本尊底層就是 `publishableUnpublish`（S5 同名）⇒ 零 schema 改動。
- 表單級 dirty 是實測出的核心語義（不是即時 mutation）——help 的 Save 步驟互證。
- 共用機制 export 而不複製：`serverScheduleOf` 判準有紅線（S6b handoff），
  複製一份＝設計與實作各說各話的 C-2 形態。

## ③ 還有什麼沒解決

worklog Pending 全列。最大三項：層④ CSS 逐 px 對齊（另一包）、Add 方向 variables（V）、
新建表單形態（V，我方 isNew 隱藏）。

## ④ 下一個人要注意什麼

- 🔴 **不要把 popover 的 toggle 改成即時 mutation**——本尊實測＋help 雙源都是表單級。
- 🔴 **ProductDetailPage 的 export 是共用契約**：改 `serverScheduleOf`／`sameDelta`
  兩邊一起變，動之前讀 S6b handoff 的紅線。
- 🔴 測試查 popover 用 `getByRole("group", { name: "銷售管道" })`——Popover 原語是
  role=group 不是 dialog。
- 下一步候選：S7（狀態機交互）或 PR-C（五格已裁定）；列表行內唯讀 popover（§9.3 第三列）
  是 S6c 的自然延伸，值域已在 82 §9.3。
