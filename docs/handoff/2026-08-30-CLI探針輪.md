# handoff — CLI 探針輪（2026-08-30 晚）

## ① 我改了什麼
真引擎探針（S9-Probe #166056231147）＋pull 普查；兩處 drop 分歧修正＋spec；
83 §12 新章。輸入 ref：main=55e47d7。

## ② 為什麼這樣改
#200 的未取證格需真引擎對表；使用者授權 CLI。0 fallback 與 kg 都是量測值
（83 §12 表），不是猜測。

## ③ 還有什麼沒解決
三個殘餘未取證格（worklog Pending）；json-filter parity 兩項。

## ④ 下一個人要注意什麼
- CLI 已認證（本機 token）；探針主題別刪，那是可重用儀器；再量時
  `?preview_theme_id=166056231147` 疊在任何商品頁上即可（要 View store
  session cookie，約 10–20 分鐘過期）。
- live-ella 快照在本輪 scratchpad，session 清理後要重 pull。
