# Handoff — 2026-08-20：PR #61 第三輪驗收修復

## ① 我改了什麼

`CLAUDE.md` 17.3 已補齊無代行授權的非 18.3 PR：四條件齊後仍須通知使用者人工合併；
有授權但條件未齊時必須繼續驗收。所有尚未進 main 的 PR 都凍結依賴鏈，無依賴包可並行。

本輪 worklog：`docs/worklog/2026-08-20-PR61-第三輪驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：D32 開出代行通道後，17.3 只需保留 18.3 的人工停點。** D31／D32 都是具名、
  不可外推的授權；未獲授權的常規 PR 仍真實可達，也必須有人工通知與依賴鏈停點。
- **沒有照抄：四條件未齊就通知人工合併。** 鐵律 18.1 的四條件是所有合併通道共同門檻；
  缺條件時只能繼續驗收，不能改走人工按鈕規避。
- 原 `195823b` 的安全根是「未進 main 前不得開依賴工作」；本輪保留這個根，只縮窄合併通道。

## ③ 還有什麼沒解決

- PR #61 尚待本輪 commit、push 與 Claude／Codex current-head 雙清，最後仍由使用者人工合併。
- Codex 對 `716cb24` 已接單但尚未交 review，不能用 👀 或舊 `d8bd9557` review 代替。
- 評估器文案與 `docs/dev/m0-automation-infra.md` 的 D32 通道缺口仍只在 `91` §3.5 登記。
- 其餘 §3.5 範圍外條目未擴修。

## ④ 下一個人要注意什麼

1. 四條件是所有合併通道的共同前提；「人工」不是缺條件時的逃生門。
2. D31／D32 只決定四條件齊後誰執行合併，不改變「依賴鏈等實際進 main」的不變量。
3. Codex 若晚到，必須用 review 的 `commit_id`／`original_commit_id` 與建立時間區分新意見，
   不能只看 inline comment 的可重定位 `commit_id`。
4. PR #61 無論四條件是否齊都不得代行合併；完成後通知使用者人工合併。
