# Handoff：G2 步 12a 系列線（2026-09-01）

## ①我改了什麼

20 步方案步 12 前半（系列線）。base＝main `53be21a`，分支
`g2/step12a-collections-paginate-view`。/collections 清單＋collections/all_products
全域＋collection.products 真分頁＋/collections/all＋?view=。逐檔＝worklog Changes；
取證正典＝`docs/research/96-storefront-g2-teardown.md`；防線＝
`docs/dev/g6-storefront-g2.md` §2。

## ②為什麼這樣改

- 三源方法先行（使用者 2026-09-01 裁定）：官方文檔（agent 深潛逐字）＋真店親點
  （chill.deals）＋live payload 先落 96 號，實作全部對錨。
- ?view= 不存在 suffix 的 fallback＝真店實測（200 渲染預設，非 404）——不是猜。
- handle 查詢走 liquid_method_missing 不覆寫 `[]`：Drop#key? 恆 true 的派發劫持
  是本輪紅測實錘（dev doc §3），被推翻的初版假設「覆寫 [] 即可」。
- 商品格射程＝discoverable 不是 purchasable：模型正典（UNLISTED 官方定義）。

## ③還有什麼沒解決

- 12b 搜尋線（/search＋suggest.json＋section 形＋recommendations 雙形）未動工
  ——96 §3–§5 取證已齊，直接照錨實作。
- 91 §3.60 五 ⚪；生產煙測待部署。

## ④下一個人要注意什麼

- 🔴 新 drop 要 handle/鍵查詢一律 liquid_method_missing；覆寫 `[]` 必劫持屬性派發。
- paginate parts URL 必須帶 locale 前綴（registers request_path 已組好，直接用）。
- 12b 的 suggest.json 金額＝十進位字串、recommendations＝整數分（96 §4.2/§5
  ——兩個出口不得共用序列化）。
- Ella header 預測搜尋打的是 section 形 `/search/suggest?...&section_id=…`
  （96 §4.3）——12b 兩形都要，只做 .json 版 demo 頭搜尋仍然壞。
