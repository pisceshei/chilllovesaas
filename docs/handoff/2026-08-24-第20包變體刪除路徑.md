# handoff：第 20 包 PR

## ① 我改了什麼
B1 方案②全量落地（migration＋DeleteVariant＋model 不變量＋釘板 v2＋五語 locale）。
輸入＝整合規格 §1.1／§4-20＋schema 實測（line_items.product_variant_id 本來可空）。

## ② 為什麼這樣改（含被推翻的假設）
- 「先置 NULL 再 destroy 就能繞過 dependent: :destroy」被 rspec 推翻——連鎖走記憶體
  快取關聯物件。正解＝拿掉 dependent、改 before_destroy 孤兒化（全域不變量，
  product.destroy 連鎖同樣安全）。
- 「釘板測試是回歸」被測試自己的註釋推翻——它明寫「第 20 包改變行為時它會轉紅，
  逼人回來讀這段」，照其指示改寫成 v2。

## ③ 還有什麼沒解決
對外刪除 mutation（22 的 diff 是首個呼叫端）；productDelete 服務層；孤兒 item
清理政策——三者登記於 worklog Pending。

## ④ 下一個人要注意什麼
1. 刪變體只准走 `Catalog::DeleteVariant`；guard 引 limits 不硬編。
2. 孤兒 item 判別式＝`product_variant_id IS NULL`（variant_deleted_at 是稽核欄不是判別鍵）。
3. 第 22 包 diff 的刪除分支呼叫本 service 時，事件由 SaveProduct 統一發——service 不自發。
