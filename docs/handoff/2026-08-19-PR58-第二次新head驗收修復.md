# Handoff — 2026-08-19：PR #58 第二次新 head 驗收修復

## ① 我改了什麼

處理 Codex review `4971515577` 的三條 P1與 Claude R30 comment `5341520727` 的一條 🔴／兩條 🟡：
#60 固定 commit 的可取得性、workflow validation 外部全稱句、P-0 終態 Changes 表同步、
SHOPLINE 試用期來源、§6.5① 歷史層保真。

本輪 worklog：`docs/worklog/2026-08-19-PR58-第二次新head驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **不是把 `eec1cfa` 換成漂移 branch head。** 2026-08-19 實跑
  `git merge-base --is-ancestor eec1cfa pr60-last-push` 回 0；缺的是接手環境先抓 tag，不是固定 SHA 錯誤。
- **被推翻：搜尋未找到就能寫「官方未公開」。** Anthropic 官方 client 固定 commit
  `65b50df0838d26293e18bb62f71492d3c11dc82d` 只證得逐字
  `WORKFLOW_VALIDATION_ERROR_CODES = new Set(["workflow_not_found_on_default_branch"])`；
  它沒有證明其他官方材料不存在。來源（取證 2026-08-19）：
  <https://github.com/anthropics/claude-code-action/blob/65b50df0838d26293e18bb62f71492d3c11dc82d/src/github/token.ts>。
- **既有終態表仍是主索引。** 新 worklog 不取代 P-0 表；凡本輪改到總方案或全專案 handoff，對應列都要回寫。
- **沒有採用「把主表降級成 aa7ef7a 快照」。** `AGENTS.md` §6 明定驗收回應須在同一 commit 回寫終態層；因此補最終 diff 缺列並更新既有四列。
- **SHOPLINE 只作輔助，不代表環境到期事實可無來源。** 官方頁仍明載試用自註冊起算、逾期會 pause；所以補證據但不恢復 SHOPLINE 建單。
- **§6.5① 是歷史層。** 正確模式是原文保留後加更正，不是以現值整段覆寫當刻認知。

## ③ 還有什麼沒解決

- 新 commit 尚待 Claude 與 Codex 重新驗收。
- token-exchange endpoint 的完整伺服器判定沒有可引用規格；現階段只能保留〔推論〕與驗證路徑。

## ④ 下一個人要注意什麼

1. 在乾淨 clone 複驗 #60 前，先抓 `pr60-last-push`，不要假設 PR 分支物件已在本地。
2. 「公開 client 沒呈現」不等於「官方從未公開」；後者沒有直接證據時只能標〔推論〕。
3. 每輪新增 worklog／handoff 之外，若修改 P-0 終態表已索引的檔案，仍須同步主表對應列。
