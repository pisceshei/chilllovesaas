# Handoff — 鐵律 3 同步＋C2 `_psp_number`（2026-08-31；PR B／18.3 人工合併）

## ① 我改了什麼
branch `g6/iron-rule3-sync`，base＝main `939dfde`（#217 之後）。四檔：CLAUDE.md 鐵律 3
／check-money-boundary C2／test-money-rules forbidden 欄／c2 fixture 對照行。

## ② 為什麼這樣改
- D46 判例：規範本文＋scripts/ 改動獨立成 PR、人工合併——#217（65 全文＋代碼）刻意
  不含這兩類檔，本 PR 收尾。
- forbidden 欄：只斷言「[C2] 有命中」證明不了「合法後綴沒被誤擋」——白名單縮小這種
  回歸需要反向斷言才看得見（實測紅過）。
- 被推翻的假設：無。

## ③ 還有什麼沒解決
無——本包射程即兩處同步；理由＝G6-1 未動工前 C2 對 `_psp_number` fail-closed 只是
待解鎖，不是缺陷。

## ④ 下一個人要注意什麼
- 🔴 **等使用者人工合併**；合併前 G6-1 不得開工（adapter 的 `_psp_number` kwarg 會被
  main 上的舊 C2 打紅）。
- 重跑：`ruby scripts/check-money-boundary.rb`＋`ruby scripts/test-money-rules.rb`。
