# Handoff：G6 步 7 棄單挽回（2026-09-01）

## ①我改了什麼

20 步方案步 7。base＝main `2434205`，分支 `g6/step7-abandoned-checkouts`。
判定 job＋挽回端點＋手動寄送 mutation＋keyset 讀面＋admin 列表頁。
檔案清單＝worklog Changes；控件級＝`docs/dev/g6-abandoned-checkouts.md`。

## ②為什麼這樣改

- 10 分鐘閾值進 limits（鐵律 6）而非硬編碼；判定「最後活動」用 updated_at 近似
  （官方內部欄未公開＝未取得，誠實登記）。
- 挽回 URL 走我方單段 token 形而非仿本尊 `/ac/<t>/recover?key=`——本尊的 key 參數
  語義未取得，仿形＝貨物崇拜；recovery_token 已是 48 hex 高熵單因子。
- sent_at 回填放寄送後（漏記→可再寄 vs 記了沒寄→顧客收不到；選前者，
  DeliverJob 檔頭有方向論證）。

## ③還有什麼沒解決

- 自動排程寄送／詳情頁／Export＝⚪；V-237 未寄態控件形未實測。
- 抑制情形只做兩種（worklog Pending 末條）。
- 生產煙測待部署後做（建假棄單→標記→寄送鏈→recover 302→清理）。

## ④下一個人要注意什麼

- 🔴 routes 裡 recover 必須保持在 `checkouts/:token` 之前（MA3 家族；
  挪動位置會讓 "recover" 被當 token 吃掉且測試 R1 紅）。
- KeysetCursor 新排序鍵要在 ORDER_KEYS 登記（白名單 fail-closed；本步 abandoned_at
  就是先撞了 unknown order_key 才登記的）。
- 步 8 顧客模組的邀請信走同一通知鏈：Catalog 加 entry＋liquid 檔＋觸發點三件事。
