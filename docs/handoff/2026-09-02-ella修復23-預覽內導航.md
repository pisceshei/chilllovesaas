# Handoff：Ella 修復 PR-23（2026-09-02）

## ①我改了什麼

編輯器預覽內導航：橋攔站內 `<a>` ⇒ cl:navigate ⇒ 父頁換 admin 預覽 src
＋templateForPath 同步左欄模板。逐檔＝worklog。

## ②為什麼這樣改

- 逃逸的本質是 iframe 導去無 design_mode 的公開頁——修在橋層（攔）＋
  父頁層（重新進 admin 預覽端點）兩端，比在渲染層改寫全部 href 乾淨
  （href 改寫會汙染 cl:replace 片段與 SEO 快照面）。
- templateForPath 剝 locale 前綴是防禦性：admin 預覽渲染 url_prefix=""，
  正常不出前綴，但 draft_page/儲存後 reload 各路徑來源不齊。

## ③還有什麼沒解決

- form submit 逃逸；分享預覽連結；主題頁大卡。見 worklog Pending。

## ④下一個人要注意什麼

- 橋的連結攔截白名單＝`/` 開頭且非 `/admin/`——新增編輯器內部連結（如
  日後把預覽列塞進 iframe）記得走 /admin/ 前綴或 data-attr 豁免。
- templateForPath 是 export 的純函式——路由新增頁型（gift_card 等）在
  這裡補對映，測試直接單測它。
