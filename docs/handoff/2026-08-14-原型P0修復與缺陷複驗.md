# 交接：83 號 P0 複驗與修復 —— **查證比照做重要**（2026-08-14 第二十二場）

> 本輪 worklog（1 份）：`2026-08-14-原型P0四條修復.md`
> 產出：`docs/specs/89-prototype-defect-reverify.md`（缺陷複驗總表，行號為當日現值）

## ① 我改了什麼

1. **先查證再修**：對 83 號的四條 P0 各派一名 agent 按**內容**定位（不採信其行號），
   每條再配 2 名對抗式複核（預設立場「它已經被修好了」）。
   **8 次推翻嘗試全部失敗** ⇒ 四條全部仍成立。
2. **修了四條並實跑驗證**：T-01 請款四模式／O-01 退款軟上限／C-01 分群求值器／
   PR-01 商品批次選取（含匯出空指向）。對照表見 89 §6.1。
3. **另外掃出 25 條同類缺陷**（13 條死控件 ＋ 12 條假數字），**全部登記於 89 號，本輪未修**。

## ② 為什麼這樣改（含被推翻的假設）

- 🔴 **83 號的行號全部失效**。它寫於 2026-08-13 的另一個 session，原型此後改到 12,943 行。
  實測 `A:8136` 現在是目錄程式碼、`A:6553` 是庫存歷史。**照它的行號改會改錯地方。**
  ⇒ 89 號的存在理由就是這個：同一批發現，換成**當日現值行號**重新登記。
- 🔴 **我自己猜錯了一條，而且是用 grep 猜的**。啟動查證前我看到 `bulkbar` 有 19 處命中，
  在給使用者的訊息裡寫「PR-01 很可能已被修掉」。錯的——那 19 處屬於**其他 12 個模組**，
  商品列表正好是唯一沒有的。agent 的原話：「肉眼掃 grep 很容易誤判成已修好」。
  **這是本輪最該記住的一件事：grep 命中數不是證據。**
- **O-01 的形態比報告寫的還糟**：報告說「UI help 與 DOCS 雙錯」，實際是**三層各錯各的**——
  錯的規則被寫成文案（硬擋）、正確的規則一行都沒有（無軟上限）、
  **而它宣告的硬擋連 demo 都沒實作**（輸入 999999 照樣「成功」）。
- **C-01 的修法重點不是「把解析器寫對」，是「算不出來就明說」**。
  原本三條 regex 都不中就回全部顧客數——那是**錯誤數字不是佔位符**，長得像模像樣。
  新版無法求值時回 `ok:false`，UI 顯示「—」＋原因。
  🔴 **顯示一個算錯的人數，比顯示「—」危險得多。**
- **順帶修了一個沒人提的鐵律 3 違反**：`amount_spent > 5000` 的字面量是主單位、
  `c.spent` 是 cents，原本直接比較等於在問「消費超過 HK$50」。
  ⚠️ 因此**修好後的數字與 89 §1.3 所載「真值」不同**——是口徑被修正，不是算錯。
- **PR-01 刻意不共用訂單頁的 `.rowcb`**：本原型是 SPA、所有頁面同時在 DOM，
  共用 class 會讓 `bulk()` 把訂單列一起數進來。

## ③ 還有什麼沒解決

- 🔴 **`lcolumns`（89 §2 #4）未修，嚴重度不低於本次四條**：17 個清單頁的欄位設定
  checkbox 無 onchange、`c.cols` 不回寫，卻 toast「已存回目前檢視」。**假成功訊息比沒反應更糟。**
- 🔴 **89 §2 其餘 10 條死控件 ＋ §3 的 12 條假數字全部未修**。其中兩條直接踩鐵律 7：
  AOV 三處兩個值（且違反它自己 tooltip 的公式）、Live View 前三名商品加總比全店總額多 85%。
- **`segMatch()` 不支援括號巢狀**，而 UI 說明文案寫著支援——已判為不可求值（誠實地不做），
  但文案與能力仍不一致。
- **驗收閉環仍未被證實**（見下）。

## ④ 下一個人要注意什麼

1. 🔴 **本 PR 是驗收閉環的第一次真實驗證**。身分修正（`claude[bot]` 加進 `REVIEWERS`）
   已隨 #25 進 main，而本 PR **不碰 `claude-review.yml`**，所以這次會真的跑。
   判準兩條**都要成立**：①有 Claude 驗收留言 ②**沒有**「⚠️ 沒有產生結論」。
   **只有兩者都對，那條修正才算被證實**——在那之前它仍是未驗證的改動。
2. 🔴 **要修原型缺陷，先讀 89 不要讀 83**。83 的行號全數失效。
3. 🔴 **自動化點擊掃描前必須強制 `annot=false`**。「⌗ 註釋模式」開著時，
   `[data-doc]` 的 click 會被 `preventDefault(); stopPropagation()`，
   **inline onclick 根本不執行，掃出來全綠而且是假的**（89 §0）。
4. **驗互動行為要等渲染**：`openSegment()` → `goDetail()` 是非同步，
   同步讀 DOM 會拿到上一個分群的結果，看起來像「數字全錯」。
5. **lint 守得住「onclick 指向不存在的函式」（實測 0 條），但守不住「死控件」**——
   控件畫出來卻沒接 handler 那一類 **lint 沒有規則，25 條全部漏光**。
   要不要把它機制化，是下一個值得做的判斷（同 G24／撤銷命名那兩次的思路）。

---

## 追加：🎉 驗收閉環終於走通了（第四次嘗試）

**三次失敗、三種不同根因之後，2026-08-14 在 PR #26 上完整驗證通過。**

### 四次嘗試的完整帳

| # | PR | 現象 | 根因 | 處置 |
|---|---|---|---|---|
| 1 | #22／#23 | 每個 PR 都誤報「沒有產生結論」，但 Claude 明明貼了完整意見 | **作者身分不符**——解析步驟只認 `github-actions[bot]`，而驗收留言是 `claude[bot]` 貼的 | 允許清單加 `claude[bot]` |
| 2 | #25 | run 只跑 16–20 秒、Job 標 **success**、沒有任何 Claude 留言 | **防竄改機制**——PR 動到 `claude-review.yml` 就整個跳過（非 bug 是設計） | 記進 workflow 檔頭；改本檔的 PR 只能合併後驗證 |
| 3 | #26 首跑 | run 跑滿 3m20s、Job 標 **Failure**、沒有留言 | **`--max-turns 40` 用盡**——該 PR 動到 12,943 行的原型檔，光讀檔就燒光回合數 | 提到 200 ＋ 失敗歸因拆三種 |
| **4** | **#26 重跑** | ✅ **完整走通** | — | — |

### 第四次的完整鏈路（實測）

```
claude[bot]          →  【驗收結論】通過          ← Claude 真的貼出來了
github-actions[bot]  →  Claude 驗收通過 ✅（自動核准）  ← 🔴 解析器讀到了，身分修正證實有效
github-actions[bot]  →  ✅ 驗收通過。AUTO_MERGE 已關閉，請手動合併
```

判準兩條都成立：①有 Claude 驗收留言 ②本輪**沒有**產生新的「⚠️ 沒有產生結論」。
（頁面上仍看得到那句 ⚠️，那是 #22／#23 兩輪留下的**歷史留言**，不是本輪。）

驗收意見本身也是實質的——逐條點名四項修復，並確認了 Codex 抓到的
「我自己引入的重繪後選取遺失」已修好。

### 🔴 這件事的教訓

1. **同一個功能可以有多個互不相干的失敗形態，修好一個不代表它會動。**
   前三次每一次我都以為「這下總該好了」，每一次都是新的根因。
   **只有看到完整鏈路跑完，才算證實。**
2. **fail-safe 會掩蓋 fail-silent。** `AUTO_MERGE=false` 讓閉環失效得非常安靜——
   它從上線起就沒運作過，而沒有人發現。
3. **診斷訊息的精確度直接決定除錯速度。** 前兩次的「可能失敗或逾時」把人指去看 log
   卻沒說看哪一行，我自己因此多繞了一圈。現在拆成三種，且會印出實際作者／outcome。
4. 🔴 **回合數夠不夠取決於 diff 大小，不是取決於 workflow 有沒有寫對。**
   原型檔是本倉庫最大的單檔，任何碰它的 PR 都會壓到上限——這條不會因為改成 200 就消失，
   只是門檻提高了。再撞到時要考慮的是**縮小驗收範圍**，不是繼續加大。
