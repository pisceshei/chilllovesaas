# Handoff — 2026-08-19：PR #58 第七次新 head 驗收修復

## ① 我改了什麼

處理 GitHub Codex review `4973106464` 對 `e2c3573` 的三條 inline：全專案 handoff 兩處重建裁定錯錨、同檔實況表的三組行數無法重現，以及 `91` 兩個剩餘暫存條目的取證日期。

本輪 worklog：`docs/worklog/2026-08-19-PR58-第七次新head驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：`HANDOFF.md:126` 仍指向重建裁定。** 現行檔案的穩定內容錨命中第 5 行摘要與第 138 行裁定；歷史 handoff 不直接覆寫原句，故兩處都採原文保留加鄰行更正。
- **被推翻：實況表的數字只是隨 head 前進而自然腐化。** 初建 commit `f760118` 已是 `docs=79621 app=6053 scripts=4387`，reviewed head `e2c3573` 是 `docs=80331 app=6053 scripts=4387`；原列的 docs／scripts 數字在初建快照也不成立。
- **標題日期不能代替條目日期。** `91` §0.3 要求每項都有摘寫、來源錨與日期；R30／R31 與後續 Codex-scan 來源均發生於 2026-08-19，不能由 §3.4 的 2026-08-18 標題代填。
- **不擴修。** 日期只補 inline `3813761581` 明確點名的兩項，其餘條目未順手修改。

## ③ 還有什麼沒解決

- 新 commit 尚待 Claude 與 Codex 對精確 head 重新驗收。
- 全專案 handoff 保留了歷史錯錨與錯數字；讀者必須採用鄰近 HTML 更正中的現行錨與快照輸出。
- #60 的最終 rebase 與 workflow PR 人工裁定仍須在 #58 合併後處理。

## ④ 下一個人要注意什麼

1. 行號引用必須用內容錨複驗；歷史層即使行號錯，也要保留原句後追加更正。
2. 快照數字要綁 commit 重算；只在工作樹重跑 `find` 無法判斷原數字是老化還是從未成立。
3. `91` §3 每個暫存項的來源錨與日期都要落在該項本身，不能借用小節標題。
