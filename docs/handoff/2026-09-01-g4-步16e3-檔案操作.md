# Handoff：G4 步 16e3 檔案操作（2026-09-01）

## ①我改了什麼

20 步方案步 16 第八包（收步 16 主線）。base＝main `0295410`，分支
`g4/step16e3-file-ops`。themeFileDelete＋overlayState＋New file/revert UI。
逐檔＝worklog Changes。

## ②為什麼這樣改

- 刪 overlay 列一個 mutation 兩語義（還原/刪除）由 overlayState 標示——
  比兩支 mutation 少一個狀態面；base 檔真刪除需 tombstone（91 §3.71）不趕。
- templates/ 不給建新檔：建了也會被後端白名單拒——前端不給入口。

## ③還有什麼沒解決

- rename／tombstone／modal 化 prompt（91 §3.71-72）；生產煙測待部署。
- 步 16 殘餘豐富化見 91 §3.69-72；下一步依 20 步方案＝步 20 獨立件
  （webhookSubscriptions CRUD＋F5 壓測；步 17/18/19 仍卡鐵律 2 條款③）。

## ④下一個人要注意什麼

- themeFileDelete 對「被覆寫檔」＝還原不是刪——UI 文案別寫反。
- overlayState 用 Sources.base_resolve 對照 base——改 resolve 佈局兩處同步。
- 步 20 的 webhookSubscriptions：outbox 已有事件流（步 5/6），CRUD 面接
  28 號 API 契約。
