# Handoff — G6-1d 卡與錢包 SDK（2026-08-31）

## ① 我改了什麼
branch `g6/airwallex-card`，base＝main `566030f`。結帳頁新增 card/applepay/googlepay
三式（SDK 內嵌；QR 雙式不動）；Apple 網域驗證檔上線。清單見同名 worklog；
SDK/AP 網域契約逐字證據在 worklog＋limits 註。

## ② 為什麼這樣改
- confirm 結果不信客戶端：SDK promise resolve 只當導航訊號，成單入帳一律
  /pay/status server 權威重取（與 QR/webhook 同終點）——客戶端偽造 resolve 拿不到單。
- env 取 'demo'：三源矛盾下以可執行證據（quickstart＋CDN bundle enum）定值；Q10 釘
  production→'prod' 映射。
- 被推翻的假設：digest 記「wallet amount.value 是字串」——現行參考頁逐字 number
  （同頁 recurring 備註又用字串；本包送 number，登記張力）。

## ③ 還有什麼沒解決
worklog Pending 四項；核心＝卡付/錢包端到端（sandbox 或授權）。

## ④ 下一個人要注意什麼
- 🔴 正式環境卡片頁**不得輸入真卡**（真扣款）；視覺驗證只看 SDK iframe mount。
- Apple Pay 換發驗證檔＝重下載官方連結覆蓋 public/.well-known/ 同名檔（URL 會輪替，
  從 enable-apple-pay guide 頁取當期連結）。
- 重跑：`bundle exec rspec spec/requests/storefront_psp_payment_spec.rb`。
