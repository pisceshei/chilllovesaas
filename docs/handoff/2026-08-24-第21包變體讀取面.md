# handoff：第 21 包 PR

## ① 我改了什麼
cursor/connection 泛化（白名單＋方向參數化）＋變體 connection（position 序）＋
options/selectedOptions 讀取面＋前端同步。輸入＝整合規格 §4-21＋排程 §2.1③。

## ② 為什麼這樣改（含被推翻的假設）
- 「名帶 Connection 的自訂型別可直接當 field 型別」被推翻：graphql-ruby 對它自動
  注入分頁引數 ⇒ DuplicateNamesError。解＝`connection: false`（QueryType#products
  既有慣例，這次寫進 ProductType 註釋）。
- 「cursor 泛化必然破壞舊 cursor」不成立：預設鍵 payload 逐位元組同構＋spec 釘住。
- 反序 fixture（position 3→1 vs created_at 升序）是本包核心測試設計——排序鍵誤退
  created_at 時 2 條立即紅（注入回歸實證）。

## ③ 還有什麼沒解決
多變體 UI（第 23 包）；寫入契約（第 22 包）；各資源 keyset 實作的收斂（登記不做；現況查法 `grep -rln -i keyset app/graphql`）。

## ④ 下一個人要注意什麼
1. 新排序鍵必須登記 `KeysetCursor::ORDER_KEYS`（fail-closed）。
2. 變體讀取一律經 connection resolver（preload 在那裡）；直接摸
   `product.product_variants` 會繞過 N+1 守衛。
3. 前端送出 payload 的 `variants` 仍是 list 形（ProductSetInput）——只有查詢回應
   是 connection 形，兩者長得像但不是同一個東西。
