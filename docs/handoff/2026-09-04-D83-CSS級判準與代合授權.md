# Handoff：D83 渲染判準精確到 CSS 級別、Ella 原檔複製、代行合併授權（2026-09-04）

配對 worklog：`docs/worklog/2026-09-04-D83-CSS級判準與代合授權.md`。

## ① 我改了什麼

- 目標：把使用者 2026-09-04 的第二次渲染裁定（精確到 CSS 級別、已購主題原檔直接複製）與合併授權（含 18.3 清單）寫入規範。
  輸入 ref：main `6cc21f6e`（#315 鐵律 22／D82 之後）；分支 `rules/d83-merge-authorization-and-css-level`。
- 做了：CLAUDE.md 鐵律 22.1／22.3／沿革、17.3 例外覆寫、18.3 標題；AGENTS.md 前言與鐵律 22 摘要；DECISIONS D82 追加＋D83。純文字，無程式碼。
- 驗證：全閘門（見 worklog 閘門段）；doc-claims 兩支在 worklog／handoff 寫入後補跑。

## ② 為什麼這樣改

- 「精確到 CSS 級別」放在 22.1（射程）而非只在 22.2（驗收），因為它改的是「一樣」的定義，不只是量測方法。
- Ella 原檔複製放在 22.3（鐵律 8／9 的讀法段），因為它回答的是版權問題；鐵律 9 的「不得隨平台散布」讀法不動，避免被讀成放寬。
- 合併授權同時改 17.3（流程出口）與 18.3（清單標題），並在 AGENTS.md 前言同步——否則前言的「不自行合併」會與 CLAUDE.md 打架
  （鐵律 20.2.2 生產者／消費者同步）。仍明寫「不是 workflow 自動合併、AUTO_MERGE 維持 false」，避免與 18.4 混淆。

## ③ 還有什麼沒解決

- computed style 逐屬性 diff 的工具與憑證格式尚未建（下一包）；本機量測環境的 font-weight 注入污染要先消融。
- 代合授權的「合併後通知」形式：目前＝工作階段最終訊息＋memory 指標；未另建通知機制。

## ④ 下一個人要注意什麼

- 本 PR 本身是 18.3 PR（改 CLAUDE.md／AGENTS.md），依 D83 由工作階段在 CI 兩測綠後 `gh pr merge --squash --match-head-commit` 代合。
- 之後所有 PR：CI `quality`＋`test` 綠 ⇒ 直接合併 ⇒ 通知；不再停下等使用者確認。使用者收回授權時要同步改 D83／17.3／18.3／AGENTS 前言。
