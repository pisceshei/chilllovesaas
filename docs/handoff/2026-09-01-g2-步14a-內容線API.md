# Handoff：G2 步 14a 內容線 API（2026-09-01）

## ①我改了什麼

20 步方案步 14 第一包。base＝main `bdd53b0`，分支 `g2/step14a-content-admin`。
三表＋12 mutations＋queries＋SaveMenu。逐檔＝worklog Changes；取證＝98 號。

## ②為什麼這樣改

- article handle 唯一域＝(shop, blog)：Liquid 層 handle 是 blog/article 複合形
  （官方範例值），URL 空間天生按 blog 分域——全域唯一反而錯（MC3 紅證）。
- menuUpdate 整棵替換：官方語義（"replaces the entire menu"）；SaveMenu 交易內
  destroy_all＋重建，position＝陣列序。
- blogDelete 連文章刪＝官方沉默處的我方裁定（dependent: :destroy；98 §5-3）。
- comment_policy 預設 closed＝admin 實測 Disabled（不是 GraphQL enum 首值）。

## ③還有什麼沒解決

- 14b admin 三頁／14c 前台線（blogs、articles drops＋/blogs 路由＋tagged＋
  留言 POST＋sitemap blogs 分片）——98 §1/§2 取證已齊。
- 留言提交回應三層未取證（CAPTCHA 紅線）——98 §5-4 V 項，14c 實作時以我方形
  補測。

## ④下一個人要注意什麼

- S14 Probe Post（測試店 article 596366491883）＝長期 fixture 勿刪。
- 新增 MenuItemType 值：MenuItem 三個常數＋MenuItemKindType enum＋SaveMenu
  RESOURCE_MODELS/STATIC_TYPES＋（14c）LinkListsDrop 對映——四處同批。
- Menu::DEFAULT_HANDLES 的 footer 兩形（footer/footer-menu）＝本尊 handle 未逐字
  取證的保守寬收；取證後收斂。
