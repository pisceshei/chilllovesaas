# Handoff：Ella 修復 PR-14（2026-09-02）

## ①我改了什麼

圖片尺寸不變量三件套：StoredFile 條件驗證（image/*∧ready ⇒ 正整數尺寸）、
BackfillDimensions 存量回填服務（blob 缺失轉 failed）、ImageDrop nil 遙測；
7 個既有 spec 檔的 ready 建檔點補尺寸。逐檔＝worklog。

## ②為什麼這樣改

- 官方 image 物件三屬性恆 number（shopify.dev objects/image 2026-09-01）；
  「有圖無尺寸」在本尊不可表示，我方 nullable 欄讓它可表示 ⇒ Ella 的
  if-image 守衛內除法只在我方炸。修資料層不修 drop——drop 改回 0/佔位
  會翻轉全主題存在性守衛（艦隊軸②的反面教訓）。
- 首頁那兩條 `divided by 0` 刻意**不修**：空 image_picker 回 nil 是官方
  契約、主題無守衛除法本尊同樣出錯——修它反而破壞對位。

## ③還有什麼沒解決

- 生產回填 runner 執行（部署後一次，輸出補登 worklog）；
  search_controller image_aspect 同形（有守衛，登記）；worklog Pending 全列。

## ④下一個人要注意什麼

- 🔴 建 ready 圖片列（spec/seed/匯入器）必帶 width/height——驗證會擋，
  但錯誤訊息出現在離炸點很遠的 create 呼叫處，先讀本 handoff 再猜。
- 回歸判準不能只掃 HTML 'Liquid error'：assign 位除零被 Liquid 靜默吞
  （艦隊機制實錘）——要斷言關鍵輸出值（height/width 屬性非空）。
- BackfillDimensions backend 可插拔；本機/CI 無 libvips，真像素驗證
  在 bt3。
