# Handoff：引擎缺口 PR-9——系列 tag 路徑＋系列語境商品 URL（2026-09-03）

> 工作包＝分支 `engine/collection-tags-9`（自 PR-8 長出，PR-8 squash 後 `git rebase --onto origin/main <PR-8 head>`）。
> 依鐵律 21 四段。配對 worklog：`docs/worklog/2026-09-03-引擎缺口-系列tag路徑.md`。
> 本包是 `docs/handoff/2026-09-03-引擎缺口-filters.md` §④ 排定的下一批第 3 包（三個候選至此全部落地）。

## ① 我改了什麼
- 路由兩形（tag 段、系列語境商品）、CollectionDrop 的 tag 解析與當前檢視／全集分家、PageTitles 的 tagged-all 字串。
  驗證：CT1–CT7 綠；六個突變各自轉紅；collections／drops_gap／page_title／section_wrapper／filters／conformance 回歸綠；rubocop 綠。

## ② 為什麼這樣改
- 官方 Liquid 頁對 tag 路徑無逐字，證據來自 help 兩頁（路徑形＋「無此 tag 仍有效」＋多 tag AND）與真店 200／標題形。
- URL 段是 handle 形（link_to_add_tag 官方例 `extra-potent`）而商品 tag 可含大寫空白 ⇒ 用系列內實際 tag 做 handleize 對照，
  SQL 仍用 JSON_SEARCH 精確比對（不在 SQL 裡 handleize）。
- 被推翻的假設：「tagged /collections/all 的標題與 /collections/all 相同」——hoko 逐字是「产品」vs「商品」。

## ③ 還有什麼沒解決
- all_products_count 口徑、系列語境商品頁的 `collection` 物件、tagged-all 英文標題——見 worklog Pending。
- 真店產品無 tag（Tags 欄輸入未成功）；要驗 tag 命中形得先在 pnrjnw-sy 給產品加 tag。

## ④ 下一個人要注意什麼
- 三個候選包完成後，下一批應回到金標本逐頁 diff（`tools/theme-conformance/golden/*` vs 我方輸出）挑高分差異，
  以及矩陣 119 個 missing object（B2B／selling_plan／metaobject／order…）。
- 本輪已做的一份靜態 diff（Ella 金標本 `ella-7.2.0-products/index.html` 的 `<head>` vs demo 首頁，2026-09-03）給出的
  候選：①`window.Shopify` 形——金標本鍵集 `MCP／PaymentButton／SignInWithShop／cdnHost／country／currency／
  featureAssets／locale／routes／shop／shopJsCdnBaseUrl／theme`，且 `Shopify.currency` 是物件 `{"active":"HKD","rate":"1.0"}`、
  `Shopify.cdnHost = "hoko.vip/cdn"`、`Shopify.country = "TW"`；我方多出 `designMode = false`（本尊公開頁不出此鍵）與一批
  自家 helper 鍵；②`content_for_header` 平台注入——`<script id="shopify-features" type="application/json">`、
  `<script id="__st">`、`<meta id="shopify-digital-wallet">`、`shopify-y`／`shopify-s` meta、`captcha-bootstrap`、
  analytics（trekkie／ShopifyAnalytics）——我方全無（主題 JS 是否消費各項＝逐項查 fixture 再決定）；
  ③我方多出 `hreflang` alternate（本尊單語店不出）。複驗法：對兩份 HTML 取 `<head>` 內 meta／link／script 的屬性集合差。
- `CollectionProductsDrop.base_relation` 現在有第三個維度（tags）；Facets 計數基底也帶 tag（同一定義，別分家）。
