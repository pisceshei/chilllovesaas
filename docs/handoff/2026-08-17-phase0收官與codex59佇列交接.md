# Handoff — Phase 0 收官與 PR #52 Codex 59 條佇列（2026-08-17，跨 session 交接）

> 🔴 本檔是**給下一個 Claude Code session 的完整交接**。接手第一步：讀完本檔全文，
> 然後讀 `~/.claude/projects/C--Users-pisce-Downloads-shopifysystem/memory/MEMORY.md`
> （長期記憶索引——使用者鐵律都在裡面）。本檔在 #52 收官輪隨 commit 入庫前為
> untracked 狀態，屬刻意（不擴大審查面）。

## ① 現況快照（我改了什麼／進行到哪）

> 🔴 2026-08-17 時點錨（PR #55 入庫時加；本檔以下內容＝**寫成當刻（#52 第 17 輪後）
> 的歷史快照**，非 HEAD 現況）：#52 已於當日稍晚**第 26 輪雙零合併**（merge
> `a093dda`）、59 條佇列已全數處理、tag `pr52-last-push` 已刪（遠端＋本地）。
> **HEAD 現況一律以 `docs/handoff/2026-08-17-phase1開工與91骨架.md` 為準**；
> 本檔 §③「還有什麼沒解決」第 1 項與 §⑤ 佇列皆已完成，僅供沿革與收割取材。

**已合併進 main（Phase 0 制度全套）**：
- PR #50 鐵律 13/14＋驗收改制條款（CLAUDE.md/AGENTS.md）
- PR #51 claude-review.yml 新判詞（🔴＋未清🟡 全清才過；⚪ 範圍外不擋）
- PR #53 **鐵律 15 提交前復核**（27ff8ea，經 17 輪打磨）——**現在對所有 push 生效**
- main 閘門全綠（check-/test- 腳本逐支；名單複驗：`ls scripts/ | grep -cE "^(check|test)-"`；跑法見下 §④）

**進行中：PR #52**（`m1/shopify-business-logic-blueprint`，總綱＋15 領域章合入 main）：
- 現任 head＝**1073b3a**（第 17 輪）；**bot 已判「通過」**（🔴🟡 全清）
- 但 #53 合併使 base 移動，Codex 佇列 flush：對新 merge ref 發**三個全量 review
  ＝59 條唯一意見（11 條 P1）**，全部待處理——工作佇列全文見本檔 §⑤
- 清完 59 條＋雙方對同一 head 零意見 ⇒ 合併 #52 ⇒ Phase 0 收官

**push HEAD 歷史（15.1 兩點 diff 的基準鏈）**：
```
#52: f2f8f4f→00fbd40→0806d66→74f2bcc→21f7f41→07e9084→1073b3a(現任)
#53: …→2b3b89b(已合併 27ff8ea)
```
遠端基準 tag：`pr52-last-push`（=1073b3a，在遠端）；pr53 的已按 15.4 刪除。

## ② 為什麼這樣做（關鍵裁定與被推翻的假設）

**使用者鐵律（2026-08-17 當日新增，全在記憶檔）**：
1. 🔴 **「所有修復只針對指出來的問題，禁止擴到其他地方」**（`fix-only-what-is-flagged`）
   ——主動 sweep／順帶修／預防性修正／範圍宣稱（「清零」「全收」「無殘餘」）**全部禁止**。
   教訓：我做的每次擴範圍（掃描宣稱、額外註記、憑印象引節號）本身成為下一輪意見來源。
   驗收方自己會全倉掃；你只修它點名的那一處、選它給的最小修法。
2. **修復回歸三步**（`fix-regression-lesson`）：修前全文互動分析／最小爆炸半徑
   （修正文字不新增會腐化的計數、行號、輪次）／commit 前整體重讀。與鐵律 1 並用：
   分析歸分析，**動手只動點名處**。
3. **全自動化授權**（`full-automation-authorized`）：全步驟免確認；例外＝憑證紅線、
   破壞性操作、計畫外新裁定。合併條件不變＝雙 bot 零意見。
4. **Ella 裁定**（`ella-public-repo-ruling`）：repo 已轉 **public**（解 Actions 額度），
   Ella fixture 留倉、視為無授權問題，**不再就此提出警告**。

**被推翻的假設**：①「字面 grep 清零＝家族清零」假（中文欄名變體漏網）②「三點 diff 可作
回應輪基準」假（rebase 後退回 merge-base）③「只記 SHA/本地 tag 可跨環境」假（object
不可達）——三者的正解已全部寫進 CLAUDE.md 鐵律 15 條文本文，**照條文執行即可**。

## ③ 還有什麼沒解決（按序做）

1. **清 #52 的 59 條佇列**（§⑤）→ bot＋Codex 對同一 head 雙零 → 合併 →
   刪分支＋`git push origin :refs/tags/pr52-last-push` → 拉 main 跑閘門。
   合併前順手：更新 PR #52 描述的 ⚪ 段與陳舊欄（bot 曾點名；網頁編輯即可）。
2. **Phase 1 立即開工**（重啟計畫，見 `C:\Users\pisce\.claude\plans\memoized-skipping-papert.md`
   ＋任務清單 #4/#5/#6）：PR-E1 `specs/91-pit-register.md` 坑登記簿骨架（大量 ⚪ 等它落地）
   → A1 `specs/92` 宣稱索引 → C0 `scripts/rwd-check.mjs`（排查階段 PR-C0 引入，**尚未建立**；**Playwright 依賴需使用者裁定**）
   ＋D0 `specs/94` → reaudit r1（訂單模組起）。
3. 懸而未決：總綱 §7 開工前必裁 9 條 Q（M2 前）；limits.yml:765 informational float
   口徑（⚪ 已登記，下次動該檔時處理）。

## ④ 下一個人要注意什麼（操作 SOP，全部實測有效）

**每輪固定流程（＝CLAUDE.md 鐵律 15，逐字照做）**：
修復（只修點名處）→ 16 支閘門逐支看退出碼 → commit → 15.1（`git diff <上輪 push HEAD> HEAD`
**兩點**，逐意見核 hunk）→ 15.2 重拉兩類留言（有新意見⇒回款起點）→ push ＋ 記 HEAD
＋ `git tag -f pr52-last-push && git push -f origin pr52-last-push` → PR 留言
（**只寫「意見→已修」對映**＋`@codex review`）→ 等 15–25 分鐘查判詞。

**閘門一鍵**（Git Bash）：
<!-- 🔴 2026-08-17 更正（PR #55 首輪 Codex）：下列迴圈只匹配 .rb/.sh，漏掉 scripts/ 的
     兩支 Python 閘門（check-baseline-raise.py／test-lint-rules.py）——照跑會在 Python
     閘門失敗時仍印 FAIL=0。原文保留；完整跑法＝下列迴圈之外另跑：
     python scripts/check-baseline-raise.py && PYTHONIOENCODING=utf-8 python scripts/test-lint-rules.py -->

```bash
cd chilllovesaas && FAIL=0; for s in $(ls scripts/ | grep -E "^(check|test)-.*\.(rb|sh)$"); do case "$s" in *.rb) ruby "scripts/$s" >/dev/null 2>&1 || { echo "RED $s"; FAIL=1; };; *.sh) bash "scripts/$s" >/dev/null 2>&1 || { echo "RED $s"; FAIL=1; };; esac; done; echo FAIL=$FAIL
```

**讀判詞（#52 頁 14 萬字常卡骨架）**：
- 首選：登入 Chrome 分頁對 PR 頁執行 fetch＋`\uXXXX` 解碼（React 殼把資料內嵌成
  escaped JSON）：
  ```js
  const r=await fetch('/pisceshei/chilllovesaas/pull/52?_='+Date.now(),{cache:'no-store'});
  const d=(await r.text()).replace(/\\u([0-9a-fA-F]{4})/g,(m,h)=>String.fromCharCode(parseInt(h,16)));
  // d.lastIndexOf('【驗收結論】') 取 bot 判詞；有時回空殼，重試 3-5 次
  ```
- Codex inline 用 innerText 抓：pattern `chatgpt-codex-connector Bot\n\n <標題>`；
  內文含 `=?&;` 會觸發 DLP，先 `.replace(/[=?&;]/g,'·')`。
- 留言框在骨架態依然存在：`#new_comment_field` 填值→`dispatchEvent(input)`→
  `form.requestSubmit()`。
- 頁面卡骨架＝GitHub 慢，重載＋等 10 秒×3；Actions 失敗看 run 頁 Annotations
  （曾發生 billing 阻斷，已因 repo 轉 public 解除；重跑＝run 頁 Re-run all jobs）。

**驗收方行為**：bot 每輪自動跑（審 push 的 head，判詞含「審 <sha>」與兩點增量聲明）；
Codex 需留言 `@codex review` 觸發，**會排隊**、有時遲到成批 flush（本佇列即此來源）。
Codex 空殼回覆「Didn't find any major issues」＝零意見。

**歷史層紀律**：worklog/handoff 敘事段**永不回改**，錯了加
`<!-- 🔴 日期 更正（來源）：… -->` 註記；終態層（worklog Changes 表、handoff §①、
docs/dev）每輪必須等於 HEAD。渲染保真：`<sha>`、`{N}` 這類 token 必須包反引號
（尖括號會被 HTML 吞、花括號進解析）；意見點名哪行就修哪行，**不要憑掃描推定對象**。

## ⑤ PR #52 Codex 59 條工作佇列（逐條處理；★=舊家族殘餘副本、◆=新問題）

> 處理法：到 PR #52 頁搜標題取完整 body＋檔案錨點（每條意見上方有檔名＋行號連結），
> 修**該處**，逐條打勾。分批 commit 可（10–15 條一批），15.1 逐項核 hunk。
> 下列摘要僅供定位，**動手前必讀原意見全文**。

1. ★總綱 T3：Preserve approved over-refund branch——原子式 unconditional `≤captured−refunded`，漏 `allowOverRefunding=true` 合法分支（§2.4 M7 自述的 store-credit over-refund）
2. ◆「487 條唯一 URL」不實（487 出現／459 唯一）→ 改「來源條目」措辭或標唯一數
3. ★總綱 S3/S4：Persist checkout payment attempt before PSP（specs/15 已修、總綱列未同步——PSP 成功後 S4 回滾＝無對帳孤兒）
4. ★總綱：Tax the post-discount unit——某不變量用 `unit_price_cents` 未扣分攤折扣（M-9 相鄰）
5. ◆AOV inequality 測試 fixture 需含 post-order adjustment 才能證明不等式
6. ◆Tier C 條目的 M0 gating 自相矛盾
7. ◆async payment settle 需發 `orders/paid`（事件欄漏）
8. ◆shipment 單位被 reject 時 incoming 未沖（02 章）
9. ◆aggregate inventory 量「互斥」措辭錯誤
10. ★總綱：Compare requested refund not cumulative（Σrefunds 語義雙扣：`new≤cap−refunded` 的 new/Σ 定義）
11. ◆事件欄 List paid-transition events（承諾 orders/paid＋orders/updated 但欄未列）
12. ◆02 章 C.9 舊句「rejection changes no location quantity」與新 reject 沖 incoming 矛盾
13. ◆AGENTS.md 本輪終態回寫
14. ◆Make AOV fixture prove both inequalities（與 5 同族不同點）
15. ◆Update cart index to preserve distinct line attributes
16. ◆Branch draft conversion when inventory was not reserved
17. ◆Key return-time restocks without requiring a refund line
18. ◆Apply oversell guard to order-edit additions
19. ◆Derive…（標題截斷——回頁取）
20. ★02 章：Debit available on direct draft transfers（某列兩態同用 reserved−）
21. ★總綱併發表：metafield 唯一索引補 shop_id 前綴
22. ★總綱：QST 997.5 float 殘留於 _bp map（另處已 ppm）
23. ◆specs/15 F5：Add non-PSP variants to the contract
24. ◆總綱 T1：Branch draft inventory transition（draft_reserved→committed 與 available 分支）
25. ◆12 章：UNAPPROVED comments 的 entry path 未定義
26. ◆Snapshot COGS before publishing order event
27. ◆B2B catalog specificity 在 price resolve 的順序
28. ★總綱：Persist inbound webhooks before ack（殘留列）
29. ◆總綱：fulfillment cancellation 的庫存帳務列缺
30. ★總綱 §6/D-11/M-11 vs 新 _ppm map 權威衝突（Align tax engine with ppm scale）
31. ◆09 章：shipment events 應 override 普通 SUCCESS（新回寫規則仍 status 優先）
32. ◆14 章 C.13:259 仍讀 current unitCost（vs T1 快照）
33. ◆specs/15：Preserve authorization-only PSP payments（manual capture）
34. ◆Skip incoming updates for external destinations（殘留處）
35. ◆Separate manual unpaid orders from PSP-pending locks
36. ◆14 章：product-only reversal basis 傳播到下游公式
37. ◆worklog：Refresh terminal Changes inventory（round 12 側）
38. ◆worklog：`git diff main...HEAD --name-only` 在合併後不可重現→改式
39. ★specs/86：refunds without refund_line_items（order-level／goodwill refund 的商品段歸屬）
40. ◆總綱：notifications 讀 event snapshot 非現值（延遲 job）
41. ◆總綱：market deletion enum vs 不落庫矛盾
42. ★總綱：edit-session 唯一性 only while open（X-19 已 open_flag、某句仍 plain UNIQUE）
43. ◆API contract 410 redirects（28 號相關句）
44. ◆unresolved PSP attempts 不得記 FAILURE
45. ◆UNLISTED products 應通過 purchase availability
46. ◆bundle parent 應入 cart merge identity（Q-44 相關）
47. ◆09 章：carrier-rate cache key 補完整正規化請求
48. ◆12 章：theme archive extraction 安全邊界（zip bomb／路徑穿越）
49. ★specs/16：plain UNIQUE(shop_id,order_id) landing 句殘留（Scope to open rows）
50. ◆worklog：Annotate unreproducible sweep counts
51. ◆Preserve checkout-extension subscription guard
52. ◆Remove intentionally rejected 2019 edit guard
53. ◆Propagate generated edit-session key to remaining contracts
54. ◆Skip releasing stock that deferred lines never committed
55. ◆08 章：customer redaction 覆蓋全部 persisted PII 副本
56. ◆總綱：Move available inventory with the fulfillment order（move 列只動 committed）
57. ◆10 章：inclusive-tax extraction formula before rounding
58. ◆（k63 尾段標題未抓全——回頁補）
59. ◆（同上）

## 附：本輪產生的 worklog 檔
- `docs/worklog/2026-08-17-鐵律15提交前復核.md`（#53 全程 17 輪沿革，已隨 #53 入 main）
- `docs/worklog/2026-08-15-業務邏輯總綱合成.md`（#52 全程各輪節，在 #52 分支）

## ⑤-補：live 47 條精確清單（k＝處理序；q＝意見引文片段，·＝DLP 遮字）

分類法：引文最長乾淨片段仍存在於錨定檔 ⇒ 待修；已不存在 ⇒ 已修（回覆時標「已於第 N 輪修」）。
13 條 Outdated 不需處理（GitHub 已標記行已變）。

k0 唯一URL計數→合成worklog「487 條唯一 URL」句
k1 S3付款attempt→總綱 A4/S3 列（3DS/47 errorCode 那列附近）
k2 M-11舊式引文（現行已 SCALE——核實後標已修）
k3 AOV不等式fixture→A-3 列
k4 TierC M0 gating→L4 分層表
k5/k6 orders/paid 事件欄→order_transactions/create 列（§04 E.1）
k7 AGENTS終態回寫（第3輪節錨）
k8 AOV fixture 斷言兩個不等式→「至少一筆 reversal」句
k9 cart update.js 不驗數量列（405 行域附近）
k10 A4 draft 轉正列（未保留分支）
k11 退貨處理子冪等列＝refund_line_item 單路鍵句
k12 order-edit 加品 oversell guard
k13 displayStatus 三軸合成（09章）
k14 X-33 metafield 鍵無 shop_id（總綱併發表）
k15 specs/15 F5 non-PSP 變體（B2B terms/orderCreate PENDING 引文）
k16 總綱 §8 ppm 註 vs §6/D-11 權威衝突
k17 09章 displayStatus latest-event 句
k18 14章 C.13 現值 unitCost
k19 specs/15 authorization-only（第5輪註釋錨）
k20 外部 destination incoming 殘留
k21 manual unpaid vs PSP-pending 鎖分離
k22 86 §3.2 product-only basis 下游傳播
k23 AGENTS 第12輪終態
k24 worklog c63 補記 main...HEAD 合併後不可重現
k25 86 無 refund_line_items 的退款歸屬
k26 A1 通知列讀現值→event snapshot
k27 F6 Market DELETED enum 矛盾
k28 X-19 相鄰句 plain UNIQUE 殘留
k29 (漏抓——回頁補)
k30 limits.yml AWAITING_RESPONSE 超時→FAILURE 句
k31 V-2 可得式漏 UNLISTED
k32 bundle parent 入 merge 鍵（Q-44 錨）
k33 09章 carrier cache 鍵
k34 12章 D.1 zip 安全
k35 specs/16 :933 註 plain UNIQUE 句（16輪已改 open_flag——核實）
k36 worklog sweep counts 註記
k37 checkout-extension 訂閱 guard
k38 2019 edit guard 刪除
k39 specs/16 生成欄鍵傳播到其餘契約
k40 deferred 行未 commit 不釋放（S13 附近）
k41 08章 redaction 全 PII 副本
k42/43 FO move available 同步（重複條）
k44 10章 inclusive-tax 抽取式捨入前
k45 14章 COGS 讀單時快照
k46 01章 unit-price 小數位隨 market locale（zero-decimal）
