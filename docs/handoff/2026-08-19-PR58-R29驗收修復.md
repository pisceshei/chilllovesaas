# Handoff — 2026-08-19：PR #58 R29 驗收修復

## ① 我改了什麼

R29 七項均取得合法落點：D1 補附錄 A 與 P-1 雙向集合判準；D2 修終態 Changes 列；
A1 依使用者裁定把 Shopify／SHOPLINE 用途分開；A2 把 CD 驗收分配到三個 PR；A3 拆五個獨立 PR；
B1 刪無證據的設計意圖；B2 把有競態的 approve 排除於 P-8 單元一安全收口。
`docs/DECISIONS.md` 同批補入 D17–D27。

本輪 worklog：`docs/worklog/2026-08-19-PR58-R29驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：R-9 必須在 SHOPLINE 建立訂單。** R-9 交付物只要求 SHOPLINE 作促銷、會員、Builder、
  報表參考；使用者再確認交互邏輯取證屬 Shopify。故 2% 費用不是要授權的成本，而是錯套平台用途。
- **被推翻：#60 已讓 approve 原子綁 head。** `git log -p` 與分支實物顯示 REST `commit_id` 沒有
  head 前置條件；#60 的正確方向是降級 approve 為稽核紀錄，不是假稱競態消失。
- **程序教訓：review body 也是意見載體。** 只拉 inline 會漏 D1；15.2 必須同時讀 conversation、
  review `.body` 與分頁 inline threads。

## ③ 還有什麼沒解決

- PR #58 尚待新 head 的 GitHub 驗收與合併。
- P-1 checker 的程式與 fixture 尚未建立；只在 P-1 開工時實作，不得在 P-0 順手擴做。
- #60 的 workflow 更正仍須走它自己的人工合併程序；本 PR 不改任何 workflow。

## ④ 下一個人要注意什麼

1. 驗收「七項全收」時逐項對 `git diff pr58-last-push..HEAD`，不要靠文字計數。
2. push 前依鐵律 15.2 全量重拉三種 GitHub 意見形式；reviews 的 `.body` 不可省。
3. GitHub Codex 對本次實作不算獨立驗收，合併仍以 Claude 判詞與機械閘門為必要條件。
4. SHOPLINE 不得被當成訂單交互測試場；若日後出現付費操作必要性，另案回問使用者。
