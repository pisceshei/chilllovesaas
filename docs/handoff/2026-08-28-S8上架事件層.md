# Handoff — S8 上架事件層（2026-08-28）

工作包＝三包指示的第三包。worklog＝`docs/worklog/2026-08-28-S8上架事件層.md`。
輸入 ref：`main` = `cbbc677`（S7）。分支 `m2/s8-listing-events`。

## ① 我改了什麼
六個 listing topic＋Write 即時發射＋到點 translator＋13 格 spec。突變 5/5 轉紅。

## ② 為什麼這樣改
- 閘門逐 topic（ADD 有 active、REMOVE 無）＝官方逐字，不是統一閘。
- 🔴 translator 判準依 DB 現值不比 payload——PR-C 到點會改寫 published_at，
  兩消費者順序不保證；比 payload 的版本在後跑時 100% 誤判 superseded。
- 冪等：到點面 dedupe_key；即時面同交易單發。

## ③ 還有什麼沒解決
worklog Pending 五項（訂閱面／status 翻轉／R6／UPDATE 寬義／variant ours）。

## ④ 下一個人要注意什麼
- 🔴 **同 topic 多消費者：任何一個 raise，整筆事件 pending 重試、全部消費者重跑**
  （delivery 隔離只擋「成功者不重複」，不擋「失敗者拖著大家重試」）——
  新消費者的不合格分支一律 return 禁 raise（D53 紀律，translator 已照做）。
- 🔴 **關聯名是 `product_variants` 不是 `variants`**——joins 寫錯不會在載入時炸，
  會在第一次真的走到那行時炸（本輪靠 PR-C 的 T08b 全鏈路測試抓到）。
- 加新的 listing 轉場前先讀 dev doc ② 的九格矩陣——每格要嘛有事件要嘛有
  fail-closed 理由，不要留下第十格沒歸類。
