# Handoff：引擎缺口 PR-7——section 包裝三件＋真店金標本入倉（2026-09-03）

> 工作包＝分支 `engine/section-wrapper-7`（base main `54879d64`）。依鐵律 21 四段。
> 配對 worklog：`docs/worklog/2026-09-03-引擎缺口-section包裝三件.md`。
> 本包是 `docs/handoff/2026-09-03-引擎缺口-filters.md` §④ 排定的下一批第 1 包。

## ① 我改了什麼
- 真店（pnrjnw-sy／hoko.vip）本日完成：Publish 切換三套主題抓金標本、建三個產品、Ella 復原（worklog Done 逐步）。
  接手要點：Chrome 分頁必須 `document.visibilityState === "visible"` 才能截圖／點擊；點擊座標＝截圖像素框
  （1568×750），不是 CSS px；主題庫列的 Publish 有時要點兩次才出 modal；產品表單是自訂元件（form_input 不支援），
  用點擊＋打字；Tags 欄本輪未成功輸入（三產品皆無 tag）。
- 引擎：section wrapper id／`section.id` 帶來源前綴、群組 class、群組 BEGIN／END；SRA 收完整 id；編輯器橋以裸 key 定址。
  驗證：SW1–SW4 綠；五個突變各自轉紅；SRA／cart／search／editor／conformance 回歸綠；rubocop 綠。

## ② 為什麼這樣改
- 三套主題金標本一致（同一形出現在 Kalles／Minimog／Ella），且主題 CSS／JS 以 `#shopify-section-{{ section.id }}`
  定位（`grep -rhoE "#shopify-section-\{\{" test/fixtures/themes` 有命中）——wrapper id 與 `section.id` 必須同值。
- 數字段用模板鍵／群組名：我方沒有本尊那種 per-template／per-group 數字 id；形同即可讓主題選擇器與 JS 正常，
  值不同登記為 ours。
- 編輯器 op 仍以裸 key 定址：改 wrapper 不改編輯器資料模型，橋 JS 兩個 helper 吸收差異。

## ③ 還有什麼沒解決
- 靜態 section 的 id 形官方未逐字；`section.id` 數字段 ours；產品 tags 未設——見 worklog Pending。
- hoko.vip 現在有三個產品（Acme Tee／Bolt Mug／Cosy Lamp）與自動系列「首頁」（frontpage）；`/collections/all`、
  `/collections/frontpage`、三個商品頁的金標本已抓。無 blog／article、無自建 page（`/pages/contact` 是主題頁）。

## ④ 下一個人要注意什麼
- 下一包＝`page_title` 各頁型（真店 zh-TW 形已在 `docs/handoff/2026-09-03-引擎缺口-filters.md` §④；英文字串
  仍未取得——chill.deals 全站密碼牆、hoko 無英文 locale），再下一包＝系列 tag 路徑路由（`/collections/{handle}/{tag}`）。
- 金標本 diff 入口：`tools/theme-conformance/golden/<theme>/index.html` vs 我方 `ThemeEngine::PageRenderer` 輸出
  （run.rb 範例）；先比 `<head>`、section 包裝、`window.Shopify`，再比 section 內容（handoff 2026-09-02 §④B）。
- 改 `minimal-1.0/templates/index.json` 的 order 會牽動 `storefront_blocks_schemes_spec` K1（頁快取計數）與本包 SW1。
