你接手 CHILL LOVE（多租戶電商 SaaS，功能與渲染 1:1 對齊 Shopify）。上一個 Claude 已把 E15（鏡像店五語言五市場）與 D80 方案 1（語言 URL 前綴、市場 cookie、hreflang 本尊形）做完、合併、部署到 bt3 並複驗。你從下面的狀態接續。

一、環境
- 真倉庫：`C:\Users\pisce\Documents\ChatGPT\CHILL LOVE SYSTEM\worktrees\p2-claim-index-r6`（工作目錄 `C:\Users\pisce\Downloads\shopifysystem` 不是倉庫；每個 shell 命令先 `cd` 進真倉庫，shell cwd 每次重置）。遠端 `github.com/pisceshei/chilllovesaas`，`gh` 已登入。
- main 現在是 `6d2dfd85`（#327）；bt3 生產機 `ssh bt3-wan`，`/www/wwwroot/chilllove/app`，head `a9c6852`；公開站 `https://demo.chilling.com.hk`、`https://mirror.chilling.com.hk`（鏡像店，D80 後是根路徑形：`/`＝預設語言 zh-CN、`/zh-hant/`、`/en/`…；舊形 `/zh-hans-tw/` 一律 404）。
- Windows：`python`（不是 python3）且一律 `PYTHONIOENCODING=utf-8`；Bash heredoc 會吃反斜線（含 `\` 的腳本用 Write 工具寫檔再跑）；殺背景工作用 Windows pid；`.claude/launch.json` 的 rails 設定會落到 WSL bash 起不來（要修就改成 `C:/Program Files/Git/bin/bash.exe` 並進 commit）。

二、先讀（全文）
1. `docs/handoff/2026-09-04-總交接-語言前綴本尊形後.md`（本次總交接；§1 環境、§2 現況、§4 未解、§5 工具與流程、§9 下一包、§10 紅線、§11 教訓）。
2. `docs/handoff/2026-09-04-總交接-編輯器對表與國家字典.md`（上一份總交接的 §1／§5／§6／§10 仍有效）。
3. `CLAUDE.md` 鐵律 1–22、`AGENTS.md`、`docs/DECISIONS.md` 的 D40／D47／D79／D80（末段＝方案 1 裁定）／D82／D83。
4. `docs/dev/d80-url-structure.md`、`docs/dev/e8-render-parity.md`、`docs/dev/e12-computed-parity.md` §2b、`docs/dev/external-facts.md` §G22–§G23、`docs/specs/91-pit-register.md` §3.83–§3.84。

三、流程（D40／鐵律 15.4／17／D83；總交接 §5 有逐步命令）
分支 → targeted spec → 凍結 tree 跑全部 `config/ci.rb` 閘門（逐支親眼看退出碼；`bin/setup` 在 Windows 的 ENOEXEC 不是判準）→ 填 worklog 閘門表 → **`git add -A` 之後**跑 `ruby scripts/check-doc-claims.rb --base origin/main`（未 add 的新檔不會被掃，上一包因此在 CI 才紅）→ commit → 突變全紅（先 commit 再跑，跑完 `git diff HEAD` 必須空）→ push＋`pr{N}-last-push` tag → PR（body 含規格章節／自測／假設清單／突變表）→ 有界輪詢 CI `quality`＋`test` 綠（`pnpm audit` 撞 npm registry 逾時 ⇒ 同 head `gh run rerun --failed`）→ `gh pr merge --squash --match-head-commit <head>`（D83 已授權代合，含 18.3）→ 刪 tag → bt3 部署（deploy.sh → `render_parity:mirror[mirror]` → cache clear）→ 公開站 curl 複驗 → 八頁三寬 computed 對表（`CAND_BASE=https://mirror.chilling.com.hk`，不帶 `CAND_PREFIX`）→ docs-only 收尾 PR。每包一份 worklog＋handoff 同 commit 入庫（D47）。

四、第 1 包
頁首區段 fetch 內容（31 國／5 語言清單）的 SRA HTML 段 diff：`https://hoko.vip/?section_id=sections--19763396837479__header_default` vs mirror 店同段（`render_parity:diff`，段 id 由 Normalizer 抹成 `G__`）；資料面 LC1／LC2 已對齊，只差 HTML 逐字。做完給我一段中文收尾。之後依總交接 §9 順序：每語言一組 sitemap 子表（本尊形已取證）、`pnpm audit` 重試登記 91 §2、其餘 V。

五、需要裁定先問
目前沒有待裁定事項。射程外三件（62 §K.2 地區重導、市場獨立幣別／價格、舊 URL 301 對照表）要做之前先問我一句；要在真店建子資料夾市場或改真店設定也先問。

六、紅線（不得放寬）
永不輸入密碼／金鑰、不解 CAPTCHA、不授權 OAuth；真店副本主題 143506604135 不得 Publish；不點銷售管道 Uninstall；避免產生費用與對外發信；受保護 fixtures 見總交接 §10；GPLv3 專案禁讀禁抄，Shopify 的 CSS／文案／商標不抄，已購 Ella 原檔可整份複製。oseid、id_token、cookie 值這類票證不入倉、不貼 PR。

七、習慣
一包做完給我一段獨立可讀的中文收尾：合併了什麼、部署到哪、剩下什麼差異、下一步。有不確定的裁定就停下來問，其餘不用等我確認。
