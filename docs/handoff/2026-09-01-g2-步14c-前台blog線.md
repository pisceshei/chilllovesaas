# Handoff：G2 步 14c 前台 blog 線（2026-09-01）

## ①我改了什麼

20 步方案步 14 第三包（步 14 全收）。base＝main `7ed11d6`（與 14b 無檔案交集、
並行開發）。路由三型＋六 drop＋留言 POST＋linklist 擴充＋搜尋 article 接線。
逐檔＝worklog Changes；取證＝98 §1/§2。

## ②為什麼這樣改

- ArticleDrop.handle＝複合形是官方範例值（98 §1）——主題拿 handle 組 URL 的
  慣用法才不斷鏈；Admin API 層維持裸 handle（兩層語義不同，98 明載）。
- 留言三分流位置在 controller 不在 model：closed 是「表單根本不該出現」的
  404 語義（頁面層 comments_enabled? 同軸），不是驗證錯誤。
- FormTag 位置參數只對 new_comment 生效：其他 form 型的靜態表行為不動
  （MB5 紅證 fallback 路徑）。

## ③還有什麼沒解決

- 91 §3.65 四項（form 狀態機/CAPTCHA/next·previous_article/sitemap blogs）。
- 生產煙測待部署後補（本 handoff 提交時 CI 中）。

## ④下一個人要注意什麼

- 新增可搜尋型別四處同批（12b handoff 既載）——article 本包已示範一輪。
- BlogArticlesDrop tagged＝AND 語義（官方 combining 句）；改 OR 要先取證。
- 留言 POST 路由在 :token 類 catch-all 前顯式收（裸＋帶前綴兩形都要）。
