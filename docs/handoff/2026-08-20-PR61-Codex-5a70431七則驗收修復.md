# Handoff — 2026-08-20：PR #61 Codex `5a70431` 七則驗收修復

## ① 我改了什麼

七則 exact-head inline 已精準落地：validation-skip 有界終止、通知中心補官方證據且未選 schema
降級、TikTok 缺口同步、P-5 比例／金額分界、CD-1 真 PITR 演練、先完整覆蓋再收斂，以及每頁
B→C→D 串行基線。配對 worklog 為
`docs/worklog/2026-08-20-PR61-Codex-5a70431七則驗收修復.md`。
初版修改後完整 29 支閘門均已到退出 0；Rails 284/0、Vitest 5/0、doc-claims 274 份／警告 0。
workflow checker 的 WSL Bash 假紅已用 Git Bash PATH 原樣重跑排除，沒有改機制；驗證數據回寫後
已再跑完整一輪，第二輪同樣 29/29，Rails 284/0、Vitest 5/0、doc-claims 警告 0、附錄集合
missing 0／extra 0；改動表格亦通過逐列分隔符檢查與 GitHub `/markdown` API 實際渲染。

## ② 為什麼這樣改（含被推翻的假設）

- 未採「通知中心整段改未取得」：Shopify 官方已直接支撐多項互動；只把沒有逐範本證據的
  `can_deactivate` schema/default 降級，避免丟掉真證據。
- 未採把 percent 轉 cents：`currency_conversion_fee_percent` 是比例，basis points 會改資料契約，
  本輪沒有使用者裁定。
- 未採只加 schema／列數檢查：那仍只驗 full dump，無法證明 dump 後故障的 PITR。
- 未採 B/C/D 同舊 base 並行：B 的原型修復會使 C/D 舊證據失效，故在頁內串行、頁間並行。

## ③ 還有什麼沒解決

- commit 後文件保真與 GitHub 最新 head 驗收尚未完成。
- 七個舊 head inline 尚未回覆／resolve；新 head 也尚未觸發 Codex。
- 總方案兩個同型敘述已登記但未修，必須維持「未同步」的誠實邊界。

## ④ 下一個人要注意什麼

1. validation-skip 逾時是停點，不是允許略過 Claude 證據；PR #61 仍需使用者明文合併觸發。
2. PITR 證據必含 source coordinates、完整 binlog chain、目標點與前後 sentinel。
3. A–F 的兩輪零新只有在權威覆蓋清單全清後才有意義。
4. B/C/D 每頁下游證據都綁 base SHA；上游再改頁面就必須重取，不能沿用舊快照。
5. push 前重拉三個 REST 端點、review body 與 paginated GraphQL；七則全回覆且 resolve 不等於
   新 head 零意見，仍須重新觸發並取得 exact-head review。
