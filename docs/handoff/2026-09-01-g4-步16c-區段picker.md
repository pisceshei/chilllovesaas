# Handoff：G4 步 16c add-section picker（2026-09-01）

## ①我改了什麼

20 步方案步 16 第三包。base＝main `81439da`，分支 `g4/step16c-section-picker`。
sectionCatalog（preset-only）＋picker UI＋addSection op。逐檔＝worklog Changes。

## ②為什麼這樣改

- preset-only 過濾＝24 §1.4 本尊語義（「加入區段」清單由 presets 驅動；
  骨架區段不可手加）。preset 取第一個（我方 minimal 每區段至多一個）。
- addSection 走 applyOp（快照先推）⇒ 新增可 undo；preset 深拷貝防 catalog
  物件被後續編輯污染（M3 紅證）。
- 撞名 id 用 `{type}-N` 尾碼（同型多實例＝本尊常態）。

## ③還有什麼沒解決

- dnd 拖曳＝⚪（未討論依賴不引入）；16d 佈景設定寫回；16e+ code editor。
- 生產煙測待部署後補。

## ④下一個人要注意什麼

- sectionCatalog 是 picker 唯一資料源——新增可加區段＝給該 section 寫 presets，
  不是改 resolver 白名單。
- 🔴 突變還原：帶未提交工作的檔案**不得 `git checkout --`**（本輪誤殺一次），
  一律 python patch 正反向。
