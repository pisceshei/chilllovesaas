# Handoff：G6 步 9a 折扣引擎核心（2026-09-01）

## ①我改了什麼

20 步方案步 9 前半。base＝main `a756a15`，分支 `g6/step9-discounts`。
Engine＋Calculator 管線＋F3 硬保證＋結帳接線。逐檔＝worklog Changes；
語義與紅證對照＝`docs/dev/g6-discounts-engine.md` §2。

## ②為什麼這樣改

- **Engine 不算錢**：鐵律 7 的唯一金額計算點是 Calculator——把 F2.1 數學放進
  Calculator（純函式、無 DB），Engine 只解析；被推翻的候選＝「Engine 全包」
  （會出現第二個金額實作點）。
- **legacy discount: 不動**：44 組手算表格的既有契約，動它＝重驗全部手算；
  新舊參數互斥閘擋混用。
- **E5 的縱深測法**：shipping 疊 shipping 硬規則被 model 旗標驗證天然遮蔽
  （突變不紅的第三類：預期內殺不掉）⇒ update_all 打穿旗標層單測（refunds M3
  seam 同法，mutation-not-red-triage 三分類實用例）。

## ③還有什麼沒解決

- 步 9b 全部 UI/API 面；91 §3.54 五 ⚪；threads 併發實測未做（worklog Pending）。
- 生產煙測隨 9b 一起（引擎無 UI 入口，煙測要等碼輸入欄）。

## ④下一個人要注意什麼

- 🔴 改 evaluate_discounts 前先跑 `calculator_discounts_spec` 的 D1/D2/D3——
  它們是官方逐字的金額正確性錨（複利方向一旦寫反，每單多收/少收真錢）。
- checkout 的折扣求值點只有 delivery recompute 一處；9b 加碼輸入端點時
  **重用同一段**（抽私有 method），不要複製第二份 Engine→Calculator 呼叫。
- claim_discounts! 的 Failure 一定帶碼（("CODE", msg) 雙參——單參是 8a 撞過的
  ArgumentError）。
