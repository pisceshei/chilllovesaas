# Handoff：G4 步 16d2 佈景設定寫回（2026-09-01）

## ①我改了什麼

20 步方案步 16 第五包。base＝main `f724e86`，分支 `g4/step16d2-theme-settings`。
themeSettingsUpsert＋讀面三件＋編輯器佈景設定面板。逐檔＝worklog Changes。

## ②為什麼這樣改

- 與 themeTemplateUpsert 同構（顯式鎖比對＋touch）：16b 實錘「直接賦值
  lock_version 不武裝 AR 鎖」在本表同樣成立。
- settingsDraft 獨立於模板快照棧：整合 undo 需把棧改 {tpl, settings} 複合快照
  ——風險大於本包收益，登記 91 §3.70 由 16e 一次做。
- theme_info 跳過寫在 resolver 不在前端：分組定義是 server 契約。

## ③還有什麼沒解決

- 佈景設定 undo／即時預覽（91 §3.70）；color_scheme_group 編輯器＝16e；
  生產煙測待部署。

## ④下一個人要注意什麼

- ThemeSetting 是佈景設定唯一寫點且必 touch theme；Runtime#db_settings 讀
  同一列——改 schema 形要兩頭看。
- SettingControl 已被 section 面板與佈景面板共用——改它先跑 ED1-9 全套。
- 16e 整合 undo 時 Save 後兩個 dirty 都要清、兩個 lock 都要更新。
