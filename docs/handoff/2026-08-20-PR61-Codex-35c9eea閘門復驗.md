# Handoff — 2026-08-20：PR #61 Codex `35c9eea` 修復後閘門復驗

## ① 我改了什麼

本步驟驗證實作 commit `ee7835e` 對 Codex review `4979980175` 的修復內容。第一次從 setup 起跑，
前 27 項通過；第 28 項因 PATH 上的 WSL `/bin/bash` 無法開啟 Windows Ruby Tempfile 而失敗，
第 29 項未執行。先前口頭「27/2」已在本交接更正，正式集合是 27 綠、1 假紅、1 未跑。

依既有 `docs/handoff/2026-08-20-鐵律21閘門Shell路徑復驗.md` 的 A/B 方法，在同一 PowerShell
process 將 `C:\Program Files\Git\bin` 前置 PATH，smoke check 確認 Git Bash、repo `pwd` 與目標
腳本後，從 setup 起完整重跑。最終 `GATES_ALL_GREEN=29`；Rails 284/0、frontend 5/0、prototype
0/136、doc-claims warning 0、workflow 2 份／33 個 `run:` 及其 11 條 regression 全綠。配對
worklog：`docs/worklog/2026-08-20-PR61-Codex-35c9eea閘門復驗.md`。

## ② 為什麼這樣改（含被推翻的假設）

- 被推翻：命令名 `bash` 在 Windows 只會解析到 Git Bash。第一輪實際解析到 `/bin/bash`，其
  No-such-file 訊息指向路徑翻譯，不是 workflow 內容。
- 未採：只重跑第 28、29 項後與前 27 項拼接。既有倉庫紀律要求修正執行環境後從 setup 起完整
  重跑，第二輪因此是一個自足的 29/29 證據集合。
- 未採：修改 checker 或 workflow。這次失敗是本機 PATH，且 Codex 未點名 checker；依 17.2
  只採環境修正，根因仍由 `91` 的既有條目承接。

## ③ 還有什麼沒解決

- 本步 handoff-only 產物尚未 commit，commit 後 doc-claims 尚未執行。
- Node engine 警告仍存在；沒有新增產品碼修復或支持聲明。
- PR #61 的新 head 尚未 push，五則 thread 尚未回覆／resolve，current-head Codex 與 GitHub CI
  尚未重跑；Claude skip 仍是證據未取得。

## ④ 下一個人要注意什麼

1. staging 本步兩檔、終態入口與附錄 A，先做集合雙向比對，再 commit；commit 後 doc-claims 必須
   exit 0 且 warning 0。
2. 15.1 從 `pr61-last-push..HEAD` 對 review `4979980175` 的五則 inline 逐條核 hunk；15.2 用三個
   paginated REST 端點與 paginated GraphQL threads 全量讀取，任何新增未清都回修復循環。
3. push 後以 GitHub CI 驗證新 head；Claude Review 若再出現 success，仍須查 log 是否
   `Exiting due to workflow validation skip`，不得看綠色外殼。
4. PR #61 只通知使用者人工審核／合併；不執行 `gh pr merge`。
