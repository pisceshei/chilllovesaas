# Handoff：E12 部署後複驗（2026-09-05）

配對 worklog：`docs/worklog/2026-09-05-E12部署後複驗.md`。規範：`docs/dev/e12-computed-parity.md`；鐵律 22。

## ① 我改了什麼

- 目標：把 E12（#318，main `83e85398`）部署到 bt3 後的公開 computed 量測回寫到規範（22.4 憑證）。輸入 ref：main `83e85398`。
- 做了：bt3 部署（`deploy.sh` → restart → cache clear；無 migration；8 頁健康；`/search?section_id=` 回段 3690B）；公開重量商品頁 1280 與七頁 768／390；
  e12 doc §2 追加十五列（由報告檔生成）。
- 驗證：全閘門見 worklog 閘門段；無程式碼變更。

## ② 為什麼這樣改

- 鐵律 19：合併前的商品頁數字來自本機 dev server，部署後必須以公開站重量才可作為現值；768／390 是鐵律 13 三裝置的補齊。
- 結果與本機一致（13/15；七頁只差 `__root__`），證實修法在生產環境同樣生效。

## ③ 還有什麼沒解決

- 主題編輯器 live preview 的 computed 對表未跑（22.1「兩者皆是」）。
- 91 §3.79 的 V 項不變。

## ④ 下一個人要注意什麼

- 重跑：`node scripts/computed-parity.mjs capture <hoko url> a.json --width W --height H --wait 3000 --open-details 1`、同法量 mirror、`diff a.json b.json --out r.md`；
  兩邊同寬同高；`__root__` 的 iframe／shop-cart-sync／body grid 列差是平台注入，不是引擎缺口。
- 商品頁 `main` 的 countdown 秒數每次不同，屬時間性內容。
