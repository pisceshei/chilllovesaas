# Handoff — 變體 × 選項 join 表（PR #38，含驗收輪修正）

## ① 我改了什麼

- `product_variant_option_values` join 表 migration（複合外鍵＋唯一索引組；
  2026-08-16 就地補 `ix_pvov_by_value [shop_id, option_value_id]`）。
- `Catalog::OptionValuesDigest`：id 對編碼 → SHA1 物化 digest；驗收輪改嚴格
  `is_a?(Integer)`（Float／可轉字串一律 `TypeError`）。
- `ProductVariant`：`has_many ... autosave: true`＋`effective_option_value_pairs`
  （記憶體關聯狀態＋DB 列的合成視圖），digest 與驗證共用。
- 回歸 spec：模型頂層三條＋digest 兩組 TypeError；全套件見 worklog 自測節。
- 本輪 worklog：`docs/worklog/2026-08-16-變體選項join表.md`（本 handoff 對應的唯一一份）。

## ② 為什麼這樣改（含被推翻的假設）

- 「digest 從 DB pluck 就夠」被推翻：`assoc.build` 的列與已編輯未存的列都不在 DB，
  兩層靜默資料遺失。背後是三個 Rails 邊界（詳見 docs/dev「驗收輪修正」節）：
  預設 has_many 不存髒的持久化子物件；未載入關聯 `.first` 回脫離物件；
  `build` 不標 `loaded?`。
- 「`Integer()` 當防呆」被推翻：`Integer(1.9)==1` 是截斷不是拒絕——與檔頭 @raise
  宣稱矛盾。兜底層對輸入必須嚴格。
- 「索引夠用」被推翻：`restrict_with_error` 的 `shop_id+option_value_id` 反查，
  三支既有索引都只能吃 shop 前綴；migration 註釋還宣稱已涵蓋（不實），一併修。

## ③ 還有什麼沒解決

- 寫入 service／兩階段 diff 未實作（規格已寫，D12 不含）。
- `product_variants.title` 衍生規則未裁定；部分刪選項的塌縮 tie-break 無規格。
- `effective_option_value_pairs` 每驗證兩次 pluck，暫不記憶化（理由見 worklog Pending）。

## ④ 下一個人要注意什麼

- 🔴 **schema.rb 重生**：本機空 DB 時 Rails 8 `db:migrate` 會直接載 schema.rb
  跳過 migration（runner 也會經 maintain_test_schema 載入）。要真跑 migration：
  暫移 schema.rb → drop → create → migrate → 比對 dump diff（本輪恰好一行新索引）。
- 🔴 讀變體座標的記憶體狀態一律走 `effective_option_value_pairs`，不要自己 pluck——
  否則兩層遺失會以新形態回歸。
- `insert_all`／`upsert_all` 繞過 callback，但 digest 欄 `null: false` 無 default
  會大聲失敗——這是刻意的，不要補 default。
