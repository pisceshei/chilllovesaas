# Handoff — P-0 方案落庫＋鐵律 16/17/18（2026-08-18）

## ① 我改了什麼

`docs/plans/2026-08-18-總方案.md` 入庫（使用者批准的合併版總方案正本）；
CLAUDE.md 立法鐵律 16（修復研究授權）/17（等待自動化）/18（自動合併）全文＋鐵律 9
增補 GPLv3 授權紅線；AGENTS.md 同步摘要；本輪 worklog 與本檔。第 2 輪（驗收回應）再加
HANDOFF.md 現況覆寫與 `docs/specs/91-pit-register.md` §3 的 ⚪ 落籍。
第 3 輪起本 PR 含**CI 行為改動**：`scripts/check-doc-claims.rb` 的 `IN_SCOPE` 擴入
`docs/plans/`（僅 R1／R3；影響往後每個 PR 的 quality 閘門掃描面），
`docs/dev/m0-review-convergence.md` 納管範圍清單第 4 輪同步回寫；第 6 輪再加
`doc_plans_scope` canary fixture＋CASE（quality job 自此多一條常設斷言，
`MIN_CASES` 第 7 輪隨升 10），fixture 表與突變清單第 7 輪回寫。
完整檔案清單以 worklog Changes 表（終態層）為準。詳細見 worklog。
🔴 **第 26–27 輪補入本節**（此前本段停在第 7 輪）：①**CD-1 新增備份前置**——每日
`mysqldump`＋binlog 保留須**實際啟用**並完成**最小還原演練**，**演練通過前不得開 production**
（原矩陣只要 skeleton／`/up`／rollback 即可宣告 CD 完成，而 §9.2 在 M3 開 production、
還原演練排到 M7 ⇒ 真實訂單會落在沒有已驗證備份的庫上；rollback 只回滾映像不回滾資料）；
②**P-8 的 `scripts/await-verdict.sh` 已隨 #59 合併落地**，條文與計畫中的「尚未建立」已同步清除；
③第 27 輪依使用者糾正**撤除了 P-9 工作包**（它是擴散產物），`IN_SCOPE` 擴充改列為 91 §3.4 的 ⚪。

## ② 為什麼這樣改（含被推翻的假設）

立法照 13/14/15 慣例：全文 CLAUDE.md、AGENTS.md 摘要句。三條新鐵律把使用者 2026-08-18
的指示（修復可上網研究／等待掛倒計時自動檢查／全自動推進／雙零自動合併）從記憶條目
升格為條文。**設計取捨**：17/18 的機械部分（輪詢腳本、判詞格式驗證、自動合併 workflow）
不在本 PR——按「機制而非紀律」的立法考量本應同批，但鐵律 15 已有「程序性條款先行」的
先例，且把 workflow 改動混進立法 PR 會讓本 PR 落入反竄改死角（改 claude-review.yml 的
PR 自己不被驗收）；拆開後本 PR 可走正常雙 bot 驗收。機制包＝P-8，鐵律 18 明文「機制
實測全鏈路前不啟用自動合併」，條文與現實不脫節。

## ③ 還有什麼沒解決

1. P-8 自動化基建（`scripts/await-verdict.sh`**已隨 #59 於 2026-08-19 合併落地**、判詞格式驗證、自動合併
   workflow）——下一包。
2. CD 部署三包等伺服器到位（使用者裁定：明天）。
3. 方案內待建檔（92–121 等）依統一路線圖逐包落地。

## ④ 下一個人要注意什麼

- 本 PR 改鐵律本文 ⇒ 依鐵律 18.3 **不適用自動合併**，雙零意見後人工按鈕。
- 本 PR 同時是 P-00 改制的驗證組：看驗收留言是否引用 `gh pr checks` 的 CI 結論；
  出現「requires approval／零實跑證據」＝allowlist 或 prompt 沒對齊，回開 #57 的改動處。
- 新 session 接手入口＝記憶 `merged-master-plan` → `docs/plans/2026-08-18-總方案.md` §0.3。
- 總方案分層（第 9/10 輪定制）：快照段（Context／§〇.1–.2／§一–§四）不回改只加註；
  路線圖段（§〇.3、§五–§十二全部、Verification）**每輪隨現值回寫**——其中 §六/§9.3 是
  CLAUDE.md 條文的終態摘要，兩者衝突＝本檔 🔴 過期，一律以 CLAUDE.md 為準。

本輪 worklog：`docs/worklog/2026-08-18-P0-方案落庫與鐵律16-18.md`
