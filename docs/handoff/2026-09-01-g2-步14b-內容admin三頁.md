# Handoff：G2 步 14b 內容 admin 三頁（2026-09-01）

## ①我改了什麼

20 步方案步 14 第二包。base＝main `7ed11d6`，分支 `g2/step14b-content-admin-pages`。
三頁＋i18n＋路由。逐檔＝worklog Changes；UI 形對錨＝98 §4。

## ②為什麼這樣改

- 單頁面板（列表＋表單同頁）而非 detail 路由：redirects/languages 頁同款
  輕頁式；content 線的表單欄位少，不需 ProductDetailPage 級三態框架。
- MenusPage 本地樹＝唯一真相＋Save 一次整棵送出：**跟 menuUpdate 替換語義
  同構**——增量式 UI 對替換式 API 是縫（MN2/MF3 紅證：漏舊項＝後端清掉）。
- 貼文表單預設 Hidden＝本尊 Add blog post 實測預設。

## ③還有什麼沒解決

- 14c（前台 blog 線）——98 §1/§2 取證已齊；本包 UI 生產驗證隨 14c 部署。
- GID 直填的資源型選單項（picker ⚪）。

## ④下一個人要注意什麼

- MenusPage 樹操作全走 path 索引鏈＋structuredClone 不可變更新；新增操作類型
  （搬移/排序）照 mutateTree 形擴充。
- i18n 鍵羣 pages.*/blog.*/menus.*（含 menus.kind.{TYPE} 十值）——新增
  MenuItemKind 值要同步 i18n 五語言＋MenusPage ITEM_KINDS。
