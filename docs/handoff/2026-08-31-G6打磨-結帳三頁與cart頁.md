# Handoff — G6 打磨包（2026-08-31）

## ① 我改了什麼
branch `g6/checkout-polish`，base＝main `c8ab834`。使用者指示「繼續把 UI 和其他部分
打磨」：結帳線三頁視覺（tokens 自寫 CSS、雙欄+sticky 摘要）＋/cart 404 縫補＋
admin 設定索引樣式。清單見同名 worklog；本地全流程截圖驗過。

## ② 為什麼這樣改
- 樣式吃 `[data-*]` 選擇器：HTML 幾乎零改動 ⇒ 既有 request specs 的 hook/文案
  全數不動（跑過回歸 23/23＋pages 15/15）。
- /cart 繞過快取而非快取空車：14 §F1-4 的正解——個人化頁「不進快取」。
- 不宣稱 1:1：87 量測輪未做（需使用者放行 chill.deals）；本包＝形先立、值後校。

## ③ 還有什麼沒解決
worklog Pending 四項（87 校準／地址表單／單方法快照 UX／collections 頁）。

## ④ 下一個人要注意什麼
- checkout.css 值一律 var(--*)（tokens.css 同頁載入）；87 校準時改值不改結構。
- /cart 的 no-store 與 buyer_cart_json 只讀不建車——改動前看 C1/C2 兩格。
- 重跑：`bundle exec rspec spec/requests/storefront_pages_spec.rb
  spec/requests/storefront_order_creation_spec.rb spec/requests/storefront_psp_payment_spec.rb`。
