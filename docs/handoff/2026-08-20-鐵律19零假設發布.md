# Handoff — 2026-08-20：鐵律 19 零假設發布

## ① 我改了什麼

新增鐵律 19：全部倉庫內容與外部發布先取證，未取得時只准記證據缺口與取得方式，不得把
假設或推論作為事實、實作輸入、驗收依據或發布結論。`AGENTS.md`、現行方案、入口與 D33 已同步。

配對 worklog：`docs/worklog/2026-08-20-鐵律19零假設發布.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **直接依據**：2026-08-20 使用者逐字裁定「所有一切項目內容禁止假設，必須取得證據，
  才能發佈」；這是專案規則與授權的第一手證據，已落籍 D33。
- **被推翻的舊規則**：`AGENTS.md` §8.2 原允許外部斷言在無證據時標 `〔推論〕` 並附驗證法；
  新裁定要求先有證據才能發布，所以該路徑撤銷，既存標記降為未證實缺口。
- **沒有外推**：使用者裁定可決定專案紀律，但不能證明 GitHub、第三方 API、CI 或部署行為；
  這些聲明仍須取得相應第一方或執行證據。

## ③ 還有什麼沒解決

- PR #61 尚待本輪 commit、push 與當前 head 的 CI／Claude／Codex 驗收；因修改規範本文，
  雙清後由使用者人工合併。
- 裁定前的既有 `〔推論〕` 仍散見歷史與方案；它們不是證據，後續工作包不得直接採用，須先取證。
- 本輪未取得把鐵律 19 機械化為 checker 的使用者裁定，因此沒有修改 `scripts/` 或 workflow。

## ④ 下一個人要注意什麼

1. 發布任何聲明前，逐句問「哪一份證據支持這句，而且是否對應當前 head／版本／時間」。
2. 外部語義優先用官方／第一方 URL、日期與英文原文；查不到就寫「未取得」與取得法，不補答案。
3. 內部聲明附可重跑命令／輸出或精確 commit、diff、沿革；CI／GitHub／部署附當前 run／API 證據。
4. 舊 `〔推論〕` 不能當作既定需求；需要它時先取證，取不到就停該聲明與依賴實作。
5. 發現既有假設時，歷史層追加日期更正與撤回，終態層改成有證據的現值，不靜默洗掉事故。
