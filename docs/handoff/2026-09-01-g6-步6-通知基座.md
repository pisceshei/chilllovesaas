# Handoff：G6 步 6 通知基座（2026-09-01）

## ①我改了什麼

20 步方案的步 6。base＝main `d5c8229`，分支 `g6/step6-notifications`。
交付＝89 號 teardown＋通知鏈（outbox 消費者→DeliverJob→mailer→Renderer）＋
模板 overlay（M0 表採用）＋GraphQL 四支＋admin 兩頁＋預設模板三支＋production
SMTP 分流。檔案與測試映射＝`docs/worklog/2026-09-01-g6-步6-通知基座.md`；
控件級細節＝`docs/dev/g6-notifications.md`。

## ②為什麼這樣改

- **M0 表採用而非平行表**：首版 migration 的 table_exists? 守衛揭露 M0 早已建
  notification_templates（被推翻的假設：「通知模板表要新建」）。M0 形多 channel／
  enabled／name 三欄——正好是 SMS／停用 toggle／顯示名的預留位，照用。
- **攤平 payload**：官方逐字（89 §5）email 模板不帶 order. 前綴——渲染器不做
  named drop，Payloads 直接產攤平 hash；money filter 與主題引擎同模組（cents 直通）。
- **notify 閘在消費者**：fulfillmentCreate 的 notifyCustomer 已進 outbox payload，
  消費者自審——信的去留跟出貨動作解耦（勾掉＝事件照發、信不寄）。

## ③還有什麼沒解決

- 生產 SMTP 未配置（刻意；見 worklog Pending 首條）。煙測驗「渲染與 job 鏈」，
  不驗真外寄。
- 步 7 要接：棄單判定 job（abandoned_at 已有欄）＋recovery URL 端點＋
  abandonedCheckouts query；模板／payload 本步已備。
- V-236（模板停用 toggle 兩源不一致）待複測；91 §3 已隨本步登記 ⚪ 批。

## ④下一個人要注意什麼

- **改 Payloads 的變數名先讀 89 §5**——那是官方契約攤平形，巢狀化會讓全部
  預設模板的 {{ name }} 渲染成空（突變 M1 的紅格就是它）。
- 新通知只要三件事：Catalog 加 entry＋預設 liquid 檔＋消費者掛 registry；
  不要另起渲染鏈。
- spec 內建 Struct 假 event（shop_id/payload 兩欄）即可測消費者；別為此建 outbox 列。
