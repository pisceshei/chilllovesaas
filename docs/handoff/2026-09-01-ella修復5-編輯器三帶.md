# Handoff：Ella 修復 PR-5 編輯器三帶（2026-09-01）

## ①我改了什麼

六軸診斷 editor 軸 critical ①。base＝main（#266；與 #267 無檔面交集）、
分支 `ella/editor-groups`。逐檔＝worklog Changes。

## ②為什麼這樣改

- 群組寫回走 themeFileUpsert 而非新 mutation：群組 JSON 本來就是主題檔，
  16e1 覆寫層＋鎖＋touch 全現成；模板繼續走 themeTemplateUpsert
  （templates/ 在檔案面白名單被拒——雙真相源禁令維持）。
- undo 棧跨帶同棧：本尊 ↺ 是全編輯器一條時間線，分棧會出現「按復原沒反應
  （動的是另一帶）」的假死 UX。

## ③還有什麼沒解決

- PR-6 blocks／PR-7 即時預覽／PR-8 UI 對位；群組帶 picker。

## ④下一個人要注意什麼

- 所有新 op 一律 `applyOp(band, mutator)`——漏帶 band 會 TypeError（好事，
  fail-fast）。
- 共用 BOOTSTRAP mock 加了群組 ⇒ 新測試對 hero 這類同名節點要
  getAllByRole＋帶序註釋（ED2/ED8 這輪的修法）。
