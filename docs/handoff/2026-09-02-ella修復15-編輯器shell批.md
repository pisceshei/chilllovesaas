# Handoff：Ella 修復 PR-15（2026-09-02）

## ①我改了什麼

編輯器 shell 批四項：行動版預覽切換、作用中 badge、undo/redo 快捷鍵、
選中狀態 URL 化＋設定面板底部移除區段。逐檔＝worklog。

## ②為什麼這樣改

- 全部有 24 §1.1 頂列逐字實測背書（badge 原文、📱 行動版、URL 深連結形）。
- 快捷鍵經 undoRef/redoRef：effect 掛一次、閉包不過期；輸入控件內讓原生
  文字 undo 先行（不搶 field 編輯）。
- URL section 值用我方 section 鍵不用本尊 `template--N__key` 實例形——
  與 83 §12.3 已登記的 id 語義差異同一條，不另立形。

## ③還有什麼沒解決

- 編輯器 parity 軸剩餘 ⑤a/⑤c/⑤e/⑤f 與 custom_css——見 worklog Pending。

## ④下一個人要注意什麼

- 🔴 `gh pr merge` 後本地 `origin/main` ref 是舊的——切新分支前必
  `git fetch origin main`（本輪實錘：從舊 main 開工 anchors 全失配）。
- syncStateParams 用 functional setSearchParams＋replace:true——別改回
  `setSearchParams({...})` 全量覆蓋形，那會把 template param 洗掉。
- 新增會改 draft 的操作記得同時 bump draftsVersion（PR-11 的規則）＋
  視需要 syncStateParams。
