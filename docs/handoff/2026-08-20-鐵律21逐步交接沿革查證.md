# Handoff — 2026-08-20：鐵律 21 逐步交接沿革查證

## ① 我改了什麼

本步驟處理的問題是：現行 `CLAUDE.md` 只要求「每完成一個可獨立驗收部分」寫 worklog，並在
「每次工作結束前」寫 handoff；兩者都沒有要求研究、測試、驗收、阻塞等中間決策節點在進入
下一步前各自留下交接。已完整讀取兩份根規則，並用 `git log -p -S` 取得原始沿革 commit，
再以 `rg` 列出規範、方案、決策與終態文件的影響面。

本步驟的純文檔閘門以 native Python 3.12、Ruby 3.4.10 與 Git for Windows Bash 逐支執行，
快照輸出為 `GATE_ENUM_COUNT=17`、`GATE_FAILED_COUNT=0`。完整 `bin/ci` 另有明確失敗終態，
Rails specs 的輸出是本機 MySQL `127.0.0.1:3306` 拒絕連線，故沒有宣稱完整 CI 通過。
使用者其後指出既有 MySQL 可喚起；後續處置與結果另見
`docs/handoff/2026-08-20-鐵律21本機MySQL與閘門復驗.md`。

配對 worklog：`docs/worklog/2026-08-20-鐵律21逐步交接沿革查證.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：既有「工作結束前寫 handoff」自然涵蓋每一步。** 沿革 diff 顯示觸發點明確是整次
  工作結束，不是每個中間節點；只沿用原句無法滿足使用者本輪「每一個步驟」的裁定。
- **被推翻：只改 `CLAUDE.md` 就會生效。** `AGENTS.md` 管 handoff 的寫法，階段一'執行方案
  管實際迴圈；若不同步，規則生產者、操作消費者與 PR #61 終態會再次分裂。
- 本步驟只記錄已取得的倉庫內部證據；使用者裁定證明專案選擇，不拿來替代 GitHub 或執行結果。

## ③ 還有什麼沒解決

- 鐵律 21、D35、`AGENTS.md` 寫法規則與階段一'迴圈尚未落地。
- 本步驟尚未產生新 head 的 GitHub 驗收證據；提交與推送後必須重新綁定實際 head 檢查。
- 本步驟當時未取得完整 Rails 成功證據；後續環境復驗已有獨立 handoff，不能把本段歷史快照
  改寫成初次執行即成功。
- Claude 對修改 workflow 的 PR 可能再次 validation skip；若發生只能記「證據未取得」。

## ④ 下一個人要注意什麼

1. 下一步要保持既有 handoff 四段與 worklog 三段，不得以新鐵律取代其中任何一份。
2. 每步 handoff 至少要寫清楚：問題與證據、實際動作與產物、驗證結果、未解事項、下一步前置與
   紅線；失敗、阻塞或純讀取步驟同樣不能省略。
3. 要明定非遞迴邊界：同一具名步驟內的命令逐項記錄在一份 handoff；寫該 handoff、跑閘門與
   提交是該步驟的收尾，不再觸發無限新增 handoff。
4. PR #61 命中鐵律 18.3，任何新增提交都只能推送驗收，不能由互動式 Codex 代行合併。
