# 交接 · D59 中性階換值（2026-08-28）

輸入 base＝`1e88874`（＝`origin/main`，D58 #171 已合併）。分支 `m2/decision-a2-neutral-palette`。

配對 worklog＝`docs/worklog/2026-08-28-D59中性階換值.md`。

## ① 我改了什麼

中性階 **20 顆** token 換成本尊實測值，**591 個消費點**受影響——本 session 影響面最大的一包。
另把 `--text-inverse` **拆成兩顆**。

## ② 為什麼這樣改

「整體 UI 不協調」三層診斷的**最後一層**：我方中性階帶藍色偏（`--text` 飽和 **13.3%**、
`--surface-inverse` **10.3%**），而本尊中性階飽和度**一律 0.0**。換值後我方最大值也是 **0.0**。

**被推翻的假設**：
- ~~`112` §2.1 的 16 項就是全部~~ → 系統性掃整個中性區塊另外找到 **2 顆**
  （`--link` `#2a5bd7`→`#005bd3`、`--text-inverse` `#fff`→`#e3e3e3`）。
  **不要信計畫表，逐顆對回 `111` 的節號。**
- ~~`--text-inverse` 一顆就夠~~ → 本尊有**兩顆不同值**：`text-inverse` `#e3e3e3`（深中性面板）
  與 `text-brand-on-bg-fill` `#fff`（brand 填色）。我方一顆同時服務三種角色
  ——**與 `--t-lg` 同型**。

## ③ 還有什麼沒解決

1. 🔴 **未做 live 視覺並排比對**（鐵律 13.1 三寬度）。591 個消費點的顏色都變了。
2. ⚠️ **設定區導覽的 hover／selected 在本尊是對調的**（`81` §8.4：selected `#f3f3f3`、
   hover `#f1f1f1`，hover 比 selected 深）⇒ 那一區**必須單獨複驗**，不能只信 token 名。
3. ⚠️ **`--text-on-brand` 用在 ai／focus 填底是近似**——本尊那兩族的 on-fill 未取得。
4. ⚠️ **次級中性 token 未對照**：`--surface-chip`／`--surface-count`／`--text-nav`／
   `--text-nav-2`／`--text-quiet`／`--scroll-thumb`／`--skeleton-*`／`--grid`（本尊對應物未取得）。
5. 🔴 **包 C-3 仍未做**：原型 93 處硬編字重（32 域外）、11 處硬編字級、26 處無單位行高。

## ④ 下一個人要注意什麼

- **入口**：`docs/DECISIONS.md` **D59** → 原型 `:root` 中性階區塊的註釋。
- 🔴 **`--surface-selected` 從品牌藍變中性灰**（`#f0f5ff` → `#f1f1f1`）。
  這是視覺上最明顯的一項——**本尊的選取態不用品牌色**。若有人回報「選取列看起來不對」，
  先確認那是不是預期內的 1:1 對齊。
- 🔴 **`--text-inverse` 現在只用於深色中性面板**（12 個消費點，全部在 `--surface-inverse`
  或近黑 rgba 上）。**飽和填色上的字用 `--text-on-brand`**（10 個消費點），
  critical 填色用 `--sem-critical-on-fill`。加新的深底元件時要選對。
- 🔴 **`--text-disabled: #b5b5b5` 對白底只有 2.05，那是本尊自己的值，不要「修好」它**
  ——WCAG 1.4.3 明文豁免停用控件。
- 🔴 **批次改檔不得在 `re.finditer` 迭代中修改被迭代的字串。** 本輪我這樣寫，
  而替換字串每次長 1 字元 ⇒ 偏移累積漂移，**切掉了三個選擇器的左大括號**
  （`.ai-badge{`→`.ai-badgewidth`、`.ai-send{`→`.ai-senwidth`、`.annot-bar{`→`.annot-bposition`）。
  `lint-prototype.py` 的大括號平衡檢查抓到（`{=1049 }=1053`）。
  ✅ **固定處理**：一律「錨定字串 ＋ `assert count == 1` ＋ `replace`」，不用偏移改寫。
- **本包純 CSS ＋ 文檔**，未觸及 18.3 類檔案 ⇒ 依 D40，兩 job 綠即合併。
