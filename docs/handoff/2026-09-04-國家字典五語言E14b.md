# Handoff：E14b 國家字典五語言在地名與本尊觀察序（2026-09-04）

配對 worklog：`docs/worklog/2026-09-04-國家字典五語言E14b.md`。規範：`docs/dev/e13-theme-editor-preview-parity.md` §1 #4；鐵律 22。

## ① 我改了什麼

- 目標：使用者裁定「與本尊一模一樣」——`all_country_option_tags` 的在地名補齊（原本只有 zh-CN）。輸入 ref：main `4c00a724`。
- 做了：在真店（使用者授權）安裝 Translate & Adapt、新增並發布 zh-TW／en／fr／ja，並建立 美國／香港／日本／欧盟 四個市場（全程無費用提示；
  worklog Pending 列 id）；用 Section Rendering 抓五語言 cart drawer；字典 v2 含每語言在地名與觀察序；引擎依觀察序輸出、退 en；
  規格 W4／W4b／W4c；D80 旁證（語言前綴、hreflang、市場無自身前綴）入 external-facts §G21。
- 證據：五份 SRA 回應各 238 option（scratchpad `hoko_cart_drawer_sra_{zhcn,zhhant,en,fr,ja}.html`）；`/zh-hant/` 200、`/zh-tw/` 404、`/en/`／`/fr/`／`/ja/` 200、
  `/zh-hans/` 404；hreflang 三條逐字（§G21）。

## ② 為什麼這樣改

- 順序存觀察序而不是執行期排序：本尊各語言用不同 collation（en Åland 在 A 後、ja 依讀音、zh 碼位），沒有官方規則可引，複製觀察值最忠實。
- en 當退路：本尊英文店面的在地名可與 value 不同（`Hong Kong SAR`），先前「退英文名＝value」不是本尊形。
- 被推翻的假設：「本尊繁中 URL 前綴＝`/zh-tw`」——實測 `/zh-hant`；「Traditional 的在地名可由 ISO／CLDR 推」——本尊用自家名（`象牙海岸` vs CLDR `科特迪瓦`）。

## ③ 還有什麼沒解決

- 市場費用的官方句未取得（表單／結果頁／Markets 頁皆無提示；若帳單出現市場費用即回報使用者）。欧盟市場有 13 個地區缺運費（Shopify 頁面提示）。
  其他語言的在地名未取得（退 en）。`__root__` 兩項與 §3.81 其他 V 照舊。

## ④ 下一個人要注意什麼

- 重抓：語言已在真店發布，直接 `curl "https://hoko.vip/{zh-hant|en|fr|ja}/?section_id=sections--19763396870247__cart_drawer_PFLQy3"`（live 主題的 group id；
  副本主題是 19774792499303），再跑 scratchpad `gen_country_option_tags_v2.py`。
- 加語言＝補 `labels.<本尊碼>`＋`sort.<本尊碼>`（每國都要有，`render` 直接以它排序）。
- D80 尚未裁定；§G21 的 URL 形是裁定輸入，不是已生效規則。
