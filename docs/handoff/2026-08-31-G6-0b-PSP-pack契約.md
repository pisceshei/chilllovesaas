# Handoff — G6-0(b) PSP pack 契約落地（2026-08-31）

## ① 我改了什麼
branch `g6/psp-pack-contract`，base＝main `694fe24`。範圍＝總方案 G6-0(b)：65 號增
R7/A6c/A7、V-132 結案、69 號 19.5 更正、真 pack airwallex/paypal 落 limits、Money 三
分流＋入向閘門＋往返自檢。清單見同名 worklog；一手證據 URL＋逐字引句都在
limits.yml pack 註釋與 65 §D.4／A7。

## ② 為什麼這樣改
- R7 不與 R6 共用型別：單位語義同（主單位）但 wire form 不同（number vs string）——
  共用會讓宣告錯誤靜默通過（65 §A R7 註）。
- A7 覆蓋允許 0/1 而基準維持 A6b（≥2）：不對稱是刻意的——覆蓋幣別的湊整風險由
  A6c 在轉換層 raise 擋（pack.rb 註釋全文）；D16 不放寬。
- 被推翻的假設：①69 號「Airwallex＝decimal_string」（一手複驗推翻，19.5 更正）；
  ②「Airwallex per-currency 位數未給」（payments-and-fee-rounding 頁有逐字 20 幣清單；
  ⚠ payouts 產品線另一張表含 3 位小數與 SAR/KES——**射程不同，不得混用**，65 §D.4 註）。

## ③ 還有什麼沒解決
worklog Pending 四項；重點＝PR B（CLAUDE.md 鐵律 3 摘要句＋scripts C2 後綴）走 18.3
人工合併，G6-1 adapter 依賴它。

## ④ 下一個人要注意什麼
- G6-1 adapter：金額出向只走 `to_psp_amount(psp: :airwallex)` 得 PspNumber，
  `to_payload` 回 BigDecimal——**JSON 序列化必須原文注入數字字面**（BigDecimal#to_json
  吐字串、to_f 禁）；webhook 解析 `JSON.parse(raw, decimal_class: BigDecimal)` 後走
  `Money.from_psp_amount`（Float 恆 TypeError）。
- enable pack 前置：sandbox charge＋webhook 實測＋65 §K.8–9 可觀測落地；
  `enabled: false` 是最後一道把關，不得先翻。
- 重跑：`bundle exec rspec spec/models/money_spec.rb spec/models/psp_amount_matrix_spec.rb spec/models/psp`
  ＋`ruby scripts/check-money-boundary.rb`。
