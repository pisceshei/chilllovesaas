# Handoff：Ella 修復 PR-26（2026-09-02）

## ①我改了什麼

編輯器 sections 樹 HTML5 拖曳重排（同帶；moveSectionTo 任意位置 op）。
逐檔＝worklog。

## ②為什麼這樣改

- op 走 applyOp＝一行接管 undo/改即見/儲存——編輯器所有 op 的既定紅線。
- 跨帶 dragOver 不 preventDefault＝瀏覽器原生「不可放」游標，零自寫狀態。

## ③還有什麼沒解決

- block 子層拖曳；視覺回饋；插前/插後細粒度（V）。見 worklog Pending。

## ④下一個人要注意什麼

- jsdom 的 fireEvent.dragStart/drop 不帶 dataTransfer——實作刻意用
  dragRef（React ref）不用 dataTransfer，測試才可直驅；改用 dataTransfer
  要換測試策略（真瀏覽器）。
- moveSectionTo 的 targetIndex 語義＝「先移除再插入後的位置」——
  向下拖時目標 index 已因移除而左移，這是刻意（放到目標列當前位置）。
