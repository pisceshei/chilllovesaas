# Handoff — S7 狀態機交互（2026-08-28）

工作包＝S7（三包指示中的第一包；PR-C 經查已完成、S8 接續）。
worklog＝`docs/worklog/2026-08-28-S7狀態機交互.md`。
輸入 ref：`main` = `17803d2`（S6c-2）。分支 `m2/s7-status-machine`。

## ① 我改了什麼
批量狀態動作（列表第三面）＋排程 Active banner。實測 82 §19。測試 +4。

## ② 為什麼這樣改
- 頂層鈕動態＝本尊實測；混合規則 ours（標明可推翻）。
- 逐筆 productSet｛id, status｝＝重用「缺席保持現值」契約，status 變更自動接 PR-C 事件。
- banner 控件不禁用＝本尊 F1-② 形態（排程照存、到點才閘）。

## ③ 還有什麼沒解決
worklog Pending 全列；最大者＝混合選集規則未取得、批量無進度指示。

## ④ 下一個人要注意什麼
- 🔴 **不要把 banner 做成禁用控件**——本尊語義是「照樣能存」，閘門在 PR-C 到點消費者。
- 🔴 批量動作若改成並發送，注意後端逐筆鎖與事件順序（現為順序送，刻意）。
- 🔴 Python 插入程式碼**不得用空字串當錨**（空錨＋`\n`＝檔案第一個換行）；
  插完 `head` 驗檔頭。
- 下一包＝S8（事件與快取失效）：outbox topic 對偶／webhook 未來包邊界見
  分步方案 §S8 與 PR-C 裁定書 §3.3。
