# handoff — 商品前台補完包（2026-08-30）

## ① 我改了什麼
缺口分析（#199）切分 1＋A2 選中半邊：drops 庫存感知／選項值 drop／?variant=
佈線／position 排序／preload 擴充。詳見同名 worklog Changes。
輸入 ref：main=d7535d9。

## ② 為什麼這樣改
使用者裁定「按照研究和分析來做」。A3 的 selected 沒有選中源就是空殼，
故把 A2 的 ?variant= 佈線併入本包（Section Rendering 端點面仍留包 33）——
對切分的偏離已在 PR 描述聲明。OptionValueDrop 的 == 覆寫來自 Ella
`color == value` 消費形實測（fixture product-variant-options.liquid:36-40）。

## ③ 還有什麼沒解決
三個未取證格（壞 variant 值／全售罄 fallback／compare_at nil 混值）取安全側
＋註記，待 CLI 探針對表。CLI 裝置流等使用者完成人手認證（碼 VPRT-TZDB，
紅線：CAPTCHA＋密碼不可代操作）。

## ④ 下一個人要注意什麼
- drops 一切關聯讀取都假設 renderer 的 with_tenant＋preload 已就位；新增
  drop 屬性先看 page_renderer 的 includes 有沒有餵。
- 別把 SP 格的 stub 改成半真值——那組測試就是為此轉紅的。
- CLI 認證成功後：theme pull 做 fixture diff、push 未發布副本跑
  `{{ product | json }}`／錯誤語義探針（83 §10 解鎖表）。
