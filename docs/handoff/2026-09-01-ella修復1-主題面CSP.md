# Handoff：Ella 整合修復 PR-1 主題面 CSP（2026-09-01）

## ①我改了什麼

使用者指正後的六軸診斷第一刀。base＝main（20c 後），分支
`ella/csp-theme-relax`。ThemeCsp concern＋兩 controller 掛載＋C1-C3 spec。

## ②為什麼這樣改

- 根因鏈：主題整包樣式與互動走 inline ⇒ 嚴格 CSP＝主題面全滅。放寬只限
  主題渲染面；nonce 必須同時關（規範行為，concern 檔頭②）。

## ③還有什麼沒解決

- 圖片 0 張／bodyBg 透明／divided-by-0／編輯器 parity——後續包
  （六軸診斷 workflow 結果定序）。

## ④下一個人要注意什麼

- 新增主題渲染 controller 必 include ThemeCsp；admin 面永不掛。
- 驗 CSP 生效要看 script-src 段**沒有 nonce**（有 nonce 則 unsafe-inline
  靜默失效——瀏覽器不報錯只忽略）。
