# Handoff — D70 頂欄 hover（視覺對齊弧收尾，2026-08-28）

工作包＝hover 量測與對齊 ＋ 兩項擱置裁定落地。
配對 worklog：`docs/worklog/2026-08-28-D70頂欄hover.md`。
輸入 ref：`main` = `989218b`（D69 已合併）。分支 `m2/d70-topbar-hover`。

## ① 我改了什麼

暗域 scoped 覆寫兩條（icon-button／store-chip 的 hover → `--surface-hover`，圖示色釘回
`--icon`），原型同構；D70 記錄兩項擱置裁定；`91` §3.43 登記 active 態與座標框架坑。

驗證：兩份檔各自真觸發實測，暗域三控件 hover 全 #222、亮域未被波及（#f7f7f7）。

## ② 為什麼這樣改

- 本尊 hover 全部收斂到暗域 `surface-hover`（#222），機制是外層包裝的能力類＋`:hover`。
- **不改基礎規則**：亮域 7 個用點的 `--surface-2` 值恰等於本尊亮域 hover 值
  （名稱倒置未解，`91` §3.39 W-3）；改基礎規則反而更遠。
- 搜尋鈕 hover 比 rest 更暗是本尊實測值，非筆誤。

## ③ 還有什麼沒解決

- `:active` 按壓態（§3.43 W-1）；名稱對映表（§3.39 W-3）；
- 兩項擱置：四族 rem（重啟條件＝實際回饋）、`rwd-check.mjs`（下一個大 UI 輪首包，18.3）。

## ④ 下一個人要注意什麼

- 🔴 **`computer` 的座標框架是最近一次截圖的像素**：頁寬 2560、截圖 1568 時比例 0.6125，
  用 CSS 座標 hover 會**靜默落空**。固定處理＝先截圖、座標乘比例、
  操作後斷言 `el.matches(':hover')` 才讀值。
- 🔴 **本尊的 CSS 不只 main-\*.css 一張**：頂欄元件規則在 `render-common-*.css`
  （CSS modules 分塊）。找不到規則先列全部 `styleSheets.href` 逐張 fetch 統計關鍵字。
- **下一步＝S6c 系列 popover**：`Popover` 原語已在 main（#161），
  分步方案＝`docs/plans/2026-08-26-發布與可見性-分步執行方案.md` 的 S6c 節。
