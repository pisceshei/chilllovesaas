# Handoff — G6-0(a) 訂單成立（2026-08-31）

## ① 我改了什麼
branch `g6/order-creation`，base＝main `711cf90`。範圍＝已批准總方案（
`C:\Users\pisce\.claude\plans\stateful-squishing-orbit.md`）G6-0(a)：15-F5 manual 形
訂單成立全鏈（服務/四 model/counter/兩路由/thank-you/按鈕點亮）。清單見同名 worklog。

## ② 為什麼這樣改
- commit 掛「訂單成立」不掛付款回呼（F5 標題更正的根因：COD/bank deposit 不佔庫存＝
  超賣）；manual 形先行因 demo 唯一可走真流程。
- 鎖序 counter→inventory 全專案固定（F5 ⚠️坑 2）；扣庫存全走條件式 UPDATE 不
  read-modify-write（F4.1(d) 同紀律）。
- 冪等鍵＝`order-<checkout.token>` per-checkout 穩定＋指紋只含 token：買家改付款
  方式後重試不觸發 PARAMETER_MISMATCH（付款重驗在 delivery/payment 端點各自把關）。
- 被推翻的假設：無（規格逐句照 F5 修訂史，21 輪更正全數吸收——open 非 active、
  counter 鎖序、authorize 金額分支見服務註釋）。

## ③ 還有什麼沒解決
worklog Pending 六項（PSP 形／確認信／admin 訂單面／jurisdiction 對映／50 執行緒
壓測／auth flake 觀察）。

## ④ 下一個人要注意什麼
- G6-1/G6-2 的付款完成回呼**必吃本服務**（雙路徑先到先贏都冪等：return handler 與
  webhook 各自帶同一把 `order-<token>` key 呼叫即可，Guard replay 保恰一單）；
  PSP capture 形改 build_manual_transaction! 為分支（金額入向 to_storage）。
- markAsPaid 路徑要補發 ORDERS_PAID＋翻 transaction pending→success＋
  financial_status paid（16 §F4.4；Topics 常數已備）。
- 重跑：`bundle exec rspec spec/requests/storefront_order_creation_spec.rb`。
- demo 驗收：bt3 部署後全流程（加車→運送→付款→完成訂單→thank-you）可親手走通。
