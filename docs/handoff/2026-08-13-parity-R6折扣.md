# 交接：parity R6 折扣（2026-08-13 第七場）

> 本輪 worklog（1 份）：`2026-08-13-parity-R6折扣.md`（研究產出 `docs/research/75`）
> 輪次標準照 R4 四層；CSS 同 73 §5 系統未重複量測。

> 🔴 **本輪收尾時使用者裁定入規**：「每做一個階段必須按鈕級完全複製功能邏輯與交互邏輯＋研究所有
> CSS＋結合 shopify 說明文檔，和 shopify 保持一致性」——已寫進 **CLAUDE.md 工作方式首條**、
> **AGENTS.md 開工前 §4**（實作階段同樣適用）、**71 號 §D**（四層硬性標準＋使用者原文）。
> 此後任何階段缺一層即打回，與 worklog/handoff 紀律同級。

## ① 我改了什麼

1. **R6 全流程**：實測（型別選擇器＋四型建立式 JS 全欄位抽取）＋help 六路 → 75 號研究檔 →
   71 §F 十一條（2 BUG＋4 MISS＋1 DOC 全修、4 V 遞延）→ discountPage 全面重寫 →
   limits.yml discount +3 鍵 → lint 0/煙霧綠/console 0。
2. **兩個 BUG**：組合規則「同類互斥」過時（2026 三勾選開放，僅運費×運費硬禁）；「所有商品」
   發明選項移除。R4 砍過發明控件，R6 第一次砍**過時規則**——形態不同：不是「本尊沒有」，
   是「本尊改版了而我方規格停在舊版」。
3. **limits 空白鍵補**：草稿訂單折扣三鍵；46 號既有 12 鍵驗證仍準（critic 質疑被雙源推翻）。

## ② 為什麼這樣改（含被推翻的假設）

- 🔴 **規則會改版**：22 §4／17 號的「同類別互斥」在 2026 已被推翻（help 原文「每筆訂單可套用
  多項折扣」）。**研究文檔的規則性斷言要標註取證日期**，parity 輪逐一過期驗證——這條教訓比
  單個 fix 值錢。17 號裁決矩陣複核已排 M5 前置（71-R6-V1）。
- 🔴 **critic 與既有研究衝突時看出處等級**：critic:1 說「分群 5/100 復現失敗不採信」，但 46c:690
  （help）＋eligibility 頁雙源在案——單次抓取失敗不推翻既有雙源。寫進 75 §2。
- **資格閘門不進原型**：組合選項的 Checkout Extensibility 閘門是本尊自身的架構債（checkout.liquid
  淘汰期），我方 M2 起點就是新架構——原型全開放、DOCS 記語義，不模擬別人的技術債。
- **JS 全欄位抽取常態化**：四型表單 4 次 navigate+JS 就收完（radio/checkbox/select 值域＋原文），
  比逐屏截圖快 5 倍；截圖只用於版面與右欄結構。
- 煙霧測試兩個 FAIL 均為選擇器誤中（aria-expanded 撞導航/摘要 bullet「無最低購買要求」撞負向
  檢查）——**負向斷言要用 data-doc 錨點不要用文字 includes**，已在複驗中改用。

## ③ 還有什麼沒解決

- 71-R6-V1（17 號矩陣複核，M5 前必做）／V2（跨幣別捨入→65 §H 候選）／V3（列表頁空態擋實測）／
  V4（Collabs 口徑 R11）。
- 22 §4「同類互斥」表述過時——R14 收斂輪回寫 22 號（已登記於 worklog Pending）。
- R7–R14 未開；下一輪 **R7＝訂單線**（列表佔位轉真/詳情補齊/草稿建單器/運送標籤批次流/棄單）
  ——R5-V1（saved views 歸屬）在此輪一併裁定。
- PR #12 等合併；B-8（RTE）等裁定；POS 範圍衝突 R13 前要答案。

## ④ 下一個人要注意什麼

1. 輪次紀律三件套＋R4 四層標準不變。
2. **引用 17/22 號折扣規則前先對 75 §3**——組合矩陣以 2026 版為準；同品項多折扣=Plus 標籤機制。
3. 折扣值/門檻的多幣別口徑（預設幣別儲存+結帳換算）落表時走 65 號 Storage 域，捨入規則未定
   （V2）前不要自己發明。
4. 負向斷言用 `querySelector('[data-doc="…"]')` 判卡片存在性，不要用 innerHTML includes 文字。
5. 折扣「匯入」不存在於本尊（export only）——有人提需求時標「超集功能」走裁定，別默做。
6. 型別 immutable（建立後不可改）——資料模型 type 欄位設不可變。
