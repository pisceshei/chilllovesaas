# Handoff：G4 步 16e1 檔案覆寫層（2026-09-01）

## ①我改了什麼

20 步方案步 16 第六包。base＝main `b4c5213`，分支 `g4/step16e1-file-overlay`。
theme_file_overlays 資料層＋OverlaySource＋AST 租戶安全＋themeFileUpsert。
逐檔＝worklog Changes。

## ②為什麼這樣改

- storage 目錄不可變（15a 內容定址）⇒ 一切編輯走 DB overlay；templates/
  settings_data 已各有覆寫層 ⇒ 白名單拒收（雙真相源禁令）。
- overlay 檔 per-row 版本鍵而非全 theme 換鍵：保未覆寫檔的共用快取效益。
- without_tenant＋顯式 shop_id：resolve 在租戶 context 外被叫（F1 實紅過）。

## ③還有什麼沒解決

- 16e2 UI／16e3 檔案操作；版本歷史／binary asset／刪檔 tombstone（91 §3.71）；
  Edit code 親點 V 項；生產煙測待部署。

## ④下一個人要注意什麼

- 🔴 新增 Sources 的呼叫端若繞過 resolve 直接 new FileSource，overlay 就靜默
  失效——一律走 Sources.resolve。
- OverlaySource 記憶 stamps 是**單實例生命週期**——長駐快取請重新 resolve。
- 16e2 的檔案讀取用 theme.files body（已走 source）＋themeFileUpsert 寫回；
  templates/*.json 在 code editor 顯示但導去模板編輯器（不可經 file upsert）。
