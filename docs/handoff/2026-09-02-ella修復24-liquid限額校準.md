# Handoff：Ella 修復 PR-24（2026-09-02）

## ①我改了什麼

Liquid resource limits 依 bt3 實測校準（assign 1M／render_score 2M）＋
真 Ella 整頁回歸格 EG1＋theme_crud_api_spec 的 zip DOS-time 釘死。
逐檔＝worklog。

## ②為什麼這樣改

- 量測法：Environment 凍結 ⇒ runner 內重建大上限環境＋prepend 攔
  ResourceLimits 實例讀真用量——這套插樁法可重用（worklog 記全）。
- EG1 場景加重到 9 媒體：gallery 陣列操作平方成長，輕場景殺不動突變
  （mutation-not-red 三分類的活例）。

## ③還有什麼沒解決

- multipart 截斷底層未定罪（時窗過、無法復現）；50+ 媒體極端頁未壓測。
  見 worklog Pending。

## ④下一個人要注意什麼

- 🔴 改 liquid_*_limit 前先跑 EG1＋想清楚 DoS 面——這組值是「真 Ella 需求
  ×10」不是隨手放大；再撞線先量（worklog 插樁法）再調。
- 🔴 spec 產 zip 一律釘 DOSTime（時戳 bytes 進 multipart body 的時間相依
  假紅——本地 Windows 限定、CI 無感、最難追的那種）。
- 🔴 連環 PR 的分支基底：`gh pr merge` 後必 `git fetch origin main`——
  本日第二次踩（記憶 fetch-before-branch）；EG1 首輪 youtube 斷言紅就是
  基底缺 PR-22。
