# Handoff：Ella 修復 PR-2 真圖鏈（2026-09-01）

## ①我改了什麼

六軸診斷 image 軸實作。base＝main（#265 CSP 後），分支 `ella/real-image-chain`。
媒體端點＋url 鏈＋settings resolver＋nil 防線＋binread。逐檔＝worklog。

## ②為什麼這樣改

- resolver 走 context register 而非建構子注入：SettingsDrop 有四個建構位
  （global/scheme/section/block），register 形零改動全覆蓋。
- 空值→nil 不許動（官方契約＋除零軸取證）；佔位只給「非空但查無」。

## ③還有什麼沒解決

- PR-2b demo 種子（視覺成立的另一半）；縮圖 variant；PR-3 filter 覆蓋批
  （stylesheet/javascript tag 聚合等）；編輯器四連發。

## ④下一個人要注意什麼

- FileSource#read 的編碼雙全形：改它前先想「Liquid 中文插值」與「png 位元
  組」兩端——單邊優化必炸另一邊（本輪兩次實錘）。
- 媒體 URL 只認 id；filename 段純可讀性。改 URL 形要同步 MediaUrl 與 route。
