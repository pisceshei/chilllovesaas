# Handoff — 包 30：Liquid 主題引擎生產化（D77）

## ① 我改了什麼

輸入：main=517763e（S10 後）；分支 m2/p30-liquid-productionization。
交付六項見 worklog；rspec 引擎/預覽/bumper 16 格＋前端 3 格全綠；突變七格紅。
測試店側無寫入（本包純代碼；/themes 頁複驗僅唯讀導航）。

## ② 為什麼這樣改

- 零 migration：五表 M0 早建（首稿起新 migration 撞表才發現——「工作卡叫你建模型
  不等於叫你建表」；db:rollback 誤退 price_lists 後靠 db:migrate 復原）。
- 檔案外源＋DB 覆寫：對齊 M0「主題是資料」裁定與 25 §6；編輯器只需寫兩張表。
- 預覽走 Lookup（ours）：本尊預覽站是商家視角（82 §20.4），但我方預覽的
  存在理由是「發布前看買家將見」——語義選擇記 91 §3.48，要商家視角另開分支。
- 被推翻的假設：①「PoC assigns 傳法可直接沿用」——{% render %} 隔離吃掉
  普通 assigns（第四反例）；②「lax 模式下壞檔案不會炸」——parse 期 SyntaxError
  照樣 raise（Ella 的 {% render block %}），要自己 rescue。

## ③ 還有什麼沒解決

91 §3.48 七條 ⚪；上傳管線／編輯器／公開店面各屬後續包。無阻塞。

## ④ 下一個人要注意什麼

- 改 drops.rb 要重啟 dev（Zeitwerk ignore）；新增 drop 屬性前先看 miss 遙測
  （ThemeEngine.miss_report）。
- 🔴 任何渲染路徑傳 assigns 一律 `runtime.build_context`（static_environments）；
  直接 `tpl.render(hash)` 的值進不了 snippet。
- 🔴 Liquid `product` drop 永不加 `status`（specs/93 §D；E2 格守著）。
- 包 33 接手點：PageRenderer 的 url_prefix／host 參數已就位；RoutesDrop 前綴
  一次到位（B11）；robots 紀律（B13）在 controller 層做，引擎不管。
- 包 32/34 接手點：LocalizationDrop/RequestDrop 的參數已真值化，把 shop_locales
  的資料接進 Runtime.initialize 的 language/available_languages 即可。
