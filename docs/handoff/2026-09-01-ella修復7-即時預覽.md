# Handoff：Ella 修復 PR-7 即時預覽（2026-09-01）

## ①我改了什麼

editor 軸 high ③第一階段。分支 `ella/editor-blocks-v2` 之上（合併後
cherry-pick）。逐檔＝worklog Changes。

## ②為什麼這樣改

- draft 走 server 渲染而非 client 猜 DOM：Liquid 輸出只有引擎知道；SRA
  單 section 端點現成（包 33）。
- debounce 400ms＋失敗靜默：預覽是增強面，儲存語義不動。

## ③還有什麼沒解決

- 非 index 語境／佈景設定即時層／結構操作 live——下一輪；
  chill.deals 逐位對表（使用者 2026-09-01 深夜新指令：完全一樣、禁自行
  發揮）艦隊出發中。

## ④下一個人要注意什麼

- 路由檔插入一律錨完整語句（本輪 regex 插進跨行語句中間＝routes 全掛）。
- draft_section 不走頁快取（admin 面）；濫打防線靠 staff 閘＋debounce。
