# Handoff — 2026-08-20：PR #61 Codex `44ebd39` 延遲意見修復

## ① 我改了什麼

本步實作 exact-head review `4980284182` 的兩則延遲 inline。P1 不照抄「補英文證據」後直接
保留我方映射，而是依官方英文頁、D23 與 `git log -p` 的查證結果，保留 SHOPLINE 輔助研究、
撤回 `single_purchase | accumulated`、事件鏈與 limits 已選案宣稱；Shopify 實測或使用者差異
裁定前，S-14 禁止把它們寫入 `specs/110`。P2 把 35／16／37 標為 2026-08-20 快照，加入
三個 fail-closed Ruby 集合導出命令，Q-1 開工必重跑且 DoD 不使用固定數字或文件行數。
正式閘門輪從 setup 起 29/29 綠；MySQL `db:prepare`、Rails 284/0、前端 5/0、doc-claims
warning 0、workflow 33 個 `run:` 均有實跑證據。GitHub Markdown API 亦證實 Q 表結構完整。

配對 worklog 為
`docs/worklog/2026-08-20-PR61-Codex-44ebd39延遲意見修復.md`；修法前證據鏈在同主題「查證」
worklog／handoff。

## ② 為什麼這樣改（含被推翻的假設）

- 被推翻：SHOPLINE 會員分級只有中文來源。官方英文頁確有兩種門檻逐字；缺口不是翻譯，
  而是競品規則被直接發布為我方下游輸入。
- 被推翻：Q-1 的固定數字無法驗證。現值確實可導出為 35／16／37；真正錯誤是拿 `wc -l`
  驗一個不相關的 105 行總數，且沒有把集合變動納入失敗條件。
- 未採：把 SHOPLINE 研究整段刪掉。它仍可作 S-14 的輔助研究種子，只是不得跨越 D23 的
  Shopify 基準與使用者選案閘門。

## ③ 還有什麼沒解決

- 本步尚待 commit 後 doc-claims 與 GitHub 新 head 驗收；舊 head `44ebd39` 的結果已失效。
- inline `3819608325`／`3819608329` 尚未回覆及 resolve。
- Claude validation-skip 的 workflow 根因沒有在本步修改；依鐵律 17.2 保持範圍不擴散。
- PR #61 尚未合併，遠端 `pr61-last-push` tag 仍指向舊 head。

## ④ 下一個人要注意什麼

1. 三式在本輪已輸出 35、16、37；後續驗收的是集合可導出與連續性，不是硬編碼預期數字。
2. 完整閘門與 15.1／15.2 後才能 push；push 後回覆 body 要從 API 逐字拉回驗證，成功才 resolve。
3. Codex summary 可能早於 inline；最後合併前必須再拉 GraphQL 全 threads，不能只看 summary。
4. 使用者已直接觸發本次 18.3 人工合併；四條件和 exact-head guard 齊全後執行 squash merge，
   不需再次請示，但任何條件不齊都必須 fail-closed。
