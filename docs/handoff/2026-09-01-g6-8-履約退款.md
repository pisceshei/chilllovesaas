# Handoff：G6-8（步 5）履約與退款線（2026-09-01）

工作包＝20 步計畫的步 5。分支 `g6/step5-fulfillment-refund`，base＝main `d3565e3`。
配對 worklog＝`docs/worklog/2026-09-01-g6-8-履約退款.md`；
dev doc＝`docs/dev/g6-fulfillment-refund.md`。

## ① 我改了什麼

mutation 逐支＝fulfillmentCreate／fulfillmentTrackingInfoUpdate／fulfillmentCancel／
refundCreate（複驗：`grep -c "mutation: Mutations::Fulfillment\|mutation: Mutations::RefundCreate" app/graphql/types/mutation_type.rb`）＋ Order.suggestedRefund ＋ 出貨卡與退款頁 UI。
16 §F5.1 軟上限的完整落地（條件式 UPDATE＋兩碼分流＋over_refund 權限＋物化欄回填）；
Airwallex /pa/refunds/create（失敗補償累計欄）；建單物化 FO＋回填。
驗證：rspec 全套綠（worklog 記各輪確數）；pnpm 304；typecheck 綠；
突變 22 個 18 紅＋2 個第二道防線接住（登記）＋2 個補拓樸後紅。
🔴 抓到三個真缺陷：captured 累計增量路徑缺（步 4 遺留）、Money::Storage.new
private、spec 頂層常數撞名（雙向順序依賴）。

## ② 為什麼這樣改

- FO 每單一張（v1 單地點）＝GraphQL 面 1:1 收 pair 陣列、服務層限恰一組——
  多地點拆單不預支。
- 退款分攤＝最大餘數＋per-unit 游標（多次部分退款 Σ 精確）；預覽與實退同一份
  Calculator（鐵律 7）。
- restock 兩型庫存語義分開（cancel＝committed−/available+；return＝available+）。
- 被推翻的假設：「超額路徑條件式一定殺得掉」——第一次超額時數學上恆真，
  連續第二次才分辨（R3b）；「CONCURRENT 分支可自然觸發」——order lock 已序列化，
  只能注入（conditional_cap_update! 縫）。

## ③ 還有什麼沒解決

worklog Pending 全表（FO hold/release 入口、RECEIVED 收斂、Return 線、
免運折扣運費規則、webhook topic 對位、restock ledger 未取證、常數撞名機制化、
生產煙測）。91 §3.24 已登記五條。

## ④ 下一個人要注意什麼

- 🔴 動退款上限前先讀 `Refunds::Create` 檔頭與 16 §F5.1——兩碼**必須**分開回；
  超額路徑仍是條件式 UPDATE。
- 🔴 PSP 退款失敗**必須**補償 refunded_total_cents（job 的 release_cumulative_cap!）
  ——Calculator 可退量與累計欄兩處以 status=failure 同步排除。
- 🔴 spec 內 describe 塊的常數＝Object 頂層，一律帶檔案前綴。
- 停止條件＝CI 兩 job 綠（D40）⇒ 自合併 ⇒ 部署 bt3 ⇒ demo 店煙測
  （出貨→取消→退款全鏈，自我清理）。
