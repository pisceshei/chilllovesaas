# Handoff — 2026-08-20：PR #61 Codex `35c9eea` 驗收修復

## ① 我改了什麼

本步驟實作 Codex review `4979980175` 對 head
`35c9eea83f2ea770013b1e383508ab240c09bb94` 的五則 inline，證據與選案由前一步 commit
`9c4cd91` 固定。`HANDOFF.md` 移除鐵律固定計數；執行方案 §2 將 A–F 物化為可派工依賴；
§6.2 加入 non-skip 常規 PR 的 whole-run rerun 路徑；§11.4 與 §11.6 分別撤回券包及銷售訂單
拆併的缺席推論，改成未取得並禁止作正式選案輸入。

本步沒有修改 workflow 或 script；晚到評估器修法使用現有受信任 workflow 的整體 rerun，並以
GitHub 官方同 SHA/ref 契約限定。Shopify 兩項只收窄證據邊界，不替使用者裁定 native／ours 或
資料模型。配對 worklog：`docs/worklog/2026-08-20-PR61-Codex-35c9eea驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- 固定最大鐵律編號在同一樹已過期，語義入口比再換一個數字穩定。
- 編排器明定讀 §2，故依賴只留在 §3.4 等於不可執行；展開 14a–14f 是物化既有前置，不是重排
  使用者已批准的功能結構。
- Codex 晚到的根因是評估器 run 已終止，不是證據永遠不存在；whole-run rerun 會對相同
  SHA/ref 建新 attempt，而 `--failed` 對 conclusion success 的原 run 沒有覆蓋。
- Shopify 履約訂單 mutation 與 single discount multiple savings 都有明確對象，不能用一層的
  存在推另一層缺席，也不能把不同優惠概念混稱券包。

## ③ 還有什麼沒解決

- 修復後本機閘門與遠端新 head 驗收尚未取得；五個 thread 尚未回覆／resolve。
- Shopify 兩個產品面問題維持未取得；後續 S-14 不得把本輪「禁止外推」誤讀成反向結論。
- 評估器 rerun 的 positive path 待下一個未 skip 的常規 PR 真實驗證；若 attempt 產生新意見，
  必須回修復循環，不能只看 run success。

## ④ 下一個人要注意什麼

1. 使用已啟動的本機 MySQL，在具完整 gem 檔案枚舉權的本機執行層跑完整 29 閘門；Rails 冷啟動
   首跑若重現既有等待競態，依 `91` 已登記的證據判別，不得把失敗直接外推。
2. commit 後 doc-claims 必須 exit 0 且 warning 0；15.1 從 `pr61-last-push..HEAD` 對五則 inline
   逐項找 hunk，15.2 全量讀三 REST 端點、review body 與 GraphQL threads。
3. push 後逐條回覆實際修法、resolve，留下綁定新 head／base／run／review／comment id 的 remote
   handoff，再觸發一次 `@codex review`。Claude validation-skip 仍記證據未取得。
4. PR #61 不代行合併；本輪沒有取得 Shopify 測試店實測，也沒有授權 native／ours 選案。
