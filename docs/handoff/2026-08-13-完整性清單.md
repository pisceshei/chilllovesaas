# 完整性清單 · 2026-08-13

> 用途：接手的人拿到 `C:\Users\pisce\Downloads\shopifysystem` 之後，用這份確認**手上有什麼、缺什麼**。
> 產生方式：`git ls-files` 逐檔比對裝置端遞迴列表（檔名＋位元組大小），不是憑印象。

---

## ① 有的（已逐檔核對）

| 類別 | 數量 | 核對結果 |
|---|---|---|
| git 追蹤的一般檔案 | 108 | **107 逐位元組相符**，1 個改名（見 ③-a） |
| 完整 git 歷史 | 69 commits | `chilllovesaas-full.bundle`，`git bundle verify` 通過（complete history） |
| Ella 7.2.0 測試 fixture | 885 檔 | `test-fixtures-ella-7.2.0.tar.gz`，tar 內檔數＝git 內檔數 |
| Liquid 引擎 PoC 產物 | 1 包 | `poc-liquid-engine-out.tar.gz`（`compat-report.json`＋渲染出來的 `index.html`＋兩張截圖） |

**缺漏 0、多出 0、大小不符 0。**

文件涵蓋範圍：`docs/research/00-69`（模組研究、實測 teardown、Shopify 官方文檔查證）｜`docs/specs/11-70`（生產級規格）｜`docs/design/20-64` ＋三份高保真原型｜`docs/handoff/`（每輪交接）｜`poc/liquid-engine/`（Liquid 引擎 PoC 原始碼）｜`config/limits.yml`｜`scripts/lint-prototype.py`｜`CLAUDE.md`／`AGENTS.md`／`HANDOFF.md`。

---

## ② 這份資料夾**足以**讓人立刻接手，因為

- 規格與研究是**自足的**：每條結論都標了來源等級（`src`/`std`/`help`/`dev`/`alt`/`press`/`blog`）與出處，不依賴任何人的記憶。
- 三份原型是**單檔 HTML**，用瀏覽器直接開就能互動，開「⌗ 註釋模式」可看每個控件的功能／邏輯／實作出處。
- 決策脈絡在 `2026-08-13-SESSION-EXPORT.md`：16 條使用者裁定（其中 4 條 🔴 標記為「覆蓋 Shopify 做法」）、9 條踩過的坑。
- 未結案的決策在 `2026-08-12-open-decisions.md`，逐條寫了選項與影響面。

---

## ③ 缺的 / 要注意的（誠實列出，共 4 項）

### a. `.github/workflows/claude-review.yml` 改了位置

裝置端把 `.github\workflows\` 判定為受保護路徑，遠端工具**不准寫**（回傳 `is a protected file and cannot be written via remote tools`）。這是安全機制。

內容原封不動放在 **`_github-workflows\claude-review.yml.txt`**（10,897 bytes，與原檔一致）。
**要恢復**：搬回 `.github\workflows\claude-review.yml`、去掉 `.txt`。或者直接從 bundle clone 出來的倉庫裡拿（那份路徑是對的）。

### b. `docs/specs/45` 從來沒寫過 🔴

`docs/design/40-full-buildout-plan.md` 的計畫表列了 `docs/specs/45`「開發需求手冊（施工序總裝）」，四處引用它（L94／L108／L124／L132），**但這份文件不存在，從來沒有被建立**。

這不是同步遺失，是**當初就沒寫**。接手的人讀到 40 號時會找不到它——那不是你的錯，是既有缺口。要不要補由使用者決定。

### c. 使用者上傳的三個原始檔已不在容器裡

`products_export.csv`／`product_template.csv`／`shoplinecsv.xlsx`（Shopline 匯出格式的樣本）在上一輪工作階段被上傳並分析，分析結果**完整寫進了 `docs/specs/70`**，衍生的樣板也在 `docs/templates/`（79 欄：43 base ＋ 12 translatable × 3 語言）。

但**原始檔本身已隨容器回收**。要重新核對 Shopline 的原始欄名，得請使用者再上傳一次。

### d. 對話逐字稿不在裡面

`SESSION-EXPORT.md` 是**萃取過的脈絡**（裁定、坑、方法論），不是逐字稿。它涵蓋了所有影響決策的內容，但如果要追「某句話當時是怎麼說的」，這裡沒有。

---

## ④ 還沒推上 GitHub 🔴

remote 已設 `https://github.com/pisceshei/chilllovesaas.git`，**69 個 commit 從來沒推上去過**（`git ls-remote` → `could not read Username`）。

在推上去之前，`chilllovesaas-full.bundle` 是**唯一**的完整備份。
Claude 不能代輸入 GitHub 帳密或 token，**第一次認證必須由使用者本人完成**。
