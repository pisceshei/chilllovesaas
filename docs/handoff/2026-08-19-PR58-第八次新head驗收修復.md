# Handoff — 2026-08-19：PR #58 第八次新 head 驗收修復

## ① 我改了什麼

處理 GitHub Codex review `4973362395` 對 `4335450` 的四條 inline：latest SHA 證據不足、primary rate-limit 全稱句過度、P-4 的 `--sp-500`×6 失真，以及 poller 接受 300 秒但鐵律要求 15–25 分鐘。

本輪 worklog：`docs/worklog/2026-08-19-PR58-第八次新head驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **latest SHA 有官方明文，但「永遠看 head」仍不精確。** 官方明說 earlier commit checks 不能滿足 latest SHA；同頁又規定 test merge commit 有 status 時看 test merge commit，故採「補證據＋收窄主詞」，沒有照評語把整句降成推論。
- **被推翻：官方頁沒寫就等於 primary 沒有門檻。** 有限檢索不能證明不存在；外部事實檔只留 reset／secondary retry 的逐字事實，設 deadline 是我方選案並登記到 `91`。
- **被推翻：把盤點時的 6 改成今天的 10 就算修好。** 使用點會繼續增減；P-4 的終態判準改由 `rg` 完整集合導出，避免第三次快照腐化。
- **內部契約以鐵律為準。** 300 秒只是 #59 當時的額度下限，不能放寬使用者已裁定的 900–1500 秒；腳本、機制註記與終態文檔一起同步。
- **不擴修。** `--text-1`×3 等未被本輪點名的同型快照數字維持原狀；只改 Codex 明確指出的 `--sp-500`。

## ③ 還有什麼沒解決

- 新 commit 尚待 Claude 與 Codex 對精確 head 重新驗收。
- #60 的五參數 poller 尚未 rebase；合併 #58 後須化解其 35 天 INTERVAL 上限與本輪 900–1500 秒契約的衝突。
- GitHub primary rate-limit 是否另有未公開的客戶端總界不作判斷；只有取得官方明文才能升格。

## ④ 下一個人要注意什麼

1. required check 的 latest SHA 規則不能簡寫成「一律看 head」；先判斷 test merge commit 是否有 status。
2. 外部文檔的沉默不是「沒有」的證據；證不出的全稱句應移出 external-facts。
3. P-4 驗收 `--sp-500` 時執行 worklog 裡的 `rg` 命令取完整集合，不要把當天輸出數量抄回方案。
4. rebase #60 時保留其 deadline 能力，但 INTERVAL 必須沿用 900–1500 秒，CLAUDE 的五參數用法也要同步。
