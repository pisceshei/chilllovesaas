# Handoff — 2026-08-20：PR #61 Codex `b96426f` 殘留層級修復

## ① 我改了什麼

本步處置 issue comment `5353241564` 的 current-head P1。§11.6 前方在 `ee7835e` 已撤回
「Shopify 銷售訂單層無拆併」，但初始 `4b31b75` 留下的「本尊把拆併放履約層」摘要及三個
競品 limits 沒同步改。現在摘要只發布已取證的 fulfillment-order 能力；Shopify 銷售訂單層
明標未取得；CHILL LOVE 掛載層、狀態機與正式 limits 都要等 Shopify 測試店證據或使用者
差異裁定。SHOPLINE 的 2–50 筆與子單不可再拆只保留為輔助研究。
修改後初輪完整閘門 29/29 綠：MySQL、Rails 284/0、前端 5/0、doc-claims warning 0、
workflow syntax 皆有實跑證據。

配對 worklog 為 `docs/worklog/2026-08-20-PR61-Codex-b96426f殘留層級修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- 被推翻：前方證據段改正後，整節的產品結論就已同步。`git blame` 顯示摘要仍停在初始
  commit，正是相同語義的下游消費端漏改。
- 被推翻：Shopify 有 fulfillment-order 拆併即可證明它「選擇履約層而沒有銷售單層」。官方
  mutation 只能證明存在的能力，不能證明另一層缺席。
- 未採：直接把 SHOPLINE 研究刪除。D23 允許其作輔助參考；合規修法是禁止升格成我方選案，
  不是丟掉競品資料。

## ③ 還有什麼沒解決

- 修改尚待 handoff 落字後的最終完整閘門與 commit 後 doc-claims，GitHub 新 head尚不存在。
- issue-level P1 不能用 resolveReviewThread 關閉；必須由新 head Codex 判詞證明已清。
- Claude workflow validation-skip 與 Node 版本 warning 均未在本步處理。
- PR #61 尚未合併，`pr61-last-push` 仍暫時指向 `b96426f`。

## ④ 下一個人要注意什麼

1. 驗收 §11.6 要同時讀前方 Shopify 證據段與最後「對我方」摘要，不得只看單一 hunk。
2. SHOPLINE 的數值可留研究層，但在使用者裁定前不能成為 `specs/112` 正式 limits。
3. 新 commit 後重跑 doc-claims、15.1／15.2、push、CI 與 `@codex review`；不得復用
   `b96426f` 的機械綠或 P1 判詞。
4. merge 前最後拉全 GraphQL threads、三端點與最新 base/head，條件缺一即停止。
