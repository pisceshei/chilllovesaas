# Handoff：G6 步 11 買家帳戶線（2026-09-01）

## ①我改了什麼

20 步方案步 11。base＝main `2bacb09`，分支 `g6/step11-customer-accounts`。
OTP 四防線＋session 雙防線＋/account 家族＋結帳預填＋customer nil stub。
逐檔＝worklog Changes；防線與紅證＝`docs/dev/g6-customer-accounts.md` §2。

## ②為什麼這樣改

- /account 走非主題化頁：主題頁全在頁快取後面（個人化不進快取——14 §F1-4），
  帳戶頁天生個人化 ⇒ 跟 checkout 同一條非主題化通道，不動快取架構。
- OTP 三個數值 ours 保守（10 分/60 秒/5 次）：官方未取得（19.3）——limits 註釋
  明標，取到官方值改 limits 即可。
- customer nil stub 而非注入：快取鍵不分票就注入＝登入者的頁被存進公共快取。

## ③還有什麼沒解決

- 🔴 SMTP 未配置 ⇒ 生產登入實際不可用（信不外寄）；使用者提供 SMTP 四鍵進
  /etc/chilllove/env 即通。91 §3.57 五 ⚪。
- 生產煙測待部署後。

## ④下一個人要注意什麼

- 🔴 突變殺不紅先剝遮蔽鏈：MO5 連兩層遮蔽（logout 刪列→cookie 先過期）——
  雙防線系統的 DB 層要 model 級直測。
- checkout 預填的「email 空才填」不可反轉（MO6 紅證：登入態蓋掉買家手動輸入）。
- 新增登入形（Shop/passkey/OIDC）都應收斂到 CustomerSession.issue!——
  session 載體單一，別開第二種 cookie。
