# Handoff — 2026-08-19：PR #58 第三次新 head 驗收修復

## ① 我改了什麼

處理 Codex review `4971737032` 對 `811a5e2` 的四條 P1，以及 Claude R31 comment
`5341844768` 的一條 🔴／一條 🟡：fixture 精確路徑、終態表手寫計數、workflow validation
第三處外部全稱句、SHOPLINE 十四天期限證據、全專案 handoff 新head2 沿革與 README 收割邊界落籍。

本輪 worklog：`docs/worklog/2026-08-19-PR58-第三次新head驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：同一 fixture 目錄可代替精確檔案。** Changes 表宣告的是最終 diff 檔案集合，
  因此只能列受追蹤的完整路徑，不能用前綴相等放寬。
- **被推翻：終態列可以維護手寫條目數。** 這類數字已多次在後續增修時腐化；本條被點名後
  改成無數字的內容描述，未被點名的歷史層同型句依鐵律 17.2 不擴修。
- **「官方未公布」不是 client 原始碼能證明的事。** 固定 client 只能證明它列出的 error code
  與未呈現伺服器演算法；要升格須有官方 endpoint 規格或受控單變量實測。
- **十四天期限必須有自己的證據。** 起算點與到期 pause 不能替代期間長度；因此另引用同一官方頁
  明載 14-day 的小節標題，並重新核對總引用量。
- **Changes 沿革按檔案的實際 hunk 歸屬。** 新head2 的全專案 handoff 只改 #60 tag fetch、
  SHOPLINE 來源與 §6.5①；伺服器判準收窄發生在另一組三件套，不能重複錯掛。
- **`docs/worklog/README.md` 是格式說明，但目前集合判準仍把它納入附錄 A。** 本輪只依 R30 ⚪1
  登記待裁邊界；P-1 再決定白名單排除或照收，不在本輪擴改判準。

## ③ 還有什麼沒解決

- 新 commit 尚待 Claude 與 Codex 重新驗收。
- Anthropic token-exchange endpoint 的伺服器比對演算法仍未證實；現值只保留可證的 client 邊界。

## ④ 下一個人要注意什麼

1. Changes 表的第一欄要與 `git diff --name-only` 做精確集合相等，不得接受目錄前綴。
2. 歷史原文有錯時在後面追加更正；只改本輪點名範圍，不順手清全樹同型句。
3. 外部期限、起算點與到期後果是三個不同斷言；來源必須逐一覆蓋。
