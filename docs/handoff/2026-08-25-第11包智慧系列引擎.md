# Handoff：第 11 包 智慧系列求值引擎（D50 案 A）

工作單位＝分支 `m2/smart-collections-engine` 一個 PR。
配對 worklog：`docs/worklog/2026-08-25-第11包智慧系列引擎.md`｜dev doc：`docs/dev/m2-smart-collections.md`
輸入 ref：`main` = `879e92e`（OpenCC PR #141 合併後）。

## ① 我改了什麼

D50（案 A）的落地：最小地基（表清單導出＝`git show 20260826058000 --stat`）＋
SQL-only 求值引擎＋rules 契約＋觸發鏈＋成員數收斂（收斂點列於 dev doc §6）＋canon 回寫。
細目＝worklog Done 1–11。

驗證（快照 2026-08-25）：pkg 專屬 spec（collections×4＋tags＋request）全綠；
全套 rspec 0 failures（過程中 locale 完整性 spec 抓到一個缺 key，已改用既有鍵）；
併發 spec 以人工閘重現「resync 讀舊規則」的窗並證明列鎖防線閉合。

**2026-08-26 對抗審查輪（8 agent，提出 5／確認 3／否決 2）＋整合修復**：
- F1 🟡 否定運算子三值邏輯（`NULL <> 'x'`＝NULL 靜默剔除未設定商品，與 tag 否定打架）
  ⇒ `not_eq`／`not_contains` 帶 `OR IS NULL`。
- F2 🔴 兩場同系列 rebuild 交錯＝**整組成員被清空**（小世代覆蓋＋大世代掃尾；
  審查 gated-threads 重現 BASELINE=[1282]→FINAL=[]）⇒ 整場 advisory `GET_LOCK`
  序列化（等不到＝讓位＋Job 延後重排）＋ upsert `GREATEST` 單調帶。
- F3 🔴 更新缺席 collectionType／sortOrder 被 `|| "manual"` 預設覆蓋（智慧系列被
  靜默改手動）⇒ 宣告式契約（缺席＝保持現值）＋**型別建立後不可變**（本尊官方逐字，
  取證 2026-08-26；顯式 flip 一律 INVALID——順帶封掉否決項②的孤兒 sources 面）。
- 否決 2 條：exclusion 讀物化成員（V-140 已裁定形態）；type-flip 後 sources 殘留
  （F3 的下游，隨 F3 硬拒一併封閉）。
- 新增 7 格突變（MF1／MF2a-c／MF3a-c）全紅；錨點逐一 grep 復原確認；59 examples 綠。

**2026-08-26 第二輪（對「修復本身」的 delta 審查，7 提出／3 確認／4 否決）**：
三條確認全部是「只封被點名的格、同元件其餘格與下游沒跟上」——
F4 🔴 型別不可變沒同步到 admin 編輯頁（下拉可翻＋被拒值毒化表單狀態 ⇒ 該頁之後
任何存檔都失敗）；F5 🔴 同一個 `normalize` 裡 `description_html` 沿用舊的
`.to_s` ⇒ 部分更新靜默清空說明；F6 🟡 rake 兜底把「鎖等逾時」當成正常跳過、
回報 `rebuild OK` 以 0 退出。修法與反向複驗見 worklog 同日第二節；追加 6 格突變全紅。

**2026-08-26 第三輪（收斂輪，7 提出／7 確認／**0 否決**）**：去重後 4 條＋自查 1 條。
G1 🔴 `sources: []` 清空條件後物化成員永久殘留、且被**別的**系列當成幽靈成員減掉
（三條清理路徑同時失能）；G2 🔴 F1 的 NULL-guard 漏掉 `compare_at_price_cents`
——走 `numeric` 的四個欄位裡，`price_cents`／`weight_grams` 在 schema 帶 `null: false`、
`variant_inventory` 的 SUM 已 COALESCE 成 0，只有 `compare_at_price_cents` 可空
（複驗：`grep -n "compare_at_price_cents\|price_cents\|weight_grams" db/schema.rb`
配 `grep -n "NUMERIC_RELATIONS\|def numeric" app/services/collections/rule_compiler.rb`）；
G3 🟡 系列說明無上限；G4 🟡 resync 無依賴排序；
F7 🔴 自查——`GREATEST(NULL, x)` 回 NULL 讓 NULL 戳的列變不死列。
修法、反向複驗與突變見 worklog 同日第三節。🔴 本輪另交付
**dev doc §7 寫入契約狀態矩陣＋消費者影響圖**（鐵律 17.4 的收斂交付物）。

**2026-08-26 第四輪（最終收斂輪，12 提出／11 確認／1 否決）**：去重後 9 條。
🔴 其中**兩條 🔴 從初始候選 `ea4467f` 就在**，前三輪、947 個 spec、前三輪的全部
突變格與四次 CI 全綠都沒抓到（🔴 2026-08-26 收斂輪 J8 更正：原文寫「32 格突變」
——第一輪的突變總數依 H10 自己的更正就是**不可導出**的〔腳本未入庫、輸出未留存〕，
任何含第一輪的累計數都不成立）——觸發條件是**規則值裡的一個字元**：H1 值含 `?` ⇒ 已 quote 的
片段被當成第二層 bind 模板、arity 炸掉、**整店的增量重算停擺**；H2 `.squish` 在內插
之後才跑 ⇒ 改寫商家值**內部**的空白、兩支引擎對同一條規則給出不同答案。
另有 H3 🔴 INVENTORY_ADJUSTED 整條鏈是死的（生產端 payload 沒有商品識別、
舊 spec 自己捏了一個有的 ⇒ 假綠）、H4 🔴 rebuild 沒有 ARCHIVED 守衛而 resync 有
（成員取決於最後跑哪一支引擎），以及 H5〜H10 六條 🟡（含四處文檔與程式碼不符）。
修法、反向複驗與突變見 worklog 同日第四節（該節逐項列出標籤與紅綠；🔴 J8 更正：
本句原文寫「7 格突變」，與它自己指向的 worklog 第四節不一致——那一節列的是
MI1〜MI7 再加 MI2b、MI4b。改為不寫死數字）。

**2026-08-26 第五輪（髒值輪，13 提出／12 確認／1 否決）**：去重後 8 條。
維度直接針對第四輪的教訓設計（fixture 的值太乾淨），一次找出**兩條 🔴 且同一類**
——DB 欄寬沒有鏡射到寫入層 ⇒ `ValueTooLong` 漏成 500：J1 規則值無上限；
J2 `Tags::Normalize.key` **會讓字串變長**（NFKC 展開、casefold）⇒ 驗原字串卻存 key
⇒ 商品側整筆回 INTERNAL、migration 回填會中斷（🔴 本包引入的回歸）。
另 J3 🔴 第四輪的「改序」把第二次求值機會一起拿掉 ⇒ 鏈式引用不收斂（本包引入），
J4〜J8 五條 🟡（status 值域／自我引用／resolver 漏 exclusion 型／事件放大／計數）。
見 worklog 同日第五節。

**2026-08-26 第六輪（10 提出／9 確認／1 否決）**：去重後 7 條。
🔴 本輪把重複六輪的根因**做成結構**：求值路徑有**三條**（`ResyncProduct` 增量／
`Rebuild` 單系列全量／`catalog:rebuild:collections` 兜底）外加「B 變了誰要跟著重算」，
前五輪每次只改一條 ⇒ **新增 `app/services/collections/reference_graph.rb`**
（`topological` ＋ `referrers`），三條路徑共用。
K1 🔴 拓樸排序的遞迴在深鏈爆 `SystemStackError`（**不是** StandardError ⇒ Relay 接不到
⇒ 永久毒丸＋拖垮同批其他商店）；K2 🔴 兜底仍照 id 序 ⇒ 兩支引擎不同答案；
K8 🔴 反向傳播完全缺席 ⇒ A 永久錯誤且無自癒；K3 🔴 金額 BIGINT 溢位漏成 500；
K5／K6／K7 三條 🟡（既有超長標籤讓商品**唯讀**／來源陣列無上限／§7 影響圖過期）。
見 worklog 同日第六節。

**2026-08-26 第七輪（16 提出／10 確認／6 否決）**：去重後 6 條。
L1 🔴 第六輪的反向傳播在**奇數長度的引用環**上永不終止——「A 排除 B」讀 B 的物化
成員 ⇒ 反單調函數，奇數環沒有不動點 ⇒ 成員週期震盪，而傳播條件是「有沒有變」
⇒ **無界 job 鏈＋無界 outbox（EXTERNAL topic，會外發 webhook）**，兜底 rake 自己跑
一輪就是新的震盪源（實測 n=3／n=5 永不終止；n=2／n=4 會停，因為偶數環是單調的）。
⇒ 修法＝**任何長度的環在寫入層一律拒**（自引只是 n=1 格）＋既有環的傳播深度上限。
L2 🔴 三值邏輯的**第三次**現形：F1／G2 兩次都只封「否定 relation」，沒封真正產生
否定的**區塊層 `NOT`** ⇒ 正向謂詞落進 exclusion 時照樣把 NULL 欄商品整列丟掉。
⇒ 改 `NOT COALESCE(expr, FALSE)`，這一層封住之後任何新型別／relation 都自動 NULL-safe。
L3 🔴 `bind` 上方的「消費紀律」寫的是 H1 **第一版被否決**的修法（三個子句對 HEAD 全假）。
L4〜L9 六條 🟡（rake 排序無 spec 覆蓋／`:skipped` 語義敘述停在 G1 之前／
handoff 與 dev doc 互相打架／`products_count` 註釋停在「引擎未落地」／本節缺兩輪）。
見 worklog 同日第七節。

**2026-08-26 第八輪（9 提出／8 確認／1 否決）**：去重後 6 條，**只有一條 🔴**
（嚴重度分布首次明顯下降）。M5 🔴 對外面（cache stamp／outbox／反向傳播）在寫成員的
txn **之外** ⇒ 失敗一次就永久遺失（重跑的 inserted／swept 都是 0）；修法三層，
後兩層都是實跑才發現的（移進 txn 還不夠、`resumed` 必須讀鎖定讀回傳的那一列，因為
`update_columns` 在回滾後仍留著記憶體裡的新值）。M1 🟡 深度上限擋的是**路徑長度**
不是環 ⇒ 合法長鏈被截斷，改用 **visited 集合**。M2 🟡 環守衛是 TOCTOU ⇒ 加店級
序列化複查。M3 🟡 `reaches?` 查詢放大 ⇒ 新增 `ancestors` 逐層批次。M4 🟡 resync 缺
unknown 守衛。M7 🟡 handoff §③ 三個子句對 HEAD 全為假。見 worklog 同日第八節。

**2026-08-26 第九輪（14 提出／10 確認／4 否決）**：去重後 8 條。
🔴 兩條都是**第八輪修法自身的回歸**：N1 M2 的序列化複查用**非鎖定讀** ⇒ REPEATABLE
READ 下讀的是**取得店鎖之前**建立的快照，看不到排隊期間對方提交的邊，環照樣落庫
（而且第八輪那格 spec 是假綠——依序跑不會交錯）；N2 `@cycle_ancestors` 記在
`class << self` 的**類別物件**上 ⇒ per-process 永不失效，被引用方的邊刪掉後合法輸入
被**永久誤拒**，外加無界記憶體與多執行緒共用裸 Hash。
🟡 六條：N3 unknown⇒ERROR 契約兩支引擎口徑不等價（`any?` 短路／只有 exclusion 的來源
兩條繞過路徑）；N4 M5 的承重理由「三者同一個資料庫」被 Solid Queue 的獨立 queue DB
推翻；N5 dev doc §3 兩句過期；N6 handoff 缺第八輪紀錄＋易腐計數；N7 新 resolver 佔用了
`collections` 的 RDoc；N8 公開契約六條分支零測試。
🔴 **本輪最強的正面訊號**：correctness 維度從零建了 19 商品 × 11 系列、**人工推導期望
成員集合**再與三支引擎比對，**沒有找到任何算錯的格**。見 worklog 同日第九節。

## ② 為什麼這樣改

- **落地形照 limits（95 §1.1 修正）不照 spec 13 舊塊**：spec 13 §F4.1 的 SQL 塊是 UI
  形態（四型×極性），比 limits 的 2026-08-24 修正舊——本 PR 順帶回寫（D50 配套）。
- **求值公式 per-source 相減**：全域相減是靜默錯誤（只在 A 排除 X＋B 包含 X 的系列露餡，
  測試資料不會自然出現——與 zero-decimal 幣別同形態）。13 §F4.2 的三條必測釘住其中兩條（第三條在 v1 無載體，登記 P11-B11——2026-08-26 第七輪 L6 更正：原文寫「全釘」，正是 H8 禁止的宣稱）。
- **觸發走 outbox 消費者**（P11-U17 ours）：鐵律 5＋不掛在寫路徑鎖持有時間上；
  重放冪等（resync 算「現值該不該在」，天生收斂）。
- **變更判定不用 affected_rows**：ON DUPLICATE 對既有列每輪記 2 ⇒ 零變更 rebuild 也會
  白打快取——煙測抓到後改 `created_at >= generation`。

**被推翻的假設**：①「affected_rows 可以當變更訊號」（上句）②「sanitize_sql_array 對
Integer 產生裸數字」——Rails 8.1 mysql2 adapter 對 Integer 也產生帶引號常量（實測），
MySQL 常量側折算一次、語義與索引不受影響，spec 註明。

## ③ 還有什麼沒解決

dev doc §5 的 P11-B1〜B12 全表（B11＝H8、B12＝K5 新增）。最重要的四條：
0. 🔴 **環（任何長度，含自引）自第七輪起在寫入層一律拒收**（L1），並由
   `replace_sources!` 的店級序列化複查擋住並行建環（M2）；`ResyncProduct` 早已是
   **單趟拓樸序**（不是兩趟）。殘留面只有「守衛上線前建立的既有環」——其成員答案
   不保證正確，由 visited 集合保證傳播不無界，`config/recurring.yml` **沒有**排
   `catalog:rebuild:collections`（排程屬 CD 包）。
   （🔴 2026-08-26 第八輪 M7 更正：本條原文寫「B1〜B10」「互相引用需要不動點迭代」
   「resync 的兩趟」，三個子句在 HEAD 全為假。）
1. **sub_collections／variants 目標／metafield 條件都延後**（schema 就位、寫入拒收）。
2. **exclusion 引用手動系列讀不到成員**（讀 memberships；手動在 collection_products）
   ——UI 包遷移手選時一併物化。
3. **變體子頁的獨立更新是否發 PRODUCTS_UPDATE 未驗證**（P11-B9）；若否，價格條件的
   增量觸發漏一面，rake rebuild 兜底。
另：本包無 UI；規則編輯器＝包 13。研究輪的 P11-U1〜U21 未取得清單要抄進正式登記
（本 PR 的審查輪一併處理）。

## ④ 下一個人要注意什麼

- 🔴 **加條件型別＝三處一起動**：dev doc §1 值域表、`RuleCompiler` 常數（含 RELATIONS）、
  `SaveCollection` 白名單——RELATIONS tripwire spec 盯著鍵集合。
- 🔴 **智慧成員永遠不進 collection_products**（spec 釘住）；前台只查 memberships。
- 🔴 **`Rebuild`/`ResyncProduct`/規則編輯共用 collection 列鎖**——新增任何動 rules 或
  memberships 的路徑都要先 `Collection.lock`，否則併發窗重開（併發 spec 的人工閘可重現）。
- 🔴 **rebuild 對 rebuild 另有一把整場 advisory lock**（2026-08-26 F2）：列鎖只護單批；
  任何繞過 `Rebuild.call` 直接批量寫 memberships 的新路徑都會重開「交錯掃尾清空系列」
  的窗——寫入一律走 `Rebuild.call`，或至少複用同名 `GET_LOCK` ＋
  `GREATEST(COALESCE(rebuilt_at, 世代), 世代)` 單調帶（🔴 **`COALESCE` 不可省**：
  `GREATEST(NULL, x)` 在 MySQL 回 NULL，少了它就是 F7 修掉的不死列；掃尾也要收
  `IS NULL`。2026-08-26 收斂輪 H6：本句原文只寫 `GREATEST` 單調帶，照字面實作
  等於把 F7 重新做壞一次）。
- 🔴 **tag 條件的鍵一律過 `Tags::Normalize.key`**；product_tags 由 SaveProduct 同 tx 維護，
  繞過 SaveProduct 寫 tags JSON＝兩表分岔。
- 🔴 **本包最重要的共同教訓（下一包帶著）**：改一條規則時，**先列出該規則所在
  方法／元件的完整欄位矩陣與全部下游消費者，逐格確認再送驗**。確認的 finding 裡
  絕大多數是「同一個方法裡的鄰居欄位」或「同一條契約的下游消費者」沒跟上——
  不是新問題，是修法射程沒畫滿（鐵律 17.2／20.2 第 2 類）。逐輪條目請數 worklog。
  ⇒ 交付物＝dev doc §7 的狀態矩陣與消費者影響圖，**下次動這個檔先讀它**。
- 🔴 **突變輪要看的不只是「紅不紅」**：第三輪第一次跑 8 格只有 6 紅，兩格綠的原因不是
  守衛無效，而是**改了生產碼卻沒加測試**——兩者在突變表上長得一模一樣。
- 🔴 **求值路徑有三條，不是一條（第六輪最重要的結構教訓）**：`ResyncProduct`（增量）、
  `Rebuild`（規則編輯後的單系列全量）、`catalog:rebuild:collections`（兜底全量），
  外加「B 變了誰要跟著重算」這個維度。前五輪每次改求值語義都只改一條，另外兩條就
  分岔——多輪的 🔴 都是這個形態（H4／K2／K8，加上 G4→H5→J3 三次都只改 resync）。
  ⇒ 已收斂成 `Collections::ReferenceGraph`（`topological` ＋ `referrers`），
  **新增任何求值路徑都必須接上這兩個**（dev doc §7 影響圖新增一列明文要求）。
- 🔴 **`SystemStackError` 不是 `StandardError`**（第六輪 K1）：任何在事件消費者裡跑的
  遞迴，深度都必須有界——`Events::Relay` 的 rescue 只接 `StandardError`，
  爆堆疊會變成**永久毒丸**並拖垮同一批裡其他商店的事件。本包已改為迭代 DFS。
- 🔴 **測試拓樸也會騙人（第五輪）**：測「依賴順序」的測試必須讓**被依賴方非空**，
  否則測的是空集合的交換律——本輪的拓樸序突變第一次是綠的就是這個原因。
  同理，被上層守衛遮住的下層防線（model 驗證）要在**它自己那一層**測。
- 🔴 **DB 欄寬必須鏡射到寫入層**（第五輪兩條 🔴 同一類）：任何 `varchar(N)` 欄，
  寫入層都要有對應上限檢查，否則 `ActiveRecord::ValueTooLong` 會**漏成 500**
  （它是 `StatementInvalid` 子類，不在服務層的 rescue 清單裡）。
  🔴 **而且要驗「真正落庫的那個值」**：`product_tags.tag_key` 存的是正規化後的字串，
  正規化會**變長**（NFKC 展開／casefold）⇒ 只驗原字串必漏。
- 🔴 **測試資料的形狀本身就是盲區（第四輪最貴的一課）**：H1／H2 兩條 🔴 從初始候選
  就在，四輪審查、947 個 spec、各輪突變與四次 CI 全綠都沒抓到（同上 J8 更正：
  不寫累計突變數），只因所有 fixture 的
  規則值都很「乾淨」——沒有一個含 `?`、沒有一個含連續空白。**下一包寫 fixture 時，
  自由文字欄位至少要有一個「髒」值**（標點、連續空白、Tab、引號、CJK 全形空白）。
- 🔴 **`sanitize_sql_array` 的產物不得再進另一個 `sanitize_sql_array`**：它用
  `count("?")` 對位且不分引號內外。本包的紀律寫在 `RuleCompiler#bind` 上方
  （產出者處），任何新的 SQL 片段生產者都照這條寫。
- 下一步入口：包 12（publication scopes——「發布過濾疊在成員之上」，discoverable ⊆
  purchasable 不變量）；它讀 memberships，不改本包。
