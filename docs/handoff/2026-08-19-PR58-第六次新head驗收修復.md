# Handoff — 2026-08-19：PR #58 第六次新 head 驗收修復

## ① 我改了什麼

處理 GitHub Codex review `4972542106` 對 `bda2455` 的三條意見，以及 Claude R34 comment
`5342869748` 的一條 🟡；去重後為三個問題：附錄 A 範圍敘事、第四次三件套的歷史層保真、
`91` §3 兩個暫存條目的取證日期。Claude comment `5342869748` 的 🟡 與 Codex inline
`3813309589` 的歷史層意見重疊。

本輪 worklog：`docs/worklog/2026-08-19-PR58-第六次新head驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：把歷史命令展開為可執行形式只是無害的機械修正。** `git show 4856da2:docs/worklog/2026-08-19-PR58-第四次新head驗收修復.md` 與 `git show 4856da2:docs/handoff/2026-08-19-PR58-第四次新head驗收修復.md` 證實兩份文件的原句都使用 `<path>`；依歷史層規則必須恢復原句，再於鄰行提供完整命令。
- **三件套不等於附錄 A 的三個清單成員。** 三件套是 worklog、handoff、commit；附錄 A 是歷史文件收割清單，只列前兩份文件。
- **來源錨不能替代日期。** `91` §0.3 明列每個 §3 暫存項都要「摘寫＋來源錨＋日期」，故兩項各自補日期，不以 §3.4 標題日期代替。
- **同型問題不擴修。** 日期只補 Codex 點名的兩條；其餘暫存項未被本輪點名，依鐵律 17.2 不順手改。

## ③ 還有什麼沒解決

- 新 commit 尚待 Claude 與 Codex 對精確新 head 重新驗收。
- 兩句 `<path>` 只作歷史原文保留；現行複驗入口是鄰行兩條完整 `git show` 命令。
- PR #60 的兩條 exact-head 文檔一致性意見仍在獨立 worktree 收斂，未混入本 PR。

## ④ 下一個人要注意什麼

1. worklog／handoff 敘事段即使修的是命令可執行性，也要保留原文後加 HTML 更正註。
2. 「三件套一起提交」是提交紀律；附錄 A 的集合只由 worklog／handoff 路徑構成，兩者不要混寫。
3. §3 暫存條目的來源錨與取證日期是兩個獨立欄位，不能互相替代。
