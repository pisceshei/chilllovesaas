# 2026-09-03 主題編輯器 E6 交接：預覽互動（D79）

## ① 我改了什麼

- 目標：D79 序列第六步——預覽內互動依本尊（100 §5／§9.5）：hover 藍框＋chip、section 邊界「+」開 picker、選中浮動工具列、
  預覽內右鍵、inspector 開關、官方 `shopify:*` 事件、`Shopify.designMode` 官方語義；橋抽檔可測；E5b（picker 貼左欄右緣）併入。
- 輸入：main `bd563d1a`（E5 已合併）；分支 `editor/e6-preview`。
- 改動：見 `docs/worklog/2026-09-03-主題編輯器E6-預覽互動.md` Changes 表；功能文檔 `docs/dev/e6-theme-editor-preview.md`。
- 驗證（本機，2026-09-03）：`pnpm -s typecheck` 退出 0；`pnpm -s vitest run app/frontend/admin/pages/ThemeEditorPage.test.tsx`
  59 例全綠；`editorBridge.test.ts` 6 例全綠；SG1／BR3 規格更新後綠；全套閘門與 doc-claims 結果補記於本節末。
- 突變十格（M45–M54）各自轉紅（worklog 表）。
- 未動真店、未動 bt3（合併後才部署）。

## ② 為什麼這樣改

- 使用者裁定 D79；100 §5 給預覽互動逐項形態；§9.5 給官方事件與 `designMode` 逐字。
- 橋抽檔：Ruby 常數字串無法前端測，抽成 JS 後同一份既注入又可在 jsdom 執行（B1–B6），避免「腳本能注入」卻沒人測行為。
- 事件在父頁回推時派：父頁是選取真相源（左樹點選與預覽點選都經同一路徑），橋不自行猜選中。
- 被推翻的假設：①「vitest 監看 `postMessage` 一次即可」——vitest 每例後還原 mock，第二例起收不到 ⇒ 每例重新監看；
  ②「jsdom 有 `scrollIntoView`」——沒有，橋加型別守衛；③「node:fs 可在測試用」——tsconfig 無 node 型別，改 Vite `?raw`。
- 未採方向：把 picker 錨點做成預覽內插入線正下方（需跨 iframe 座標轉換與插入線 DOM；E7）。

## ③ 還有什麼沒解決

- worklog Pending 逐項（picker 錨點、右鍵座標縮放、reorder 事件、手機檢視寬、chip icon）。
- 🔴 使用者新裁定（渲染輸出 1:1）需獨立工作包：真店資料鏡像進 demo＋逐段 HTML／computed CSS diff 工具；本包未做。

## ④ 下一個人要注意什麼

- 入口：`docs/dev/e6-theme-editor-preview.md` → `app/assets/javascripts/editor-bridge.js`（契約在檔頭）→ 頁面訊息處理
  （`cl:insert`／`cl:contextmenu`／`cl:op`／`cl:names`）。
- 改橋要同步改 `editorBridge.test.ts`（jsdom 執行同一份）；橋內文字一律由父頁 `cl:names.labels` 推，不寫死。
- 主題 JS 若以 `Shopify.designMode === false` 判斷會受影響（公開頁改為 undefined，官方語義）。
- 突變前必 commit（[[mutation-revert-trap]]）；動 fixture 跑全套 rspec。
- 真店副本 theme 143506604135 仍可開來對表，不要 Publish。
- 停止條件：改鐵律本文、產生費用、對外發信、動真店現行主題——停下來問。
