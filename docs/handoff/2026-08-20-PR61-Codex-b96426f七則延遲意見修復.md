# Handoff — 2026-08-20：PR #61 Codex `b96426f` 七則延遲意見修復

## ① 我改了什麼

review `4980533036` 的七則 delayed inline 已精準落地：方案明寫 GraphQL resolved 與 evaluator
C1 不同；購物金、核帳、營銷、通路及對帳段把供應商真實能力與 CHILL LOVE 選案拆開；終態
自動化文檔則把 WebSearch 改回純導航，原文未取即「未取得」。配對 worklog 為
`docs/worklog/2026-08-20-PR61-Codex-b96426f七則延遲意見修復.md`。
修改與初版交接完成後的完整本機驗證為 29/29，Rails `284/0`、Vitest `5/0`、Brakeman 0
warnings、doc-claims 266 份／警告 0；這份驗證數據回寫後仍會從第 1 支重跑到第 29 支才 commit。

## ② 為什麼這樣改（含被推翻的假設）

- 被推翻：resolve 同 head 的 inline 後，評估器會跟 GraphQL 一樣歸零。現行 C1 不讀
  `isResolved`，所以方案改要求新 commit/head，不在本 PR 擴改 workflow。
- 被推翻：五條外部意見都應一律改成「未取得」。SHOPLINE 英文與 TikTok Product API 的官方
  原文已取得；正確邊界是保留供應商事實、撤回沒有證據或使用者裁定支撐的我方選案。
- 被推翻：Stripe provider `type` 可直接作我方 ledger 底表。Stripe 官方反而建議會計分類考慮
  `reporting_category`，我方還缺非 Stripe 事件的雙向覆蓋。

## ③ 還有什麼沒解決

- commit 前最後一輪完整 29 支閘門、commit 後 doc-claims、15.1／15.2 與 GitHub 新 head
  驗收仍須逐項即時通過，第一輪 29/29 不得替代最後差異的證據。
- 七則 GitHub threads 尚未回覆／resolve；遠端仍是 `b96426f`。
- TikTok Shop feed URL 的存在／缺席與 Stripe 全事件映射仍未取得；不得在後續規格中假裝已定案。

## ④ 下一個人要注意什麼

1. 這七則處置沒有授權修改 evaluator；若日後要讓 C1 讀 GraphQL resolved，須另開 workflow PR
   並重新做鐵律 18.3 的信任分析。
2. 新 head push 前再拉三個 REST 端點、reviews body 與全部 GraphQL threads；issue-level summary
   可能先於 inline 到達，不能用短等待或 review 外殼宣稱清零。
3. 回覆 inline 時逐字用 API 回讀驗證 body，再 resolve 對應 thread；最後再全量對帳 unresolved=0。
4. 本 PR 修改鐵律／workflow，只有使用者明文「合併61」才是 18.3 人工觸發；仍必須先滿足
   exact-head、base 不漂移、機械 CI 與獨立驗收門檻。
