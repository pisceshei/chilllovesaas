# 2026-09-03 主題編輯器 E1 交接：本尊逐面板實測 teardown（D79）

## ① 我改了什麼

- 目標：D79 執行序列的第一步——在真店 pnrjnw-sy 的 Ella 副本上，把本尊主題編輯器逐面板逐控件實測，寫成
  `docs/research/100-theme-editor-live-teardown.md`，供 E2（shell＋頂欄）→ E7（RWD）逐包對表。
- 輸入：分支 `editor/e1-teardown`（自 main a09978bf 起），前兩個提交＝D79 裁定＋文檔骨架＋§9 官方雙源；本提交＝
  §0–§8＋§V＋worklog＋本檔。
- 真店動作：Duplicate 現行主題 → 副本 `Copy of ella-7-2-0-theme-source`（theme id 143506604135）→ 在副本編輯器
  逐面板操作；兩次 Ctrl+S（一次誤存隱藏、一次還原），副本終態與原主題一致；未 Publish 副本、未刪副本。
- 驗證：`ruby scripts/check-doc-claims.rb --base origin/main --require-base` 退出碼 0（本包純文檔，無 rspec）。
- 配對 worklog：`docs/worklog/2026-09-03-主題編輯器E1-teardown.md`。

## ② 為什麼這樣改

- 使用者裁定（D79 原話）：編輯器要「和 shopify 一樣，無論是 ui 還是完整的功能」；先前編輯器是照 `docs/research/66`
  與 `docs/research/24` §1 局部實測搭的骨架，缺逐控件實測。鐵律 12 六層與鐵律 13 三寬度要求「親自點擊、值域窮舉」，
  所以 E1 先做 teardown，不先動代碼。
- 走 Duplicate 副本而不是直接在現行主題操作：使用者授權全權，但 D79 把「副本上操作、現行發布主題不動」定為預設安全形；
  副本存檔與否都不影響 hoko.vip。
- 被推翻的假設：①「iframe src 可拿來直接開頂層分頁讀 DOM」——src 帶簽名票證（hmac／id_token／session）且擴充功能擋讀
  query，不可行，改截圖＋zoom；②「編輯器 CSS 可由 network 面板抓」——iframe 資源不在擴充功能可見範圍，落 §V V2；
  ③「快捷鍵表的 ⊞ CTRL N 就是 Ctrl+N」——實測 Ctrl+2 與 Alt+Ctrl+2 都無反應，鍵位登記為未取得。
- 未採方向：用 Horizon 草稿做 E1（Horizon 的 theme-block／style 設定型別更多），因我方金標本是 Ella，先以 Ella
  副本對表；Horizon 留給 E4 補 richtext／video／product 等型別。

## ③ 還有什麼沒解決

- §V 十三項（截圖未落倉、CSS bundle 與色值未取得、⊞ 快捷鍵鍵位、拖曳重排自動化失敗、Pages 子清單與 picker 分類尾段
  未捲完、五個離開編輯器的入口未點、Blog posts 灰化條件未查 help、手機預覽精確寬度與平板檔位、Undo 到乾淨態的 Save、
  主題設定手風琴互斥、未出現的設定型別、drag handle 圖示與 help 敘述不一致、AI 入口的射程）。
- E2 的 token 值表（§8）只有截圖換算尺寸；色值、字級、字重、圓角、陰影全部要本機 DevTools 補。
- 測試店 chill-love-u5q5mnzq 仍「doesn't have permission」，全部實測只能在真店副本上做。
- 記憶檔 `C:\Users\pisce\.claude\projects\...\memory\session-handoff-pointer.md` 已更新 E1 狀態（倉庫外）。

## ④ 下一個人要注意什麼

- 入口：`docs/research/100-theme-editor-live-teardown.md` §1–§8 逐面板對表，§V 先補證據再動手。
- E2 起每包照 D40：分支 → PR → CI `quality`＋`test` 綠 → `gh pr merge --squash --match-head-commit <head>` → bt3 部署
  → 煙測；每包 worklog＋handoff 入倉（D47）。
- 副本 theme id 143506604135 在草稿列，可直接開 `https://admin.shopify.com/store/pnrjnw-sy/themes/143506604135/editor`
  續測；副本可任意改與存，但**不要 Publish**（會換掉 hoko.vip 的現行主題）。
- Chrome 操作要點：分頁必須前景（`document.visibilityState === "visible"`），否則截圖逾時；點擊座標＝截圖像素框
  （1568×750）不是 CSS px；popover 用 Escape 關，但 color popover 不吃 Escape；模板選擇器會記住上次子清單；
  破壞性操作後先截圖確認再 Ctrl+S。
- 紅線不變：不輸入密碼／金鑰、不解 CAPTCHA、不授權 OAuth；Shopify 品牌、字型、CSS 文案是紅線（結構對齊、視覺自有）；
  Dawn／Horizon 源碼禁讀；GPLv3／AGPL 專案禁讀禁抄。
- 停止條件：需要真店產生費用、對外發信、或改鐵律本文時停下來問。
