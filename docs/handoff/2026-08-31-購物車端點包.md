# handoff — 購物車端點包（2026-08-31 晚）

## ① 我改了什麼
specs/15 F1 全鏈落地（worklog Changes）。輸入 ref：main=67d331d。

## ② 為什麼這樣改
六項順序計畫最後一項；設計零新裁定——全部照 specs/15 F1／blueprint 03 預裁
（DB cart／host-only cookie／merge_key_hash／cents JSON／Ajax 錯誤形）。
唯一的實作層決定：MySQL upsert 不支援 :unique_by ⇒ 靠表上唯一索引天然
ON DUPLICATE（註釋載明）。

## ③ 還有什麼沒解決
worklog Pending 四項；壞 id／超量的本尊錯誤形被 bot 牆攔＝未取得。

## ④ 下一個人要注意什麼
- 加行走 CartWriter，別繞去裸 create（Q1 突變格會紅）；properties 進合併鍵
  是 PR #52 第 4 輪事故的防線（Q2）。
- cookie 絕不能加 :domain（Q5 格釘死）。
- 買家車在預覽面即可端到端試：登入 admin → /cart/add.js → 預覽頁 cart drop
  有真值。
