# Handoff — 2026-08-20：PR #61 Codex `2ed2403` 驗收修復

## ① 我改了什麼

本步驟由鐵律 15.2 push 前復查觸發。PR #61 遠端 head 是
`2ed2403d06eb50bba0f82e74fcacf44643a81bd8`、base 是
`d9e6a458386d35e01e9b2eb721faadbbbd0531c7`；全量 CLI 結果由前一快照的 20／30／52／0 變為
issue comments 21、reviews 31、paginated inline 57、reviewThreads 31 且 unresolved 5。
新增 review `4979564233` 的五則 inline 是 `3819028745`、`3819028746`、`3819028751`、
`3819028755`、`3819028757`，因此 push 被停止。

`3819028745` 已由 local head `b5e43ce2788e214fa3f283a02114cb767b9dea3d` 的 remote handoff
窄邊界處置。本步再修四項：§0.1 改查制度 canary；§2 的 Q-2／Q-3／Q-5 依賴與 §3.3 對齊；
CD-1 明列 `mysqldump --single-transaction` 並要求還原證據保存完整 dump 命令；點數段撤回未證明
的 Shopify 缺席結論，把已取得的 SHOPLINE 英文 enum／禮品 FEFO 嚴格限縮為輔助候選，禁止在
Shopify 實測或使用者選案前進正式規格。配對 worklog：
`docs/worklog/2026-08-20-PR61-Codex-2ed2403驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：15.2 數量未變，所以可以直接 push。** 實拉新增五個 unresolved thread；上一快照
  已過期，鐵律 19 要求綁當前時間與 head。
- **被推翻：驗收方的建議可整段照抄。** MySQL、Q 依賴與 Shopify 缺席問題成立；但 SHOPLINE
  英文官方現值已逐字支撐三個 redemption-limit 選項，不能假稱「完全無英文證據」。修法是
  區分「競品事實已取得」與「我方選案未批准」，不是抹掉前者。
- **被推翻：discount Function 文件能證明 Shopify 沒有 loyalty points。** 該頁只談 discounts；
  absence 需要測試店完整流程或官方產品面明文，目前均未取得。
- **未採：把 SHOPLINE 三值直接升為我方 enum。** 使用者已裁定 Shopify 是交互主體、SHOPLINE
  只作輔助；因此仍須 Shopify 證據或使用者明文批准 ours 增補。

## ③ 還有什麼沒解決

- 本文件定稿時，結果回填前已完成 29/29 production 等價閘門（Rails 284/0、前端 5/0、
  prototype 0/136、doc-claims 警告 0）；回填後的原子收尾閘門、commit、commit 後 doc-claims、15.1／15.2 與
  GitHub current-head review 尚未完成；五個 thread 也尚未在遠端回覆／resolve。
- Shopify loyalty points 的產品面現值與我方最終策略仍未取得，不能從本輪 SHOPLINE 官方頁外推。
- PR #61 的 Claude 判詞仍可能因 workflow validation skip 無法取得；skip 不算通過。

## ④ 下一個人要注意什麼

1. 使用 Git for Windows Bash 路徑及已啟動 MySQL，從 setup 起重跑整份 Windows 等價閘門；
   Node engine warning如仍存在要照實保留。
2. commit 後跑 doc-claims；15.1 必須在 `pr61-last-push..HEAD` 找到五則各自的 hunk／證據，
   15.2 再全量重拉三 REST 端點與 GraphQL threads，新增意見就再次停止 push。
3. 推送新 head 後逐 thread 回覆實際修法並 resolve，再觸發 `@codex review`；remote handoff 綁定
   新 head、push、comment／review／run ID，不為留言另造 commit。
4. 不代行合併 PR #61；Shopify 點數未取證與我方未選案保持 Pending，不得用 SHOPLINE 代裁。
