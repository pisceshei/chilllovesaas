# Handoff — 2026-08-20：PR #61 Codex `44ebd39` 延遲意見修復查證

## ① 我改了什麼

本步輸入 head 是 `44ebd39d41e88726e6f228381fb4521bf9e85fd1`。使用者下令合併後，合併守衛
在動作前發現 exact-head review `4980284182` 新增 inline `3819608325`、`3819608329`，
GraphQL 為 38 threads／2 unresolved，故 exit 4 且沒有 merge。遠端節點已記於 comment
`5352994087`；本步接著完成兩則意見的修法前查證，配對 worklog 為
`docs/worklog/2026-08-20-PR61-Codex-44ebd39延遲意見修復查證.md`。

P1 的官方英文缺失說法被證偽：SHOPLINE 英文一手頁確有兩種升級門檻逐字，URL 為
<https://support.shoplineapp.com/hc/en-us/articles/205657286-Set-Up-Membership-Tiers>，取證
2026-08-20。但 `git log -p` 只找到 commit `4b31b75` 將其直接映成我方 enum／狀態鏈，沒有
Shopify 實測或使用者差異選案；D23 明定 SHOPLINE 只作輔助，故實作要保留外部研究、撤回我方
已選案宣稱。P2 的 35／16／37 也只由 `4b31b75` 引入；實跑現值確為 35 token、23 號 16 列、
48 號連續 §1–§37，但原 `wc -l` 無法導出三者，實作要補三個精確集合命令。

## ② 為什麼這樣改（含被推翻的假設）

- 被推翻：SHOPLINE 會員分級只有中文官方證據。英文一手頁已取得；直接把整段標成外部語義
  未取得會丟失真實研究成果。
- 被推翻：取得英文證據就足以成為我方 enum／狀態機。D23 的產品邊界仍要求 Shopify 交互取證
  或使用者明文差異裁定，競品英文只能證明競品自身。
- 被推翻：Q-1 三個數字本身是猜測。現值可從權威檔重算；缺陷在原方案沒有對應命令，且把快照
  寫成未來工作量。
- 未採：只刪除 35／16／37。快照對理解遷移量有用；加入精確集合＋數量命令並明定開工重跑，
  同時保留可讀性與抗腐化。

## ③ 還有什麼沒解決

- 方案本文尚未修改；兩個 threads 仍 unresolved。
- 新 worklog／handoff／附錄入口尚待 commit 與 commit 後 doc-claims。
- 修復後仍需完整閘門、15.1／15.2、push、新 head CI／Codex；Claude workflow validation-skip
  仍不會產生判詞。
- PR #61 尚未合併；遠端 `pr61-last-push` tag 仍在舊 current head。

## ④ 下一個人要注意什麼

1. P1 只改 §11.1：換成官方英文來源並明示 SHOPLINE 輔助候選；enum、事件鏈、limits 在 Shopify
   實測或使用者裁定前不得進 `specs/110`。
2. P2 只改 Q-1 格與緊接的說明：三個 Ruby 命令分別列出 23 號 token、23 號元件列、48 號
   連續元件節；任何解析失敗或節號不連續都非零退出。
3. 不順手修其他固定數字或外部語義；同型未點名項依 17.2 只登記 `91` §3。
4. 實作 commit 後從新 head 重跑驗收；回覆 API 的 body 先逐字驗證，成功後才 resolve thread。
