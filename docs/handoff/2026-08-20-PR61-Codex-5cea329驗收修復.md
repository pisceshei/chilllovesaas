# Handoff — 2026-08-20：PR #61 Codex `5cea329` 驗收修復

## ① 我改了什麼

處置 current-head Codex review `4978950448` 的三則 inline：執行方案的後續修復輪與雙零判定
現在會分頁讀 GraphQL `reviewThreads` 並 fail-closed；兩條交付路徑都在 commit 後、push 前補跑
doc-claims；優惠券附錄改用 SHOPLINE 英文官方逐字證據，無來源的完整生命週期已撤回為「未取得」。

配對 worklog：`docs/worklog/2026-08-20-PR61-Codex-5cea329驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：鐵律 20 寫了 GraphQL threads，執行方案自然就會照做。** 規則生產者與實際迴圈是
  兩個 consumer；本輪證明只同步規範仍會讓每輪攝取缺資料，故直接補到 §6.2 的必經路徑。
- **被推翻：commit 前全閘門包含 doc-claims 就足夠。** 該腳本讀已提交 diff；新 worklog／handoff
  只有 commit 後才進掃描輸入，因此必須在 push 前補跑，紅或 warning 都回完整閘門循環。
- **被推翻：中文功能摘要加 URL 足以產生 enum 與生命週期。** 英文頁支持三種入口、UI 分類與
  排程數字，但沒有建立一條完整狀態轉移；有證據的候選保留，缺證的序列撤回。

## ③ 還有什麼沒解決

- 新 head 尚須重新取得 Codex review；Claude validation skip 仍是「證據未取得」，不是通過。
- `specs/111` 尚未建立；本輪只修其研究種子的證據邊界，沒有越界替 S-14 定案產品 enum。
- PR #61 命中鐵律 18.3，最終仍須使用者獨立人工審核與合併，不能代行。

## ④ 下一個人要注意什麼

1. 每次後續修復輪的 15.2 都要拉三個 paginated REST 集合與 paginated GraphQL threads；任一
   端點未取或分頁未完成，就不能宣稱「全收」或 Codex 零未清。
2. commit 前全閘門與 commit 後 doc-claims 是兩個都要跑的步驟；補跑若發現問題，要回完整閘門、
   另做 commit，再補跑，不得只修散文後直接 push。
3. SHOPLINE 是輔助參考；優惠券交互以 Shopify 與我方需求為準。UI 分類不得直接改寫成後端狀態機，
   `2`／`7` 只適用其英文原文描述的定向發送場景。
