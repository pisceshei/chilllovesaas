# Handoff — S6c-2 列表管道欄（2026-08-28）

工作包＝S6c 延伸（82 §9.3 第三種 affordance）。worklog＝`docs/worklog/2026-08-28-S6c2列表管道欄.md`。
輸入 ref：`main` = `7814ae0`（S6c）。分支 `m2/s6c2-list-channels`。

## ① 我改了什麼
實測（82 §18）＋落地 `ChannelsCell`（計數＋`˅`＋唯讀 popover＋導航）；測試 10/10。

## ② 為什麼這樣改
點列＝導航是實測出的（不是標籤）；計數判準沿用 `salesChannelsOf`（單一來源）；
stopPropagation 同構 IndexTable select 格的既有做法。

## ③ 還有什麼沒解決
worklog Pending 四項（shop 停用、零管道 V、Catalogs 欄、查詢量註記）。

## ④ 下一個人要注意什麼
- 🔴 **在 IndexTable 列內新增任何可點控件都要 `stopPropagation`**——列有 onRowActivate，
  不擋就是「點按鈕＝進詳情頁」。這次是 useBlocker 的 invariant 在測試裡炸出來的；
  換一個沒有 useBlocker 的目標頁，這個缺陷會**靜默存在**。
- 🔴 測試查 popover 用 `getByRole("group", ...)`（Popover 原語不是 dialog）。
- 新管道頁落地時：把路由加進 `CHANNEL_ROUTES` 並把該列解除停用。
