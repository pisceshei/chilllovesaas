# Handoff — 2026-08-20：PR #60 第十一輪 rebase 契約同步

## ① 我改了什麼

PR #60 已 rebase 到 PR #58 合併後的 main。衝突解法同時保留五參數 deadline、INTERVAL 900–1500 秒與 MAX_POLLS 上限；CLAUDE、poller、workflow prompt 與終態文檔已同步。

本輪 worklog：`docs/worklog/2026-08-20-PR60-第十一輪rebase契約同步.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：rebase 只要讓 Git 不再衝突就完成。** #58 把 INTERVAL 收緊到 900–1500 秒，#60 原上限卻仍允許 35 天；若任選一側，會失去已裁定等待窗或第五參數能力。
- **被推翻：10080 仍是 MAX_POLLS 的現值。** 10080 由 35 天 ÷ 舊下限 300 秒導出；新下限是 900 秒，因此同一推導只能得到 3360。
- **被推翻：external-facts 仍只在 #58 分支。** PR #58 已於 `46f6133` 合併；保留舊 fallback 會讓 operational prompt 與 main 事實相反。

## ③ 還有什麼沒解決

- workflow PR 的 Claude Review 仍會在 token 交換階段 validation-skip；這是平台防竄改，不可計為通過。
- `fetch_state` API 本身的耗時仍在 deadline 契約之外；終態文件已明載，未在本輪擴修。
- 遠端新 head 尚未產生；GitHub Codex 與 CI 必須在 force-push 後重新驗收。

## ④ 下一個人要注意什麼

1. 驗收只認 rebase 後的新 head；`8a2350c` 的 Codex 結論只能作歷史證據。
2. 本 PR 依鐵律 18.3 人工合併；Claude check 若十餘秒綠，先用 `gh run view --log` 找 `Exiting due to workflow validation skip`，不得看綠勾猜通過。
3. push 前按鐵律 15.4 完成全閘門、commit、兩點 diff 逐項核對、三類留言重拉，最後才 force-push branch 與 `pr60-last-push`。
4. Windows 本機跑 workflow syntax gate 時 PATH 要優先 Git Bash；命中 WSL bash 會把 Windows temp 路徑誤報成 33 個語法錯誤。
