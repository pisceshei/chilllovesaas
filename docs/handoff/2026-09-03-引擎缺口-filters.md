# Handoff：引擎缺口 PR-6——filters（2026-09-03）

> 工作包＝分支 `engine/filters-6`（自 PR-5 長出，PR-5 squash 後 `git rebase --onto origin/main <PR-5 head>`）。
> 依鐵律 21 四段。配對 worklog：`docs/worklog/2026-09-03-引擎缺口-filters.md`。
> 本包是 `docs/handoff/2026-09-02-主題無關conformance與真店切換.md` §④E 排定的第 6 包（§④E 六包至此全部落地）。

## ① 我改了什麼
- 依官方 filters/* 各頁逐字實作 `sort_by／within／link_to_tag／link_to_add_tag／link_to_remove_tag／
  default_pagination／color_to_hsl／color_to_rgb／md5`。驗證：T1–T8 綠；六個突變各自轉紅；
  blog／collections／conformance 回歸綠；rubocop 綠。

## ② 為什麼這樣改
- `within` 是三套主題用量最高的 stub（Ella 商品卡連結全部經它）——no-op 讓系列語境的商品 URL 全退成裸
  `/products/…`，與真店 `/collections/{handle}/products/…` 對不上。
- tag 連結三支共用一個 `tag_link` helper：href 規則（系列 vs 部落格、多 tag `+`）只寫一處。
- 被推翻的假設：「link_to_tag 的 href 只放 tag」（原 stub）——官方例是當前 URL＋"/"＋tag。

## ③ 還有什麼沒解決
- remove_tag title 文字、pagination 的 prev／省略號 markup＝官方未示（登記）；系列 tag 路徑未路由；
  其餘 color 族 stub；`format_code` 未文檔化——見 worklog Pending。
- 真店 Publish／金標本仍待（前幾份 handoff §③）。

## ④ 下一個人要注意什麼
- §④E 六包完成後，下一批候選（本 session 對比 hoko.vip 金標本得到的實證）：
  ①section 包裝三件——group section 的 id 前綴 `shopify-section-sections--{group id}__{key}`、
  class `shopify-section-group-{group}`、`<!-- BEGIN sections: … --> / <!-- END sections: … -->`
  （真店首頁逐字；我方三者皆無）；②`page_title`：真店 `<title>` 形＝首頁「店名」、其餘頁
  「{頁標題} &ndash; {店名}」（/collections/all＝「商品」、/cart＝「您的購物車」、搜尋＝
  「搜尋：找到「tee」的結果，共 N 筆」、404＝「404 找不到」），我方恆店名；③系列 tag 路徑路由；
  ④矩陣 119 個 missing object（B2B／selling_plan／metaobject／order…）。
- `tag_link` 依賴 `registers[:request_path]` 與 `current_tags` assign；片段渲染（無 registers）退
  `/collections/all`。
