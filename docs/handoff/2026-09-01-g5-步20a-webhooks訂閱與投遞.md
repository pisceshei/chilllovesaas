# Handoff：G5 步 20a webhooks（2026-09-01）

## ①我改了什麼

20 步方案步 20 第一件（獨立件；17/18/19 仍卡鐵律 2 條款③裁定）。
base＝main `9a44ba5`，分支 `g5/step20a-webhooks`。訂閱 CRUD＋投遞管線。
逐檔＝worklog Changes。

## ②為什麼這樣改

- SSRF 防護在 resolve 層不是字串黑名單（18 F4 ⚠ 逐字）；投遞直連 vetted IP
  ＝把「再驗一次」升級成真正關 rebinding 窗（Net::HTTP#ipaddr）。
- per-subscription secret（18 F4 表設計逐字）而非本尊的 app shared secret
  ——無 app 層，且逐訂閱密鑰嚴格更強。
- fanout 掛在 Consumers.for 判準而非 REGISTRY 逐 topic 列舉：EXTERNAL 集合
  變動自動跟隨（新增可訂閱 topic 不用改兩處）。

## ③還有什麼沒解決

- 20b admin UI；停用通知／circuit breaker／deliveries purge（91 登記）；
  生產煙測待部署。

## ④下一個人要注意什麼

- 🔴 新增對外 topic 只改 Events::Topics::EXTERNAL——fanout 與訂閱白名單
  自動跟隨；內部 topic 千萬別搬進 EXTERNAL（訂閱面即外洩）。
- Deliver 的 HTTP 樁形＝delivery_spec fake_http（Net::HTTP.new 攔截＋
  captured hash）——改 post! 實作先看這裡。
- 20b UI 讀面：webhookSubscriptions connection＋create 的一次性 secret
  （UI 要顯示「只出現一次，請立即保存」）。
