# Handoff：Ella 修復 PR-27（2026-09-02）

## ①我改了什麼

block 列拖曳重排（同 section；moveBlockTo＋blockDragRef＋三事件
stopPropagation）。逐檔＝worklog。

## ②為什麼這樣改

- 巢狀 draggable 必須逐事件 stopPropagation——漏一個就是「拖 block 連
  section 一起動」型雙重操作。
- 斷言走 Save payload 不走 UI 文字：兩個 _parent 列視覺同文，payload 序
  才是真相。

## ③還有什麼沒解決

- 拖曳視覺回饋（PR-26 同批）。見 worklog Pending。

## ④下一個人要注意什麼

- 🔴 no-op 操作後儲存鈕禁用——「操作→Save→斷言未變」的測試結構打不出
  第二次 Save；改「汙染嘗試在前、單次 Save」形（ED26 沿革）。
- bundle 新鮮度探針＝i18n 鍵等存活字面＋記得伺服器是 UTC 鐘。
