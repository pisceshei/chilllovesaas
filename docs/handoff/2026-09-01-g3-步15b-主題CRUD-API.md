# Handoff：G3 步 15b 主題 CRUD API（2026-09-01）

## ①我改了什麼

20 步方案步 15 第二包。base＝main `b3767d3`，分支 `g3/step15b-theme-crud-api`。
匯入端點＋rename/duplicate/delete＋theme(id).files。逐檔＝worklog Changes。

## ②為什麼這樣改

- Duplicate 拷 DB 覆寫層是硬要求不是禮貌：主題內容三層（不可變目錄＋templates
  覆寫＋theme_setting）——只拷第一層的複製品在商家眼裡「編輯全消失」。
- 官方 GraphQL 無 themeDuplicate（99 §1 研究）——admin UI 行為對位、命名照
  help 逐字 "Copy of"。
- files 清單只回 filename/size：body 三形 union 是步 16 編輯器讀取面的射程。

## ③還有什麼沒解決

- 15c OS 頁 UI；91 §3.68 三 ⚪。

## ④下一個人要注意什麼

- ThemeDuplicate 的零複製前提＝storage 目錄不可變（15a 紅線）；若未來有
  「編輯匯入主題」需求，走新 checksum 不改共享目錄。
- themeDelete 不刪 storage（共享）；孤兒清理 job 已在 91 §3.66。
- Template.create! 必帶 template_type（模型 inclusion 驗證）。
