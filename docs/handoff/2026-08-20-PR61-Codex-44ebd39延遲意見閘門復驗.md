# Handoff — 2026-08-20：PR #61 Codex `44ebd39` 延遲意見閘門復驗

## ① 我改了什麼

本步對兩則 delayed inline 修復執行完整驗證。第一次在第 15 項遇到 exit 9009，證據顯示
`python3` 命中 WindowsApps App Execution Alias，而不是 Python 直譯器；指定已安裝的 Python
3.12.10 且設 UTF-8 輸出後探針通過。正式輪依 15.4 從第 1 項重跑，最後 29/29 綠：MySQL、
Rails 284/0、前端 5/0、安全掃描、全部專案自訂 invariant 與 workflow syntax 都通過。

Q-1 三個新命令實得 35／16／37；GitHub Markdown API 實際回傳 7 個 table rows、3 個 headers、
18 個 data cells，證明修改後三欄表沒有被裸直線切壞。配對 worklog 是
`docs/worklog/2026-08-20-PR61-Codex-44ebd39延遲意見閘門復驗.md`。

## ② 為什麼這樣改（含被推翻的假設）

- 被推翻：命令名稱能由 `Get-Command` 找到就代表可執行。WindowsApps alias 存在，但 gate
  仍回 9009；只有實際跑 `--version` 與目標腳本才能證明候選直譯器有效。
- 被推翻：前 14 項通過後只續跑 15–29 即可。15.4 要求任何環境修正後從完整閘門起點重跑，
  所以正式證據是第二輪獨立 29/29，不是兩輪拼接。
- 未採：為了讓本機方便而修改 `config/ci.rb`。這是範圍外同型問題，依法只進 `91` §3.5。

## ③ 還有什麼沒解決

- 新文件尚待最終閘門與 commit 後 doc-claims；新 GitHub head 尚不存在。
- 兩個 review threads 尚待經 API 驗證回覆 body 後 resolve。
- Node 版本 warning 尚在，且不等於 Node 24 相容性已確認。
- Claude workflow validation-skip 仍不產生判詞；本步未修改 workflow。

## ④ 下一個人要注意什麼

1. Windows 閘門用 Python 3.12 絕對路徑並設 `PYTHONIOENCODING=utf-8`；不要用 WindowsApps
   `python3.exe`，也不要把 exit 9009記成 lint 找到產品問題。
2. 最終閘門後不得再動檔；依序 commit、commit 後 doc-claims、15.1／15.2、push。
3. 新 head 驗收完成前，舊 head `44ebd39` 的 CI／Codex 結論全部失效。
4. 合併前最後一刻必須重拉 GraphQL 全 threads 與 base/head；使用者直接合併命令不會豁免
   fail-closed guard。
