# Handoff — 2026-08-20：階段一' PR-1 開場包

## ① 我改了什麼

階段一'執行細則已準備落庫；D28–D31 已補入 `docs/DECISIONS.md`，總方案的 CD 與 Playwright
狀態已同步，`91` 附錄 A 也已補齊與本包相關的 worklog／handoff 路徑。

本輪 worklog：`docs/worklog/2026-08-20-階段一開場包.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：D25 足以授權本階段。** `git log -p` 顯示 D25 只限 PR #58 接手任務且已到期，
  因此本階段必須另立 D31，不能把一次性授權靜默延長。
- **被推翻：總方案的 CD 暫停標記仍是現況。** 使用者已於 2026-08-20 裁定伺服器到位，
  因此終態層必須同步 D28；連線資料則維持到 CD-1 開工時才取得。
- **被推翻：附錄 A 已與 main 同步。** 雙向集合比對顯示仍有已追蹤路徑未登記，本包按 D1
  只補點名集合與自身三件套，未擴修其他 §3 觀察。

## ③ 還有什麼沒解決

- PR-1 尚未取得 GitHub 的四條件驗收證據，P-8 單元二仍未收口。
- CD-1 的 SSH、OS、網域與 registry 輸入尚待開工時取得；CD-2 仍屬鐵律 18.3 人工合併。
- 階段 B 的 `AUTO_MERGE` canary 不在 D31 授權內，仍是日後獨立裁定。
- 總方案 §2.6 的既有表格欄位不齊已登記在 `91` §3.5；依鐵律 17.2，本包未擴修。

## ④ 下一個人要注意什麼

1. PR-1 是常規文檔 PR，不得混入 workflow、scripts 或其他 18.3 判準檔改動。
2. push 前依鐵律 15.4 跑閘門、commit、核對已提交差異，再全量拉取三端點留言；首次 push
   尚無 PR 可用時只豁免 15.2。
3. 四條件缺一不可，合併時必須帶當前 head 的 `--match-head-commit`，成功後刪遠端基準 tag。
4. P-8 單元二的 run URL 與 `1111` 留言須回寫 `91` §3；若回寫造成新 commit，就必須對新 head
   重新走完整驗收，而不能沿用舊 head 的四條件。
