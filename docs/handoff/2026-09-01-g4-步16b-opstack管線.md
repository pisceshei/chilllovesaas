# Handoff：G4 步 16b op-stack 管線（2026-09-01）

## ①我改了什麼

20 步方案步 16 第二包。base＝main `908dfaf`，分支 `g4/step16b-editor-opstack`。
寫回 mutation＋五原子 op＋快照棧。逐檔＝worklog Changes。

## ②為什麼這樣改

- 快照棧不做 op-based undo＝14 §F3 明文（「編輯器複雜度的頭號來源」）。
- 樂觀鎖顯式比對：AR 的 StaleObjectError 只保「兩個並發寫者」，不保「client
  帶舊底版」（直接賦值 lock_version 不武裝 WHERE——本輪實錘）。雙層並存。
- 儲存整份不做 patch：MB5 紅證——部分內容上傳＝未動 section 被清。

## ③還有什麼沒解決

- 16c/16d/16e（picker/dnd/控件表/佈景設定/code editor）；生產煙測待部署。

## ④下一個人要注意什麼

- 新 op 一律經 applyOp（快照先推再改）；直接 setDraft＝踩掉 undo。
- themeTemplateUpsert 是模板唯一寫點——16d 佈景設定另開 themeSettingsUpsert
  （theme_setting 表），同樣要 touch theme。
- STALE_OBJECT 是 pool 碼（code_pools STALE_OBJECT）；新錯誤碼先查 pool。
