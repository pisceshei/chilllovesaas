# Handoff — 2026-08-20：PR #61 Codex `35c9eea` 修復查證

## ① 我改了什麼

本步驟是 §6.2 第 6 步「修法前先查證」。輸入 base 為
`d9e6a458386d35e01e9b2eb721faadbbbd0531c7`、受驗 head 為
`35c9eea83f2ea770013b1e383508ab240c09bb94`、Codex review 為 `4979980175`，五則 inline
為 `3819360600`、`3819360609`、`3819360615`、`3819360628`、`3819360637`，完成時間為
2026-08-20T07:08:48Z。先以 CLI 全量核對 review 與 inline，再以 `git log -p` 追查 HANDOFF
固定計數、A–F 依賴與評估器單次執行沿革；外部語義則查 Shopify 與 GitHub 官方文件。

查證選案如下：銷售訂單拆併與券包都撤回缺席結論、改成未取得並禁止作架構／分類輸入；
A–F 在 §2 物化六列依賴；Codex 晚到但其他證據齊的常規 PR，整個重跑原 current-head Claude
workflow run 以重新計算評估器，validation-skip 類排除；HANDOFF 接手入口改成讀全部現行鐵律。
本步沒有實作上述五項，只固定證據與修法邊界。配對 worklog：
`docs/worklog/2026-08-20-PR61-Codex-35c9eea修復查證.md`。

## ② 為什麼這樣改（含被推翻的假設）

- 被推翻：fulfillment-order mutation 的存在能證明 sales-order mutation 不存在。官方逐字只界定
  `FulfillmentOrder`，不能支撐另一層的缺席斷言。
- 被推翻：搜尋不到「券包」文檔就等於產品沒有。官方另有 single discount with multiple
  savings，已顯示名詞邊界不單一；有限檢索只能留下「一次領多張券未取得」。
- 被推翻：Codex 晚到只能新增 workflow 入口。GitHub 官方支持整個既有 run 重跑並保留相同
  SHA/ref；現行 workflow 每次 run 都重新取水位、跑判詞並計算 C1–C4，因此常規 PR 可用現有可信
  workflow 重算，不必在本輪再創一個未經獨立驗收的新 workflow。
- 未採：只在 §3.4 寫散文提醒編排器。§2 自稱編排器資料源，依賴必須在該資料源物化，否則仍會
  先派工後才讀到前置。

## ③ 還有什麼沒解決

- 五個被點名位置尚未修改；review threads 仍未回覆／resolve。
- Shopify 銷售訂單層拆併、一次領多張券的產品面存在與否仍未取得；本輪只證偽既有缺席推論，
  沒有建立相反結論。
- workflow 重跑後能否在未來常規 PR 產生 1111，仍須在真實 current-head run 實測；本 PR 自身
  命中 validation-skip，不可拿來驗證該正向路徑。

## ④ 下一個人要注意什麼

1. 下一步只改五則 inline 點名位置，來源逐字與 2026-08-20 取證日期保留在方案中；不得把
   「未取得」改寫成「不存在」或「純 ours」。
2. A–F 依賴採可派工節點：A 依 P-2；B/C/D 同一 cohort，依 P-6、P-7、Q-1；E 依 P-1；
   F 依 A–E 完成。這是把現行 §3.3／§3.4 前置物化，不新增功能裁定。
3. 晚到重評僅在 exact-head Codex、合法 bot 判詞與 CI 都已存在，但該 run 的 C1 因時間先後為
   `noreview`／零時執行；使用 `gh run rerun <RUN_ID>` 整體重跑，保存 attempt/run 證據，再重走
   15.2。不得用 `--failed`，不得對 validation-skip 硬重跑。
4. 實作步驟另建 worklog／handoff／commit；完整 29 閘門、post-commit doc-claims、15.1／15.2、
   push 與遠端回覆都還在後面，不能用本研究 commit 代替。
