# Handoff — 2026-08-20：PR #61 Codex `96ffc01` 驗收修復

## ① 我改了什麼

逐條處置 Codex review `4978735798` 的六條 inline，以及 Claude comment `5350972681` 的兩條
🟡：同步 workflow 與鐵律 19、收緊 Cyberbiz
證據邊界、拆開首次 push／回應輪、每個新 head 明確觸發 Codex、同步兩份 D32 終態文檔，
在代行合併前加入最新 main 更新迴圈，並在第四輪 worklog 錯誤原文後補日期更正。

本輪 worklog：`docs/worklog/2026-08-20-PR61-Codex-96ffc01驗收修復.md`。

本地 staged 候選已取得 selector 閘門 `TOTAL=17 FAILED=0`、doc-claims 警告 0、workflow
prompt expression 命中 0 與附錄集合零差集；這些只證明本地候選，不代表下一個 GitHub head。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：規範本文新增零假設就等於驗收 prompt 已遵守。** 執行端仍允許〔推論〕與未查證
  修法，必須同步改成未取得即停止。
- **被推翻：首次 push 已有 PR 編號與 last-push tag。** 兩者都要在分支首次 push／建立 PR
  後才存在，首次交付只能先做 base 盤點。
- **被推翻：輪詢既有 review 會自行觸發 Codex。** `await-verdict.sh` 只讀狀態；每個新 head
  都要另留 `@codex review` 觸發留言。
- **被推翻：`--match-head-commit` 同時保護 base。** 倉庫既有官方證據只支持 head 前置條件；
  最新 main 必須另做 fetch／合入／重驗迴圈，且不得宣稱消除最後比較後的外部競態。
- **被推翻：Cyberbiz URL 足以支撐兩項數字／事件。** 官方中文逐字已取得，但鐵律要求的英文
  原文未取得，所以不得進規格輸入。
- **被推翻：後續 worklog 寫了撤回就足以更正歷史原文。** 歷史層要求錯誤原文留存並在原處
  加日期更正，否則只讀舊檔的人仍會拿到錯誤狀態。

## ③ 還有什麼沒解決

- 本修法尚未 commit／push，GitHub 對下一個 head 的 CI、Claude、Codex 證據皆未取得；
  `96ffc01` 的 Claude 結論是需修改，不得沿用為通過。
- PR #61 因修改 `.github/workflows/` 與規範本文命中鐵律 18.3，只能由使用者人工合併。
- Cyberbiz 英文原文仍未取得；中文原文只保存取證邊界，不構成 `specs/110` 發布依據。

## ④ 下一個人要注意什麼

1. Review 規則與規範本文必須同步；只改 CLAUDE.md 會讓實際 bot 繼續發布禁用內容。
2. 新 PR 首次交付沒有 N／tag；建立 PR 後才建 `pr{N}-last-push`，後續輪才做兩點 diff。
3. 每個 push 後都要觸發當前 head 的 Codex review；舊 head review 不能外推。
4. `--match-head-commit` 只鎖 head。若 main 前進，合入後的任何新 head 都須重跑閘門與雙方驗收。
5. 外部頁只有非英文原文而規則要求英文時，明載「英文原文未取得」並停止作為實作輸入。
6. 歷史層錯誤要在原處追加有日期的更正；另寫一份撤回記錄不能取代原處可見性。
