# Handoff — 2026-08-20：PR #61 第四輪驗收修復

## ① 我改了什麼

第三輪 handoff 的 API 欄位錯誤與合併門檻過度概括已用日期更正註處理：
`original_commit_id` 屬 inline review comment，不屬 Review 物件；未取得官方語義的
`commit_id` 變化不再被描述成「可重定位」。

本輪 worklog：`docs/worklog/2026-08-20-PR61-第四輪驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：Review 物件同時有 `commit_id`／`original_commit_id`。** 官方範例與本 PR GET
  都顯示 Review 只有前者；兩欄同時存在的是 inline review comment。
- **被推翻：inline `commit_id` 的變化有官方「重定位」契約。** 官方頁只展示欄位與建立側
  outdated 提示，沒有說明後續 push 的欄位轉換；因此撤回、標為未取得。
- **被推翻：四條件是 18.3 的通知前提。** 18.3 按 17.3 雙清後通知人工；workflow 自改時
  條件②④可能因反竄改不出現，不能死等四條件。

## ③ 還有什麼沒解決

- PR #61 尚待本輪 commit、push 與 Claude／Codex current-head 雙清，最後由使用者人工合併。
- GitHub 沒有在本輪取得 inline comment 兩個 commit 欄位於後續 push 的明文語義。
- Codex 舊 11 條 threads 尚未 resolve；必須先逐條映射修復實物並確認 current-head 零新意見。
- `91` §3.5 的 workflow／機制文檔成功文案缺 D32 通道仍未擴修。

## ④ 下一個人要注意什麼

1. Reviews 端點與 review comments 端點是兩種 schema；不要混用欄位。
2. Review 新鮮度用 Review 物件 `commit_id`；inline 清單永遠全量分頁並以 ID、建立時間與
   GraphQL unresolved threads 對帳，未取證前不推論兩個 commit 欄位的轉換規則。
3. 18.3 的終態是雙清後人工通知；D31／D32 四條件代行規則不得外推造成 workflow PR 死鎖。
4. PR #61 無論四條件是否齊都不得代行合併，完成後通知使用者人工合併。
