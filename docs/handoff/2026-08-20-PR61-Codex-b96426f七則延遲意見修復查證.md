# Handoff — 2026-08-20：PR #61 Codex `b96426f` 七則延遲意見修復查證

## ① 我改了什麼

本步在 push 前 15.2 攝取到 review `4980533036` 的七則 delayed inline，GraphQL 45/7，因而
沒有 push local `487b307`。五條外部語義已逐一查官方一手來源：SHOPLINE 英文確證競品發放器
與生日每年一次，但不構成我方 enum；Shopify 只確證 manual payment 的 Pending→人工標 paid，
不支持我方四態；TikTok Shop 第一方確證 Product API，未確證 feed 缺席；Mailchimp 確證自身
trigger／rule／action／split／wait／exit，Klaviyo 只確證單一棄單案例，兩者不構成業界標準；
Stripe 公開自身 type，且官方反而建議會計分類考慮 `reporting_category`，不能直接當我方 ledger。

兩條內部一致性也已查實：workflow C1 加總同 head 的全部 Codex inline，不讀 resolved，故方案
必須要求新 head；automation 終態文檔的舊〔推論〕規則與鐵律 19／workflow 現值衝突，須同步。
配對 worklog 為
`docs/worklog/2026-08-20-PR61-Codex-b96426f七則延遲意見修復查證.md`。

## ② 為什麼這樣改（含被推翻的假設）

- 被推翻：七條都可照 bot 建議直接標「未取得」。SHOPLINE 英文與 TikTok Product API 證據
  其實已取得；應保留真實外部研究，只撤回跨到我方選案或缺席結論的部分。
- 被推翻：GraphQL resolve 後重跑同 head 就能令 evaluator C1 歸零。實碼只讀 REST inline
  數量；同 head 曾有一則即維持 `has:N`。
- 被推翻：Stripe provider type 是自然的內部 ledger 底表。官方把 accounting classification
  另指向 `reporting_category`，且我方尚未做非 Stripe 事件覆蓋。
- 未採：在本 PR 改 evaluator 讀 resolved。這會修改 workflow 信任邊界且命中 18.3；現有
  fail-closed 算法可由新 head 全量重審收斂，方案應誠實描述其成本。

## ③ 還有什麼沒解決

- 七則 threads 未回覆、未 resolve，方案與 automation 終態文檔尚未實作修復。
- TikTok Shop feed URL 的存在／缺席與 Stripe→CHILL LOVE 完整事件映射仍未取得。
- `487b307` 未 push，遠端 branch／tag 仍是 `b96426f`。
- 新實作仍須完整閘門、commit 後 doc-claims、15.1／15.2 與新 head GitHub 驗收。

## ④ 下一個人要注意什麼

1. 實作只改七則點名範圍：方案 §6.2、§11.3／11.7／11.8／11.10／11.11，以及
   `docs/dev/m0-automation-infra.md` 的降級規則 5；不要擴改 workflow 或其他候選段。
2. 所有外部原文附 URL＋取證日；已證實競品能力要保留，但後方明寫不等於我方選案。
3. same-head inline 即使 resolved 也無法產生 evaluator C1=1；任何處置都要 commit 成新 head，
   從方案第 3 步全量重審。
4. 實作後對七個 reply 逐字 API 驗證，再 resolve；merge 前仍須重拉全部 threads，防止第三次
   delayed batch。
