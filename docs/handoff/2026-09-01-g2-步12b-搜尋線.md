# Handoff：G2 步 12b 搜尋線（2026-09-01）

## ①我改了什麼

20 步方案步 12 後半。base＝main `d3f3903`，分支 `g2/step12b-search-line`。
/search 頁＋suggest 雙形＋recommendations 雙形＋section 檔名直渲染。
逐檔＝worklog Changes；契約錨＝96 §3–§5；實作對照＝dev doc §4。

## ②為什麼這樣改

- section_id＝檔名直渲染做在 section_data_for 第三層（不是控制器特例）：
  Dawn/Ella 對 cart-drawer/predictive-search/related-products 全用檔名請求
  （25 §5＋chill.deals 實錄），這是 Ajax API 的一般語義；83 §12.3 的
  「未知 id ⇒ 404」量測用的是兩層都查無的 id，與檔名層不衝突。
- /search 繞頁快取：q 鍵空間無界（S6b 防灌爆同型）；robots 既有 Disallow。
- 兩金額出口分開序列化（suggest decimal 字串 vs recommendations 整數分）＝
  鐵律 3；MP3 突變紅證 float 退化。
- complementary 恆空不是偷懶：官方 "need to be manually set up"＋live 實測
  未配置店回空陣列——空就是真實形。

## ③還有什麼沒解決

- 91 §3.61 六 ⚪；articles 恆空待步 14。
- 生產煙測待部署後（接手先跑：/search?q=IPSA 頁、suggest.json、Ella header
  下拉、recommendations.json）。

## ④下一個人要注意什麼

- SearchQuery 的變體欄位條件走 EXISTS 帶 :shop_id——新欄位照抄同形，
  不得裸 join（鐵律 2③）。
- 新增可搜尋型別（articles）要同時進：SearchQuery、SearchResultsDrop 型段、
  suggest fetch_rows、SearchDrop::TYPE_VALUES——四處缺一型別就靜默缺席。
- suggest 回應**只回請求型鍵**（MP4 紅證）；改預設集要對 96 §4.1 官方預設
  `query,product,collection,page`（不含 article）。
