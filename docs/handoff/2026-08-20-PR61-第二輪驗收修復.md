# Handoff — 2026-08-20：PR #61 第二輪驗收修復

## ① 我改了什麼

總方案已與 D30 射程對齊：R-8 從階段一'首批表移除，安全前置要求保留但排程歸階段二'。
裁定登記表第 6 項恢復原始快照，D32 以日期補正註連到現行鐵律，沒有回填歷史層。

本輪 worklog：`docs/worklog/2026-08-20-PR61-第二輪驗收修復.md`。

## ② 為什麼這樣改（含被推翻的假設）

- **被推翻：只從執行方案刪 R-8 就完成移出。** 總方案 §十是終態層且自有優先權，未同步時
  會把 R-8 重新排回首批；D30 已明列階段一'射程且不含 R-8，所以必須同步總方案。
- **被推翻：歷史裁定表應隨新制改成現值。** 檔首明定裁定登記表屬歷史快照；D32 應另加
  日期補正並指向現行權威，不可把 2026-08-17／18 的原始裁定靜默覆寫。
- workflow 評估器文案仍為真但不完整；它不在本輪修復射程且屬 18.3，因此依 17.2 只登記。

## ③ 還有什麼沒解決

- PR #61 尚待本輪 commit、push 與 Claude／Codex current-head 雙清，最後仍由使用者人工合併。
- Codex 對前一 head `8587741` 的手動重審已接單但尚未產出 review，不能算零意見。
- R-8 的 `docs/specs/107-external-adoption-register.md` 與 GPLv3 規範實作仍留在階段二'。
- `91` §3.5 登記的 workflow 成功文案、鐵律 13.3 舊錨、S-1 射程與 Windows 本機假紅均未擴修。

## ④ 下一個人要注意什麼

1. push 前全量拉 issues comments、reviews `.body` 與 inline comments；Codex 若晚到，先按其
   `commit_id` 判定是否屬 `8587741`，再決定是否已被本輪 diff 覆蓋。
2. PR 描述不得再保存會隨三件套增加而漂移的行數或集合基數；用 current head、delta=0 與
   實際 check run 作判準。
3. R-8 的安全理由沒有被撤銷，只是 D30 明定它不在階段一'；不得把「階段二'」誤讀成
   可以在外部方案採用後才補研究。
4. PR #61 無論四條件是否齊都不得代行合併；完成後通知使用者人工合併。
