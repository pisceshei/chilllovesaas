# Handoff：第 24 包媒體裁定落鍵（2026-08-24）

## ① 我改了什麼

W3 首包（S、無可見成果）。base＝main `f7f063c`。limits `media` 節落 B6–B9 六鍵＋
CI enum 對照例＋13 §F3 與排程兩處更正註。驗證：config spec 6/6、注入回歸
（拔 statuses 一值 ⇒ 紅）、rubocop 0。
配對 worklog＝`docs/worklog/2026-08-24-第24包媒體裁定落鍵.md`。

## ② 為什麼這樣改

- B7 不重複數字：尺寸正典已在 `content.files_*`（61 §9.1 覆核過）——media 節只
  登記 `size_limit_source_keys` 並由 spec 逐鍵解參照，避免第三份拷貝漂移
  （tokens 三份拷貝事故同型）。
- B8 enum 取官方逐字（APPEND_UUID／RAISE_ERROR／REPLACE）；預設值官方未載明，
  `append_uuid` 是我方裁定並在鍵旁標 ours——不寫成「照抄本尊」（鐵律 4 同型紀律）。
- 更正註不改原文（19.5 分層）：13 §F3 的 AS 敘述與 50MP 都保留，追加日期更正。

## ③ 還有什麼沒解決

- `media.status` default "ready" 矛盾＝26 包 migration（已在 worklog Pending 與
  limits 註明落點）。
- 無線上驗收必要項：本包無行為變更；部署後以 rails runner 讀四鍵作 smoke 即可。

## ④ 下一個人要注意什麼

- **W3 次包＝31（TLS 終結）**：`DISABLE_FORCE_SSL` 三處聯動移除＋nginx 443
  （記憶 `dev-mode-d40` 有三處聯動清單）；判準＝`https://demo.chilling.com.hk`
  可訪＋`curl -I http://` 301。**它在 25 之前**（presigned 直傳要 secure context）。
- 26 包 Media model 的 inclusion 一律 `Limits.enum(:media, :statuses)`；
  migration 同 PR 把 default 改 "uploaded"。
- `spec/config/m0_configuration_spec.rb` 是無 Rails 快測——別在裡面用 `Limits`。
