# 全對齊路線圖（D84，2026-09-05 起；逐包更新狀態）

> 目標與授權全文＝`docs/DECISIONS.md` D84。本檔是**執行順序與狀態板**：每一列＝一個可獨立驗收單位（一份 worklog＋一份 handoff），
> 做完即在「狀態」欄寫 PR 號與 main SHA；被推翻的排序在末段留沿革。接手者第一件事＝讀本檔 §0，再讀最新一列的 handoff。

## 0. 接手入口

1. `docs/handoff/2026-09-04-總交接-語言前綴本尊形後.md`（環境、紅線、工具）→ 本檔 → 最新 handoff（§2 表「狀態」欄最後一個有 SHA 的列的下一列＝進行中）。
2. 對表工具：整頁 `scripts/computed-parity.mjs`＋`render_parity:diff`（`docs/dev/e8-render-parity.md`／`e12-computed-parity.md`）；動態端點
   scratchpad `e17/e17_fetch*.py`＋`e17_diff.rb`（`docs/dev/e17-fetch-endpoints-parity.md`）；本尊金標本＝hoko.vip（Ella 7.2.0）＋ admin pnrjnw-sy
   （使用者 Chrome 已登入，claude-in-chrome）。
3. 真店資料自 D84 起可任意改（取證用）；改完在該包 worklog 記「真店變更清單」（可還原）。

## 1. 三個面與判準

| 面 | 對標 | 判準 | 現況（2026-09-05） |
|---|---|---|---|
| 買家前台（Ella） | hoko.vip 整頁＋全部 fetch 端點 | HTML 逐字（正規化後）＋ computed CSS 逐屬性（三寬） | 八頁三寬 20/21 級、53 對端點只剩 ⚪（登入連結／付款鈕骨架／`__head__`） |
| 主題編輯器 | admin 編輯器預覽 iframe（A′）＋編輯器 UI | 預覽＝前台判準；UI＝鐵律 12 六層＋13 三寬 | 預覽首頁 20/21；編輯器 UI 依 D79 重做中（e6／e13） |
| 商家 admin | admin.shopify.com 逐頁 | 鐵律 12 六層＋13 三寬＋14 抓包 | 37 包計畫（`2026-08-24-三方向執行順序.md`）多數已落地，未做逐頁 UI 對表稽核 |
| 平台管理後台 | 無本尊（Shopify 內部）| 以「商家 admin 之上的多租戶管理」自訂；功能完整可用 | 待盤點 |

## 2. 執行順序（塊 T＝主題／前台，塊 A＝admin，塊 P＝平台，塊 X＝跨面）

| # | 包 | 內容 | 前置 | 狀態 |
|---|---|---|---|---|
| T1 | E17 fetch 端點逐字對表 | Ella 全部動態端點 | E16 | ✅ #339 `d9b28b12`／#340 `b4d2e214` |
| T2 | cart Ajax 全流程對表 | 真店設有貨（庫存／地點）→ `/cart/add|change|update|clear.js` 帶 `sections`、cart 頁與抽屜有項目、售罄文案五語言、cart JSON `\/` 形；取證用 headless Chrome 頁內 fetch（Cloudflare 挑戰） | T1 | 進行中 |
| T3 | 顧客帳戶：本尊切 classic 帳戶 → 登入／註冊／帳戶／地址／訂單／重設／啟用頁逐字對表 | new customer accounts 的頁面是 Shopify 託管、連結指向 shopify.com ⇒ 主題頁級對齊只有 classic 可達；D85 登記 | T2 | 待做 |
| T4 | 動態結帳按鈕（portable-wallets）逐字對位 | E18：骨架＋head bootstrap＋語言別模組（升級後光 DOM、樣式回溯）＋光 DOM 樣式表＋`cartCreate`（Storefront API 第一片）＋`/cart/c/{token}` 結帳連結；`docs/dev/e18-dynamic-checkout.md` | — | ✅ #342 `fc72f17d`（bt3 已部署複驗） |
| T10 | `content_for_header` 完整本尊形 | hoko 商品頁 head 100 tag vs 我方 61（scratchpad `audit/storefront/*.headseq.txt`）：`shopify-digital-wallet` meta、hreflang 序（x-default 首）、oembed link、`preloads.js`、`shopify-features` JSON、`Shopify.*` 全域（`modules`／`loadFeatures`／`SignInWithShop`／`featureAssets`／`MCP`）、`__st`、`captcha-bootstrap`、`load_feature`／`origin_trials`、`sections-script`／`snippets-script`（主題 `{% javascript %}` 編譯）、`shopify-cfh-end`、trekkie／monorail／perf-kit、`shopify-y`／`shopify-s` meta；我方多出的重複 canonical 與 JSON-LD 要拆 | T4 | ✅ E19a（#344 → main `38debcbe`；bt3 複驗 11 頁型 `__head__` 逐節點全同、端點＋headless 探針綠，見 `docs/dev/e19-content-for-header.md` §5）；E19b＝行為對位（同意 API／驗證碼／web pixels／分析收集端／`/api/mcp`）待做 |
| T12 | 主題資產 URL 本尊形 | `asset_url`／`asset_img_url`／字型 URL 出 `//host/cdn/shop/t/{id}/assets/x.css?v={stamp}`（今 `/theme-assets/x.css`，Normalizer 對映）；`compiled_assets` 已走本尊形（E19a） | E19a | ✅ #346 `c4babdd0`（七濾鏡本尊形＋供給端五路由＋compiled `?v=` 29 位形；bt3 複驗：89 資產 URL 本尊形、抽查 200、`__head__` 11 頁型仍全同——`docs/dev/t12-theme-asset-urls.md` §5） |
| T13 | 政策頁（`/policies/{kind}`）＋`shop.*_policy`／`policy` 物件＋content_for_header 空白骨架更正 | hoko 有 `pageType "policy"` 頁（只有設了內容的政策存在，其餘 404）、平台自產容器＋首節點 `policy-{hash}.css`；Kalles／Minimog 乾跑露出 `shop.shipping_policy` 缺口；E19a 節點級 48/48 但位元組級 15 處空白差 | T12 | ✅ #348 `a045a6e5`（bt3 複驗：政策頁 200／四個 404 同本尊、節點 42/42、位元組骨架 41/42；商品頁 48/48／47/48——`docs/dev/t13-policy-pages.md` §5）；admin Settings › Policies＝A1 |
| T11 | Storefront API（GraphQL 執行器） | 以 `/api/:version/graphql.json` 為入口的 Storefront API；E18 只實作 cartCreate 一支。**2026-09-05 改按需**：grep 三套主題 JS（Ella／Kalles／Minimog）皆不打 Storefront GraphQL（Ella 走 Ajax `/cart*.js`，其餘走 `routes`），唯一消費者＝我方 portable-wallets cartCreate ⇒ 只做平台 JS／app 實際消費到的操作 | T10 | 按需（暫不投入） |
| T14 | 主題引擎缺口批次（門市取貨／系列語境上下商品／圖片焦點／format_address） | Kalles 75 頁＋Minimog 57 頁全模板乾跑（scratchpad `t8/conformance_probe.rb`）列出的引擎級缺口：`variant.store_availabilities`＋`location`／`address`／`format_address`（三套主題的 pickup-availability 都用）、`collection.previous_product`／`next_product`（系列語境商品頁）、`image.presentation.focal_point`；另把主題誤用的非官方屬性顯式宣告 nil | T13 | 進行中（本包；bt3 複驗＝收尾 PR）；引擎級 miss 鍵 Kalles 11→3、Minimog 8→1，剩下的是已證實的主題怪癖 |
| T5 | 真店資料集擴充＋整頁再對表 | 多變體商品（option_values 切換、swatch）、有圖商品（image_url／media／photoswipe）、部落格與文章、頁面、系列排序值域 → 八頁以外的模板（product 替代模板、blog、article、list-collections、404、password、gift_card、customers/*）全部進 computed 三寬 | T3 | 待做 |
| T6 | 編輯器預覽 A′ 其餘頁面 | 使用者 DevTools 貼 `oseid` 預覽 URL（每頁）→ computed 三寬 | T5 | 待做（需使用者協助取 URL） |
| T7 | 主題編輯器 UI 逐面板對齊（D79 收口） | 側欄／設定面板／頂欄／裝置切換／版本歷史／`?`（六層＋三寬） | T6 | 待做 |
| T8 | Kalles 5.4.2 安裝與裝修 | fixture 已在倉：`test/fixtures/themes/kalles-5.4.2`（＋demo-data）→ conformance（零 Liquid error／零 miss）→ 缺口修 → 主題庫「新增主題 › 上傳 zip」流程對齊 → 可裝修 | T7 | 乾跑已做（T14）：75 頁零例外零 Liquid error、引擎級 miss 只剩主題怪癖；**剩下的是上傳 zip 流程與裝修面**（T7 之後） |
| T9 | Minimog 6.0.0 同上 | `minimog-6.0.0`（＋sample-data） | T8 | 乾跑已做（T14）：57 頁零例外、18 頁 `image_url` nil 錯與本尊同型；剩上傳與裝修面 |
| A0 | admin 逐頁稽核 | 對 admin.shopify.com 每頁（側欄全部項目）做鐵律 12 六層清單 vs 我方 `app/frontend/admin/pages/*`：缺頁／缺控件／值域差 → 登記成 A1… | — | 待做 |
| A1… | admin 逐頁對齊包 | 依 A0 清單，每頁一包（或一群小頁一包） | A0 | 待做 |
| P0 | 平台管理後台盤點 | 現有 platform admin（租戶／方案／網域／部署）功能清單與缺口 | — | 待做 |
| X1 | 結帳（checkout）對表 | 本尊 checkout 為 Shopify 託管（checkout.shopify.com）：以官方 checkout UI 為對標的自有結帳；範圍大，排在 T／A 之後 | T5 | 待做 |
| X2 | 舊 V 清單收尾 | 91 §3.79–§3.86 全部 V（每語言 sitemap 子表、子資料夾市場本尊形、文章圖片、Vimeo 縮圖…） | 隨包 | 待做 |

## 3. 每包固定產出（鐵律 15／19／20／21）

worklog（Done／Changes／閘門表／突變／Pending）＋ handoff（①②③④）＋ dev doc（12.4 四件事）＋ external-facts 取證節 ＋ 91 未取得／範圍外；
PR body 含閘門與突變表；合併＝`gh pr merge --squash --match-head-commit`；部署＝bt3 `scripts/deploy.sh` → 複驗 → docs-only 收尾。

## 4. 沿革

- 2026-09-05 v1（D84 同批）：T1 完成；T2 起。
- 2026-09-05 v3：新增 T14（主題引擎缺口批次，來自 Kalles／Minimog 乾跑）；T8／T9 的 conformance 一半已由 T14 完成。
- 2026-09-05 v2：T4／T10／T12 ✅；新增 T13（政策頁，來自 T8／T9 乾跑＋audit 快照）；T11 改按需（三套主題 JS 無 Storefront GraphQL 消費者）；T2／T3 等共享 Chrome（audit workflow）空出。
