# W6 下半與結帳線：殘項盤點＋接下來六步（2026-08-31）

> 問題（使用者）：目前這一步還有什麼沒完成；接下來六步做什麼；各步與其他
> 功能模塊／買家前台／商家後台／平台管理後台的對接關係與共用件。
> 現狀基準：main=9405ae8（#197 包30、#203 包33 前半、#200–#202 drops 補完、
> #204 資料出口、#205 cart 端點全數已併）。地基表現查（2026-08-31）：
> `checkouts`／`url_redirects`／`shop_locales`／`platform_locales` 已存在；
> `markets`／`web_presences`／`domains` **不存在**。

## §0 本步（前台引擎＋cart 線）殘項清單

### A. cart 包（#205）殘項
| # | 項 | 歸屬步 |
|---|---|---|
| A1 | 🔴 vehicle：端點掛 staff 預覽 session；**host→shop 解析**與買家匿名 session 未建 | 步 2 |
| A2 | `cart_item_limit`（商家可調總件數上限）設定面未接（兩層防呆上限已接） | 步 2（設定欄）＋商家後台設定頁隨結帳偏好包 |
| A3 | 90 天未動 purge job（掃描鍵已建） | 步 2（附帶）或排程包 |
| A4 | item json＝live 30+ 欄核心子集（折扣／selling plan 欄恆空形） | 隨折扣線／訂閱線 |
| A5 | Q-44：bundle parent_id 入合併鍵＝暫定（待本尊量測） | 探針補量（獨立小項） |
| A6 | 壞 id／超量的本尊錯誤形＝未取得（cart 端點有 bot 牆，429 挑戰頁） | 慢速探針補量 |

### B. drops／json 殘項
| # | 項 | 歸屬步 |
|---|---|---|
| B1 | 帶圖商品的 image-json 形未量（測試品無圖；ImageDrop json＝ours 欄位） | 探針補量（給 S9-CAP 上傳一張圖即可） |
| B2 | `category.name` 需 taxonomy 名稱字典（**平台字典表**候選，鐵律 2 ③ 程序） | 獨立小遷移（平台管理後台維護面） |
| B3 | `weight_unit` 恆 "kg"（商家單位偏好未存） | 隨商店設定包 |
| B4 | collection json 的 `published_scope`／`template_suffix`＝常數（語義對表待） | 步 4／系列前台包 |
| B5 | metafield 進階型別（list／參照／rich_text 的 value 物件化、`metafield_tag` 濾鏡） | 隨 metafields 功能線 |

### C. Section Rendering／渲染殘項
| # | 項 | 歸屬步 |
|---|---|---|
| C1 | 動態實例 id（本尊 `template--N__key` 形）——需編輯器寫入面的 DB 實例化 | 編輯器包（spec 14） |
| C2 | template 鍵不可跨頁全域定址（本尊可）＝id 方案差異已登記 | 同 C1 |

### D. 其他線殘項（非本步射程，照登）
91 §3.45 W-1..W-5（webhook 訂閱面）、§3.44（Suspended／複製繼承）、
theme editor 寫入面、Horizon 4.1.4 升級鈕值域。

## §1 接下來六步（依 20-37 包 W6 序＋結帳線起步）

### 步 1｜包 32：markets／web_presences／domains 四表＋url_prefix（L 級）
**做什麼**：`markets`／`web_presences`／`market_web_presence_locales`／
`domains` 四表；`url_prefix(web_presence, locale)` 唯一產生器；
`hreflang_codes` 型別分離；i18n limits 鍵補齊；67 §J 不實宣稱修正。
🔴 前綴形態一次到位 `/{lang}-{region}/`（B11；67 §I L7 禁拆兩期）。
**對接**：
- **買家前台**：所有 URL 的前綴來源（drops 已有 `url_prefix:` 參數佈線
  ——包30 起 ProductDrop/CollectionDrop/RoutesDrop 全收，**零改口**接上）。
- **商家後台**：Markets 導航區（本尊 Markets nav）——v1 資料層＋預設市場
  seed，CRUD UI 隨後；域名綁定頁。
- **平台管理後台**：域名驗證／SSL 簽發 ops（bt3 nginx 配套）。
- **共用件**：`sales_catalogs`（S10 的 catalog `context.marketIds`——§9.5b
  量測形）；`shop_locales`（ML 線）＝market×locale 的語言集來源；
  `orders.locale_snapshot` 欄（回補3 已建）等本步的市場快照對偶。

### 步 2｜包 33 後半：公開店面 hosting＋五步渲染管線＋頁級快取（XL 級）
**做什麼**：host→shop 解析（查 `domains`）；帶前綴路由查表；14 §F1 五步
渲染管線；頁級快取 key＝`[shop_id, theme.version, template.updated_at,
locale, market_id, currency, page_kind, resource_stamp]`（§1.5——stamp 生產
者已全就位：4 支 stamp 欄＋`CacheStampBumper` 自包30 啟用）；🔴 key-based
expiry 唯一許可（63 §D.4 禁 delete）；robots.txt 暫 `Disallow: /`（B13，
步 4 才開放）；根路徑 302（B12）；**cart vehicle 收口（A1）**：CartController
的 shop 解析器由 staff session 換 host 解析、`_cl_buyer` 落真店網域
（host-only 語義此刻才真實）；A2 設定欄／A3 purge job 附帶。
**對接**：
- **買家前台**：整個站的存在本身；Ella 全頁上線。
- **商家後台**：預覽面保留（noindex）；`themePublish` 已有；「View store」
  對位＝我方 admin 直開真店。
- **平台管理後台**：域名→租戶映射、Rack::Attack 限流政策（本尊 cart 端點
  有 bot 牆 429——83 §12.5 量測；我方公開端點同樣要有）。
- **共用件**：`Storefront::Lookup`（S9 可見性判準——渲染與 sitemap 與
  cart 售罄閘三處同軸）；#203 fragment 端點與 #205 cart 端點**平移**到
  公開 controller（契約零變）；快取失效鏈（PR-C 排程到點 bump stamp ⇒
  頁快取自然過期——排程發布與快取在這裡合流）。

### 步 3｜包 34：i18n 前台（三層字串＋內容翻譯＋切換器）（L 級）
**做什麼**：三層字串解析（主題 locales JSONC → 商家覆寫 → 平台字串集
B14＝`config/storefront_locales/*.yml` 平台字典）；內容翻譯走 drops
（接**回補7 `Translations::Resolve`**——已在 main）；routes drop 帶前綴；
LocalizationDrop 真值（`shop_locales`）；雙控件切換器（country＋language
表單 → `/localization` 端點，cart 家族同款 controller 形）；Vary 紀律。
驗收＝SF-1～SF-11 全集（B15；🔴 SF-10 金額字串逐位元組不變）。
**對接**：
- **商家後台**：翻譯後台（GraphQL＋CSV 通道）既有——同一份 `translations`
  資料本步開始被前台消費（雙真相風險在回補7 已預防）。
- **買家前台**：多語言路由＋譯文渲染＋金額格式（jurisdiction pack
  `currency_format`——鐵律 3/10 的顯示層）。
- **平台管理後台**：平台字串集（B14 平台字典表）的維護面；OpenCC 繁簡
  稽核（D49 線）複用。
- **共用件**：`Translations::Resolve`（後台預覽與前台渲染同一支——
  回補7 檔頭裁定）；`platform_locales` 字典；`/localization` 端點與
  cart 端點共用 buyer-session／限流基建。

### 步 4｜包 35：SEO 面（hreflang／canonical／sitemap／JSON-LD／llms.txt）（M 級）
**做什麼**：hreflang 全集互指（吃步 1 的 web_presences）；canonical；
多語言 sitemap（鏡射 83 §3.6 量測形：sitemapindex＋products_N 子表；
🔴 **UNLISTED 排除**＝93 §C discoverable 判準直接複用，`limits`
`unlisted_meta_robots`／`unlisted_excluded_from` 落地為生產行為）；
robots 開放（撤 B13 的 Disallow）；商品 JSON-LD（🔴 三處價格同源——
SSR 首屏／`?section_id=` 片段／JSON-LD 同一 `Pricing::PresentmentResolver`
＋同一 formatter，一支測試斷言字串完全相同，63 §D.6；JSON-LD 金額＝
十進位數字——83 §3.1 三態量測的第三態）；llms.txt。
**對接**：
- **買家前台**：head 標籤群＋sitemap 端點。
- **商家後台**：SEO 欄位（products/collections 的 seo_title/seo_description
  既有欄）在此開始輸出；Search engine listing 卡對位。
- **平台管理後台**：爬蟲政策／robots 開關。
- **共用件**：`Product.discoverable`（S9）＝sitemap 成員判準；B4 殘項
  （published_scope 語義）在 sitemap/canonical 對表時一併收；
  步 2 的頁快取 key 涵蓋 SEO 片段。

### 步 5｜包 36：301 引擎＋後台重導管理（S 級）
**做什麼**：`url_redirects` 查表引擎（🔴 掛在 404/410 handler **之前**、
前綴保留——查表用無前綴正規路徑，表註釋既定）；後台自訂重導 CRUD
（GraphQL `urlRedirect*` 對位＋admin 頁）。
**對接**：
- **買家前台**：404 路徑的救援層。
- **商家後台**：重導管理頁（Online Store 導航區）；handle 變更生產者
  （回補6 已寫入 redirect 列——**生產者早已在跑**，本步補消費引擎，
  這正是「先建表後建引擎」當時裁定的兌現）。
- **共用件**：步 1 前綴（重導匹配在剝前綴後）；sitemap（步 4）不得收錄
  redirect 來源路徑；`Catalog::SaveProduct` 的 handle 鏈。

### 步 6｜結帳線第一包：Checkout::Calculator＋cart→checkout 快照（L 級）
**做什麼**：15 F2 純函式金額引擎（輸入 line items 快照／地址／折扣碼／
運送選擇 → 不可變 Result 全 integer cents；🔴 最大餘數法分攤＋
`Σ行分攤＝折扣總額` 恆等；含稅反推行級進位策略；表格驅動 ≥40 組＋
property test 三不變量）；cart→checkout 建立（`checkouts` 表**已建**：
line_items_snapshot／雙幣別欄／recovery_token 全就位；進入時重新快照價格
——F1 #3；token URL `/checkouts/<token>`；🔴 不扣庫存——訂單成立才扣）。
**對接**：
- **買家前台**：checkout 入口（one-page checkout UI 隨後續包）。
- **商家後台**：🔴 **Calculator 四處重用**＝checkout 預覽／訂單成立／
  draft order／退款計算（15 F2 #2）——商家後台的訂單線與退款線直接
  吃同一個 Result，杜絕兩處總計不一致；棄單列表吃 `abandoned_at`。
- **平台管理後台**：無直接面；PSP pack（65 契約 `amount_format` 雙格式）
  在後續金流包接上時吃 Calculator 輸出。
- **共用件**：`Money::Storage`／65 型別鏈（鐵律 3）；outbox 事件
  （checkout.created 等）；冪等（訂單成立 `idempotencyKey`——鐵律 5）；
  jurisdiction pack 稅務事件（HK 基準）。

## §2 跨六步的共用基建總表

| 共用件 | 現狀 | 消費步 |
|---|---|---|
| `Storefront::Lookup`＋purchasable/discoverable（S9/93） | ✅ main | 2（渲染）、4（sitemap）、cart 售罄閘（已用） |
| 4 支 cache stamp 欄＋`CacheStampBumper`＋PR-C 到點 bump | ✅ main | 2（頁快取 key）、4（sitemap 快取） |
| `Translations::Resolve`＋`shop_locales`＋`platform_locales` | ✅ main | 3（主消費）、4（hreflang 語言集） |
| drops 的 `url_prefix:` 佈線 | ✅ 包30 起全收 | 1→2→3 零改口接上 |
| #203 fragment 端點＋#205 cart 端點 | ✅ main（staff vehicle） | 2 平移公開面（契約零變） |
| `url_redirects`＋handle 變更生產者（回補6） | ✅ main | 5（消費引擎） |
| `checkouts` 表（M0 既建） | ✅ 空置 | 6（第一個消費者） |
| Rack::Attack 限流 | ✅ 存在 | 2（公開端點政策；對標本尊 cart bot 牆） |
| 平台字典表程序（鐵律 2 ③） | `platform_locales` 前例 | 3（B14 字串集）、B2（taxonomy 名稱） |

## §3 建議的探針小隊（不佔步序，隨手插隊）
B1 帶圖 image-json（給 S9-CAP 傳一張圖＋S9-Probe 重量）；A5 Q-44（本尊
bundle 車行合併）；A6 慢速 cart 錯誤形（避 429）；B4 published_scope
（多管道 collection json 對比）。
