<% content_for :title, "商品 — #{Rails.configuration.x.brand.name}" %>

<div
  id="admin-root"
  data-brand-name="<%= Rails.configuration.x.brand.name %>"
></div>

<noscript>需要啟用 JavaScript 才能使用商家後台。</noscript>
